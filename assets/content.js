window.BOOK_DATA = {"title":"魔搭紫皮书","sourceUpdated":"2026-09-20","sourceUrl":"https://github.com/modelscope/ms-cookbook/tree/main/content","chapterCount":35,"readyCount":34,"imageCount":377,"attachmentCount":12,"parts":[{"id":"part-01","title":"第一篇　认识开源模型","shortTitle":"认识开源模型","chapters":["chapter-1","chapter-2","chapter-3","chapter-4"]},{"id":"part-02","title":"第二篇　从问题出发：找到适合场景的开源模型","shortTitle":"从问题出发","chapters":["chapter-5","chapter-6"]},{"id":"part-03","title":"第三篇　跑得起：让第一个开源模型工作起来","shortTitle":"让模型工作起来","chapters":["chapter-7","chapter-8","chapter-9","chapter-10","chapter-11"]},{"id":"part-04","title":"第四篇　调得好：把通用模型变成场景模型","shortTitle":"微调与评测","chapters":["chapter-12","chapter-13","chapter-14","chapter-15"]},{"id":"part-05","title":"第五篇　场景篇：从模型走向完整业务系统","shortTitle":"业务系统实践","chapters":["chapter-16","chapter-17","chapter-18","chapter-19","chapter-20"]},{"id":"part-06","title":"第六篇　AIGC 特別篇","shortTitle":"AIGC 特别篇","chapters":["chapter-21","chapter-22","chapter-23","chapter-24","chapter-25"]},{"id":"part-07","title":"第七篇　Agent 特别篇","shortTitle":"Agent 特别篇","chapters":["chapter-26","chapter-27","chapter-28","chapter-29","chapter-30","chapter-31","chapter-32","chapter-33"]},{"id":"part-08","title":"第八篇 补充内容","shortTitle":"基础知识补充","chapters":["chapter-34","chapter-35"]}],"chapters":[{"id":"chapter-1","number":1,"title":"开源模型正在改变什么","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-01.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-01.html","minutes":6,"status":"ready","headings":[{"id":"c1-s1","text":"记住，免费的不一定是开源的，开源的也不一定是免费的？","level":"h2"},{"id":"c1-s2","text":"同样叫开源，但是开放的东西是不一样的～","level":"h2"},{"id":"c1-s3","text":"开源模型，真的能媲美闭源模型吗？","level":"h2"},{"id":"c1-s4","text":"什么场景更适合本地部署开源模型呢？","level":"h2"},{"id":"c1-s5","text":"记住模型能下载，不代表可以随便用","level":"h2"}],"html":"<p>你可能想象不到，Nvidia CEO 黄仁勋在 X 上发出的第一条帖子，<b>竟然献给了开源。</b></p>\n\n<img src=\"assets/manuscript-20260914/c01-33167eb9f1e9e3.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>2026 年 7 月 24 日，他分享了一封英伟达参与签署的公开信，谈的是开放模型为什么重要，</p>\n\n<p>这个世界既需要前沿的闭源模型，也需要前沿的开放模型。</p>\n\n<p></p>\n\n<p>可能很多人已经习惯了打开一个聊天窗口，把问题发过去，等 AI 回答。但当你准备用 AI 长期处理公司资料、接入内部系统时，可能就会出现以下问题：资料发过去安全吗？服务突然限流怎么办？能不能让模型学会公司数据的处理方式？</p>\n\n<p><b>开源模型可以让这些问题多一种解法。你可以在许可证允许的范围内，把模型下载下来，部署在自己的环境里，继续微调它，让用户自己决定如何使用。</b></p>\n\n<p></p>\n\n<p>当然，大模型的开源闭源之争，一直也是讨论不休，老黄的这一封信，也再次引起了我们的共鸣，决定联合几个共建者，开始这个开源模型应用实战紫皮书之旅。<b>这次把开源模型应用实战内容带给大家，愿开源模型繁荣昌盛～</b></p>\n\n<p></p>\n\n<h2 id=\"c1-s1\">记住，免费的不一定是开源的，开源的也不一定是免费的？</h2>\n\n<p>在网页上和 AI 聊天，或者通过 API 向模型提问，通常都由模型厂商运行模型，你提交输入，对方返回结果，服务器怎么配置、模型怎么维护，大多不用你操心。</p>\n\n<p></p>\n\n<p>开源模型提供了另一种选择，允许你获得训练完成后的模型参数，也就是通常所说的<b>权重</b>。配合适当的软件和硬件，这些参数可以在你的电脑、服务器或私有云里运行。原本需要发给外部服务的问题，也就可以在自己的环境里处理。</p>\n\n<p></p>\n\n<p>这里有两件容易混淆的事。</p>\n\n<p><b>免费，说的是要不要付钱；开源，说的是你能拿到什么，以及被允许怎样使用。</b> 一个聊天产品可以免费，却不提供模型权重。一个允许免费下载的模型，运行起来仍然需要算力和维护成本。</p>\n\n<p>API 也只是访问模型的方式。开源模型可以由服务商托管，通过 API 提供给你；你自己部署的模型，同样可以通过内部 API 供同事使用。因此，“开源还是闭源”和“调用 API 还是自己部署”，要分开看。</p>\n\n<p>日常讨论里，大家经常把能够下载权重的模型统称为开源模型。以后的章节我们也会沿用这个常见说法。</p>\n\n<p>不过，会在严格区分时讲清楚，<b>开放权重不等于开源了模型的全部训练过程</b>。训练数据、训练代码和使用权限，还要分别查看。</p>\n\n<img src=\"assets/manuscript-20260914/c01-0ebc5006d0751a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h2 id=\"c1-s2\">同样叫开源，但是开放的东西是不一样的～</h2>\n\n<p>拿到一个模型后，你大概率会好奇，究竟是怎么学出来的？</p>\n\n<p>这时，不同模型之间的区别就显出来了，有的能让你下载运行，却没有公开完整训练材料；有的进一步提供训练代码和数据说明，让外部开发者能够研究模型的训练轨迹。</p>\n\n<p></p>\n\n<p>2026 年 5 月，G7 发布了一份关于 AI 开放程度的共同参考文件，把这些差异分成四类，按开放程度从高到低，可以这样理解。，</p>\n\n<ul><li><b>连完整训练数据也公开。</b> 模型权重、部署代码、训练代码和完整训练数据，都按开源许可证提供，对应 Open Source AI with Open Data。</li><li><b>公开权重和代码，受限数据有详细交代。</b> 对应 Open Source AI。完整训练数据原则上应提供；因法律或技术原因无法分享的部分，需要用充分的数据说明替代。</li><li><b>把训练好的模型交给你运行和改造。</b> 对应 Open Weights AI，提供权重与部署代码，并采用开源许可证，但未必公开完整训练材料。</li><li><b>能下载，使用时还有额外条件。</b> 对应 Weights Available AI，权重和部署代码可以获得，许可证却可能限制商业用途、使用地区或特定场景。</li></ul>\n\n<img src=\"assets/manuscript-20260914/c01-322128af6f01f2.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>以 Qwen3 为例，官方提供了开放权重模型，以及本地运行、部署、量化和微调的相关说明。开发者可以使用 Transformers、vLLM、SGLang 等工具运行模型，也可以借助微调工具继续训练。按上面的区分，体现的是开放权重这一层的实用价值。</p>\n\n<img src=\"assets/manuscript-20260914/c01-4d4d57710fbf68.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>多数企业并不需要重做一次大模型预训练—能够运行现成模型、适配自己的设备，再用业务样本微调，已经可以解决很多业务上的具体问题。</p>\n\n<p></p>\n\n<h2 id=\"c1-s3\">开源模型，真的能媲美闭源模型吗？</h2>\n\n<p>能自己部署模型当然好，但如果模型在业务场景中经常出错，我们也很难把真实的工作交给它。</p>\n\n<p>开源模型过去给不少人的印象，是方便研究，却比最强的闭源模型差一截。随着模型能力的不断更新，这个判断需要重新看了。</p>\n\n<p></p>\n\n<p>2026 年 6 月 18 日，X 上有一场颇有意思的互动。网友讨论中国模型什么时候能达到 Anthropic 前沿模型的水平，马斯克给出了可能要到 2027 年第一季度的判断。智谱联合创始人唐杰在下面回了一句“won’t take that long”，不会等那么久。</p>\n\n<img src=\"assets/manuscript-20260914/c01-1d88cb630a1969.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>两人的判断能否兑现，还要看后续模型的表现。这场讨论吸引人的地方，开源模型已经站在了整个大模型的前沿。</p>\n\n<p></p>\n\n<p>Hugging Face 在 2026 年 7 月披露的一次安全事件，再次证明了开源模型的重要性。</p>\n\n<p>HF 的部分生产基础设施遭到入侵，安全团队需要分析大量攻击记录，弄清攻击者做过什么、碰过哪些凭证、影响到了哪里。HF 团队先尝试通过商业 API 使用前沿模型，却遇到了一个尴尬的问题。用于调查的真实攻击命令和漏洞利用内容，触发了服务商的安全限制，分析请求被拦住了。</p>\n\n<p>随后，团队在自己的基础设施上使用开放权重模型 GLM-5.2 开展取证分析，让调查得以继续。相关攻击数据和凭证也留在内部环境中。</p>\n\n<img src=\"assets/manuscript-20260914/c01-065d3d9ab0de58.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>在后续的技术复盘中，Hugging Face 进一步说明，团队重建了约 17,600 次攻击动作，并借助包括 GLM-5.2 在内的开放权重模型，解读经过混淆和加密处理的攻击载荷</p>\n\n<p>这件事体现了开源模型的重要性。</p>\n\n<p><b>开源模型</b><b>的使用</b><b>不需要受到闭源的限制</b><b>。</b><b>我们可以把数据留在本地，也可以在断网的时候使用，让任务</b><b>执行</b><b>变得更可控。</b></p>\n\n<p></p>\n\n<h2 id=\"c1-s4\">什么场景更适合本地部署开源模型呢？</h2>\n\n<p></p>\n\n<p><b>垂直领域也是开源模型比较重要的应用方向。</b>通用大模型学习了大量公开数据，但是对于企业内部设备名称、业务流程、行业术语和专业知识的理解可能仍然有限。通过对垂直领域数据的微调，可以使模型进一步学习这些专业知识。</p>\n\n<p><b>对于需要处理敏感数据的任务，本地部署同样具有优势。</b>模型可以在企业内部网络完成推理，并与企业已有的知识库、数据库和业务系统连接，从而减少敏感数据发送到外部服务的需求。</p>\n\n<p>然而，并不是所有的任务都需要使用本地部署的开源模型，</p>\n\n<p>对于模型调用次数较少、业务变化较快或者需要持续使用最新通用模型能力的任务，直接调用 API 通常更加方便。而对于一些长期稳定、重复执行的业务任务，开源模型具有较大的应用空间。</p>\n\n<p>例如，文本分类、意图识别、信息抽取、文档摘要、知识库问答等任务，通常具有比较明确的输入和输出形式。企业可以根据任务复杂程度选择合适规模的模型，并使用自己的业务数据进行训练或者微调。对于这类任务来说，并不一定需要使用参数规模最大的模型。</p>\n\n<p></p>\n\n<h2 id=\"c1-s5\">记住模型能下载，不代表可以随便用</h2>\n\n<p>模型跑起来了，效果也不错，但想在商业产品使用，或者把自己微调后的版本分享出去，还需要确认一下 LICENSE，也就是许可证。</p>\n\n<p>可以下载、可以商用、可以修改后再发布，是不同的许可。</p>\n\n<p></p>\n\n<p>一般来说，MIT 和 Apache License 2.0 都是较宽松的开源许可证，允许商业使用和修改，也不要求所有衍生代码都公开。使用 MIT 许可的软件，仅需要保留版权及许可声明；Apache 2.0 则规定了再分发时的修改说明、相关声明保留和 NOTICE 处理等要求，并包含明确的专利授权条款。</p>\n\n<p>比如，<b>Qwen3 系列模型采用 Apache 2.0，</b></p>\n\n<img src=\"assets/manuscript-20260914/c01-d899fb5015f90f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p><b>Kimi K3 对部分商业使用另有约定，</b>允许部署、微调和创建衍生作品，但模型服务业务达到规定营收门槛时，需要另行签订协议。</p>\n\n<img src=\"assets/manuscript-20260914/c01-da054efcf6e1d6.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p><b>DeepSeek-V3</b> 项目的代码则使用 MIT 许可证，</p>\n\n<img src=\"assets/manuscript-20260914/c01-41be1610632b0c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>因此，我们在使用模型中，如果要想要把微调后的模型公开发布或者提供给其他用户使用，就需要重新检查原模型的许可证，看是否允许这样使用，以及是否需要保留模型名称、许可证和来源信息。</p>\n\n<p>如果一个新模型是由多个模型合并得到的，还需要分别查看这些模型的许可证，确保它们的要求不会发生冲突。</p>\n\n<p></p>\n","imageCount":9,"attachmentCount":0,"discussionId":"chapter-1"},{"id":"chapter-2","number":2,"title":"开源模型还没用上，先卡在下载这一步？","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-02.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-02.html","minutes":4,"status":"ready","headings":[{"id":"c2-s1","text":"教你如何在魔搭上找到你想要的开源模型","level":"h2"},{"id":"c2-s2","text":"开源模型的 Model Card，值得多看两眼","level":"h2"},{"id":"c2-s3","text":"下载模型方式有很多，选一个喜欢的就行","level":"h2"},{"id":"c2-s4","text":"只想在网页上点几下","level":"h3"},{"id":"c2-s5","text":"在终端里，一条命令下载","level":"h3"},{"id":"c2-s6","text":"写进 Python 代码里，就用 SDK","level":"h3"},{"id":"c2-s7","text":"平时习惯用 Git，也可以直接拉取","level":"h3"}],"html":"<p>早在 23 年，我就写过一篇文章《大模型下载使我痛苦》，</p>\n\n<img src=\"assets/manuscript-20260914/c02-df1f804f3c3e14.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>那时 HF 需要科学上网才能使用，模型少说十几 G，流量有限，可能有一些镜像网站，但不一定全，也不一定稳定。</p>\n\n<p></p>\n\n<p><b>现在，如果问在国内下载开源模型先去哪里，那毋庸置疑是魔搭。</b>模型下载快，同时基本上所有国内外的开源模型都会第一时间上架，已然成为国内第一大开源模型社区（当然不止模型）。</p>\n\n<p></p>\n\n<h2 id=\"c2-s1\">教你如何在魔搭上找到你想要的开源模型</h2>\n\n<p>模型库是魔搭的基础功能模块，平台按任务类型（如文本生成、图像分类、语音识别等）对模型进行分类组织，支持关键词检索与多维度筛选。</p>\n\n<img src=\"assets/manuscript-20260914/c02-bef2e47d883a7f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>已经知道模型名字，可以直接搜索。</p>\n\n<img src=\"assets/manuscript-20260914/c02-754309025c1357.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>还没确定用哪个，就先想清楚要让模型做什么。</p>\n\n<p>比如，想把录音转成文字，就从可以语音识别开始找，再结合语言、参数规模、运行框架和许可证，逐步缩小范围，</p>\n\n<p>具体想找到适合你业务的模型，可以看【把业务问题翻译成模型任务】章节内容。</p>\n\n<p></p>\n\n<p></p>\n\n<h2 id=\"c2-s2\">开源模型的 Model Card，值得多看两眼</h2>\n\n<p>找到感兴趣的模型后，点进去看它的模型卡片。以 <a href=\"https://modelscope.cn/models/Qwen/Qwen3-ASR-1.7B\" target=\"_blank\" rel=\"noopener noreferrer\">Qwen3-ASR-1.7B</a> 为例，模型页面会把介绍、使用方法和效果评估放在一起。</p>\n\n<p></p>\n\n<p>先看这是什么模型，能做什么。模型名称、基本介绍和架构说明，可以帮我们确认有没有找错方向。</p>\n\n<img src=\"assets/manuscript-20260914/c02-92f1d898fabc93.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>再看适用范围，支持哪些语言、适合什么输入、有哪些限制，都和自己的任务有关。</p>\n\n<img src=\"assets/manuscript-20260914/c02-b953f3979b2616.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>接着看使用方法，需要什么运行环境，依赖哪些工具，输入应该怎样准备，通常都能在示例中找到。下载之前读一下，可以避免文件下载完了，才发现环境部署不了。</p>\n\n<img src=\"assets/manuscript-20260914/c02-9c68fc359adb01.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>最后看评估结果，这个模型在哪些数据上测过，比较的是什么指标，可以作为选型参考。当然具体效果怎么样，还是要拿自己真实数据试一试才知道，榜单只能作为一个参考。</p>\n\n<img src=\"assets/manuscript-20260914/c02-a53b3b46d7e35d.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>模型页面还提供文件清单、版本和许可证信息，上一章提过，能使用不等于能商用，</p>\n\n<p><b>准备商用或者继续微调时，这一步千万别跳过。</b></p>\n\n<img src=\"assets/manuscript-20260914/c02-8b4388b364a866.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>部分模型还提供在线体验入口，遇到可以直接试用的模型，也能帮助判断这个模型是否值得下载。</p>\n\n<p></p>\n\n<h2 id=\"c2-s3\">下载模型方式有很多，选一个喜欢的就行</h2>\n\n<p>魔搭提供网页、命令行、Git 和 Python SDK 等下载方式，下面继续用较小的<code>Qwen/Qwen3-0.6B</code>演示，方便第一次操作。</p>\n\n<h3 id=\"c2-s4\">只想在网页上点几下</h3>\n\n<p>进入模型的文件页面，找到需要的文件，点击下载按钮即可。网页方式适合查看文件和下载少量内容。</p>\n\n<p>通过模型页面提供的下载按钮获取文件，示例：</p>\n\n<img src=\"assets/manuscript-20260914/c02-7c3ecb6c57038f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c2-s5\">在终端里，一条命令下载</h3>\n\n<p>先安装最新版的 ModelScope Python 包，</p>\n\n<pre data-language=\"Bash\"><code>pip install -U modelscope</code></pre>\n\n<p>只下载一个文件，可以把文件名写在模型名称后面，下面这条命令只下载说明文档，适合先检查下载是否正常。</p>\n\n<pre data-language=\"Bash\"><code>modelscope download --model Qwen/Qwen3-0.6B README.md --local_dir ./Qwen3-0.6B</code></pre>\n\n<p>如果要下载整个模型，去掉文件名即可，</p>\n\n<pre data-language=\"Bash\"><code>modelscope download --model Qwen/Qwen3-0.6B --local_dir ./Qwen3-0.6B</code></pre>\n\n<p><code>--model</code>后面是模型在魔搭上的名称，<code>--local_dir</code>后面是本地保存目录，换一个模型时，把这两个位置改成自己需要的值。</p>\n\n<p></p>\n\n<p>上面的命令在终端中运行，如果放进 Notebook 的 Python 代码单元格，安装时可以使用<code>%pip install -U modelscope</code>，执行命令时在<code>modelscope</code>前加一个<code>!</code>。</p>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c02-6cc41770227681.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c2-s6\">写进 Python 代码里，就用 SDK</h3>\n\n<p>在 Python 程序中，可以直接调用<code>snapshot_download</code>下载模型，比如，</p>\n\n<pre data-language=\"Python\"><code>from modelscope import snapshot_download\n\nmodel_dir = snapshot_download(\n    'Qwen/Qwen3-0.6B',\n    local_dir='./Qwen3-0.6B',\n)\nprint(model_dir)</code></pre>\n\n<p>函数返回模型保存的位置，后续代码可以用这个路径加载模型。没有指定<code>local_dir</code>时，SDK 会使用默认缓存目录。</p>\n\n<img src=\"assets/manuscript-20260914/c02-70ccc6e14bbd3c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c2-s7\">平时习惯用 Git，也可以直接拉取</h3>\n\n<p>模型文件通常较大，使用 Git 下载前，需要先安装 Git 和 Git LFS。安装好 Git LFS 后，再执行下面的初始化和克隆命令。</p>\n\n<pre data-language=\"Bash\"><code>git lfs install\ngit clone https://www.modelscope.cn/Qwen/Qwen3-0.6B.git</code></pre>\n\n<p><code>git lfs install</code>是在当前环境中初始化 Git LFS，并不负责安装这个工具本身。如果提示没有<code>lfs</code>命令，需要先完成工具安装。</p>\n\n<img src=\"assets/manuscript-20260914/c02-973b14a709cdc5.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>四种方式不用全部试一遍，临时拿几个文件就用网页，终端操作用命令行，Python 项目用 SDK，已有 Git 工作习惯就继续用 Git。</p>\n\n<p>模型下载完成后，打开它的使用示例，把模型路径换成本地目录，先跑通一次输入和输出。</p>\n\n<p>真正需要花时间的模型实验，就可以从这里开始了。</p>\n\n<p></p>\n","imageCount":12,"attachmentCount":0,"discussionId":"chapter-2"},{"id":"chapter-3","number":3,"title":"数据，是你要做开源模型微调的基石","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-03.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-03.html","minutes":4,"status":"ready","headings":[{"id":"c3-s1","text":"找数据，也要先知道自己准备做什么","level":"h2"},{"id":"c3-s2","text":"下载之前，先翻几条数据看看","level":"h2"},{"id":"c3-s3","text":"先把数据读进来，再决定用哪一部分","level":"h2"},{"id":"c3-s4","text":"先加载一份完整的数据","level":"h3"},{"id":"c3-s5","text":"只需要其中一个子集，就把名字写清楚","level":"h3"},{"id":"c3-s6","text":"训练集和测试集，也可以分开取","level":"h3"},{"id":"c3-s7","text":"想复现实验，记得把版本一起记下来","level":"h3"},{"id":"c3-s8","text":"数据下载好了，先别急着全交给模型","level":"h2"}],"html":"<p>你是否遇到过这种情况，一个模型可以在写代码、做数学题表现的十分优秀，但你问一个日常的问题，却给你瞎回答。</p>\n\n<p></p>\n\n<p>我们都知道模型在训练的过程中，想要那个部分的能力提高，就针对性的找那类的数据，</p>\n\n<p>但很多数据，你直接互联网检索，很难找到，有一个数据的统一下载入口，对于模型训练者来说，会节省很大的精力。</p>\n\n<p>比如，我们当时开源的中文基于满血 DeepSeek-R1 蒸馏数据集，其中很多原始数据都是从魔搭上直接下载的，</p>\n\n<img src=\"assets/manuscript-20260914/c03-4de63a06f1d781.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>我们模型的微调，可以从一份现成的开源数据集开始，这样可以快速跑通整个流程。</p>\n\n<p></p>\n\n<h2 id=\"c3-s1\">找数据，也要先知道自己准备做什么</h2>\n\n<p>魔搭整合了超过 4 万个开源数据集，平台支持数据集搜索和预览，提供字段说明及数据集版本。</p>\n\n<p>魔搭支持根据关键词标签及任务类别搜索数据集，如，检索语言中文的二分类文本分类数据，</p>\n\n<img src=\"assets/manuscript-20260914/c03-d1a6f009d63cac.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h2 id=\"c3-s2\">下载之前，先翻几条数据看看</h2>\n\n<p>筛选出数据集后，选择感兴趣的数据集，点击“数据预览”可以在线查看样本内容与字段说明，辅助数据选型决策，如 <a href=\"https://modelscope.cn/datasets/simpleai/HC3-Chinese\" target=\"_blank\" rel=\"noopener noreferrer\">simpleai/HC3-Chinese</a>。</p>\n\n<img src=\"assets/manuscript-20260914/c03-5e43d34eec8f6c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>问题放在哪一列，答案放在哪一列，有没有标签，一条数据里是一段文字还是一组对话，这些都可以先看清楚。后面写处理代码时，就不用对着字段名猜了。</p>\n\n<p>以问答数据为例，人类回答和模型回答可能分别保存在不同字段里，准备拿它做什么任务，就要决定读取哪些字段、怎样整理，不能只看到问答两个字就直接开始训练。</p>\n\n<p></p>\n\n<p>再到数据集文件里看看文件和版本，同一份数据集会更新，训练样本、字段或者划分也可能变化。做实验时记下用的是哪个版本，后面才容易复现。</p>\n\n<img src=\"assets/manuscript-20260914/c03-c33f6d404ef93d.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>数据集的说明和许可证也值得一起看。能下载的数据，用于研究、训练或者商业产品时，可能有不同要求。</p>\n\n<p></p>\n\n<h2 id=\"c3-s3\">先把数据读进来，再决定用哪一部分</h2>\n\n<p>魔搭提供了<code>MsDataset.load</code>接口，可以在 Python 里加载数据集。安装 ModelScope 后，就可以按数据集页面的使用说明操作。</p>\n\n<p></p>\n\n<h3 id=\"c3-s4\">先加载一份完整的数据</h3>\n\n<p>以 <a href=\"https://modelscope.cn/datasets/DAMO_NLP/jd\" target=\"_blank\" rel=\"noopener noreferrer\">DAMO_NLP/jd</a> 为例，可以这样读取默认配置下的数据，</p>\n\n<pre data-language=\"Python\"><code>from modelscope.msdatasets import MsDataset\n\nds = MsDataset.load(\n    'DAMO_NLP/jd',\n    trust_remote_code=True,\n)\nprint(ds)</code></pre>\n\n<img src=\"assets/manuscript-20260914/c03-be7695c08c686f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h3 id=\"c3-s5\">只需要其中一个子集，就把名字写清楚</h3>\n\n<p>有些数据集会把不同来源、领域或用途的数据分开保存。如果只关心其中一部分，就用<code>subset_name</code>指定子集。</p>\n\n<p></p>\n\n<p>例如，读取 HC3-Chinese 里的<code>baike</code>子集。</p>\n\n<pre data-language=\"Python\"><code>from modelscope.msdatasets import MsDataset\n\nds = MsDataset.load(\n    'simpleai/HC3-Chinese',\n    subset_name='baike',\n    trust_remote_code=True,\n)\nprint(ds)</code></pre>\n\n<img src=\"assets/manuscript-20260914/c03-1e8bcc0c71e19b.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>换一份数据集时，要先查它有哪些子集。<code>baike</code>是这里的具体名称，不能原样套到其他数据集上。</p>\n\n<p></p>\n\n<h3 id=\"c3-s6\">训练集和测试集，也可以分开取</h3>\n\n<p>子集选的是哪一类数据，<code>split</code>选的是数据的哪一个划分。常见的有训练集<code>train</code>、验证集<code>validation</code>和测试集<code>test</code>。</p>\n\n<p></p>\n\n<p>只读取<code>baike</code>子集里的训练集，可以再加一个参数。</p>\n\n<pre data-language=\"Python\"><code>from modelscope.msdatasets import MsDataset\n\nds = MsDataset.load(\n    'simpleai/HC3-Chinese',\n    subset_name='baike',\n    split='train',\n    trust_remote_code=True,\n)\nprint(ds[0])</code></pre>\n\n<img src=\"assets/manuscript-20260914/c03-5bd2b9c5f8a03f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>并不是每份数据都同时提供这三种划分，具体名称要看数据集说明。拿到训练集后，先打印一条样本，确认字段和内容都符合预期，再接后面的处理代码。</p>\n\n<p></p>\n\n<h3 id=\"c3-s7\">想复现实验，记得把版本一起记下来</h3>\n\n<p>数据更新以后，同一段代码可能读取到不同内容。以 HC3-Chinese 的<code>v1</code>版本为例，可以通过<code>version</code>明确指定。</p>\n\n<pre data-language=\"Python\"><code>from modelscope.msdatasets import MsDataset\n\nds = MsDataset.load(\n    'simpleai/HC3-Chinese',\n    subset_name='baike',\n    version='v1',\n    trust_remote_code=True,\n)\nprint(ds)</code></pre>\n\n<img src=\"assets/manuscript-20260914/c03-1217bdcfbfd54f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>实际操作时，到数据集页面确认可用版本。如果某个版本会持续更新，还要保留当时使用的数据文件或校验信息，方便之后对照。</p>\n\n<p></p>\n\n<h2 id=\"c3-s8\">数据下载好了，先别急着全交给模型</h2>\n\n<p>先抽几条样本看看有没有空值、乱码或者明显错误，再确认字段是不是符合训练代码要求。同一批问题的答案格式，如果前后差别很大，也需要先整理。</p>\n\n<p></p>\n\n<p>训练数据和测试数据还要分开。准备用来检查模型效果的样本，别提前混进训练集，否则分数看着不错，却很难判断模型遇到新问题时怎么样。</p>\n\n<p></p>\n\n<p></p>\n\n<p></p>\n","imageCount":8,"attachmentCount":0,"discussionId":"chapter-3"},{"id":"chapter-4","number":4,"title":"没有资源也能玩开源模型，因为这里资源免费","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-04.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-04.html","minutes":3,"status":"ready","headings":[{"id":"c4-s1","text":"免费资源有多少，先看清楚再用","level":"h2"},{"id":"c4-s2","text":"打开浏览器，先把 Notebook 启动起来","level":"h2"},{"id":"c4-s3","text":"文件存在哪里，这件事要记住","level":"h2"},{"id":"c4-s4","text":"先运行一行代码，后面就好办了","level":"h2"},{"id":"c4-s5","text":"实验结束，别让 GPU 一直开着","level":"h2"}],"html":"<p>模型找到了，数据也准备好了，结果卡在了最后一个问题上，没有资源跑，</p>\n\n<p>自己的电脑没有显卡，或者显存不够，甚至连内存都不够，这太让人心痛了～</p>\n\n<p></p>\n\n<p>没关系，可以先用魔搭提供的免费资源，</p>\n\n<p><b>魔搭 Notebook 把开发环境放在了云端，打开浏览器，就能写代码、处理数据，也能在免费 GPU 额度内尝试模型推理和小规模微调。</b></p>\n\n<p>刚开始学习开源模型，不必先买一台新电脑，先把一个小实验跑起来，看看自己究竟需要多少资源。</p>\n\n<p></p>\n\n<h2 id=\"c4-s1\">免费资源有多少，先看清楚再用</h2>\n\n<p>魔搭提供免费 CPU 环境，也提供有时长限制的 GPU 环境，免费资源包括下面这些，</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col><col></colgroup><tbody><tr><td><p>资源</p></td><td><p>配置与额度</p></td><td><p>可以先用来做什么</p></td></tr><tr><td><p>CPU 环境</p></td><td><p>8 核 CPU、32GB 内存，免费使用时长不限</p></td><td><p>下载文件、处理数据、调试代码</p></td></tr><tr><td><p>GPU 环境</p></td><td><p>NVIDIA A10、24GB 显存，36 小时免费时长</p></td><td><p>适合该显存规模的模型推理和小规模微调</p></td></tr><tr><td><p>存储空间</p></td><td><p>100GB 持久化存储</p></td><td><p>保存代码、模型、数据和实验结果</p></td></tr></tbody></table>\n</div>\n<p></p>\n\n<h2 id=\"c4-s2\">打开浏览器，先把 Notebook 启动起来</h2>\n\n<p>登录魔搭，进入<a href=\"https://modelscope.cn/my/mynotebook\" target=\"_blank\" rel=\"noopener noreferrer\">我的 Notebook</a>，如果页面提示需要完成账号绑定或认证，按页面指引操作，再选择 CPU 或 GPU 环境。选好后，点击“启动”，等待实例创建。</p>\n\n<img src=\"assets/manuscript-20260914/c04-8e54a4319c83d2.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>实例就绪后，点击查看 Notebook 进入开发页面，下方截图里会出现体验新版的提示，按提示进入即可；界面更新后，以当前入口为准。</p>\n\n<img src=\"assets/manuscript-20260914/c04-cb10124282fbf0.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h2 id=\"c4-s3\">文件存在哪里，这件事要记住</h2>\n\n<p>进入环境后，可以在左侧文件区上传自己的代码和数据，也可以把需要的结果下载回本地。</p>\n\n<img src=\"assets/manuscript-20260914/c04-fb60b44eab2e14.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p><b>需要保留的文件，放在</b><b><code>/mnt/workspace</code></b><b>目录下，</b> 魔搭提供的 100GB 持久化存储挂载在这里，其他路径中的文件默认会在实例关闭后不会继续保留。</p>\n\n<p>可以在这个目录下分别建立模型、数据和输出文件夹，后面找东西会方便一些。在终端执行下面的命令即可。</p>\n\n<pre data-language=\"Bash\"><code>mkdir -p /mnt/workspace/models /mnt/workspace/data /mnt/workspace/outputs</code></pre>\n\n<p>下载模型时，也可以直接指定保存位置。</p>\n\n<pre data-language=\"Python\"><code>from modelscope import snapshot_download\n\nmodel_dir = snapshot_download(\n    'Qwen/Qwen3-0.6B',\n    local_dir='/mnt/workspace/models/Qwen3-0.6B',\n)\nprint(model_dir)</code></pre>\n\n<p>如果想看看已经用了多少存储，在终端执行下面这条命令。</p>\n\n<pre data-language=\"Bash\"><code>du -sh /mnt/workspace</code></pre>\n\n<p>模型文件容易占空间，不再需要的版本可以及时清理。重要代码和实验结果也可以定期下载一份到本地。</p>\n\n<p></p>\n\n<h2 id=\"c4-s4\">先运行一行代码，后面就好办了</h2>\n\n<p>在文件区新建一个<code>test.ipynb</code>文件，打开后添加代码单元格。第一次可以先写一行简单的 Python。</p>\n\n<pre data-language=\"Python\"><code>print('你好，魔搭Notebook！')</code></pre>\n\n<p>点击代码单元格左侧的运行按钮，输出会显示在下方。看到这句话，就说明你已经成功在云端执行了代码。</p>\n\n<img src=\"assets/manuscript-20260914/c04-813ceec84fa022.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>需要安装 ModelScope 时，可以在代码单元格里运行下面的命令。</p>\n\n<pre data-language=\"Python\"><code>%pip install -U modelscope</code></pre>\n\n<p>然后新建一个单元格，粘贴前面的模型下载代码。需要读取数据时，也可以继续添加<code>MsDataset.load</code>示例。</p>\n\n<p>Notebook 的方便之处就在于，代码、说明和运行结果可以放在同一个文件里，改一段、跑一段，哪里报错就回到哪里检查。</p>\n\n<p></p>\n\n<h2 id=\"c4-s5\">实验结束，别让 GPU 一直开着</h2>\n\n<p>免费 GPU 有时长限制，任务跑完后记得停止实例。只是离开页面或者关闭浏览器，不要当成已经停止了云端机器。返回 Notebook 页面可以查看剩余额度。</p>\n\n<p></p>\n\n<p>长任务要提前安排保存进度，训练得到的文件写到<code>/mnt/workspace</code>里。GPU 额度用完后，还可以继续使用免费 CPU 环境处理数据和调试代码；需要更多算力时，再按需求选择活动资源或付费服务。</p>\n\n<p></p>\n\n<p>第一次不用把任务安排得太满，打开环境，建一个 Notebook，下载一个小模型，把示例跑起来，</p>\n\n<p>可以参考【30 分钟看到第一个结果】章节。</p>\n","imageCount":4,"attachmentCount":0,"discussionId":"chapter-4"},{"id":"chapter-5","number":5,"title":"要把业务问题转换成模型任务问题","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-05.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-05.html","minutes":7,"status":"ready","headings":[{"id":"c5-s1","text":"先想清楚，你想让模型做什么","level":"h2"},{"id":"c5-s2","text":"3.2 任务确定了，模型去哪里找？","level":"h2"},{"id":"c5-s3","text":"先按任务标签找一遍","level":"h3"},{"id":"c5-s4","text":"候选太多，就再加几个条件","level":"h3"},{"id":"c5-s5","text":"知道名字，直接搜就行","level":"h3"},{"id":"c5-s6","text":"下载量高的模型，就一定适合你吗？","level":"h2"},{"id":"c5-s7","text":"通用大模型和专用模型，到底怎么选？","level":"h2"},{"id":"c5-s8","text":"通用大模型，方便先试起来","level":"h3"},{"id":"c5-s9","text":"专用模型，适合把固定任务做好","level":"h3"},{"id":"c5-s10","text":"不用非得二选一","level":"h3"},{"id":"c5-s11","text":"一件事，也可以让几个模型一起做","level":"h2"}],"html":"<p>看到一个新模型发布，大家的第一想法一定是先下载试试。但轮到自己的业务，问题就具体了，能处理公司业务问题吗？能判断客户在问什么吗？我们有必要用这么大的模型吗？</p>\n\n<p></p>\n\n<p><b>所以在选开源模型之前，先把要做的事想清楚。</b> </p>\n\n<p>同样是处理合同，给合同分类、抽取金额和日期、生成摘要，需要关注的能力就不一样。目标明确了，再去模型库里找，才知道该看哪些模型更适配。</p>\n\n<p></p>\n\n<h2 id=\"c5-s1\">先想清楚，你想让模型做什么</h2>\n\n<p>业务方提出需求时，通常不会直接说“需要一个分类模型”或“需要一个信息抽取模型”，一般从实际业务出发描述问题，例如“帮我把合同里的关键信息抽出来”或“自动回复用户的提问”。</p>\n\n<p>模型选型的第一步，就是把这些业务需求转换成模型任务。任务确定后，才能进一步判断需要什么类型的模型。</p>\n\n<p>判断一个需求属于什么模型任务，可以先从两个方面入手：</p>\n\n<p>1）任务目标</p>\n\n<p>按照模型的工作目标，常见任务可分为以下几类：</p>\n\n<div class=\"table-scroll\"><table><tr><td>任务类型</td><td>核心定义</td><td>通俗理解</td></tr><tr><td>生成任务</td><td>根据输入内容生成新的文本、图片、音频等内容</td><td>“写一段新内容”</td></tr><tr><td>分类任务</td><td>根据输入内容判断其所属类别</td><td>“判断属于哪一类”</td></tr><tr><td>信息抽取任务</td><td>从原始数据中提取需要的信息，并按照一定格式输出</td><td>“抽取出固定字段”</td></tr><tr><td>检索与排序任务</td><td>从一批候选内容中找出相关内容，并按照相关程度排序</td><td>“找出最匹配的并排序”</td></tr><tr><td>预测任务</td><td>根据已有数据，对未来或未知的结果进行判断</td><td>“预测一个数值或走势”</td></tr></table>\n</div>\n<p>2）处理的数据类型</p>\n\n<p>面对不同的数据类型，模型的处理方式和内部结构都不一样的，所以需要选择专门的模型来处理。常见的数据类型如下：</p>\n\n<div class=\"table-scroll\"><table><tr><td>数据类型</td><td>说明</td><td>常见数据</td></tr><tr><td>文本</td><td>处理文本内容</td><td>文章、合同、聊天记录</td></tr><tr><td>语音</td><td>处理音频</td><td>录音、语音对话</td></tr><tr><td>图像</td><td>处理单张图片</td><td>照片、扫描件、商品图片</td></tr><tr><td>视频</td><td>处理连续的图像和声音信息</td><td>监控视频、短视频</td></tr></table>\n</div>\n<p>还需要注意输入和输出是不是只涉及一种数据。当输入输出都是同一种数据类型时，就按这种数据类型来划分模型任务。</p>\n\n<p>比如，输入输出都是图像，属于图像任务。当输入输出涉及两种或多种数据类型的任务时，属于多模态任务。常见的多模态任务有：语音转文字，输入是语音，输出是文本；根据图片回答问题，输入是图像和文本，输出是文本。</p>\n\n<p>实际判断时，可以先看业务问题的最终目标，再看输入和输出的数据类型。样例如下：</p>\n\n<pre data-language=\"Plain Text\"><code>样例一：判断用户评价为正面、负面或中性\n该业务的目标是输出一个类别标签，输入是文本，这是文本分类任务。\n\n样例二：上传一张图片，在商品库中找出最相似的商品并排序\n该业务的目标是从候选中匹配最相关项并排序 ，属于检索与排序任务。处理的数据类型是图片，所以这是图像检索与排序任务。\n\n样例三：根据一段文字描述，生成对应的图片\n该业务的目标是生成图片，属于生成任务。输入是文本，输出是图片，输入输出数据类型不一样，这是多模态生成任务，是常说的文生图任务。</code></pre>\n\n<h2 id=\"c5-s2\">3.2 任务确定了，模型去哪里找？</h2>\n\n<p>找到模型任务后，就进入模型选型环节。魔搭平台提供了多种模型筛选方法，可以参考以下方法选择模型。</p>\n\n<h3 id=\"c5-s3\">先按任务标签找一遍</h3>\n\n<p>魔搭为每个模型标注了任务类型标签。在模型库页面中：</p>\n\n<p>1.选择“模型库”进入模型浏览页；</p>\n\n<p>2.在左侧筛选栏中选择“任务类型”；</p>\n\n<p>3.选择对应的任务分类（如“文本分类”）。</p>\n\n<p>平台会自动筛选出标注了该任务标签的模型列表。这是最直接的筛选方式，优先推荐使用。</p>\n\n<img src=\"assets/manuscript-20260914/c05-7153456fc8f267.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c5-s4\">候选太多，就再加几个条件</h3>\n\n<p>当任务标签筛选后的候选模型仍然较多时，可以在左侧的“框架”和“其他”栏增加其他标签来缩小范围：</p>\n\n<p>框架：筛选 PyTorch、TensorFlow 等特定框架的模型；</p>\n\n<p>开源协议：商用场景需筛选 Apache 2.0、MIT 等宽松许可证；</p>\n\n<p>结构：筛选 llama、qwen3、deepseek_v2 等结构；</p>\n\n<p>语种：选择任务需要支持的中文、英文、日语、韩语等语种。</p>\n\n<img src=\"assets/manuscript-20260914/c05-9ba9a038167b55.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c5-s5\">知道名字，直接搜就行</h3>\n\n<p>如果知道具体任务名称或模型名称，可以直接在搜索框中输入关键词检索，如“OCR”。</p>\n\n<img src=\"assets/manuscript-20260914/c05-07e3644845dfba.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h2 id=\"c5-s6\">下载量高的模型，就一定适合你吗？</h2>\n\n<p>魔搭平台上对模型的热度也做了一定统计，可参考的指标如下：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col></colgroup><tbody><tr><td><p>指标</p></td><td><p>含义与用途</p></td></tr><tr><td><p>下载量</p></td><td><p>反映模型的整体使用频率。下载量越高，通常意味着模型经过了更多场景的验证</p></td></tr><tr><td><p>喜欢数</p></td><td><p>反映用户对模型的认可程度。收藏量高说明模型质量或场景适配度较好</p></td></tr><tr><td><p>更新时间</p></td><td><p>近期更新的模型通常具备更好的性能或更完善的生态支持。更新时间过久的模型可能存在兼容性问题</p></td></tr><tr><td><p>社区活跃度</p></td><td><p>Issue 和 Discussion 区的问题响应速度、讨论热度间接反映模型的维护状态</p></td></tr></tbody></table>\n</div>\n<p>平台支持根据下载量排序或喜欢数对筛选后的模型进行排序，在模型库右上角可以选择判断依据，同时还可以看到模型的更新时间。</p>\n\n<img src=\"assets/manuscript-20260914/c05-4021d9513fb58f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>查看社区活跃度必须进入模型卡片页面，查看社区的交流反馈数。</p>\n\n<img src=\"assets/manuscript-20260914/c05-c810af2c61fb02.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>通用模型的下载量通常显著高于垂直领域专用模型，所以跨领域直接对比数值意义不大，建议在同类任务范围内进行比较。</p>\n\n<h2 id=\"c5-s7\">通用大模型和专用模型，到底怎么选？</h2>\n\n<p>在实际使用大模型时，经常会遇到一个问题：应该选择通用大模型，还是针对具体任务训练的专用模型？</p>\n\n<p>两者各有其特点，选择时主要看任务类型、数据量、推理速度和部署成本等因素。</p>\n\n<h3 id=\"c5-s8\">通用大模型，方便先试起来</h3>\n\n<p>通用大模型通常在大量、多领域的数据上进行预训练，可以处理多种类型的任务，例如文本分类、内容生成、信息抽取和问答等。不针对某一个具体任务进行优化，而是希望在更多场景下都能使用。</p>\n\n<p>优势：一个模型可以处理多种任务，通常只需要调整 Prompt，就可以让模型完成不同的工作。对于数据量较少的任务，也可以先通过少样本或零样本的方式进行尝试，不一定要先准备大量标注数据。</p>\n\n<p>劣势：通用大模型通常参数量较大，因此对计算资源的要求也比较高。模型越大，推理时占用的显存和计算资源通常越多，在高并发场景下还需要考虑推理速度和部署成本。另外，在某些比较明确的专业任务上，经过针对性训练的专用模型可能会有更好的效果。</p>\n\n<p>通用大模型比较适合任务类型比较多、还处于探索阶段，或者暂时没有足够数据训练专用模型的场景。如果需要快速验证一个新的应用方向，直接使用通用大模型通常是比较方便的选择。</p>\n\n<h3 id=\"c5-s9\">专用模型，适合把固定任务做好</h3>\n\n<p>专用模型主要针对某一类任务或某个领域进行训练和优化。例如，用于文本分类、OCR、语音识别、目标检测等任务的模型，都可以看作专用模型。</p>\n\n<p><b>优势：</b>由于模型针对特定任务进行了优化，通常不需要处理大量无关任务，因此在推理速度、资源占用和任务效果方面都有一定优势。对于任务比较固定的应用，还可以根据实际数据进一步训练模型，使模型更适合自己的业务。</p>\n\n<p><b>劣势：</b>专用模型的适用范围相对有限，换一个任务后可能就无法直接使用。例如，一个用于文本分类的模型，并不能直接拿来完成文本生成。如果业务需求发生变化，原来的模型可能需要重新训练或调整。同时，如果一个系统中使用了很多不同的专用模型，也需要分别管理这些模型的版本和部署环境。</p>\n\n<p>专用模型比较适合任务比较明确、业务需求相对稳定，而且对推理速度、资源占用或部署成本有要求的场景。如果已经积累了比较好的训练数据，使用专用模型通常更容易针对具体任务进行优化。</p>\n\n<h3 id=\"c5-s10\">不用非得二选一</h3>\n\n<p>实际项目中不一定要在通用大模型和专用模型之间二选一，两者经常搭配着使用。</p>\n\n<p>例如，在智能客服中，可以先使用通用大模型理解用户的问题，判断用户想解决什么事情；确定任务后，再交给对应的专用模型处理。</p>\n\n<p>在实际选型时，可以先从任务本身出发。如果任务还比较模糊，或者需要同时处理多种类型的问题，可以优先考虑通用大模型；如果任务已经比较明确，而且对速度、成本或稳定性要求较高，则可以进一步考虑专用模型。</p>\n\n<p></p>\n\n<h2 id=\"c5-s11\">一件事，也可以让几个模型一起做</h2>\n\n<p>一个业务系统通常不只使用一个模型。根据具体任务的复杂程度，可以让一个模型完成全部工作，也可以将不同模型组合起来，让每个模型负责其中一个环节。例如，在一个智能文档处理系统中，从文档输入到最终结果输出，可能会经过文档分类、OCR、版面分析、表格识别、信息抽取等多个步骤：</p>\n\n<img src=\"assets/manuscript-20260914/c05-84652f13d47536.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>在这个系统中，每个模型分别处理不同任务。多模型组合通常有以下几个特点：</p>\n\n<ul><li>每个模型负责一个独立任务。 如 OCR 模型负责识别文字，版面分析模型负责判断元素位置，信息抽取模型负责从识别结果中提取需要的内容。</li><li>如果某一个环节的效果不好，可以只替换这个环节的模型，不需要重新修改整个系统。没有依赖关系的任务可以并行处理，从而减少整体处理时间。</li><li>对准确率要求高的任务选择大模型，对速度要求高、任务简单的环节则使用轻量模型。</li></ul>\n\n<p></p>\n\n<p>但多模型组合也存在模型衔接问题，前一个模型的输出格式必须能够被后一个模型正确解析，这要求设计系统时，需要提前考虑不同模型之间的数据格式。例如 OCR 输出的是带坐标信息的文本块，而信息抽取模型更适合处理纯文本，这就需要在中间增加数据转换环节，将 OCR 结果转为目标格式。</p>\n\n<p>在进行模型选型时，不能只看某一个模型的效果，还要看它放到整个处理流程中是否合适。一个单独测试效果很好的模型，如果输出格式难以和其他模型衔接，在实际系统中也可能并不好用。</p>\n\n<p></p>\n","imageCount":6,"attachmentCount":0,"discussionId":"chapter-5"},{"id":"chapter-6","number":6,"title":"先评再选：用 EvalScope 形成开源模型的第一份报告","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-06.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-06.html","minutes":12,"status":"ready","headings":[{"id":"c6-s1","text":"只试几个问题，就能选出好模型吗？","level":"h2"},{"id":"c6-s2","text":"榜单上的分数，到底测的是什么？","level":"h2"},{"id":"c6-s3","text":"用自己的业务数据，再测一遍","level":"h2"},{"id":"c6-s4","text":"评测这件事，EvalScope 能帮上什么忙？","level":"h2"},{"id":"c6-s5","text":"动手跑一次评测，看看模型的实际表现","level":"h2"},{"id":"c6-s6","text":"先用公开题库，测一测基础能力","level":"h3"},{"id":"c6-s7","text":"换成业务数据，看看检索效果","level":"h3"},{"id":"c6-s8","text":"不仅能测效果，还能测试性能","level":"h3"},{"id":"c6-s9","text":"彩蛋","level":"h2"}],"html":"<p>这是一个真实且惨痛的教训。</p>\n\n<p>之前在给业务部门进行模型训练的时候，误以为越新的在通用榜单上效果越好的模型，就会在业务场景中表现的越出色。</p>\n\n<p>当出了一个在通用榜单上效果超群的模型时，<b>信誓旦旦</b><b>地承诺</b><b>，马上可以给出更好的微调模型。</b></p>\n\n<p>但实则不然，因为每个模型在预训练和后训练中采用的数据大不相同。有的模型恰好在预训练阶段见过大量和你业务相似的数据，微调时自然事半功倍；有的虽然通用能力强，但和你的领域\"八字不合\"，怎么调都差一口气。</p>\n\n<p><b>所有模型在选择前，通用指标只能是一个参考标准，最终模型选择还要看在业务数据上的表现。</b></p>\n\n<p></p>\n\n<h2 id=\"c6-s1\">只试几个问题，就能选出好模型吗？</h2>\n\n<p>我们在选择模型时，经常会先看模型排行榜、下载量或者网上的评价，也可能直接准备几个问题问一下模型，看看回答得怎么样。这些方法可以帮助我们快速了解一个模型，但都有一定的局限。</p>\n\n<img src=\"assets/manuscript-20260914/c06-4316b3e21864b5.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>例如，模型下载量高，并不一定代表模型能力最强，也可能与模型发布时间、参数规模和使用门槛有关；几个问题回答得很好，也不能代表模型在这一类问题上都能保持同样的效果。</p>\n\n<p>特别是在多个模型之间进行选择时，如果每个模型测试的问题不同，或者判断答案好坏的标准不同，就很难进行直接比较。有时候两个模型看起来回答得都不错，但真正放到几百甚至几千条数据上测试以后，准确率和稳定性可能存在明显的差异。</p>\n\n<p></p>\n\n<p>模型评测的目的就是把不规范的评测标准，变成一套更加统一的测试方法，例如，准备 500 道固定的测试题，让 Qwen、DeepSeek 等候选模型分别回答，再使用相同的规则计算准确率。这样既可以比较不同模型的整体效果，也可以减少个人判断对结果的影响。</p>\n\n<p>通过模型评测，我们不仅可以看到模型最终得到多少分，还可以进一步分析模型哪些任务表现较好、哪些类型的问题容易出错，以及不同模型之间的差距有多大。这些结果可以帮助我们从多个候选模型中筛选出更加合适的模型，也可以为后续的业务数据测试、模型微调和部署提供参考。</p>\n\n<p></p>\n\n<h2 id=\"c6-s2\">榜单上的分数，到底测的是什么？</h2>\n\n<p>为了方便比较不同模型，Benchmark 中包含了不同类型的数据，例如，</p>\n\n<p>MMLU 包含数学、历史、计算机、法律等多个学科的问题，主要用于观察模型的知识和推理能力；</p>\n\n<img src=\"assets/manuscript-20260914/c06-f4faad78672f50.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>C-Eval 和 CMMLU 包含较多中文考试和知识类问题，可以用于测试模型的中文知识能力；</p>\n\n<p>GSM8K 主要由数学应用题组成，可以用于测试数学推理能力；</p>\n\n<img src=\"assets/manuscript-20260914/c06-75145880db6223.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>HumanEval 主要测试模型根据要求编写代码的能力；</p>\n\n<p>IFEval 则重点测试模型能不能按照用户提出的要求完成任务。</p>\n\n<img src=\"assets/manuscript-20260914/c06-63d8b874dceff0.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>在实际评测时，可以根据自身业务需求，选择更贴合自己的场景的数据进行评测。例如，如果场景是更加关注中文知识的问答效果，可以重点测试中文理解、知识和推理相关的数据集；如果模型主要用于数学推理，则可以重点测试 GSM8K 等数学数据集。</p>\n\n<p>通过这些公开 Benchmark，可以比较不同模型在相同数据的效果，从中筛选出一些能力比较符合要求的模型。<b>但是 Benchmark 分数高，并不代表模型在实际业务中一定好用。</b></p>\n\n<p>公开 Benchmark 里的数据都是通用数据，而真实业务中可能存在大量专业术语、内部规则，这些内容很难通过公开数据集测试出来。也就是说，我们再选择模型时，可以通过 Benchmark 做一次模型初筛，选出效果不错的模型，但是具体场景的效果，还需要使用业务数据进行评测。</p>\n\n<p></p>\n\n<h2 id=\"c6-s3\">用自己的业务数据，再测一遍</h2>\n\n<p>通用 Benchmark 测的是模型的通用能力，而业务评测着重的是它在具体场景中的实际表现。在第一轮筛选结束后，接下来的核心问题很明确：这个模型放到我们的业务里，到底适不适用？</p>\n\n<p>业务数据评测，本质上就是用真实业务产生的数据去验证模型。比如：做智能客服就用用户的真实咨询记录，做企业知识库就用员工日常的检索与问答，做工业运维则可以用一线的设备故障日志。</p>\n\n<p>在准备评测数据时，大家常有一个误区，认为数据量越大越好。相比于单纯的数量，数据的代表性和覆盖面其实更关键。如果准备了几万条数据，但大多是简单、重复的问题，测出来的分数哪怕很高，也没有太多参考价值，一份合理的业务测试集，应该具备清晰的梯度：既要有日常高频的基础问题，也要包含一定比例的复杂长难问题以及边界异常问题。只有通过这种有层次的真实数据检验，测出的结果才足够客观。</p>\n\n<img src=\"assets/manuscript-20260914/c06-b3877cf337a08f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h2 id=\"c6-s4\">评测这件事，EvalScope 能帮上什么忙？</h2>\n\n<p>EvalScope 是 ModelScope 社区推出的大模型评测框架，可以实现对不同模型的效果与推理性能进行统一评估与横向对比。EvalScope 支持范围比较广，包括大语言模型、多模态模型，以及检索中常用的 Embedding 和 Reranker 等多种模型类型。EvalScope 的优势体现在以下几个方面：</p>\n\n<p>1）兼顾通用 Benchmark 与自定义业务评测</p>\n\n<p>EvalScope 预置了 MMLU、GSM8K、HumanEval 等常见的公开基准数据集，可以直接对模型的通用知识、数学、代码及逻辑推理能力进行评测。同时，它支持用户自定义评测集，例如将业务中的文本分类、问答、信息抽取等真实数据引入评测流程，从而准确判断模型在具体业务场景中的表现。</p>\n\n<p>2）支持多形态的模型接入与服务调用</p>\n\n<p>EvalScope 框架既支持评测本地离线模型，也兼容标准 OpenAI 格式接口。对于通过 vLLM、SGLang 等主流推理引擎部署的模型服务，可以直接接入评测，适配不同的开发与部署环境。</p>\n\n<p>3）具备推理性能压测能力</p>\n\n<p>除了效果评估，模型部署后的运行性能同样是选型的重要考量。EvalScope 能够模拟多用户并发请求，测试吞吐量、请求延迟、首 Token 延迟（TTFT）以及单 Token 输出耗时（TPOT）等核心指标，直观反映服务在并发场景下的承载能力与响应速度。</p>\n\n<p>4）提供多维度的对比分析与报告可视化</p>\n\n<p>评测完成后，EvalScope 框架支持自动生成统计报表与对比图表。</p>\n\n<p></p>\n\n<h2 id=\"c6-s5\">动手跑一次评测，看看模型的实际表现</h2>\n\n<p>本实验 ModelScope 的 Notebook 进行实验，采用 ubuntu22.04-cuda12.8.1-py312-torch2.10.0-1.39.0 这个镜像</p>\n\n<img src=\"assets/manuscript-20260914/c06-776b3c96504c69.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>查看环境中是否已经安装 evalscope</p>\n\n<pre data-language=\"Plain Text\"><code>!pip3 list |grep evalscope</code></pre>\n\n<p>终端中输出下面内容，说明已经安装了 evalscope</p>\n\n<img src=\"assets/manuscript-20260914/c06-d64df78b854d6f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>如果没有安装，可以通过 pip 直接安装</p>\n\n<pre data-language=\"Plain Text\"><code>!pip3  install evalscope\n如果需要扩展更多的功能，可以使用下面的命令安装\n!pip3 install -e '.[opencompass]'   # Install OpenCompass backend\n!pip3 install -e '.[perf]'          # Install Perf dependencies\n!pip3 install -e '.[app]'           # Install visualization dependencies\n!pip3 install -e '.[all]'           # Install all backends (Native, OpenCompass, VLMEv</code></pre>\n\n<h3 id=\"c6-s6\"><b>先用公开题库，测一测基础能力</b></h3>\n\n<p>安装完成后，我们可以使用一个公开数据进行评测，比如 math_500，模型采用 Qwen3-0.6B，为了快速测试，我们可以只选择 10 条数据进行评测，评测命令如下：</p>\n\n<pre data-language=\"Plain Text\"><code>!evalscope eval --model Qwen/Qwen3-0.6B --datasets math_500 --limit 10</code></pre>\n\n<p><code>--model</code> 参数指定模型名称或者本地模型路径，<br><code>--datasets</code> 指定评测数据集 ，<br><code>--limit</code>指定只测试前多少条 。</p>\n\n<p>输出结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c06-5396601da03fdd.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<img src=\"assets/manuscript-20260914/c06-1377e09547df87.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>我们可以在对应的目录下查看评测结果，<code>eval_log.log</code>记录了 10 条数据的评测结果。</p>\n\n<img src=\"assets/manuscript-20260914/c06-a8bb210e3c0a60.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c6-s7\"><b>换成业务数据，看看检索效果</b></h3>\n\n<p>为了评测模型在业务数据的效果，我们可以自定义一些数据进行评测，本章采用某运营商公开的检索数据进行评测，主要测试<code>Q</code><code>wen3-</code><code>E</code><code>mbedding-0.6B</code>模型效果。</p>\n\n<p>数据形式如下：</p>\n\n<pre data-language=\"Plain Text\"><code>retrieval_data1\n├── corpus.jsonl\n├── queries.jsonl\n└── qrels.jsonl\n   </code></pre>\n\n<p>其中<code>corpus.jsonl</code>文档库数据，数据形式如下：包括<code>{\"_id\": \"xxx\", \"text\": \"xxx\"}</code>，_id 为语料库的 id，text 为语料库的文本。形式如下：</p>\n\n<img src=\"assets/manuscript-20260914/c06-55127d99247885.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p><code>queries.jsonl</code>为查询文件，每行包括<code>{\"_id\": \"xxx\", \"text\": \"xxx\"}</code>，_id 为查询数据的 id，text 为语查询文本。形式如下：</p>\n\n<img src=\"assets/manuscript-20260914/c06-ae7febb9ef1efc.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p><code>qrels.jsonl</code> 是标准答案文件，也就是这个问题对应的正确文档，每行包括<code>{\"query-id\": \"xxx\", \"corpus-id\": \"xxx\",\"score\":1}</code>，<code>query-id</code>为查询数据的 id，<code>corpus-id</code>为文档库 id,<code>score</code>为相关性得分。形式如下：</p>\n\n<img src=\"assets/manuscript-20260914/c06-5f19142927756d.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>把这三个文件直接右击上传到指定目录下即可，</p>\n\n<img src=\"assets/manuscript-20260914/c06-8f6fbe965e2a77.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>安装<code>mteb</code>包</p>\n\n<pre data-language=\"Plain Text\"><code>!pip install mteb</code></pre>\n\n<img src=\"assets/manuscript-20260914/c06-f36e3c459d3a19.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>安装<code>langchain_core</code>包</p>\n\n<pre data-language=\"Plain Text\"><code>!pip install langchain_core</code></pre>\n\n<img src=\"assets/manuscript-20260914/c06-1c64b252b9549f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>接下来，我们可以评价 Embedding 的检索效果，代码如下：</p>\n\n<pre data-language=\"Python\"><code>from evalscope.run import run_task\n\ntask_cfg = {\n    \"work_dir\": \"outputs\",\n\n    \"eval_backend\": \"RAGEval\",\n\n    \"eval_config\": {\n\n        \"tool\": \"MTEB\",\n\n        \"models\": [\n            {\n                \"model_name_or_path\": \"Qwen/Qwen3-Embedding-0.6B\",\n\n                \"max_seq_length\": 512,\n\n                \"model_kwargs\": {\n                    \"torch_dtype\": \"auto\"\n                },\n\n                \"encode_kwargs\": {\n                    \"batch_size\": 8,\n                },\n            }\n        ],\n\n        \"eval\": {\n\n            \"custom_tasks\": [\n                {\n                    \"name\": \"MyRetrieval\",\n                    \"data_path\": \"./retrieval_data1\"\n                }\n            ],\n\n            \"overwrite_results\": True,\n\n            \"verbosity\": 2\n        },\n    },\n}\n\nrun_task(task_cfg=task_cfg)</code></pre>\n\n<p>执行结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c06-5d36797f262d49.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c6-s8\"><b>不仅能测效果，还能测试性能</b></h3>\n\n<p>完成模型效果测试之后，还可以继续进行性能压测。如果模型已经通过 vLLM 等推理框架部署，可以使用 EvalScope 向模型服务发送不同并发量的请求。</p>\n\n<p>例如，可以分别设置 1、10、50、100 等不同并发量，观察并发增加之后模型的吞吐量、首 Token 延迟和请求成功率。EvalScope 的性能测试结果能够统计 RPS、输出吞吐量、平均延迟以及 P50、P99 等指标，并将结果保存为测试报告。</p>\n\n<p>这个镜像下操作，默认安装包可能会报错，通过降低 modelscope 版本即可</p>\n\n<pre data-language=\"Plain Text\"><code>!python -m pip install --no-cache-dir --force-reinstall \"modelscope==1.37.1\"</code></pre>\n\n<p>打开 Notebook，在终端中用 vLLM 启动 Qwen3.5-2B</p>\n\n<pre data-language=\"Plain Text\"><code>CUDA_VISIBLE_DEVICES=0 \\\nvllm serve Qwen/Qwen3-0.6B \\\n    --served-model-name qwen3-0.6b \\\n    --host 0.0.0.0 \\\n    --port 8000 \\\n    --gpu-memory-utilization 0.6</code></pre>\n\n<img src=\"assets/manuscript-20260914/c06-59ca2059927dea.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<img src=\"assets/manuscript-20260914/c06-ef3bfe899faf9c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>模型启动完成后，需要测试下接口是否可以调通，输入下面代码：</p>\n\n<pre data-language=\"C++\"><code>import requests\n\nurl = \"http://127.0.0.1:8000/v1/chat/completions\"\n\ndata = {\n    \"model\": \"qwen3-0.6b\",\n    \"messages\": [\n        {\n            \"role\": \"user\",\n            \"content\": \"你好，请介绍一下自己\"\n        }\n    ],\n    \"max_tokens\": 100\n}\n\nresponse = requests.post(url, json=data)\n\nprint(\"状态码:\", response.status_code)\nprint(response.text)</code></pre>\n\n<p>如果可以调通，会显示模型输出结果</p>\n\n<img src=\"assets/manuscript-20260914/c06-f82a1385ef78e1.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>接下来，就是使用 EvalScope 评测性能，一般我们比较关注的指标就是首字出现的时间（TTFT）以及单 Token 输出耗时（TPOT）等指标。</p>\n\n<pre data-language=\"SQL\"><code>!evalscope perf \\\n    --model qwen3-0.6b \\\n    --url http://127.0.0.1:8000/v1/chat/completions \\\n    --api openai \\\n    --api-key sk-123456 \\\n    --dataset random \\\n    --tokenizer-path Qwen/Qwen3-0.6B \\\n    --min-prompt-length 1024 \\\n    --max-prompt-length 1024 \\\n    --min-tokens 1024 \\\n    --max-tokens 1024 \\\n    --parallel 1 2 4 8 \\\n    --number 10 20 50 100</code></pre>\n\n<p>其中，相关参数说明：</p>\n\n<ul><li><code>--model qwen3.5-2b</code>：模型名称，需要和 vllm 推理服务中配置的模型名称保持一致</li><li><code>--url</code> <a href=\"http://127.0.0.1:8000/v1/chat/completions\" target=\"_blank\" rel=\"noopener noreferrer\">http://127.0.0.1:8000/v1/chat/completions</a>：模型服务的实际接口地址</li><li><code>--api-key sk-123456</code>：API 鉴权密钥。如果服务端要求鉴权则必须传入；若未配置，该参数可省略。</li><li><code>--dataset random</code>：指定测试数据集为随机生成模式，无需准备本地测试文件。</li><li><code>--tokenizer-path Qwen/Qwen3.5-2B</code>：指定分词器（Tokenizer）的路径或 Hugging Face 模型名称，用于精确控制生成文本的 Token 长度。</li><li><code>--min-prompt-length 1024</code> 与 <code>--max-prompt-length 1024</code>：控制输入提示词的长度区间。两者均设为 1024，表示固定每条请求的输入长度为 1024 个 Token。</li><li><code>--min-tokens 1024</code> 与 <code>--max-tokens 1024</code>：控制模型生成的输出长度区间。两者均设为 1024，表示强制模型每次输出固定的 1024 个 Token（忽略结束符）。</li><li><code>--parallel 1 2 4 8</code>：设置并发数梯度。压测时将依次模拟 1、2、4、8 个用户同时发起请求，以观察系统在不同并发压力下的表现。</li><li><code>--number 10 20 50 100</code>：设置各并发梯度下的总请求数。该列表与 <code>--parallel</code> 一一对应，例如在 1 并发时累计发送 10 个请求，在 8 并发时累计发送 100 个请求。</li></ul>\n\n<img src=\"assets/manuscript-20260914/c06-f8a82974fbf4aa.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p><code>benchmark.log</code>的结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c06-2dd7dcab8a21d7.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>EvalScope 给我们提供了一种快速评测模型的方法，我们可以在对应的输出文件夹中看到相关 html 报告内容。更多 EvalScope 的用法，可以在 https://evalscope.readthedocs.io/zh-cn/v1.6.1/get_started/introduction.html 查看。</p>\n\n<p></p>\n\n<p>具体实验过程，可参考：https://modelscope.cn/gallery/liucong/5a794182-147f-4c97-83c5-233ca6965f4b</p>\n\n<p></p>\n\n<h2 id=\"c6-s9\">彩蛋</h2>\n\n<p><b>快用你自己真实的业务数据，来测测现在开源模型的效果～</b></p>\n\n<p></p>\n","imageCount":22,"attachmentCount":0,"discussionId":"chapter-6"},{"id":"chapter-7","number":7,"title":"30 分钟带你快速看到第一个结果","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-07.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-07.html","minutes":5,"status":"ready","headings":[{"id":"c7-s1","text":"打开 Notebook，先连上运行实例","level":"h2"},{"id":"c7-s2","text":"检查一下环境准备好了没有","level":"h2"},{"id":"c7-s3","text":"","level":"h4"},{"id":"c7-s4","text":"直接按模型名称加载模型","level":"h2"},{"id":"c7-s5","text":"传入一段音频，看看识别出了什么","level":"h2"}],"html":"<p>开源模型下载下来，可以在自己的笔记本上跑，也可以租一台云服务器，按需要配置 CPU、GPU 和运行环境。设备不同，准备工作也不一样。</p>\n\n<p></p>\n\n<p>如果只是想先试一下模型能做什么，魔搭 Notebook 提供了一个免费的入口。打开浏览器，选择运行实例，就能在网页里写代码、下载模型并查看结果。</p>\n\n<p></p>\n\n<p>这一篇从创建 Notebook、检查环境到加载模型，跟着做完，就能看到第一个结果。</p>\n\n<h2 id=\"c7-s1\">打开 Notebook，先连上运行实例</h2>\n\n<p>进入模型库，找到 Qwen/Qwen3-ASR-1.7B 模型，点击右侧的“Notebook 快速开发”即可创建魔搭 Notebook。</p>\n\n<p>模型链接：https://www.modelscope.cn/models/Qwen/Qwen3-ASR-1.7B</p>\n\n<img src=\"assets/manuscript-20260914/c07-24e3ac0b44038a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>点击右上角的“连接运行时”选择实例，这里我们选择 GPU 进行演示，可以根据模型依赖选择合适的预装镜像。</p>\n\n<img src=\"assets/manuscript-20260914/c07-842702a8d17940.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h2 id=\"c7-s2\">检查一下环境准备好了没有</h2>\n\n<p>加载实例后，首先需要检查当前环境，可以在 notebook 执行以下命令：</p>\n\n<p>1.查看 python 版本</p>\n\n<pre data-language=\"Plain Text\"><code>!python3 --version</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c07-0c22d724d88227.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>2.查看相关依赖</p>\n\n<p>不同模型的依赖不同，需要查看模型卡片了解信息，根据 Qwen3-ASR-1.7B 模型的模型卡片，了解到其推理依赖<code>qwen3-asr</code>。如果环境缺失 qwen3-asr，需要自行安装</p>\n\n<pre data-language=\"Plain Text\"><code># 查询依赖\n!pip3 show qwen-asr\n# 安装qwen-asr(如缺失)\n!pip3 install -U qwen-asr</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c07-d57a19908cfa63.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>安装成功的查询结果：</p>\n\n<img src=\"assets/manuscript-20260914/c07-bb1099aa862cef.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>3.查看 CPU 配置</p>\n\n<pre data-language=\"Plain Text\"><code>!lscpu</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c07-cea051219a074d.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>4.查看 GPU 及显存</p>\n\n<pre data-language=\"Plain Text\"><code>!nvidia-smi</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c07-cbdbb23a46f508.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>5.查看内存</p>\n\n<pre data-language=\"Plain Text\"><code>!free -h</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c07-eae28c100f0724.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>6.查看磁盘</p>\n\n<pre data-language=\"Plain Text\"><code>!df -h</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c07-65859c8cb5ee99.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h4 id=\"c7-s3\"></h4>\n\n<h2 id=\"c7-s4\">直接按模型名称加载模型</h2>\n\n<p>参考模型卡片示例代码进行模型加载，这里会自动下载需要的模型文件。</p>\n\n<pre data-language=\"Python\"><code>import torch\nfrom qwen_asr import Qwen3ASRModel\n \nmodel = Qwen3ASRModel.from_pretrained(\n    \"Qwen/Qwen3-ASR-1.7B\",\n    dtype=torch.bfloat16,\n    device_map=\"cuda:0\",\n    # attn_implementation=\"flash_attention_2\",\n    max_inference_batch_size=32, # Batch size limit for inference. -1 means unlimited. Smaller values can help avoid OOM.\n    max_new_tokens=256, # Maximum number of tokens to generate. Set a larger value for long audio input.\n)\nprint(\"模型加载完成！\")</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c07-654f63dcc5e91f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>如果想要自己单独下载模型，可参考本书 2.5 节的模型下载方法。如将模型下载到目录：<code>/mnt/workspace/Qwen3-ASR-1.7B</code>，以命令行下载方式为例，在终端执行以下命令：</p>\n\n<pre data-language=\"Plain Text\"><code># 安装 ModelScope（如未安装）\n!pip3 install modelscope\n# 下载完整模型库\n!modelscope download --model Qwen/Qwen3-ASR-1.7B --local_dir /mnt/workspace/Qwen3-ASR-1.7B</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c07-559068ac157a75.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>有下载好的模型文件，就可将<code>Qwen/Qwen3-ASR-1.7B</code>替换成你的模型路径。</p>\n\n<pre data-language=\"Python\"><code>import torch\nfrom qwen_asr import Qwen3ASRModel\n\nmodel = Qwen3ASRModel.from_pretrained(\n    \"/mnt/workspace/Qwen3-ASR-1.7B\",\n    dtype=torch.bfloat16,\n    device_map=\"cuda:0\",\n    # attn_implementation=\"flash_attention_2\",\n    max_inference_batch_size=32, # Batch size limit for inference. -1 means unlimited. Smaller values can help avoid OOM.\n    max_new_tokens=256, # Maximum number of tokens to generate. Set a larger value for long audio input.\n)\nprint(\"模型加载完成！\")</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c07-f77df5d805f7ba.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h2 id=\"c7-s5\">传入一段音频，看看识别出了什么</h2>\n\n<p>模型加载后，即可传入音频文件进行模型推理。可将 audio 的值替换成你的音频文件路径。</p>\n\n<pre data-language=\"SQL\"><code>results = model.transcribe(\n    audio=\"https://qianwen-res.oss-cn-beijing.aliyuncs.com/Qwen3-ASR-Repo/asr_en.wav\",\n    language=None, # set \"English\" to force the language\n)\n \nprint(results[0].language)\nprint(results[0].text)\nprint(\"模型推理完成！\")</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c07-eb17ac93b1fd85.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>可将 audio 的值替换成你的音频文件路径，进行模型推理。右击左侧工作区的“DSW-GPU”文件夹，点击“上传”，选择本地的音频文件，即可上传到 Notebook 环境的“DSW-GPU”文件夹下。</p>\n\n<img src=\"assets/manuscript-20260914/c07-3bebb1610065d9.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>然后修改代码如下：</p>\n\n<pre data-language=\"SQL\"><code>results = model.transcribe(\n    audio=\"/mnt/workspace/asr_en.wav\",\n    language=None, # set \"English\" to force the language\n)\n \nprint(results[0].language)\nprint(results[0].text)\nprint(\"模型推理完成！\")</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c07-df433592f8b1fb.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>本章代码及相关文件见：https://www.modelscope.cn/gallery/liucong/54c2ca38-b797-43d1-97ab-4b9f569aaa95</p>\n\n<p></p>\n","imageCount":15,"attachmentCount":0,"discussionId":"chapter-7"},{"id":"chapter-8","number":8,"title":"多大模型，选择什么样的服务器配置","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-08.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-08.html","minutes":6,"status":"ready","headings":[{"id":"c8-s1","text":"CPU 和 GPU，分别擅长什么？","level":"h2"},{"id":"c8-s2","text":"一个擅长灵活处理，一个擅长并行计算","level":"h3"},{"id":"c8-s3","text":"GPU 是不是做什么都更快？","level":"h3"},{"id":"c8-s4","text":"除了买设备，还要考虑哪些开销？","level":"h3"},{"id":"c8-s5","text":"CPU 和 GPU 怎么选？","level":"h3"},{"id":"c8-s6","text":"模型有多大，内存和显存要留多少？","level":"h2"},{"id":"c8-s7","text":"先看参数量，再看加载精度","level":"h3"},{"id":"c8-s8","text":"先看参数量，再看加载精度","level":"h3"},{"id":"c8-s9","text":"同一个模型，训练为什么更占资源？","level":"h3"}],"html":"<p>第一次跑出结果以后，很容易想试试更大的模型。可从 0.5B 换到 7B，但原来的机器就未必还够用了。</p>\n\n<p></p>\n\n<p>选资源时，先分清几个东西。CPU 和 GPU 负责计算，内存和显存用来容纳运行中的数据，磁盘负责保存模型文件。模型下载得下来，只说明磁盘放得下，能不能顺利运行，还得看内存、显存和计算能力。同一个模型，用什么精度加载、输入多长、同时处理多少请求，都会影响资源占用。推理和训练的需求也不同。</p>\n\n<p></p>\n\n<p>下面先看 CPU 和 GPU 各自适合做什么，再算一算模型参数会占用多少空间，为后面选择笔记本或云端实例做准备。</p>\n\n<p></p>\n\n<h2 id=\"c8-s1\">CPU 和 GPU，分别擅长什么？</h2>\n\n<h3 id=\"c8-s2\">一个擅长灵活处理，一个擅长并行计算</h3>\n\n<p>CPU 和 GPU 在计算方式上存在明显差异。</p>\n\n<p>CPU 面向通用计算，每个核心都具备较完整的控制和运算能力，可以根据程序执行过程进行判断、跳转和任务调度，能够灵活处理不同类型的计算任务。</p>\n\n<p>GPU 采用并行计算架构，包含大量计算单元，可以同时执行大量计算任务。适合处理数据规模较大、计算过程重复性较高的任务。</p>\n\n<h3 id=\"c8-s3\">GPU 是不是做什么都更快？</h3>\n\n<p>CPU 和 GPU 的处理速度取决于任务类型和计算规模。</p>\n\n<p>对于包含大量逻辑判断、程序控制和任务调度的场景，CPU 具有更高的执行效率。像操作系统运行、数据库查询这样的任务，适合在 CPU 上完成。</p>\n\n<p>对于大规模并行计算任务，GPU 能提供更高的计算吞吐量。深度学习中的矩阵乘法和卷积运算，可以拆分成多个并行子任务，由 GPU 计算单元同时执行，提升整体计算效率。</p>\n\n<p>不过，只有计算任务规模较大时，GPU 的优势才会比较明显。当数据量较小时，GPU 的计算资源无法充分利用，CPU 和 GPU 之间的数据传输和任务调度也会带来额外开销，此时使用 CPU 可能更加高效。</p>\n\n<h3 id=\"c8-s4\">除了买设备，还要考虑哪些开销？</h3>\n\n<p>CPU 和 GPU 的成本不仅包括硬件采购价格，还包括运行过程中的功耗，以及后续的使用和维护成本。</p>\n\n<p>在硬件采购方面，CPU 是标配，普通服务器和个人计算机都需要配置 CPU。用于 AI 训练和推理的高性能 GPU 价格通常较高，除了 GPU 本身，还需要考虑服务器、显存、存储以及其他配套硬件的成本。</p>\n\n<p>运行过程中，GPU 进行大规模计算时功耗较高，服务器中 GPU 数量越多，供电和散热的压力越大，数据中心的电力、散热成本会显著增加。</p>\n\n<p>使用和维护时，CPU 生态比较成熟，常规服务器维护相对简单。GPU 需要考虑驱动程序、计算框架和硬件环境的兼容性，不同版本间的冲突可能导致模型无法正常运行。多 GPU 环境还涉及设备之间的数据通信，对硬件连接和软件配置有一定要求。</p>\n\n<h3 id=\"c8-s5\">CPU 和 GPU 怎么选？</h3>\n\n<p>训练和推理任务对资源的要求不同，不同规模和类型的模型对计算资源的差异也很大。硬件选型需综合考虑模型规模、数据量、性能要求，以及训练与推理两个场景下的资源需求。</p>\n\n<p>在模型训练阶段，数据规模较小时，CPU 可以满足大部分传统机器学习模型的训练需求，如线性回归、逻辑回归、随机森林。部分轻量级神经网络也可以使用 CPU 完成训练，但随着数据规模和模型结构复杂度的增加，训练时间会明显变长。如果需要频繁进行模型调优，使用 GPU 能够缩短训练时间。</p>\n\n<p>大规模神经网络和大语言模型训练需要进行大量矩阵计算，对计算能力和显存容量的要求很高。像 Qwen 这种数十亿参数量级的大语言模型，需要依赖 GPU 等高性能加速设备完成训练。</p>\n\n<p>模型在推理阶段对硬件的要求和训练阶段不同。参数规模较小、访问量较低的场景，CPU 就可以满足推理需求，还可以通过量化等方法降低资源占用。大模型、高并发服务以及需要快速响应的场景，GPU 能提供更高的推理效率。</p>\n\n<p>CPU 和 GPU 各有适用场景，需要综合考虑模型规模、数据量、并发请求、性能要求和部署成本。</p>\n\n<h2 id=\"c8-s6\">模型有多大，内存和显存要留多少？</h2>\n\n<h3 id=\"c8-s7\">先看参数量，再看加载精度</h3>\n\n<p>评估模型对运行资源的需求，主要看两个指标：参数规模和数据精度。</p>\n\n<p>模型参数规模一般用 B 表示，1B 代表 10 亿参数。参数是模型在训练过程中学习得到的数值，推理时需要将这些参数加载到内存（CPU）或显存（GPU）中。参数量越大，占用的资源就越大。</p>\n\n<p>数据精度是指模型参数采用什么样的数值格式进行存储和计算。常见的格式包括 FP32、FP16、BF16，用于量化的 INT8、INT4 等。相同参数量的模型，数据精度越高，占用的资源就越大。</p>\n\n<p>不同精度下，1B 参数的存储占用量估算如下：</p>\n\n<div class=\"table-scroll\"><table><tr><td>精度</td><td>每 1B 参数占用空间（约）</td><td>常见用途</td></tr><tr><td>FP32（单精度）</td><td> 4GB</td><td>训练、高精度推理</td></tr><tr><td>FP16（半精度）</td><td> 2GB</td><td>训练、推理</td></tr><tr><td>BF16</td><td> 2GB</td><td>训练、推理</td></tr><tr><td>INT8</td><td> 1GB</td><td>量化推理</td></tr><tr><td>INT4</td><td> 0.5GB</td><td>低精度量化推理</td></tr></table>\n</div>\n<p>模型权重 = 参数量 × 该精度下 1B 参数存储占用量。以 7B 模型、FP16 精度推理为例，模型权重占用约为：7 × 2GB ≈ 14GB。</p>\n\n<h3 id=\"c8-s8\">先看参数量，再看加载精度</h3>\n\n<p>模型推理阶段除了加载模型权重，还需要额外的资源用于 KV Cache、运行框架以及临时计算。</p>\n\n<p>KV Cache 用于保存生成过程中已经计算过的 Token 相关信息，避免重复计算。它的占用大小与模型结构、上下文长度和并发请求数量有关。上下文越长，需要保存的信息越多；同时处理的请求越多，KV Cache 的占用也会增加。</p>\n\n<p>推理资源占用的估算公式：</p>\n\n<p>CPU 推理所需内存 ≈ 模型权重 + 框架及临时开销。</p>\n\n<p>GPU 推理所需显存 ≈ 模型权重 + KV Cache + 框架及临时开销。</p>\n\n<p>其中权重是静态的，框架开销基本恒定，变量全在 KV Cache。在长序列场景下，需要预留 KV Cache 的空间。</p>\n\n<h3 id=\"c8-s9\">同一个模型，训练为什么更占资源？</h3>\n\n<p>模型训练阶段的资源需求高于推理阶段。除了保存模型权重，还需要存储梯度、优化器状态以及训练过程中的中间结果，需要更多资源。训练资源需求取决于优化器类型和训练策略。</p>\n\n<p>（1）完整模型训练</p>\n\n<p>对于完整模型训练，需要同时存储模型权重、梯度、优化器状态、激活值。以采用 Adam 优化器、FP16 精度的完整训练为例，每 1B 参数约需：</p>\n\n<div class=\"table-scroll\"><table><tr><td>组成部分</td><td>每 1B 参数占用</td></tr><tr><td>模型权重（FP16）</td><td>2 GB</td></tr><tr><td>梯度（FP16）</td><td>2 GB</td></tr><tr><td>优化器状态</td><td>8 GB</td></tr><tr><td>FP32 权重副本</td><td>4 GB</td></tr><tr><td>小计（不含激活值）</td><td>16 GB</td></tr></table>\n</div>\n<p>注：Adam 优化器需要保存两份 FP32 精度的状态（动量和方差），因此优化器状态占 8GB；FP32 权重副本用于优化器参数更新。</p>\n\n<p>完整训练初步估算公式为：</p>\n\n<p>GPU 训练所需显存 ≈ 参数量 × 16 Byte + 激活值开销</p>\n\n<p>CPU 训练所需内存 ≈ 参数量 × 16 Byte + 激活值开销</p>\n\n<p>在 GPU 训练受限于显存容量，CPU 上训练则受限于计算速度。如 7B 模型采用 FP16 在 GPU 上进行完整训练，至少需要 7B × 16 Byte ≈ 112GB 显存，加上激活值后实际需求会更大。</p>\n\n<p>（2）参数高效微调（如 LoRA）</p>\n\n<p>对于大模型微调，实际应用中更常采用 LoRA（Low-Rank Adaptation）等参数高效微调方法。LoRA 训练时不更新原模型全部参数，而是冻结原模型权重，仅训练新增的低秩适配参数，显存需求明显降低。</p>\n\n<p>LoRA 训练显存 ≈ 基础模型权重 + LoRA 参数 + LoRA 参数梯度和优化器状态 + 激活值。</p>\n\n<p>其中，基础模型权重只需要加载，不参与更新；LoRA 新增参数量仅为原模型的 0.1%~1%，其梯度与优化器开销几乎忽略不计。实际显存主要取决于模型权重、上下文长度、批次大小以及训练框架优化方式，LoRA 微调相比完整训练可以显著降低显存需求。</p>\n\n<p>例如，使用 FP16 加载 7B 模型，模型权重占 4GB。在单张 24GB 显存的 GPU 上，LoRA 微调通常可以运行（需适当调整批次大小和上下文长度）。若使用 QLoRA（将基础模型量化为 INT4），可以继续降低显存占用，从而在更有限的硬件环境下完成微调。</p>\n","imageCount":0,"attachmentCount":0,"discussionId":"chapter-8"},{"id":"chapter-9","number":9,"title":"笔记本也能跑开源模型，先从 Ollama 开始","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-09.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-09.html","minutes":5,"status":"ready","headings":[{"id":"c9-s1","text":"为什么想把模型放在自己的电脑上？","level":"h2"},{"id":"c9-s2","text":"先看看笔记本的系统和硬件","level":"h2"},{"id":"c9-s3","text":"选自己的操作系统，安装 Ollama","level":"h2"},{"id":"c9-s4","text":"Windows 下安装 Ollama","level":"h3"},{"id":"c9-s5","text":"ModelScope Notebook 安装 Ollama","level":"h3"},{"id":"c9-s6","text":"Linux 下安装 Ollama","level":"h3"},{"id":"c9-s7","text":"MAC 下安装 Ollama","level":"h3"},{"id":"c9-s8","text":"也可以从魔搭下载模型，用 Ollama 启动","level":"h2"}],"html":"<p>模型在本地或者云端部署，比如，本地设备包括笔记本、台式机，以及其他端侧设备等等，</p>\n\n<p>同时本地运行也有不同的工具选择，Ollama、以及 vLLM、SGLang 等框架。</p>\n\n<p></p>\n\n<p>本章，我们先把 Ollama 的基本用法讲清楚。<b>其他端侧设备的适配，以及 vLLM、SGLang 等框架的使用，会在后续补充。</b></p>\n\n<p></p>\n\n<h2 id=\"c9-s1\">为什么想把模型放在自己的电脑上？</h2>\n\n<p>通过 API 调用大模型是最省心的做法：请求发过去，模型计算好把结果回传，用户不需要采购 GPU，也不用维护底层环境。</p>\n\n<p>但 API 模式也有绕不开的限制，主要表现在以下几个方面，</p>\n\n<ul><li>数据安全，核心文档、客户隐私和内部业务数据，无法直接传给第三方公有云。</li><li>离线可用，内网专线等生产环境根本无法连接外网。</li><li>成本控制：当调用量大且稳定时，自己搭服务器的综合成本通常会比一直按 Token 付费更划算。</li></ul>\n\n<p>所以，如果业务涉及数据敏感、要求纯离线运行、调用量较大，本地部署就是更合适的选择。</p>\n\n<p></p>\n\n<h2 id=\"c9-s2\">先看看笔记本的系统和硬件</h2>\n\n<p>Ollama 是目前用起来比较顺手的工具之一，把模型下载、本地管理和接口服务都打包好了。用户不需要自己写代码去加载模型，只需几行命令就能直接启动模型。</p>\n\n<p>模型能否在本地顺利运行，很大程度上取决于计算机的硬件资源。Ollama 对常见的硬件平台提供了较好的支持，支持 Windows、Linux 和 macOS 等常见操作系统。无论是普通 CPU、GPU，还是搭载 Apple Silicon 的 Mac 电脑，都可以用来运行模型。</p>\n\n<p><b>CPU 运行，</b>即使电脑没有独立显卡，也可以通过 CPU 运行模型。由于大模型推理需要进行大量计算，使用 CPU 时生成速度通常比较慢，因此更适合参数规模较小的模型，或者对响应速度要求不高的场景。</p>\n\n<p><b>GPU 运行，</b>这是目前比较常见的大模型运行方式。GPU 具有较强的并行计算能力，可以明显提升模型的生成速度。模型越大，需要的显存通常也越多，因此选择模型时需要考虑显卡是否有足够的显存。</p>\n\n<p><b>Apple Silicon 运行，</b>苹果 M 系列芯片采用统一内存设计，CPU 和 GPU 可以使用同一块内存。对于 Mac 用户来说，如果设备具有 32GB、64GB 或者更大的统一内存，就可以尝试运行一些参数规模较大的量化模型。</p>\n\n<h2 id=\"c9-s3\">选自己的操作系统，安装 Ollama</h2>\n\n<h3 id=\"c9-s4\">Windows 下安装 Ollama</h3>\n\n<p>1）首先在在 Ollama 官网，<a href=\"https://ollama.com/download\" target=\"_blank\" rel=\"noopener noreferrer\">Download Ollama</a> 下载对应的 windows 版本</p>\n\n<img src=\"assets/manuscript-20260914/c09-562fed6f376b07.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>2)下载完成后，直接点击安装，点击 continue</p>\n\n<img src=\"assets/manuscript-20260914/c09-84fbbdb032e197.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>3) 安装完成后，显示，此时 Ollama 服务已经启动</p>\n\n<img src=\"assets/manuscript-20260914/c09-4a2c1cee33adac.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>4）在 cmd 中，启动想要加载的模型，比如：qwen3.5:2b，直接在终端中输入命令</p>\n\n<pre data-language=\"Plain Text\"><code>ollama run qwen3.5:2b</code></pre>\n\n<p>页面输出如下，说明模型启动成功，此时可以直接在客户端中对话，也可以直接调用接口测试模型</p>\n\n<img src=\"assets/manuscript-20260914/c09-3ec90eccd2e903.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>测试接代码如下：</p>\n\n<pre data-language=\"JSON\"><code>from openai import OpenAI\n\nclient = OpenAI(\n    base_url=\"http://127.0.0.1:11434/v1\",\n    api_key=\"ollama\",  \n)\n\nresponse = client.chat.completions.create(\n    model=\"qwen3.5:2b\", \n    messages=[\n        {\n            \"role\": \"system\",\n            \"content\": \"你是一个有帮助的中文助手。\",\n        },\n        {\n            \"role\": \"user\",\n            \"content\": \"你是谁？\",\n        },\n    ],\n    temperature=0,\n    max_tokens=512,\n)\n\nprint(response.choices[0].message.content)</code></pre>\n\n<p>结果显示如下：</p>\n\n<img src=\"assets/manuscript-20260914/c09-b655606fa9a68a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c9-s5\">ModelScope Notebook 安装 Ollama</h3>\n\n<p>1）在 Notebook 中安装 Ollama，首先下载安装包，执行下面命令</p>\n\n<pre data-language=\"Plain Text\"><code>!modelscope download --model=modelscope/ollama-linux --local_dir ./ollama-linux  </code></pre>\n\n<img src=\"assets/manuscript-20260914/c09-dc92f54670f6c7.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>2）安装包下载后，进入 <code>ollama-linux</code> 文件夹，执行下面命令进行安装 </p>\n\n<pre data-language=\"Plain Text\"><code>%cd ollama-linux\n</code></pre>\n\n<img src=\"assets/manuscript-20260914/c09-0876f501356221.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<pre data-language=\"Plain Text\"><code>sudo chmod 777 ./ollama-modelscope-install.sh\n./ollama-modelscope-install.sh</code></pre>\n\n<p>安装完成会显示 API 接口信息，默认 127.0.0.1:11434，如果显示如下信息，说明安装完成。</p>\n\n<img src=\"assets/manuscript-20260914/c09-63cd320bae0306.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c9-s6\">Linux 下安装 Ollama</h3>\n\n<p>linux 下的安装步骤和以上在 ModelScope Notebook 中安装 Ollama 的方式一样。</p>\n\n<h3 id=\"c9-s7\">MAC 下安装 Ollama</h3>\n\n<p>1）首先在在 Ollama 官网，<a href=\"https://ollama.com/download\" target=\"_blank\" rel=\"noopener noreferrer\">Download Ollama</a> 下载对应的 macOS 版本</p>\n\n<img src=\"assets/manuscript-20260914/c09-41a856385d9791.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>2)下载完成后，双击下载文件，将 ollama 拖入到 applications 中</p>\n\n<img src=\"assets/manuscript-20260914/c09-94e39bfe8d6df6.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>3) 安装完成后，显示，此时 Ollama 服务已经启动</p>\n\n<img src=\"assets/manuscript-20260914/c09-8f8ec60e9899c4.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>4）在 cmd 中，启动想要加载的模型，比如：qwen3.5:2b，直接在终端中输入命令</p>\n\n<pre data-language=\"Plain Text\"><code>ollama run qwen3.5:2b</code></pre>\n\n<p>页面输出如下，说明模型启动成功，此时可以直接在客户端中对话，也可以直接调用接口测试模型</p>\n\n<img src=\"assets/manuscript-20260914/c09-60177d3b2d5627.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>在客户端也可以进行 chat</p>\n\n<img src=\"assets/manuscript-20260914/c09-9c52eab89512b0.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h2 id=\"c9-s8\">也可以从魔搭下载模型，用 Ollama 启动</h2>\n\n<p>Ollama 提供了很多模型库，我们可以在 ModelScope 中查找我们需要的模型，通过 Ollama 启动。</p>\n\n<p>1）启动 Ollama 服务</p>\n\n<p>这种常驻服务，Jupyter 的单个内核一次只能执行一个单元，所以导致后面的单元不太好执行，所以此部分在终端中执行，打开 Notebook 之后，直接进入工作区，下面有终端字样，点击即可。</p>\n\n<img src=\"assets/manuscript-20260914/c09-79e4a22935cf09.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>在终端中输入：</p>\n\n<pre data-language=\"Plain Text\"><code>ollama serve</code></pre>\n\n<img src=\"assets/manuscript-20260914/c09-7cafe12d23d148.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>2）接下来运行我们想要启动的模型，比如：<code>Qwen3-4B-GGUF</code>，模型第一次运行时，需要先从模型仓库下载对应文件。这个命令也需要在终端执行，可以打开一个新的终端，执行命令如下：</p>\n\n<img src=\"assets/manuscript-20260914/c09-ffba51d8bbbcc6.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<pre data-language=\"Plain Text\"><code> ollama run modelscope.cn/unsloth/Qwen3-4B-GGUF</code></pre>\n\n<img src=\"assets/manuscript-20260914/c09-9d3696215d29ff.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>此时模型已经启动好，可以调用接口测试模型接口是否连通，代码如下：</p>\n\n<pre data-language=\"JSON\"><code>import requests\nurl = \"http://127.0.0.1:11434/v1/chat/completions\"\ndata = {\n    \"model\": \"modelscope.cn/unsloth/Qwen3-4B-GGUF\",\n    \"messages\": [\n        {\n            \"role\": \"user\",\n            \"content\": \"你好，请介绍一下自己\"\n        }\n    ],\n    \"max_tokens\": 100\n}\n\nresponse = requests.post(url, json=data)\n\nprint(\"状态码:\", response.status_code)\nprint(response.text)</code></pre>\n\n<p>模型调用成功，会输出对应的模型结果。</p>\n\n<img src=\"assets/manuscript-20260914/c09-07612cad9b3680.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>此章节所涉及的全部实验过程，具体可参照：https://modelscope.cn/gallery/liucong/b1ef397b-fe80-4fb9-812f-969fa25ea44c</p>\n\n<p></p>\n","imageCount":18,"attachmentCount":0,"discussionId":"chapter-9"},{"id":"chapter-10","number":10,"title":"把模型放到云端跑，用 Notebook 试试 CPU 和 GPU","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-10.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-10.html","minutes":5,"status":"ready","headings":[{"id":"c10-s1","text":"打开 Notebook，选择这次要用的运行实例","level":"h2"},{"id":"c10-s2","text":"先用 CPU，判断一句评价是正面还是负面","level":"h2"},{"id":"c10-s3","text":"换到 GPU，把文字描述变成图片","level":"h2"}],"html":"<p>笔记本上的模型跑起来了，想试更大的模型，或者做一次图片生成，却不一定有合适的硬件。还可以把计算放到云端，让服务器来承担这些工作。</p>\n\n<p></p>\n\n<p>云服务器可以按需要配置。这里继续使用魔搭 Notebook，直接在浏览器里选择 CPU 或 GPU 运行实例，下载模型并执行代码。页面开在自己的电脑上，模型运行在连接的云端实例里。</p>\n\n<p></p>\n\n<p>这一篇做两个实验。先用 CPU 运行 0.5B 级模型，判断一句评价的情感；再用 GPU 运行图像生成模型，把文字描述变成图片。分别展示两种资源下的使用方法，任务和模型都不同，不能用运行耗时直接比较 CPU 与 GPU 谁更快。</p>\n\n<p></p>\n\n<p><b>其他云服务器资源，我们会后续补充。</b></p>\n\n<p></p>\n\n<h2 id=\"c10-s1\">打开 Notebook，选择这次要用的运行实例</h2>\n\n<p>从魔搭模型页面进入“Notebook 快速开发”，再点击“连接运行时”。这次先选择 CPU 实例完成情感分析，再切换到 GPU 实例做图片生成。Notebook 的页面操作可以参照【30 分钟带你快速看到第一个结果】内容</p>\n\n<p></p>\n\n<p>切换实例后，原来的 Python 进程和已加载模型不会自动搬到新实例里，需要在当前环境重新检查依赖，并从头执行对应实验的代码。</p>\n\n<p></p>\n\n<h2 id=\"c10-s2\">先用 CPU，判断一句评价是正面还是负面</h2>\n\n<p>以 <a href=\"https://www.modelscope.cn/models/Qwen/Qwen2.5-0.5B-Instruct\" target=\"_blank\" rel=\"noopener noreferrer\">Qwen/Qwen2.5-0.5B-Instruct</a> 模型为例，介绍如何在 CPU 上使用 0.5B 级的模型做情感分析任务。</p>\n\n<p>启动 CPU 实例后，进入 notebook 开发环境。模型加载及推理代码如下：</p>\n\n<pre data-language=\"Python\"><code>from modelscope import AutoModelForCausalLM, AutoTokenizer\nmodel_name = \"Qwen/Qwen2.5-0.5B-Instruct\"\nmodel = AutoModelForCausalLM.from_pretrained(\n    model_name,\n    torch_dtype=\"auto\",\n    device_map=\"cpu\" \n    # device_map=\"cpu\" # gpu推理\n)\nprint(f\"模型所在设备: {model.device}\")\ntokenizer = AutoTokenizer.from_pretrained(model_name)\n\n# 提示词\nprompt = f\"\"\"你是一个文本情感分类专家。请将以下用户评价分类为【正面】、【负面】或【中性】。仅输出这三个词之一，不要包含任何解释、标点或额外文字。\n评价内容：今天的电影真难看，浪费时间。\n分类结果：\"\"\"\n\nmessages = [\n    {\"role\": \"system\", \"content\": \"You are Qwen, created by Alibaba Cloud. You are a helpful assistant.\"},\n    {\"role\": \"user\", \"content\": prompt}\n]\ntext = tokenizer.apply_chat_template(\n    messages,\n    tokenize=False,\n    add_generation_prompt=True\n)\nmodel_inputs = tokenizer([text], return_tensors=\"pt\").to(model.device)\n\ngenerated_ids = model.generate(\n    **model_inputs,\n    max_new_tokens=512\n)\ngenerated_ids = [\n    output_ids[len(input_ids):] for input_ids, output_ids in zip(model_inputs.input_ids, generated_ids)\n]\n\nresponse = tokenizer.batch_decode(generated_ids, skip_special_tokens=True)[0]\nprint(\"response: \", response )</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c10-13fa64bd7dfeaa.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h2 id=\"c10-s3\">换到 GPU，把文字描述变成图片</h2>\n\n<p>使用 <a href=\"https://www.modelscope.cn/models/Tongyi-MAI/Z-Image-Turbo\" target=\"_blank\" rel=\"noopener noreferrer\">Tongyi-MAI/Z-Image-Turbo</a> 模型演示如何在 GPU 上加载模型，完成图片生成任务。</p>\n\n<p>启动 GPU 实例后，进入 notebook 开发环境。</p>\n\n<p>按照模型卡片，需要在终端安装最新版 diffusers。</p>\n\n<pre data-language=\"Plain Text\"><code>!pip3 install git+https://github.com/huggingface/diffusers</code></pre>\n\n<p>安装完成：</p>\n\n<img src=\"assets/manuscript-20260914/c10-187e1d063093cf.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>模型加载及推理代码如下：</p>\n\n<pre data-language=\"Python\"><code>import torch\nfrom modelscope import ZImagePipeline\n\n# 1. 模型加载\npipe = ZImagePipeline.from_pretrained(\n    \"Tongyi-MAI/Z-Image-Turbo\",\n    torch_dtype=torch.bfloat16,\n    low_cpu_mem_usage=False,\n)\npipe.to(\"cuda\")\nprint(f\"模型所在设备: {pipe.device}\")\n\nprompt = \"一位年轻的中国女子，身着一袭红色汉服，衣袂间绣满精致繁复的纹样。她的妆容精致无瑕，额间一抹红色花钿，更添几分古典风韵。高耸的发髻华美端庄，上缀金色凤冠，簪以红花与珠串，摇曳生姿。她手中执一柄圆形折扇，扇面上绘有仕女、古树与飞鸟，意境悠然。一盏霓虹闪电造型的灯盏悬于她伸出的左掌之上，散发着明亮的暖黄色光芒。夜色温柔，灯火朦胧，远处西安大雁塔的塔身层层叠叠，隐现于柔和的光晕之中，背景光斑斑斓，如梦似幻。\"\n# 2. 生成图片\nimage = pipe(\n    prompt=prompt,\n    height=1024,\n    width=1024,\n    num_inference_steps=9, \n    guidance_scale=0.0, \n    generator=torch.Generator(\"cuda\").manual_seed(42),\n).images[0]\nprint(\"图片已生成\")\n\nimage.save(\"image-古风.png\")\nprint(\"图片保存成功！\")</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c10-89687fab7e866d.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>生成的图片：</p>\n\n<img src=\"assets/manuscript-20260914/c10-47e02eaf05ba4a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>本章代码及相关文件见：https://www.modelscope.cn/gallery/liucong/4a8eb492-54dc-4062-82c0-65b17fd94d5f</p>\n\n<p></p>\n","imageCount":4,"attachmentCount":0,"discussionId":"chapter-10"},{"id":"chapter-11","number":11,"title":"模型需要资源太多，量化能帮上什么忙？","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-11.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-11.html","minutes":4,"status":"ready","headings":[{"id":"c11-s1","text":"量化之后，模型能省多少空间？","level":"h2"},{"id":"c11-s2","text":"想把模型量化，有哪些做法？","level":"h2"},{"id":"c11-s3","text":"GGUF 文件里，装了些什么？","level":"h2"},{"id":"c11-s4","text":"Q4、Q8 这些名字，该怎么看？","level":"h2"},{"id":"c11-s5","text":"同一个模型，我该下载哪个版本？","level":"h2"}],"html":"<p>模型下载好了，运行时却提示内存/显存不足，这大概是很多人尝试本地模型时最不想看到的结果。</p>\n\n<p></p>\n\n<p>前面介绍过，模型运行时需要把权重加载到内存或显存中，生成回答还要占用额外的空间。如果手头的设备装不下模型，除了换一个参数量更小的模型，还可以看看有没有量化版本。</p>\n\n<p></p>\n\n<h2 id=\"c11-s1\">量化之后，模型能省多少空间？</h2>\n\n<p>模型的权重由大量数值组成，保存每个数值都需要一定的空间。量化（Quantization）通过降低这些数值的表示精度，减少模型的资源占用。例如，将原本以 16 bit 浮点数保存的权重，转换为以 8 bit 或 4 bit 为主的低精度表示。参数数量通常没有变，每个参数占用的空间变小了。</p>\n\n<p></p>\n\n<p>以一个 70 亿参数的模型为例，只计算权重本身，忽略量化附加信息，可以得到下面这组粗略估算。</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col></colgroup><tbody><tr><td><p>权重表示方式</p></td><td><p>理论权重大小</p></td></tr><tr><td><p>16 bit</p></td><td><p>约 14 GB</p></td></tr><tr><td><p>8 bit</p></td><td><p>约 7 GB</p></td></tr><tr><td><p>4 bit</p></td><td><p>约 3.5 GB</p></td></tr></tbody></table>\n</div>\n<p>这里的 GB 按十进制计算，实际量化文件还会保存缩放系数等信息，也可能混用不同精度，因此大小会与估算有差异。</p>\n\n<p>对普通用户来说，量化最直接的好处是模型文件更小，下载和存储更省空间，运行时权重占用的内存或显存也更少。在硬件和推理框架支持合适的情况下，量化还可能提升推理速度。</p>\n\n<p>不过，文件缩小到原来的四分之一，并不意味着回答速度也会提高四倍。</p>\n\n<p></p>\n\n<p>降低精度也可能影响回答质量，具体影响取决于模型、量化方法和任务。选择量化版本时，除了确认能运行，还要用自己关心的问题试一试，看看回答是否仍然可靠。</p>\n\n<p></p>\n\n<h2 id=\"c11-s2\">想把模型量化，有哪些做法？</h2>\n\n<p>对于已经下载或微调完成的模型，常见做法是<b>训练后量化（PTQ）</b>，也就是在模型训练完成后，再把权重等数值转换为更低精度的表示。有些方法需要准备一小批有代表性的数据，用来校准量化过程，尽量减小误差。</p>\n\n<p>另一类是<b>量化感知训练（QAT）</b>，在训练过程中模拟量化带来的影响，让模型提前适应低精度表示。</p>\n\n<p></p>\n\n<p><b>本章先讲本地运行中常见的 GGUF 量化模型，帮助大家看懂文件、区分版本并作出选择。其他量化方法的具体操作，后续再补充。</b></p>\n\n<p></p>\n\n<h2 id=\"c11-s3\">GGUF 文件里，装了些什么？</h2>\n\n<p>使用 Ollama 运行模型时，经常会看到 GGUF、Q4、Q8 等名称。这些名称主要与模型的存储格式和量化方式有关，也是大模型能够在个人电脑上运行的重要原因。</p>\n\n<p>2023 年 8 月份，Georgi Gerganov 推出 GGUF 格式，主要优势包含单文件部署、可扩展、支持内存映射、信息完整、易于使用。一个 GGUF 文件包括文件头、元数据键值对和张量信息等。这些组成部分共同定义了模型的结构和行为。具体如下所示：GGUF 是一种用于大模型推理的模型文件格式，目前被 llama.cpp、Ollama 等本地推理工具广泛使用。Ollama 将 GGUF 格式、模型量化和模型加载等过程进行了封装，用户无需自己转换模型格式，直接下载和运行已经量化好的模型。一个 GGUF 文件中并不只有模型参数，还包含模型的基本信息、模型结构以及张量等内容。GGUF 通过统一的文件格式将这些信息组织在一起，方便 llama.cpp 等推理工具直接读取和加载。其基本结构如下图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c11-31ba57f7256006.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>ModelScope 上也是支持展示结构化 GGUF 文件，如下图所示，推理工具加载 GGUF 文件时，可以直接读取这些信息并加载模型，不需要再单独准备多个模型配置文件。</p>\n\n<img src=\"assets/manuscript-20260914/c11-288687baac9be9.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>GGUF 本身只是一种模型文件格式，并不会直接降低模型的内存和显存占用。真正降低模型资源占用的是量化（Quantization）。</p>\n\n<p></p>\n\n<h2 id=\"c11-s4\">Q4、Q8 这些名字，该怎么看？</h2>\n\n<p>在 llama.cpp 和 GGUF 模型中，Q4、Q5、Q8、Q4_K_M 表示不同的量化名称。其中，Q 表示量化，后面的数字表示主要采用的量化位数，例如 Q4 表示以 4bit 量化为主，Q8 表示以 8bit 量化为主。</p>\n\n<p>需要注意的是，Q4 并不代表模型中的所有参数都统一使用 4bit 表示。以常见的 Q4_K_M 为例，它属于 llama.cpp 中的一种 K-quant 量化类型。名称中的 Q4 表示以 4 bit 量化为主，K 表示使用 K-quant 量化方案，而 M 表示该方案中的 Medium 配置。实际模型中，不同张量可能使用不同的量化精度，而不是所有参数统一采用 4bit 表示，具体如下图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c11-d589636edb4494.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h2 id=\"c11-s5\">同一个模型，我该下载哪个版本？</h2>\n\n<p>选择 GGUF 模型时，先确认推理工具支持这个模型，再看具体的量化版本。较低的量化精度通常能减少内存和显存占用，让模型更容易在普通设备上运行，但可能损失一部分模型效果；较高的量化精度通常能保留更多原始模型的能力，同时也需要更多硬件资源。</p>\n\n<p></p>\n\n<p>如果一个版本已经接近设备的内存或显存上限，就要考虑换一个占用更小的版本，给上下文和运行过程留出余量。如果资源足够，可以用相同的问题比较不同版本的回答，尤其关注自己要用的任务，例如信息抽取、代码生成或规定格式的输出。</p>\n\n<p></p>\n\n<p>先确认能稳定运行，再检查回答质量。这样选出来的量化版本，才更适合自己的设备和用途。</p>\n\n<p></p>\n","imageCount":3,"attachmentCount":0,"discussionId":"chapter-11"},{"id":"chapter-12","number":12,"title":"业务素材这么多，怎么变成能训练的数据？","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-12.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-12.html","minutes":33,"status":"ready","headings":[{"id":"c12-s1","text":"业务要学，通用能力也得保留","level":"h2"},{"id":"c12-s2","text":"业务自己的资料，该怎么用？","level":"h2"},{"id":"c12-s3","text":"只有文档没有问答，让模型帮忙合成数据","level":"h2"},{"id":"c12-s4","text":"生成的数据，先别急着拿去训练","level":"h2"},{"id":"c12-s5","text":"答案有没有和原文对不上？","level":"h3"},{"id":"c12-s6","text":"原文没写的，别让模型自己编","level":"h3"},{"id":"c12-s7","text":"换个说法，可能还是同一道题","level":"h3"},{"id":"c12-s8","text":"不同地方来的数据，先整理到一起","level":"h2"},{"id":"c12-s9","text":"先收集需要的内容，把字段统一好","level":"h3"},{"id":"c12-s10","text":"数据需要先清理一遍","level":"h3"},{"id":"c12-s11","text":"重复的数据，有必要都留着吗？","level":"h3"},{"id":"c12-s12","text":"相同数据去重","level":"h4"},{"id":"c12-s13","text":"相似数据去重","level":"h4"},{"id":"c12-s14","text":"真实信息，训练前先脱敏","level":"h3"},{"id":"c12-s15","text":"识别敏感信息方法","level":"h4"},{"id":"c12-s16","text":"脱敏方法","level":"h4"},{"id":"c12-s17","text":"最后一步，整理成训练需要的格式","level":"h2"},{"id":"c12-s18","text":"Alpaca 格式","level":"h3"},{"id":"c12-s19","text":"Messages 格式","level":"h3"},{"id":"c12-s20","text":"ShareGPT 格式","level":"h3"},{"id":"c12-s21","text":"如何选择数据格式","level":"h3"}],"html":"<p>我们在训练模型之前，往往会遇到这样一个问题：</p>\n\n<p>我有很多原始资料、产品说明、操作手册、业务规范，各有各的格式，同时内容也比较零散，</p>\n\n<p><b>我要如何把这些数据用起来？</b></p>\n\n<p>并且如果我直接用领域或者场景的数据，训练完的模型，还有没有回答通用知识的能力？</p>\n\n<p>所以，关键点是我们如何把所有的素材变成模型可用的训练数据。</p>\n\n<p></p>\n\n<p>同时数据的清洗、去重和脱敏也是必不可少的，我们可能蒸馏了数据，但是需要适配我们想要的结果，</p>\n\n<p>就好比，训练一个模型，如果自我认知数据都没有训练好的话，可能它会回答它是 ChatGPT 模型、是 Claude 模型等等。</p>\n\n<p></p>\n\n<p>数据是一切的基础，这一章我们从数据来源开始，看看这些零散资料怎样经过合成、清洗、去重和脱敏，最后变成可以交给模型训练的数据。</p>\n\n<p></p>\n\n<h2 id=\"c12-s1\">业务要学，通用能力也得保留</h2>\n\n<p>从头采集和标注训练数据需要投入较多时间和成本。对于文本分类、问答、文本生成等常见任务，可以寻找合适的公开数据集加入训练。</p>\n\n<p>常见的公开数据集包括：</p>\n\n<ul><li>文本分类与推理：GLUE、SuperGLUE、CLUE 等；</li><li>阅读理解与问答：SQuAD、CMRC 等；</li><li>文本摘要：CNN/DailyMail 等；</li><li>对话与指令微调：Alpaca、ShareGPT、OpenOrca 等；</li><li>多模态任务：COCO 等图文数据集。</li></ul>\n\n<p>除了从数据集官方网站获取数据，还可以通过魔搭 Dataset Hub 查找和使用开源数据集。Dataset Hub 提供了数据集搜索、在线预览和下载等功能，部分数据集还可以通过 ModelScope SDK 直接加载，方便后续处理和训练。具体数据集下载方法见【数据，是你要做开源模型微调的基石】章节内容。</p>\n\n<img src=\"assets/manuscript-20260914/c12-999c072626454b.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>选择开源数据集时，不仅要看数据内容是否与训练任务匹配，还需要检查数据集的样本数量、字段格式、标签类型和数据质量，判断是否满足训练要求。魔搭提供的数据预览功能可以帮助快速查看数据集的字段和样本内容，样例如下：</p>\n\n<img src=\"assets/manuscript-20260914/c12-aa5aa0a312f8d7.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>开源数据主要用于补充模型的通用能力。如果模型需要学习企业内部的产品、术语、业务规则和处理流程，需要准备相应的业务数据。</p>\n\n<h2 id=\"c12-s2\">业务自己的资料，该怎么用？</h2>\n\n<p>业务数据来自企业实际的业务系统和业务资料，与模型最终使用的场景更加接近。通用问答数据可以让模型学习基本的问答方式，而企业内部的问答数据可以进一步让模型学习产品名称、业务术语和处理规则。</p>\n\n<p>常见的业务数据来源包括：</p>\n\n<ul><li>企业文档：产品说明书、操作手册、业务规范、流程文档等，可以从中提取产品知识、业务规则和操作流程；</li><li>客服记录：用户与客服的历史对话、工单和投诉记录，可以用于构造意图识别、问答和对话数据；</li><li>FAQ：由业务人员整理的问题和标准答案，可以直接转换为问答训练数据；</li><li>专家案例：业务专家处理过的典型问题及处理结果，可以用于构造复杂业务场景的训练数据。</li></ul>\n\n<p>业务数据一般不能直接用于模型训练。像一份几十页的业务手册，需要先从 Word、PDF 等文件中提取正文，再根据训练任务整理成问答对、分类样本或指令数据。客服记录中也可能包含系统消息、无效对话和重复内容，需要先进行筛选和整理。</p>\n\n<p>业务数据中还可能包含姓名、电话号码、证件号码、客户信息等敏感内容。在进入训练数据集之前，需要按照企业的数据安全要求进行脱敏，避免将真实敏感信息直接用于模型训练。</p>\n\n<h2 id=\"c12-s3\">只有文档没有问答，让模型帮忙合成数据</h2>\n\n<p>实际项目中，已有数据不一定能够满足所有训练需求。比如某些业务类型只有少量样本，或者已有的文档只有知识，没有可以直接用于训练的问答数据。这时可以使用大模型生成新的训练样本，这种方式称为数据合成。</p>\n\n<p>常见的数据合成方式包括：</p>\n\n<ul><li>指令数据合成：根据已有任务或少量示例，让大模型生成新的指令和回答；</li><li>问答数据合成：输入业务文档，让大模型根据文档生成问题和答案；</li><li>分类数据合成：指定分类标签和样例，让大模型生成相应类别的文本；</li><li>边界数据合成：针对容易混淆的类别生成相似样本，用于加强模型对类别边界的判断能力。</li></ul>\n\n<p>例如，企业只有产品操作手册，没有问答数据。需要先将手册按照章节或段落进行切分，利用大模型根据段落内容生成若干问题和答案。检查结果后，整理成问答训练数据。</p>\n\n<p>数据合成可以减少人工编写训练样本的工作量，也可以补充原始数据不足的场景。但合成数据生成后不能直接用于训练。大模型可能生成错误答案、重复样本，也可能虚构不存在的产品或业务规则，还需要进行质量检查。</p>\n\n<img src=\"assets/manuscript-20260914/c12-9a4f0c9a0e80d2.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h2 id=\"c12-s4\">生成的数据，先别急着拿去训练</h2>\n\n<p>合成数据的质量会直接影响模型训练效果。在将合成数据加入训练集之前，需要检查生成内容是否正确，并过滤错误、虚构、重复和过于相似的数据。</p>\n\n<p></p>\n\n<h3 id=\"c12-s5\">答案有没有和原文对不上？</h3>\n\n<p>如果合成数据是根据业务文档生成的，需要检查生成内容是否与原文一致。</p>\n\n<p>例如，原始业务文档规定：</p>\n\n<blockquote><p>退款申请需要经过人工审核，审核通过后才能退款。</p></blockquote>\n\n<p>模型生成的问答数据：</p>\n\n<blockquote><p>问：退款是否需要审核？</p><p>答：退款提交后即可自动退款，无需人工审核。</p></blockquote>\n\n<p>该答案与业务规则冲突，如果用于训练，模型可能学习到错误的业务流程。</p>\n\n<p>实际处理中，可以利用大模型对合成数据进行一致性评估。将原始资料、问题和答案同时输入模型，判断答案是否与资料一致。对于判断为错误的数据，可以删除或重新生成。处理代码如下：</p>\n\n<pre data-language=\"Python\"><code>from modelscope import AutoModelForCausalLM, AutoTokenizer\nimport torch\nmodel_id = \"Qwen/Qwen3-4B\"\ntokenizer = AutoTokenizer.from_pretrained(\n    model_id,\n    trust_remote_code=True\n)\n\nmodel = AutoModelForCausalLM.from_pretrained(\n    model_id,\n    torch_dtype=torch.float16,\n    device_map=\"auto\",\n    trust_remote_code=True\n)\n\ndocument = \"\"\"退款申请需要经过人工审核，审核通过后才能退款。\"\"\"\nquestion = \"退款是否需要审核？\"\nanswer = \"退款提交后即可自动退款，无需人工审核。\"\nprompt = f\"\"\"\n请根据参考资料判断下面的问答数据是否正确。\n\n参考资料：\n{document}\n\n问题：\n{question}\n\n答案：\n{answer}\n\n请严格按照以下 JSON 格式输出，不要输出其他内容：\n{{\"result\": \"通过/不通过\",\"reason\": \"判断原因\"}}\n\"\"\"\n\nmessages = [{\"role\": \"user\", \"content\": prompt}]\ntext = tokenizer.apply_chat_template(messages, tokenize=False, add_generation_prompt=True, enable_thinking=False)\ninputs = tokenizer(text, return_tensors=\"pt\").to(model.device)\noutputs = model.generate(**inputs, max_new_tokens=512)\nresult = tokenizer.decode(\n    outputs[0][inputs.input_ids.shape[1]:],\n    skip_special_tokens=True\n)\n\nprint(\"判断结果：\", result)</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c12-05036d32782086.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h3 id=\"c12-s6\">原文没写的，别让模型自己编</h3>\n\n<p>大模型生成数据时，可能会产生原始资料中不存在的信息，这类问题通常称为幻觉。例如，生成不存在的产品名称、产品参数或业务流程。</p>\n\n<p>例如，原始资料：</p>\n\n<blockquote><p>产品最大支持 100GB 存储空间。</p></blockquote>\n\n<p>生成数据：</p>\n\n<blockquote><p>问：产品是否支持自动扩容？</p><p>答：支持，当存储空间不足时，系统会自动扩容</p></blockquote>\n\n<p>原始资料中没有说明产品是否支持自动扩容，因此生成答案缺少资料依据，属于虚构内容。</p>\n\n<p>对于根据业务资料生成的数据，可以利用大模型进行依据检查，判断答案中的关键信息是否能够从原始资料中得到支持。对于缺少依据或无法确认的数据，可以删除、重新生成或进行人工复核。涉及重要业务规则时，还可以由业务人员进行抽检。处理代码如下：</p>\n\n<pre data-language=\"SQL\"><code>from modelscope import AutoModelForCausalLM, AutoTokenizer\nimport torch\nmodel_id = \"Qwen/Qwen3-4B\"\ntokenizer = AutoTokenizer.from_pretrained(\n    model_id,\n    trust_remote_code=True\n)\n\nmodel = AutoModelForCausalLM.from_pretrained(\n    model_id,\n    torch_dtype=torch.float16,\n    device_map=\"auto\",\n    trust_remote_code=True\n)\n\ndocument = \"产品最大支持100GB存储空间。\"\nquestion = \"产品最大支持多少存储空间？\"\nanswer = \"产品最大支持500GB存储空间。\"\n\nprompt = f\"\"\"\n请根据参考资料判断下面的答案是否包含虚构信息。\n\n参考资料：\n{document}\n\n问题：\n{question}\n\n答案：\n{answer}\n\n请严格按照以下 JSON 格式输出，不要输出其他内容：\n{{\"result\": \"通过/不通过\",\"reason\": \"判断原因\"}}\n\n判断要求：\n1. 如果答案中的信息能够从参考资料中得到支持，result输出“通过”；\n2. 如果答案包含参考资料中不存在的信息，result输出“不通过”；\n3. 只根据参考资料进行判断，不要补充外部知识。\n\"\"\"\n\nmessages = [{\"role\": \"user\", \"content\": prompt}]\ntext = tokenizer.apply_chat_template(messages, tokenize=False, add_generation_prompt=True, enable_thinking=False)\ninputs = tokenizer(text, return_tensors=\"pt\").to(model.device)\noutputs = model.generate(**inputs, max_new_tokens=512)\nresult = tokenizer.decode(\n    outputs[0][inputs.input_ids.shape[1]:],\n    skip_special_tokens=True\n)\n\nprint(\"判断结果：\", result)</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c12-6882b6e6d81599.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c12-s7\">换个说法，可能还是同一道题</h3>\n\n<p>大模型批量生成数据时，容易产生重复或高度相似的样本。例如：“如何修改登录密码？”、“登录密码怎么修改？”，这些问题的表达方式不同，但内容基本相同。如果类似样本过多，会降低数据集的有效信息量。</p>\n\n<p>在合成数据生成后，需要检查样本之间的重复和相似情况，过滤完全重复的数据，并控制高度相似样本的数量，保留具有代表性的样本。具体的数据去重方法将在下面节介绍。</p>\n\n<p></p>\n\n<h2 id=\"c12-s8\">不同地方来的数据，先整理到一起</h2>\n\n<p>无论数据来自开源数据集、企业业务数据还是大模型合成，在用于模型训练前，都需要进行统一处理。常见的处理过程包括数据采集、数据清洗、数据去重、数据脱敏。</p>\n\n<p></p>\n\n<h3 id=\"c12-s9\">先收集需要的内容，把字段统一好</h3>\n\n<p>数据采集是根据训练任务，从不同数据来源获取所需数据，并进行统一整理。</p>\n\n<p>采集前需要先确定训练任务所需的数据内容。分类任务需要文本和类别标签，问答任务需要问题和答案，指令微调则需要指令和对应的回答。采集时只保留与训练任务相关的数据和字段。</p>\n\n<p>不同来源的数据结构可能不同。如一个数据集使用 question 和 answer 字段，另一个数据集可能使用 query 和 response 字段。采集后需要统一字段名称和数据结构，方便后续处理。</p>\n\n<p>对于缺少必要信息的数据，可以在采集过程中进行补充。比如为分类数据补充类别标签，或者为问答数据整理参考答案。</p>\n\n<h3 id=\"c12-s10\"><b>数据需要先清理一遍</b></h3>\n\n<p>原始数据中可能会包含一些无效或错误内容，需要在训练前进行清洗。</p>\n\n<p>常见的处理包括：删除空白数据和无效记录、去除多余的 HTML 标签和特殊字符、统一文本编码和格式，以及修正明显的 OCR 识别错误等。</p>\n\n<p>数据清洗并不是删除所有特殊内容。代码、公式、单位和标点符号可能本身就是训练内容的一部分，需要根据具体任务决定是否保留。</p>\n\n<p>以 FAQ 数据集为例，原始数据：</p>\n\n<pre data-language=\"Plain Text\"><code>faq_data = [\n    {\n        \"question\": \"  如何修改登录密码？  \",\n        \"answer\": \"&lt;p&gt;进入个人中心，点击“修改密码”。&lt;/p&gt;\"\n    },\n    {\n        \"question\": \"\",\n        \"answer\": \"请联系管理员。\"\n    },\n    {\n        \"question\": \"忘记密码怎么办？\",\n        \"answer\": \"点击“忘记密码”\\n\\n按照提示操作。\"\n    },\n    {\n        \"question\": \"客服电话是多少？\",\n        \"answer\": \"客服电话为 400-123-4567\\xa0\"\n    }\n]</code></pre>\n\n<p>清洗代码如下：</p>\n\n<pre data-language=\"Plain Text\"><code>import re\n\ndef clean_text(text):\n    if not text:\n        return \"\"\n\n    # 去除HTML标签\n    text = re.sub(r\"&lt;[^&gt;]+&gt;\", \"\", text)\n\n    # 将特殊空格转换成普通空格\n    text = text.replace(\"\\xa0\", \" \")\n\n    # 合并连续空白字符\n    text = re.sub(r\"\\s+\", \" \", text)\n\n    # 去除文本前后的空格\n    text = text.strip()\n\n    return text\n\nclean_data = []\n\nfor item in faq_data:\n    question = clean_text(item[\"question\"])\n    answer = clean_text(item[\"answer\"])\n\n    # 删除问题或答案为空的数据\n    if not question or not answer:\n        continue\n\n    clean_data.append({\n        \"question\": question,\n        \"answer\": answer\n    })\n\nprint(\"清洗结果：\", clean_data)</code></pre>\n\n<p>清洗结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c12-395abc84ea2168.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>实际业务中的清洗规则需要根据数据特点决定。</p>\n\n<h3 id=\"c12-s11\"><b>重复的数据，有必要都留着吗？</b></h3>\n\n<p>从不同来源采集的数据汇总后，可能出现内容相同或相似的数据。同一个 FAQ 可能同时出现在公开数据集、产品手册和客服知识库中，合成数据也可能与已有数据内容相似。如果重复数据过多，会降低训练数据的多样性，使模型过度学习某些知识或表达方式，需要在训练前对数据进行统一去重。</p>\n\n<p>根据数据的重复程度，数据去重可以分为相同数据去重和相似数据去重：</p>\n\n<p>相同数据去重：判断两条数据的内容是否完全一致，可以通过文本匹配或哈希值进行去重。</p>\n\n<p>相似数据去重：判断两条数据虽然文字不同，但内容是否高度接近，可以使用 MinHash、SimHash 或向量相似度等方法检测。</p>\n\n<p>不同类型的数据需要采用不同的去重规则。以 FAQ 数据集为例，可以将问题和答案组合成一条完整文本，再判断不同样本是否重复或相似。</p>\n\n<h4 id=\"c12-s12\">相同数据去重</h4>\n\n<p>数据样例：</p>\n\n<pre data-language=\"JSON\"><code>faq_data = [\n    {\n        \"question\": \"如何修改登录密码？\",\n        \"answer\": \"进入个人中心，点击“修改密码”。\"\n    },\n    {\n        \"question\": \"忘记密码怎么办？\",\n        \"answer\": \"点击“忘记密码”，按照提示操作。\"\n    },\n    {\n        \"question\": \"如何修改登录密码？\",\n        \"answer\": \"进入个人中心，点击“修改密码”。\"\n    }\n]</code></pre>\n\n<p>（1）文本匹配</p>\n\n<p>文本匹配去重代码如下：</p>\n\n<pre data-language=\"Plain Text\"><code>seen = set()\nresult = []\n\nfor item in faq_data:\n    # 将问题和答案组合成一条文本\n    text = item[\"question\"] + item[\"answer\"]\n\n    if text not in seen:\n        seen.add(text)\n        result.append(item)\n\nprint(\"去重结果：\", result)</code></pre>\n\n<p>去重结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c12-ff9cdad8b7fbd3.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>处理后，两条内容相同的 FAQ 只保留一条。</p>\n\n<p>（2）哈希值</p>\n\n<p>哈希算法可以将一段文本转换成固定长度的值，相同文本会得到相同的哈希值，可以通过比较哈希值判断数据是否重复。例如：</p>\n\n<pre data-language=\"Python\"><code>import hashlib\n\nseen = set()\nresult = []\n\nfor item in faq_data:\n    # 将问题和答案组合成一条文本\n    text = item[\"question\"] + item[\"answer\"]\n\n    # 计算哈希值\n    text_hash = hashlib.sha256(\n        text.encode(\"utf-8\")\n    ).hexdigest()\n\n    if text_hash not in seen:\n        seen.add(text_hash)\n        result.append(item)\n\nprint(\"去重结果：\", result)</code></pre>\n\n<p>去重结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c12-69de99dff8d7f7.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>普通哈希值只能用于判断内容是否完全相同。只要问题或答案中的文字发生变化，计算得到的哈希值就会不同。对于文字不同但内容相近的数据，还需要进一步进行相似度判断。</p>\n\n<h4 id=\"c12-s13\">相似数据去重</h4>\n\n<p>数据样例：</p>\n\n<pre data-language=\"JSON\"><code>faq_data = [\n    {\n        \"question\": \"如何修改登录密码？\",\n        \"answer\": \"进入个人中心，点击“修改密码”即可。\"\n    },\n    {\n        \"question\": \"怎么修改登录密码\",\n        \"answer\": \"进入个人中心后，点击“修改密码”即可。\"\n    },\n    {\n        \"question\": \"如何申请发票？\",\n        \"answer\": \"进入订单页面，选择发票申请并提交信息。\"\n    }\n]</code></pre>\n\n<p>前两条 FAQ 的问题和答案在文字上存在差异，但整体表达的内容比较接近。对于这类数据，可以使用 MinHash、SimHash 或向量相似度进行检测。</p>\n\n<p><b>（1）MinHash</b></p>\n\n<p>MinHash 主要根据文本中词或字符的重合程度判断相似性，更适合检测字面相似的数据。对于 FAQ 数据，可以先将问题和答案组合成一条文本，再将文本拆分成词、字符或字符片段进行计算。</p>\n\n<p>MinHash 通常用于近似计算 Jaccard 相似度：</p>\n\n<p><span class=\"arithmatex display-math\">\\(J(A,B)=\\frac{|A\\cap B|}{|A\\cup B|}\n\n\\)</span></p>\n\n<p>其中，A 和 B 表示两段文本包含的词或字符集合，<span class=\"arithmatex display-math\">\\(A\\cap B\n\\)</span>表示两者共同包含的内容，<span class=\"arithmatex display-math\">\\(A\\cup B\n\\)</span>表示两者包含的全部内容。计算结果在 0 到 1 之间，越接近 1，表示两段文本越相似。</p>\n\n<p>Python 中可以使用<code>datasketch</code>库实现 MinHash：</p>\n\n<pre data-language=\"Plain Text\"><code>!pip install datasketch</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c12-b0f0b64f52c822.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>示例代码如下：</p>\n\n<pre data-language=\"Python\"><code>from datasketch import MinHash\n\ndef get_minhash(item):\n    text = item[\"question\"] + item[\"answer\"]\n\n    m = MinHash(num_perm=128)\n    for char in set(text):\n        m.update(char.encode(\"utf-8\"))\n\n    return m\n\nresult = []\nhashes = []\n\nfor item in faq_data:\n    current_hash = get_minhash(item)\n\n    is_duplicate = any(\n        current_hash.jaccard(h) &gt; 0.8\n        for h in hashes\n    )\n\n    if not is_duplicate:\n        result.append(item)\n        hashes.append(current_hash)\n\nprint(\"去重结果：\", result)</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c12-bcb03689199ef4.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p><b>（2）SimHash</b></p>\n\n<p><code>SimHash</code>会根据文本内容生成一个固定长度的指纹，再通过指纹之间的汉明距离判断文本是否相似。</p>\n\n<p>与普通 Hash 不同，普通 Hash 主要用于判断内容是否完全相同，而<code>SimHash</code>可以用于检测内容相近的文本。两个 SimHash 指纹之间的汉明距离越小，通常表示文本越相似。</p>\n\n<p>需要安装 simhash：</p>\n\n<pre data-language=\"Plain Text\"><code>!pip install simhash</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c12-629cbf0dcc86a6.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>使用<code>SimHash</code>去除相似数据的代码如下：</p>\n\n<pre data-language=\"Python\"><code>from simhash import Simhash\n\ndef get_simhash(item):\n    text = item[\"question\"] + item[\"answer\"]\n    return Simhash(text)\n\nresult = []\nkept_hashes = []\n\nfor item in faq_data:\n    current_hash = get_simhash(item)\n\n    # 判断是否已经存在相似FAQ\n    is_duplicate = any(\n        current_hash.distance(h) &lt;= 20\n        for h in kept_hashes\n    )\n\n    # 不存在相似数据，则保留\n    if not is_duplicate:\n        result.append(item)\n        kept_hashes.append(current_hash)\n\nprint(\"去重结果：\", result)</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c12-5e443e6bfafa27.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p><b>（3）向量相似度</b></p>\n\n<p>如果两条数据使用的文字不同，但表达的语义比较接近，可以使用文本向量模型进行判断。</p>\n\n<p>文本向量模型可以将文本转换成一组数值组成的向量。对于 FAQ 数据，将问题和答案组合成一条文本，再计算两条文本向量之间的余弦相似度：</p>\n\n<p><span class=\"arithmatex display-math\">\\(\\operatorname{sim}(A,B) = \\frac{A\\cdot B}{\\|A\\|\\|B\\|}\n\\)</span></p>\n\n<p>其中，A 和 B 表示两条 FAQ 对应的文本向量。余弦相似度越高，两条 FAQ 的语义越接近。</p>\n\n<p>下面使用<a href=\"https://www.modelscope.cn/models/BAAI/bge-small-zh-v1.5\" target=\"_blank\" rel=\"noopener noreferrer\"><code>BAAI/bge-small-zh-v1.5</code></a>模型进行计算。该模型面向中文文本向量任务，模型规模约为 24M，输出向量维度为 512，模型规模较小，速度快。</p>\n\n<p>模型下载命令：</p>\n\n<pre data-language=\"Plain Text\"><code>!modelscope download --model BAAI/bge-small-zh-v1.5 --local_dir /mnt/workspace/bge-small-zh-v1.5</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c12-856c297aea69a3.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>安装<code>sentence-transformers</code>：</p>\n\n<pre data-language=\"Plain Text\"><code>!pip3 install -U sentence-transformers</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c12-bab3604c7cc92e.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>计算向量相似度去除相似数据代码如下：</p>\n\n<pre data-language=\"Python\"><code>from sentence_transformers import SentenceTransformer\n\ntexts = [\n    item[\"question\"] + item[\"answer\"]\n    for item in faq_data\n]\n\nmodel = SentenceTransformer(\n    \"/mnt/workspace/bge-small-zh-v1.5\"\n)\n\nembeddings = model.encode(\n    texts,\n    normalize_embeddings=True\n)\n\nresult = []\nkept_embeddings = []\n\nfor item, embedding in zip(faq_data, embeddings):\n    is_duplicate = any(\n        embedding @ kept_embedding &gt; 0.9\n        for kept_embedding in kept_embeddings\n    )\n\n    if not is_duplicate:\n        result.append(item)\n        kept_embeddings.append(embedding)\n\nprint(\"去重结果：\", result)</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c12-17fbed9ef6799f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>不同方法适用于不同类型的数据去重：</p>\n\n<div class=\"table-scroll\"><table><tr><td>方法</td><td>判断依据</td><td>适合场景</td></tr><tr><td>文本直接匹配</td><td>文本是否完全相同</td><td>少量相同数据</td></tr><tr><td>Hash</td><td>文本的哈希值是否相同</td><td>大量相同数据</td></tr><tr><td>MinHash</td><td>文本中词或字符的重合程度</td><td>字面内容相似的数据</td></tr><tr><td>SimHash</td><td>文本指纹之间的汉明距离</td><td>整体内容相似但存在少量修改的数据</td></tr><tr><td>向量相似度</td><td>文本的语义相似程度</td><td>表达不同但含义相近的数据</td></tr></table>\n</div>\n<p>实际处理数据时，可以先通过文本匹配或 Hash 去除完全相同的数据，再使用 MinHash、SimHash 或向量相似度检测相似数据。对于检测出的相似数据，需要根据相似度阈值和业务需求判断是否删除、合并或保留，避免误删除具有训练价值的数据。</p>\n\n<h3 id=\"c12-s14\"><b>真实信息，训练前先脱敏</b></h3>\n\n<p>企业业务数据中可能包含姓名、电话号码、证件号码、地址、账号等敏感信息。这些内容在进入训练数据集前，需要根据数据安全要求进行脱敏。</p>\n\n<p>数据脱敏需要先识别数据中的敏感信息，再根据敏感信息类型选择合适的处理方式。</p>\n\n<h4 id=\"c12-s15\">识别敏感信息方法</h4>\n\n<p>在进行脱敏之前，需要先确定数据中哪些内容属于敏感信息。对于文本数据，可以使用正则表达式或敏感信息识别模型进行识别。</p>\n\n<p>（1）使用正则表达式</p>\n\n<p>正则表达式根据预先定义的格式规则匹配文本，适合识别具有固定格式的信息，例如手机号、身份证号、银行卡号、邮箱等。使用正则表达式识别敏感信息的代码如下：</p>\n\n<pre data-language=\"Python\"><code>import re\n\ntext = \"\"\"\n客户张三，手机号13812345678，\n身份证号320102199001011234，\n邮箱zhangsan@example.com。\n\"\"\"\n\npatterns = {\n    \"手机号\": r\"(?&lt;!\\d)(1[3-9]\\d{9})(?!\\d)\",\n    \"身份证号\": r\"(?&lt;!\\d)(\\d{17}[\\dXx])(?!\\d)\",\n    \"邮箱\": r\"([A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\\.[A-Za-z]{2,})\"\n}\n\nentities = []\n\nfor label, pattern in patterns.items():\n    for match in re.finditer(pattern, text):\n        entities.append({\n            \"label\": label,\n            \"text\": match.group(),\n            \"start\": match.start(),\n            \"end\": match.end()\n        })\n\nfor entity in entities:\n    print(entity)</code></pre>\n\n<p>运行结果：</p>\n\n<img src=\"assets/manuscript-20260914/c12-bee96eafb9e576.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>通过正则表达式可以得到敏感信息的内容、类型及原文中的位置，后续可以根据这些信息进行脱敏处理。正则表达式主要依赖格式特征，对于结构固定的信息效果较好。实际使用时，需要根据业务数据的格式制定和测试相应的规则。对于姓名、地址这种需要结合上下文理解的信息，单纯依靠正则表达式较难准确识别。</p>\n\n<p>（2）使用敏感信息识别模型</p>\n\n<p>对于姓名、地址、组织机构等需要语义理解的信息，可以使用敏感信息识别模型。</p>\n\n<p>例如，开源的 <a href=\"https://modelscope.cn/models/ZJUICSR/AIguard-pii-detection-fast\" target=\"_blank\" rel=\"noopener noreferrer\"><code>ZJUICSR/AIguard-pii-detection-fast</code></a> 是一个面向中文个人敏感信息（PII）识别的模型，可以识别姓名、手机号、身份证号、地址、邮箱等多种敏感信息。</p>\n\n<p>使用该模型识别敏感信息的代码如下：</p>\n\n<pre data-language=\"Plain Text\"><code>from transformers import AutoModelForTokenClassification, AutoTokenizer\nimport torch\n\nmodel_name = \"/mnt/workspace/AIguard-pii-detection-fast\"\n\ntokenizer = AutoTokenizer.from_pretrained(\n    model_name,\n    trust_remote_code=True\n)\n\nmodel = AutoModelForTokenClassification.from_pretrained(\n    model_name,\n    trust_remote_code=True,\n    device_map=\"auto\"\n)\n\n# 获取标签映射\nid2label = model.config.id2label\n\ndef predict_pii(text, max_length=512):\n    inputs = tokenizer(\n        text,\n        return_tensors=\"pt\",\n        truncation=True,\n        max_length=max_length,\n        return_offsets_mapping=True\n    )\n    offset_mapping = inputs.pop(\"offset_mapping\")[0].tolist()\n    \n    inputs = {k: v.to(model.device) for k, v in inputs.items()}\n\n    with torch.no_grad():\n        outputs = model(**inputs)\n\n    predictions = torch.argmax(outputs.logits, dim=-1)[0].tolist()\n    \n    # BIOE 解码\n    entities = []\n    current_entity = None\n\n    for idx, (pred_id, (start, end)) in enumerate(zip(predictions, offset_mapping)):\n        label = id2label[pred_id]\n\n        if label.startswith(\"B-\"):\n            if current_entity:\n                entities.append(current_entity)\n            current_entity = {\n                \"start\": start,\n                \"end\": end,\n                \"label\": label[2:],\n                \"text\": text[start:end]\n            }\n        elif label.startswith(\"I-\") or label.startswith(\"E-\"):\n            if current_entity and current_entity[\"label\"] == label[2:]:\n                current_entity[\"end\"] = end\n                current_entity[\"text\"] = text[current_entity[\"start\"]:end]\n                if label.startswith(\"E-\"):\n                    entities.append(current_entity)\n                    current_entity = None\n        else:\n            if current_entity:\n                entities.append(current_entity)\n                current_entity = None\n\n    if current_entity:\n        entities.append(current_entity)\n\n    return {\"text\": text, \"entities\": entities}\n    \n# 测试\ntext = \"你好，我叫张三，身份证号是110101199001011234，银行卡号6222021234567890123，邮箱zhangsan@example.com，地址江苏省南京市鼓楼区中山北路100号。\"\n\nresult = predict_pii(text)\nfor entity in result[\"entities\"]:\n    print(entity)</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c12-21c69c31c9d95a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>识别结果包含敏感信息内容、敏感信息类型、在原文本中的位置。 后续可以根据这些信息选择对应的脱敏方式。</p>\n\n<p>实际业务中，正则表达式和敏感信息识别模型常结合使用，正则表达式负责快速处理格式固定的信息，例如手机号、邮箱；  模型负责处理需要语义理解的信息，例如姓名、地址。</p>\n\n<h4 id=\"c12-s16\">脱敏方法</h4>\n\n<p>完成敏感信息识别后，需要根据敏感信息类型和训练任务要求选择合适的处理方式。</p>\n\n<p>常见的脱敏方式包括：</p>\n\n<p>（1）替换</p>\n\n<p>替换是将真实敏感信息替换为其他内容。替换可以分为固定替换和随机替换：</p>\n\n<p>固定替换：按照固定规则生成替换结果，例如将姓名中的名字统一替换为“某”，“张三”替换为“张某”、“李四”替换为“李某”。由于规则固定，同一个信息每次都会得到相同结果，不需要额外保存映射关系。处理代码如下：</p>\n\n<pre data-language=\"Python\"><code>text = \"客户张三提交申请，李四负责审核。\"\n\n# 上一步识别得到的敏感信息\nentities = [\n    {\"label\": \"姓名\", \"text\": \"张三\"},\n    {\"label\": \"姓名\", \"text\": \"李四\"}\n]\n\ndef replace_name(name):\n    return name[0] + \"某\"\n\n# 根据识别结果进行替换\nfor entity in entities:\n    if entity[\"label\"] == \"姓名\":\n        entity[\"result\"] = replace_name(entity[\"text\"])\n\n        text = text.replace(\n            entity[\"text\"],\n            entity[\"result\"]\n        )\n\nprint(\"脱敏后：\", text)</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c12-f74628a6442d4f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>随机替换：随机选择虚构信息替换真实信息，如 “张三”替换为“李明”。如果同一个“张三”在多个位置出现，需要保证替换结果一致，否则原本属于同一对象的数据可能被替换成不同身份，破坏数据中的关联关系。因此，随机替换通常需要建立映射表：<code>{\"张三\":\"李明\"}</code>，后续再次遇到“张三”时，直接使用“李明”。处理代码如下：</p>\n\n<pre data-language=\"Python\"><code>import random\n\ntext = \"客户张三提交申请，客服随后联系张三。\"\nentities = [\n    {\"label\": \"姓名\", \"text\": \"张三\"},\n    {\"label\": \"姓名\", \"text\": \"张三\"}\n]\nfake_names = [\"李明\", \"王强\", \"赵伟\"]\nname_map = {}\n\ndef replace_name_random(name):\n    if name not in name_map:\n        name_map[name] = random.choice(fake_names)\n    return name_map[name]\n\nfor entity in entities:\n    if entity[\"label\"] == \"姓名\":\n\n        entity[\"result\"] = replace_name_random(\n            entity[\"text\"]\n        )\n\n        text = text.replace(\n            entity[\"text\"],\n            entity[\"result\"]\n        )\n\nprint(\"脱敏后：\", text)\nprint(\"映射关系：\", name_map)</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c12-f42171a1f3d340.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>（2）掩码</p>\n\n<p>掩码是隐藏部分敏感内容，保留部分信息，如手机号、身份证、银行卡、邮箱等。</p>\n\n<p>处理代码如下：</p>\n\n<pre data-language=\"Python\"><code>text = \"客户手机号13812345678，身份证号110101199001011234，银行卡号6222021234567890123，邮箱zhangsan@example.com。\"\n\n# 上一步识别得到的敏感信息\nentities = [\n    {\"label\": \"手机号\", \"text\": \"13812345678\"},\n    {\"label\": \"身份证号\", \"text\": \"110101199001011234\"},\n    {\"label\": \"银行卡号\", \"text\": \"6222021234567890123\"},\n    {\"label\": \"邮箱\", \"text\": \"zhangsan@example.com\"}\n]\n\n\ndef mask_value(entity):\n    text = entity[\"text\"]\n\n    if entity[\"label\"] == \"手机号\":\n        return (\n            text[:3]\n            + \"****\"\n            + text[-4:]\n        )\n\n    if entity[\"label\"] == \"身份证号\":\n        return (\n            text[:6]\n            + \"********\"\n            + text[-4:]\n        )\n\n    if entity[\"label\"] == \"银行卡号\":\n        return (\n            text[:4]\n            + \"***********\"\n            + text[-4:]\n        )\n\n    if entity[\"label\"] == \"邮箱\":\n        username, domain = text.split(\"@\")\n        return \"***@\" + domain\n\n    return text\n\n# 根据识别结果进行掩码\nfor entity in entities:\n    result = mask_value(entity)\n\n    text = text.replace(\n        entity[\"text\"],\n        result\n    )\n\nprint(\"脱敏后：\", text)</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c12-fe8483d330f364.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>（3）泛化</p>\n\n<p>泛化是降低信息精度，减少数据定位到具体对象的能力。常用于地址、年龄、时间等连续型信息。</p>\n\n<p>处理代码如下：</p>\n\n<pre data-language=\"Python\"><code>text = \"客户地址：江苏省南京市鼓楼区中山北路100号，年龄：36岁，注册时间：2025年8月15日。\"\n\n# 上一步识别得到的敏感信息\nentities = [\n    {\n        \"label\": \"地址\",\n        \"text\": \"江苏省南京市鼓楼区中山北路100号\"\n    },\n    {\n        \"label\": \"年龄\",\n        \"text\": \"36岁\"\n    },\n    {\n        \"label\": \"时间\",\n        \"text\": \"2025年8月15日\"\n    }\n]\n\ndef generalize(entity):\n\n    text = entity[\"text\"]\n\n    # 地址：降低到城市级别\n    if entity[\"label\"] == \"地址\":\n        if \"市\" in text:\n            return text.split(\"市\")[0] + \"市\"\n\n    # 年龄：转换为年龄区间\n    if entity[\"label\"] == \"年龄\":\n        age = int(text.replace(\"岁\", \"\"))\n\n        if age &lt; 18:\n            return \"18岁以下\"\n        elif age &lt; 40:\n            return \"18-40岁\"\n        elif age &lt; 60:\n            return \"40-60岁\"\n        else:\n            return \"60岁以上\"\n\n    # 时间：降低到月份\n    if entity[\"label\"] == \"时间\":\n        return text[:7]\n\n    return text\n\n# 根据识别结果进行泛化\nfor entity in entities:\n    result = generalize(entity)\n\n    text = text.replace(\n        entity[\"text\"],\n        result\n    )\n\nprint(\"脱敏后：\", text)</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c12-630f84951425c3.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>（4）删除</p>\n\n<p>对于训练任务无关的敏感字段，可以直接删除。如果训练任务只需要学习问答关系，则可以删除姓名和手机号。</p>\n\n<pre data-language=\"Python\"><code>data = {\"姓名\": \"张三\", \"手机号\": \"13812345678\", \"问题\": \"如何办理业务？\", \"答案\": \"可以在线提交申请。\"}\n\nremove_fields = [\"姓名\", \"手机号\"]\n\nfor field in remove_fields:\n    data.pop(field, None)\n\nprint(\"脱敏后：\",data)</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c12-00a6fb97c6d967.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>实际处理时，脱敏方式需要结合训练任务和数据特点选择。需要保留不同样本之间关联关系时，可以采用固定替换或带映射关系的随机替换；需要保留部分信息特征时，可以采用掩码或泛化；对于训练任务无关的敏感信息，则直接删除。</p>\n\n<p>不同业务场景的数据特点不同，具体脱敏规则需要根据实际需求进行调整。本节代码仅用于说明基本处理方法。</p>\n\n<h2 id=\"c12-s17\">最后一步，整理成训练需要的格式</h2>\n\n<p>完成数据处理后，还需要按照训练框架要求将数据整理成相应的格式。</p>\n\n<p>指令微调（Instruction Tuning）是大语言模型常用的微调方式。训练数据包含用户的指令以及模型期望生成的回答，让模型学习如何按照指令完成任务。</p>\n\n<p>不同训练框架使用的数据字段可能不同。常见的指令数据格式主要包括 Alpaca、ShareGPT 和 Messages 等。</p>\n\n<h3 id=\"c12-s18\">Alpaca 格式</h3>\n\n<p>Alpaca 格式结构简单，通常使用 instruction、input 和 output 三个字段，适合单轮指令数据。</p>\n\n<pre data-language=\"Plain Text\"><code>{\n  \"instruction\": \"将下面这段中文翻译成英文\",\n  \"input\": \"今天天气很好。\",\n  \"output\": \"The weather is very nice today.\"\n}</code></pre>\n\n<p>其中：instruction 表示模型需要执行的任务；input 表示完成任务所需要的输入内容；output 表示模型期望生成的回答。</p>\n\n<p>如果任务本身不需要额外输入，input 也可以为空。</p>\n\n<p>例如：</p>\n\n<pre data-language=\"Plain Text\"><code>{\n  \"instruction\": \"介绍一下什么是机器学习\",\n  \"input\": \"\",\n  \"output\": \"机器学习是一种让计算机从数据中学习规律的方法。\"\n}</code></pre>\n\n<p>这种格式字段少、结构清晰，适合问答、分类、文本生成等单轮训练任务。</p>\n\n<h3 id=\"c12-s19\">Messages 格式</h3>\n\n<p>另一种常见方式是使用 messages 保存对话，每条消息包含 role 和 content 两个主要字段。</p>\n\n<p>例如：</p>\n\n<pre data-language=\"Plain Text\"><code>{\n  \"messages\": [\n    {\n      \"role\": \"system\",\n      \"content\": \"你是一位专业的客服助手。\"\n    },\n    {\n      \"role\": \"user\",\n      \"content\": \"我的订单什么时候到货？\"\n    },\n    {\n      \"role\": \"assistant\",\n      \"content\": \"请提供订单号，我帮您查询。\"\n    }\n  ]\n}</code></pre>\n\n<p>其中，system 用于设置模型的角色、任务要求或回答规则，user 表示用户输入，assistant 表示模型期望生成的回答。</p>\n\n<p>Messages 格式同样支持多轮对话，只需要按照实际对话顺序继续增加 user 和 assistant 消息即可。</p>\n\n<p>例如：</p>\n\n<pre data-language=\"Plain Text\"><code>{\n  \"messages\": [\n    {\n      \"role\": \"system\",\n      \"content\": \"你是一位专业的客服助手。\"\n    },\n    {\n      \"role\": \"user\",\n      \"content\": \"如何修改登录密码？\"\n    },\n    {\n      \"role\": \"assistant\",\n      \"content\": \"进入账号设置页面，选择“修改密码”。\"\n    },\n    {\n      \"role\": \"user\",\n      \"content\": \"忘记原密码怎么办？\"\n    },\n    {\n      \"role\": \"assistant\",\n      \"content\": \"可以在登录页面选择“忘记密码”，通过手机号或邮箱完成验证后重新设置密码。\"\n    }\n  ]\n}</code></pre>\n\n<p></p>\n\n<h3 id=\"c12-s20\">ShareGPT 格式</h3>\n\n<p>ShareGPT 是开源大模型训练中常见的对话数据格式之一。它通常使用 conversations 保存完整对话，并使用 human 和 gpt 区分用户与模型。</p>\n\n<p>例如：</p>\n\n<pre data-language=\"Plain Text\"><code>{\n  \"conversations\": [\n    {\n      \"from\": \"human\",\n      \"value\": \"将下面这段中文翻译成英文：今天天气很好。\"\n    },\n    {\n      \"from\": \"gpt\",\n      \"value\": \"The weather is very nice today.\"\n    }\n  ]\n}</code></pre>\n\n<p>ShareGPT 格式也可以保存多轮对话：</p>\n\n<pre data-language=\"Plain Text\"><code>{\n  \"conversations\": [\n    {\n      \"from\": \"human\",\n      \"value\": \"帮我写一封请假邮件。\"\n    },\n    {\n      \"from\": \"gpt\",\n      \"value\": \"请告诉我请假的时间和原因。\"\n    },\n    {\n      \"from\": \"human\",\n      \"value\": \"感冒了，请两天病假。\"\n    },\n    {\n      \"from\": \"gpt\",\n      \"value\": \"好的，下面是一封请假邮件……\"\n    }\n  ]\n}</code></pre>\n\n<h3 id=\"c12-s21\">如何选择数据格式</h3>\n\n<p>Alpaca、Messages 和 ShareGPT 格式本质上都是在描述模型的输入和期望输出，只是字段组织方式不同。</p>\n\n<p>可以根据任务类型和训练框架进行选择：</p>\n\n<div class=\"table-scroll\"><table><tr><td>数据格式</td><td>主要特点</td><td>适合场景</td></tr><tr><td>Alpaca</td><td>使用 instruction、input 和 output 组织数据</td><td>单轮指令、问答、分类</td></tr><tr><td>Messages</td><td>使用 messages 保存对话，通过 role 区分角色</td><td>单轮或多轮对话</td></tr><tr><td>ShareGPT</td><td>使用 conversations 保存对话，通过 from 区分角色</td><td>单轮或多轮对话</td></tr></table>\n</div>\n<p>不同格式之间通常可以相互转换。例如，一条 Alpaca 数据可以转换成一组 user 和 assistant 消息，ShareGPT 中的 human 和 gpt 也可以分别转换成 user 和 assistant。实际训练时，应先确认训练框架支持的数据格式和模型模板，再按照相应要求整理数据。</p>\n\n<p></p>\n\n<p></p>\n\n<p>模型代码及相关文件见：https://www.modelscope.cn/gallery/liucong/b41e2395-f795-400f-bafd-e53ed7f5c8ca</p>\n\n<p></p>\n","imageCount":22,"attachmentCount":0,"discussionId":"chapter-12"},{"id":"chapter-13","number":13,"title":"快速用 ms-swift，完成开源模型的轻量微调","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-13.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-13.html","minutes":14,"status":"ready","headings":[{"id":"c13-s1","text":"微调到底在改模型的什么？","level":"h2"},{"id":"c13-s2","text":"全参数、LoRA 和 QLoRA，差别在哪里？","level":"h2"},{"id":"c13-s3","text":"微调方式怎么选，先看任务和显卡","level":"h2"},{"id":"c13-s4","text":"用 ms-swift，跑通一次微调","level":"h2"},{"id":"c13-s5","text":"ms-swift 能帮我们省下哪些工作？","level":"h3"},{"id":"c13-s6","text":"从实体识别任务开始，动手训练","level":"h3"},{"id":"c13-s7","text":"训练结束了，模型怎么用起来？","level":"h2"},{"id":"c13-s8","text":"把 LoRA 权重合并回基础模型","level":"h3"},{"id":"c13-s9","text":"加载微调后的模型，试试实际效果","level":"h3"}],"html":"<p>当通用模型无法直接满足具体的业务需求时，可以使用自己的业务数据对模型进行微调，让模型进一步学习特定的任务和输出方式。</p>\n\n<p>拿电商实体识别任务来说，我们需要模型从文本里找出商品名、品牌名、型号，给出对应类别和位置。模型聊起这些商品也许头头是道，但最终使用时，要求该提取的信息不能漏，字段要统一，输出格式也得稳定。</p>\n\n<p>如果任务和输出要求已经明确，也有了整理好的示例，就可以尝试用这些数据做微调，让模型学习我们希望它完成的工作。至于要不要更新全部参数、手里的显卡够不够用，还得结合任务和资源来选。</p>\n\n<p>下面就用 ms-swift，从一次 LoRA 微调开始，把数据准备、训练、权重合并和推理依次跑通。</p>\n\n<p></p>\n\n<h2 id=\"c13-s1\">微调到底在改模型的什么？</h2>\n\n<p>通用大模型本身已经具备较强的语言理解、知识问答、文本生成等通用能力。但在实际业务中，通用模型的表现并不一定能够直接满足具体需求。</p>\n\n<p>例如，我们希望模型能够准确完成某类信息抽取任务、并按照规定的格式输出结果，或者采用统一的方式回答特定问题。针对这些需求，可以在已有模型的基础上，使用特定数据进行训练，从而使模型进一步学习相应的任务要求和回答方式，其实这个过程就称为模型微调（Fine-tuning）。</p>\n\n<p>微调并不是重新训练一个模型，而是在模型已有能力的基础上进一步调整，让它更加适合具体的应用场景。与从头训练大模型相比，微调需要的数据和计算资源通常更少。</p>\n\n<img src=\"assets/manuscript-20260914/c13-7c572d15a09e10.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>目前，大模型微调中比较常见的方法是 SFT（Supervised Fine-Tuning，监督微调）。SFT 使用带有标准答案的数据对模型进行训练，每条训练数据通常包含输入和对应的输出，让模型学习在给定输入后应该生成什么样的结果。例如，在信息抽取任务中，可以将一段文本作为输入，将正确的字段抽取结果作为输出。SFT 比较适合能够准备明确训练样本的任务，例如文本分类、信息抽取、问答和固定格式生成等。</p>\n\n<p>除了 SFT 之外，大模型训练还可以使用强化学习（Reinforcement Learning，RL）。与 SFT 直接提供标准答案不同，强化学习主要通过奖励信号来评价模型生成结果的好坏，并根据奖励结果不断调整模型的输出行为。SFT 和强化学习都属于大模型的后训练（Post-training）阶段。后训练是指模型完成大规模预训练之后，为了进一步提升指令遵循、推理以及特定任务能力而进行的一系列训练。大模型训练通常会先通过 SFT 学习如何按照指令完成任务，再结合强化学习等方法进一步优化模型的回答质量和行为表现。</p>\n\n<p></p>\n\n<h2 id=\"c13-s2\">全参数、LoRA 和 QLoRA，差别在哪里？</h2>\n\n<p>根据训练方式和硬件资源的不同，可以选择 LoRA、QLoRA 或者全参数微调等方式，不同方式在训练参数量、显存占用和计算成本等方面存在一定差异。</p>\n\n<p>1、全参数微调（Full Fine-Tuning），在训练过程中，模型的全部参数都会参与更新，因此模型能够进行较为充分的调整。全参数微调需要的显存和计算资源也会明显增加，训练成本较高。</p>\n\n<p>2、LoRA 微调（Low-Rank Adaptation），为了降低大模型微调对硬件资源的要求，LoRA 的核心思想是通过冻结预训练模型的权重，并将可训练的秩分解矩阵注入到 Transformer 架构的每一层，从而显著减少下游任务中可训练参数的数量。在训练过程中，只需要固定原始模型的参数，然后训练降维矩阵 A 和升维矩阵 B。LoRA 的相关示意图如图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c13-bbfb8860ca6c32.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>                         </p>\n\n<p>具体来看，假设预训练的矩阵为<span class=\"arithmatex\">\\(W_0 \\in \\mathbb{R}^{d \\times k}\\)</span> ，它的更新可表示为： </p>\n\n<p><span class=\"arithmatex display-math\">\\(W = W_0 + \\Delta W = W_0 + BA \n\\)</span></p>\n\n<p>其中，<span class=\"arithmatex\">\\(\\Delta W\\)</span>表示微调过程中需要学习的权重变化，<span class=\"arithmatex display-math\">\\(B \\in \\mathbb{R}^{d \\times r}, \\quad\nA \\in \\mathbb{R}^{r \\times k}, \\quad\nr \\ll \\min(d,k)\n\\)</span>,A 和 B 是 LoRA 新增并参与训练的参数，模型训练完成后，会得到一个单独的 LoRA Adapter 文件，保存本次微调得到的参数。使用时，需要将基础模型和对应的 Adapter 一起加载。</p>\n\n<p>3、QLoRA 微调（Quantized LoRA），LoRA 主要减少的是训练参数带来的资源开销，基础模型本身仍然需要加载到显存中。如果模型规模较大，加载基础模型依然可能占用较多显存。为了进一步降低显存需求，可以使用 QLoRA 微调.</p>\n\n<p>相关模型架构如图所示，从图中可以看出，QLoRA 是针对 LoRA 的改进，而改进的主要模式是采用 4-bit 精度和分页优化来共同减少模型的显存消耗。</p>\n\n<img src=\"assets/manuscript-20260914/c13-97d0ea6f68166b.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>QLoRA 可以简单理解为量化与 LoRA 的结合。它将基础模型以较低精度进行量化，QLoRA 的创新内容主要如下：</p>\n\n<p>1）4bit NormalFloat（NF4），NF4 是一种新型数据类型，它对正态分布的权重来说是信息理论上的最优选择；</p>\n\n<p>2）双重量化技术，双重量化减少了平均内存使用，它通过对已量化的常量进行再量化实现此目的； </p>\n\n<p>3）分页优化器，分页优化器使用有助于管理内存峰值，防止梯度检查点时出现内存不足的错误。 </p>\n\n<p></p>\n\n<h2 id=\"c13-s3\">微调方式怎么选，先看任务和显卡</h2>\n\n<p>选择哪种微调方式，主要需要综合考虑模型规模、任务需求、GPU 资源和训练成本。不同方式各有适用场景，并不是更新的模型参数越多，微调效果就一定越好。对于大多数微调任务，可以优先尝试 LoRA。LoRA 只需要训练少量新增参数，对 GPU 显存和计算资源的要求相对较低，训练得到的 Adapter 文件也比较小，便于保存。</p>\n\n<p>如果基础模型规模较大，即使采用 LoRA，加载模型后仍然没有足够的显存进行训练，可以进一步考虑 QLoRA。QLoRA 通过量化降低基础模型本身的显存占用，使较大的模型也能够在有限的 GPU 资源下进行微调。QLoRA 更适合模型规模较大、GPU 显存有限的场景。</p>\n\n<p>如果 GPU 资源比较充足，并且任务需要对模型进行更充分的调整，则可以考虑全参数微调。由于训练过程中需要更新模型的全部参数，全参数微调对显存、计算资源和训练数据的要求都更高，训练和保存模型的成本也更大，因此通常需要结合实际任务判断是否有必要采用。</p>\n\n<p>在实际项目中，我们可以先用较低的成本验证效果，再根据实际需求选择是否增加训练成本。 如果 LoRA 已经能够达到预期效果，通常没有必要仅仅为了更新更多参数而选择全参数微调。</p>\n\n<p></p>\n\n<h2 id=\"c13-s4\">用 ms-swift，跑通一次微调</h2>\n\n<h3 id=\"c13-s5\">ms-swift 能帮我们省下哪些工作？</h3>\n\n<p>ms-swift（SWIFT，Scalable lightWeight Infrastructure for Fine-Tuning）是 ModelScope 社区开源的大模型训练与部署框架，主要面向大语言模型和多模态大模型，提供从模型训练、微调到推理、评测和部署的一整套工具。</p>\n\n<img src=\"assets/manuscript-20260914/c13-666c2da44787e1.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>目前已经支持 Qwen、DeepSeek、Llama、GLM、InternLM 等主流大语言模型，以及 Qwen-VL、InternVL 等多模态模型,同时支持 Embedding、Reranker 和文本分类等模型或任务的训练。</p>\n\n<p>可以在 <a href=\"https://github.com/modelscope/ms-swift?utm_source=chatgpt.com\" target=\"_blank\" rel=\"noopener noreferrer\">ms-swift</a> 查看更多使用方法。</p>\n\n<p>除了训练之外，ms-swift 还提供了比较完整的模型使用流程。训练完成后，可以直接使用 swift infer 进行模型推理，也可以通过 swift deploy 将模型部署为 OpenAI 兼容的 API 服务。在推理和部署方面，还可以结合 vLLM、SGLang、LMDeploy 等推理引擎进行加速。</p>\n\n<p>对于初学者来说，ms-swift 的一个特点是将大模型训练过程中很多复杂的配置进行了统一封装。通过命令行参数指定基础模型、训练数据、微调方式以及训练参数，就可以完成一次模型微调。在下面的实验中，我们将使用 ms-swift 完成一次完整的模型微调流程，包括准备训练数据、启动 LoRA 训练、查看训练结果、合并 LoRA 权重以及加载微调后的模型进行推理和部署。</p>\n\n<p></p>\n\n<h3 id=\"c13-s6\">从实体识别任务开始，动手训练</h3>\n\n<p>下面将以一个实体识别任务为例，在 ModelScope Notebook 环境中演示完整的模型微调流程，包括数据准备、模型训练以及微调后的模型推理等步骤。通过这个示例，可以了解如何使用 ms-swift 从零开始完成大模型微调。本实验继续使用 ubuntu22.04-cuda12.8.1-py312-torch2.10.0-1.39.0 镜像作为运行环境。</p>\n\n<img src=\"assets/manuscript-20260914/c13-776b3c96504c69.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>1) 查看环境中是否已经安装 ms-swift，注意安装包是 ms_swift</p>\n\n<pre data-language=\"Plain Text\"><code>!pip3 list |grep ms_swift</code></pre>\n\n<p>显示下面这种形式，说明已经安装了 ms-swift</p>\n\n<img src=\"assets/manuscript-20260914/c13-5e9feb760083d7.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>如果没有安装，则执行</p>\n\n<pre data-language=\"Plain Text\"><code>!pip3 install ms-swift</code></pre>\n\n<p>2）数据准备，本次实验的数据采用的是开源数据，电商实体识别，数据集包含四种实体类型：<code>HCCX</code> 商品名，<code>HPPX</code> 品牌名，<code>XH</code> 商品型号，<code>MISC</code> 表示其他实体，包括国家、尺寸容量、人物、作品及活动名称等。</p>\n\n<p>3）首新建一个目录 data，然后把数据右击上传至文件夹中，数据形式如下，这个任务只要求模型输出实体类别，实体，以及实体在文本中对应的起始位置，数据划分为训练集和测试集，其中 train.jsonl 为 5400 条，val.jsonl 为 600 条。</p>\n\n<pre data-language=\"JSON\"><code>{\"messages\":[{\"role\":\"system\",\"content\":\"你是一个实体识别模型。请识别用户文本中的实体，严格按实体在原文中的顺序输出，每个实体单独一行，格式为：(类型,实体文本,起始位置)。起始位置从0开始；类型只能是HCCX、HPPX、MISC、XH之一。没有实体时只输出：无实体。不要输出解释、Markdown或其他内容。\"},{\"role\":\"user\",\"content\":\"推bb护肤刮痧l背疗橄榄油全身按开背足体按油身m油5摩油摩精00\"},{\"role\":\"assistant\",\"content\":\"(HCCX,橄榄油,10)\\n(HCCX,按油,20)\\n(HCCX,油,24)\\n(HCCX,油,27)\"}]}</code></pre>\n\n<p>目录格式如下：</p>\n\n<img src=\"assets/manuscript-20260914/c13-0bf9dbfe6a6d60.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>4）模型训练，此部分采用<code>Qwen/Qwen3-0.6B</code>进行微调，在终端中直接输入下面命令即可训练</p>\n\n<pre data-language=\"SQL\"><code>!CUDA_VISIBLE_DEVICES=0 swift sft \\\n  --model Qwen/Qwen3-0.6B \\\n  --dataset data/train.jsonl \\\n  --val_dataset data/val.jsonl \\\n  --tuner_type lora \\\n  --target_modules all-linear \\\n  --lora_rank 16 \\\n  --lora_alpha 32 \\\n  --lora_dropout 0.05 \\\n  --torch_dtype bfloat16 \\\n  --num_train_epochs 2 \\\n  --per_device_train_batch_size 4 \\\n  --per_device_eval_batch_size 4 \\\n  --gradient_accumulation_steps 4 \\\n  --learning_rate 1e-4 \\\n  --warmup_ratio 0.05 \\\n  --max_length 512 \\\n  --eval_strategy steps \\\n  --eval_steps 100 \\\n  --save_strategy steps \\\n  --save_steps 100 \\\n  --save_total_limit 3 \\\n  --logging_steps 10 \\\n  --load_from_cache_file true \\\n  --dataset_num_proc 4 \\\n  --dataloader_num_workers 4 \\\n  --output_dir output/qwen3_0_6b_ner_lora</code></pre>\n\n<p>核心参数说明：</p>\n\n<div class=\"table-scroll\"><table><tr><td>参数</td><td>含义</td></tr><tr><td>--model Qwen/Qwen3-0.6B</td><td>使用 Qwen3-0.6B 基础模型</td></tr><tr><td>--tuner_type lora</td><td>使用 LoRA 微调</td></tr><tr><td>--target_modules all-linear</td><td>对全部线性层添加 LoRA</td></tr><tr><td>--lora_rank 16</td><td>LoRA 容量</td></tr><tr><td>--lora_alpha 32</td><td>LoRA 缩放系数</td></tr><tr><td>--num_train_epochs 2</td><td>训练轮数</td></tr><tr><td>--per_device_train_batch_size 4</td><td>单卡每步 4 条数据</td></tr><tr><td>--gradient_accumulation_steps 4</td><td>累积 4 步更新一次参数</td></tr><tr><td>--learning_rate 1e-4</td><td>LoRA 学习率</td></tr><tr><td>--max_length 512</td><td>单条样本最大长度</td></tr><tr><td>--eval_steps 100</td><td>每 100 步验证一次</td></tr><tr><td>--save_steps 100</td><td>每 100 步保存一次</td></tr><tr><td>--save_total_limit 3</td><td>最多保留 3 个检查点</td></tr><tr><td>--output_dir</td><td>模型和日志保存路径</td></tr></table>\n</div>\n<p>启动训练以后，ms-swift 会不断输出当前的训练信息，如下：</p>\n\n<img src=\"assets/manuscript-20260914/c13-51d4dd500aa94b.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<img src=\"assets/manuscript-20260914/c13-60634611be055a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h2 id=\"c13-s7\">训练结束了，模型怎么用起来？</h2>\n\n<h3 id=\"c13-s8\">把 LoRA 权重合并回基础模型</h3>\n\n<p>LoRA 训练完成以后，会保存对应的 LoRA Adapter。Adapter 并不是一个完整的大模型，因此在使用时还需要加载原来的基础模型，通过训练脚本中的 output_dir，可以在目录中看到的 checkpoint 以及对应的 Adapter 文件，在实际部署时，可以将 LoRA Adapter 合并到基础模型中，生成一个完整的模型，在离线部署或模型迁移时更加方便。模型训练完成后的目录形式如下：</p>\n\n<img src=\"assets/manuscript-20260914/c13-53f899a319e3d7.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>模型合并命令如下：</p>\n\n<pre data-language=\"SQL\"><code>!CUDA_VISIBLE_DEVICES=0 swift export \\\n  --adapters output/qwen3_0_6b_ner_lora/v2-20260903-172616/checkpoint-676 \\\n  --merge_lora true \\\n  --output_dir output/qwen3_0_6b_ner_merged</code></pre>\n\n<p>其中，<code>--adapters</code> 指定训练得到的 LoRA checkpoint 路径，<code>--merge_lora true</code> 表示将 LoRA 参数合并到基础模型中，<code>--output_dir</code> 指定合并后模型的保存目录。</p>\n\n<p>执行结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c13-7f4ed7665ab43d.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h3 id=\"c13-s9\">加载微调后的模型，试试实际效果</h3>\n\n<p>合并完成后，可以直接加载生成的完整模型进行推理。为了方便应用程序调用，也可以将模型启动为 OpenAI 兼容接口，通过 API 的方式访问微调后的模型，这个也是一个常驻服务，需要在终端中启动，可以参考第七章怎么在终端中启动命令，启动命令如下：</p>\n\n<pre data-language=\"Plain Text\"><code>CUDA_VISIBLE_DEVICES=0 swift deploy \\\n  --model output/qwen3_0_6b_ner_merged \\\n  --load_args false \\\n  --infer_backend vllm \\\n  --enable_thinking false \\\n  --host 0.0.0.0 \\\n  --port 8000 \\\n  --served_model_name qwen3-0.6b-ner \\\n  --api_key 123 \\\n  --vllm_gpu_memory_utilization 0.7 \\\n  --vllm_max_model_len 1024 \\\n  --max_new_tokens 128</code></pre>\n\n<p>参数说明如下：</p>\n\n<div class=\"table-scroll\"><table><tr><td>参数</td><td>含义</td></tr><tr><td>swift deploy</td><td>启动 ms-swift 的 OpenAI 兼容服务</td></tr><tr><td>--model</td><td>指定合并后的完整模型目录</td></tr><tr><td>--load_args</td><td>不加载模型目录中 args.json 参数</td></tr><tr><td>--infer_backend</td><td>使用 vLLM 推理引擎</td></tr><tr><td>--enable_thinking</td><td>关闭 Qwen3 思考模式</td></tr><tr><td>--host</td><td>接口 IP</td></tr><tr><td>--port</td><td>端口</td></tr><tr><td>--served_model_name</td><td>设置接口访问模型名称</td></tr><tr><td>--api_key</td><td>设置接口访问密钥</td></tr><tr><td>--vllm_gpu_memory_utilization</td><td>模型使用约 70% 显存</td></tr><tr><td>--vllm_max_model_len</td><td>最大总 token 数</td></tr><tr><td>--max_new_tokens</td><td>最大输出长度</td></tr></table>\n</div>\n<p>模型启动完成后，结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c13-aa5ee4202786f2.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>模型启动完成后，可以通过接口测试下是否连通，代码如下：</p>\n\n<pre data-language=\"C++\"><code>import json\nimport requests\n\nurl = \"http://127.0.0.1:8000/v1/chat/completions\"\n\nheaders = {\n    \"Authorization\": \"Bearer 123\",\n    \"Content-Type\": \"application/json\"\n}\n\npayload = {\n    \"model\": \"qwen3-0.6b-ner\",\n    \"messages\": [\n        {\n            \"role\": \"system\",\n            \"content\": \"你是一个实体识别模型。请识别用户文本中的实体，严格按实体在原文中的顺序输出，每个实体单独一行，格式为：(类型,实体文本,起始位置)。起始位置从0开始；类型只能是HCCX、HPPX、MISC、XH之一。没有实体时只输出：无实体。不要输出解释、Markdown、think标记或其他内容。\"\n        },\n        {\n            \"role\": \"user\",\n            \"content\": \"3539,2017消防灭火防滑耐磨长筒抢险救援胶靴\"\n        }\n    ],\n    \"temperature\": 0,\n    \"max_tokens\": 128\n}\n\ntry:\n    response = requests.post(url, headers=headers, json=payload, timeout=30)\n    response.raise_for_status()  \n    \n    result = response.json()\n\n    output_text = result[\"choices\"][0][\"message\"][\"content\"]\n    print(\"识别结果：\")\n    print(output_text)\n\nexcept requests.exceptions.RequestException as e:\n    print(f\"请求失败: {e}\")</code></pre>\n\n<p>模型输出结果如下，我们也可以用正则把 think 标签去除掉。</p>\n\n<img src=\"assets/manuscript-20260914/c13-46d0b2c839d362.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>本章节所有实验数据和代码，可参考：https://modelscope.cn/gallery/liucong/ab458cbd-b47f-4830-91b6-314ea2036fc6</p>\n","imageCount":13,"attachmentCount":0,"discussionId":"chapter-13"},{"id":"chapter-14","number":14,"title":"模型已经会回答了，为什么还要做偏好对齐？","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-14.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-14.html","minutes":38,"status":"ready","headings":[{"id":"c14-s1","text":"先梳理一下，后训练有哪些做法？","level":"h2"},{"id":"c14-s2","text":"给模型看示范，SFT 能教会它什么？","level":"h2"},{"id":"c14-s3","text":"同一个问题，怎样的回答更合适？","level":"h2"},{"id":"c14-s4","text":"偏好数据应该怎样构造","level":"h2"},{"id":"c14-s5","text":"奖励模型怎样学会给回答打分？","level":"h2"},{"id":"c14-s6","text":"RLHF，人类的反馈怎样参与训练？","level":"h2"},{"id":"c14-s7","text":"PPO，让模型根据奖励逐步调整","level":"h2"},{"id":"c14-s8","text":"GRPO，同一道题多答几次，再比较得分","level":"h2"},{"id":"c14-s9","text":"DPO，直接从回答的优劣中学习","level":"h2"},{"id":"c14-s10","text":"RLHF、PPO、GRPO 和 DPO，到底怎么区分和选择？","level":"h2"},{"id":"c14-s11","text":"用 ms-swift，试试 DPO 和 GRPO","level":"h2"},{"id":"c14-s12","text":"实验准备","level":"h3"},{"id":"c14-s13","text":"准备 DPO 偏好数据","level":"h3"},{"id":"c14-s14","text":"启动 DPO 训练","level":"h3"},{"id":"c14-s15","text":"比较 DPO 训练前后的结果","level":"h3"},{"id":"c14-s16","text":"准备 GRPO 数据与奖励函数","level":"h3"},{"id":"c14-s17","text":"启动 GRPO 训练并观察日志","level":"h3"},{"id":"c14-s18","text":"全量评测与结果分析","level":"h3"}],"html":"<p>模型微调完，能按要求回答问题了，可把几条回复放在一起看，会有好有坏，并且有些人看起来会更舒服一些。</p>\n\n<p></p>\n\n<p>拿退款咨询来说，同样是在告诉用户去哪里申请，一个回答只给了操作入口，另一个还说明需要审核、能否退款要看订单情况。两条回复都谈到了退款，后者把用户接下来可能遇到的情况交代得更清楚，也少了一些误解。</p>\n\n<p></p>\n\n<p>前面做 SFT 时，我们会把这样的回答整理成示例，让模型照着学习。现在还可以把同一个问题的不同回答放在一起，告诉模型我们更认可哪一个，以及评价的标准是什么。</p>\n\n<p>这就是偏好对齐，我们从这里开始。</p>\n\n<p></p>\n\n<h2 id=\"c14-s1\"><b>先梳理一下，后训练有哪些做法？</b></h2>\n\n<p>大模型后训练的主要目的是，帮助已经具备文本续写能力的基础模型，逐步变成能够更好地直接对话、遵循指令并完成等具体任务。后训练并不是某一种固定算法，而是一组训练方法的统称，常见方法包括 SFT、偏好优化、奖励模型训练以及强化学习等，不同模型会根据训练目标、数据条件和成本选择不同的组合方式。</p>\n\n<p>其中，SFT 主要使用“指令、输入和期望回答”形式的数据，让模型学习如何理解任务、遵循指令以及按照期望的格式和方式回答。在此基础上，还可以进一步利用偏好数据进行对齐，例如为同一个问题构造优选回答和非优选回答，让模型学习人类或业务评价标准。偏好数据既可以用于训练奖励模型，把人工偏好转化为可计算的奖励分数，再结合 PPO、GRPO 等强化学习方法持续优化模型的生成策略；也可以直接采用 DPO 等方法，将优选与非优选回答之间的偏好关系直接写入训练目标，而不单独训练奖励模型。</p>\n\n<p>因此，大模型的后训练并不存在一套固定流程，SFT、奖励模型、PPO、GRPO 和 DPO 也承担着不同职责。SFT 主要用于建立基础的指令跟随能力，奖励模型负责提供可量化的评价信号，PPO 和 GRPO 根据奖励信号进一步优化生成策略，而 DPO 则直接利用偏好数据完成模型对齐。实际训练过程中，这些方法不一定全部使用，也不一定按照完全相同的顺序组合，而是根据模型能力、任务目标和训练资源灵活选择。</p>\n\n<img src=\"assets/manuscript-20260914/c14-b34d9903ffac90.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h2 id=\"c14-s2\"><b>给模型看示范，SFT 能教会它什么？</b></h2>\n\n<p>SFT 即监督微调，它使用已经整理好的输入和期望输出继续训练模型，让模型在看到类似输入时，提高生成期望答案的概率。</p>\n\n<p>一条 SFT 数据可以写成下面的形式：</p>\n\n<pre data-language=\"JSON\"><code>{\n  \"instruction\": \"根据用户的问题生成客服回复\",\n  \"input\": \"会员昨天自动续费，我想申请退款。\",\n  \"output\": \"请先进入订单页面确认续费订单状态。符合退款条件时，可以在订单详情中提交申请；如果页面没有退款入口，请联系人工客服核验。\"\n}</code></pre>\n\n<p>训练时，分词器（Tokenizer）会把指令、输入和回答转换为词元（Token）。模型根据前面的 Token 预测下一个 Token，再用预测结果与期望答案之间的差异计算损失。大模型 SFT 通常采用交叉熵损失和教师强制训练：训练每个位置时，模型看到的是数据中已经给出的正确前文，而不是自己刚刚生成的错误内容。很多指令微调实现只对回答部分计算损失，指令和用户输入用于提供上下文，但不要求模型把它们重新生成出来。</p>\n\n<p>SFT 首先改变的是任务格式。模型会逐渐理解，输入是一条用户咨询，输出应是一段客服回复，而不是继续补写用户问题。用于信息抽取时，它可以学习输出固定 JSON 字段；用于会议纪要时，它可以学习按照会议信息、关键结论和行动项组织内容。其次，SFT 会改变模型的回答方式。训练数据中的语气、篇幅、段落结构、解释程度和拒答方式，都会成为模型模仿的对象。如果期望回答普遍简洁，模型会更倾向直接给出结论；如果示例要求先说明依据再给出步骤，模型也会学习这种组织方式。SFT 还可以让模型接触领域示例。客服数据中的订单状态、退款条件和升级人工服务，法律数据中的条款结构，代码数据中的接口用法，都会影响模型在相应任务中的输出。不过，这并不意味着模型自动掌握了该领域的全部知识。SFT 数据覆盖到什么任务、包含什么术语和处理方式，模型才有机会在参数更新中学习这些模式。</p>\n\n<p>SFT 的训练目标本质上仍是模仿期望输出。它能让模型知道这类问题通常怎样回答，却不能保证回答中的事实已经经过核验，也不能仅凭一条示范学会比较所有可能答案。同一个退款问题可能得到下面两条回复：</p>\n\n<pre data-language=\"Plain Text\"><code>回答A：请在订单详情中申请退款，如无法操作，请联系人工客服。\n\n回答B：您可以先查看订单状态。符合退款条件时，可在订单详情中提交申请；如果没有退款入口，请联系人工客服核验。退款结果和到账时间以实际审核为准。</code></pre>\n\n<p>两条回答都与退款相关，但回答 B 补充了审核条件，也避免承诺确定的退款结果。如果业务更重视信息完整和合规边界，回答 B 更合适。SFT 可以把回答 B 作为示范交给模型，偏好数据则可以同时给出 A 和 B，并明确告诉模型为什么更倾向 B。这就是从监督学习进入偏好对齐的原因。</p>\n\n<p></p>\n\n<h2 id=\"c14-s3\">同一个问题，怎样的回答更合适？</h2>\n\n<p>偏好对齐关注的重点，不是模型“能不能回答”，而是面对多个可能都能成立的回答时，应该更倾向于生成哪一种。它通常通过比较同一输入下不同回答的优劣，将准确性、相关性、帮助性、表达方式、安全性等要求转化为训练信号。与 SFT 相比，SFT 更像是在教模型“怎么答”，通常使用一个输入对应一个期望输出的数据，让模型学习任务格式、内容结构、领域表达和基本回答方式；偏好对齐则是在模型已经具备一定回答能力之后，进一步教模型“怎样答得更好”。</p>\n\n<p>以退款客服场景为例，如果一个回答遗漏审核条件、承诺固定到账时间，或者把不确定事项表述成确定结果，就可以被标记为较差回答；如果另一个回答能够明确说明办理路径、保留必要的审核边界，同时没有虚构政策，则可以被标记为更优回答。通过大量这样的比较训练，模型会逐渐提高优选回答的生成概率，从而在相似问题中更倾向于输出符合业务要求的结果。需要注意的是，偏好对齐学习的并不是一套抽象且统一的“人类价值”，而是结合奖励函数，针对具体任务定义的评价标准。不同的任务需要构造与任务匹配的偏好数据模式。因此，在构造偏好数据之前，需要先明确“什么叫更好”，否则不同标注人员可能因为理解不同而给出相互矛盾的选择。</p>\n\n<p>此外，偏好对齐也不能替代事实验证。一个回答可能表达流畅、语气友好，却引用了错误政策；另一个回答也可能事实正确，却没有遵循要求的输出格式。因此，实际评价体系通常需要分别定义事实正确性、任务完成度、表达风格和安全合规等标准。对于能够自动验证的内容，还可以结合代码测试、答案校验、格式检查和业务规则等方式进行评价，以减少完全依赖人工主观判断带来的偏差。</p>\n\n<p></p>\n\n<h2 id=\"c14-s4\"><b>偏好数据应该怎样构造</b></h2>\n\n<p>偏好数据（Preference Data）是用来告诉模型“多个回答中哪个更好”的训练数据。它与 SFT 数据最大的区别在于：SFT 通常提供一个输入和一个期望答案，而偏好数据通常会针对同一个 Prompt 准备多个候选回答，并标注它们之间的优劣关系。最常见的偏好数据由一个输入、一个优选回答和一个非优选回答组成：</p>\n\n<pre data-language=\"JSON\"><code>{\n  \"prompt\": \"会员昨天自动续费，我想申请退款。\",\n  \"chosen\": \"请先查看订单状态。符合退款条件时，可以在订单详情中提交申请；如果没有退款入口，请联系人工客服核验。\",\n  \"rejected\": \"可以，退款会在三个工作日内到账。\"\n}</code></pre>\n\n<p>其中，<code>chosen</code>表示在当前评价标准下更合适的回答，<code>rejected</code>表示相对不合适的回答。非优选回答不一定完全错误，也可能只是遗漏必要条件、表达过于冗长、语气不符合要求，或者包含没有依据的承诺。偏好数据表达的是相对关系，而不是给每个回答贴上永久的正确或错误标签。</p>\n\n<p>偏好数据通常按照下面的过程构造：</p>\n\n<ol><li>从真实业务或测试集中准备能够代表任务分布的 Prompt；</li><li>使用当前模型、不同版本模型或不同采样参数，为同一个 Prompt 生成多个候选回答；</li><li>按照统一的评价标准，让标注人员对回答进行两两比较或完整排序；</li><li>把排序结果转换为优选回答和非优选回答组成的数据对；</li><li>抽查标注一致性，并把无法判断、事实依据不足和意见冲突的样本交给复核人员处理。</li></ol>\n\n<p>如果同一个输入生成了 A、B、C 和 D 四个回答，标注结果为 B 优于 A、A 优于 D、D 优于 C，就可以转换成多组偏好对。实际构造时不一定要穷举所有组合。差异过小的回答很难稳定标注，差异过大的回答又可能只让模型学会避开低级错误。更有价值的数据通常来自都能读通、但在关键条件、事实边界或任务完成度上存在明确差别的回答。</p>\n\n<p>偏好标注容易受到一些表面因素影响。较长的回答看起来更完整，措辞更自信的回答看起来更可靠，排在前面的候选也可能更容易被选择。为了降低这些偏差，可以随机调整候选顺序，隐藏模型名称，要求标注人员分别检查事实、任务完成度、风格和安全性，并在部分样本上安排多人重复标注。若同一批数据中标注者经常意见相反，通常说明评价标准还不够清楚，而不是简单地把少数意见删除。</p>\n\n<p></p>\n\n<h2 id=\"c14-s5\">奖励模型怎样学会给回答打分？</h2>\n\n<p>偏好数据已经把“同一个 Prompt 下哪个回答更好”表示成了 <code>chosen</code> 和 <code>rejected</code> 之间的相对关系。奖励模型（Reward Model，RM）的作用，就是利用这些偏好数据学习一个自动评分函数，把人工比较结果转换成可以被后续强化学习算法使用的奖励信号。常见奖励模型会以语言模型为基础，在最后增加一个输出标量的评分层。训练时，Prompt 和回答会被拼接成一段序列，经过模型得到隐藏状态，再由评分层输出一个数值。</p>\n\n<p>奖励模型并不需要知道两个回答分别应该得到多少分，它只需要学习一个相对关系：在这个 Prompt 下，<code>chosen</code> 的得分应该高于 <code>rejected</code>。因此，训练时通常会分别把 Prompt 与两个候选回答拼接后输入奖励模型：</p>\n\n<pre data-language=\"Plain Text\"><code>Prompt + chosen   → rchosen\nPrompt + rejected → rrejected</code></pre>\n\n<p>其中，<code>rchosen</code>和 <code>rrejected</code> 是奖励模型输出的两个标量分数。训练目标就是让优选回答的分数高于非优选回答。常见的成对排序损失可以写成：</p>\n\n<p><span class=\"arithmatex display-math\">\\(\\mathcal{L} = -\\log \\sigma\\left(r_{\\text{chosen}} - r_{\\text{rejected}}\\right)\n\\)</span></p>\n\n<p>其中，<code>r_chosen</code>和<code>r_rejected</code>分别表示奖励模型对优选回答和非优选回答的评分，<code>σ</code>表示把分数差映射到 0 至 1 之间。优选回答得分越高、非优选回答得分越低，损失越小。经过大量偏好对训练后，奖励模型可以对没有见过的回答作出相对评价。</p>\n\n<p>奖励模型通常以语言模型为基础，再增加一个输出标量分数的评分头。它输入的仍然是 Prompt 和回答文本，只是输出不再是下一个 Token，而是一个用于表示相对质量的奖励分数。这个分数没有统一的物理含义，也不能直接理解为“正确率”或“满分 10 分中的几分”。真正有意义的是同一个奖励模型下，不同回答之间的分数高低。例如，某个回答得到 6 分，另一个得到 4 分，只能说明前者在当前奖励模型看来更符合偏好标准，并不意味着它在客观意义上就是“6 分质量”。</p>\n\n<p>奖励模型能学到什么，很大程度上取决于前面构造的偏好数据。如果偏好数据中的标注者总是倾向于选择更长的回答，奖励模型可能把长度误认为质量；如果标注人员过度关注语言流畅性，它也可能给措辞漂亮但事实错误的回答较高分。因此，奖励模型并不会真正理解什么是正确的答案，而是在模仿偏好数据中体现出来的评价规律。这也是为什么奖励模型训练完成后不能只看训练损失，还需要单独进行评测。</p>\n\n<p>正是由于奖励模型本身只负责“评价”，不会直接修改语言模型。因而，后续在 PPO、GRPO 等强化学习阶段，语言模型会持续生成新的回答，奖励模型对这些回答进行打分，强化学习算法再根据奖励结果更新模型参数，使模型逐渐提高高奖励回答的生成概率。需要注意的是，如果策略模型发现某些表面模式能够稳定获得高分，就可能反复利用这些模式，而不是真正提升任务能力，这种现象通常称为<b>奖励投机（Reward Hacking）</b>。因此，奖励模型不仅需要能够区分训练数据中的优劣回答，还要尽量避免被简单的长度、固定措辞、格式模板等表面特征“欺骗”。</p>\n\n<p></p>\n\n<h2 id=\"c14-s6\">RLHF，人类的反馈怎样参与训练？</h2>\n\n<p>RLHF（Reinforcement Learning from Human Feedback）通常称为基于人类反馈的强化学习。它描述人工反馈怎样进入强化学习训练，是一套训练框架，不是一种具体的优化算法，也不是某个固定的模型结构。</p>\n\n<p>经典 RLHF 通常包括三个阶段：</p>\n\n<ol><li>收集人工示范数据，通过 SFT 得到能够遵循指令的初始策略模型；</li><li>对同一 Prompt 下的多个回答进行人工排序，使用这些偏好数据训练奖励模型；</li><li>让策略模型生成新回答，由奖励模型评分，再使用 PPO 等强化学习算法更新策略模型。</li></ol>\n\n<p>经典 RLHF 路线分成三个连续阶段如下图所示。第一阶段由标注人员编写示范回答，用这些数据完成 SFT；第二阶段让模型针对同一个 Prompt 生成多个回答，再由标注人员排序，用比较数据训练奖励模型；第三阶段让策略模型生成新回答，由奖励模型计算分数，并使用 PPO 更新策略。三类数据承担的作用不同：示范数据教模型怎样回答，比较数据教奖励模型怎样评价，Prompt 和奖励信号则用于强化学习。</p>\n\n<img src=\"assets/manuscript-20260914/c14-ac423e555e46b5.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>把这个过程放到强化学习概念中，语言模型就是策略模型，Prompt 和已经生成的 Token 构成当前状态，模型选择下一个 Token 相当于采取动作，从开始回答到生成结束标记构成一次完整轨迹。</p>\n\n<p>在 RLHF 中，奖励模型通常在回答结束后给出一个总体分数，强化学习算法再判断应该提高哪些 Token 选择的概率。在 RLHF 的训练通常还会保留参考模型。参考模型一般是冻结的 SFT 模型，用于约束正在训练的策略模型不要偏离原有语言能力太远。</p>\n\n<p></p>\n\n<h2 id=\"c14-s7\">PPO，让模型根据奖励逐步调整</h2>\n\n<p>近端策略优化（Proximal Policy Optimization， PPO）。它是一种策略梯度算法，核心目标是在提高奖励的同时，限制每次参数更新的幅度。大模型参数很多，如果仅因为一批回答获得高分就大幅改变策略，模型可能很快偏向少数奖励模式，甚至破坏原有的语言能力。PPO 中的近端，强调新策略要在旧策略附近逐步更新。在大模型后训练中，一轮 PPO 通常包含两个阶段。第一个阶段是采样：从 Prompt 数据集中抽取一批输入，让当前策略模型生成回答，同时保存每个 Token 在旧策略下的概率，再由奖励模型给出分数。第二个阶段是更新：根据奖励和价值估计计算优势，使用这些样本更新策略模型和价值模型。完成若干次小批量更新后，再用新的策略重新生成数据。</p>\n\n<p>PPO 需要比较新策略和旧策略对同一个 Token 给出的概率。概率比可以简化写成：</p>\n\n<p><span class=\"arithmatex display-math\">\\(\nr_t(\\theta)\n=\n\\frac{\\text{新策略选择当前 Token 的概率}}\n{\\text{旧策略选择当前 Token 的概率}}\n\n\\)</span></p>\n\n<p>其中，<code>rₜ</code>表示计算的得分，</p>\n\n<p><code>θ</code> 表示待优化的模型。</p>\n\n<p>当<code>rₜ</code>接近 1，表示新旧策略差别不大；当<code>rₜ</code>明显大于 1，表示新策略更倾向当前 Token；当<code>rₜ</code>明显小于 1，则表示新策略降低了它的概率。只有概率变化还不够，还需要优势<code>Aₜ</code>判断这个 Token 选择比当前平均水平好还是差。优势为正时，应适当提高该选择的概率；优势为负时，应降低它的概率。</p>\n\n<img src=\"assets/manuscript-20260914/c14-f79b0ff3c8860e.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>上图左侧表示优势为正的情况。概率比从 1 向右增加时，目标先随之提高；超过<code>1+ε</code>后曲线变平，继续提高该 Token 的概率不再增加目标。右侧表示优势为负的情况。当概率比下降到<code>1-ε</code>以下后，目标同样停止继续改善。两条曲线共同说明，PPO 允许策略朝正确方向调整，但会削弱过大更新带来的额外收益。</p>\n\n<p>一套典型的大模型 PPO 训练需要同时维护几个组件：策略模型负责生成并接受更新；旧策略是采样时策略参数的快照，用于计算概率比；参考模型是冻结的 SFT 模型，用于计算 KL 约束；奖励模型负责评价完整回答；价值模型负责估计每个生成位置后续可能获得的收益。价值模型的估计与实际奖励之间存在差异，PPO 通常使用 GAE 等方法把这些差异整理为每个 Token 的优势。PPO 的整体流程如下图所示。策略模型根据输入<code>q</code>生成回答<code>o</code>，奖励模型和参考模型共同形成奖励<code>r</code>，价值模型给出价值估计<code>v</code>，随后通过 GAE 计算得到优势<code>A</code>。策略模型根据优势和裁剪目标更新，价值模型则通过价值损失学习更准确的收益估计。</p>\n\n<img src=\"assets/manuscript-20260914/c14-096fa24e49d187.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h2 id=\"c14-s8\">GRPO，同一道题多答几次，再比较得分</h2>\n\n<p>组相对策略优化（Group Relative Policy Optimization， GRPO）。它是 PPO 的一种变体，保留了策略采样、概率比裁剪和 KL 约束等思路，主要变化是不再单独训练价值模型，而是使用同一个 Prompt 下多条回答的相对得分估计优势。GRPO 的整体框架如下图所示。对于同一个输入<code>q</code>，策略模型一次生成一组回答<code>o₁</code>到<code>oG</code>，奖励模型或规则系统分别给出<code>r₁</code>到<code>rG</code>。</p>\n\n<img src=\"assets/manuscript-20260914/c14-f21e88aba9da8c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>系统先计算这一组奖励的平均值和标准差，再把每条回答的得分转换为组内相对优势，得分高于组内平均水平的回答获得正优势，低于平均水平的回答获得负优势。对于只在回答结束时给一次总体奖励的任务，同一回答中的 Token 通常共享由该回答得到的相对优势；如果还提供过程奖励，也可以进一步形成更细的 Token 级反馈。随后，GRPO 像 PPO 一样比较新旧策略概率，并通过裁剪目标和 KL 约束更新策略模型。</p>\n\n<p>相较于 PPO，GRPO 减少了价值模型带来的参数、显存和训练开销，但并不意味着训练成本一定很低。每个 Prompt 需要生成多条回答，推理采样本身会消耗大量计算资源。如果同一组回答得分完全相同，标准差接近 0，组内比较就很难提供有效方向，实际实现需要跳过、平滑或重新采样这类数据。组大小、采样温度、奖励尺度和 KL 系数也会影响训练稳定性。例如，一个数学问题生成四个解答，其中两个答案正确，一个计算错误，一个没有完成。系统可以使用答案校验规则给它们评分，再在四个结果内部比较。正确且过程完整的回答得到较高优势，错误或未完成的回答得到较低优势，模型据此提高较好解法出现的概率。代码任务也可以使用编译结果和单元测试作为奖励，不一定先训练一个专门的奖励模型。</p>\n\n<p></p>\n\n<h2 id=\"c14-s9\"><b>DPO，直接从回答的优劣中学习</b></h2>\n\n<p>直接偏好优化（Direct Preference Optimization，DPO）。在其论文标题中，作者就明确指出，偏好模型可能并不需要，我们构建的大语言模型本身也许就是潜在的偏好模型。这种想法直接放弃了偏好模型的单独建模，而是直接从原有模型出发，增加单独为偏好模型专项设计的损失函数，使其在优化模型生成结果得到进一步提高。DPO 通过使用已经收集好的优选回答和非优选回答直接训练语言模型，不需要显式训练奖励模型，也不需要运行 PPO 或 GRPO 那样的在线强化学习循环。</p>\n\n<img src=\"assets/manuscript-20260914/c14-cad0c241b4b6b6.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>上图对比了经典 RLHF 路线和 DPO。偏好数据先用于训练奖励模型，策略模型再生成新回答，根据奖励模型的分数进行强化学习。DPO 路线，偏好数据直接进入语言模型训练。两条路线都从回答之间的优劣关系出发，但训练环节和资源需求不同。DPO 的整体流程仅需两个阶段：</p>\n\n<p>第一阶段，构造生成偏好正负例样本。这步相较于之前微调阶段的样本构造方式，将原有的“输入（Input）-输出（Output）”数据集改为“输入（Input）-正反馈（Accept Response）-负反馈（Negative Response）”。这种样本训练学习方法并不是首创，其实就是从图像领域兴起的对比学习。对比学习的研究者发现，模型在同时学习正负例时，模型可以快速收敛，整体表现可以进一步提升。</p>\n\n<p>第二阶段，基于设计好的损失函数，利用对比学习相关方法，通过极大似然函数，对原始生成模型进行参数优化。这样的设计省去了原有偏好奖励模型与强化学习过程训练全过程，在效率提高的同时，精度与稳定性相比于 PPO 也有显著提升。通过第二阶段的优化调整，模型参数朝着靠近正例远离负例的方向不断优化，最终实现模型生成向正例内容的偏好对齐。</p>\n\n<p>DPO 的训练形式接近普通 SFT。训练数据已经包含 Prompt、优选回答和非优选回答，模型不需要在每一步重新生成候选，也不需要同时运行奖励模型和价值模型。因此，它属于离线偏好优化，组件少，显存和工程复杂度通常低于 PPO。这种简化并没有消除对数据质量的要求。DPO 只能学习偏好数据中已经出现的差异。如果优选回答本身存在事实错误，非优选回答过于低劣，或者数据主要来自与当前模型差异很大的生成分布，模型可能学到表面风格而不是目标能力。DPO 也不会像在线强化学习那样不断探索新回答，再针对新出现的问题获取反馈。</p>\n\n<p></p>\n\n<h2 id=\"c14-s10\"><b>RLHF、PPO、GRPO 和 DPO，到底怎么区分和选择？</b></h2>\n\n<p>前文已经分别介绍了 RLHF、PPO、GRPO 和 DPO 的基本原理与训练方式。为了更清楚地理解它们之间的关系，可以进一步从方法定位、训练过程中是否需要在线生成回答、是否依赖奖励模型和价值模型等几个维度进行对比。通过这种横向比较，可以更直观地看出不同后训练方法在实现路径、训练成本和适用方式上的主要差异。下表是相关模型对比。</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col><col><col><col><col></colgroup><tbody><tr><td><p>概念</p></td><td><p>属于什么</p></td><td><p>训练时是否生成新回答</p></td><td><p>是否需要显式奖励模型</p></td><td><p>是否需要价值模型</p></td><td><p>主要特点</p></td></tr><tr><td><p>RLHF</p></td><td><p>反馈与强化学习框架</p></td><td><p>通常需要</p></td><td><p>经典路线需要</p></td><td><p>取决于所用优化算法</p></td><td><p>规定人工反馈、奖励和策略更新怎样衔接</p></td></tr><tr><td><p>PPO</p></td><td><p>在线强化学习算法</p></td><td><p>需要</p></td><td><p>经典 RLHF 中通常需要</p></td><td><p>需要</p></td><td><p>通过价值估计和概率比裁剪稳定更新策略</p></td></tr><tr><td><p>GRPO</p></td><td><p>在线强化学习算法</p></td><td><p>需要，同一 Prompt 通常生成一组回答</p></td><td><p>可以使用奖励模型或规则奖励</p></td><td><p>不需要</p></td><td><p>用组内相对奖励估计优势</p></td></tr><tr><td><p>DPO</p></td><td><p>离线偏好优化方法</p></td><td><p>通常不需要在线生成</p></td><td><p>不需要单独训练</p></td><td><p>不需要</p></td><td><p>直接提高优选回答相对于非优选回答的概率</p></td></tr></tbody></table>\n</div>\n<p>综合来看，RLHF、PPO、GRPO 和 DPO 虽然都与大模型偏好对齐有关，但它们解决的问题并不完全相同。RLHF 更偏向一套完整的反馈与强化学习框架，PPO 和 GRPO 则是在这一类框架中用于更新策略模型的在线强化学习算法，而 DPO 则直接利用已有偏好数据完成离线优化。从训练机制上看，PPO 依赖奖励模型和价值模型，整体工程链路更完整，但训练成本和实现复杂度也更高；GRPO 通过组内相对奖励估计优势，省去了价值模型，更适合具有规则奖励、答案校验等明确反馈的任务；DPO 不需要在线生成回答，也不需要单独训练奖励模型和价值模型，因此实现相对简单，适合作为偏好数据较充分时的低成本对齐方案。</p>\n\n<p>在实际应用中，方法选择应从模型当前缺少的能力出发，而不是简单追随某一种训练算法。模型还不能稳定完成任务时，应优先通过 SFT 建立基础能力；模型已经能够完成任务，但回答质量和偏好选择不稳定时，可以进一步采用 DPO、PPO 或 GRPO。已有高质量偏好数据、希望快速完成第一轮对齐时，可以优先考虑 DPO；任务具有可验证奖励并希望模型主动探索更优解时，可以考虑 GRPO；具备成熟奖励模型、价值模型和强化学习基础设施，并需要精细控制在线策略更新时，则可以采用 PPO。无论最终选择哪种方法，都不能只用训练损失、奖励分数或偏好胜率判断效果，而应回到真实任务中持续评估模型的准确性、任务完成度、安全性、业务边界和通用能力。后训练的最终目标不是获得更高的奖励，而是让模型在真实使用环境中更稳定、更可靠地完成目标任务。</p>\n\n<h2 id=\"c14-s11\"><b>用 ms-swift，试试 DPO 和 GRPO</b></h2>\n\n<p>前面介绍了偏好对齐和强化学习的基本原理，下面使用 ms-swift 完成两个具体实验。第一个实验使用中文偏好数据进行 DPO 训练，观察模型对优选回答和非优选回答的偏好怎样变化；第二个实验使用数学题进行 GRPO 训练，让模型生成候选回答，再根据答案和格式得到奖励。两个实验都使用已经具备指令跟随能力的 Qwen2.5-0.5B-Instruct 作为初始模型，分别训练一个独立的 LoRA 适配器。</p>\n\n<h3 id=\"c14-s12\"><b>实验准备</b></h3>\n\n<p>本次实验在魔搭 Notebook 的单卡 GPU 环境中完成，模型和原始数据文件均从魔搭社区下载。两个实验的配置如下：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col><col></colgroup><tbody><tr><td><p>项目</p></td><td><p>DPO 中文偏好对齐</p></td><td><p>GRPO 数学答案优化</p></td></tr><tr><td><p>初始模型</p></td><td><p>Qwen/Qwen2.5-0.5B-Instruct</p></td><td><p>同一份 Qwen2.5-0.5B-Instruct 权重</p></td></tr><tr><td><p>数据集</p></td><td><p>AI-ModelScope/hh_rlhf_cn</p></td><td><p>AI-ModelScope/gsm8k</p></td></tr><tr><td><p>使用子集</p></td><td><p>helpful_base_cn</p></td><td><p>main</p></td></tr><tr><td><p>训练集</p></td><td><p>256 条偏好对</p></td><td><p>128 道题</p></td></tr><tr><td><p>验证集</p></td><td><p>32 条偏好对</p></td><td><p>16 道题</p></td></tr><tr><td><p>测试集</p></td><td><p>32 条偏好对</p></td><td><p>官方测试文件全部 1319 道题</p></td></tr><tr><td><p>训练方式</p></td><td><p>LoRA，20 个优化器更新步骤</p></td><td><p>LoRA，10 个优化器更新步骤</p></td></tr><tr><td><p>训练时反馈</p></td><td><p>数据中给定的优选回答和非优选回答</p></td><td><p>模型生成回答后，由规则计算奖励</p></td></tr></tbody></table>\n</div>\n<p>这里的训练规模较小，主要用于演示完整流程。训练步骤表示优化器更新参数的次数，不等于训练轮数，也不表示训练数据已经全部使用一遍。本次实操部分仅使用了较小部分的数据，并采用了较少次数的迭代步骤，实验仅作参考。</p>\n\n<p>1）检查运行环境。</p>\n\n<p>打开配套 Notebook 后，先检查 GPU 是否可用，以及当前内核实际使用的软件版本。</p>\n\n<img src=\"assets/manuscript-20260914/c14-6743e9815165e1.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p></p>\n\n<p>2）准备模型和运行目录。</p>\n\n<p>公共准备部分会建立缓存目录和本次实验目录。模型配置的核心代码如下：</p>\n\n<pre data-language=\"Python\"><code>MODEL_ID = \"Qwen/Qwen2.5-0.5B-Instruct\"\nMODEL_REVISION = \"master\"\nDPO_DATA_ID = \"AI-ModelScope/hh_rlhf_cn\"\nGRPO_DATA_ID = \"AI-ModelScope/gsm8k\"\nSEED = 42\n\nMODEL_DIR = Path(snapshot_download(\n    MODEL_ID,\n    revision=MODEL_REVISION,\n    cache_dir=str(CACHE_DIR / \"models\"),\n    allow_file_pattern=[\n        \"*.json\", \"*.safetensors\", \"*.txt\", \"*.model\", \"*.tiktoken\"\n    ],\n)).resolve()</code></pre>\n\n<p></p>\n\n<p>其中，<code>snapshot_download</code>来自 ModelScope，<code>CACHE_DIR</code>由公共准备代码建立。下载完成后，训练和推理都使用<code>MODEL_DIR</code>指向的本地模型快照。本次实操内容将保存在<code>ms_swift_dpo_grpo_runs/</code>目录下。读者重新运行时，程序会生成新的实验编号，后面的<code>RUN_DIR</code>应使用自己的运行目录。DPO</p>\n\n<p></p>\n\n<h3 id=\"c14-s13\"><b>准备 DPO 偏好数据</b></h3>\n\n<p>DPO 需要同一个上下文下的两个候选回答。实验使用中文 HH-RLHF 数据集的<code>helpful_base_cn</code>子集，其中<code>context</code>保存对话历史，<code>chosen</code>保存优选回答，<code>rejected</code>保存非优选回答。这里的优选和非优选来自数据集标签，不表示回答中的事实已经逐条核验。</p>\n\n<p>1）转换为 ms-swift 使用的数据格式。</p>\n\n<p>ms-swift 使用的偏好样本结构如下。下面只展示字段关系，实际内容从数据集读取：</p>\n\n<pre data-language=\"JSON\"><code>{\n  \"messages\": [\n    {\"role\": \"user\", \"content\": \"同一个问题\"},\n    {\"role\": \"assistant\", \"content\": \"优选回答\"}\n  ],\n  \"rejected_response\": \"非优选回答\"\n}</code></pre>\n\n<p>与【快速用 ms-swift，完成开源模型的轻量微调】的 SFT 样本相比，这里增加了<code>rejected_response</code>。<code>messages</code>最后一条 assistant 消息保存优选回答，非优选回答放在单独字段中。多轮对话里的历史 assistant 消息仍然属于上下文，不能把它们误当成本次比较的回答。Notebook 中的转换函数如下：</p>\n\n<pre data-language=\"Python\"><code>def convert_dpo(row):\n    role_map = {\n        \"human\": \"user\", \"user\": \"user\",\n        \"assistant\": \"assistant\", \"system\": \"system\"\n    }\n    context = [\n        {\"role\": role_map[m[\"role\"]], \"content\": m[\"text\"].strip()}\n        for m in row[\"context\"]\n    ]\n    chosen = row[\"chosen\"][\"text\"].strip()\n    rejected = row[\"rejected\"][\"text\"].strip()\n\n    assert context and context[-1][\"role\"] == \"user\"\n    assert chosen and rejected and chosen != rejected\n    assert all(m[\"content\"] for m in context)\n    if context[0][\"role\"] != \"system\":\n        context.insert(0, {\"role\": \"system\", \"content\": GENERAL_SYSTEM})\n\n    return {\n        \"messages\": context + [{\"role\": \"assistant\", \"content\": chosen}],\n        \"rejected_response\": rejected,\n    }</code></pre>\n\n<p>2）清洗并划分数据。</p>\n\n<p>本次分别计算优选回答和非优选回答与上下文拼接后的长度，只保留两者均不超过 1024 Token 的样本。超长时丢弃整对数据，避免截断后恰好删掉决定回答优劣的关键内容。随后按上下文去重，从原始训练来源中划分 256 条训练数据和 32 条验证数据。处理后的文件分别为<code>dpo_train.jsonl</code>、<code>dpo_val.jsonl</code>和<code>dpo_test.jsonl</code>，保存在本次运行目录的<code>data</code>文件夹中。三个文件承担的作用不同：训练集用于更新参数，验证集用于观察训练过程，测试集用于训练后的独立比较。相关执行结果如下图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c14-e356f75edc9e63.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h3 id=\"c14-s14\"><b>启动 DPO 训练</b></h3>\n\n<p>训练前，先用初始模型记录测试集上的表现。Notebook 中的<code>dpo_evaluate()</code>会计算 32 条固定候选对的回答对数概率，并对其中 4 个上下文生成回答，供训练后比较：</p>\n\n<pre data-language=\"Python\"><code>def dpo_evaluate(adapter=None):\n    def action(model):\n        rows = []\n        for index, row in enumerate(dpo_test):\n            context = row[\"messages\"][:-1]\n            lp_chosen = response_logp(model, context, row[\"messages\"][-1][\"content\"])\n            lp_rejected = response_logp(model, context, row[\"rejected_response\"])\n            rows.append({\"sample_id\": index, \"chosen_logp\": lp_chosen,\n                         \"rejected_logp\": lp_rejected, \"gap\": lp_chosen - lp_rejected})\n        # 只生成 4 条供精读，其余候选对仍参与概率诊断。\n        generations = [{\"sample_id\": i, \"context\": dpo_test[i][\"messages\"][:-1],\n                        \"response\": generate_one(model, dpo_test[i][\"messages\"][:-1])}\n                       for i in range(min(4, len(dpo_test)))]\n        return rows, generations\n    return with_local_model(action, adapter)\n\ndpo_before, dpo_before_text = dpo_evaluate()</code></pre>\n\n<p></p>\n\n<p>启动 DPO 训练的相关步骤，如下：</p>\n\n<p>1）设置训练参数。</p>\n\n<p>两个实验通过<code>common_options()</code>复用公共配置，包括本地模型路径、Qwen 对话模板、随机种子、bfloat16 精度和 LoRA 设置。其中 LoRA rank 为 8、alpha 为 16，目标模块为<code>all-linear</code>。下面保留 DPO 的完整实验配置；公共函数需要先在配套 Notebook 中运行。<code>common_options() </code> 函数把公共参数与本实验参数合并。<code>train_swift</code>将这些参数转换为命令行形式，并通过当前内核的 Python 调用<code>swift.cli.rlhf</code>实现模型训练。</p>\n\n<pre data-language=\"Python\"><code>DPO_OUTPUT = RUN_DIR / \"dpo\"\nDPO_BETA = 0.1\ndpo_options = common_options() | {\n    \"rlhf_type\": \"dpo\", \"loss_type\": \"sigmoid\", \"beta\": DPO_BETA,\n    \"dataset\": str(DPO_PATHS[\"train\"]),\n    \"val_dataset\": str(DPO_PATHS[\"val\"]),\n    \"output_dir\": str(DPO_OUTPUT), \"max_length\": 1024,\n    \"truncation_strategy\": \"delete\", \"max_steps\": 20,\n    \"learning_rate\": 5e-5, \"lr_scheduler_type\": \"cosine\",\n    \"warmup_ratio\": 0.1,\n    \"per_device_train_batch_size\": 1,\n    \"per_device_eval_batch_size\": 1,\n    \"gradient_accumulation_steps\": 8,\n    \"eval_strategy\": \"steps\", \"eval_steps\": 10,\n    \"save_strategy\": \"steps\", \"save_steps\": 10,\n}\ntrain_swift(dpo_options, RUN_DIR / \"dpo_train.log\")\nDPO_ADAPTER = latest_adapter(DPO_OUTPUT)</code></pre>\n\n<p>主要参数的含义如下：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col><col></colgroup><tbody><tr><td><p>参数</p></td><td><p>本实验值</p></td><td><p>作用</p></td></tr><tr><td><p>rlhf_type / loss_type</p></td><td><p>dpo / sigmoid</p></td><td><p>使用标准 sigmoid 形式的 DPO 目标</p></td></tr><tr><td><p>beta</p></td><td><p>0.1</p></td><td><p>控制 DPO 目标中相对参考策略的尺度</p></td></tr><tr><td><p>learning_rate</p></td><td><p>5e-5</p></td><td><p>控制每次参数更新的幅度</p></td></tr><tr><td><p>max_length</p></td><td><p>1024</p></td><td><p>限制上下文与候选回答的总长度</p></td></tr><tr><td><p>per_device_train_batch_size</p></td><td><p>1</p></td><td><p>单卡每个训练微批次处理 1 条偏好对</p></td></tr><tr><td><p>gradient_accumulation_steps</p></td><td><p>8</p></td><td><p>累计 8 个微批次后更新一次参数</p></td></tr><tr><td><p>max_steps</p></td><td><p>20</p></td><td><p>完成 20 次优化器更新后结束训练</p></td></tr><tr><td><p>eval_steps / save_steps</p></td><td><p>10 / 10</p></td><td><p>每 10 步进行验证并保存检查点</p></td></tr></tbody></table>\n</div>\n<p>2）查看训练输出。</p>\n\n<p>训练开始后，ms-swift 会输出损失、隐式奖励、验证指标和检查点保存位置。本次训练完成后，模型将保存于<code>dpo/checkpoint</code> 目录中。模型训练及保存的结果如下图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c14-6abfc0d35b28ba.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>日志中的<code>rewards/chosen</code>和<code>rewards/rejected</code>是根据策略与参考策略的对数概率差计算的隐式奖励，不是某个外部奖励模型对回答质量打出的分数。<code>rewards/accuracies</code>表示隐式奖励排序符合偏好标签的比例，不能直接理解为模型生成回答的正确率。相关日志执行结果如下图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c14-827cd776fec13b.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c14-s15\"><b>比较 DPO 训练前后的结果</b></h3>\n\n<p>加载 DPO 适配器后，再次使用相同测试数据进行评测。比较时保持系统提示、对话模板和生成参数一致，避免把提示词变化或解码方式变化当成训练效果。</p>\n\n<pre data-language=\"Python\"><code>dpo_after, dpo_after_text = dpo_evaluate(DPO_ADAPTER)\nbefore_df = pd.DataFrame(dpo_before).set_index(\"sample_id\")\nafter_df = pd.DataFrame(dpo_after).set_index(\"sample_id\")\n\ncomparison = pd.DataFrame({\n    \"before_gap\": before_df[\"gap\"],\n    \"after_gap\": after_df[\"gap\"],\n    \"relative_dpo_margin\": DPO_BETA * (\n        after_df[\"gap\"] - before_df[\"gap\"]\n    ),\n})</code></pre>\n\n<p></p>\n\n<p>其中，<code>gap</code>等于优选回答的序列对数概率减去非优选回答的序列对数概率。<code>gap</code>大于 0，表示当前模型对给定优选回答的序列概率更高。对测试样本统计这一条件成立的比例，就得到<code>pair_preference_rate</code>。</p>\n\n<p>本次测试结果如下图所示：</p>\n\n<img src=\"assets/manuscript-20260914/c14-aae994e220ecb7.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>由于训练轮数较少，部分样本的差值可以变大，但仍然没有从负数变成正数，有些样本改善，有些样本退步。因此，平均相对 margin 为正，不一定会让优选回答占优的样本比例提高。本次实验中，这个比例在训练前后都为 43.75%。以测试集中的一个用户询问家庭网络信号的问题为例：</p>\n\n<pre data-language=\"Plain Text\"><code>用户：我能用什么方法强化我的家庭网络信号?我的笔记本电脑很难连接到网络路由器!</code></pre>\n\n<p>初始模型的回答包含下面的内容，摘录保留原文：</p>\n\n<pre data-language=\"Plain Text\"><code>1. **使用无线路由器**：如果你的家中有多个设备需要上网，可以考虑安装一个无线路由器。这将允许你通过Wi-Fi连接到互联网。</code></pre>\n\n<p>DPO 训练后的回答则包含：</p>\n\n<pre data-language=\"Plain Text\"><code>3. **重启路由器**：有时，简单的重启可以解决一些网络连接的问题。请按照路由器的指示进行操作。</code></pre>\n\n<p>从完整输出看，初始回答较多地建议更换或增加设备，训练后更多地围绕网络设置、驱动和重启等操作组织回答，说明训练改变了生成内容。本次 DPO 的训练改变了部分固定候选的相对概率和部分生成回答，但受限于训练论述和数据，读者可以使用更大规模的数据和训练轮数镜像实验。</p>\n\n<p></p>\n\n<h3 id=\"c14-s16\"><b>准备 GRPO 数据与奖励函数</b></h3>\n\n<p>DPO 使用数据中已经给定的两个回答，GRPO 则需要模型在训练时生成候选回答，再由奖励函数评价。本实验使用 GSM8K 数学应用题，以最终数字和输出格式作为可自动检查的反馈，不单独训练奖励模型。</p>\n\n<p>1）把题目和标准答案分开。</p>\n\n<p>原始 GSM8K 数据包含<code>question</code>和<code>answer</code>两个字段。<code>answer</code>中既有解题过程，也有位于<code>####</code>之后的最终数字。数据转换时，只把题目交给模型，把最终数字提取到<code>solution</code>字段中，相关代码如下：</p>\n\n<pre data-language=\"Python\"><code>def convert_grpo(row):\n    question = row[\"question\"].strip()\n    original_answer = row[\"answer\"].strip()\n    assert question and \"####\" in original_answer\n    answer = parse_numeric(original_answer.rsplit(\"####\", 1)[-1])\n    assert answer is not None\n    return {\n        \"messages\": [\n            {\"role\": \"system\", \"content\": MATH_SYSTEM},\n            {\"role\": \"user\", \"content\": question},\n        ],\n        \"solution\": str(answer),\n    }</code></pre>\n\n<p>转换后的<code>messages</code>没有标准解题过程，也没有供模型模仿的 assistant 回答。<code>solution</code>作为附加数据列交给奖励函数，不拼接到 Prompt 中。这样训练时，模型需要自己生成答案，评分程序再把它与标准答案比较。本次从原始训练来源中清洗、去重并划分 128 道训练题和 16 道验证题。训练和验证 Prompt 的长度限制为 512 Token。测试使用官方测试文件的全部 1319 道题，保留原始顺序，不按训练长度阈值删题，并检查它们与训练、验证数据是否重叠。相关执行结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c14-e21bc5b02dad13.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>2）定义正确性奖励和格式奖励。</p>\n\n<p>本实验的总奖励为：</p>\n\n<pre data-language=\"Plain Text\"><code>总奖励 = 正确性奖励 + 0.1 × 格式奖励</code></pre>\n\n<p>正确性奖励要求模型输出符合指定标签格式，并且标签中的数字与<code>solution</code>相等，满足时为 1，否则为 0。格式奖励只检查标签是否唯一、是否位于回答末尾，以及内容能否解析为数字，满足时为 1，否则为 0。相关代码如下：</p>\n\n<pre data-language=\"Python\"><code>def extract_answer(completion):\n    if not isinstance(completion, str):\n        return None\n    if completion.count(\"&lt;answer&gt;\") != 1 or completion.count(\"&lt;/answer&gt;\") != 1:\n        return None\n    match = re.search(r\"&lt;answer&gt;\\s*([^&lt;&gt;]+?)\\s*&lt;/answer&gt;\\s*\\Z\", completion)\n    return parse_numeric(match.group(1)) if match else None\n\n\nclass Chapter10Accuracy(ORM):\n    def __call__(self, completions, solution, **kwargs):\n        if len(completions) != len(solution):\n            raise ValueError(\"候选回答与 solution 数量不匹配。\")\n        rewards = []\n        for text, gold in zip(completions, solution):\n            expected = parse_numeric(gold)\n            if expected is None:\n                raise ValueError(f\"标准答案格式无效：{gold!r}\")\n            predicted = extract_answer(text)\n            rewards.append(float(predicted is not None and predicted == expected))\n        return rewards\n\n\nclass Chapter10Format(ORM):\n    def __call__(self, completions, **kwargs):\n        return [float(extract_answer(text) is not None) for text in completions]</code></pre>\n\n<p>其中，<code>completions</code>是一组模型生成的回答，<code>solution</code>是训练器传入的对应标准答案。<code>extract_answer</code>先检查标签数量，再确认标签位于回答末尾，最后提取其中的数字。这种奖励只检查最终结果，不验证解题过程。模型即使没有给出解释，只要输出正确的答案标签，也可以得到全部奖励。系统提示中的“简要解释”并没有被写入评分条件，因此不能把获得高分理解为模型已经学会完整、可靠的推理。</p>\n\n<p>奖励类还需要注册，本次训练器使用的奖励插件为：</p>\n\n<pre data-language=\"Python\"><code>orms[\"chapter10_accuracy\"] = Chapter10Accuracy\norms[\"chapter10_format\"] = Chapter10Format</code></pre>\n\n<p>Notebook 把完整插件保存为<code>plugins/chapter10_rewards.py</code>，后面通过<code>external_plugins</code>指定文件路径，并通过<code>reward_funcs</code>指定上述注册名称。插件应包含自己的导入和辅助函数，因为它会在训练子进程中独立加载。注册方法可参照 <a href=\"https://github.com/modelscope/ms-swift/blob/v4.4.2/examples/train/grpo/plugin/plugin.py\" target=\"_blank\" rel=\"noopener noreferrer\">ms-swift 4.4.2 官方奖励插件示例</a>。</p>\n\n<p>3）先检查奖励函数，再开始训练。</p>\n\n<p>以下是部分结果的奖励计算结果：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col><col><col><col></colgroup><tbody><tr><td><p>模型输出</p></td><td><p>标准答案</p></td><td><p>正确性奖励</p></td><td><p>格式奖励</p></td><td><p>总奖励</p></td></tr><tr><td><p><code>We obtain twelve. &lt;answer&gt;12&lt;/answer&gt;</code></p></td><td><p>12</p></td><td><p>1</p></td><td><p>1</p></td><td><p>1.1</p></td></tr><tr><td><p><code>&lt;answer&gt;12.0&lt;/answer&gt;</code></p></td><td><p>12</p></td><td><p>1</p></td><td><p>1</p></td><td><p>1.1</p></td></tr><tr><td><p><code>&lt;answer&gt;13&lt;/answer&gt;</code></p></td><td><p>12</p></td><td><p>0</p></td><td><p>1</p></td><td><p>0.1</p></td></tr><tr><td><p><code>The answer is 12.</code></p></td><td><p>12</p></td><td><p>0</p></td><td><p>0</p></td><td><p>0</p></td></tr><tr><td><p><code>&lt;answer&gt;11&lt;/answer&gt;&lt;answer&gt;12&lt;/answer&gt;</code></p></td><td><p>12</p></td><td><p>0</p></td><td><p>0</p></td><td><p>0</p></td></tr><tr><td><p><code>&lt;answer&gt;12&lt;/answer&gt; or maybe 13</code></p></td><td><p>12</p></td><td><p>0</p></td><td><p>0</p></td><td><p>0</p></td></tr></tbody></table>\n</div>\n<p>根据本次所采用的奖励函数计算方法，普通文本<code>The answer is 12.</code>虽然包含正确数字，但没有满足格式要求，所以严格正确性奖励仍然为 0。多个答案标签也不会通过检查，避免模型靠枚举答案获得奖励。</p>\n\n<h3 id=\"c14-s17\"><b>启动 GRPO 训练并观察日志</b></h3>\n\n<p>完成数据和奖励函数准备后，完成相关实验的设置和训练任务。</p>\n\n<p>1）设置生成组和训练参数。</p>\n\n<p>本实验对同一道题生成 4 个候选回答，生成批量也设置为 4，因此一个生成批次对应 1 道题的 4 个候选。随后每个训练微批次处理 1 个候选，累计 4 个微批次更新一次参数。</p>\n\n<p>下面是本次 GRPO 的实验配置。<code>GRPO_PATHS</code>、<code>PLUGIN_PATH</code>等相关配置及内容。</p>\n\n<pre data-language=\"Python\"><code>GRPO_OUTPUT = RUN_DIR / \"grpo\"\ngrpo_options = common_options() | {\n    \"rlhf_type\": \"grpo\", \"loss_type\": \"grpo\",\n    \"dataset\": str(GRPO_PATHS[\"train\"]),\n    \"val_dataset\": str(GRPO_PATHS[\"val\"]),\n    \"output_dir\": str(GRPO_OUTPUT),\n    \"external_plugins\": str(PLUGIN_PATH),\n    \"reward_funcs\": [\"chapter10_accuracy\", \"chapter10_format\"],\n    \"reward_weights\": [1.0, 0.1], \"remove_unused_columns\": False,\n    \"num_generations\": 4, \"generation_batch_size\": 4,\n    \"per_device_train_batch_size\": 1,\n    \"gradient_accumulation_steps\": 4,\n    \"per_device_eval_batch_size\": 4, \"num_generations_eval\": 4,\n    \"max_length\": 512, \"max_completion_length\": 256,\n    \"truncation_strategy\": \"left\",\n    \"max_steps\": 10, \"learning_rate\": 1e-5,\n    \"lr_scheduler_type\": \"constant\", \"warmup_ratio\": 0.0,\n    \"beta\": 0.04, \"num_iterations\": 1,\n    \"temperature\": 0.9, \"top_p\": 0.95,\n    \"use_vllm\": False, \"log_completions\": True,\n    \"eval_strategy\": \"steps\", \"eval_steps\": 5,\n    \"save_strategy\": \"steps\", \"save_steps\": 5,\n}\ntrain_swift(grpo_options, RUN_DIR / \"grpo_train.log\")\nGRPO_ADAPTER = latest_adapter(GRPO_OUTPUT)</code></pre>\n\n<p>组大小与生成批量需要配合设置。生成批量必须能被组大小整除，也必须能被单卡训练微批量乘以 GPU 进程数的结果整除。本次是单卡单进程，生成批量 4、组大小 4、训练微批量 1 满足这些条件；验证时批量和组大小也都为 4。如果调整组大小，应同时核对生成批量、训练梯度累积和验证配置。本次训练完成 10 步，保存了<code>grpo/checkpoint-10</code>。</p>\n\n<p></p>\n\n<img src=\"assets/manuscript-20260914/c14-891621009514f8.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>2）读取训练日志。</p>\n\n<p>GRPO 训练损失不是数学题正确率。观察训练时，应把总奖励、奖励分项和组内差异放在一起看。日志中的<code>reward</code>表示当前汇总的奖励，<code>reward_std</code>用于观察组内奖励差异，<code>frac_reward_zero_std</code>表示组内奖励标准差为零的组所占比例。相关训练统计结果可视化结果如下图所示：</p>\n\n<img src=\"assets/manuscript-20260914/c14-b2c4047eb8d6c9.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<img src=\"assets/manuscript-20260914/c14-602fc1cb962711.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>从图中可以看到，训练从第 2 步开始出现非零的组内奖励差异，同分组比例降为 0，说明奖励函数已经能够区分不同候选回答，为 GRPO 提供有效的优化信号。本次仅进行了 10 个训练步骤，正确性奖励较为稀疏，数学答案正确率尚未提高；后续可以增加训练轮数，让模型接触更多题目和候选回答，并结合验证集观察训练效果可能更为明显。</p>\n\n<h3 id=\"c14-s18\"><b>全量评测与结果分析</b></h3>\n\n<p>训练日志反映的是训练过程中采样到的候选，最终还需要用独立测试集比较模型表现。每道题只生成一个回答，不属于多次随机采样后选择最好答案的评测。</p>\n\n<p>初始结果由训练前的<code>grpo_evaluate()</code>保存。训练结束后传入 GRPO 适配器，再汇总相同题目上的结果。下面摘录评测与汇总代码；Notebook 还会检查样本编号、题目和标准答案是否一一对应。</p>\n\n<pre data-language=\"Python\"><code>grpo_after = grpo_evaluate(GRPO_ADAPTER)\ngrpo_summary = pd.DataFrame([\n    {\n        \"模型\": name,\n        \"严格答案正确率\": np.mean([r[\"correct\"] for r in rows]),\n        \"答对题数\": int(sum(r[\"correct\"] for r in rows)),\n        \"格式有效比例\": np.mean([r[\"format_ok\"] for r in rows]),\n        \"格式合格题数\": int(sum(r[\"format_ok\"] for r in rows)),\n        \"平均总奖励\": np.mean([r[\"total_reward\"] for r in rows]),\n        \"测试题数\": len(rows),\n    }\n    for name, rows in [\n        (\"初始 Instruct\", grpo_before), (\"GRPO LoRA\", grpo_after)\n    ]\n])\ndisplay(grpo_summary)</code></pre>\n\n<p>本次全量评测结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c14-e759ab0f69155f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>结合上述全量评测结果，模型的格式有效比例由 89.16% 提高到 94.24%，体现了本次训练在规范输出格式方面的作用。后续的实验和训练，可以进一步调整题目难度或奖励设计，使后续结果能够获得更充分的答案正确性反馈。</p>\n\n<p></p>\n\n<p>本章节所有实验数据和代码，可参考：https://modelscope.cn/gallery/liucong/8a5fefc5-6f90-42df-9a09-bc9781ed3da8</p>\n","imageCount":16,"attachmentCount":0,"discussionId":"chapter-14"},{"id":"chapter-15","number":15,"title":"微调有没有用，测才知道？","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-15.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-15.html","minutes":21,"status":"ready","headings":[{"id":"c15-s1","text":"分类任务评测指标","level":"h2"},{"id":"c15-s2","text":"准确率（Accuracy）","level":"h3"},{"id":"c15-s3","text":"精确率（Precision）和召回率（Recall）、F1","level":"h3"},{"id":"c15-s4","text":"混淆矩阵（Confusion Matrix）","level":"h3"},{"id":"c15-s5","text":"信息抽取评测指标","level":"h2"},{"id":"c15-s6","text":"字段准确率","level":"h3"},{"id":"c15-s7","text":"实体级 Precision、Recall 和 F1","level":"h3"},{"id":"c15-s8","text":"生成任务评测指标","level":"h2"},{"id":"c15-s9","text":"正确性","level":"h3"},{"id":"c15-s10","text":"完整性","level":"h3"},{"id":"c15-s11","text":"相关性","level":"h3"},{"id":"c15-s12","text":"格式遵循","level":"h3"},{"id":"c15-s13","text":"幻觉","level":"h3"},{"id":"c15-s14","text":"语音识别指标","level":"h2"},{"id":"c15-s15","text":"CER：字错率","level":"h3"},{"id":"c15-s16","text":"WER：词错率","level":"h3"},{"id":"c15-s17","text":"RTF：实时率","level":"h3"},{"id":"c15-s18","text":"文本转语音评价指标","level":"h2"},{"id":"c15-s19","text":"内容准确性","level":"h3"},{"id":"c15-s20","text":"语音自然度","level":"h3"},{"id":"c15-s21","text":"说话人相似度","level":"h3"},{"id":"c15-s22","text":"实时率","level":"h3"},{"id":"c15-s23","text":"图像生成客观评价","level":"h2"},{"id":"c15-s24","text":"图文一致性","level":"h3"},{"id":"c15-s25","text":"图像相似度","level":"h3"},{"id":"c15-s26","text":"FID","level":"h3"},{"id":"c15-s27","text":"美学评分","level":"h3"},{"id":"c15-s28","text":"图像生成主观评价","level":"h2"},{"id":"c15-s29","text":"视频生成模型评测","level":"h2"},{"id":"c15-s30","text":"文本与视频一致性","level":"h4"},{"id":"c15-s31","text":"弗雷歇视频距离","level":"h4"},{"id":"c15-s32","text":"时序一致性  ","level":"h4"},{"id":"c15-s33","text":"运动平滑度","level":"h4"},{"id":"c15-s34","text":"VBench","level":"h4"},{"id":"c15-s35","text":"人工评测","level":"h2"}],"html":"<p>模型微调完成后，还需要通过评测来判断模型是否真正达到了预期效果。由于不同任务的目标不同，使用的评测方法也会有所区别。</p>\n\n<p>例如，分类任务主要关注模型能否判断出正确的类别；信息抽取任务关注需要的信息是否被完整、准确地提取出来；生成任务除了判断答案是否正确，还需要关注内容的完整性和相关性；语音识别通常通过错误率评价识别结果；图像生成则需要结合自动指标和人工评价进行判断。</p>\n\n<p>具体如何测试见【先评再选：用 EvalScope 形成开源模型的第一份报告】，我们这一章告诉你不同任务有哪些评测指标～</p>\n\n<p></p>\n\n<h2 id=\"c15-s1\">分类任务评测指标</h2>\n\n<p>文本分类是模型微调中比较常见的一类任务，例如意图识别、情感分类、故障分类和风险分类等。</p>\n\n<p>通常可以将预测结果划分为四种情况：</p>\n\n<ul><li>TP（True Positive）：实际为正样本，模型预测为正样本；</li><li>TN（True Negative）：实际为负样本，模型预测为负样本；</li><li>FP（False Positive）：实际为负样本，但模型预测为正样本；</li><li>FN（False Negative）：实际为正样本，但模型预测为负样本。</li></ul>\n\n<h3 id=\"c15-s2\">准确率（Accuracy）</h3>\n\n<p>准确率表示所有样本中预测正确的比例：</p>\n\n<p><span class=\"arithmatex display-math\">\\(\\mathrm{Accuracy}=\\frac{TP+TN}{TP+TN+FP+FN}  \n\\)</span></p>\n\n<p>例如，测试 1000 条数据，模型正确预测了 900 条，那么 Accuracy 就是 90%。Accuracy 可以比较直观地反映模型整体的分类效果。但是，当不同类别的数据数量相差较大时，只看 Accuracy 可能会产生误判。当 1000 条情感分类数据中只有 20 条负向数据，如果模型简单地将所有数据都预测为“正向”，Accuracy 仍然可以达到 98%，但实际上 20 条负向数据一条都没有识别出来。所以，在类别分布不均衡的情况下，通常还需要结合 Precision、Recall 和 F1 一起评价模型。</p>\n\n<h3 id=\"c15-s3\">精确率（Precision）和召回率（Recall）、F1</h3>\n\n<p>精确率（Precision）表示模型预测为正样本的数据中，有多少真正属于正样本：</p>\n\n<p><span class=\"arithmatex\">\\(\\mathrm{Precision}=\\frac{TP}{TP+FP}  \\)</span></p>\n\n<p>召回率（Recall）表示标注为正样本的数据中，有多少被模型成功识别：</p>\n\n<p><span class=\"arithmatex\">\\(\\mathrm{Recall}=\\frac{TP}{TP+FN}  \\)</span></p>\n\n<p>F1 Score 综合考虑 Precision 和 Recall，F1 较高，通常说明模型在 Precision 和 Recall 之间取得了比较好的平衡。</p>\n\n<p><span class=\"arithmatex display-math\">\\(F1 = 2 \\times \\frac{\\mathrm{Precision} \\times \\mathrm{Recall}}\n{\\mathrm{Precision} + \\mathrm{Recall}}\n\\)</span></p>\n\n<h3 id=\"c15-s4\">混淆矩阵（Confusion Matrix）</h3>\n\n<p>Accuracy、Precision、Recall 和 F1 可以用一个数值表示模型的整体效果，但如果想知道模型容易把哪些类别判断错，我们需要使用混淆矩阵进一步分析，混淆矩阵会把每条测试数据的真实类别和模型预测类别进行对照，然后统计不同类别之间的预测情况，并将结果排列成一个矩阵。</p>\n\n<p>例如，测试集中包含网络故障，硬件故障，软件故障三种类别，模型预测结果统计如下：</p>\n\n<div class=\"table-scroll\"><table><tr><td>真实类别 \\ 预测类别</td><td>网络故障</td><td>硬件故障</td><td>软件故障</td></tr><tr><td>网络故障</td><td>45</td><td>2</td><td>3</td></tr><tr><td>硬件故障</td><td>1</td><td>47</td><td>2</td></tr><tr><td>软件故障</td><td>6</td><td>2</td><td>42</td></tr></table>\n</div>\n<p>每一行表示数据的真实类别，每一列表示模型预测的类别。矩阵对角线上的数字表示预测正确的数量，例如 50 条网络故障数据中，有 45 条被正确识别；非对角线上的数字表示预测错误的情况，例如有 6 条软件故障被错误预测成了网络故障。从混淆矩阵中不仅可以看到模型预测正确了多少数据，还可以进一步发现哪些类别之间容易发生混淆。在上面的结果中，软件故障被错误预测为网络故障的情况相对较多，说明模型对这两个类别的区分能力还有提升空间。通过分析这些容易混淆的类别，可以进一步发现模型存在的问题，为后续补充训练数据和优化模型提供参考。</p>\n\n<h2 id=\"c15-s5\">信息抽取评测指标</h2>\n\n<p>对于命名实体识别、字段抽取、结构化信息提取等任务，常用的评测指标包括字段准确率、实体级 Precision、Recall 和 F1。不同指标关注的评测粒度不同，可以根据具体任务选择使用。</p>\n\n<h3 id=\"c15-s6\">字段准确率</h3>\n\n<p>字段准确率主要用于衡量模型对指定字段的抽取是否正确，其计算方式为：</p>\n\n<p><span class=\"arithmatex display-math\">\\(\\text{字段准确率} =\n\\frac{\\text{正确抽取的字段数量}}\n{\\text{需要抽取的字段总数量}}\n\\)</span></p>\n\n<p>对于包含多个字段的任务，可以分别统计不同字段的准确率。例如分别计算公司名、人名的准确率，从而进一步分析模型在哪些字段上的抽取能力较弱，但是这种方法也有缺陷，无法确定模型漏抽了哪些实体，因此对于包含多个实体的信息抽取任务，还需要使用实体级的 Precision、Recall 和 F1 进行更加细致的评测。</p>\n\n<h3 id=\"c15-s7\">实体级 Precision、Recall 和 F1</h3>\n\n<p>对于命名实体识别（NER）任务，实体级 Precision、Recall 和 F1 进行评价，这个部分和上一节的分类人评价指标类似。</p>\n\n<p>其中：</p>\n\n<ul><li>TP（True Positive）：模型正确识别出的实体；</li><li>FP（False Positive）：模型识别出了实体，但识别结果错误；</li><li>FN（False Negative）：标准答案中存在实体，但模型没有识别出来。</li></ul>\n\n<p>Precision 用于衡量模型识别出的实体中，有多少是正确的：</p>\n\n<p><span class=\"arithmatex display-math\">\\(Precision = \\frac{TP}{TP + FP}\n\\)</span></p>\n\n<p>Recall 用于衡量标准答案中的实体，有多少被模型成功识别出来：</p>\n\n<p><span class=\"arithmatex display-math\">\\(Recall = \\frac{TP}{TP + FN}\n\\)</span></p>\n\n<p>F1 综合考虑 Precision 和 Recall：</p>\n\n<p><span class=\"arithmatex display-math\">\\(F1 =\n\\frac{2 \\times Precision \\times Recall}\n{Precision + Recall}\n\\)</span></p>\n\n<p>Precision 较高，说明模型识别出的实体整体比较准确；Recall 较高，说明模型漏掉的实体较少；F1 则用于综合衡量两方面的表现。</p>\n\n<h2 id=\"c15-s8\">生成任务评测指标</h2>\n\n<p>与分类和信息抽取任务不同，大语言模型生成的内容通常具有较强的开放性，模型输出通常不存在唯一的标准答案，这类任务通常需要从正确性、完整性、相关性、格式遵循和幻觉等多个维度进行评价。</p>\n\n<h3 id=\"c15-s9\">正确性</h3>\n\n<p>正确性主要判断模型生成的内容是否正确，包括事实、结论以及推理结果等是否存在错误。对于微调后的模型，即使回答语言流畅、格式完整，如果其中包含错误的事实或结论，也不能认为模型取得了较好的生成效果。正确性通常是评价生成结果时最基本的要求。</p>\n\n<h3 id=\"c15-s10\">完整性</h3>\n\n<p>完整性主要判断模型是否充分回答了问题，是否遗漏了应该包含的重要信息。有些回答虽然内容本身没有明显错误，但只回答了问题的一部分，遗漏了关键内容。这种情况下，模型的回答具有一定正确性，但完整性仍然不足。完整性关注的不只是回答得对不对，还需要判断应该回答的内容是否都回答到了。</p>\n\n<h3 id=\"c15-s11\">相关性</h3>\n\n<p>相关性主要判断模型生成的内容是否围绕用户的问题展开。大语言模型有时会生成大量看起来合理但与问题关系不大的内容。虽然这些内容本身可能没有错误，但会降低回答的有效性。相关性较好的回答应该直接回应用户的问题，并尽量减少无关、重复或者过度扩展的内容。</p>\n\n<h3 id=\"c15-s12\">格式遵循</h3>\n\n<p>在实际应用中，除了回答内容本身，模型通常还需要按照指定的格式输出结果。例如，要求模型按照 JSON 格式返回结果，或者按照规定的字段、顺序和结构组织内容。格式遵循主要评价模型是否按照给定的输出要求生成内容。即使回答内容正确，如果没有按照规定格式输出，也可能导致后续程序无法正常解析和使用。对于经过指令微调或具有固定输出要求的模型，格式遵循也是一个重要的评价维度。</p>\n\n<h3 id=\"c15-s13\">幻觉</h3>\n\n<p>幻觉（Hallucination）是指模型生成了缺少依据、与事实不符或者凭空编造的内容。当模型不知道某项信息时，没有说明信息不足，而是生成了一个看起来合理但实际上并不存在的事实，这就属于典型的幻觉问题。</p>\n\n<p>幻觉与正确性存在一定联系，但关注的重点有所不同。正确性关注回答内容是否正确，而幻觉更关注模型是否生成了没有可靠依据的信息。对于知识问答等对事实可靠性要求较高的场景，需要重点关注模型的幻觉情况。</p>\n\n<h2 id=\"c15-s14\">语音识别指标</h2>\n\n<p>语音识别（ASR）任务，通常需要从识别准确性和处理效率两个方面进行评测。其中，CER 和 WER 用于衡量识别结果中的错误情况，RTF 用于衡量模型处理语音的速度。</p>\n\n<p>在介绍 CER 和 WER 之前，先说明语音识别错误率计算中常用的几个符号：</p>\n\n<ul><li>S（Substitution）：替换错误，将原本正确的字符或单词识别成其他内容； </li><li>D（Deletion）：删除错误，漏掉了原本应该识别的字符或单词； </li><li>I（Insertion）：插入错误，额外识别出了文本中不存在的字符或单词； </li><li>N：标准文本中的字符或单词总数。 </li></ul>\n\n<p>CER 和 WER 都基于这几类错误进行计算，主要区别在于两者采用的统计单位不同。</p>\n\n<h3 id=\"c15-s15\">CER：字错率</h3>\n\n<p>CER（Character Error Rate，字错率）以字符为单位衡量语音识别结果与标准文本之间的差异，其计算公式为：</p>\n\n<p><span class=\"arithmatex display-math\">\\(CER = \\frac{S + D + I}{N}\n\\)</span></p>\n\n<p>CER 越低，通常表示语音识别结果越准确。对于中文语音识别任务，CER 是较为常用的评测指标。</p>\n\n<h3 id=\"c15-s16\">WER：词错率</h3>\n\n<p>WER（Word Error Rate，词错率）与 CER 的计算方式基本相同：</p>\n\n<p><span class=\"arithmatex display-math\">\\(WER = \\frac{S + D + I}{N}\n\\)</span></p>\n\n<p>CER 以字符为基本单位，而 WER 以单词为基本单位，在英文等以单词为基本语言单位的语音识别任务中使用较多。</p>\n\n<h3 id=\"c15-s17\">RTF：实时率</h3>\n\n<p>除了识别结果是否准确，语音识别模型在实际部署时还需要考虑处理速度。RTF（Real-Time Factor，实时率）是衡量语音识别处理效率的常用指标，其计算公式为：</p>\n\n<p><span class=\"arithmatex display-math\">\\(RTF =\n\\frac{\\text{模型处理时间}}\n{\\text{语音时长}}\n\\)</span></p>\n\n<p>例如，一段 10 秒的语音需要 5 秒完成识别，则：</p>\n\n<p><span class=\"arithmatex display-math\">\\(RTF = \\frac{5}{10} = 0.5\n\\)</span></p>\n\n<p>RTF 越小，说明模型处理速度越快。当 RTF 小于 1 时，表示模型的处理速度快于语音的实际播放速度，具备实时处理的基本条件。</p>\n\n<p>除了 CER、WER 和 RTF 等指标，还可以在不同测试环境下对模型进行评测。例如，在背景噪声、不同口音、远距离录音和多人说话等条件下分别计算 CER 或 WER，通过比较不同场景下的错误率，进一步评价模型在复杂环境中的识别能力和稳定性。</p>\n\n<h2 id=\"c15-s18\">文本转语音评价指标</h2>\n\n<p>文本转语音（Text-to-Speech，TTS）的目标是将输入文本转换为自然、清晰的语音。与文本生成模型不同，TTS 模型不仅需要保证生成内容与输入文本一致，还需要关注语音的自然度、音色以及生成速度。TTS 模型通常从内容准确性、语音自然度、说话人相似度和推理性能等方面进行评测。</p>\n\n<h3 id=\"c15-s19\">内容准确性</h3>\n\n<p>TTS 首先需要保证输入的文本能够被正确朗读，避免出现错读、漏读或重复等问题。评测时可以使用 ASR 模型将 TTS 生成的语音重新识别为文本，再将识别结果与原始输入文本进行比较。内容准确性的指标和上一章节 ASR 中的指标一致，计算字错率（CER）与词错率（WER）。</p>\n\n<h3 id=\"c15-s20\">语音自然度</h3>\n\n<p>除了正确朗读文本，TTS 生成的语音还需要具有较好的自然度，例如语速是否合理、停顿是否自然、语调是否流畅，以及是否存在明显的机械感。通常采用平均意见得分（Mean Opinion Score，MOS）。评测时由多名测试人员试听生成语音，并按照一定的评分标准进行打分，通常采用 1～5 分制，其中 1 分表示语音质量较差，5 分表示语音自然、流畅，接近真人语音。</p>\n\n<p>最终 MOS 可以表示为：</p>\n\n<p><span class=\"arithmatex display-math\">\\(MOS = \\frac{1}{M}\\sum_{i=1}^{M}s_i\n\\)</span></p>\n\n<p>其中，<span class=\"arithmatex\">\\(M\\)</span> 表示参与评分的人数，<span class=\"arithmatex\">\\(s_i\\)</span> 表示第 <span class=\"arithmatex\">\\(i\\)</span> 位评测人员给出的分数。</p>\n\n<p>MOS 能够较直观地反映用户对生成语音的真实听感，但由于依赖人工试听，其评测成本较高，同时也具有一定的主观性。</p>\n\n<h3 id=\"c15-s21\">说话人相似度</h3>\n\n<p>对于声音克隆、多说话人 TTS 或零样本 TTS 模型，仅保证语音自然还不够，还需要评价生成语音是否保持了目标说话人的音色特征。常见方法是使用说话人识别模型分别提取参考语音和生成语音的 Speaker Embedding，然后计算两个向量之间的余弦相似度：</p>\n\n<p><span class=\"arithmatex display-math\">\\(SIM =\n\\frac{\n\\mathbf{e}{ref} \\cdot \\mathbf{e}{gen}\n}{\n|\\mathbf{e}{ref}| |\\mathbf{e}{gen}|\n}\n\\)</span></p>\n\n<p>其中，<span class=\"arithmatex\">\\(\\mathbf{e}{ref}\\)</span> 表示参考语音的说话人向量，<span class=\"arithmatex\">\\(\\mathbf{e}{gen}\\)</span> 表示生成语音的说话人向量。</p>\n\n<p>SIM 越高，说明生成语音与参考说话人在音色特征上越接近。该指标尤其适合用于声音克隆模型的效果评测。</p>\n\n<h3 id=\"c15-s22\">实时率</h3>\n\n<p>在推理性能方面，TTS 与语音识别模型类似，也可以使用实时率（RTF）衡量模型的处理效率。不同之处在于，语音识别中的 RTF 通常表示识别耗时与输入语音时长的比值，而 TTS 中则表示语音生成耗时与生成语音时长的比值。RTF 越低，表示模型的语音生成效率越高。</p>\n\n<p>RTF 的计算公式为：</p>\n\n<p><span class=\"arithmatex display-math\">\\(RTF =\n\\frac{T_{infer}}{T_{audio}}\n\\)</span></p>\n\n<p>其中，<span class=\"arithmatex\">\\(T_{infer}\\)</span> 表示生成语音所需要的推理时间，<span class=\"arithmatex\">\\(T_{audio}\\)</span> 表示最终生成语音的时长。</p>\n\n<p>对于流式 TTS 模型，还可以进一步关注首包延迟（Time to First Audio），即从模型接收到文本请求到输出第一段可播放音频所经历的时间。首包延迟越低，用户越快能够听到生成语音，因此该指标对于实时语音交互场景尤为重要。</p>\n\n<h2 id=\"c15-s23\">图像生成客观评价</h2>\n\n<p>对于文生图、图生图和图像编辑等任务，仅通过一个指标很难全面评价生成效果。例如，文生图需要判断生成图片是否符合文本描述，图像编辑还需要判断生成结果是否保留了原图中的主体和结构，而在评价一个图像生成模型的整体效果时，还需要考虑生成图片的质量和真实性。</p>\n\n<h3 id=\"c15-s24\">图文一致性</h3>\n\n<p>对于文生图任务，首先需要判断生成图片是否符合输入的文本描述。例如，提示词中要求生成特定的主体、场景或者风格，模型生成的图片应当尽可能包含这些内容。</p>\n\n<p>一种常见的评价思路是使用 CLIP 等视觉语言模型，将文本和图片映射到同一个特征空间，再计算两者之间的相似程度。<a href=\"https://arxiv.org/abs/2103.00020\" target=\"_blank\" rel=\"noopener noreferrer\">CLIP </a>由 Radford 等人在 2021 年提出，通过大规模“图像—文本”数据进行训练，使模型能够学习图像内容与自然语言之间的对应关系，图像与文本的余弦相似度可以表示为：</p>\n\n<p><span class=\"arithmatex display-math\">\\(Similarity =\n\\frac{\\mathbf{v}{image}\\cdot\\mathbf{v}{text}}\n{|\\mathbf{v}{image}||\\mathbf{v}{text}|}\n\n\\)</span></p>\n\n<p>其中，<span class=\"arithmatex display-math\">\\(\\mathbf{v}{image} \n\\)</span>表示图片特征向量，<em><span class=\"arithmatex display-math\">\\(\\mathbf{v}{text} \n\\)</span></em>表示文本特征向量。通常情况下，相似度越高，说明图片与文本在整体语义上越接近。这类方法主要评价整体语义是否匹配，并不能保证文本中的所有细节都被准确生成。比如判断图片中的数量、位置和细节是否准确。</p>\n\n<h3 id=\"c15-s25\">图像相似度</h3>\n\n<p>对于图生图和图像编辑任务，通常还会提供一张原始图片或者参考图片。这类任务不仅需要判断生成结果是否满足文本要求，还需要关注原图中的重要内容是否被正确保留下来。</p>\n\n<p>例如，在商品图片编辑中，可以修改背景、光照或者整体风格，但通常希望商品主体、外观结构和 Logo 等重要信息保持一致。此时，可以通过比较生成图片和参考图片之间的差异评价编辑效果。</p>\n\n<p>SSIM（Structural Similarity Index，结构相似性指数）是一种经典的全参考图像质量评价方法，由 Wang 等人在 2004 年提出。它主要从亮度、对比度和结构等方面比较两张图片，尤其关注图像结构信息是否发生变化。</p>\n\n<p>除了 SSIM，还可以使用 LPIPS（Learned Perceptual Image Patch Similarity）衡量两张图片之间的感知差异。LPIPS 利用深度神经网络提取图像特征，再根据特征之间的距离判断两张图片在视觉感知上的差异。相关研究表明，深度特征能够较好地反映人对图像相似程度的感知。SSIM 和 LPIPS 虽然都需要参考图片，但关注的角度并不完全相同：SSIM 更加关注图像结构的变化，而 LPIPS 更加关注视觉感知层面的差异。在实际任务中，可以根据图像编辑目标选择合适的指标。</p>\n\n<p>对于图像编辑、图生图等具有参考图片的任务，可以计算生成图片与参考图片之间的相似程度。</p>\n\n<h3 id=\"c15-s26\">FID</h3>\n\n<p>前面的指标主要用于比较一张生成图片与文本或者参考图片之间的关系。如果希望评价一个图像生成模型的整体生成效果，通常需要对一批生成图片进行统计分析。</p>\n\n<p>FID（Fréchet Inception Distance）由 Heusel 等人在 2017 年提出，主要用于衡量生成图片与真实图片在特征分布上的差异，<a href=\"https://arxiv.org/abs/1706.08500\" target=\"_blank\" rel=\"noopener noreferrer\">FID</a>。FID 首先使用图像模型提取真实图片和生成图片的特征，然后分别统计两组特征的分布，并计算两个分布之间的距离。其基本形式为：</p>\n\n<p><span class=\"arithmatex display-math\">\\(FID =\n|\\mu_r-\\mu_g|_2^2\n+\nTr\\left(\n\\Sigma_r+\\Sigma_g\n-2(\\Sigma_r\\Sigma_g)^{1/2}\n\\right)\n\\)</span></p>\n\n<p>其中，<span class=\"arithmatex\">\\(\\mu_r\\)</span>和<span class=\"arithmatex\">\\(\\Sigma_r\\)</span>表示真实图片特征的均值和协方差矩阵，<span class=\"arithmatex\">\\(\\mu_g\\)</span>和<span class=\"arithmatex\">\\(\\Sigma_g\\)</span>表示生成图片特征的均值和协方差矩阵。FID 比较的不是两张具体图片，而是两批图片在特征分布上的差异。FID 越低，说明生成图片与真实图片的特征分布越接近，比较适合用于模型之间的整体效果比较。</p>\n\n<h3 id=\"c15-s27\">美学评分</h3>\n\n<p>除了评价图片是否符合文本描述，还可以从视觉效果的角度对生成图片进行自动评价。美学评分（Aesthetic Score）通常利用包含人类审美评分或偏好信息的数据训练评价模型，让模型学习人对图片视觉质量的偏好，再对生成图片进行自动打分。</p>\n\n<p>这类方法通常会综合反映图片的构图、色彩、光影、清晰度和整体视觉吸引力等特征。评分越高，一般表示图片在所使用的评价模型下具有更好的视觉表现。美学评价具有一定的主观性，不同评分模型的训练数据和审美偏好可能存在差异，因此美学评分更适合作为辅助指标，而不能单独代表图像生成质量。</p>\n\n<h2 id=\"c15-s28\">图像生成主观评价</h2>\n\n<p>客观指标可以从图文一致性、图像相似度和特征分布等方面评价生成结果，但这些指标并不能完全反映图片的实际视觉效果。因此，在图像生成任务中，通常还需要结合人工评价，从人的视觉感受和实际使用需求出发判断生成图片的质量。</p>\n\n<p>人工评价可以重点关注以下几个方面：</p>\n\n<ul><li>内容一致性：生成图片是否符合输入的文本描述或编辑要求； </li><li>视觉质量：图片是否清晰、自然，是否存在明显的变形、模糊或结构错误； </li><li>主体一致性：对于图生图和图像编辑任务，人物、商品等主要对象是否与参考图片保持一致； </li><li>细节质量：人物手部、文字、Logo、边缘等容易出现错误的位置是否正常； </li><li>实际可用性：生成结果是否能够直接用于海报、商品展示等实际业务场景。 </li></ul>\n\n<p>为了减少个人主观判断带来的差异，可以提前制定统一的评分标准，并由多名评测人员分别进行评价，主观评价能够发现自动化指标难以反映的视觉问题，因此在实际图像生成评测中，通常需要将客观指标与主观评价结合使用，从而更加全面地判断模型的生成效果。</p>\n\n<h2 id=\"c15-s29\">视频生成模型评测</h2>\n\n<p>视频生成模型需要根据文本、图像等输入生成连续的视频内容。与图像生成相比，视频不仅需要保证单帧画面的质量，还需要关注不同帧之间的连续性、运动合理性以及生成内容与输入条件的一致性。因此，视频生成模型通常需要从文本与视频一致性、整体视频质量、时序一致性、运动质量以及主观观看效果等多个方面进行综合评测。</p>\n\n<h4 id=\"c15-s30\">文本与视频一致性</h4>\n\n<p>与文生图任务类似，文生视频任务也需要评价生成内容与输入文本之间的语义一致性。不同之处在于，视频由连续的视频帧组成，需要进一步考虑整个视频内容与文本描述的匹配程度。主要方法是从生成视频中抽取若干视频帧，使用 CLIP 等视觉语言模型分别计算各视频帧与输入文本之间的语义相似度，再对多个视频帧的结果进行平均（常称为 Frame-average CLIP Score）： </p>\n\n<p><span class=\"arithmatex\">\\(\\text{CLIP-Score} = \\frac{1}{N} \\sum_{i=1}^{N} \\operatorname{Similarity}(\\mathbf{v}_{\\text{frame}_i}, \\mathbf{v}_{\\text{text}})\\)</span> </p>\n\n<p>其中，<span class=\"arithmatex\">\\(N\\)</span> 表示参与评测的视频帧数量，<span class=\"arithmatex\">\\(\\mathbf{v}_{\\text{frame}_i}\\)</span> 表示第 <span class=\"arithmatex\">\\(i\\)</span> 个视频帧的特征向量，<span class=\"arithmatex\">\\(\\mathbf{v}_{\\text{text}}\\)</span> 表示输入文本的特征向量，<span class=\"arithmatex\">\\(\\operatorname{Similarity}(\\cdot, \\cdot)\\)</span> 采用余弦相似度，得分越高，表示视频画面内容与输入文本的语义匹配程度越高。该指标主要关注空间静态内容与文本的匹配，无法充分反映视频中的动作连贯性与物理时序逻辑，因此仍需结合时序一致性、运动质量等指标综合衡量。  </p>\n\n<h4 id=\"c15-s31\">弗雷歇视频距离</h4>\n\n<p>弗雷歇视频距离（Fréchet Video Distance，FVD）是视频生成任务中衡量生成分布质量的核心指标，用于度量生成视频集与真实视频集在深层特征分布上的差异。计算 FVD 时，首先提取真实视频和生成视频的深层特征，统计两组特征的高斯分布参数（均值与协方差矩阵），并计算其 Fréchet 距离： <span class=\"arithmatex\">\\(\\mathrm{FVD} = \\|\\mu_r - \\mu_g\\|_2^2 + \\operatorname{Tr}\\left(\\Sigma_r + \\Sigma_g - 2\\left(\\Sigma_r^{1/2} \\Sigma_g \\Sigma_r^{1/2}\\right)^{1/2}\\right)\\)</span> </p>\n\n<p>其中，<span class=\"arithmatex\">\\(\\mu_r\\)</span> 和 <span class=\"arithmatex\">\\(\\Sigma_r\\)</span> 分别表示真实视频特征的均值向量和协方差矩阵，<span class=\"arithmatex\">\\(\\mu_g\\)</span> 和 <span class=\"arithmatex\">\\(\\Sigma_g\\)</span> 分别表示生成视频特征的均值向量和协方差矩阵，<span class=\"arithmatex\">\\(\\operatorname{Tr}(\\cdot)\\)</span> 表示矩阵的迹。FVD 数值越低，表示生成视频的特征分布与真实视频越接近，说明模型在视觉保真度、时序合理性及样本多样性上的综合表现越好。</p>\n\n<h4 id=\"c15-s32\">时序一致性  </h4>\n\n<p>时序一致性（Temporal Consistency）主要用于评价生成视频在时间维度上的稳定性。视频由连续帧组成，不仅要求每一帧具有良好的画质，还要求主体外观、背景纹理、色彩与风格在连续帧之间平稳过渡。例如，在人物生成场景中，如果面部特征或服装纹理在帧间频繁闪烁、变形，即使单帧质量再高，观感也会大打折扣。 在实际评测中，一种常见的方法是使用图像特征提取网络获取连续帧的表征，并计算相邻帧特征的余弦相似度均值。对于包含 <span class=\"arithmatex\">\\(N\\)</span> 个视频帧的视频，其时序一致性 <span class=\"arithmatex\">\\(TC\\)</span> 可以表示为：</p>\n\n<p> <span class=\"arithmatex\">\\(TC = \\frac{1}{N-1} \\sum_{i=1}^{N-1} \\frac{\\mathbf{f}_i^{\\top} \\mathbf{f}_{i+1}}{\\|\\mathbf{f}_i\\|_2 \\|\\mathbf{f}_{i+1}\\|_2}\\)</span> </p>\n\n<p>其中，<span class=\"arithmatex\">\\(\\mathbf{f}_i\\)</span> 与 <span class=\"arithmatex\">\\(\\mathbf{f}_{i+1}\\)</span> 分别表示第 <span class=\"arithmatex\">\\(i\\)</span> 帧与第 <span class=\"arithmatex\">\\(i+1\\)</span> 帧的视觉特征向量，<span class=\"arithmatex\">\\(N\\)</span> 为采样帧总数。通常情况下，<span class=\"arithmatex\">\\(TC\\)</span> 得分越高，说明相邻帧的视觉跳变越小、画面越稳定。时序一致性并非绝对越高越好：当模型退化并生成几乎完全静止的画面时，<span class=\"arithmatex\">\\(TC\\)</span> 也会异常偏高。时序一致性必须配合动态程度（Dynamic Degree）、运动幅度等指标联合判断。  </p>\n\n<h4 id=\"c15-s33\">运动平滑度</h4>\n\n<p>视频生成模型不仅要避免画面闪烁，还需要保证主体运动轨迹连贯自然，避免出现局部肢体突变、瞬间位移等不符合物理常识的现象。 运动平滑度（Motion Smoothness）主要用于衡量运动速度与加速度在时间序列上的变化平稳性。在具体计算时，通常借助预训练光流估计模型（如 RAFT）提取相邻帧之间的稠密光流场或运动矢量。 设第 <span class=\"arithmatex\">\\(t\\)</span> 帧到第 <span class=\"arithmatex\">\\(t+1\\)</span> 帧的运动矢量（或平均光流）为 <span class=\"arithmatex\">\\(\\mathbf{v}_t\\)</span>，可以通过相邻时段运动矢量的差异来定义运动变化误差 <span class=\"arithmatex\">\\(E_{\\text{motion}}\\)</span>： </p>\n\n<p><span class=\"arithmatex\">\\(E_{\\text{motion}} = \\frac{1}{N-2} \\sum_{t=1}^{N-2} \\|\\mathbf{v}_{t+1} - \\mathbf{v}_t\\|_2\\)</span> </p>\n\n<p>其中，<span class=\"arithmatex\">\\(\\mathbf{v}_t\\)</span> 反映第 <span class=\"arithmatex\">\\(t\\)</span>阶段的运动状态，<span class=\"arithmatex\">\\(E_{\\text{motion}}\\)</span> 越小，说明运动加速度突变越少，动作过渡越平缓平滑。</p>\n\n<h4 id=\"c15-s34\">VBench</h4>\n\n<p>由于单一指标很难全面反映视频生成模型的能力，目前也可以使用 VBench 等综合评测框架对视频生成模型进行多维度评价。VBench 将视频生成质量划分为多个评价维度，例如主体一致性（Subject Consistency）、背景一致性（Background Consistency）、运动平滑度（Motion Smoothness）、动态程度（Dynamic Degree）、美学质量（Aesthetic Quality）以及成像质量（Imaging Quality）等。与 CLIP Score、FVD 等单一指标相比，VBench 的优势在于能够从画面质量、时序稳定性、运动表现和条件一致性等多个角度分析视频生成效果，更加适合用于不同视频生成模型之间的综合能力比较。</p>\n\n<h2 id=\"c15-s35\">人工评测</h2>\n\n<p>大语言模型的输出具有较强的开放性，同一个问题往往可以有多种合理的回答方式。仅依靠自动化指标，很难完整判断回答是否正确并符合用户需求，因此，在评价微调后的大语言模型时，人工评测仍然是重要的评测方式。对于难以通过自动指标准确评价的开放式内容，可以采用盲测或多人评分的方式进行评测。评测人员可以从正确性、完整性、相关性、指令遵循、表达质量和安全性等方面，对模型输出进行综合判断。</p>\n\n<p>实际评测时，可以提前制定统一的评分标准，例如按照 1～5 分对不同评价维度进行评分。为了减少评测人员对模型的先入为主，可以隐藏模型名称和版本，让评测人员只根据回答内容进行盲测。如果需要比较微调前后的模型，还可以将两个模型对同一问题的回答匿名展示，由评测人员选择表现更好的回答。</p>\n\n<p>由于人工评价具有一定的主观性，对于重要的评测还可以采用多人评分。同一批内容由多名评测人员独立评价，再对评分结果进行汇总。当不同评测人员的评分差异较大时，还需要进一步检查评价标准是否清晰，从而提高评测结果的一致性和可靠性。人工评测虽然需要投入更多时间和人力，但能够发现自动化指标难以反映的问题。在评价大语言模型的微调效果时，可以将自动化评测与人工评测结合起来，从而更加全面地判断模型是否真正得到提升。</p>\n","imageCount":0,"attachmentCount":0,"discussionId":"chapter-15"},{"id":"chapter-16","number":16,"title":"AI 健身教练：跟着视频练，让 AI 指出动作不标准的地方","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-16.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-16.html","minutes":39,"status":"ready","headings":[{"id":"c16-s1","text":"先在视频里找出肩、肘、髋、膝的位置","level":"h2"},{"id":"c16-s2","text":"人有高有矮，先把坐标尺度统一","level":"h2"},{"id":"c16-s3","text":"动作有快有慢，要比较同一个阶段","level":"h2"},{"id":"c16-s4","text":"把动作差异算出来，看看关节角度差多少","level":"h2"},{"id":"c16-s5","text":"有了角度和画面，让多模态模型解释差异","level":"h2"},{"id":"c16-s6","text":"先分清做的是什么动作，再逐段分析","level":"h3"},{"id":"c16-s7","text":"把角度差异和画面对上，生成动作反馈","level":"h3"},{"id":"c16-s8","text":"多记录几次，看看动作有没有变化","level":"h3"}],"html":"<p>健身很容易出现动作不标准的情况，如果没有教练指导，AI 能顶替教练吗？</p>\n\n<p>如果把跟练过程录下来，能不能让 AI 先帮我们找出明显的差别？这一章会沿着这个思路做一个 AI 健身教练的示例。</p>\n\n<p></p>\n\n<p>先从视频里找出肩、肘、髋、膝等人体关键点，把动作变成可以比较的数据；再结合关节角度和动作发生的时间，找出学生与教练示范之间的差异。最后将这些结果和对应画面一起交给多模态模型，尝试生成更容易理解的反馈。</p>\n\n<p></p>\n\n<h2 id=\"c16-s1\">先在视频里找出肩、肘、髋、膝的位置</h2>\n\n<p>分析健身动作的第一步，是让计算机能够识别人体在视频中的姿态。视频本质上由连续的图像组成。对于计算机来说，原始图像也只是像素，并不知道画面中的肩膀、膝盖或者脚踝分别位于什么位置，所以我们首先使用人体姿态估计（Human Pose Estimation）技术，从每一帧图像中识别人体的主要位置。</p>\n\n<p>本节实验使用 MediaPipe Pose 进行人体姿态识别。MediaPipe Pose 可以检测人体的 33 个关键点，包括肩部、肘部、手腕、髋部、膝盖和脚踝等位置。</p>\n\n<img src=\"assets/manuscript-20260914/c16-d01e3ff2d01c12.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>本次实验还是在 ModelScope 中的 Notebook 进行实验，实现要从视频中抽取关键点.</p>\n\n<p>1) 如果是直接运行代码的话，可能会报错 OSError: libEGL.so.1: cannot open shared object file: No such file or directory，所以这个步骤，我们需要先安装一些包</p>\n\n<pre data-language=\"Plain Text\"><code>apt-get install -y libegl1 libgl1 libgles2 libglib2.0-0 libsm6 libxext6 libxrender1</code></pre>\n\n<p>安装完成后提示：</p>\n\n<img src=\"assets/manuscript-20260914/c16-291413e81f782e.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>2）抽取关键点,代码如下：</p>\n\n<pre data-language=\"Python\"><code># -*- coding: utf-8 -*-\n\nimport cv2\nimport json\nimport argparse\nimport numpy as np\nimport mediapipe as mp\nfrom mediapipe.tasks import python as mp_python\nfrom mediapipe.tasks.python import vision\n\n# 用于画骨骼图的连线 (只画主干，不画手指/脚趾细节，demo够用)\nPOSE_CONNECTIONS = [\n    (11, 12),               # 双肩\n    (11, 13), (13, 15),     # 左臂: 肩-肘-腕\n    (12, 14), (14, 16),     # 右臂: 肩-肘-腕\n    (11, 23), (12, 24),     # 肩到髋\n    (23, 24),               # 双髋\n    (23, 25), (25, 27),     # 左腿: 髋-膝-踝\n    (24, 26), (26, 28),     # 右腿: 髋-膝-踝\n    (27, 29), (28, 30),     # 踝-脚跟\n    (27, 31), (28, 32),     # 踝-脚尖\n]\n\ndef create_landmarker(model_path):\n    \"\"\"创建一个 PoseLandmarker 实例 (视频模式，逐帧按时间戳处理)\"\"\"\n    base_options = mp_python.BaseOptions(model_asset_path=model_path)\n    options = vision.PoseLandmarkerOptions(\n        base_options=base_options,\n        running_mode=vision.RunningMode.VIDEO,\n        num_poses=1,                       # 每个画面只有一个人，设为1提高速度和准确率\n        min_pose_detection_confidence=0.5,\n        min_pose_presence_confidence=0.5,\n        min_tracking_confidence=0.5,\n    )\n    return vision.PoseLandmarker.create_from_options(options)\n\ndef detect(landmarker, frame_bgr, timestamp_ms):\n    \"\"\"对一帧图像做姿态检测，返回 33x4 的关键点列表 [x,y,z,visibility]，检测不到返回 None\"\"\"\n    rgb = cv2.cvtColor(frame_bgr, cv2.COLOR_BGR2RGB)\n    mp_image = mp.Image(image_format=mp.ImageFormat.SRGB, data=rgb)\n    result = landmarker.detect_for_video(mp_image, timestamp_ms)\n    if not result.pose_landmarks:\n        return None\n    lm = result.pose_landmarks[0]  # 取第一个人\n    return [[p.x, p.y, p.z, p.visibility] for p in lm]\n\ndef draw_skeleton(frame, landmarks):\n    \"\"\"手动画骨骼线 (不依赖 mediapipe.solutions，因为部分新版本裁掉了这个模块)\"\"\"\n    if landmarks is None:\n        return frame\n    h, w = frame.shape[:2]\n    pts = [(int(p[0] * w), int(p[1] * h)) for p in landmarks]\n    for i, j in POSE_CONNECTIONS:\n        cv2.line(frame, pts[i], pts[j], (0, 255, 0), 2)\n    for x, y in pts:\n        cv2.circle(frame, (x, y), 3, (0, 0, 255), -1)\n    return frame\n\ndef main():\n\n    video=\"/mnt/workspace/AIfitness/0912.mp4\"\n    model=\"/mnt/workspace/AIfitness/pose_landmarker_full.task\"\n    split=0.5\n    out=\"/mnt/workspace/AIfitness/keypoints.json\"  #\"输出关键点json路径\"\n    debug_video=\"/mnt/workspace/AIfitness/debug_overlay.mp4\"\n    cap = cv2.VideoCapture(video)\n    if not cap.isOpened():\n        raise RuntimeError(f\"打不开视频: {video}\")\n\n    fps = cap.get(cv2.CAP_PROP_FPS)\n    w = int(cap.get(cv2.CAP_PROP_FRAME_WIDTH))\n    h = int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT))\n    split_x = int(w * split)\n\n    print(f\"视频信息: {w}x{h}, {fps:.1f}fps, 分割线 x={split_x}\")\n\n    landmarker_coach = create_landmarker(model)\n    landmarker_student = create_landmarker(model)\n\n    writer = cv2.VideoWriter(debug_video, cv2.VideoWriter_fourcc(*\"mp4v\"), fps, (w, h))\n\n    frames_data = []\n    frame_idx = 0\n    detected_coach, detected_student = 0, 0\n\n    while True:\n        ret, frame = cap.read()\n        if not ret:\n            break\n\n        timestamp_ms = int(frame_idx * 1000 / fps)\n\n        left = frame[:, :split_x].copy()\n        right = frame[:, split_x:].copy()\n\n        coach_lm = detect(landmarker_coach, left, timestamp_ms)\n        student_lm = detect(landmarker_student, right, timestamp_ms)\n\n        if coach_lm is not None:\n            detected_coach += 1\n        if student_lm is not None:\n            detected_student += 1\n        left_draw = draw_skeleton(left, coach_lm)\n        right_draw = draw_skeleton(right, student_lm)\n        writer.write(np.concatenate([left_draw, right_draw], axis=1))\n\n        frames_data.append({\n            \"frame_idx\": frame_idx,\n            \"timestamp_ms\": timestamp_ms,\n            \"coach\": coach_lm,\n            \"student\": student_lm,\n        })\n        frame_idx += 1\n\n        if frame_idx % 50 == 0:\n            print(f\"已处理 {frame_idx} 帧...\")\n\n    cap.release()\n    writer.release()\n\n    with open(out, \"w\", encoding=\"utf-8\") as f:\n        json.dump({\n            \"fps\": fps, \"width\": w, \"height\": h, \"split_x\": split_x,\n            \"frames\": frames_data,\n        }, f)\n\n    print(f\"\\n完成，共 {frame_idx} 帧\")\n    print(f\"教练检测成功率: {detected_coach/frame_idx*100:.1f}%\")\n    print(f\"学生检测成功率: {detected_student/frame_idx*100:.1f}%\")\n\n    \nmain()</code></pre>\n\n<p>经过姿态估计后，一帧普通的视频图像就可以转换成人体关键点数据，执行结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c16-f064caa63bbfc5.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>通过逐帧处理学生和教练的视频，可以得到连续的人体关键点序列。这样就把原本难以直接计算的视频内容，转换成了可以进一步分析的结构化数据。</p>\n\n<h2 id=\"c16-s2\">人有高有矮，先把坐标尺度统一</h2>\n\n<p>得到人体关键点以后，还不能直接比较学生和教练的坐标。这是因为两个人的身高、身体比例以及距离摄像头的位置可能不同。即使完成完全相同的动作，关键点在画面中的坐标也可能存在明显差异。</p>\n\n<p>因此，在比较人体姿态之前，可以先对关键点进行归一化处理。本节采用一种比较直接的方法：以左右髋部的中点作为人体的相对原点，再使用肩部中心到髋部中心的距离作为躯干长度，对人体尺度进行统一。归一化过程可以表示为：</p>\n\n<p><span class=\"arithmatex display-math\">\\(\\mathbf{p}'_i=\n\\frac{\\mathbf{p}_i-\\mathbf{p}_{hip}}\n{L_{torso}}\n\\)</span></p>\n\n<p>其中：</p>\n\n<ul><li><span class=\"arithmatex\">\\(\\mathbf{p}_i\\)</span> 表示原始关键点坐标；</li><li><span class=\"arithmatex\">\\(\\mathbf{p}_{hip}\\)</span> 表示左右髋部的中心位置；</li><li><span class=\"arithmatex\">\\(L_{torso}\\)</span> 表示肩部中心到髋部中心的距离；</li><li><span class=\"arithmatex\">\\(\\mathbf{p}'_i\\)</span> 表示归一化后的关键点坐标。</li></ul>\n\n<p>对应代码如下：</p>\n\n<pre data-language=\"Python\"><code>import numpy as np\n\nLM = {\n    \"left_shoulder\": 11,\n    \"right_shoulder\": 12,\n    \"left_hip\": 23,\n    \"right_hip\": 24,\n}\n\ndef normalize_landmarks(landmarks):\n    pts = np.array([[p[0], p[1]] for p in landmarks], dtype=float)\n\n    mid_hip = (\n        pts[LM[\"left_hip\"]] +\n        pts[LM[\"right_hip\"]]\n    ) / 2\n\n    mid_shoulder = (\n        pts[LM[\"left_shoulder\"]] +\n        pts[LM[\"right_shoulder\"]]\n    ) / 2\n\n    torso_len = np.linalg.norm(mid_shoulder - mid_hip)\n\n    if torso_len &lt; 1e-6:\n        torso_len = 1e-6\n\n    normed = (pts - mid_hip) / torso_len\n    return normed</code></pre>\n\n<p>经过归一化以后，教练和学生的人体关键点会被转换到相对统一的坐标尺度中，后续比较更加关注身体姿态本身，而不是人在画面中的位置和大小。</p>\n\n<h2 id=\"c16-s3\">动作有快有慢，要比较同一个阶段</h2>\n\n<p>除了人体大小不同，教练和学生完成动作的速度通常也不会完全一致。例如，教练已经到达深蹲最低点时，学生可能仍然处于下蹲过程中。如果简单地比较两段视频中的相同时间点，就可能把动作节奏不同误认为姿态不同。对于已经同步录制的视频，可以直接按照视频帧进行比较。如果教练和学生来自两段独立视频，则可以使用动态时间规整（Dynamic Time Warping，DTW）进行时间对齐。DTW 的基本思路是根据两段动作序列的变化寻找更加合理的对应关系。在具体实现中，可以先将每一帧的归一化人体关键点展开成一个向量，再计算教练帧和学生帧之间的距离：</p>\n\n<p><span class=\"arithmatex display-math\">\\(d(i,j)=\\left\\|\n\\mathbf{x}^{coach}_i-\n\\mathbf{x}^{student}_j\n\\right\\|_2\n\\)</span></p>\n\n<p>其中：</p>\n\n<p><span class=\"arithmatex\">\\(\\mathbf{x}^{coach}_i\\)</span> 表示教练第 <span class=\"arithmatex\">\\(i\\)</span> 帧的姿态特征；</p>\n\n<p><span class=\"arithmatex\">\\(\\mathbf{x}^{student}_j\\)</span> 表示学生第 <span class=\"arithmatex\">\\(j\\)</span> 帧的姿态特征；</p>\n\n<p><span class=\"arithmatex\">\\(d(i,j)\\)</span> 表示两帧姿态之间的距离。</p>\n\n<p>DTW 再根据这些距离寻找一条累计代价较小的对齐路径，使动作节奏不同的两段视频能够进行对应比较。在本节实验中，教练和学生的视频已经同步录制，因此可以直接按帧进行比较，而不需要额外使用 DTW。</p>\n\n<h2 id=\"c16-s4\">把动作差异算出来，看看关节角度差多少</h2>\n\n<p>完成关键点提取以后，就可以进一步根据人体关键点计算关节角度。例如，通过髋部、膝盖和脚踝三个关键点，可以计算膝关节角度；通过肩部、肘部和手腕三个关键点，可以计算肘关节角度。</p>\n\n<p>设三个关键点分别为 <span class=\"arithmatex\">\\(A\\)</span>、<span class=\"arithmatex\">\\(B\\)</span> 和 <span class=\"arithmatex\">\\(C\\)</span>，其中 <span class=\"arithmatex\">\\(B\\)</span> 为需要计算角度的关节位置。先构造两个向量：<span class=\"arithmatex\">\\(\\mathbf{u}=\\mathbf{A}-\\mathbf{B}\\)</span>，</p>\n\n<p><span class=\"arithmatex\">\\(\\mathbf{v}=\\mathbf{C}-\\mathbf{B}\\)</span>,根据向量点积，可以得到两个向量之间的夹角：</p>\n\n<p><span class=\"arithmatex display-math\">\\(\\theta=\n\\arccos\n\\left(\n\\frac{\n\\mathbf{u}\\cdot\\mathbf{v}\n}{\n\\|\\mathbf{u}\\|\n\\|\\mathbf{v}\\|\n}\n\\right)\n\\)</span></p>\n\n<p>代入三个关键点后，也可以写成：</p>\n\n<p><span class=\"arithmatex display-math\">\\(\\theta=\n\\arccos\n\\left(\n\\frac{\n(\\mathbf{A}-\\mathbf{B})\\cdot(\\mathbf{C}-\\mathbf{B})\n}{\n\\|\\mathbf{A}-\\mathbf{B}\\|\n\\|\\mathbf{C}-\\mathbf{B}\\|\n}\n\\right)\n\\)</span></p>\n\n<p>例如，计算左膝角度时，<span class=\"arithmatex\">\\(A\\)</span> 为左髋，<span class=\"arithmatex\">\\(B\\)</span> 为左膝，<span class=\"arithmatex\">\\(C\\)</span> 为左脚踝，最终得到的就是左膝位置处的夹角。本次实验主要关注左、右肘关节，左、右膝关节，左、右髋关节以及躯干前倾角。躯干前倾角通过肩部中心和髋部中心形成的连线，与竖直向上方向之间的夹角计算得到。</p>\n\n<p>关节角度计算代码如下：</p>\n\n<pre data-language=\"Python\"><code>import numpy as np\n\nANGLE_CN_NAME = {\n    \"left_elbow\": \"左肘\",\n    \"right_elbow\": \"右肘\",\n    \"left_knee\": \"左膝\",\n    \"right_knee\": \"右膝\",\n    \"left_hip\": \"左髋\",\n    \"right_hip\": \"右髋\",\n    \"trunk_tilt\": \"躯干前倾角\",\n}\n\nANGLE_DEFS = {\n    \"left_elbow\":  (\"left_shoulder\", \"left_elbow\", \"left_wrist\"),\n    \"right_elbow\": (\"right_shoulder\", \"right_elbow\", \"right_wrist\"),\n    \"left_knee\":   (\"left_hip\", \"left_knee\", \"left_ankle\"),\n    \"right_knee\":  (\"right_hip\", \"right_knee\", \"right_ankle\"),\n    \"left_hip\":    (\"left_shoulder\", \"left_hip\", \"left_knee\"),\n    \"right_hip\":   (\"right_shoulder\", \"right_hip\", \"right_knee\"),\n}\n\nLM = {\n    \"left_shoulder\": 11,\n    \"right_shoulder\": 12,\n    \"left_elbow\": 13,\n    \"right_elbow\": 14,\n    \"left_wrist\": 15,\n    \"right_wrist\": 16,\n    \"left_hip\": 23,\n    \"right_hip\": 24,\n    \"left_knee\": 25,\n    \"right_knee\": 26,\n    \"left_ankle\": 27,\n    \"right_ankle\": 28,\n}\n\ndef calculate_angle(a, b, c):\n    \"\"\"计算B点处A-B-C三点形成的夹角，单位为度。\"\"\"\n    a = np.array(a, dtype=float)\n    b = np.array(b, dtype=float)\n    c = np.array(c, dtype=float)\n\n    ba = a - b\n    bc = c - b\n\n    cos_angle = np.dot(ba, bc) / (\n        np.linalg.norm(ba) * np.linalg.norm(bc) + 1e-8\n    )\n\n    cos_angle = np.clip(cos_angle, -1.0, 1.0)\n\n    return float(\n        np.degrees(np.arccos(cos_angle))\n    )\n\ndef compute_all_angles(landmarks):\n    \"\"\"计算一帧中需要关注的全部关节角度。\"\"\"\n    pts = [(p[0], p[1]) for p in landmarks]\n\n    angles = {}\n\n    for name, (a_name, b_name, c_name) in ANGLE_DEFS.items():\n        a = pts[LM[a_name]]\n        b = pts[LM[b_name]]\n        c = pts[LM[c_name]]\n\n        angles[name] = calculate_angle(a, b, c)\n\n    # 计算躯干前倾角\n    left_sh = pts[LM[\"left_shoulder\"]]\n    right_sh = pts[LM[\"right_shoulder\"]]\n    left_hip = pts[LM[\"left_hip\"]]\n    right_hip = pts[LM[\"right_hip\"]]\n\n    mid_sh = (\n        (left_sh[0] + right_sh[0]) / 2,\n        (left_sh[1] + right_sh[1]) / 2\n    )\n\n    mid_hip = (\n        (left_hip[0] + right_hip[0]) / 2,\n        (left_hip[1] + right_hip[1]) / 2\n    )\n\n    vertical_ref = (\n        mid_hip[0],\n        mid_hip[1] - 1.0\n    )\n\n    angles[\"trunk_tilt\"] = calculate_angle(\n        vertical_ref,\n        mid_hip,\n        mid_sh\n    )\n\n    return angles</code></pre>\n\n<p>得到学生和教练的关节角度后，可以计算两者之间的角度差异：<span class=\"arithmatex display-math\">\\(\\Delta\\theta=\n\\left|\n\\theta_{student}\n-\n\\theta_{coach}\n\\right|\n\\)</span>，其中，<span class=\"arithmatex\">\\(\\theta_{student}\\)</span> 表示学生的关节角度，<span class=\"arithmatex\">\\(\\theta_{coach}\\)</span> 表示教练的关节角度，<span class=\"arithmatex\">\\(\\Delta\\theta\\)</span> 表示两者之间的角度偏差。</p>\n\n<p>人体关键点检测可能会存在轻微抖动。如果仅根据某一帧判断动作是否存在明显差异，容易受到偶然误差影响。因此，本节实验还会对角度差序列进行平滑处理。以长度为 <span class=\"arithmatex\">\\(w\\)</span> 的滑动窗口为例，可以表示为：</p>\n\n<p><span class=\"arithmatex display-math\">\\(\\overline{\\Delta\\theta}_t=\n\\frac{1}{w}\n\\sum_{k=0}^{w-1}\n\\Delta\\theta_{t-k}\n\\)</span></p>\n\n<p>其中，<span class=\"arithmatex\">\\(\\overline{\\Delta\\theta}_t\\)</span> 表示平滑后的角度差。随后设置偏差阈值：<span class=\"arithmatex display-math\">\\(\\overline{\\Delta\\theta}_t&gt;T\n\\)</span>，<span class=\"arithmatex\">\\(T\\)</span> 为设定的角度阈值。只有当角度差连续多帧超过阈值时，才将这一时间段标记为明显偏差，从而减少单帧抖动带来的误判。</p>\n\n<p>对应的偏差分析代码如下：</p>\n\n<pre data-language=\"Python\"><code>import numpy as np\n\nDEVIATION_THRESHOLD_DEG = 15\nMIN_DEVIATION_FRAMES = 6\nSMOOTH_WINDOW = 5\n\ndef smooth(arr, window):\n    if window &lt;= 1:\n        return arr\n\n    kernel = np.ones(window) / window\n    return np.convolve(arr, kernel, mode=\"same\")\n\ndef find_deviation_segments(diff_arr, fps):\n\n    segments = []\n    in_seg = False\n    start = 0\n    for i, v in enumerate(diff_arr):\n        if v &gt; DEVIATION_THRESHOLD_DEG and not in_seg:\n            in_seg = True\n            start = i\n        elif v &lt;= DEVIATION_THRESHOLD_DEG and in_seg:\n            in_seg = False\n            if i - start &gt;= MIN_DEVIATION_FRAMES:\n                segments.append((start, i - 1))\n    if in_seg and len(diff_arr) - start &gt;= MIN_DEVIATION_FRAMES:\n        segments.append((start, len(diff_arr) - 1))\n    return segments</code></pre>\n\n<p>下面是核心代码主逻辑：</p>\n\n<pre data-language=\"Python\"><code>\ndef main():\n  \n    out_dir=\"/mnt/workspace/AIfitness/analysis\"\n    keypoints=\"/mnt/workspace/AIfitness/keypoints.json\" \n    align=True\n\n    os.makedirs(out_dir, exist_ok=True)\n    os.makedirs(os.path.join(out_dir, \"plots\"), exist_ok=True)\n\n    with open(keypoints, \"r\", encoding=\"utf-8\") as f:\n        data = json.load(f)\n    fps = data[\"fps\"]\n    frames = data[\"frames\"]\n\n    coach_angles, coach_normed, coach_idx = build_angle_series(frames, \"coach\")\n    student_angles, student_normed, student_idx = build_angle_series(frames, \"student\")\n\n    print(f\"教练有效帧数: {len(coach_idx)}, 学生有效帧数: {len(student_idx)}\")\n\n    n = min(len(coach_idx), len(student_idx))\n    print(f\"跳过DTW，按帧号直接对齐 (取前 {n} 个共同有效帧)\")\n    aligned_coach = {name: coach_angles[name][:n] for name in coach_angles}\n    aligned_student = {name: student_angles[name][:n] for name in student_angles}\n        \n\n    report = {\"joints\": []}\n    csv_lines = [\"frame,joint,coach_angle,student_angle,diff\"]\n\n    for name in aligned_coach:\n        coach_arr = np.array(aligned_coach[name])\n        student_arr = np.array(aligned_student[name])\n        n = min(len(coach_arr), len(student_arr))\n        coach_arr, student_arr = coach_arr[:n], student_arr[:n]\n\n        diff_raw = np.abs(coach_arr - student_arr)\n        diff_smooth = smooth(diff_raw, SMOOTH_WINDOW)\n\n        for i in range(n):\n            csv_lines.append(f\"{i},{name},{coach_arr[i]:.2f},{student_arr[i]:.2f},{diff_raw[i]:.2f}\")\n\n        segments = find_deviation_segments(diff_smooth, fps)\n        cn_name = ANGLE_CN_NAME.get(name, name)\n        for (s, e) in segments:\n            report[\"joints\"].append({\n                \"joint\": name,\n                \"joint_cn\": cn_name,\n                \"start_frame\": int(s),\n                \"end_frame\": int(e),\n                \"start_time_sec\": round(s / fps, 2),\n                \"end_time_sec\": round(e / fps, 2),\n                \"avg_diff_deg\": round(float(np.mean(diff_smooth[s:e+1])), 1),\n                \"coach_avg_angle\": round(float(np.mean(coach_arr[s:e+1])), 1),\n                \"student_avg_angle\": round(float(np.mean(student_arr[s:e+1])), 1),\n            })\n\n        # 画对比曲线图\n        plt.figure(figsize=(10, 4))\n        t = np.arange(n) / fps\n        plt.plot(t, coach_arr, label=\"教练\", color=\"tab:blue\", linewidth=1.5)\n        plt.plot(t, student_arr, label=\"学生\", color=\"tab:orange\", linewidth=1.5)\n        for (s, e) in segments:\n            plt.axvspan(s / fps, e / fps, color=\"red\", alpha=0.15)\n        plt.title(f\"{cn_name}角度对比 (红色区域=偏差超过{DEVIATION_THRESHOLD_DEG}度)\")\n        plt.xlabel(\"时间(秒)\")\n        plt.ylabel(\"角度(度)\")\n        plt.legend()\n        plt.tight_layout()\n        plt.savefig(os.path.join(out_dir, \"plots\", f\"{name}.png\"), dpi=120)\n        plt.close()\n\n    with open(os.path.join(out_dir, \"deviation_report.json\"), \"w\", encoding=\"utf-8\") as f:\n        json.dump(report, f, ensure_ascii=False, indent=2)\n\n    with open(os.path.join(out_dir, \"angles.csv\"), \"w\", encoding=\"utf-8\") as f:\n        f.write(\"\\n\".join(csv_lines))\n\n    print(f\"\\n完成，共发现 {len(report['joints'])} 处明显偏差片段\")\n    for item in report[\"joints\"]:\n        print(f\"  [{item['start_time_sec']}s-{item['end_time_sec']}s] {item['joint_cn']}: \"\n              f\"教练{item['coach_avg_angle']}° vs 学生{item['student_avg_angle']}° \"\n              f\"(差{item['avg_diff_deg']}°)\")\n    print(f\"\\n详细报告 -&gt; {out_dir}/deviation_report.json\")\n    print(f\"角度对比图 -&gt; {out_dir}/plots/\")\n\nif __name__ == \"__main__\":\n    main()</code></pre>\n\n<p>执行代码结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c16-4f12abaf6bef5f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>经过这一过程，可以得到每个明显偏差片段对应的关节、开始和结束位置、教练平均角度、学生平均角度以及平均角度差。这些结构化结果会在下一步与视频关键帧一起提供给多模态大模型。</p>\n\n<h2 id=\"c16-s5\">有了角度和画面，让多模态模型解释差异</h2>\n\n<p>经过前面基于姿态估计的量化分析，已经能够得到学生动作在关键点、关节角度层面上与教练示范之间的具体差异。这些差异以数值形式呈现，精确但缺乏语义层面的理解，也难以直接转化为学员能够理解和执行的反馈建议。本节介绍如何在已有量化结果的基础上，引入多模态大模型完成动作语义理解与自然语言反馈生成，视频的信息如下：</p>\n\n<figure><a href=\"assets/manuscript-20260914/c16-b542ef5b332059.mp4\" class=\"attachment\" download=\"0912.mp4\">0912.mp4</a></figure>\n\n<h3 id=\"c16-s6\">先分清做的是什么动作，再逐段分析</h3>\n\n<p>实际拍摄的健身跟练视频往往并非单一动作的持续重复，而是由多个不同动作依次组成，期间还可能穿插准备、休息、动作切换等过渡片段。如果不加区分，直接把整段视频的关键点数据和偏差结果一并交给大模型分析，会带来两方面问题：一是不同动作关注的关键关节和评价标准并不相同（例如深蹲重点关注膝、髋角度，弯举则重点关注肘部角度），混在一起容易造成评价标准相互干扰；二是让模型在同一次推理中同时完成\"识别当前动作\"与\"评价动作质量\"两项任务，会显著增加提示词的复杂度和模型的认知负担，对参数规模较小的模型尤其明显，容易导致输出质量下降。因此，在进行动作评价之前，本实验先对视频进行动作识别与分段，将其划分为若干相对独立、类型单一的动作片段，再针对每个片段分别展开后续分析。</p>\n\n<p>本实验按固定时间间隔（如每隔 2 秒）从视频中抽取画面帧，只取教练侧画面输入多模态大模型判断当前画面属于哪一类动作（如\"哑铃深蹲\"\"哑铃弯举\"\"侧平举\"\"俯身划船\"等），由于视觉分类本身存在一定的误判率，直接使用逐帧分类结果容易产生零散片段（例如连续动作中某一帧被偶然误判成其他类别）。为此，在得到连续帧的分类标签序列后，先做一次滑动窗口多数投票平滑，剔除孤立的误判帧；再将标签相同的连续帧合并为同一个动作片段；最后将时长过短的片段并入相邻片段，得到最终的分段结果。核心实现如下：</p>\n\n<pre data-language=\"Python\"><code>import cv2\nimport json\nimport base64\n\nimport numpy as np\nimport requests\n\nREPORT_PATH = \"/mnt/workspace/AIfitness/analysis/deviation_report.json\"  \n\nKEYPOINTS_PATH = \"/mnt/workspace/AIfitness/keypoints.json\"\nSEGMENTS_OUTPUT_PATH = \"/mnt/workspace/AIfitness/segments.json\"  \nOUTPUT_PATH = \"/mnt/workspace/AIfitness/analysis/feedback.md\"\nVLLM_BASE_URL = \"http://127.0.0.1:8000\"\n\nSAMPLE_INTERVAL_SEC = 2.0        \nMIN_SEGMENT_SEC = 4.0            \nSMOOTH_WINDOW = 3               \nMAX_IMAGES_PER_SEGMENT = 3      \n\nCLASSIFY_PROMPT = (\n    \"这张图片是一个人在做健身动作的画面。请用2-6个字的中文简洁回答这是什么动作，\"\n    \"例如：哑铃深蹲、哑铃弯举、侧平举、俯身划船、开合跳、休息/准备。\"\n    \"只回答动作名称本身，不要任何其他文字、标点或解释。\"\n)\n\nwith open(KEYPOINTS_PATH, \"r\", encoding=\"utf-8\") as f:\n    SPLIT_X = json.load(f)[\"split_x\"]\nprint(f\"split_x = {SPLIT_X}\")\n\nresp = requests.get(f\"{VLLM_BASE_URL}/v1/models\", timeout=30)\nresp.raise_for_status()\nMODEL_NAME = resp.json()[\"data\"][0][\"id\"]\nprint(\"当前 vLLM 模型：\", MODEL_NAME)\n\ndef extract_coach_frame_b64(video_path, frame_idx, split_x, max_width=480):\n    \"\"\"分类只用教练那一侧的画面，教练动作标准、少遮挡，分类更准，还省token\"\"\"\n    cap = cv2.VideoCapture(video_path)\n    cap.set(cv2.CAP_PROP_POS_FRAMES, frame_idx)\n    ret, frame = cap.read()\n    cap.release()\n    if not ret:\n        return None\n    coach = frame[:, :split_x].copy()\n    h, w = coach.shape[:2]\n    if w &gt; max_width:\n        scale = max_width / w\n        coach = cv2.resize(coach, (max_width, int(h * scale)))\n    ok, buf = cv2.imencode(\".jpg\", coach, [cv2.IMWRITE_JPEG_QUALITY, 85])\n    if not ok:\n        return None\n    return base64.b64encode(buf.tobytes()).decode(\"utf-8\")\nVIDEO_PATH = \"/mnt/workspace/AIfitness/0912.mp4\"\n\ndef classify_frame(img_b64):\n    payload = {\n        \"model\": MODEL_NAME,\n        \"messages\": [{\n            \"role\": \"user\",\n            \"content\": [\n                {\"type\": \"text\", \"text\": CLASSIFY_PROMPT},\n                {\"type\": \"image_url\", \"image_url\": {\"url\": \"data:image/jpeg;base64,\" + img_b64}},\n            ],\n        }],\n        \"temperature\": 0.1,\n        \"max_tokens\": 20,\n    }\n    resp = requests.post(f\"{VLLM_BASE_URL}/v1/chat/completions\", json=payload, timeout=60)\n    resp.raise_for_status()\n    return resp.json()[\"choices\"][0][\"message\"][\"content\"].strip().strip(\"。\").strip()\n\ndef merge_labels_to_segments(samples, min_segment_sec, smooth_window):\n    if not samples:\n        return []\n    labels = [l for _, l in samples]\n    times = [t for t, _ in samples]\n    n = len(labels)\n    half = smooth_window // 2\n    smoothed = [max(set(labels[max(0, i-half):min(n, i+half+1)]),\n                     key=labels[max(0, i-half):min(n, i+half+1)].count) for i in range(n)]\n\n    segs = []\n    seg_start, seg_label = times[0], smoothed[0]\n    for i in range(1, n):\n        if smoothed[i] != seg_label:\n            segs.append({\"name\": seg_label, \"start_sec\": seg_start, \"end_sec\": times[i]})\n            seg_start, seg_label = times[i], smoothed[i]\n    last_gap = (times[-1] - times[-2]) if n &gt; 1 else SAMPLE_INTERVAL_SEC\n    segs.append({\"name\": seg_label, \"start_sec\": seg_start, \"end_sec\": times[-1] + last_gap})\n\n    merged = []\n    for seg in segs:\n        if merged and (seg[\"end_sec\"] - seg[\"start_sec\"]) &lt; min_segment_sec:\n            merged[-1][\"end_sec\"] = seg[\"end_sec\"]\n        else:\n            merged.append(seg)\n    return merged\n\ndef detect_segments():\n    cap = cv2.VideoCapture(VIDEO_PATH)\n    fps = cap.get(cv2.CAP_PROP_FPS)\n    duration = cap.get(cv2.CAP_PROP_FRAME_COUNT) / fps\n    cap.release()\n    print(f\"\\n视频时长 {duration:.1f}s，每 {SAMPLE_INTERVAL_SEC}s 抽一帧分类，\"\n          f\"共约 {int(duration/SAMPLE_INTERVAL_SEC)} 次调用...\")\n\n    samples = []\n    t = 0.0\n    while t &lt; duration:\n        img_b64 = extract_coach_frame_b64(VIDEO_PATH, int(t * fps), SPLIT_X)\n        if img_b64 is not None:\n            label = classify_frame(img_b64)\n            samples.append((t, label))\n            print(f\"  {t:6.1f}s -&gt; {label}\")\n        t += SAMPLE_INTERVAL_SEC\n\n    segments = merge_labels_to_segments(samples, MIN_SEGMENT_SEC, SMOOTH_WINDOW)\n    print(f\"\\n识别出 {len(segments)} 个动作片段:\")\n    for seg in segments:\n        print(f\"  [{seg['start_sec']:.1f}s - {seg['end_sec']:.1f}s]  {seg['name']}\")\n\n    with open(SEGMENTS_OUTPUT_PATH, \"w\", encoding=\"utf-8\") as f:\n        json.dump(segments, f, ensure_ascii=False, indent=2)\n    print(f\"分段结果已保存 -&gt; {SEGMENTS_OUTPUT_PATH}\")\n    return segments\nsegments = detect_segments()</code></pre>\n\n<p>首先需要在终端中启动 VLLM 程序，本实验采用 Qwen3-VL-2B-Instruct 模型，启动命令如下：</p>\n\n<pre data-language=\"Plain Text\"><code>vllm serve /mnt/workspace/models/Qwen3-VL-2B-Instruct \\\n--dtype bfloat16 \\\n--max_model_len 16384 \\\n--max-num-batched-tokens 16384\n</code></pre>\n\n<img src=\"assets/manuscript-20260914/c16-e6e0dfd5867dd4.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<img src=\"assets/manuscript-20260914/c16-54a4ec9c354f48.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>启动完成后，执行上面的代码，下面是执行结果：</p>\n\n<img src=\"assets/manuscript-20260914/c16-da18e872515cf0.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c16-s7\">把角度差异和画面对上，生成动作反馈</h3>\n\n<p>完成动作识别与分段后，系统已经知道视频中包含哪些动作，以及每个动作对应的时间范围。接下来需要将姿态分析得到的关节角度偏差与这些动作片段对应起来，并进一步转化为学员能够理解的动作评价和改进建议。</p>\n\n<p>姿态分析得到的结果主要是关节角度、偏差大小以及偏差出现的时间等量化信息。这些数据能够说明学生与教练之间“差了多少”，但对于普通用户来说并不直观。本阶段按照动作分段对偏差数据进行重新组织，将属于同一动作的姿态偏差和关键帧组合在一起，再分别交给多模态大模型进行分析。在这一过程中，多模态大模型并不是直接根据视频重新判断动作是否标准，而是以姿态估计得到的量化结果作为主要依据，再结合关键帧观察动作幅度、身体姿态和稳定性等信息。例如，当系统已经检测到学生与教练在肘关节角度上存在明显差异时，大模型可以结合对应画面进一步解释这种差异在实际动作中的表现，并将角度数据转化为更加直观的训练建议。同时，评价过程按照动作片段分别进行。对于哑铃弯举、侧平举等不同动作，分别输入对应的偏差数据和关键帧，使模型每次只针对一个明确的动作进行分析。相比将整段视频中的多种动作一次性交给模型，这种方式能够减少不同动作之间的信息干扰，使生成的评价更加聚焦，下面是核心逻辑实现：</p>\n\n<pre data-language=\"Python\"><code>\ndef filter_deviations_by_segment(all_joints, seg_start, seg_end):\n    \"\"\"用偏差片段的中点时间判断这条偏差属于哪个动作分段\"\"\"\n    return [d for d in all_joints\n            if seg_start &lt;= (d[\"start_time_sec\"] + d[\"end_time_sec\"]) / 2 &lt; seg_end]\n\ndef extract_keyframe_b64(video_path, frame_idx, split_x, max_width=640):\n    cap = cv2.VideoCapture(video_path)\n    cap.set(cv2.CAP_PROP_POS_FRAMES, frame_idx)\n    ret, frame = cap.read()\n    cap.release()\n    if not ret:\n        return None\n    left = frame[:, :split_x].copy()\n    right = frame[:, split_x:].copy()\n    cv2.putText(left, \"Coach\", (10, 35), cv2.FONT_HERSHEY_SIMPLEX, 1, (0, 0, 255), 2)\n    cv2.putText(right, \"Student\", (10, 35), cv2.FONT_HERSHEY_SIMPLEX, 1, (0, 0, 255), 2)\n    merged = np.concatenate([left, right], axis=1)\n    h, w = merged.shape[:2]\n    if w &gt; max_width:\n        scale = max_width / w\n        merged = cv2.resize(merged, (max_width, int(h * scale)))\n    ok, buf = cv2.imencode(\".jpg\", merged, [cv2.IMWRITE_JPEG_QUALITY, 85])\n    if not ok:\n        return None\n    return base64.b64encode(buf.tobytes()).decode(\"utf-8\")\n\ndef build_prompt(exercise_name, deviations):\n    if not deviations:\n        return (f\"这是一段学生跟随教练完成「{exercise_name}」的训练视频片段。\"\n                f\"系统没有检测到明显的关节角度偏差，请给学生一句简短的鼓励和1个继续保持的建议。\"\n                f\"直接输出点评内容。\")\n    lines = [\n        f\"这是一段学生跟随教练完成「{exercise_name}」的训练视频片段。\",\n        \"\", \"系统已经通过人体姿态估计算法得到以下动作差异：\", \"\",\n    ]\n    for i, d in enumerate(deviations, 1):\n        lines.append(\n            f\"{i}. 时间 {d['start_time_sec']}s - {d['end_time_sec']}s，\"\n            f\"{d['joint_cn']}：教练平均角度 {d['coach_avg_angle']}°，\"\n            f\"学生平均角度 {d['student_avg_angle']}°，平均差值 {d['avg_diff_deg']}°。\"\n        )\n    lines.extend([\n        \"\", \"同时提供了这些偏差时间点对应的视频截图。每张图片左侧是教练，右侧是学生。\", \"\",\n        \"请结合关节角度数据和图片分析学生在这个动作里的表现。\", \"\",\n        \"要求：\",\n        \"1. 先总体评价这个动作完成情况。\",\n        \"2. 再指出比较明显的差异，并给出改进建议。\",\n        \"3. 优先参考已经计算出的关节角度数据，不要仅凭图片重新判断关节角度。\",\n        \"4. 语言自然、简单，像健身教练给学生反馈，不要堆术语。\",\n        \"5. 最后给出1个学生自己检查这个动作的小技巧。\",\n        \"6. 不进行疾病、损伤等医学诊断。\",\n        \"7. 直接输出点评内容，不要重复原始数据格式，不用再复述一遍是什么动作。\",\n    ])\n    return \"\\n\".join(lines)\n\ndef analyze_segment(name, deviations):\n    print(f\"\\n--- 分析动作: {name}，匹配到偏差片段数: {len(deviations)} ---\")\n    top_deviations = sorted(deviations, key=lambda x: -x[\"avg_diff_deg\"])[:MAX_IMAGES_PER_SEGMENT]\n\n    content = [{\"type\": \"text\", \"text\": build_prompt(name, deviations)}]\n    for i, d in enumerate(top_deviations, 1):\n        mid_frame = (d[\"start_frame\"] + d[\"end_frame\"]) // 2\n        img_b64 = extract_keyframe_b64(VIDEO_PATH, mid_frame, SPLIT_X)\n        if img_b64 is None:\n            continue\n        content.append({\"type\": \"text\", \"text\": f\"↑ 第{i}张：{d['joint_cn']}偏差画面\"})\n        content.append({\"type\": \"image_url\", \"image_url\": {\"url\": \"data:image/jpeg;base64,\" + img_b64}})\n\n    payload = {\n        \"model\": MODEL_NAME,\n        \"messages\": [{\"role\": \"user\", \"content\": content}],\n        \"temperature\": 0.3, \"max_tokens\": 800, \"stream\": False,\n    }\n    response = requests.post(f\"{VLLM_BASE_URL}/v1/chat/completions\", json=payload, timeout=300)\n    try:\n        response.raise_for_status()\n    except requests.HTTPError:\n        print(\"vLLM 返回了错误，响应内容如下：\")\n        print(response.text[:2000])\n        raise\n\n    result = response.json()\n    if \"choices\" not in result:\n        print(\"vLLM 返回格式异常：\", json.dumps(result, ensure_ascii=False)[:2000])\n        raise RuntimeError(\"响应里没有 choices 字段\")\n\n    feedback = result[\"choices\"][0][\"message\"][\"content\"]\n    print(feedback[:200] + (\"...\" if len(feedback) &gt; 200 else \"\"))\n    return feedback\n\nwith open(REPORT_PATH, \"r\", encoding=\"utf-8\") as f:\n    flat_report = json.load(f)\nall_joints = flat_report[\"joints\"]\nprint(f\"\\nStep2原始偏差条数(整段视频): {len(all_joints)}\")\n\nmd_parts = [\"# AI健身教练分析报告\\n\"]\nfor seg in segments:\n    matched = filter_deviations_by_segment(all_joints, seg[\"start_sec\"], seg[\"end_sec\"])\n    feedback = analyze_segment(seg[\"name\"], matched)\n    md_parts.append(f\"## {seg['name']} ({seg['start_sec']:.1f}s - {seg['end_sec']:.1f}s)\\n\")\n    md_parts.append(feedback)\n    md_parts.append(\"\\n\")\n\nfull_text = \"\\n\".join(md_parts)\nwith open(OUTPUT_PATH, \"w\", encoding=\"utf-8\") as f:\n    f.write(full_text)\n\nprint(\"\\n\" + \"=\" * 60)\nprint(full_text)\nprint(\"=\" * 60)\nprint(f\"\\n分析结果已保存：{OUTPUT_PATH}\")</code></pre>\n\n<p>代码执行结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c16-073624de36e98a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>我们也可以在生成的文档中看到模型对此次训练更加具体的总结反馈。</p>\n\n<img src=\"assets/manuscript-20260914/c16-f19af53180de2d.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c16-s8\">多记录几次，看看动作有没有变化</h3>\n\n<p>前面的动作识别、姿态分析和多模态评价，主要解决的是一次训练中动作完成得怎么样的问题。对于 AI 健身教练而言，如果进一步结合训练过程和历史数据，还可以从单次动作纠正扩展到更加连续的训练分析与个性化指导。可以将不同训练场次的数据进行保存和关联，形成长期训练记录。例如，持续记录同一动作的关节角度偏差、动作稳定性和完成情况，就可以比较学生经过多次训练后的变化。如果某项动作的偏差逐渐减小，说明动作掌握程度正在提高；如果某类问题长期存在，则可以在后续训练中持续提醒和重点关注。这样，AI 健身教练的分析对象就从一次训练扩展到了一个连续的训练过程。在此基础上，还可以进一步结合用户的训练目标和个人训练信息。例如，将增肌、减脂或体态改善等目标，以及训练频率、训练时长和历史训练情况与动作分析结果结合，大模型就可以生成更加符合个人情况的训练建议。AI 健身教练就不只是对某一个动作进行即时纠正，而是能够逐步形成从动作识别、姿态量化、动作评价到训练记录与个性化指导的完整应用流程。</p>\n\n<p></p>\n\n<p>以上所有实验步骤，可参考：https://modelscope.cn/gallery/liucong/bc955e19-cb4b-4029-bb0e-810eeaa166b4</p>\n\n<p></p>\n\n<p></p>\n\n<p></p>\n\n<p></p>\n\n<p></p>\n\n<p></p>\n\n<p></p>\n\n<p></p>\n","imageCount":9,"attachmentCount":1,"discussionId":"chapter-16"},{"id":"chapter-17","number":17,"title":"智能客服质检：AI 能从一通客服电话中分析出什么？","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-17.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-17.html","minutes":29,"status":"ready","headings":[{"id":"c17-s1","text":"质检一通电话，我们到底要检查什么？","level":"h2"},{"id":"c17-s2","text":"先看客户问了什么，客服回应得怎么样","level":"h3"},{"id":"c17-s3","text":"把服务要求写成具体的评分规则","level":"h3"},{"id":"c17-s4","text":"从电话录音到质检报告，模型怎样分工？","level":"h2"},{"id":"c17-s5","text":"整理通话内容，也听听语气和互动","level":"h3"},{"id":"c17-s6","text":"按规则评分，让每个判断都有依据","level":"h3"},{"id":"c17-s7","text":"动手做一次电话客服质检","level":"h2"},{"id":"c17-s8","text":"准备运行环境","level":"h3"},{"id":"c17-s9","text":"音频预处理","level":"h3"},{"id":"c17-s10","text":"说话人分析","level":"h3"},{"id":"c17-s11","text":"语音识别","level":"h3"},{"id":"c17-s12","text":"说话人角色识别","level":"h3"},{"id":"c17-s13","text":"语音综合分析","level":"h3"},{"id":"c17-s14","text":"分析结果汇总","level":"h3"},{"id":"c17-s15","text":"大模型综合质检","level":"h3"},{"id":"c17-s16","text":"完整质检流程","level":"h3"},{"id":"c17-s17","text":"运行测试","level":"h3"}],"html":"<p>一通客服电话结束了，用户的问题有没有解决，客服有没有漏掉重要条件，录音少的时候，可以逐条听；录音多了，人工抽听就很难顾及每一通电话。</p>\n\n<p></p>\n\n<p>传统客服质检主要依靠人工抽听，不仅需要投入较多时间，也很难覆盖全部通话。利用语音识别和音频多模态模型，可以从电话录音中分析通话内容、说话人、情绪、服务语气和双方互动，再由大语言模型按照质检规则完成评分并生成质检报告。</p>\n\n<p></p>\n\n<p>这一章就用语言培训机构的一段客服通话记录，把从录音到质检报告的过程跑一遍。</p>\n\n<p></p>\n\n<h2 id=\"c17-s1\">质检一通电话，我们到底要检查什么？</h2>\n\n<p>不同业务场景对客服服务质量的要求有所不同。在进行自动质检之前，需要先明确质检需要关注哪些问题，并根据实际业务要求制定相应的质检规则。</p>\n\n<p></p>\n\n<h3 id=\"c17-s2\">先看客户问了什么，客服回应得怎么样</h3>\n\n<p>语言培训机构的客服通常需要通过电话处理课程咨询、开课通知、请假、课程调整、学习反馈和售后等业务。</p>\n\n<p>质检时，需要检查客服是否正确理解客户需求，回答是否完整、准确，对于无法立即确定的问题是否给出了后续处理方式，以及是否存在不当承诺。同时，电话中的服务态度和沟通过程也会影响服务质量，例如客服是否存在不耐烦、敷衍或责怪，是否打断客户，客户是否出现负面情绪等。</p>\n\n<p>这些信息并不能只通过电话转写文本获得，还需要结合说话人以及电话录音中的情绪、语气和双方互动等信息进行分析。</p>\n\n<p></p>\n\n<h3 id=\"c17-s3\">把服务要求写成具体的评分规则</h3>\n\n<p>确场景需求后，还需要将这些要求进一步整理为具体的质检规则，明确一通电话需要检查哪些内容，以及不同问题对应的评分标准，为后续自动判断和评分提供依据。</p>\n\n<p>结合语言培训机构电话客服的服务要求，从需求回应、信息准确性、问题解决、服务态度、通话行为和客户情绪等方面制定质检规则，如下表所示。</p>\n\n<div class=\"table-scroll\"><table><tr><td>质检项</td><td>主要检查内容</td><td>分值</td></tr><tr><td>开场与身份说明</td><td>是否主动问候，说明机构和本人身份，并询问客户需求</td><td>5</td></tr><tr><td>需求识别与回应完整性</td><td>是否识别客户提出的问题，并逐项进行有效回应</td><td>20</td></tr><tr><td>信息准确性与风险控制</td><td>是否编造不确定的信息，是否存在学习效果等不当承诺</td><td>15</td></tr><tr><td>问题解决与承诺闭环</td><td>客户核心问题是否解决，无法立即解决时是否说明后续处理方式</td><td>15</td></tr><tr><td>服务态度与专业措辞</td><td>是否存在不耐烦、责怪、敷衍、辱骂或推卸责任等表达</td><td>15</td></tr><tr><td>打断与通话节奏</td><td>是否频繁打断客户、抢话或存在明显影响沟通的异常节奏</td><td>10</td></tr><tr><td>客户情绪与服务修复</td><td>客户是否出现明显负面情绪，客服是否及时解释和安抚</td><td>10</td></tr><tr><td>结束语与待办确认</td><td>是否礼貌结束，并对尚未完成的事项进行确认</td><td>10</td></tr><tr><td>合计</td><td></td><td>100</td></tr></table>\n</div>\n<p>后续自动质检将按照这些规则逐项进行判断和评分。不同质检项所需的信息并不相同，例如需求识别、信息准确性和问题解决情况主要根据通话内容判断，服务态度、客户情绪以及打断等情况需要结合电话录音进行分析，最后再根据这些分析结果完成综合质检。</p>\n\n<p></p>\n\n<h2 id=\"c17-s4\">从电话录音到质检报告，模型怎样分工？</h2>\n\n<p>根据前面的场景需求，系统先从电话录音中获得通话文本和说话人信息，再结合录音分析客户情绪、客服服务语气和双方的互动情况，最后将这些分析结果与客服质检规则一起交给大语言模型进行综合判断、评分并生成质检报告。整个自动质检过程如下图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c17-d9cd2c1009983c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>整个过程可以分为电话数据分析和综合质检两个部分。</p>\n\n<p></p>\n\n<h3 id=\"c17-s5\">整理通话内容，也听听语气和互动</h3>\n\n<p>电话录音输入系统后，需要先统一音频格式，将采样率和声道转换为后续模型能够处理的形式。</p>\n\n<p>处理后的音频使用 FSMN-VAD 检测有效语音区域，并结合 CAM++区分不同说话人，得到每个说话人的发言时间。此时得到的是<code>spk0</code>、<code>spk1</code>等说话人编号，还不能直接确定其身份。</p>\n\n<p>语音识别使用 Qwen3-ASR-0.6B 完成。模型将录音转换为文本，并通过时间戳记录每段文本对应的时间。将语音识别结果与说话人分析结果按照时间进行对齐，可以得到带有说话人编号和时间信息的通话文本。随后，根据不同说话人的对话内容，使用参数量较小的 Qwen3-4B 判断其在本次电话中的角色，并将说话人编号转换为“客服”和“客户”。最终可以得到带有角色和时间信息的完整通话文本，例如：</p>\n\n<pre data-language=\"Plain Text\"><code>第1轮 [客服] 0.00-4.82秒：……\n第2轮 [客户] 5.10-10.36秒：……</code></pre>\n\n<p>电话中的声音还包含文本无法直接反映的信息，例如客服的情绪和服务语气、客户的情绪变化以及双方的互动情况。使用 Qwen2.5-Omni-3B 对电话录音进行分析，并结合通话文本判断客服是否存在不耐烦、敷衍、责怪或语气生硬等情况，客户是否出现明显负面情绪以及客服是否进行了适当回应，同时分析通话过程中是否存在明显抢话或打断客户等问题。</p>\n\n<p>主要分析模块如下表所示。</p>\n\n<div class=\"table-scroll\"><table><tr><td>分析任务</td><td>模型或组件</td><td>主要输出</td></tr><tr><td>语音识别</td><td>Qwen3-ASR-0.6B</td><td>带时间信息的识别文本</td></tr><tr><td>说话人分析</td><td>FSMN-VAD、CAM++</td><td>不同说话人及说话时间</td></tr><tr><td>说话人角色识别</td><td>Qwen3-4B</td><td>客服、客户角色</td></tr><tr><td>语音综合分析</td><td>Qwen2.5-Omni-3B</td><td>客服情绪与服务语气、客户情绪与客服应对、通话互动</td></tr></table>\n</div>\n<p>这些分析结果需要整理为统一格式，便于后续大语言模型直接读取。例如：</p>\n\n<pre data-language=\"Plain Text\"><code>【通话转写】\n第1轮 [客服] XX.XX-XX.XX秒：……\n第2轮 [客户] XX.XX-XX.XX秒：……\n\n【信息核验说明】\n本次分析未提供企业业务规则或标准答案。\n对于课程时间、请假规则等业务信息，只能判断客服是否进行了明确回应，无法核验其内容是否准确。\n\n【语音综合分析】\n\n客服情绪与服务语气：\n情绪表现：……\n服务语气：……\n异常情况：……\n\n客户情绪与客服应对：\n客户情绪：……\n情绪变化：……\n客服应对：……\n\n通话互动：\n抢话：……\n打断客户：……\n其他异常：……\n\n分析说明：……</code></pre>\n\n<p>规范化结果中保留完整通话文本、说话人角色以及语音综合分析结果。客户需求、回答完整性、问题解决、承诺和合规等业务内容不再提前单独分析，而是在后面的综合质检中由大语言模型结合完整通话内容统一判断。</p>\n\n<p></p>\n\n<h3 id=\"c17-s6\">按规则评分，让每个判断都有依据</h3>\n\n<p>完成电话数据分析后，将 20.1.2 节制定的客服质检规则和规范化分析结果一起输入 Qwen3-8B。模型根据完整通话文本判断客户提出了哪些需求、客服如何回应以及问题是否得到处理，并结合客服情绪与服务语气、客户情绪与客服应对、通话互动等信息，对各项质检规则进行判断和评分。</p>\n\n<p>对于课程时间、请假规则等需要企业业务规则才能验证的信息，如果没有提供相应的业务标准，只判断客服是否进行了明确回应，不直接判断其内容是否正确，并在报告中注明“现有信息无法核验”。</p>\n\n<p>质检时按照 20.1.2 节的规则逐项评分。每个质检项除了给出得分，还需要说明判断结果和主要依据。发现问题时，可以引用对应的通话轮次、客服原话或语音综合分析结果作为证据，避免只给出“服务态度较差”“问题处理不到位”等缺少依据的结论。</p>\n\n<p>最终生成的客服质检报告可以采用如下格式：</p>\n\n<pre data-language=\"Plain Text\"><code>客服质检报告\n\n综合得分：XX/100\n质检结论：……\n\n一、总体评价\n……\n\n二、质检结果\n\n1. 开场与身份说明：X/5\n判断结果：……\n主要依据：……\n扣分原因：……\n\n2. 需求识别与回应完整性：X/20\n判断结果：……\n主要依据：……\n扣分原因：……\n\n3. 信息准确性与风险控制：X/15\n判断结果：……\n主要依据：……\n扣分原因：……\n\n4. 问题解决与承诺闭环：X/15\n判断结果：……\n主要依据：……\n扣分原因：……\n\n5. 服务态度与专业措辞：X/15\n判断结果：……\n主要依据：……\n扣分原因：……\n\n6. 打断与通话节奏：X/10\n判断结果：……\n主要依据：……\n扣分原因：……\n\n7. 客户情绪与服务修复：X/10\n判断结果：……\n主要依据：……\n扣分原因：……\n\n8. 结束语与待办确认：X/10\n判断结果：……\n主要依据：……\n扣分原因：……\n\n三、主要问题\n1. ……\n2. ……\n\n四、改进建议\n1. ……\n2. ……</code></pre>\n\n<h2 id=\"c17-s7\">动手做一次电话客服质检</h2>\n\n<p>下面按照前面的技术方案编写程序，逐步实现电话客服自动质检。</p>\n\n<h3 id=\"c17-s8\">准备运行环境</h3>\n\n<p>安装需要使用的依赖库。</p>\n\n<pre data-language=\"Plain Text\"><code>!pip3 install modelscope transformers accelerate\n!pip3 install funasr qwen-asr qwen-omni-utils\n!pip3 install soundfile scipy</code></pre>\n\n<img src=\"assets/manuscript-20260914/c17-2054efb58564c9.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>提前下载需要使用的模型，使后续加载时可以直接使用本地缓存。</p>\n\n<pre data-language=\"Python\"><code>from modelscope import snapshot_download\n\nsnapshot_download(\"iic/speech_fsmn_vad_zh-cn-16k-common-pytorch\")\nsnapshot_download(\"iic/speech_campplus_sv_zh-cn_16k-common\")\nsnapshot_download(\"Qwen/Qwen3-ASR-0.6B\")\nsnapshot_download(\"Qwen/Qwen3-ForcedAligner-0.6B\")\nsnapshot_download(\"Qwen/Qwen3-4B\")\nsnapshot_download(\"Qwen/Qwen3-8B\")\nsnapshot_download(\"Qwen/Qwen2.5-Omni-3B\")</code></pre>\n\n<img src=\"assets/manuscript-20260914/c17-a123d84d436711.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>安装完成后，统一导入后续程序需要使用的依赖。</p>\n\n<pre data-language=\"Python\"><code>import os\nimport gc\nimport json\nimport torch\nimport soundfile as sf\nfrom scipy.signal import resample_poly\nfrom funasr import AutoModel\nfrom qwen_asr import Qwen3ASRModel\nfrom modelscope import AutoTokenizer, AutoModelForCausalLM, snapshot_download\nfrom transformers import Qwen2_5OmniForConditionalGeneration, Qwen2_5OmniProcessor\nfrom qwen_omni_utils import process_mm_info</code></pre>\n\n<p>测试音频文件：</p>\n\n<figure><a href=\"assets/manuscript-20260914/c17-11cc32fc941475.wav\" class=\"attachment\" download=\"客服通话记录.wav\">客服通话记录.wav</a></figure>\n\n<h3 id=\"c17-s9\">音频预处理</h3>\n\n<p>将输入录音统一转换为 16 kHz 单声道 WAV 音频。</p>\n\n<pre data-language=\"Python\"><code>def preprocess_audio(audio_path, output_path=\"./customer_service/preprocessed.wav\",\n                     target_sr=16000):\n    audio, sr = sf.read(audio_path)\n\n    # 转单声道\n    if audio.ndim &gt; 1:\n        audio = audio.mean(axis=1)\n\n    # 重采样\n    if sr != target_sr:\n        audio = resample_poly(audio, target_sr, sr)\n        sr = target_sr\n\n    # 保存为统一的wav格式\n    os.makedirs(os.path.dirname(output_path), exist_ok=True)\n    sf.write(output_path, audio, sr)\n\n    return output_path</code></pre>\n\n<h3 id=\"c17-s10\">说话人分析</h3>\n\n<p>使用 FunASR 组合 FSMN-VAD 和 CAM++获得不同说话人的语音片段及时间信息。</p>\n\n<pre data-language=\"Python\"><code>def analyze_speakers(audio_path):\n    model = AutoModel(\n        model=\"paraformer-zh\",\n        vad_model=\"fsmn-vad\",\n        punc_model=\"ct-punc\",\n        spk_model=\"cam++\",\n        device=\"cuda:0\"\n    )\n\n    result = model.generate(\n        input=audio_path,\n        batch_size_s=60,\n        sentence_timestamp=True\n    )\n\n    sentence_info = result[0].get(\"sentence_info\", [])\n    segments = []\n\n    for item in sentence_info:\n        segments.append({\n            \"speaker\": f\"spk{item['spk']}\",\n            \"start\": round(item[\"start\"] / 1000, 2),\n            \"end\": round(item[\"end\"] / 1000, 2)\n        })\n\n    del model\n    gc.collect()\n    torch.cuda.empty_cache()\n\n    return segments</code></pre>\n\n<h3 id=\"c17-s11\">语音识别</h3>\n\n<p>使用 Qwen3-ASR-0.6B 识别整段录音，并通过 ForcedAligner 获得识别文本对应的时间戳。</p>\n\n<pre data-language=\"Python\"><code>def analyze_asr(audio_path, language=\"Chinese\"):\n    model_path = \"Qwen/Qwen3-ASR-0.6B\"\n    aligner_path = \"Qwen/Qwen3-ForcedAligner-0.6B\"\n    model = Qwen3ASRModel.from_pretrained(\n        model_path, dtype=torch.bfloat16, device_map=\"cuda:0\",\n        max_inference_batch_size=1, max_new_tokens=2048,\n        forced_aligner=aligner_path,\n        forced_aligner_kwargs={\"dtype\": torch.bfloat16, \"device_map\": \"cuda:0\"}\n    )\n\n    result = model.transcribe(\n        audio=audio_path, language=language, return_time_stamps=True\n    )[0]\n\n    segments = []\n    if result.time_stamps:\n        for item in result.time_stamps:\n            segments.append({\n                \"start\": round(item.start_time, 2), \"end\": round(item.end_time, 2),\n                \"text\": item.text.strip()\n            })\n\n    output = {\n        \"language\": result.language,\n        \"text\": result.text.strip(),\n        \"segments\": segments\n    }\n\n    del model\n    gc.collect()\n    torch.cuda.empty_cache()\n    return output</code></pre>\n\n<h3 id=\"c17-s12\">说话人角色识别</h3>\n\n<p>使用 Qwen3-4B 根据不同说话人的对话内容判断其角色，并将说话人编号转换为“客服”和“客户”。</p>\n\n<pre data-language=\"Python\"><code>def identify_speaker_roles(segments):\n    speaker_texts = {}\n    for seg in segments:\n        speaker_texts.setdefault(seg[\"speaker\"], []).append(seg[\"text\"])\n\n    speaker_text = \"\\n\".join(\n        f\"{speaker}：{''.join(texts)[:1000]}\"\n        for speaker, texts in speaker_texts.items()\n    )\n\n    model_name = \"Qwen/Qwen3-4B\"\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n    model = AutoModelForCausalLM.from_pretrained(\n        model_name, torch_dtype=torch.bfloat16, device_map=\"cuda:0\"\n    )\n\n    prompt = f\"\"\"下面是一段语言培训机构客服电话中不同说话人的对话内容。\n请根据对话内容判断每个说话人的角色，将机构客服标记为“客服”，接受服务的一方标记为“客户”。\n\n【对话内容】\n{speaker_text}\n\n只返回JSON，例如：\n{{\"spk0\": \"客户\", \"spk1\": \"客服\"}}\n不要输出其他内容。\"\"\"\n\n    messages = [{\"role\": \"user\", \"content\": prompt}]\n    text = tokenizer.apply_chat_template(\n        messages, tokenize=False, add_generation_prompt=True, enable_thinking=False\n    )\n    inputs = tokenizer(text, return_tensors=\"pt\").to(model.device)\n    outputs = model.generate(**inputs, max_new_tokens=128)\n    outputs = outputs[:, inputs.input_ids.shape[1]:]\n    role_map = json.loads(tokenizer.decode(outputs[0], skip_special_tokens=True))\n\n    for seg in segments:\n        seg[\"speaker\"] = role_map.get(seg[\"speaker\"], seg[\"speaker\"])\n\n    del model, tokenizer\n    gc.collect()\n    torch.cuda.empty_cache()\n    return segments</code></pre>\n\n<h3 id=\"c17-s13\">语音综合分析</h3>\n\n<p>前面已经获得了带有角色和时间信息的通话文本，接下来使用 Qwen2.5-Omni-3B 结合电话录音，进一步分析客服情绪与服务语气、客户情绪与客服应对以及双方互动情况。</p>\n\n<p>对于较长的电话录音，直接将完整音频一次输入模型会占用较多显存。根据前面得到的通话轮次对音频进行切分，不按照固定时间直接截断，而是在片段达到指定长度后，等待客服当前一轮发言结束再进行切分，使每个片段尽量保持完整的会话内容。Qwen2.5-Omni-3B 只加载一次，依次分析各个音频片段，最后将结果按照对应的时间范围进行整理。</p>\n\n<p>实现代码如下：</p>\n\n<pre data-language=\"Python\"><code>def split_audio_by_conversation(\n    audio_path,\n    segments,\n    max_seconds=30,\n    output_dir=\"./customer_service/audio_segments\"\n):\n    audio, sr = sf.read(audio_path)\n    os.makedirs(output_dir, exist_ok=True)\n\n    groups = []\n    current = []\n    start_time = segments[0][\"start\"]\n\n    # 按完整会话切分，达到指定长度后等待客服当前一轮发言结束\n    for seg in segments:\n        current.append(seg)\n\n        if seg[\"end\"] - start_time &gt;= max_seconds and seg[\"speaker\"] == \"客服\":\n            groups.append(current)\n            current = []\n            start_time = seg[\"end\"]\n\n    if current:\n        groups.append(current)\n\n    results = []\n\n    # 根据每组会话的时间范围截取音频\n    for i, group in enumerate(groups):\n        start = group[0][\"start\"]\n        end = group[-1][\"end\"]\n\n        segment_audio = audio[int(start * sr):int(end * sr)]\n        output_path = os.path.join(output_dir, f\"segment_{i + 1}.wav\")\n        sf.write(output_path, segment_audio, sr)\n\n        results.append({\n            \"path\": output_path,\n            \"start\": start,\n            \"end\": end,\n            \"segments\": group\n        })\n\n    return results\n\ndef build_segment_transcript(segments):\n    # 生成当前音频片段对应的通话文本\n    return \"\\n\".join(\n        f\"第{seg['turn_id']}轮 [{seg['speaker']}] \"\n        f\"{seg['start']:.2f}-{seg['end']:.2f}秒：{seg['text']}\"\n        for seg in segments\n    )\n\ndef format_audio_analysis(results):\n    # 按时间范围整理各片段的语音分析结果\n    return \"\\n\\n\".join(\n        f\"{item['start']:.2f}-{item['end']:.2f}秒语音综合分析结果：\\n\"\n        f\"{item['analysis']}\"\n        for item in results\n    )\n\ndef analyze_audio(audio_path, segments, max_seconds=30):\n    # 按完整会话切分长音频\n    audio_segments = split_audio_by_conversation(\n        audio_path,\n        segments,\n        max_seconds=max_seconds\n    )\n\n    # 模型只加载一次，依次分析所有音频片段\n    model_path = snapshot_download(\"Qwen/Qwen2.5-Omni-3B\")\n    processor = Qwen2_5OmniProcessor.from_pretrained(model_path)\n    model = Qwen2_5OmniForConditionalGeneration.from_pretrained(\n    model_path,\n    torch_dtype=torch.bfloat16,\n    device_map=\"cuda:0\",\n    enable_audio_output=False\n)\n\n    analysis_results = []\n\n    for i, item in enumerate(audio_segments):\n        print(\n            f\"语音综合分析 {i + 1}/{len(audio_segments)}：\"\n            f\"{item['start']:.2f}-{item['end']:.2f}秒\"\n        )\n\n        transcript = build_segment_transcript(item[\"segments\"])\n\n        prompt = f\"\"\"\n这是一段语言培训机构的客服电话录音。请结合录音和对应的通话文本，对这段通话中的语音表现进行分析。\n\n【通话文本】\n{transcript}\n\n请重点分析以下内容：\n\n1. 客服情绪与服务语气\n分析客服整体情绪和服务语气，判断是否存在明显不耐烦、敷衍、责怪客户、语气生硬等情况。\n\n2. 客户情绪与客服应对\n分析客户的主要情绪及变化。如果客户出现明显不满、焦虑、生气等负面情绪，判断客服是否进行了适当解释、回应或安抚。\n\n3. 通话互动\n结合实际语音中的说话先后关系和双方同时说话情况，判断客服是否存在明显抢话或打断客户，以及是否存在其他明显影响沟通的问题。\n\n只根据实际录音和提供的通话文本进行分析，不要猜测没有依据的问题。\n\n请按照下面的格式输出：\n\n客服情绪与服务语气：\n情绪表现：……\n服务语气：……\n异常情况：……\n\n客户情绪与客服应对：\n客户情绪：……\n情绪变化：……\n客服应对：……\n\n通话互动：\n抢话：……\n打断客户：……\n其他异常：……\n\n分析说明：……\n\"\"\"\n\n        conversation = [{\n            \"role\": \"user\",\n            \"content\": [\n                {\"type\": \"audio\", \"audio\": item[\"path\"]},\n                {\"type\": \"text\", \"text\": prompt}\n            ]\n        }]\n\n        text = processor.apply_chat_template(\n            conversation,\n            add_generation_prompt=True,\n            tokenize=False\n        )\n\n        audios, images, videos = process_mm_info(\n            conversation,\n            use_audio_in_video=False\n        )\n\n        inputs = processor(\n            text=text,\n            audio=audios,\n            images=images,\n            videos=videos,\n            return_tensors=\"pt\",\n            padding=True\n        ).to(model.device)\n        \n        with torch.no_grad():\n            output = model.generate(\n                **inputs,\n                use_audio_in_video=False,\n                return_audio=False,\n                max_new_tokens=500\n            )\n\n        output = output[:, inputs.input_ids.shape[1]:]\n\n        analysis = processor.batch_decode(\n            output,\n            skip_special_tokens=True,\n            clean_up_tokenization_spaces=False\n        )[0]\n\n        analysis_results.append({\n            \"start\": item[\"start\"],\n            \"end\": item[\"end\"],\n            \"analysis\": analysis\n        })\n\n        # 每段推理后释放中间变量，模型继续保留\n        del inputs, output, audios, images, videos\n        gc.collect()\n        torch.cuda.empty_cache()\n\n    # 所有片段处理完成后释放模型\n    del model, processor\n    gc.collect()\n    torch.cuda.empty_cache()\n\n    return format_audio_analysis(analysis_results)</code></pre>\n\n<h3 id=\"c17-s14\">分析结果汇总</h3>\n\n<p>将各个模块的分析结果整理为统一的文本格式。</p>\n\n<pre data-language=\"Python\"><code>def format_analysis_result(transcript, audio_analysis):\n    result = f\"\"\"\n【通话转写】\n{transcript}\n\n【信息核验说明】\n本次分析未提供企业业务规则或标准答案。\n对于课程时间、请假规则等业务信息，只能判断客服是否进行了明确回应，\n无法核验其内容是否准确。\n\n【语音综合分析】\n{audio_analysis}\n\"\"\"\n    return result.strip()</code></pre>\n\n<h3 id=\"c17-s15\">大模型综合质检</h3>\n\n<p>将规范化分析结果与 20.1.2 节制定的客服质检规则一起输入 Qwen3-8B，由模型综合分析通话内容和各项分析结果，完成质检评分并生成报告。</p>\n\n<pre data-language=\"Markdown\"><code>def generate_qc_report(analysis_text, qc_rules):\n    model_name = \"Qwen/Qwen3-8B\"\n\n    tokenizer = AutoTokenizer.from_pretrained(model_name)\n    model = AutoModelForCausalLM.from_pretrained(\n        model_name,\n        torch_dtype=torch.bfloat16,\n        device_map=\"cuda:0\"\n    )\n\n    prompt = f\"\"\"\n你是一名语言培训机构电话客服质检人员。请严格按照客服质检规则，根据电话分析结果完成综合质检并生成质检报告。\n\n【客服质检规则】\n{qc_rules}\n\n【电话分析结果】\n{analysis_text}\n\n【质检要求】\n1. 按照8个质检项逐项检查，只根据提供的电话分析结果进行判断，不得猜测或补充未提供的信息。每项判断和扣分都需要有明确依据。\n2. 采用从严评分原则。没有明确问题时不扣分；发现明确问题后，应根据问题严重程度充分扣分。明显违反质检规则、影响客户体验或带来业务风险的问题，应进行较大幅度扣分，不能因为客服整体表现尚可而从轻处理，也不能只扣1～2分作为象征性处罚。\n3. 对课程时间、请假规则等需要企业业务规则才能验证的信息，如果没有提供相应依据，应标记为“现有信息无法核验”，不要自行判断正确或错误。对于学习效果等无法保证的事项，如果客服使用“保证”“一定”“肯定”等表述作出确定性承诺，应判定为不当承诺并明显扣分。\n4. 服务态度、专业措辞和问题解决情况应同时结合通话文本进行判断；客服情绪、客户情绪以及抢话、打断等语音表现结合语音综合分析结果进行判断。语音情绪正常不能覆盖文本中明确存在的不当话术。\n5. 各质检项按照其对应的检查内容进行判断，不要增加质检规则中没有规定的要求，也不要将问题归入无关的质检项。同一个问题原则上不要重复扣分；如果同一行为确实影响不同质检项，应分别说明对应依据。\n6. 完成全部质检项后，检查分项得分、综合得分和质检结论是否一致。综合得分必须等于8个质检项得分之和。\n\n【质检结论】\n根据综合得分确定质检结论：\n- 95～100分：优秀\n- 90～94分：良好\n- 80～89分：一般\n- 80分以下：较差\n\n【输出格式】\n请使用标准Markdown格式输出，不要转义Markdown符号。\n\n# 客服质检报告\n\n## 一、综合结果\n- 综合得分：XX/100\n- 质检结论：优秀/良好/一般/较差\n- 总体评价：简要概括本次客服服务质量，不超过3句话。\n\n## 二、质检项结果\n\n### 1. 开场与身份说明（X/5）\n- 判断结果：\n- 主要依据：\n- 扣分原因：无/具体原因\n\n### 2. 需求识别与回应完整性（X/20）\n- 判断结果：\n- 主要依据：\n- 扣分原因：无/具体原因\n\n### 3. 信息准确性与风险控制（X/15）\n- 判断结果：\n- 主要依据：\n- 扣分原因：无/具体原因\n\n### 4. 问题解决与承诺闭环（X/15）\n- 判断结果：\n- 主要依据：\n- 扣分原因：无/具体原因\n\n### 5. 服务态度与专业措辞（X/15）\n- 判断结果：\n- 主要依据：\n- 扣分原因：无/具体原因\n\n### 6. 打断与通话节奏（X/10）\n- 判断结果：\n- 主要依据：\n- 扣分原因：无/具体原因\n\n### 7. 客户情绪与服务修复（X/10）\n- 判断结果：\n- 主要依据：\n- 扣分原因：无/具体原因\n\n### 8. 结束语与待办确认（X/10）\n- 判断结果：\n- 主要依据：\n- 扣分原因：无/具体原因\n\n## 三、主要问题\n只列出本次通话中有明确证据的主要问题。如果没有明显问题，写“未发现明显问题”。\n\n## 四、改进建议\n针对主要问题给出具体、简洁的改进建议。如果没有明显问题，可以给出简短的保持建议。\n\"\"\"\n\n    messages = [{\"role\": \"user\", \"content\": prompt}]\n\n    text = tokenizer.apply_chat_template(\n        messages,\n        tokenize=False,\n        add_generation_prompt=True,\n        enable_thinking=False\n    )\n\n    inputs = tokenizer(\n        text,\n        return_tensors=\"pt\"\n    ).to(model.device)\n\n    with torch.no_grad():\n        outputs = model.generate(\n            **inputs,\n            max_new_tokens=4096\n        )\n\n    outputs = outputs[:, inputs.input_ids.shape[1]:]\n\n    report = tokenizer.decode(\n        outputs[0],\n        skip_special_tokens=True\n    )\n\n    del model, tokenizer\n    gc.collect()\n    torch.cuda.empty_cache()\n\n    return report</code></pre>\n\n<h3 id=\"c17-s16\">完整质检流程</h3>\n\n<p>主程序还需要定义几个辅助函数，用于根据时间将 ASR 文本与说话人片段对齐、生成完整通话文本以及保存质检报告。</p>\n\n<pre data-language=\"Python\"><code>def merge_asr_speakers(asr_segments, speaker_segments):\n    results = [{\"turn_id\": i + 1, \"speaker\": seg[\"speaker\"], \"start\": seg[\"start\"], \"end\": seg[\"end\"], \"texts\": []} for i, seg in enumerate(speaker_segments)]\n\n    for item in asr_segments:\n        best_index, best_overlap = None, 0\n        for i, seg in enumerate(speaker_segments):\n            overlap = min(seg[\"end\"], item[\"end\"]) - max(seg[\"start\"], item[\"start\"])\n            if overlap &gt; best_overlap:\n                best_overlap, best_index = overlap, i\n        if best_index is not None and best_overlap &gt; 0:\n            results[best_index][\"texts\"].append(item[\"text\"])\n\n    results = [item for item in results if item[\"texts\"]]\n\n    for i, item in enumerate(results):\n        item[\"turn_id\"] = i + 1\n        item[\"text\"] = \"\".join(item.pop(\"texts\"))\n\n    return results\n\ndef build_transcript(segments):\n    return \"\\n\".join(\n        f\"第{seg['turn_id']}轮 [{seg['speaker']}] {seg['start']:.2f}-{seg['end']:.2f}秒：{seg['text']}\"\n        for seg in segments\n    )\n\ndef save_report(report, output_path=\"./customer_service/客服质检报告.md\"):\n    os.makedirs(os.path.dirname(output_path), exist_ok=True)\n    with open(output_path, \"w\", encoding=\"utf-8\") as f:\n        f.write(report)\n    return output_path\n\nQC_RULES = \"\"\"\n电话客服质检规则，总分100分：\n\n1. 开场与身份说明（5分）\n检查是否主动问候，说明机构和本人身份，并询问客户需求。\n\n2. 需求识别与回应完整性（20分）\n检查是否识别客户提出的问题，并逐项进行有效回应。\n\n3. 信息准确性与风险控制（15分）\n检查客服是否编造不确定的信息，是否对课程效果、学习成果等作出不当承诺。\n需要特别注意学习效果相关的保证性表述。学习效果受到学员基础、学习投入、练习情况等多种因素影响，客服不得对学习效果作出确定性保证。\n如果客服使用“保证”“一定可以”“肯定能”等确定性表述，或者明确承诺在固定时间内达到某种学习效果，应判定为不当承诺并扣分。\n\n例如，“坚持一个月基本就能流利说英语”“这个效果可以保证”等表述，属于学习效果不当承诺。\n4. 问题解决与承诺闭环（15分）\n检查客户核心问题是否解决；无法立即解决时，是否说明后续处理方式。\n\n5. 服务态度与专业措辞（15分）\n检查是否存在不耐烦、责怪、敷衍、辱骂或推卸责任等表达。\n\n6. 打断与通话节奏（10分）\n检查是否频繁打断客户、抢话或存在明显影响沟通的异常节奏。\n\n7. 客户情绪与服务修复（10分）\n检查客户是否出现明显负面情绪，客服是否及时解释和安抚。\n\n8. 结束语与待办确认（10分）\n检查是否礼貌结束，并对尚未完成的事项进行确认。\n\"\"\"\n\ndef main(audio_path, report_save_path):\n    result = {}\n\n    # 音频预处理\n    audio_path = preprocess_audio(audio_path)\n    print(\"音频预处理 已完成✅️\")\n\n    # 说话人分析\n    speaker_segments = analyze_speakers(audio_path)\n    print(\"说话人分析 已完成✅️\")\n\n    # 语音识别\n    asr_result = analyze_asr(audio_path)\n    print(\"语音识别 已完成✅️\")\n\n    # 文本与说话人对齐\n    segments = merge_asr_speakers(asr_result[\"segments\"], speaker_segments)\n    print(\"文本与说话人对齐 已完成✅️\")\n\n    # 说话人角色识别\n    segments = identify_speaker_roles(segments)\n    result[\"transcript\"] = build_transcript(segments)\n    print(\"说话人角色识别 已完成✅️\")\n\n    # 语音综合分析\n    result[\"audio_analysis\"] = analyze_audio(audio_path, segments)\n    print(\"语音综合分析 已完成✅️\")\n\n    # 汇总分析结果\n    analysis_text = format_analysis_result(result[\"transcript\"], result[\"audio_analysis\"])\n    print(\"分析结果汇总 已完成✅️\")\n\n    # 综合质检\n    report = generate_qc_report(analysis_text, QC_RULES)\n    print(\"综合质检 已完成✅️\")\n\n    # 保存并输出结果\n    report_path = save_report(report, report_save_path)\n    print(\"\\n========== 规范化分析结果 ==========\\n\")\n    print(analysis_text)\n    print(\"\\n========== 客服质检报告 ==========\\n\")\n    print(report)\n    print(f\"\\n质检报告已保存到：{report_path}\")\n    return report</code></pre>\n\n<h3 id=\"c17-s17\">运行测试</h3>\n\n<p>输入测试音频进行客服质检测试。</p>\n\n<pre data-language=\"Plain Text\"><code>if __name__ == \"__main__\":\n    main(\n    audio_path=\"./customer_service/客服通话记录.wav\", \n    report_save_path=\"./customer_service/客服质检报告.md\"\n    )</code></pre>\n\n<img src=\"assets/manuscript-20260914/c17-6bdbf54a06b196.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>...</p>\n\n<img src=\"assets/manuscript-20260914/c17-ef059711b5dabe.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>生成的质检报告如下：</p>\n\n<figure><a href=\"assets/manuscript-20260914/c17-d1f09657da099c.md\" class=\"attachment\" download=\"客服质检报告.md\">客服质检报告.md</a></figure>\n\n<p></p>\n\n<p>本章代码及相关文件见：https://www.modelscope.cn/gallery/liucong/df09152b-2d3c-414e-abe6-f2e768a4ef4b</p>\n\n<p></p>\n","imageCount":5,"attachmentCount":2,"discussionId":"chapter-17"},{"id":"chapter-18","number":18,"title":"搭建一个能听也能说的语音助手","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-18.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-18.html","minutes":71,"status":"ready","headings":[{"id":"c18-s1","text":"从开口提问到听见回答，中间经过哪些步骤？","level":"h2"},{"id":"c18-s2","text":"ASR，先把你说的话转成文字","level":"h3"},{"id":"c18-s3","text":"LLM，接过问题来回答","level":"h3"},{"id":"c18-s4","text":"TTS，把文字回答读出来","level":"h3"},{"id":"c18-s5","text":"把录音、识别、回答和播放连起来","level":"h3"},{"id":"c18-s6","text":"动手搭一个能语音问答的助手","level":"h2"},{"id":"c18-s7","text":"准备运行环境","level":"h3"},{"id":"c18-s8","text":"采集麦克风音频","level":"h3"},{"id":"c18-s9","text":"VAD 检测语音活动","level":"h3"},{"id":"c18-s10","text":"ASR 语音识别","level":"h3"},{"id":"c18-s11","text":"LLM 理解问题并生成回答","level":"h3"},{"id":"c18-s12","text":"TTS 生成回答语音","level":"h3"},{"id":"c18-s13","text":"流式处理","level":"h3"},{"id":"c18-s14","text":"实现多轮语音对话","level":"h3"},{"id":"c18-s15","text":"一个完整的本地语音助手","level":"h3"},{"id":"c18-s16","text":"语音助手评测","level":"h3"},{"id":"c18-s17","text":"评测维度设计","level":"h4"},{"id":"c18-s18","text":"自动化评测脚本","level":"h4"}],"html":"<p>在聊天框里问模型一个问题，已经很熟悉了。如果把键盘换成麦克风，直接开口问，等它把答案说出来，会是什么样？</p>\n\n<p></p>\n\n<p>这里会用到前面介绍过的几类模型，ASR 负责把语音转成文字，LLM 负责回答，TTS 再把文字转成声音。录音什么时候开始、用户有没有说话、历史对话怎样保存，也都要安排好。</p>\n\n<p></p>\n\n<h2 id=\"c18-s1\">从开口提问到听见回答，中间经过哪些步骤？</h2>\n\n<p>语音助手需要接收用户的语音问题，理解问题后生成回答，并通过语音将回答播放出来。这个过程主要由 ASR、LLM、TTS 三个核心模型共同完成。</p>\n\n<h3 id=\"c18-s2\">ASR，先把你说的话转成文字</h3>\n\n<p>ASR 需要将麦克风采集到的音频信号转换为文本。</p>\n\n<p>例如，用户对着麦克风说：“北京有哪些著名景点？”，麦克风采集到的是音频数据：</p>\n\n<figure><a href=\"assets/manuscript-20260914/c18-97a3eee17bf6ef.wav\" class=\"attachment\" download=\"query.wav\">query.wav</a></figure>\n\n<p>LLM 不能按照普通文本的方式处理这些数据，需要先通过 ASR 进行语音识别，ASR 识别结果会作为文字输入交给后面的 LLM。</p>\n\n<p>语音助手中的 ASR 不仅需要关注识别内容是否正确，还需要考虑口音、语速、环境噪声等因素。例如，同一句话在安静环境和嘈杂环境中的识别效果可能存在差异。</p>\n\n<p></p>\n\n<h3 id=\"c18-s3\">LLM，接过问题来回答</h3>\n\n<p>LLM 拿到 ASR 输出的用户问题文本，生成对应的回答文本。单轮对话中，问题进去回答出来，流程相对直接。</p>\n\n<p>例如，ASR 识别得到：“北京有哪些著名景点？”，将这段文字发送给 LLM 后，模型生成回答：“北京比较著名的景点包括故宫、天坛、颐和园和八达岭长城等。”</p>\n\n<p>多轮对话则不同。为了让 LLM 理解用户问题在上一轮或前几轮中的指代，需要将前几轮的用户问题和助手回答一并放入上下文。</p>\n\n<p></p>\n\n<h3 id=\"c18-s4\">TTS，把文字回答读出来</h3>\n\n<p>TTS 负责将 LLM 生成的回答转换成对应的语音，由语音助手把回答“说出来”，。</p>\n\n<p>例如，针对 LLM 生成的回答：“北京比较著名的景点包括故宫、天坛、颐和园和八达岭长城等。”，利用 TTS 转为音频：</p>\n\n<figure><a href=\"assets/manuscript-20260914/c18-5a6f60ab3affcb.wav\" class=\"attachment\" download=\"answer.wav\">answer.wav</a></figure>\n\n<p>生成的语音可以直接通过扬声器播放，也可以保存为音频文件。</p>\n\n<p>除了保证文字内容能够正确朗读，TTS 还会影响语音助手最终的听感。例如，声音是否清晰、停顿是否自然、语速是否合适，都会影响实际的交互体验。</p>\n\n<p>在语音助手中，TTS 的输出方式分为非流式和流式两种。如果等 LLM 生成完整回答后再启动 TTS，用户会明显感知到等待时间。更合理的做法是流式 TTS——LLM 每生成几个字，TTS 就立即开始合成对应的音频片段并推送给播放模块，实现“边说边放”。</p>\n\n<p></p>\n\n<h3 id=\"c18-s5\">把录音、识别、回答和播放连起来</h3>\n\n<p>在实际的语音助手中，除了 ASR、LLM 和 TTS 三个核心模型，还需要音频采集、VAD 语音活动检测和音频播放等模块。各模块协同工作，将用户的语音问题转换为语音回答，并支持流式音频输出和多轮对话。完整的处理流程如下图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c18-2f2d5ab6857d11.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>下面按照这条处理链路，从音频输入开始，逐步搭建一个可以在本地运行的语音助手。</p>\n\n<p></p>\n\n<h2 id=\"c18-s6\">动手搭一个能语音问答的助手</h2>\n\n<p>前面介绍了语音助手的基本处理流程。下面按照音频采集、VAD、ASR、LLM 和 TTS 的顺序，逐步实现各个功能模块，并在此基础上增加流式音频输出和多轮对话，最后将这些模块组合成一个完整的本地语音助手。</p>\n\n<p>为了便于后续组合，本节会将需要加载模型的 ASR、LLM 和 TTS 封装成类，将音频采集、播放等功能封装成函数。每完成一个功能模块，都先单独进行测试，确认能够正常运行后，再继续后面的实现。</p>\n\n<h3 id=\"c18-s7\">准备运行环境</h3>\n\n<p>安装基础依赖：</p>\n\n<pre data-language=\"YAML\"><code>!pip3 install torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0\n!pip3 install transformers accelerate modelscope\n!pip3 install qwen-asr \n!pip3 install soundfile webrtcvad-wheels scipy\n!pip3 install gradio\n%pip install --no-deps --force-reinstall qwen-tts==0.1.1 faster-qwen3-tts==0.3.0\n!pip3 install numpy==2.2.6 scipy\n!pip3 install jiwer bert_score onnxruntime</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c18-e8861a4ee62ef9.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>安装完以来后，需要点击上方的<code>Restart</code>重启 kernel。</p>\n\n<img src=\"assets/manuscript-20260914/c18-c3cb3a269cb1fe.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c18-s8\">采集麦克风音频</h3>\n\n<p>语音助手首先需要获取用户的音频。</p>\n\n<p>如果程序直接运行在本地电脑上，可以通过 <code>sounddevice</code> 等库访问麦克风设备。但本章使用的是 ModelScope Notebook，Python 程序实际运行在远程服务器上，服务器无法直接访问用户电脑上的麦克风。</p>\n\n<p>因此，本节使用 Gradio 在浏览器中采集麦克风音频，再将音频数据传给后端程序。</p>\n\n<p>音频采集有 2 种类型：</p>\n\n<p>1）实时音频采集</p>\n\n<p>开始采集后，会一直收集麦克风音频。代码如下：</p>\n\n<pre data-language=\"Python\"><code>import gradio as gr\nimport numpy as np\n\ndef receive_audio(stream, new_chunk):\n    if new_chunk is None:\n        return stream, \"等待麦克风音频……\"\n\n    sample_rate, audio = new_chunk\n\n    if audio.ndim &gt; 1:\n        audio = audio.mean(axis=1)\n\n    audio = audio.astype(np.float32)\n\n    if stream is None:\n        stream = audio\n    else:\n        stream = np.concatenate([stream, audio])\n\n    duration = len(stream) / sample_rate\n    status = f\"收到音频块，当前采集长度：{len(stream)}，已采集：{duration:.2f}秒\"\n\n    print(status)\n    return stream, status\n\nwith gr.Blocks() as demo:\n    gr.Markdown(\"# 麦克风实时采集测试\")\n    gr.Markdown(\"点击麦克风开始录音，说话过程中可以观察音频采集状态。\")\n\n    audio_input = gr.Audio(sources=[\"microphone\"], streaming=True, label=\"麦克风\")\n    status_output = gr.Textbox(label=\"采集状态\", value=\"等待麦克风音频……\")\n    audio_state = gr.State(None)\n\n    audio_input.stream(fn=receive_audio, inputs=[audio_state, audio_input], outputs=[audio_state, status_output])\n\ndemo.launch(share=True)</code></pre>\n\n<p>运行代码结果：</p>\n\n<img src=\"assets/manuscript-20260914/c18-fe2c8085814289.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>打开日志打印的网页地址“<a href=\"http://127.0.0.1:7862/\" target=\"_blank\" rel=\"noopener noreferrer\">http://127.0.0.1:7862</a>”，点击“录制”，并允许浏览器使用麦克风。</p>\n\n<img src=\"assets/manuscript-20260914/c18-bff6ff2f651046.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>网页就会开始采集音频信息，直至手动停止。</p>\n\n<img src=\"assets/manuscript-20260914/c18-dc2bb84bc81fda.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>2）非实时音频采集</p>\n\n<p>每次都需要用户手动触发，才会开始采集音频。代码如下：</p>\n\n<pre data-language=\"Python\"><code>import gradio as gr\n\n# 一个简单的音频采集页面\nwith gr.Blocks() as demo:\n    gr.Markdown(\"# 🎤 麦克风非实时音频采集\")\n    \n    # 音频输入组件\n    audio = gr.Audio(\n    sources=[\"microphone\"],\n    type=\"numpy\",\n    streaming=False,\n    label=\"🎤 点击「录制」说话，说完点击「停止」\",\n    visible=True,\n    interactive=True,\n    elem_classes=\"audio-component\"\n)\n    \n    # 显示音频信息\n    audio_info = gr.Textbox(label=\"音频信息\", lines=3)\n    \n    # 当录制完成时显示信息\n    def show_audio_info(audio_data):\n        if audio_data is None:\n            return \"未录制音频\"\n        sr, data = audio_data\n        return f\"采样率: {sr} Hz\\n时长: {len(data)/sr:.2f} 秒\\n数据形状: {data.shape}\"\n    \n    audio.change(show_audio_info, inputs=audio, outputs=audio_info)\n\ndemo.launch()</code></pre>\n\n<p>运行代码结果：</p>\n\n<img src=\"assets/manuscript-20260914/c18-ebe5a7312e8a5d.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>打开日志打印的网页地址“<a href=\"http://127.0.0.1:7868/\" target=\"_blank\" rel=\"noopener noreferrer\">http://127.0.0.1:7868</a>”，点击“录制”，并允许浏览器使用麦克风。</p>\n\n<img src=\"assets/manuscript-20260914/c18-bff6ff2f651046.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>然后说话即可，点击停止则结束录音。</p>\n\n<p>停止后可以看到音频信息，这说明程序已经接采集到麦克风音频了。</p>\n\n<img src=\"assets/manuscript-20260914/c18-7a5099960ef1d2.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h3 id=\"c18-s9\">VAD 检测语音活动</h3>\n\n<p>VAD 在语音助手中，主要用于判断用户什么时候开始说话，以及什么时候结束一轮语音输入。</p>\n\n<p>WebRTC VAD 的检测等级可以设置为 0、1、2、3，数字越大，对非语音声音的过滤越严格。代码如下：</p>\n\n<p>下面结合采集麦克风音频进行测试：</p>\n\n<p>1）实时音频采集+VAD 语音活动检测</p>\n\n<p>针对实时的音频采集，VAD 主要用来监测用户是否说话了，判断语音开始和结束的时间，并将用户说话的音频裁剪出来。这里采用逐 30ms 音频帧判断是否存在语音活动，VAD 检测代码如下：</p>\n\n<pre data-language=\"Python\"><code>import numpy as np\nimport webrtcvad\nfrom scipy.signal import resample_poly\n\nclass StreamingVAD:\n\n    def __init__(self, sample_rate=16000, frame_ms=30, mode=2, silence_seconds=1.0):\n        self.sample_rate = sample_rate\n        self.frame_ms = frame_ms\n        self.frame_size = int(sample_rate * frame_ms / 1000)\n        self.max_silence_frames = int(silence_seconds * 1000 / frame_ms)\n        self.vad = webrtcvad.Vad(mode)\n        self.reset()\n\n    def reset(self):\n        self.speech_started = False\n        self.silence_frames = 0\n        self.audio_frames = []\n        self.buffer = np.array([], dtype=np.int16)\n\n    def process(self, audio, sample_rate):\n        if audio.ndim &gt; 1:\n            audio = audio.mean(axis=1)\n\n        if np.issubdtype(audio.dtype, np.integer):\n            audio = audio.astype(np.float32) / 32768.0\n        else:\n            audio = audio.astype(np.float32)\n\n        if sample_rate != self.sample_rate:\n            audio = resample_poly(audio, self.sample_rate, sample_rate)\n\n        audio = np.clip(audio, -1.0, 1.0)\n        audio = (audio * 32767).astype(np.int16)\n\n        self.buffer = np.concatenate([self.buffer, audio])\n\n        while len(self.buffer) &gt;= self.frame_size:\n            frame = self.buffer[:self.frame_size]\n            self.buffer = self.buffer[self.frame_size:]\n\n            is_speech = self.vad.is_speech(frame.tobytes(), self.sample_rate)\n\n            if is_speech:\n                if not self.speech_started:\n                    print(\"检测到用户开始说话\", flush=True)\n\n                self.speech_started = True\n                self.silence_frames = 0\n                self.audio_frames.append(frame)\n\n            elif self.speech_started:\n                self.silence_frames += 1\n                self.audio_frames.append(frame)\n\n                if self.silence_frames &gt;= self.max_silence_frames:\n                    print(\"检测到连续静音，本轮语音结束\", flush=True)\n                    return True\n\n        return False\n\n    def get_audio(self):\n        if not self.audio_frames:\n            return None\n\n        return np.concatenate(self.audio_frames)</code></pre>\n\n<p>测试页面代码：</p>\n\n<pre data-language=\"Python\"><code>import gradio as gr\n\nvad_detector = StreamingVAD(mode=2, silence_seconds=1.0)\nvad_logs = []\nturn_index = 0\n\ndef detect_speech(chunk):\n    global vad_logs, turn_index\n\n    if chunk is None:\n        return \"\\n\".join(vad_logs) if vad_logs else \"等待语音输入……\"\n\n    sample_rate, audio = chunk\n    finished = vad_detector.process(audio, sample_rate)\n\n    if finished:\n        speech_audio = vad_detector.get_audio()\n        if speech_audio is not None:\n            turn_index += 1\n            duration = len(speech_audio) / vad_detector.sample_rate\n            log = f\"第{turn_index}轮：检测到有效语音，时长{duration:.2f}秒\"\n            vad_logs.append(log)\n            print(log, flush=True)\n        vad_detector.reset()\n\n    return \"\\n\".join(vad_logs) if vad_logs else \"等待语音输入……\"\n\nwith gr.Blocks() as demo:\n    gr.Markdown(\"# 实时音频采集 + VAD语音活动检测\")\n    gr.Markdown(\"点击“录制”启动麦克风，说话结束后保持约1秒静音。\")\n\n    audio_input = gr.Audio(sources=[\"microphone\"], type=\"numpy\", streaming=True, label=\"麦克风\")\n    log_output = gr.Textbox(label=\"VAD检测日志\", value=\"等待语音输入……\", lines=6)\n\n    audio_input.stream(fn=detect_speech, inputs=audio_input, outputs=log_output, stream_every=0.5)\n\ndemo.launch(share=True)</code></pre>\n\n<p>运行结果：</p>\n\n<img src=\"assets/manuscript-20260914/c18-b7152920d11c1c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>打开日志打印的网页地址：“<a href=\"http://127.0.0.1:7871/\" target=\"_blank\" rel=\"noopener noreferrer\">http://127.0.0.1:7871</a>”，点击“录制”并允许浏览器访问麦克风，然后说话即可。点击停止后页面会显示日志，样例如下。</p>\n\n<img src=\"assets/manuscript-20260914/c18-c617719236fd27.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>2）非实时音频采集+VAD 语音活动检测</p>\n\n<p>针对非实时采集的音频，VAD 主要作为前置过滤器，去除静音和环境噪声，避免 ASR 做无效识别，节省资源。系统会将 Gradio 接收到的整个音频切片，分割为若干 20 毫秒的短帧，并对每一帧进行语音/非语音状态标记。最后根据该切片中的语音帧数量与阈值的关系，判断该段音频的是否交给 ASR 处理。VAD 检测代码如下：</p>\n\n<pre data-language=\"Python\"><code>import webrtcvad\nimport numpy as np\nfrom collections import deque\n\nclass StreamingVAD:\n    def __init__(self, mode=3, silence_seconds=1.0, min_speech_seconds=0.5, \n                 energy_threshold=0.03, start_frames=3, pre_buffer_chunks=5,\n                 target_sr=16000):  # 添加目标采样率\n        self.vad = webrtcvad.Vad(mode)\n        self.silence_seconds = silence_seconds\n        self.min_speech_seconds = min_speech_seconds\n        self.energy_threshold = energy_threshold\n        self.start_frames = start_frames\n        self.pre_buffer_chunks = pre_buffer_chunks\n        self.target_sr = target_sr  # VAD 支持的采样率\n        self.reset()\n\n    def reset(self):\n        self.speech_started = False\n        self.audio_chunks = []\n        self.pre_buffer = deque(maxlen=self.pre_buffer_chunks)\n        self.speech_count = 0\n        self.speech_samples = 0\n        self.silence_samples = 0\n        self.sample_rate = None\n\n    def _resample(self, audio, orig_sr, target_sr):\n        \"\"\"重采样到目标采样率\"\"\"\n        if orig_sr == target_sr:\n            return audio\n        # 计算重采样比例\n        ratio = target_sr / orig_sr\n        # 重采样\n        resampled = signal.resample(audio, int(len(audio) * ratio))\n        return resampled.astype(np.float32)\n\n    def _is_speech(self, audio, sample_rate):\n        try:\n            # 先重采样到目标采样率\n            if sample_rate != self.target_sr:\n                audio = self._resample(audio, sample_rate, self.target_sr)\n                sample_rate = self.target_sr\n            \n            # 检查音频幅度\n            rms = np.sqrt(np.mean(audio ** 2) + 1e-10)\n            print(f\"RMS: {rms:.6f}, 阈值: {self.energy_threshold}\")\n            \n            if rms &lt; self.energy_threshold:\n                print(f\"音频太安静\")\n                return False\n            \n            # 归一化到 [-1, 1]\n            max_val = np.max(np.abs(audio))\n            if max_val &gt; 0:\n                audio = audio / max_val\n            \n            frame_size = int(sample_rate * 0.02)  # 20ms\n            speech_frames = 0\n            total_frames = 0\n            \n            # 转换为 int16\n            audio_int16 = (audio * 32767).astype(np.int16)\n            \n            for i in range(0, len(audio) - frame_size + 1, frame_size):\n                frame = audio_int16[i:i + frame_size]\n                is_speech = self.vad.is_speech(frame.tobytes(), sample_rate)\n                speech_frames += int(is_speech)\n                total_frames += 1\n                \n            if total_frames == 0:\n                return False\n            \n            ratio = speech_frames / total_frames\n            print(f\"语音帧比例: {ratio:.2f} ({speech_frames}/{total_frames})\")\n            return ratio &gt;= 0.5\n            \n        except Exception as e:\n            print(f\"VAD _is_speech 错误: {e}\")\n            traceback.print_exc()\n            return False\n\n    def process(self, audio, sample_rate):\n        try:\n            self.sample_rate = sample_rate\n            audio = np.asarray(audio)\n            if audio.ndim == 2:\n                audio = audio.mean(axis=1)\n            audio = audio.astype(np.float32)\n            \n            # 归一化（如果需要）\n            if np.max(np.abs(audio)) &gt; 1.0:\n                audio = audio / 32768.0\n            \n            is_speech = self._is_speech(audio, sample_rate)\n            print(f\"is_speech: {is_speech}\")\n            \n            if not self.speech_started:\n                self.pre_buffer.append(audio)\n                if is_speech:\n                    self.speech_count += 1\n                else:\n                    self.speech_count = 0\n                if self.speech_count &gt;= self.start_frames:\n                    self.speech_started = True\n                    self.audio_chunks.extend(list(self.pre_buffer))\n                    self.speech_samples = sum(len(chunk) for chunk in self.pre_buffer)\n                    self.silence_samples = 0\n                return False, is_speech\n                \n            self.audio_chunks.append(audio)\n            if is_speech:\n                self.speech_samples += len(audio)\n                self.silence_samples = 0\n            else:\n                self.silence_samples += len(audio)\n                \n            speech_duration = self.speech_samples / sample_rate\n            silence_duration = self.silence_samples / sample_rate\n            \n            if silence_duration &gt;= self.silence_seconds:\n                if speech_duration &gt;= self.min_speech_seconds:\n                    return True, is_speech\n                self.reset()\n            return False, is_speech\n            \n        except Exception as e:\n            print(f\"VAD process 错误: {e}\")\n            traceback.print_exc()\n            return False, False\n\n    def get_audio(self):\n        if not self.audio_chunks:\n            return np.array([], dtype=np.float32)\n        return np.concatenate(self.audio_chunks)</code></pre>\n\n<p>测试代码：</p>\n\n<pre data-language=\"Python\"><code>import gradio as gr\nimport numpy as np\nimport traceback\n\nvad = StreamingVAD(mode=3, silence_seconds=1.0)\n\ndef test_vad(audio_data):\n    try:\n        if audio_data is None:\n            return \"❌ 没有音频数据\"\n        \n        sr, audio = audio_data\n        vad.reset()\n        \n        # 分块处理（每块100ms）\n        chunk_size = int(sr * 0.1)  # 100ms\n        is_complete = False\n        speech_found = False\n        \n        for i in range(0, len(audio), chunk_size):\n            chunk = audio[i:i+chunk_size]\n            if len(chunk) &lt; chunk_size:\n                break\n            is_complete, is_speech = vad.process(chunk, sr)\n            if is_speech:\n                speech_found = True\n            if is_complete:\n                break\n        \n        # 获取裁剪后的音频\n        speech_audio = vad.get_audio()\n        speech_duration = len(speech_audio) / sr if len(speech_audio) &gt; 0 else 0\n        \n        result = f\"\"\"✅ 检测完成\n采样率: {sr} Hz\n音频长度: {len(audio)} 样本\n时长: {len(audio)/sr:.2f} 秒\n检测到语音: {'是 ✅' if speech_found else '否 ❌'}\n触发结束: {'是' if is_complete else '否'}\n裁剪后时长: {speech_duration:.2f} 秒\n        \"\"\"\n        \n        return result\n        \n    except Exception as e:\n        return f\"❌ 错误: {str(e)}\\n{traceback.format_exc()}\"\n\nwith gr.Blocks() as demo:\n    gr.Markdown(\"# 🎤 非实时音频采集+VAD语音活动检测\")\n    \n    audio_input = gr.Audio(\n        sources=[\"microphone\"],\n        type=\"numpy\",\n        label=\"录制音频（录长一点，包含停顿）\"\n    )\n    \n    result = gr.Textbox(label=\"检测结果\", lines=8)\n    \n    audio_input.change(test_vad, audio_input, result)\n\ndemo.launch(debug=True)</code></pre>\n\n<p>运行结果：</p>\n\n<img src=\"assets/manuscript-20260914/c18-7a11023344c1c0.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>打开日志打印的网页地址：“<a href=\"http://127.0.0.1:7869/\" target=\"_blank\" rel=\"noopener noreferrer\">http://127.0.0.1:7869</a>”，点击“录制”并允许浏览器访问麦克风，然后说话即可。点击停止后页面会显示日志，样例如下。</p>\n\n<img src=\"assets/manuscript-20260914/c18-461ac2cc3c020e.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c18-s10\">ASR 语音识别</h3>\n\n<p>获得音频以后，下一步是将语音转换成文字。以 <a href=\"https://modelscope.cn/models/Qwen/Qwen3-ASR-0.6B\" target=\"_blank\" rel=\"noopener noreferrer\">Qwen/Qwen3-ASR-0.6B</a> 模型为例，ASR 实现代码如下：</p>\n\n<pre data-language=\"Python\"><code>import torch\nfrom modelscope import snapshot_download\nfrom qwen_asr import Qwen3ASRModel\n\nclass ASRService:\n\n    def __init__(self, model_id=\"Qwen/Qwen3-ASR-0.6B\", device=\"cuda:0\"):\n        print(\"正在加载ASR模型……\")\n\n        model_dir = snapshot_download(model_id)\n        self.model = (\n            Qwen3ASRModel.from_pretrained(\n                model_dir,\n                dtype=torch.bfloat16,\n                device_map=device,\n                max_inference_batch_size=1,\n                max_new_tokens=256,\n            )\n        )\n\n        print(\"ASR模型加载完成\")\n\n    def transcribe(self, audio_file,language=\"Chinese\"):\n        result = self.model.transcribe(audio=audio_file, language=language)\n        return result[0].text</code></pre>\n\n<p>测试命令：</p>\n\n<pre data-language=\"Plain Text\"><code>asr = ASRService()\ntext = asr.transcribe(\"query.wav\")\n\nprint(\"ASR识别结果：\", text)</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c18-bbba4b10d3a721.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c18-s11\">LLM 理解问题并生成回答</h3>\n\n<p>本节使用 <a href=\"https://modelscope.cn/models/Qwen/Qwen3-4B\" target=\"_blank\" rel=\"noopener noreferrer\">Qwen/Qwen3-4B</a> 模型进行问题理解和答案生成，模型加载和推理代码如下：</p>\n\n<pre data-language=\"Python\"><code>from modelscope import AutoModelForCausalLM, AutoTokenizer\n\nclass LLMService:\n\n    def __init__(self, model_id=\"Qwen/Qwen3-4B\"):\n        print(\"正在加载LLM……\")\n\n        self.tokenizer = AutoTokenizer.from_pretrained(model_id)\n\n        self.model = AutoModelForCausalLM.from_pretrained(model_id, torch_dtype=\"auto\", device_map=\"auto\")\n\n        print(\"LLM加载完成\")\n\n    def chat(self, user_text, history=None):\n        messages = [\n            {\n                \"role\": \"system\",\n                \"content\": \"你是一个语音助手，请用简洁、自然的语言回答问题，尽量控制在150字以内。\",\n            }\n        ]\n\n        if history:\n            messages.extend(history)\n\n        messages.append({\"role\": \"user\", \"content\": user_text})\n\n        text = self.tokenizer.apply_chat_template(\n            messages,\n            tokenize=False,\n            add_generation_prompt=True,\n            enable_thinking=False,\n        )\n\n        inputs = self.tokenizer([text], return_tensors=\"pt\").to(self.model.device)\n\n        generated_ids = self.model.generate(\n            **inputs,\n            max_new_tokens=512,\n            do_sample=True,\n            temperature=0.7,\n            top_p=0.8,\n            top_k=20,\n        )\n\n        output_ids = generated_ids[:, inputs.input_ids.shape[1]:]\n        answer = self.tokenizer.batch_decode(output_ids, skip_special_tokens=True)[0]\n\n        return answer.strip()</code></pre>\n\n<p>将<code>enable_thinking</code>设置为<code>False</code>，关闭思考模式，更适合普通语音问答，也可以减少用户等待时间。</p>\n\n<p>测试问答效果：</p>\n\n<pre data-language=\"Plain Text\"><code>llm = LLMService()\nanswer = llm.chat(\"北京有哪些著名景点？\")\n\nprint(\"回答：\", answer)</code></pre>\n\n<img src=\"assets/manuscript-20260914/c18-8c4bf36bbd1b9b.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c18-s12\">TTS 生成回答语音</h3>\n\n<p>本节选择 <a href=\"https://modelscope.cn/models/Qwen/Qwen3-TTS-12Hz-0.6B-CustomVoice\" target=\"_blank\" rel=\"noopener noreferrer\">Qwen/Qwen3-TTS-12Hz-0.6B-CustomVoice</a> 模型将 LLM 答案转为语音，CustomVoice 模型支持指定预置音色生成语音，可以根据业务需要选择合适的音色。具体实现代码如下：</p>\n\n<pre data-language=\"Python\"><code>import torch\nfrom modelscope import snapshot_download\nfrom qwen_tts import Qwen3TTSModel\n\nclass TTSService:\n\n    def __init__(\n        self,\n        model_id=(\"Qwen/Qwen3-TTS-12Hz-0.6B-CustomVoice\"),\n        device=\"cuda:0\",\n        speaker=\"Vivian\",\n    ):\n        print(\"正在加载TTS模型……\")\n\n        model_dir = snapshot_download(model_id)\n\n        self.model = (\n            Qwen3TTSModel.from_pretrained(model_dir,device_map=device,dtype=torch.bfloat16)\n        )\n\n        self.speaker = speaker\n\n        print(\"TTS模型加载完成\")\n\n    def synthesize(self, text, language=\"Chinese\"):\n        wavs, sample_rate = (\n         self.model.generate_custom_voice(text=text,language=language,speaker=self.speaker)\n        )\n\n        return wavs[0], sample_rate</code></pre>\n\n<p>生成语音测试代码：</p>\n\n<pre data-language=\"Python\"><code>import soundfile as sf\n\ntts = TTSService()\n\nanswer = \"北京有许多著名景点，如故宫、长城、颐和园、天坛、景山、北海公园、南锣鼓巷等。这些地方历史悠久，风景优美，是北京的标志性景点。\"\n\nwav, sample_rate = tts.synthesize(answer)\n\nsf.write(\"tts_answer.wav\", wav, sample_rate)\n\nprint(\"语音已保存：tts_answer.wav\")\nprint(\"采样率：\", sample_rate)</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c18-863d54e0b2c39a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>生成音频如下，可试听：</p>\n\n<figure><a href=\"assets/manuscript-20260914/c18-3a026da3de7a82.wav\" class=\"attachment\" download=\"tts_answer.wav\">tts_answer.wav</a></figure>\n\n<h3 id=\"c18-s13\">流式处理</h3>\n\n<p>等整段语音合成完毕再播放，用户等待时间较长。流式生成在推理阶段同步输出音频，边生成边播放，首帧延迟明显降低，用户交互体验更流畅。TTSService 服务流式音频输出的实现代码如下：</p>\n\n<pre data-language=\"Python\"><code>import torch\nimport numpy as np\nfrom modelscope import snapshot_download\nfrom faster_qwen3_tts import FasterQwen3TTS\n\nclass TTSService:\n\n    def __init__(\n        self,\n        model_id=(\"Qwen/Qwen3-TTS-12Hz-0.6B-CustomVoice\"),\n        device=\"cuda:0\",\n        speaker=\"Vivian\",\n        chunk_size=4,\n    ):\n        print(\"正在加载TTS模型……\")\n\n        model_dir = snapshot_download(model_id)\n        self.model = FasterQwen3TTS.from_pretrained(\n            model_dir,\n            device=device,\n            dtype=torch.bfloat16,\n            attn_implementation=\"sdpa\",\n        )\n\n        self.speaker = speaker\n        self.chunk_size = chunk_size\n        print(\"TTS模型加载完成\")\n\n    def synthesize(self, text, language=\"Chinese\"):\n        wavs, sample_rate = (\n         self.model.generate_custom_voice(text=text,language=language,speaker=self.speaker)\n        )\n        return wavs[0], sample_rate\n\n    def synthesize_stream(self, text, language=\"Chinese\", cancelled=None):\n        \"\"\"流式输出，逐块返回新增波形。\"\"\"\n        if not text.strip():\n            return\n        cancelled = cancelled or (lambda: False)\n        if cancelled():\n            return\n        stream = self.model.generate_custom_voice_streaming(\n            text=text,\n            language=language,\n            speaker=self.speaker,\n            chunk_size=self.chunk_size,\n            max_new_tokens=1024,\n        )\n        sample_rate = None\n        try:\n            for audio_chunk, sr, timing in stream:\n                if cancelled():\n                    break\n                sr = int(sr)\n                if sr &lt;= 0 or (sample_rate is not None and sample_rate != sr):\n                    raise ValueError(\"TTS 音频块的采样率发生变化\")\n                sample_rate = sr\n                audio_chunk = np.asarray(audio_chunk, dtype=np.float32).reshape(-1)\n                if audio_chunk.size:\n                    yield np.clip(np.nan_to_num(audio_chunk), -1, 1), sr\n        finally:\n            stream.close()\n</code></pre>\n\n<p>测试代码：</p>\n\n<pre data-language=\"Plain Text\"><code>import numpy as np\nimport soundfile as sf\n\ntts = TTSService()\ntext = \"今天天气真不错，适合出门散步。\"\n\nchunks = []\nfor audio_chunk, sr in tts.synthesize_stream(text):\n    chunks.append(audio_chunk)\n    print(f\"收到音频块，当前共 {len(chunks)} 块\")\n\nif chunks:\n    full_audio = np.concatenate(chunks)\n    sf.write(\"output.wav\", full_audio, sr)\n    print(f\"保存完成，共 {len(chunks)} 块，总时长 {len(full_audio)/sr:.2f}s\")</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c18-af38afcc77fb2f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>生成音频如下，可试听：</p>\n\n<figure><a href=\"assets/manuscript-20260914/c18-1acadc154136e3.wav\" class=\"attachment\" download=\"output.wav\">output.wav</a></figure>\n\n<h3 id=\"c18-s14\">实现多轮语音对话</h3>\n\n<p>多轮对话场景中，对于存在上下文关联的问题，如果只把当前问题发给 LLM，模型无法理解指代对象。例如：</p>\n\n<pre data-language=\"Plain Text\"><code>用户：北京有哪些著名景点？\n助手：北京有许多著名景点，如故宫、长城、颐和园、天坛、景山、北海公园、南锣鼓巷等。这些地方历史悠久，风景优美，是北京的标志性景点。\n\n用户：第一个什么时候去比较好？</code></pre>\n\n<p>第二轮中的“第一个”依赖上一轮回答，但系统不保存历史记录，模型无从得知指代内容。因此需要保存历史问答记录，将完整对话上下文一并送入模型。</p>\n\n<p>可以将历史对话封装成<code>ConversationManager</code>类：</p>\n\n<pre data-language=\"Python\"><code>class ConversationManager:\n\n    def __init__(self):\n        self.history = []\n\n    def get_history(self):\n        return self.history\n\n    def add_turn(self, user_text, assistant_text):\n        self.history.append({\"role\": \"user\", \"content\": user_text})\n        self.history.append({\"role\": \"assistant\", \"content\": assistant_text})\n\n    def clear(self):\n        self.history.clear()</code></pre>\n\n<p>会话记录保存样例：</p>\n\n<pre data-language=\"Python\"><code>conversation = ConversationManager()\nllm = LLMService()\n\nquestion1 = \"北京有哪些著名景点？\"\n\nanswer1 = llm.chat(question1, conversation.get_history())\nconversation.add_turn(question1, answer1)\nprint(\"第一轮会话结束\")\n\n\nquestion2 = (\"第一个什么时候去比较好？\")  \nanswer2 = llm.chat(question2, conversation.get_history())  \nconversation.add_turn(question2, answer2)  \nprint(\"第二轮会话结束\") \n\nprint(\"会话历史：\", conversation.get_history())</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c18-5d9b9eccd5f601.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>多轮对话保存的是用户问题文本和助手回答文本。下一轮对话时，再把这些历史消息与新的用户问题一起发送给 LLM。这样，语音输入和输出仍然负责用户与系统之间的交互，而对话上下文则由 LLM 使用的文字历史进行管理。</p>\n\n<h3 id=\"c18-s15\">一个完整的本地语音助手</h3>\n\n<p>前面已经分别介绍了音频采集、VAD、ASR、LLM、TTS、流式处理和多轮对话。现在可以直接复用前面定义的类和函数，将这些模块连接成一个完整的本地语音助手，其中对已有的<code>ConversationManager</code>类进行了改造，扩充了用于前端展示的额外信息存储。</p>\n\n<p>由于 gadio 展示限制，这里采用非实时音频采集（点击录制→上传处理）和非流式音频输出（生成完整音频后返回）的方式搭建本地语音助手。</p>\n\n<p>完整代码如下：</p>\n\n<pre data-language=\"Plain Text\"><code>import gradio as gr\nimport numpy as np\nimport soundfile as sf\nimport tempfile, os, threading, time\nfrom datetime import datetime\nimport base64\nimport librosa\nimport json\n\nos.environ[\"GRADIO_ANALYTICS_ENABLED\"] = \"False\"\n\nclass ConversationManager:\n    def __init__(self): self.reset()\n    def reset(self):\n        self.messages, self.conversation_history = [], []\n        self.current_user_text = self.current_assistant_text = self.current_audio_html = None\n    def add_user_message(self, text):\n        self.messages.append((\"user\", text, None))\n        self.conversation_history.append({\"role\": \"user\", \"content\": text})\n        self.current_user_text = text\n    def add_assistant_message(self, text, audio_html=None):\n        self.messages.append((\"assistant\", text, audio_html))\n        self.conversation_history.append({\"role\": \"assistant\", \"content\": text})\n        self.current_assistant_text, self.current_audio_html = text, audio_html\n    def get_history_html(self):\n        if not self.messages:\n            return '&lt;p style=\"color: #999; text-align: center; padding: 40px;\"&gt;暂无对话记录&lt;/p&gt;'\n        parts = []\n        for role, text, audio_html in self.messages:\n            if role == \"user\":\n                parts.append(f'''\n                &lt;div style=\"display: flex; justify-content: flex-end; margin: 6px 0;\"&gt;\n                    &lt;div style=\"max-width: 75%; background: #e3f2fd; padding: 8px 14px; border-radius: 16px 16px 4px 16px;\"&gt;\n                        &lt;div style=\"font-size: 12px; color: #1565c0; font-weight: bold;\"&gt;👤 我&lt;/div&gt;\n                        &lt;div style=\"font-size: 14px; line-height: 1.5;\"&gt;{text}&lt;/div&gt;\n                    &lt;/div&gt;\n                &lt;/div&gt;\n                ''')\n            else:\n                parts.append(f'''\n                &lt;div style=\"display: flex; justify-content: flex-start; margin: 6px 0;\"&gt;\n                    &lt;div style=\"max-width: 75%; background: #f5f5f5; padding: 8px 14px; border-radius: 16px 16px 16px 4px;\"&gt;\n                        &lt;div style=\"font-size: 12px; color: #2e7d32; font-weight: bold;\"&gt;🤖 助手&lt;/div&gt;\n                        &lt;div style=\"font-size: 14px; line-height: 1.5;\"&gt;{text}&lt;/div&gt;\n                        {audio_html if audio_html else ''}\n                    &lt;/div&gt;\n                &lt;/div&gt;\n                ''')\n        return ''.join(parts)\n    def get_history_for_llm(self): return self.conversation_history.copy()\n    def get_current_round(self): return len([m for m in self.messages if m[0] == \"user\"])\n\nchat, logs, pending_audio, is_processing, is_waiting_for_response = (\n    ConversationManager(), [], None, False, False\n)\nprocess_lock, log_lock = threading.Lock(), threading.Lock()\n\ndef add_log(msg):\n    line = f\"[{datetime.now().strftime('%H:%M:%S')}] {msg}\"\n    with log_lock:\n        logs.append(line)\n        if len(logs) &gt; 50: logs.pop(0)\n    print(line)\n\ndef save_temp_audio(audio, sr):\n    fd, path = tempfile.mkstemp(suffix=\".wav\", prefix=\"audio_\")\n    os.close(fd)\n    audio = np.asarray(audio, dtype=np.float32)\n    max_val = np.max(np.abs(audio))\n    if max_val &gt; 0: audio = audio / max_val\n    sf.write(path, audio, sr)\n    return path\n\ndef get_audio_duration(audio, sr): return len(np.asarray(audio)) / sr\n\ndef create_html_audio_player(audio_path):\n    if not audio_path or not os.path.exists(audio_path): return None\n    try:\n        with open(audio_path, 'rb') as f:\n            audio_bytes = base64.b64encode(f.read()).decode('utf-8')\n        return f'''\n        &lt;div style=\"margin-top: 6px; padding: 4px 6px; background: #f8f9fa; border-radius: 6px; border: 1px solid #e0e0e0;\"&gt;\n            &lt;audio controls style=\"width: 100%; height: 32px;\"&gt;\n                &lt;source src=\"data:audio/wav;base64,{audio_bytes}\" type=\"audio/wav\"&gt;\n                您的浏览器不支持音频播放\n            &lt;/audio&gt;\n        &lt;/div&gt;\n        '''\n    except Exception as e:\n        add_log(f\"❌ 创建HTML播放器失败: {e}\")\n        return None\n\ndef process_with_yield(audio_data):\n    global pending_audio, is_processing, is_waiting_for_response\n    \n    if is_processing or is_waiting_for_response:\n        add_log(\"⏳ 正在处理中，请稍候...\")\n        yield gr.skip(), gr.skip()\n        return\n    \n    if audio_data is None:\n        yield gr.skip(), gr.skip()\n        return\n    \n    try:\n        sr, audio = audio_data\n        if audio is None or len(audio) == 0:\n            yield gr.skip(), gr.skip()\n            return\n        audio = np.asarray(audio)\n        if audio.ndim == 2: audio = audio.mean(axis=1)\n        duration = get_audio_duration(audio, sr)\n        if duration &lt; 0.8:\n            add_log(f\"⚠️ 音频太短 ({duration:.1f}秒)，请说长一点\")\n            yield gr.skip(), gr.skip()\n            return\n        \n        is_processing = is_waiting_for_response = True\n        add_log(f\"✅ 录制完成，{duration:.1f}秒\")\n        \n        user_audio_file = save_temp_audio(audio, sr)\n        add_log(\"🎤 开始处理...\")\n        add_log(\"🔊 ASR识别中...\")\n        \n        try:\n            user_text = asr.transcribe(user_audio_file).strip()\n        except Exception as e:\n            add_log(f\"❌ ASR错误: {e}\")\n            is_processing = is_waiting_for_response = False\n            if os.path.exists(user_audio_file): os.remove(user_audio_file)\n            yield gr.skip(), gr.skip()\n            return\n        \n        if not user_text or len(user_text) &lt; 2:\n            add_log(f\"⚠️ 未识别到有效文本: '{user_text}'\")\n            is_processing = is_waiting_for_response = False\n            if os.path.exists(user_audio_file): os.remove(user_audio_file)\n            yield gr.skip(), gr.skip()\n            return\n        \n        add_log(f\"👤 我: {user_text}\")\n        \n        # 第一步：显示用户文本\n        chat.add_user_message(user_text)\n        yield chat.get_history_html(), gr.update(value=None)\n        \n        history = chat.get_history_for_llm()\n        add_log(f\"📚 历史对话: {len(history)//2} 轮\")\n        add_log(\"🧠 LLM思考中...\")\n        \n        try:\n            answer = llm.chat(user_text, history=history).strip()\n        except Exception as e:\n            add_log(f\"❌ LLM错误: {e}\")\n            is_processing = is_waiting_for_response = False\n            if os.path.exists(user_audio_file): os.remove(user_audio_file)\n            chat.add_assistant_message(f\"❌ LLM错误: {e}\", None)\n            yield chat.get_history_html(), gr.update(value=None)\n            return\n        \n        if not answer:\n            add_log(\"⚠️ LLM未生成回答\")\n            is_processing = is_waiting_for_response = False\n            if os.path.exists(user_audio_file): os.remove(user_audio_file)\n            chat.add_assistant_message(\"⚠️ LLM未生成回答\", None)\n            yield chat.get_history_html(), gr.update(value=None)\n            return\n        \n        add_log(f\"🤖 助手: {answer}\")\n        add_log(\"🗣️ TTS生成中...\")\n        chunks, tts_sr = [], None\n        \n        try:\n            for audio_chunk, sample_rate in tts.synthesize_stream(answer):\n                if audio_chunk is not None and len(audio_chunk) &gt; 0:\n                    chunk = np.asarray(audio_chunk, dtype=np.float32)\n                    chunks.append(chunk)\n                    if tts_sr is None: tts_sr = sample_rate\n        except Exception as e:\n            add_log(f\"❌ TTS错误: {e}\")\n            is_processing = is_waiting_for_response = False\n            if os.path.exists(user_audio_file): os.remove(user_audio_file)\n            chat.add_assistant_message(f\"❌ TTS错误: {e}\", None)\n            yield chat.get_history_html(), gr.update(value=None)\n            return\n        \n        if not chunks:\n            add_log(\"⚠️ TTS未生成音频\")\n            is_processing = is_waiting_for_response = False\n            if os.path.exists(user_audio_file): os.remove(user_audio_file)\n            chat.add_assistant_message(\"⚠️ TTS未生成音频\", None)\n            yield chat.get_history_html(), gr.update(value=None)\n            return\n        \n        wav = np.concatenate(chunks)\n        audio_path = save_temp_audio(wav, tts_sr)\n        total_rounds = chat.get_current_round() + 1\n        audio_html = create_html_audio_player(audio_path)\n        \n        # 第二步：显示助手答案 + 音频\n        chat.add_assistant_message(answer, audio_html)\n        \n        add_log(f\"✅ 第 {total_rounds} 轮对话完成，{len(wav)/tts_sr:.1f}秒\")\n        if os.path.exists(user_audio_file): os.remove(user_audio_file)\n        is_processing = is_waiting_for_response = False\n        \n        yield chat.get_history_html(), gr.update(value=None)\n        \n    except Exception as e:\n        add_log(f\"❌ 处理错误: {e}\")\n        is_processing = is_waiting_for_response = False\n        yield gr.skip(), gr.skip()\n\ndef clear_all():\n    global generation_id, pending_audio, is_processing, is_waiting_for_response\n    generation_id += 1\n    chat.reset()\n    pending_audio = None\n    is_processing = is_waiting_for_response = False\n    return '&lt;p style=\"color: #999; text-align: center; padding: 40px;\"&gt;暂无对话记录&lt;/p&gt;', gr.update(value=None)\n\nprint(\"🚀 正在加载模型...\")\n# asr = ASRService()\n# llm = LLMService()\n# tts = TTSService()\nprint(\"✅ 所有模型加载完成！\")\n\ncss = \"\"\"\n.progress-container { display: none !important; }\n.gr-progress { display: none !important; }\n.loading { display: none !important; }\n#header-area { max-width:800px; margin:0 auto 16px auto; padding:12px 16px; background:#f5f5f5; border-radius:10px; }\n#mic-area { display:flex; align-items:center; gap:12px; }\n#mic-area .audio-component { flex:1; }\n#mic-area .clear-button { flex-shrink:0; }\n#chat-container { min-height:400px; max-height:500px; overflow-y:auto; padding:10px 14px; background:#fff; border-radius:8px; border:1px solid #e0e0e0; }\n#chat-container::-webkit-scrollbar { width:5px; }\n#chat-container::-webkit-scrollbar-track { background:#f1f1f1; border-radius:3px; }\n#chat-container::-webkit-scrollbar-thumb { background:#c1c1c1; border-radius:3px; }\n#chat-container::-webkit-scrollbar-thumb:hover { background:#a8a8a8; }\nfooter { display:none !important; }\n\"\"\"\n\nwith gr.Blocks(css=css, theme=gr.themes.Soft(), title=\"语音助手\") as demo:\n    gr.Markdown(\"## 🎙️ 语音助手\")\n    with gr.Group(elem_id=\"header-area\"):\n        with gr.Row(elem_id=\"mic-area\"):\n            mic_audio = gr.Audio(\n                sources=[\"microphone\"],\n                type=\"numpy\",\n                streaming=False,\n                label=\"🎤 点击「录制」说话，说完点击「停止」\",\n                visible=True,\n                interactive=True,\n                elem_classes=\"audio-component\"\n            )\n            clear_btn = gr.Button(\n                \"🗑️ 清空对话\",\n                variant=\"stop\",\n                size=\"sm\",\n                elem_classes=\"clear-button\"\n            )\n    chat_history = gr.HTML(\n        value='&lt;p style=\"color: #999; text-align: center; padding: 40px;\"&gt;暂无对话记录&lt;/p&gt;',\n        label=\"💬 对话记录\",\n        elem_id=\"chat-container\"\n    )\n    \n    mic_audio.change(\n        fn=process_with_yield,\n        inputs=mic_audio,\n        outputs=[chat_history, mic_audio]\n    )\n    \n    clear_btn.click(\n        fn=clear_all,\n        outputs=[chat_history, mic_audio],\n        queue=False\n    )\n\ndemo.queue(max_size=10)\ndemo.launch(share=True, show_error=True, debug=False)</code></pre>\n\n<p>运行结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c18-25b6abbb9ca6b4.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>进入网址：<a href=\"http://127.0.0.1:7861/\" target=\"_blank\" rel=\"noopener noreferrer\">http://127.0.0.1:7861</a>。点击“录制”后开始录音，点击“停止”后就会开始解析问题、生成答案音频。</p>\n\n<img src=\"assets/manuscript-20260914/c18-e6591451112a20.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c18-s16\">语音助手评测</h3>\n\n<h4 id=\"c18-s17\">评测维度设计</h4>\n\n<p>语音助手的评测需要从四个相互独立的维度分别考察，每个维度对应一项或多项可计算的指标。四个维度分别为准确性、实时性、鲁棒性和交互自然度。</p>\n\n<p>1）准确性</p>\n\n<p>准确性衡量语音助手“说得对不对”和“答得对不对”两个层面。</p>\n\n<p>ASR 字错率（WER） 是衡量语音转文字精度的硬指标。计算方法是将 ASR 输出的识别文本与人工标注的标准转写文本按最小编辑距离对齐，统计替换（S）、删除（D）、插入（I）三类错误的总数，除以标准文本总字数 N，即 WER = (S+D+I)/N。中文语音识别在实际业务中，WER 控制在 5%~15%就算可接受区间，再低当然更好，但要付出的优化成本会指数上升。</p>\n\n<p>LLM 回答语义相似度评估生成回答与标准答案在语义层面的接近程度。采用 BERTScore 指标，通过预训练语言模型将生成回答和标准答案分别编码为上下文向量，计算两段文本中每个 token 向量的余弦相似度，再综合得到整体相似性分数。BERTScore 取值范围为 0 到 1，分数越接近 1 表示语义越一致。该指标相比传统的 BLEU 或 ROUGE，对同义表达具有更好的容忍度。对于无法自动判定的开放性问题，可辅以人工评分（5 分制）作为补充。</p>\n\n<p>2）耗时</p>\n\n<p>耗时衡量语音助手各阶段及整体的处理速度，直接反映系统的计算效率。</p>\n\n<p>端到端总耗时是从 VAD 检测到用户语音结束（t_vad_end）到 TTS 完成全部音频输出（t_tts_end）之间的时间间隔，即 T = t_tts_end − t_vad_end。该指标反映了用户从结束发言到获得完整语音回答的完整等待时间。</p>\n\n<p>各阶段独立耗时将端到端总耗时拆解为四个子阶段的分别耗时：</p>\n\n<ul><li>VAD 耗时：从音频输入到完成语音活动检测的时间</li><li>ASR 耗时：从语音段提取到完成文字识别的时间</li><li>LLM 耗时：从用户文本输入到完成回答生成的时间</li><li>TTS 耗时：从回答文本输入到完成全部音频合成的时间</li></ul>\n\n<p>分别记录各阶段的起止时间戳，便于定位系统瓶颈。四个子阶段耗时的加和即为端到端总耗时。</p>\n\n<p>3）鲁棒性</p>\n\n<p>鲁棒性考察语音助手在非理想条件下的性能衰减程度。</p>\n\n<p>评测时需构建多组对照测试集，各组包含相同的问句集合，但施加不同的干扰条件，并将各条件下的评测结果与基线条件（安静环境、标准普通话、正常语速）进行对比，以性能衰减幅度作为鲁棒性的量化依据。</p>\n\n<p>噪声环境：在原始语音上叠加三类背景噪声（白噪声、街道噪声、多说话人噪声），每种噪声按三个信噪比档位（0dB、10dB、20dB）分别施加，记录各档位下的 WER 和端到端延迟，观察噪声对识别精度和响应速度的影响。</p>\n\n<p>口音差异：采集或合成带有不同方言口音（如四川话、东北话、粤式普通话）的测试语音，与标准普通话测试集的结果进行对比，WER 的差值直接反映 ASR 在各口音方向上的偏倚程度。</p>\n\n<p>语速变化：通过语音信号处理工具将测试语音加速至 1.3 倍和减速至 0.7 倍，观察 WER 随语速变化而产生的波动。</p>\n\n<p>4）交互自然度</p>\n\n<p>交互自然度评估的是多轮对话场景下语音助手的行为是否连贯、顺畅。</p>\n\n<p>VAD 误断率包含两类错误：误触发（用户未说话时 VAD 判定为有语音）和漏检（用户说话时 VAD 未能检测到）。误触发会导致助手在静默状态下意外开始录音和处理，漏检则会导致用户语音被截断，两者均影响交互体验。评测时在真实对话场景中连续运行 30 分钟，统计 VAD 判定状态与人工标注的真实语音活动之间的不一致帧数占总帧数的比例。</p>\n\n<p>多轮对话上下文保持能力：测试语音助手能否在连续 3~5 轮对话中正确指代上文提到的实体和意图。例如用户依次说“今天北京天气怎么样”“那上海呢”“两个地方哪个更冷”，考察第二轮的“那”和第三轮的“两个地方”能否被正确解析。该指标无法自动计算，需人工对照对话记录逐轮判定上下文引用是否正确，以正确轮次占总轮次的比例表示。</p>\n\n<p>TTS 自然度 MOS 分采用平均意见分（Mean Opinion Score）方法评估合成语音的自然程度。邀请不少于 10 名听音人对 TTS 生成的语音样本进行 5 分制主观打分（5 分=与真人无异，1 分=完全机械声），取所有打分的算术平均值。测试样本需涵盖陈述句、疑问句、感叹句和长短句四种类型，每类不少于 5 条，以避免单一文本类型对评分的偏差。</p>\n\n<p>四个维度的全部指标汇总如下表所示：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col><col><col><col></colgroup><tbody><tr><td><p>维度</p></td><td><p>指标</p></td><td><p>计算方式</p></td><td><p>数值类型</p></td><td><p>是否支持自动化评测</p></td></tr><tr><td><p>准确性</p></td><td><p>ASR WER</p></td><td><p>(S+D+I)/N</p></td><td><p>百分比，越低越好</p></td><td><p>是</p></td></tr><tr><td><p>准确性</p></td><td><p>LLM 语义相似度</p></td><td><p>BERTScore</p></td><td><p>0~1，越高越好</p></td><td><p>是</p></td></tr><tr><td><p>耗时</p></td><td><p>端到端总耗时</p></td><td><p>t_tts_end − t_vad_end</p></td><td><p>毫秒，越低越好</p></td><td><p>是</p></td></tr><tr><td><p>耗时</p></td><td><p>VAD 耗时</p></td><td><p>t_vad_end − t_vad_start</p></td><td><p>毫秒，越低越好</p></td><td><p>是</p></td></tr><tr><td><p>耗时</p></td><td><p>ASR 耗时</p></td><td><p>t_asr_end − t_asr_start</p></td><td><p>毫秒，越低越好</p></td><td><p>是</p></td></tr><tr><td><p>耗时</p></td><td><p>LLM 耗时</p></td><td><p>t_llm_end − t_llm_start</p></td><td><p>毫秒，越低越好</p></td><td><p>是</p></td></tr><tr><td><p>耗时</p></td><td><p>TTS 耗时</p></td><td><p>t_tts_end − t_tts_start</p></td><td><p>毫秒，越低越好</p></td><td><p>是</p></td></tr><tr><td><p>鲁棒性</p></td><td><p>各条件下 WER 变化</p></td><td><p>施扰结果 − 基线结果</p></td><td><p>差值（pp），越小越鲁棒</p></td><td><p>是</p></td></tr><tr><td><p>交互自然度</p></td><td><p>VAD 误断率</p></td><td><p>不一致帧数 / 总帧数</p></td><td><p>百分比，越低越好</p></td><td><p>是</p></td></tr><tr><td><p>交互自然度</p></td><td><p>上下文保持准确率</p></td><td><p>正确指代轮次 / 总轮次</p></td><td><p>百分比，越高越好</p></td><td><p>否</p></td></tr><tr><td><p>交互自然度</p></td><td><p>TTS 自然度 MOS</p></td><td><p>多人打分的算术均值</p></td><td><p>1~5，越高越好</p></td><td><p>否</p></td></tr></tbody></table>\n</div>\n<h4 id=\"c18-s18\">自动化评测脚本</h4>\n\n<p>自动化评测脚本的核心任务是：读取标准测试集，对每条测试音频依次执行 VAD、ASR、LLM、TTS 四个阶段，记录每阶段的耗时和中间结果，最后与标准答案比对，生成量化评测报告。</p>\n\n<p>1）测试集结构设计</p>\n\n<p>测试集以 JSON 格式组织，存放于 /mnt/workspace/ 目录下。每条测试样本包含以下字段：</p>\n\n<pre data-language=\"JSON\"><code>{\n  \"samples\": [\n    {\n      \"id\": \"sample_001\",\n      \"audio_path\": \"/mnt/workspace/query.wav\",\n      \"reference_text\": \"北京有哪些著名景点\",\n      \"reference_answer\": \"北京有许多著名景点，如故宫、长城、颐和园、天坛、景山、北海公园、南锣鼓巷等。这些地方历史悠久，风景优美，是北京的标志性景点。\",\n      \"category\": \"weather\",\n      \"noise_type\": \"clean\",\n      \"speaker_accent\": \"standard\"\n    }\n  ]\n}</code></pre>\n\n<p>其中 <code>reference_text</code> 为 ASR 阶段的标准转写文本，用于计算 WER；<code>reference_answer</code> 为 LLM 阶段的标准回答，用于计算语义相似度。</p>\n\n<p>测试音频文件的采样率统一为 16000 Hz，单声道，时长控制在 2~8 秒之间。若需要测试鲁棒性，可在 <code>noise_type</code> 和 <code>speaker_accent</code> 字段中标注干扰条件。</p>\n\n<p>2）自动化评测</p>\n\n<p>构建评测执行器 <code>AudioEvaluator</code> 接收音频文件路径，依次调用 VAD、ASR、LLM、TTS 四个模块，记录各阶段的起始和结束时间戳，保存中间输出，并计算 WER 和 BERTScore 指标。 遍历测试集所有样本，调用执行器逐条处理，汇总统计结果。评测代码如下：</p>\n\n<pre data-language=\"Python\"><code>import os\nimport json\nimport time\nimport tempfile\nimport traceback\nfrom pathlib import Path\nfrom typing import Dict\nfrom collections import defaultdict\n\nimport numpy as np\nimport soundfile as sf\nimport librosa\nfrom jiwer import wer\nimport torch\nfrom transformers import AutoTokenizer, AutoModel\n\n# 使用 modelscope 加载 BERT 模型（会自动下载）\nfrom modelscope import AutoTokenizer, AutoModel\n\ndef compute_bert_score(pred_text: str, ref_text: str, model, tokenizer) -&gt; float:\n    \"\"\"\n    使用本地 BERT 模型计算语义相似度（简化版 BERTScore）\n    \"\"\"\n    if not pred_text or not ref_text:\n        return 0.0\n    \n    # 编码\n    pred_tokens = tokenizer(pred_text, return_tensors=\"pt\", truncation=True, max_length=512)\n    ref_tokens = tokenizer(ref_text, return_tensors=\"pt\", truncation=True, max_length=512)\n    \n    with torch.no_grad():\n        pred_outputs = model(**pred_tokens)\n        ref_outputs = model(**ref_tokens)\n    \n    # 获取最后一层的隐藏状态\n    pred_embeds = pred_outputs.last_hidden_state.squeeze(0)\n    ref_embeds = ref_outputs.last_hidden_state.squeeze(0)\n    \n    # 计算 token 级别余弦相似度（取平均）\n    pred_norm = torch.nn.functional.normalize(pred_embeds, p=2, dim=1)\n    ref_norm = torch.nn.functional.normalize(ref_embeds, p=2, dim=1)\n    \n    # 计算余弦相似度矩阵\n    sim_matrix = torch.matmul(pred_norm, ref_norm.T)\n    \n    # 取每个预测 token 的最佳匹配，然后取平均\n    max_sim, _ = sim_matrix.max(dim=1)\n    score = max_sim.mean().item()\n    \n    return score\n\nclass AudioEvaluator:\n    def __init__(self, vad, asr, llm, tts, bert_model, bert_tokenizer):\n        self.vad = vad\n        self.asr = asr\n        self.llm = llm\n        self.tts = tts\n        self.bert_model = bert_model\n        self.bert_tokenizer = bert_tokenizer\n    \n    def evaluate_single(self, audio_path: str) -&gt; Dict:\n        result = {\n            \"audio_path\": audio_path,\n            \"stages\": {},\n            \"asr_text\": \"\",\n            \"llm_answer\": \"\",\n            \"status\": \"success\",\n            \"error_detail\": \"\"\n        }\n        \n        try:\n            # 检查音频文件是否存在\n            if not os.path.exists(audio_path):\n                result[\"status\"] = \"音频文件不存在\"\n                result[\"error_detail\"] = f\"文件路径: {audio_path}\"\n                return result\n            \n            # 加载音频\n            try:\n                audio, sr = sf.read(audio_path)\n            except Exception as e:\n                result[\"status\"] = \"音频加载失败\"\n                result[\"error_detail\"] = f\"{e}\\n{traceback.format_exc()}\"\n                return result\n            \n            if sr != 16000:\n                try:\n                    audio = librosa.resample(audio, orig_sr=sr, target_sr=16000)\n                    sr = 16000\n                except Exception as e:\n                    result[\"status\"] = \"音频重采样失败\"\n                    result[\"error_detail\"] = f\"{e}\\n{traceback.format_exc()}\"\n                    return result\n            \n            # ---------- VAD 阶段 ----------\n            t0 = time.perf_counter()\n            try:\n                # 重置 VAD 状态\n                self.vad.reset()\n                \n                # 分块处理（每块100ms）\n                chunk_size = int(sr * 0.1)\n                is_complete = False\n                speech_found = False\n                \n                for i in range(0, len(audio), chunk_size):\n                    chunk = audio[i:i+chunk_size]\n                    if len(chunk) &lt; chunk_size:\n                        break\n                    is_complete, is_speech = self.vad.process(chunk, sr)\n                    if is_speech:\n                        speech_found = True\n                    if is_complete:\n                        break\n                \n                # 获取裁剪后的语音音频\n                speech_audio = self.vad.get_audio()\n                \n                # 如果检测到语音，使用裁剪后的音频\n                if speech_found and len(speech_audio) &gt; 0:\n                    audio = speech_audio\n                    \n            except Exception as e:\n                result[\"status\"] = f\"VAD错误: {e}\"\n                result[\"error_detail\"] = traceback.format_exc()\n                return result\n            t1 = time.perf_counter()\n            result[\"stages\"][\"vad\"] = (t1 - t0) * 1000\n            \n            # ---------- ASR 阶段 ----------\n            t0 = time.perf_counter()\n            try:\n                with tempfile.NamedTemporaryFile(suffix=\".wav\", delete=False) as tmp:\n                    sf.write(tmp.name, audio, sr)\n                    tmp_path = tmp.name\n                asr_text = self.asr.transcribe(tmp_path).strip()\n                os.unlink(tmp_path)\n            except Exception as e:\n                result[\"status\"] = f\"ASR错误: {e}\"\n                result[\"error_detail\"] = traceback.format_exc()\n                return result\n            t1 = time.perf_counter()\n            result[\"stages\"][\"asr\"] = (t1 - t0) * 1000\n            result[\"asr_text\"] = asr_text\n            \n            if not asr_text or len(asr_text) &lt; 1:\n                result[\"status\"] = \"ASR结果为空\"\n                result[\"error_detail\"] = f\"asr_text: '{asr_text}'\"\n                return result\n            \n            # ---------- LLM 阶段 ----------\n            t0 = time.perf_counter()\n            try:\n                llm_answer = self.llm.chat(asr_text, history=[]).strip()\n            except Exception as e:\n                result[\"status\"] = f\"LLM错误: {e}\"\n                result[\"error_detail\"] = traceback.format_exc()\n                return result\n            t1 = time.perf_counter()\n            result[\"stages\"][\"llm\"] = (t1 - t0) * 1000\n            result[\"llm_answer\"] = llm_answer\n            \n            if not llm_answer:\n                result[\"status\"] = \"LLM回答为空\"\n                result[\"error_detail\"] = f\"llm_answer: '{llm_answer}'\"\n                return result\n            \n            # ---------- TTS 阶段 ----------\n            t0 = time.perf_counter()\n            try:\n                chunks = []\n                tts_sr = None\n                for chunk, sr_chunk in self.tts.synthesize_stream(llm_answer):\n                    if chunk is not None and len(chunk) &gt; 0:\n                        chunks.append(np.asarray(chunk, dtype=np.float32))\n                        if tts_sr is None:\n                            tts_sr = sr_chunk\n                if not chunks:\n                    result[\"status\"] = \"TTS未生成音频\"\n                    result[\"error_detail\"] = \"TTS返回空chunks\"\n                    return result\n            except Exception as e:\n                result[\"status\"] = f\"TTS错误: {e}\"\n                result[\"error_detail\"] = traceback.format_exc()\n                return result\n            t1 = time.perf_counter()\n            result[\"stages\"][\"tts\"] = (t1 - t0) * 1000\n            \n            result[\"total_time\"] = sum(result[\"stages\"].values())\n            \n        except Exception as e:\n            result[\"status\"] = f\"未知错误: {e}\"\n            result[\"error_detail\"] = traceback.format_exc()\n        \n        return result\n\ndef run_batch_evaluation(test_set_path: str, evaluator: AudioEvaluator) -&gt; Dict:\n    with open(test_set_path, 'r', encoding='utf-8') as f:\n        test_set = json.load(f)\n    \n    samples = test_set[\"samples\"]\n    total = len(samples)\n    \n    all_results = []\n    stage_times = defaultdict(list)\n    wer_list = []\n    bert_scores = []\n    e2e_latencies = []\n    errors = []\n    \n    for idx, sample in enumerate(samples):\n        print(f\"评测进度: {idx+1}/{total} - {sample['id']}\")\n        \n        result = evaluator.evaluate_single(sample[\"audio_path\"])\n        result[\"id\"] = sample[\"id\"]\n        result[\"reference_text\"] = sample[\"reference_text\"]\n        result[\"reference_answer\"] = sample[\"reference_answer\"]\n        result[\"category\"] = sample.get(\"category\", \"unknown\")\n        result[\"noise_type\"] = sample.get(\"noise_type\", \"clean\")\n        result[\"speaker_accent\"] = sample.get(\"speaker_accent\", \"standard\")\n        result[\"speed_factor\"] = sample.get(\"speed_factor\", 1.0)\n        \n        # 打印错误详情\n        if result[\"status\"] != \"success\":\n            print(f\"❌ 样本 {sample['id']} 失败: {result['status']}\")\n            print(f\"   详情: {result.get('error_detail', '无详细信息')}\")\n            errors.append(result)\n            all_results.append(result)\n            continue\n        \n        print(f\"✅ 样本 {sample['id']} 成功\")\n        \n        # 计算 WER\n        wer_value = wer(sample[\"reference_text\"], result[\"asr_text\"])\n        result[\"wer\"] = wer_value\n        wer_list.append(wer_value)\n        \n        # 计算 BERTScore（使用本地 modelscope 加载的模型）\n        try:\n            score = compute_bert_score(\n                result[\"llm_answer\"], \n                sample[\"reference_answer\"],\n                evaluator.bert_model,\n                evaluator.bert_tokenizer\n            )\n            result[\"bert_score\"] = score\n            bert_scores.append(score)\n        except Exception as e:\n            print(f\"⚠️ BERTScore计算失败: {e}\")\n            result[\"bert_score\"] = None\n        \n        e2e_latencies.append(result[\"total_time\"])\n        \n        for stage, duration in result[\"stages\"].items():\n            stage_times[stage].append(duration)\n        \n        all_results.append(result)\n    \n    wer_by_noise = {}\n    wer_by_accent = {}\n    wer_by_speed = {}\n    baseline_wer = None\n    \n    for r in all_results:\n        if r[\"status\"] != \"success\":\n            continue\n        noise = r.get(\"noise_type\", \"clean\")\n        wer_by_noise.setdefault(noise, []).append(r[\"wer\"])\n        accent = r.get(\"speaker_accent\", \"standard\")\n        wer_by_accent.setdefault(accent, []).append(r[\"wer\"])\n        speed = str(r.get(\"speed_factor\", 1.0))\n        wer_by_speed.setdefault(speed, []).append(r[\"wer\"])\n        \n        if noise == \"clean\" and accent == \"standard\" and r.get(\"speed_factor\", 1.0) == 1.0:\n            baseline_wer = r[\"wer\"]\n    \n    robustness_results = {}\n    for noise, wers in wer_by_noise.items():\n        if noise == \"clean\":\n            continue\n        mean_wer = np.mean(wers)\n        robustness_results[f\"noise_{noise}\"] = {\n            \"mean_wer\": mean_wer,\n            \"delta_from_baseline\": mean_wer - baseline_wer if baseline_wer is not None else None,\n            \"count\": len(wers)\n        }\n    for accent, wers in wer_by_accent.items():\n        if accent == \"standard\":\n            continue\n        mean_wer = np.mean(wers)\n        robustness_results[f\"accent_{accent}\"] = {\n            \"mean_wer\": mean_wer,\n            \"delta_from_baseline\": mean_wer - baseline_wer if baseline_wer is not None else None,\n            \"count\": len(wers)\n        }\n    for speed, wers in wer_by_speed.items():\n        if speed == \"1.0\":\n            continue\n        mean_wer = np.mean(wers)\n        robustness_results[f\"speed_{speed}x\"] = {\n            \"mean_wer\": mean_wer,\n            \"delta_from_baseline\": mean_wer - baseline_wer if baseline_wer is not None else None,\n            \"count\": len(wers)\n        }\n    \n    summary = {\n        \"total_samples\": total,\n        \"success_count\": len([r for r in all_results if r[\"status\"] == \"success\"]),\n        \"error_count\": len(errors),\n        \"errors\": errors,\n        \"wer\": {\n            \"mean\": np.mean(wer_list) if wer_list else None,\n            \"std\": np.std(wer_list) if wer_list else None,\n            \"min\": np.min(wer_list) if wer_list else None,\n            \"max\": np.max(wer_list) if wer_list else None\n        },\n        \"bert_score\": {\n            \"mean\": np.mean([s for s in bert_scores if s is not None]) if bert_scores else None,\n            \"std\": np.std([s for s in bert_scores if s is not None]) if bert_scores else None,\n            \"min\": np.min([s for s in bert_scores if s is not None]) if bert_scores else None,\n            \"max\": np.max([s for s in bert_scores if s is not None]) if bert_scores else None\n        },\n        \"e2e_latency_ms\": {\n            \"mean\": np.mean(e2e_latencies) if e2e_latencies else None,\n            \"p50\": np.percentile(e2e_latencies, 50) if e2e_latencies else None,\n            \"p90\": np.percentile(e2e_latencies, 90) if e2e_latencies else None,\n            \"p99\": np.percentile(e2e_latencies, 99) if e2e_latencies else None\n        },\n        \"stage_times_ms\": {\n            stage: {\n                \"mean\": np.mean(times),\n                \"std\": np.std(times),\n                \"p50\": np.percentile(times, 50),\n                \"p90\": np.percentile(times, 90),\n                \"p99\": np.percentile(times, 99)\n            }\n            for stage, times in stage_times.items()\n        },\n        \"robustness\": robustness_results,\n        \"details\": all_results\n    }\n    \n    return summary\n\n# ============================================================\n# 在 Jupyter Notebook 中直接运行\n# ============================================================\n\nprint(\"🚀 加载模型...\")\n\n# 1. 加载 VAD\n# vad = StreamingVAD(mode=3, silence_seconds=1.0)\n\n# # 2. 加载 ASR\n# asr = ASRService()\n\n# # 3. 加载 LLM\n# llm = LLMService()\n\n# # 4. 加载 TTS\ntts = TTSService()\n\n# 5. 加载 BERT 模型（使用 modelscope）\nprint(\"📚 加载 BERT 模型...\")\nbert_model_name = \"google-bert/bert-base-chinese\"\nbert_tokenizer = AutoTokenizer.from_pretrained(bert_model_name)\nbert_model = AutoModel.from_pretrained(bert_model_name)\nbert_model.eval()\nprint(\"✅ BERT 模型加载完成\")\n\nprint(\"✅ 所有模型加载完成\")\n\n# 创建评测执行器\nevaluator = AudioEvaluator(vad, asr, llm, tts, bert_model, bert_tokenizer)\n\n# 设置测试集路径\nTEST_SET_PATH = \"/mnt/workspace/test.json\"\n\n# 加载测试集\nprint(f\"📂 加载测试集: {TEST_SET_PATH}\")\nwith open(TEST_SET_PATH, 'r', encoding='utf-8') as f:\n    test_set = json.load(f)\n\n# 执行评测\nprint(f\"开始评测 {len(test_set['samples'])} 条样本...\")\nsummary = run_batch_evaluation(TEST_SET_PATH, evaluator)\n\n# 打印结果摘要\nprint(\"\\n\" + \"=\" * 60)\nprint(\"自动化评测结果摘要\")\nprint(\"=\" * 60)\nprint(f\"样本总数: {summary['total_samples']}\")\nprint(f\"成功数: {summary['success_count']} | 失败数: {summary['error_count']}\")\n\nif summary[\"wer\"][\"mean\"] is not None:\n    print(f\"\\n📊 ASR WER: {summary['wer']['mean']*100:.2f}% ± {summary['wer']['std']*100:.2f}%\")\n\nif summary[\"bert_score\"][\"mean\"] is not None:\n    print(f\"📊 LLM BERTScore: {summary['bert_score']['mean']:.3f} ± {summary['bert_score']['std']:.3f}\")\n\nif summary[\"e2e_latency_ms\"][\"mean\"] is not None:\n    print(f\"⏱️  端到端耗时: {summary['e2e_latency_ms']['mean']:.1f} ms (P50: {summary['e2e_latency_ms']['p50']:.1f} ms, P90: {summary['e2e_latency_ms']['p90']:.1f} ms)\")\n\nprint(\"\\n⏱️  各阶段耗时 (均值 ± 标准差):\")\nfor stage, stats in summary[\"stage_times_ms\"].items():\n    print(f\"  {stage.upper()}: {stats['mean']:.1f} ± {stats['std']:.1f} ms\")\n\nif summary[\"robustness\"]:\n    print(\"\\n🛡️  鲁棒性 (各条件下WER劣化):\")\n    for cond, stats in summary[\"robustness\"].items():\n        if stats[\"delta_from_baseline\"] is not None:\n            print(f\"  {cond}: +{stats['delta_from_baseline']*100:.2f}% (WER={stats['mean_wer']*100:.2f}%)\")\n\nif summary[\"errors\"]:\n    print(\"\\n❌ 失败样本详情:\")\n    for err in summary[\"errors\"]:\n        print(f\"  - {err['id']}: {err['status']}\")\n        if err.get(\"error_detail\"):\n            detail = err[\"error_detail\"][:500]\n            print(f\"    详情: {detail}...\")\n\nprint(\"=\" * 60)\n\n# 保存报告到文件\nwith open(\"evaluation_result.json\", \"w\", encoding=\"utf-8\") as f:\n    json.dump(summary, f, ensure_ascii=False, indent=2)\nprint(\"📊 报告已保存至 evaluation_result.json\")</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c18-ecd830fca53415.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>本章代码及相关文件见：https://www.modelscope.cn/gallery/liucong/59e8531d-ec1b-4a83-bf3e-fe463e3fdfa2</p>\n","imageCount":21,"attachmentCount":4,"discussionId":"chapter-18"},{"id":"chapter-19","number":19,"title":"实现一个企业知识问答助手","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-19.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-19.html","minutes":23,"status":"ready","headings":[{"id":"c19-s1","text":"RAG，让模型先查资料再开口","level":"h2"},{"id":"c19-s2","text":"知识经常更新，什么时候适合用 RAG？","level":"h2"},{"id":"c19-s3","text":"该补知识，还是该微调模型？","level":"h2"},{"id":"c19-s4","text":"从一份文档开始，搭起问答系统","level":"h2"},{"id":"c19-s5","text":"先把 PDF、Word 里的内容读出来","level":"h3"},{"id":"c19-s6","text":"文档太长，怎么切成合适的片段？","level":"h3"},{"id":"c19-s7","text":"用 Embedding，把文档变成可检索的向量","level":"h3"},{"id":"c19-s8","text":"Embedding 模型","level":"h4"},{"id":"c19-s9","text":"向量检索工具","level":"h4"},{"id":"c19-s10","text":"用 Reranker，把更相关的内容排到前面","level":"h3"},{"id":"c19-s11","text":"先召回再重排，实际找一次答案","level":"h3"},{"id":"c19-s12","text":"资料找到了，怎样让模型据此回答？","level":"h3"},{"id":"c19-s13","text":"构建 LLM 参考内容","level":"h4"},{"id":"c19-s14","text":"返回答案及来源","level":"h4"},{"id":"c19-s15","text":"没答好，问题出在检索还是生成？","level":"h2"}],"html":"<p>通用大模型虽然具备较强的知识问答能力，但在企业实际应用中，很多问题涉及企业内部的产品资料、技术文档、业务制度和操作手册等内容。这些知识通常没有包含在模型的训练数据中，而且还会随着业务不断更新。</p>\n\n<h2 id=\"c19-s1\">RAG，让模型先查资料再开口</h2>\n\n<p>RAG（Retrieval-Augmented Generation，检索增强生成）提供了一种解决方案，在大模型回答问题之前，先从企业知识库中检索与问题相关的内容，再将检索结果提供给大模型生成答案。</p>\n\n<p>RAG 的核心思想可以概括为：先检索，再回答。传统的大模型问答主要依赖模型参数中已经学习到的知识。例如，当用户提出一个问题时，模型会根据训练阶段学习到的信息直接生成答案。RAG 在这个过程中增加了一个知识检索步骤。当用户提出问题后，系统首先从外部知识库中查找与问题相关的内容，然后将用户问题和检索到的知识一起发送给大模型，由模型结合这些内容生成最终答案。</p>\n\n<p>一个基本的 RAG 流程可以表示为：用户问题 → 知识检索 → 获取相关内容 → LLM 生成答案 → 返回结果。RAG 并不是让模型重新学习这些知识，而是在模型回答问题时，临时为模型提供当前问题所需要的参考资料。</p>\n\n<h2 id=\"c19-s2\">知识经常更新，什么时候适合用 RAG？</h2>\n\n<p>RAG 并不是所有大模型应用都必须使用，但对于企业知识问答、智能客服和文档助手等场景非常常见。</p>\n\n<p>如果业务知识需要持续更新，通常比较适合使用 RAG。例如企业的产品说明、业务制度和操作规范可能会不断调整。如果将这些知识通过模型训练的方式写入模型，每次知识发生变化都重新训练模型，成本较高，也不方便维护。</p>\n\n<p>RAG 可以直接更新外部知识库。当文档发生变化后，只需要重新处理相关文档并更新索引，不需要重新训练整个大模型。如果回答需要提供来源依据，RAG 也比较适合。由于模型的回答是基于检索到的文档生成的，因此可以同时返回文档名称、章节、页码或者原文片段，让用户知道答案来自哪里。</p>\n\n<h2 id=\"c19-s3\">该补知识，还是该微调模型？</h2>\n\n<p>RAG 和模型微调都可以用于提升大模型在实际业务中的表现，但两者解决问题的思路不同。RAG 主要解决“模型缺少相关知识”的问题，而模型微调主要解决“模型不能按照要求完成任务”的问题。</p>\n\n<p>RAG 并不会修改模型本身的参数，而是在模型回答问题之前，先从外部知识库中检索与问题相关的内容，再将这些内容提供给模型作为回答依据。</p>\n\n<p>模型微调的方式则不同。微调需要使用特定的训练数据进一步训练模型，并在训练过程中调整模型参数，使模型逐渐学习特定任务的处理方式。例如第九章中提到的实体识别任务，需要模型输出固定格式的回答。当任务要求发生较大变化时，通常需要准备新的训练数据并重新进行微调。</p>\n\n<p>在实际应用中，可以根据需要解决的问题选择不同的方法：需要补充或更新知识时，更适合使用 RAG；需要调整模型的任务能力、输出格式或行为方式时，更适合使用模型微调。RAG 和模型微调两者可以结合使用。例如，可以通过微调让模型掌握问答或指令遵循能力，再通过 RAG 为模型提供领域内的知识，使模型既能够按照业务要求完成任务，又能够基于最新的领域知识生成回答。</p>\n\n<h2 id=\"c19-s4\">从一份文档开始，搭起问答系统</h2>\n\n<p>一个完整的 RAG 系统通常可以分为两个主要阶段：知识库构建和在线问答。知识库构建主要负责将 PDF、Word、Markdown、网页等原始资料处理成可以被检索的知识。在线问答则是在用户提出问题之后，从已经建立的知识库中寻找相关内容，本次实验采用网上公开的《中华人民共和国道路交通安全法实施条例》作为文档库来源，搭建关于交通安全法问答的 RAG 系统。</p>\n\n<h3 id=\"c19-s5\">先把 PDF、Word 里的内容读出来</h3>\n\n<p>企业知识通常分散在 PDF、Word、Excel、Markdown、网页等不同类型的文件中。由于不同文件的格式和内部结构存在较大差异，在构建 RAG 知识库之前，通常需要先将这些文档转换为统一、可处理的文本或结构化数据，这一过程称为文档解析。对于 RAG 系统而言，一个较好的文档解析工具不仅只是识别文本中的文字，还需要尽可能识别和保留原始文档中的结构信息，例如：文档标题、文档段落、表格、图片、公式等等。</p>\n\n<p>目前已经有许多开源工具可以用于文档解析，比较常见的包括：</p>\n\n<p><b>1）</b>MinerU 是一个面向复杂文档的开源解析工具，可以将 PDF、图片、Word、PPT、Excel 等文档转换为 Markdown、JSON 等机器可读格式。它能够识别标题、正文、表格、图片、公式等内容，并尽可能按照人类阅读顺序还原文档结构，因此比较适合作为 RAG 系统中的文档预处理工具，<a href=\"https://github.com/opendatalab/MinerU?utm_source=chatgpt.com\" target=\"_blank\" rel=\"noopener noreferrer\">opendatalab/MinerU</a></p>\n\n<p><b>2）</b>MonkeyOCR 是一个基于多模态模型的文档解析项目，通过结构识别、内容识别和关系建模对文档进行解析，能够处理文本、表格、公式等复杂内容，并支持中文和英文文档。对于版式比较复杂、传统文本提取工具效果不理想的 PDF，可以考虑使用这类基于视觉模型的解析方法，<a href=\"https://github.com/Yuliang-Liu/MonkeyOCR?utm_source=chatgpt.com\" target=\"_blank\" rel=\"noopener noreferrer\">Yuliang-Liu/MonkeyOCR</a></p>\n\n<p><b>3）</b>Dolphin 是字节跳动开源的文档图像解析模型，采用“先分析、再解析”的方式处理文档。它能够先识别页面布局和阅读顺序，再进一步解析文本、表格、公式、代码等不同类型的文档元素，适合处理布局复杂的 PDF 或扫描类文档，<a href=\"https://github.com/ByteDance/Dolphin?utm_source=chatgpt.com\" target=\"_blank\" rel=\"noopener noreferrer\">ByteDance/Dolphin</a></p>\n\n<p><b>4）</b>PaddleOCR 是 PaddlePaddle 开源的 OCR 与文档解析工具。除了常见的文字识别之外，还提供版面分析、表格识别以及文档结构化解析等能力，可以将 PDF 和图片中的内容转换为更适合后续 AI 系统处理的结构化数据。对于扫描 PDF、图片型文档以及包含大量中文文字的文档，PaddleOCR 是一种常见的选择，<a href=\"https://github.com/PaddlePaddle/PaddleOCR?utm_source=chatgpt.com\" target=\"_blank\" rel=\"noopener noreferrer\">PaddlePaddle/PaddleOCR</a></p>\n\n<p>不同文档解析工具各有特点，并不存在一种工具能够适用于所有文档。在实际构建 RAG 系统时，可以根据文档的类型、版式复杂程度、解析准确率以及部署成本等因素选择合适的解析工具。</p>\n\n<p>本次实验采用 MinerU 进行文档解析，首先在 ModelScope 的 Notebook 环境中安装 MinerU，执行命令如下：</p>\n\n<pre data-language=\"Plain Text\"><code>pip install -U \"mineru[all]\"</code></pre>\n\n<img src=\"assets/manuscript-20260914/c19-4634a78ab4ee88.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>安装完成后，可以直接对文档解析，输入需要解析的文件路径，执行命令如下：</p>\n\n<pre data-language=\"Plain Text\"><code>mineru -p /mnt/workspace/RAG/data -o /mnt/workspace/RAG/output </code></pre>\n\n<p>其中：-p 是输入文件路径，-o 是模型输出的文件路径，也就是解析后的文本。</p>\n\n<img src=\"assets/manuscript-20260914/c19-8d82808cdcf0d9.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>解析后的文件如下，里面包括了很多中间文件，可以按需选择，本次实验我们选择 md 文件作为文档源，里面包括了文本的段落格式信息。</p>\n\n<img src=\"assets/manuscript-20260914/c19-fa3ebd62d3431c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c19-s6\">文档太长，怎么切成合适的片段？</h3>\n\n<p>完成文档解析后，需要将较长的文档拆分成多个较小的文本片段，这些文本片段通常称为 Chunk。在 RAG 系统中，Embedding 和检索通常以 Chunk 为基本单位，因此合理的文档切分有助于提高后续检索的准确性。</p>\n\n<p>对于解析后的 Markdown 文档，可以先按照空行将内容拆分成段落，再按照设定的 <code>chunk_size</code> 将相邻段落依次合并。当内容超过指定长度时，则生成一个新的 Chunk。</p>\n\n<p>为了避免切分位置造成上下文信息丢失，还可以设置 <code>chunk_overlap</code>，让相邻 Chunk 之间保留少量重复内容。例如：chunk_size = 500，chunk_overlap = 50，表示每个 Chunk 控制在 500 个字符左右，相邻 Chunk 保留约 50 个字符的重叠内容。切分完成后，可以为每个 Chunk 保存 <code>id</code>、<code>content</code> 和 <code>file</code> 等信息，方便后续进行向量化、检索以及答案来源定位。</p>\n\n<p>下面是切分 chunk 的代码</p>\n\n<pre data-language=\"Python\"><code>import os\nimport json\ndef load_markdown(file_path):\n    with open(file_path, \"r\", encoding=\"utf-8\") as f:\n        return f.read()\n\ndef split_markdown(text, chunk_size=500, chunk_overlap=50):\n    \"\"\"\n    将 Markdown 文本切分成多个 Chunk\n    chunk_size:每个 Chunk 大约包含多少个字符\n    chunk_overlap:相邻 Chunk 之间保留多少个重复字符\n    \"\"\"\n    paragraphs = text.split(\"\\n\\n\")\n\n    chunks = []\n    current_chunk = \"\"\n\n    for paragraph in paragraphs:\n        paragraph = paragraph.strip()\n        if not paragraph:\n            continue\n        if len(current_chunk) + len(paragraph) &lt;= chunk_size:\n            if current_chunk:\n                current_chunk += \"\\n\\n\" + paragraph\n            else:\n                current_chunk = paragraph\n\n        else:\n\n            if current_chunk:\n                chunks.append(current_chunk)\n            overlap_text = current_chunk[-chunk_overlap:] if current_chunk else \"\"\n\n            current_chunk = overlap_text + \"\\n\\n\" + paragraph\n    if current_chunk:\n        chunks.append(current_chunk)\n\n    return chunks\n\ndef save_chunks(chunks, source_file, output_file):\n    \"\"\"\n    将 Chunk 保存为 JSONL 文件\n    \"\"\"\n    file_name = os.path.basename(source_file)\n\n    with open(output_file, \"w\", encoding=\"utf-8\") as f:\n        for i, chunk in enumerate(chunks):\n            data = {\"id\": i,\"content\": chunk,\"file\": file_name\n            }\n\n            f.write(\n                json.dumps(data, ensure_ascii=False) + \"\\n\"\n            )\n\nif __name__ == \"__main__\":\n\n    file_path = \"./output/中华人民共和国道路交通安全法实施条例/hybrid_auto/中华人民共和国道路交通安全法实施条例.md\"\n\n    text = load_markdown(file_path)\n    chunks = split_markdown(\n        text,\n        chunk_size=500,\n        chunk_overlap=50\n    )\n    print(\"Chunk 数量：\", len(chunks))\n    save_chunks(\n        chunks,\n        source_file=file_path,\n        output_file=\"output/chunks.jsonl\"\n    )\n</code></pre>\n\n<p>执行完之后，可以看到刚才解析的 md 文件中，共切分出 39 个 chunk。</p>\n\n<img src=\"assets/manuscript-20260914/c19-c4e1759f0c6f34.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c19-s7\">用 Embedding，把文档变成可检索的向量</h3>\n\n<h4 id=\"c19-s8\">Embedding 模型</h4>\n\n<p>完成文档切分后，需要进一步构建可检索的文档库。由于计算机无法直接根据自然语言的语义进行检索，因此需要使用 Embedding 模型将每个 Chunk 转换为向量表示。Embedding 模型可以将文本映射到高维向量空间，语义越相近的文本，其向量在空间中的距离也越接近。因此，在用户提出问题后，可以将问题同样转换为向量，再与文档库中的 Chunk 向量进行比较，从而检索出与问题语义最相关的内容。</p>\n\n<p>目前开源社区已经提供了多种 Embedding 模型，比如 BGE-M3 和 Qwen3-Embedding。BGE-M3 是 BAAI 推出的多语言 Embedding 模型，支持 100 多种语言以及最长 8192 Token 的文本输入。与普通的 Dense Embedding 模型相比，BGE-M3 同时支持稠密检索、稀疏检索和 Multi-Vector 检索，因此既可以用于常见的向量语义检索，也可以用于混合检索等场景 <a href=\"https://github.com/FlagOpen/FlagEmbedding\" target=\"_blank\" rel=\"noopener noreferrer\">FlagEmbedding</a>。</p>\n\n<p>Qwen3-Embedding 是 Qwen 团队推出的文本向量模型系列，基于 Qwen3 架构构建，主要面向文本检索、文本聚类、文本分类和代码检索等任务。Qwen3-Embedding 提供 0.6B、4B 和 8B 等不同参数规模，可以根据模型效果和计算资源进行选择，同时具有较好的多语言和长文本处理能力 <a href=\"https://github.com/QwenLM/Qwen3-Embedding\" target=\"_blank\" rel=\"noopener noreferrer\">Qwen3-Embedding</a>。</p>\n\n<p>本节采用 Qwen3-Embedding-0.6B 作为 Embedding 模型进行实验，第九章已经介绍过 ms-swift，除了大模型训练和微调之外，ms-swift 也支持 Qwen3-Embedding 系列的训练和推理。因此，本节继续使用 ms-swift 启动 Qwen3-Embedding-0.6B，并通过接口的方式完成文档 Chunk 和用户问题的向量化。</p>\n\n<p>可以使用如下命令启动 Embedding 服务：</p>\n\n<pre data-language=\"Plain Text\"><code>CUDA_VISIBLE_DEVICES=0 \\\nswift deploy \\\n    --model Qwen/Qwen3-Embedding-0.6B \\\n    --task_type embedding \\\n    --vllm_gpu_memory_utilization 0.2 \\\n    --vllm_max_model_len 512 \\\n    --host 0.0.0.0 \\\n    --port 18000</code></pre>\n\n<p>服务启动后，显示如下信息：</p>\n\n<img src=\"assets/manuscript-20260914/c19-1ea5a862527b52.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h4 id=\"c19-s9\">向量检索工具</h4>\n\n<p>目前常用的向量检索工具包括 Faiss 和 Milvus。Faiss 是 Meta 开源的向量相似度检索库，主要用于高效地进行向量索引和近邻搜索。它部署简单，不需要单独启动数据库服务，可以直接在 Python 程序中使用，比较适合学习、实验以及中小规模 RAG 系统。Milvus 是面向大规模向量数据的向量数据库，除了向量检索之外，还提供数据持久化、分布式存储、索引管理和多种查询能力，更适合数据规模较大、需要长期运行和工程化部署的场景。相比 Faiss，Milvus 的功能更加完整，但部署和使用过程也相对复杂。</p>\n\n<p>本实验采用 Faiss 进行向量存储和相似度检索，首先安装 faiss 库，执行命令如下：</p>\n\n<pre data-language=\"Plain Text\"><code>pip install faiss-cpu</code></pre>\n\n<img src=\"assets/manuscript-20260914/c19-c07144937fca5a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>安装完成后，可以将上一节生成的 Chunk 依次发送给 Embedding 接口，获得对应的向量，并将向量与 Chunk 的 <code>id</code>、<code>content</code>、<code>file</code> 等信息建立对应关系。为了后续能够快速完成相似度计算和知识检索。</p>\n\n<p>创建索引代码 build_index.py 如下：</p>\n\n<pre data-language=\"Python\"><code>import json\nimport requests\nimport numpy as np\nimport faiss\n\nEMBEDDING_URL = \"http://localhost:18000/v1/embeddings\"\nMODEL_NAME = \"Qwen3-Embedding-0.6B\"\n\ndef get_embedding(text):\n    payload = {\"model\": MODEL_NAME,\"input\": text}\n    response = requests.post(\n        EMBEDDING_URL,\n        json=payload,\n        timeout=60\n    )\n    response.raise_for_status()\n    data = response.json()\n    embedding = data[\"data\"][0][\"embedding\"]\n    return np.array(embedding, dtype=\"float32\")\n\ndef load_chunks(file_path):\n    chunks = []\n    with open(file_path, \"r\", encoding=\"utf-8\") as f:\n        for line in f:\n            chunks.append(json.loads(line))\n    return chunks\ndef build_faiss_index(chunks,index_path=\"faiss.index\", metadata_path=\"metadata.json\"):\n\n    embeddings = []\n    for i, chunk in enumerate(chunks):\n        text = chunk[\"content\"]\n        embedding = get_embedding(text)\n        embeddings.append(embedding)\n        print(f\"已处理 {i + 1}/{len(chunks)}\")\n    embeddings = np.array(embeddings, dtype=\"float32\")\n    faiss.normalize_L2(embeddings)\n    dimension = embeddings.shape[1]\n    index = faiss.IndexFlatIP(dimension)\n    index.add(embeddings)\n    print(\"向量数量:\", index.ntotal)\n    print(\"向量维度:\", dimension)\n    faiss.write_index(index, index_path)\n    with open(metadata_path, \"w\", encoding=\"utf-8\") as f:\n        json.dump(chunks,f,ensure_ascii=False,indent=2\n        )\n    print(f\"Faiss 索引已保存到: {index_path}\")\n    print(f\"Chunk 信息已保存到: {metadata_path}\")\n\nif __name__ == \"__main__\":\n\n    chunks = load_chunks(\"output/chunks.jsonl\")\n\n    build_faiss_index(\n        chunks,\n        index_path=\"output/faiss.index\",\n        metadata_path=\"output/metadata.json\"\n    )</code></pre>\n\n<p>执行后输出对应的索引文件以及向量</p>\n\n<img src=\"assets/manuscript-20260914/c19-ebb9afa3f554f7.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c19-s10\">用 Reranker，把更相关的内容排到前面</h3>\n\n<p>Embedding 模型能够将用户问题和文档 Chunk 分别转换为向量，并通过向量之间的距离或相似度进行检索。这种方式的优势是：文档向量可以提前计算并存储，在用户提问时只需要计算一次问题向量，就能够快速从大量文档中召回相关内容。但是，这种高效的检索方式也存在一定局限。Embedding 模型将 Query 和 Chunk 分别进行编码，在计算相关性时，比较两个向量在向量空间中的相似程度。Reranker 模型是交互式匹配。通过将 Query 和候选 Chunk 同时输入模型，让模型直接分析两段文本之间的相关关系，因此能够进行更加细致的语义匹配。</p>\n\n<p>因此，在 RAG 系统中通常采用 Embedding 召回 + Reranker 重排的两阶段检索方式：首先利用 Embedding 从大量 Chunk 中快速筛选出一批候选结果，再利用 Reranker 对少量候选结果进行更加精细的相关性判断和重新排序。这样既能够保证检索效率，又能够进一步提高最终提供给大语言模型的上下文质量。</p>\n\n<p>本节采用 Qwen3-Reranker-0.6B 作为重排模型，可以使用 ms-swift 启动服务：</p>\n\n<pre data-language=\"Plain Text\"><code>CUDA_VISIBLE_DEVICES=0 \\\nswift deploy \\\n    --model Qwen/Qwen3-Reranker-0.6B \\\n    --task_type generative_reranker \\\n    --infer_backend transformers \\\n    --host 0.0.0.0 \\\n    --port 18001</code></pre>\n\n<img src=\"assets/manuscript-20260914/c19-59e65688b71b86.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>服务启动后，可以将用户问题和 Embedding 检索得到的候选 Chunk 输入 Reranker，根据模型计算得到的相关性分数重新排序，并选择排名靠前的 Chunk 作为后续大语言模型生成答案的参考内容。</p>\n\n<h3 id=\"c19-s11\">先召回再重排，实际找一次答案</h3>\n\n<p>前面经完成了 Embedding 服务、Reranker 服务以及 Faiss 向量索引的构建。在此基础上，按照提到的采用 Embedding 召回 + Reranker 重排的两阶段检索方式筛选出候选 Chunk。</p>\n\n<p>第一阶段使用 Faiss 从知识库中进行向量检索，首先获取相似度排名前 10 的候选 Chunk,下面是针对问题：初次申领机动车号牌、行驶证应向哪个部门申请注册登记？向量检索 top10 结果。</p>\n\n<pre data-language=\"Plain Text\"><code>\nimport json\nimport faiss\nfrom build_index import get_embedding\ndef load_faiss_index(index_path=\"faiss.index\",\n                     metadata_path=\"metadata.json\"):\n\n    index = faiss.read_index(index_path)\n\n    with open(metadata_path, \"r\", encoding=\"utf-8\") as f:\n        metadata = json.load(f)\n\n    return index, metadata\ndef search(query,index,metadata,top_k=10):\n\n    query_embedding = get_embedding(query)\n    query_embedding = query_embedding.reshape(1, -1)\n    faiss.normalize_L2(query_embedding)\n    scores, indices = index.search(query_embedding,top_k)\n    results = []\n    for score, idx in zip(scores[0], indices[0]):\n        if idx == -1:\n            continue\n        chunk = metadata[idx]\n        results.append({\n            \"score\": float(score),\n            \"id\": chunk.get(\"id\"),\n            \"content\": chunk.get(\"content\"),\n            \"file\": chunk.get(\"file\")\n        })\n\n    return results\nindex, metadata = load_faiss_index(\n        \"output/faiss.index\",\n        \"output/metadata.json\"\n    )\nquery=\"初次申领机动车号牌、行驶证应向哪个部门申请注册登记？\"\ncandidates = search(\n    query=query,\n    index=index,\n    metadata=metadata,\n    top_k=10\n)\n\nprint(\"candidates\",candidates)</code></pre>\n\n<img src=\"assets/manuscript-20260914/c19-ef8b80aa906e3a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>第二阶段使用 Reranker 对候选结果进行重排序。将用户问题和召回的候选 Chunk 提交给 Reranker，重新计算两者之间的相关性得分，并按照得分从高到低进行排序。</p>\n\n<pre data-language=\"Plain Text\"><code>def rerank(query, candidates):\n    results = []\n    for candidate in candidates:\n        response = rerank_client.chat.completions.create(\n            model=\"Qwen3-Reranker-0.6B\",\n            messages=[\n                {\n                    \"role\": \"user\",\n                    \"content\": query\n                },\n                {\n                    \"role\": \"assistant\",\n                    \"content\": candidate[\"content\"]\n                }\n            ]\n        )\n\n        score = response.choices[0].message.content[0]\n        item = candidate.copy()\n        item[\"rerank_score\"] = float(score)\n        results.append(item)\n    results.sort(\n        key=lambda x: x[\"rerank_score\"],\n        reverse=True\n    )\n    for rank, item in enumerate(results, start=1):\n        item[\"rerank_rank\"] = rank\n\n    return results</code></pre>\n\n<p>下面是针对问题：初次申领机动车号牌、行驶证应向哪个部门申请注册登记？经过 Reranker 模型重新排序后的结果：</p>\n\n<img src=\"assets/manuscript-20260914/c19-9d5052bb89590b.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>从上面两个结果可以看出，经过重排序后的 Chunk,排序明显发生了变化，在实际应用中，在实际项目中，可以根据知识库规模、Chunk 长度、问题复杂程度以及实际检索效果进行调整。例如，对于答案分布在多个文档片段中的问题，可以适当增加最终保留的 Chunk 数量；也可以结合 Reranker 得分设置相关性阈值，进一步过滤相关性较低的内容。除了上述方式，实际 RAG 系统还可以采用混合检索（Hybrid Search），将向量检索与 BM25 等关键词检索方式结合，对多路召回结果进行融合后再进行重排序。</p>\n\n<h3 id=\"c19-s12\">资料找到了，怎样让模型据此回答？</h3>\n\n<p>完成检索和重排序后，已经获得了与用户问题相关性较高的若干个 Chunk。接下来需要将这些内容作为参考资料，与用户问题一起提交给 LLM，如果相关性满足要求，则将检索内容提交给大语言模型生成答案；如果知识库中没有找到足够相关的内容，则直接返回拒答结果，避免模型在缺少依据的情况下生成答案。</p>\n\n<p>首先，在 Notebook 中启动大模型服务，本次实验采用 Qwen3-4B 模型，启动方式如下：</p>\n\n<pre data-language=\"SQL\"><code>CUDA_VISIBLE_DEVICES=0 swift deploy \\\n  --model Qwen/Qwen3-4B \\\n  --load_args false \\\n  --infer_backend vllm \\\n  --enable_thinking false \\\n  --host 0.0.0.0 \\\n  --port 18002 \\\n  --api_key 123 \\\n  --vllm_gpu_memory_utilization 0.8 \\\n  --vllm_max_model_len 8000 \\\n  --max_new_tokens 2000</code></pre>\n\n<p>启动后显示如下，说明模型启动成功，服务启动后，即可通过 <code>18002</code> 端口调用 Qwen3-4B。</p>\n\n<img src=\"assets/manuscript-20260914/c19-d73faa5ebd537f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h4 id=\"c19-s13\">构建 LLM 参考内容</h4>\n\n<p>Reranker 会根据用户问题与候选 Chunk 的相关程度重新调整排序。本实验直接选择排名前 5 的 Chunk 作为 LLM 的参考资料，为了使模型尽量依据知识库内容回答，可以在 Prompt 中明确限定回答范围。同时，当提供的参考资料无法回答用户问题时，要求模型直接拒答，而不是利用自身知识补充答案，prompt 如下：</p>\n\n<pre data-language=\"Plain Text\"><code>prompt = f\"\"\"\n请根据下面的参考资料回答用户问题。\n\n参考资料：\n{context}\n\n用户问题：\n{query}\n\n要求：\n1. 仅根据参考资料回答问题；\n2. 回答准确、简洁；\n3. 不要补充参考资料中不存在的信息；\n4. 如果参考资料无法回答该问题，请回答：\n   “根据当前知识库暂时无法回答该问题。”\n\"\"\"</code></pre>\n\n<h4 id=\"c19-s14\">返回答案及来源</h4>\n\n<p>大模型生成答案后，除了向用户返回最终回答，还可以同时返回本次回答所参考的文档来源。对于企业知识问答场景，来源信息可以帮助用户了解答案来自哪些知识库文档，在需要进一步确认时，也可以回到原始文档中查看相关内容。来源信息不需要由大模型生成，而是直接从检索结果的元数据中获取。前面构建知识库时，每个 Chunk 都保留了对应的 <code>file</code> 字段，因此可以从 Reranker 排名前 5 的 Chunk 中提取文件名称，并对重复的文件进行去重。</p>\n\n<p>下面是调用大模型实现问答的核心代码：</p>\n\n<pre data-language=\"Python\"><code>llm_client = OpenAI(\n    api_key=\"123\",\n    base_url=\"http://127.0.0.1:18002/v1\"\n)\nLLM_MODEL = \"Qwen3-4B\"\n\ndef generate_answer(query, top_chunks):\n\n    context = \"\\n\\n\".join(\n        [\n            f\"[参考资料{i + 1}]\\n{item['content']}\"\n            for i, item in enumerate(top_chunks)\n        ]\n    )\n    prompt = f\"\"\"\n请根据下面提供的参考资料回答用户问题。\n\n参考资料：\n{context}\n\n用户问题：\n{query}\n\n要求：\n1. 仅根据参考资料中的内容回答问题；\n2. 回答应准确、简洁，不要补充参考资料中不存在的信息；\n3. 如果参考资料中没有与问题相关的答案，请回答：\n   “根据当前知识库暂时无法回答该问题。”\n\"\"\"\n    response = llm_client.chat.completions.create(\n        model=LLM_MODEL,\n        messages=[{\"role\": \"user\",\"content\": prompt }],\n        temperature=0.1,\n        max_tokens=512\n    )\n\n    return response.choices[0].message.content</code></pre>\n\n<p>至此，一个企业问答助手已经搭建完成，下面我们可以在终端中进行简单测试，测试结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c19-15eb250aad89d0.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h2 id=\"c19-s15\">没答好，问题出在检索还是生成？</h2>\n\n<p>完成 RAG 系统后，还需要通过评测判断整个问答流程是否真正有效。与普通大模型问答不同，RAG 的结果同时受到<b>知识检索</b>和<b>答案生成</b>两个阶段的影响。因此，RAG 评测通常也需要从这两个方面分别进行。</p>\n\n<p>1） 检索效果评测</p>\n\n<p>检索阶段的评测标准是用户的问题是否可以在召回的 Chunk 中找到答案，常用指标为 Recall@K。其中，Recall@K 是比较直观的指标，用于衡量前 K 个检索结果覆盖了多少相关文档：</p>\n\n<p>Recall@K：正确的文档是否出现在前 K 个检索结果中。</p>\n\n<p><span class=\"arithmatex display-math\">\\(Recall@K =\n\\frac{\\text{Top K 中检索到的相关文档数量}}\n{\\text{所有相关文档数量}}\n\\)</span></p>\n\n<p>例如，一个问题对应 2 个正确的知识片段，在 Top 5 检索结果中找到了其中 2 个，则 Recall@5 为 100%。</p>\n\n<p>2） 生成效果评测</p>\n\n<p>检索到相关知识并不代表最终答案一定正确，还需要进一步评价 LLM 的生成结果，生成阶段可以重点关注答案正确性、完整性、相关性、引用准确性以及幻觉，此部分可参考第十二章中的 12.3 生成任务评测指标，通过这些指标，可以判断模型是否能够准确利用检索到的知识生成答案，并进一步发现答案遗漏、内容偏差或无依据生成等问题。在实际应用中，如果检索结果已经较为准确，但生成效果仍然无法满足要求，可以考虑选择能力更强、参数规模更大的模型作为 RAG 的生成模型，以提升复杂问题的理解和回答能力。</p>\n\n<p></p>\n\n<p>此章节涉及到的实验数据以及实验代码，可参考：https://modelscope.cn/gallery/liucong/564dcc20-ccfc-4d5a-bb12-20386d9d932f</p>\n","imageCount":12,"attachmentCount":0,"discussionId":"chapter-19"},{"id":"chapter-20","number":20,"title":"场景有千千万，欢迎你来共建出你的场景","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-20.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-20.html","minutes":1,"status":"ready","headings":[],"html":"<p>邀请千行百业来分享自己的落地教程，但要注意场景需要可复现、是真实的落地实践</p>\n\n<p></p>\n","imageCount":0,"attachmentCount":0,"discussionId":"chapter-32"},{"id":"chapter-21","number":21,"title":"10 个 Case，带你看看 AIGC 的开源模型能做到什么地步","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-21.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-21.html","minutes":7,"status":"ready","headings":[{"id":"c21-s1","text":"先介绍一下这两个模型","level":"h2"},{"id":"c21-s2","text":"Z-Image，已经不只是把脸画得好看","level":"h2"},{"id":"c21-s3","text":"MiniMax H3，开源视频开始有导演感了","level":"h2"}],"html":"<p>现在最强的生图、生视频模型应该是 GPT Images 2.5 和 Seedance 2.5 了，生图、生视频的上限又被往前推了一截。</p>\n\n<p>那如果把它们当成闭源模型的第一梯队，那开源模型现在到底是什么水平？是玩具吗？是否能真正带来生产力？</p>\n\n<p>这篇直接看看大佬们，能把开源模型玩到什么地步，其中生图选择阿里的 Z-Image，生视频选择 MiniMax H3。</p>\n\n<p></p>\n\n<h2 id=\"c21-s1\"><b>先介绍一下这两个模型</b></h2>\n\n<p>Z-Image 是做的 6B 生图模型，采用 Apache 2.0 许可证。完整的 Z-Image 是非蒸馏底模，支持 CFG、负面提示词和微调；社区里更常见的是 Z-Image-Turbo，只用 8 步，追求的就是一个快。</p>\n\n<p>MiniMax H3 则是 33B 的音视频生成模型，可以输出 4 到 15 秒、24fps、带立体声音频的视频。严格来说，它属于开放权重模型，使用的是 MiniMax H3 Community License。开源出来的 H3-Base 可以在本地生成 768p 音视频，但官方那套负责理解复杂输入的 Context-IR，以及 2K 再生成流程，没有完整开源。</p>\n\n<p></p>\n\n<h2 id=\"c21-s2\"><b>Z-Image，已经不只是把脸画得好看</b></h2>\n\n<p>现在很多生图模型都能生成美女，单看一张精修脸其实没啥意思。真正难的是：文字写对没有？数量数对没有？同一件商品能不能在不同视图里保持一致？复杂的空间关系有没有理解正确？</p>\n\n<p></p>\n\n<p>先看 @witcheer 做的一组本地测试。</p>\n\n<img src=\"assets/manuscript-20260914/c20-4e303a2853b092.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>他在 RTX 5090 上跑 Z-Image-Turbo，1024px 图片大约只要 3.2 秒一张。提示词里要求商店招牌写出指定单词，再画出“正好三只鸭子”，结果文字和数量都对了。</p>\n\n<p>这两个任务看着幼稚，其实一直是扩散模型的老大难。字母容易糊，三只鸭子经常咔咔咔变成四五只。</p>\n\n<img src=\"assets/manuscript-20260914/c20-c027c81c79257c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<img src=\"assets/manuscript-20260914/c20-372f0213a6907c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>原帖：https://x.com/witcheer/status/2074020722972758139</p>\n\n<p></p>\n\n<p>接着是一个更接近电商交付的 case。</p>\n\n<p>@rizavelioglu 让模型生成一张左右分栏图：左边只放一件白色花纹 T 恤，右边让真人模特穿上同一件衣服。提示词还要求保留印花、文字、面料、缝线和版型。</p>\n\n<p>最后出来的图里，两边的衣服没有各画各的，花纹和款式基本能对上。对于电商团队来说，这比单纯生成一张“漂亮模特图”值钱多了。商品图、上身图、营销图可以从同一个设计继续往下做。</p>\n\n<p>当然，这只是一张成功样本，离稳定批量出图还有距离。但它至少把方向跑通了。</p>\n\n<img src=\"assets/manuscript-20260914/c20-2778aa92ca4f07.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>原帖：https://x.com/rizavelioglu/status/2000012841525649621</p>\n\n<p></p>\n\n<p>写实人像这块，@dreamydigiarts 做了一组同提示词对比。</p>\n\n<p>他让 Z-Image-Turbo 和 Nano Banana 2 同时生成一张手机仰拍：蓝天、逆光、人物后仰、手里抱着一大束野花，头发被风吹起来，还要求保留手机照片那种轻微颗粒感。</p>\n\n<p>Z-Image 给出的画面已经有很强的随手拍味道。牛仔裤、皮肤和逆光没有糊成一层塑料，仰拍构图也没问题。拿来做人物氛围图、社媒配图，已经完全不是玩具级别。</p>\n\n<img src=\"assets/manuscript-20260914/c20-1692e5595374ae.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<img src=\"assets/manuscript-20260914/c20-7b130b1305b0e0.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>原帖：https://x.com/dreamydigiarts/status/2084233075245171142</p>\n\n<p></p>\n\n<p>Z-Image 也不只会写实，@tisch_eins 用同一条提示词测试了 Boogu、Flux 2 Klein 和 Z-Image-Turbo。画面要求一个黑发女术士站在暴风雨山顶，手里的金色法杖必须从手到顶端完整出现，一道闪电从杖尖直冲云层，同时还要有低机位、全身构图、披风、符文、电弧和强烈的金色轮廓光。</p>\n\n<p>这种 prompt 很容易顾此失彼：法杖被裁掉，闪电接错位置，人物也可能只剩一个大头。Z-Image 的结果把主要关系都保持住了，说明它对复杂插画构图也较强的控制力。</p>\n\n<img src=\"assets/manuscript-20260914/c20-f95dd32bae9f1e.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>原帖：https://x.com/tisch_eins/status/2081490372405174375</p>\n\n<p></p>\n\n<p>最后一个是极端微距，@aisthetiic 的提示词很短：让某种动物的瞳孔占据画面主体，用左上方的戏剧光线照出虹膜纹理，背景做成由暗到亮的虚化渐变。</p>\n\n<p>更有意思的是，画面没有散。视线被牢牢锁定在瞳孔上，光线、阴影和背景都在给同一个主体服务。做海报主视觉或者情绪化封面时，这种构图服从度比堆一万句“8K、杰作、超细节”都有用。</p>\n\n<img src=\"assets/manuscript-20260914/c20-b5f58339b18a57.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>原帖：https://x.com/aisthetiic/status/1994424107451007336</p>\n\n<p></p>\n\n<p>这 5 个 case 放在一起看，Z-Image 的优势还是很明显了：</p>\n\n<p>模型不大、速度快、写实能力在线，而且能听懂比较长的自然语言。完整底模还可以继续做 LoRA、ControlNet 和行业微调。</p>\n\n<p></p>\n\n<h2 id=\"c21-s3\"><b>MiniMax H3，开源视频开始有导演感了</b></h2>\n\n<p>生视频比生图难得多，一张图里画错一根手指，有时候还能裁掉。视频里角色 15 秒不换脸、镜头按时间走、台词不串人、音效跟动作对上，任何一项掉链子，整条片子就废了。</p>\n\n<p></p>\n\n<p>H3 的 case 里，最值得先看的就是双角色一致性。</p>\n\n<p>@coolthor 给 H3 的 Ref2VA 模式喂了两张 Z-Image 生成的角色设定图。一个穿赭色长袍，一个穿蓝灰色衣服，外形故意拉开差异。提示词安排其中一名角色在第 6 到 8 秒入场，还写了三句中文对白。</p>\n\n<p>10.125 秒的视频里，两个人没有互相“串脸”“串衣服”。该入场的角色在前半段没出现，到 7 秒左右才进画面。作者又拿 ASR 对 44 个汉字做了核对，字符错误率是 6.8%，错的还是同音字。</p>\n\n<p>这条是在单张 RTX 5090 上跑的，243 帧用了 437 秒。速度谈不上非常快，但角色、时间和对白可以一起控制，已经很能打了。</p>\n\n<img src=\"assets/manuscript-20260914/c20-21399ea076ac36.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<img src=\"assets/manuscript-20260914/c20-66554ca06322ab.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<figure><a href=\"assets/manuscript-20260914/c20-7d9146c1b9d02e.mp4\" class=\"attachment\" download=\"案例6-H3双角色中文对白-完整10秒.mp4\">案例 6-H3 双角色中文对白-完整 10 秒.mp4</a></figure>\n\n<p>原帖：https://x.com/coolthor/status/2096779996320719307</p>\n\n<p></p>\n\n<p>另一条本地 case 来自 @Lumosous。</p>\n\n<p>他用 RTX 4090 和 ComfyUI 做了三种尝试：四场景旅行短片、跟着鼓点切换画面的音乐视频，以及带 4 个镜头和环境声的海边故事。原始想法先交给 MiniMax 官方的 Prompt Writing Skill，整理成带镜头、时间、动作和声音的结构化提示词，再丢给 H3。</p>\n\n<p>这条最有用的地方，是它不只把画面做得“挺漂亮”。鼓点到了，画面真会跟着换；海边那条里，四个镜头、氛围和背景音能在一轮里一起出来。</p>\n\n<p>他的实测数据：4090 跑 15 秒、20 步的低分辨率视频要 200 多秒；升到 768p 就要 1000 多秒。开源可以让你反复改，但免费不等于没有成本，电费、显存和等待时间也是成本。</p>\n\n<img src=\"assets/manuscript-20260914/c20-145acd757537f0.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<figure><a href=\"assets/manuscript-20260914/c20-6bd384c463328e.mp4\" class=\"attachment\" download=\"案例7-H3本地工作流-演示节选22秒.mp4\">案例 7-H3 本地工作流-演示节选 22 秒.mp4</a></figure>\n\n<p>原帖：https://x.com/Lumosous/status/2089351551361937490</p>\n\n<p></p>\n\n<p>@PixelAigc 做的 30 秒《简·爱》式英伦庄园片段更夸张。</p>\n\n<p>他的提示词直接把 30 秒拆成 4 个镜头。每段写清焦段、机位、人物站位、微表情、对白、呼吸、环境音和禁止项。人物情绪从试探、反驳一路走到动容，男女声线也分别约束。</p>\n\n<p>这条片子用的是本地 ComfyUI、H3 Turbo LoRA，先出 480p，再用 Topaz 超到 1080p。作者给出的时间是，25 秒大约要 13 到 15 分钟。它不能代表官方原版模型的裸跑效果，但能代表开源生态最有意思的地方：模型出来一个月，社区已经开始改速度、做长视频、接自动超分了。</p>\n\n<img src=\"assets/manuscript-20260914/c20-2fe3efcf3dbfde.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<figure><a href=\"assets/manuscript-20260914/c20-5139f6a635a7e1.mp4\" class=\"attachment\" download=\"案例8-H3英伦庄园多镜头对白-完整30秒.mp4\">案例 8-H3 英伦庄园多镜头对白-完整 30 秒.mp4</a></figure>\n\n<p>原帖：https://x.com/PixelAigc/status/2093563293306929579</p>\n\n<p></p>\n\n<p>H3 官方单段上限是 15 秒，那更长的视频怎么做？</p>\n\n<p>@superalesha 用 4 张 RTX 3090 跑了一条 30 秒第一人称动作片。他把两个 15 秒片段串起来，前一段故意停在一个稳定画面，后一段从同一帧接着生成，再把重复帧剪掉，声音继续往后走。</p>\n\n<p>成片的接缝藏在第 15 秒，正常看很难发现。整条片从生成到完成用了 44 分钟。这个 case 的价值不在于 H3 突然突破了时长限制，而是有人把限制研究明白以后，用工作流绕过去了。</p>\n\n<img src=\"assets/manuscript-20260914/c20-2e30c4ea95fddb.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<figure><a href=\"assets/manuscript-20260914/c20-2b841bfdc3eae3.mp4\" class=\"attachment\" download=\"案例9-H3双段串联第一人称动作片-完整30秒.mp4\">案例 9-H3 双段串联第一人称动作片-完整 30 秒.mp4</a></figure>\n\n<p>原帖：https://x.com/superalesha/status/2086171185134686509</p>\n\n<p></p>\n\n<p>还有一个特别适合拿来研究“视频提示词到底该怎么写”的 case。</p>\n\n<p>@ou_zhen599 用本地 ComfyUI 做了一段 15 秒赛博飞船短片。画面里有三名女性角色，一人一直坐在左边，一人站在中间拿刀，一人站在右前方拿汽水罐。提示词不只写剧情，还规定谁在第几秒说话、没说话的人不能动嘴、汽水罐必须一直留在右手、红色追踪点什么时候出现在屏幕上，最后舷窗外的阴影再压过来。</p>\n\n<p>这种 case 最难的是空间别乱、道具别丢、台词别串、沉默的人真能闭嘴。H3 已经能把这么多约束塞进一条 15 秒短片里执行，开源视频的玩法明显变了。</p>\n\n<img src=\"assets/manuscript-20260914/c20-58907f0c3e3aa4.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<figure><a href=\"assets/manuscript-20260914/c20-7b0dbb64916194.mp4\" class=\"attachment\" download=\"案例10-H3赛博飞船多角色对白-完整15秒.mp4\">案例 10-H3 赛博飞船多角色对白-完整 15 秒.mp4</a></figure>\n\n<p>原帖：https://x.com/ou_zhen599/status/2097988690102390893</p>\n\n<p></p>\n\n<p></p>\n\n<p>总的来看，</p>\n\n<p>如果你只是想最快出一条成片，闭源模型依然省事。</p>\n\n<p>但如果你想反复试、批量生成、训练自己的风格，或者把能力装进本地工作流，Z-Image 和 H3 已经值得认真研究了。先用开源模型把镜头和素材咔咔咔试出来，真正卡住的时候再调用闭源模型，成本会低很多。</p>\n\n<p>开源模型以前给人的感觉是等等党平替，现在它已经能参与真正的创作了。</p>\n\n<p>对天天要做图、做视频的人来说，最爽的变化就是，以后每冒出一个想法，不用先算这一轮要烧掉多少积分。</p>\n\n<p></p>\n","imageCount":14,"attachmentCount":5,"discussionId":"chapter-20"},{"id":"chapter-22","number":22,"title":"定制？使用 DiffSynth 训练图像 LoRA","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-22.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-22.html","minutes":12,"status":"ready","headings":[{"id":"c22-s1","text":"一句提示词，怎样变成一张图片？","level":"h2"},{"id":"c22-s2","text":"想让模型画得更像，图像 LoRA 能做什么？","level":"h2"},{"id":"c22-s3","text":"训练之前，先明确想让模型学什么","level":"h2"},{"id":"c22-s4","text":"风格 LoRA","level":"h3"},{"id":"c22-s5","text":"人物 LoRA","level":"h3"},{"id":"c22-s6","text":"商品 LoRA","level":"h3"},{"id":"c22-s7","text":"概念 LoRA","level":"h3"},{"id":"c22-s8","text":"从训练到出图，DiffSynth 能帮我们做什么？","level":"h2"},{"id":"c22-s9","text":"用一组柯基图片，动手训练 LoRA","level":"h2"},{"id":"c22-s10","text":"准备环境","level":"h3"},{"id":"c22-s11","text":"选择模型","level":"h3"},{"id":"c22-s12","text":"准备训练数据","level":"h3"},{"id":"c22-s13","text":"配置训练参数","level":"h3"},{"id":"c22-s14","text":"启动 LoRA 训练","level":"h3"},{"id":"c22-s15","text":"使用 LoRA 生成图片","level":"h3"}],"html":"<p>在实际应用中，通用图像生成模型不一定能够准确生成指定的人物、商品或者画面风格。如果希望模型能生成特定的人物、商品或风格特征，可以使用 LoRA 进行训练。本章首先介绍图像生成模型和 LoRA 的基本概念，再使用 DiffSynth 完成一次图像 LoRA 训练。</p>\n\n<h2 id=\"c22-s1\">一句提示词，怎样变成一张图片？</h2>\n\n<p>目前常见的图像生成模型主要由文本编码器、扩散模型和 VAE 等组件组成。用户输入提示词后，模型先理解文字内容，再逐步生成图像特征，最后将这些特征转换为可以直接查看的图片。</p>\n\n<p>文本编码器主要负责理解提示词，将文字转换为模型能够处理的文本特征。例如，输入“一只戴着墨镜的橘猫坐在沙滩上”，文本编码器会对“橘猫”“墨镜”“沙滩”等内容进行编码，为后续图像生成提供条件。</p>\n\n<p>扩散模型是生成图像的核心组件。它从随机噪声开始，根据文本特征经过多次计算逐渐去除噪声，形成画面中的物体、颜色和结构。例如，在生成上述图片时，最开始的画面只是一片随机噪声，经过多轮处理后，橘猫、墨镜和沙滩等内容会逐渐形成。</p>\n\n<p>VAE（Variational Autoencoder，变分自编码器）负责在图像和特征空间之间进行转换。为了减少计算量，扩散模型通常不会直接在高分辨率图片上进行计算，而是在压缩后的特征空间中生成图像。生成完成后，再通过 VAE 将图像特征还原为最终图片。</p>\n\n<p>将这些组件组合起来，一次基本的文生图过程如下所示：</p>\n\n<img src=\"assets/manuscript-20260914/c21-f0637e13299fd7.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>用户输入提示词后，模型依次经过文本编码、图像特征生成和图像还原，最终得到图片。不同图像生成模型在具体结构上会有所不同，但整体都需要完成从文本输入到图像输出的转换过程。</p>\n\n<h2 id=\"c22-s2\">想让模型画得更像，图像 LoRA 能做什么？</h2>\n\n<p>通用图像生成模型通常是使用大量图片训练得到的，能够生成多种类型的内容，但对于特定人物、商品或画面风格，生成效果可能不够准确。</p>\n\n<p>例如，希望模型生成某个指定商品时，即使在提示词中详细描述商品的颜色、形状和外观，生成结果也可能与真实商品存在较大差异。</p>\n\n<p>可以通过训练让模型学习这些新的视觉特征，但重新训练整个模型需要大量训练数据和计算资源。对于这类特定特征的学习，可以采用参数高效训练方法 LoRA，具体原理可见本文 9.2 节。训练时冻结原模型的大部分参数，只训练新增的少量参数，使模型学习新的特征。</p>\n\n<p>训练图像 LoRA 只需要准备少量目标图片，就可以让模型学习特定的人物、商品、风格或其他视觉特征。训练完成后得到的 LoRA 文件比完整模型小很多。使用时加载原始模型，再加载对应的 LoRA，就可以使用新学习的视觉特征。</p>\n\n<p></p>\n\n<h2 id=\"c22-s3\">训练之前，先明确想让模型学什么</h2>\n\n<p>根据学习目标不同，图像 LoRA 可以分为风格 LoRA、人物 LoRA、商品 LoRA 和概念 LoRA 等类型。</p>\n\n<h3 id=\"c22-s4\">风格 LoRA</h3>\n\n<p>风格 LoRA 学习的是画面的整体视觉风格：色彩、线条、构图、表现方式。训练时需要准备一组相似画风的图片，图片中的内容可以不同，但整体风格应尽量保持一致。</p>\n\n<p>例如：使用一组水墨画画风的图片训练风格 LoRA，可以让模型生成具有水墨画风格的人物、动物或风景图片。</p>\n\n<p>训练集示例图：</p>\n\n<img src=\"assets/manuscript-20260914/c21-ce6de2b2e02e44.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>测试提示词：一只坐在竹林里吃竹子的熊猫。</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col></colgroup><tbody><tr><td><p>未加载 LoRA 结果：</p></td><td><p>加载风格 LoRA 结果：</p></td></tr><tr><td><img src=\"assets/manuscript-20260914/c21-3fd3a32c64192e.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></td><td><img src=\"assets/manuscript-20260914/c21-5d3e6ea0689d4c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></td></tr></tbody></table>\n</div>\n<p>可以看到，加载风格 LoRA 后，画面转换为水墨画风格。</p>\n\n<h3 id=\"c22-s5\">人物 LoRA</h3>\n\n<p>人物 LoRA 主要学习特定人物的外观特征，包括脸部、发型和整体形象等。训练需要同一个人物不同角度、表情、背景和服装的图片，让模型学习人物相对稳定的外观特征。训练完成后，可以通过提示词让这个人物以不同的状态出现在办公室、街道或雪山等不同场景中。</p>\n\n<p>样例：提供一个男生的多张生活照，让模型学习他的外观特征。</p>\n\n<p>训练集示例图：</p>\n\n<img src=\"assets/manuscript-20260914/c21-0358bf22af1ebf.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>测试提示词：一个男生坐在地铁里玩手机。</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col></colgroup><tbody><tr><td><p>未加载 LoRA 结果：</p></td><td><p>加载人物 LoRA 结果：</p></td></tr><tr><td><img src=\"assets/manuscript-20260914/c21-86fa3181793ea9.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></td><td><img src=\"assets/manuscript-20260914/c21-050a6a06b15640.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></td></tr></tbody></table>\n</div>\n<p>加载人物 LoRA 后，模型可以保持人物的脸部、发型等外观特征稳定。</p>\n\n<h3 id=\"c22-s6\">商品 LoRA</h3>\n\n<p>商品 LoRA 主要学习特定商品的外观特征，例如包装、形状、颜色和标志性结构。训练时可以准备同一商品在不同角度、背景和光照条件下的图片，让模型学习商品相对固定的外观。训练完成后，可以将该商品应用到不同展示场景中，生成商品展示图、电商素材和广告图片。</p>\n\n<p>样例：准备待宣传运动鞋的一组图片，让模型学习该商品特征，生成广告图。</p>\n\n<p>训练集示例图：</p>\n\n<img src=\"assets/manuscript-20260914/c21-394431c612a4d1.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>测试提示词：一双白色运动鞋放置在现代城市街头的混凝土地面上。清晨阳光从侧面照射，背景为模糊的现代建筑和城市道路，运动品牌广告摄影风格，低机位拍摄，浅景深。</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col></colgroup><tbody><tr><td><p>未加载 LoRA 结果：</p></td><td><p>加载商品 LoRA 结果：</p></td></tr><tr><td><img src=\"assets/manuscript-20260914/c21-9586bc196eadee.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></td><td><img src=\"assets/manuscript-20260914/c21-5411cf6bf8b9c9.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></td></tr></tbody></table>\n</div>\n<p>加载商品 LoRA 后，生成结果保持了运动鞋的外观特征。</p>\n\n<h3 id=\"c22-s7\">概念 LoRA</h3>\n\n<p>概念 LoRA 学习的是一类抽象视觉概念或组合特征，例如未来城市、机械风建筑、科幻元素或特殊服饰元素。与人物 LoRA 和商品 LoRA 不同，概念 LoRA 学习的不是某个具体对象，而是多个对象共同具有的视觉特征。训练时需要准备一组具有相似概念的图片，图片中的具体内容可以不同，但整体概念特征需尽量保持一致。</p>\n\n<p>样例：准备一组未来科技城市图片，让模型学习未来感建筑、智能设备和科幻视觉元素。</p>\n\n<p>训练集示例图：</p>\n\n<img src=\"assets/manuscript-20260914/c21-1d1c74d25e4af1.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>测试提示词：一座未来科技城市，宏伟的悬浮建筑和空中交通，飞行汽车在空中穿梭，巨大的环形轨道和全息广告，蓝天白云，电影级构图，充满未来感的科幻氛围。</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col></colgroup><tbody><tr><td><p>未加载 LoRA 结果：</p></td><td><p>加载概念 LoRA 结果：</p></td></tr><tr><td><img src=\"assets/manuscript-20260914/c21-50abbbbdc3040e.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></td><td><img src=\"assets/manuscript-20260914/c21-c587b8f2fac6ff.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></td></tr></tbody></table>\n</div>\n<p>加载概念 LoRA 后，生成结果体现了未来科技城市的视觉元素。</p>\n\n<h2 id=\"c22-s8\">从训练到出图，DiffSynth 能帮我们做什么？</h2>\n\n<p>DiffSynth 是一个面向图像和视频生成、编辑与训练的开源框架。</p>\n\n<p>在进行图像 LoRA 训练时，需要完成基础模型加载、训练数据读取、训练参数配置、LoRA 训练和模型保存等工作。DiffSynth 对这些过程进行了封装，并针对不同的图像生成模型提供了相应的训练脚本。使用时只需要按照模型要求准备训练数据并配置相关参数，就可以启动 LoRA 训练。</p>\n\n<p>DiffSynth 还提供图像生成 Pipeline。训练完成后，可以直接加载基础模型和训练得到的 LoRA 权重，通过提示词生成图片，测试 LoRA 是否学习到了目标特征。从训练到测试可以在同一个框架中完成。</p>\n\n<p>使用 DiffSynth 训练图像 LoRA 的基本过程如下所示：</p>\n\n<img src=\"assets/manuscript-20260914/c21-6601e8dc60e5d6.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>其中，基础模型决定模型原本具备的图像生成能力，训练数据决定 LoRA 学习的内容，而训练参数会影响最终的训练效果。</p>\n\n<h2 id=\"c22-s9\">用一组柯基图片，动手训练 LoRA</h2>\n\n<p>下面使用 DiffSynth 官方提供的样例数据集完成一次图像 LoRA 训练。</p>\n\n<h3 id=\"c22-s10\">准备环境</h3>\n\n<p>需要下载并安装 DiffSynth-Studio。安装命令如下：</p>\n\n<pre data-language=\"Plain Text\"><code>!git clone https://github.com/modelscope/DiffSynth-Studio.git  \n%cd DiffSynth-Studio\n!pip3 install -e .</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c21-b55aa52f6df6f5.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c22-s11\">选择模型</h3>\n\n<p>LoRA 需要基于已有的图像生成模型进行训练，需要先选择基础模型。</p>\n\n<p>DiffSynth 支持多种图像生成模型，本节选择 <a href=\"https://www.modelscope.cn/models/Tongyi-MAI/Z-Image-Turbo\" target=\"_blank\" rel=\"noopener noreferrer\">Tongyi-MAI/Z-Image-Turbo</a> 作为基础模型。该模型参数量较小，需要的显存和计算资源较少，在当前实验环境下可以比较方便快速地完成 LoRA 训练和图片生成测试。</p>\n\n<p>训练完成后，得到的 LoRA 权重需要与 Z-Image-Turbo 基础模型配合使用，不能单独用于图片生成。</p>\n\n<h3 id=\"c22-s12\">准备训练数据</h3>\n\n<p>本节使用 ModelScope 上的 <a href=\"https://www.modelscope.cn/datasets/DiffSynth-Studio/diffsynth_example_dataset\" target=\"_blank\" rel=\"noopener noreferrer\">DiffSynth-Studio/diffsynth_example_dataset</a> 样例数据集进行 LoRA 训练，可以通过以下命令下载：</p>\n\n<pre data-language=\"Bash\"><code>!modelscope download --dataset DiffSynth-Studio/diffsynth_example_dataset \\\n--include \"z_image/Z-Image-Turbo/*\" \\\n--local_dir ./data/diffsynth_example_dataset</code></pre>\n\n<p>下载完成后，样例数据会保存在<code>data/diffsynth_example_dataset/</code>。</p>\n\n<img src=\"assets/manuscript-20260914/c21-28a00ba2392742.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>DiffSynth 采用统一的数据集格式。一个基本的图像训练数据集通常由训练图片和元数据文件组成。元数据文件 metadata.csv 记录训练图片及其对应的提示词，其中 image 表示图片文件，prompt 表示图片对应的文本描述。训练时，DiffSynth 会根据这些信息读取图片和提示词。数据集结构如下所示：</p>\n\n<img src=\"assets/manuscript-20260914/c21-30072dfd2cc460.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>查看数据集可以发现，里面包含着同一只柯基犬在不同场景下的图片，下面展示其中一张训练图片。通过这些图片进行训练，可以让模型学习这只柯基犬的外观特征。</p>\n\n<img src=\"assets/manuscript-20260914/c21-2475b46a99ed23.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c22-s13\">配置训练参数</h3>\n\n<p>准备好模型和数据集后，需要配置 LoRA 训练参数。下面训练脚本中使用的主要参数如下：</p>\n\n<div class=\"table-scroll\"><table><tr><td>参数</td><td>作用</td></tr><tr><td>dataset_base_path</td><td>训练数据所在目录</td></tr><tr><td>dataset_metadata_path</td><td>数据集元数据文件路径</td></tr><tr><td>max_pixels</td><td>训练图片允许的最大像素数</td></tr><tr><td>dataset_repeat</td><td>每轮训练中数据集重复使用的次数</td></tr><tr><td>model_id_with_origin_paths</td><td>基础模型及其模型文件路径</td></tr><tr><td>learning_rate</td><td>学习率，控制每次训练时参数更新的幅度</td></tr><tr><td>num_epochs</td><td>训练轮数</td></tr><tr><td>output_path</td><td>LoRA 权重的保存目录</td></tr><tr><td>lora_base_model</td><td>指定 LoRA 训练的基础模块</td></tr><tr><td>lora_target_modules</td><td>指定需要添加 LoRA 的模型模块</td></tr><tr><td>lora_rank</td><td>LoRA 的 Rank，影响可训练参数量</td></tr><tr><td>use_gradient_checkpointing</td><td>启用 Gradient Checkpointing，减少训练时的显存占用</td></tr><tr><td>dataset_num_workers</td><td>加载训练数据时使用的进程数量</td></tr></table>\n</div>\n<p>训练时可以根据训练数据和 GPU 显存调整 learning_rate、num_epochs、dataset_repeat、lora_rank 和 max_pixels 等参数。</p>\n\n<h3 id=\"c22-s14\">启动 LoRA 训练</h3>\n\n<p>配置好训练参数后，可以使用<code>accelerate launch</code>启动 LoRA 训练。DiffSynth 针对不同模型提供了对应的训练脚本，本节使用 Z-Image-Turbo 的训练脚本，如下所示：</p>\n\n<pre data-language=\"SQL\"><code>!accelerate launch examples/z_image/model_training/train.py \\\n  --dataset_base_path data/diffsynth_example_dataset/z_image/Z-Image-Turbo \\\n  --dataset_metadata_path data/diffsynth_example_dataset/z_image/Z-Image-Turbo/metadata.csv \\\n  --max_pixels 1048576 \\\n  --dataset_repeat 50 \\\n  --model_id_with_origin_paths \"Tongyi-MAI/Z-Image-Turbo:transformer/*.safetensors,Tongyi-MAI/Z-Image-Turbo:text_encoder/*.safetensors,Tongyi-MAI/Z-Image-Turbo:vae/diffusion_pytorch_model.safetensors\" \\\n  --learning_rate 1e-4 \\\n  --num_epochs 2 \\\n  --remove_prefix_in_ckpt \"pipe.dit.\" \\\n  --output_path \"./models/train/Z-Image-Turbo_lora\" \\\n  --lora_base_model \"dit\" \\\n  --lora_target_modules \"to_q,to_k,to_v,to_out.0,w1,w2,w3\" \\\n  --lora_rank 32 \\\n  --use_gradient_checkpointing \\\n  --dataset_num_workers 8</code></pre>\n\n<p>训练日志展示：</p>\n\n<img src=\"assets/manuscript-20260914/c21-623521a08f9dc6.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>训练过程中，需要关注程序是否正常运行，以及显存占用和模型保存情况。</p>\n\n<p>如果训练过程中出现显存不足，可以适当降低 max_pixels 或 lora_rank 等参数。当前命令启用了 Gradient Checkpointing，可以减少训练时的显存占用。</p>\n\n<p>训练完成后，LoRA 权重会保存到 output_path 指定的目录中：</p>\n\n<img src=\"assets/manuscript-20260914/c21-84491df0a7f172.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>后续生成图片时，需要加载这里保存的 LoRA 权重。</p>\n\n<h3 id=\"c22-s15\">使用 LoRA 生成图片</h3>\n\n<p>完成训练后，需要加载基础模型和刚刚训练得到的 LoRA 进行图片生成。</p>\n\n<p>针对训练目标，让模型生成一张柯基戴着墨镜站在雪地上的图片，观察生成图片里的小狗是否和训练图片中一致。</p>\n\n<pre data-language=\"Python\"><code>from diffsynth.pipelines.z_image import ZImagePipeline, ModelConfig\nimport torch\n\npipe = ZImagePipeline.from_pretrained(\n    torch_dtype=torch.bfloat16,\n    device=\"cuda\",\n    model_configs=[\n        ModelConfig(model_id=\"Tongyi-MAI/Z-Image-Turbo\", origin_file_pattern=\"transformer/*.safetensors\"),\n        ModelConfig(model_id=\"Tongyi-MAI/Z-Image-Turbo\", origin_file_pattern=\"text_encoder/*.safetensors\"),\n        ModelConfig(model_id=\"Tongyi-MAI/Z-Image-Turbo\", origin_file_pattern=\"vae/diffusion_pytorch_model.safetensors\"),\n    ],\n    tokenizer_config=ModelConfig(model_id=\"Tongyi-MAI/Z-Image-Turbo\", origin_file_pattern=\"tokenizer/\"),\n)\npipe.load_lora(pipe.dit, \"./models/train/Z-Image-Turbo_lora/epoch-1.safetensors\")\nprompt = \"dog, corgi dog, standing in the snow with sunglasses.\"\nimage = pipe(prompt=prompt, seed=42, rand_device=\"cuda\")\nimage.save(\"image-dog.jpg\")\nprint(\"图片保存成功\")</code></pre>\n\n<p>结果展示：</p>\n\n<img src=\"assets/manuscript-20260914/c21-8170bdfd74ca38.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>生成图片如下：</p>\n\n<img src=\"assets/manuscript-20260914/c21-b3c54155322268.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>测试时可以修改提示词中的场景、构图或其他描述，多生成几张图片进行比较。如果生成结果能够体现训练数据中的目标特征，又可以根据提示词改变场景和构图，说明 LoRA 已经学习到了相应特征。</p>\n\n<p></p>\n\n<p>本章代码及相关文件见：https://www.modelscope.cn/gallery/liucong/ba42f266-8a77-447c-aba3-b6d90a391791</p>\n\n<p></p>\n","imageCount":22,"attachmentCount":0,"discussionId":"chapter-21"},{"id":"chapter-23","number":23,"title":"一张商品营销图，从生成到修改怎么做？","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-23.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-23.html","minutes":21,"status":"ready","headings":[{"id":"c23-s1","text":"先用文字描述，把营销创意画出来","level":"h2"},{"id":"c23-s2","text":"有了参考图，再加品牌和营销元素","level":"h2"},{"id":"c23-s3","text":"只想改一处，怎样尽量保住商品原样？","level":"h2"},{"id":"c23-s4","text":"竖版还要变横版，试试扩图和尺寸适配","level":"h2"},{"id":"c23-s5","text":"想让一组图片风格接近，再试试 LoRA","level":"h2"}],"html":"<p>过去制作一张商品宣传海报，通常需要经过素材准备、背景设计、商品排版、文案添加和尺寸调整等多个环节。如果同一款商品还需要投放到多个平台，设计人员往往还要针对不同尺寸重新调整画面。</p>\n\n<p>现在通过文生图、图生图、局部编辑和模型微调等技术，可以将创意生成、商品设计、图片修改和尺寸适配串联起来，形成一条相对完整的营销内容生产流程。</p>\n\n<p>例如，一款饮料需要制作“夏日营销”主题海报，希望画面中包含冰块、水花、柠檬和绿叶等元素，可以先通过文字描述快速生成初始营销画面，再结合商品图片进行图生图编辑，加入品牌 Logo，并根据实际需求继续修改局部元素。最后，还可以将同一张营销图片扩展为不同尺寸，用于社交媒体、短视频和网页广告等不同场景。</p>\n\n<p>本章将以 FRESH DAY 夏日饮料营销图为例，介绍如何基于 ModelScope 中的开源图像生成模型搭建一条简的 AIGC 营销内容生产线。实验从文生图开始，逐步介绍图生图、商品主体保持、局部编辑、尺寸适配以及品牌风格 LoRA 等内容。</p>\n\n<p></p>\n\n<h2 id=\"c23-s1\">先用文字描述，把营销创意画出来</h2>\n\n<p>文生图（Text-to-Image）是指根据文字描述直接生成图片。用户不需要准备完整的设计素材，只需要通过 Prompt 描述想要的画面内容，例如主题、主体、背景、色彩、构图和视觉风格等，模型就可以根据这些信息生成对应的图片。</p>\n\n<p>对于营销内容生产来说，文生图比较适合用于创意探索和初始视觉方案生成。例如，在制作夏日饮料海报之前，可以先告诉模型“清爽的夏日氛围、冰块、水花、柠檬、蓝绿色调、商业摄影风格”等要求，让模型快速生成一个完整的营销画面。相比从空白画布开始设计，这种方式能够更快地看到不同创意方案。</p>\n\n<p>目前开源社区已经提供了 Stable Diffusion、FLUX、Qwen-Image、Z-Image-Turbo 等多种图像生成模型。不同模型在画面质量、Prompt 理解、文字生成能力和硬件资源需求等方面存在差异</p>\n\n<p>本节选择 ModelScope 上的 Z-Image-Turbo 模型完成文生图实验。Turbo 版本重点优化了生成效率，可以通过较少的推理步数完成图片生成，因此比较适合个人开发者以及 GPU 资源有限的实验环境。</p>\n\n<p>1）安装环境</p>\n\n<p>我们使用 DiffSynth-Studio 框架进行文生图，下载 DiffSynth-Studio</p>\n\n<pre data-language=\"Plain Text\"><code>!git clone https://github.com/modelscope/DiffSynth-Studio.git  </code></pre>\n\n<img src=\"assets/manuscript-20260914/c22-e33b489045b898.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>2）进入 DiffSynth-Studio 目录</p>\n\n<pre data-language=\"Plain Text\"><code>%cd DiffSynth-Studio</code></pre>\n\n<img src=\"assets/manuscript-20260914/c22-9475639db44bdf.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>3) 安装</p>\n\n<pre data-language=\"Plain Text\"><code>%pip install -e .</code></pre>\n\n<img src=\"assets/manuscript-20260914/c22-e472c79cee1dbe.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>4) 在 Cell 单元中输入下面代码，进行文生图。</p>\n\n<pre data-language=\"Python\"><code>import torch\nfrom modelscope import ZImagePipeline\npipe = ZImagePipeline.from_pretrained(\n    \"Tongyi-MAI/Z-Image-Turbo\",\n    torch_dtype=torch.bfloat16,\n    low_cpu_mem_usage=False,\n)\npipe.to(\"cuda\")\nprint(f\"模型所在设备: {pipe.device}\")\n\nprompt = \"\"\" 夏日饮料商业宣传海报，\n 清爽明亮的夏日氛围， 背景包含冰块、水花、柠檬和绿色树叶， 蓝绿色清爽色调， 商业广告摄影风格， 画面中央预留饮料商品展示区域， 背景简洁，光影自然， 高质量，细节清晰。 \"\"\"\nimage = pipe(\n    prompt=prompt,\n    height=1024,\n    width=1024,\n    num_inference_steps=9, \n    guidance_scale=0.0, \n    generator=torch.Generator(\"cuda\").manual_seed(42),\n).images[0]\n\nprint(\"88888888888888888888888\")\n#display(image)\nimage.save(\"summer_drink_poster.png\")</code></pre>\n\n<p>执行完成后，可以在工作空间中看到刚保存的图片。</p>\n\n<img src=\"assets/manuscript-20260914/c22-875c2e48ff0c97.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>生成的图片效果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c22-9c80bf9a063d2d.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>对于初学者来说，不需要一开始就编写非常复杂的 Prompt。只要把需求描述清楚，再根据生成结果逐步增加细节即可。</p>\n\n<h2 id=\"c23-s2\">有了参考图，再加品牌和营销元素</h2>\n\n<p>文生图非常适合快速生成营销创意和背景方案，但它存在一个明显的问题：如果直接要求模型生成具体商品，商品包装、Logo 和文字可能与真实商品存在差异，在正式的商品营销场景中，还需要结合真实商品图片进一步处理。</p>\n\n<p>文生图主要依赖文字描述，而图生图（Image-to-Image）会同时输入参考图片和文字描述。模型可以参考原图中的商品主体、包装和构图，再根据 Prompt 对背景、光影和营销元素进行调整。</p>\n\n<p>本节选择 SenseNova-U1.5-8B-MoT 作为图像编辑模型。该模型支持文生图和图像编辑等任务，可以同时理解输入图片和文字指令，并根据 Prompt 对原图中的指定内容进行调整。相比仅根据文字生成图片，SenseNova-U1.5-8B-MoT 能够利用输入图片中的视觉信息，在保留原有主体和整体结构的基础上，对商品包装、背景、颜色和装饰元素等内容进行修改，因此比较适合商品营销图片的生成与编辑。</p>\n\n<p>实验部分继续使用上一节生成的夏日饮料图片作为输入，通过图生图模型对其进行编辑。在尽量保留饮料瓶主体和整体结构的基础上，为瓶身增加“FRESH DAY” 品牌 Logo，并加入冰块、水花、柠檬等夏日元素，最终生成一张更加完整的商品营销宣传图，默认的 SenseNova-U1.5-8B-MoT 加载方式，会导致显存不足，本次代码中采用低显存加载方式，从而降低显存占用。</p>\n\n<p>代码如下：</p>\n\n<pre data-language=\"Python\"><code>from diffsynth.pipelines.sensenova_u1_image import (\n    SenseNovaU1ImagePipeline,\n    ModelConfig\n)\nfrom PIL import Image\nimport torch\nimport os\n\nMODEL_ID = \"SenseNova/SenseNova-U1.5-8B-MoT\"\nINPUT_IMAGE = \"/mnt/workspace/summer_drink_poster.png\"\nOUTPUT_IMAGE = \"/mnt/workspace/drink_fresh_day_ad.png\"\n\nvram_config = {\n    \"offload_dtype\": \"disk\",\n    \"offload_device\": \"disk\",\n    \"onload_dtype\": \"disk\",\n    \"onload_device\": \"disk\",\n\n    # 真正需要计算时再以 BF16 加载到 GPU\n    \"preparing_dtype\": torch.bfloat16,\n    \"preparing_device\": \"cuda\",\n\n    \"computation_dtype\": torch.bfloat16,\n    \"computation_device\": \"cuda\",\n}\n\nprint(\"正在加载 SenseNova-U1.5-8B-MoT...\")\n\npipe = SenseNovaU1ImagePipeline.from_pretrained(\n    torch_dtype=torch.bfloat16,\n    device=\"cuda\",\n\n    model_configs=[\n        ModelConfig(\n            model_id=MODEL_ID,\n            origin_file_pattern=\"model*.safetensors\",\n            **vram_config\n        ),\n    ],\n\n    tokenizer_config=ModelConfig(\n        model_id=MODEL_ID,\n        origin_file_pattern=\"./\"\n    ),\n    vram_limit=(\n        torch.cuda.mem_get_info(\"cuda\")[1] / (1024 ** 3)\n        - 0.5\n    ),\n)\n\nprint(\"模型加载完成。\")\n\nif not os.path.exists(INPUT_IMAGE):\n    raise FileNotFoundError(\n        f\"找不到输入图片：{INPUT_IMAGE}\"\n    )\n\nedit_image = Image.open(INPUT_IMAGE).convert(\"RGB\")\n\nprint(\n    f\"输入图片尺寸：\"\n    f\"{edit_image.width} × {edit_image.height}\"\n)\n\nprompt = \"\"\"\n这是一项饮料商品图像编辑任务。\n\n请基于输入的饮料瓶商品图，生成一张专业、清爽的夏日饮料营销广告图。\n\n请重点保留原图中的饮料瓶主体。\n尽量保持饮料瓶原有的瓶身形状、瓶盖、包装结构、瓶身比例、\n材质质感和整体商品外观，不要重新生成或替换成另一款饮料瓶。\n饮料瓶应保持清晰完整，并作为画面的视觉中心。\n\n在饮料瓶正面的标签区域，为这款饮料增加一个新的品牌标识：\n“FRESH DAY”。\n\n“FRESH DAY”是该饮料的品牌名称。\n请将品牌 Logo 自然地设计在瓶身正面，使它看起来像原本就印刷\n在商品包装上的真实品牌标识，而不是悬浮在画面上的普通文字。\n\n品牌 Logo 采用简洁、现代、清新的视觉设计风格。\n使用清晰、醒目的英文字体，并搭配一个简约的小叶子图形，\n体现自然、健康、清爽和年轻化的品牌感觉。\n\n“FRESH DAY”文字应尽量保持完整、清晰、容易识别。\n不要生成其他品牌名称，不要添加无关文字，\n不要在瓶身之外重复出现“FRESH DAY”。\n\n在保留饮料瓶主体的基础上，对周围的营销场景进行优化。\n\n在饮料瓶周围加入少量透明冰块、清爽自然的水花、\n瓶身冷凝水珠以及少量新鲜柠檬片，\n突出饮料冰凉、清爽、新鲜的感觉。\n\n使用明亮自然的商业产品摄影光线，\n突出瓶身的透明感、冷凝水珠和产品质感。\n背景保持简洁、干净，不要加入过多复杂元素。\n\n整体画面采用清新、明亮、高级的夏日商业广告风格，\n主体突出，构图简洁，具有真实商业产品摄影的质感。\n\n最终效果应像一张真实饮料品牌发布的专业夏日营销广告，\n而不是插画、卡通或重新设计的饮料产品。\n\"\"\"\n\nprint(\"开始生成夏日饮料营销图...\")\n\nimage = pipe(\n    prompt=prompt,\n\n    # 原始商品图\n    edit_image=edit_image,\n\n    # 固定随机种子，方便实验复现\n    seed=42,\n\n    # 3:4 竖版营销图片\n    height=1536,\n    width=1152,\n\n    num_inference_steps=50,\n    cfg_scale=4.0,\n    shift=3.0,\n)\n\nimage.save(OUTPUT_IMAGE)\n\nprint(\n    f\"生成完成，图片已保存至：{OUTPUT_IMAGE}\"\n)</code></pre>\n\n<p>执行结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c22-0011e2ac818bb0.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>最终生成的效果图为：</p>\n\n<img src=\"assets/manuscript-20260914/c22-3dcd4196c2b35a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>相比完全从文字生成，图生图能够利用原始图片中的视觉信息，更适合已经拥有商品素材或参考设计稿的场景。从上面生成的图中也可以看出，这个和真实想要的图还是有差距的，即使提供了真实商品图片，模型也可能重新绘制商品的部分内容。在实际使场景中还需要进一步考虑商品主体保持与局部编辑问题。</p>\n\n<h2 id=\"c23-s3\">只想改一处，怎样尽量保住商品原样？</h2>\n\n<p>在商品营销图片的实际应用中，除了追求画面美观，还需要保证商品信息的准确性。饮料瓶的外观、包装结构、品牌 Logo 等内容通常属于商品的核心视觉信息，如果在图像编辑过程中发生明显变化，生成的图片就可能与实际商品不一致。在使用图像生成模型进行编辑时，需要明确区分需要保留的内容和允许修改的内容。例如，可以要求模型保持饮料瓶、品牌 Logo 和整体构图不变，只调整背景、装饰元素或局部物体。通过在 Prompt 中明确这些约束，可以减少模型对商品主体的重新生成，使编辑结果更加可控。</p>\n\n<p>本节继续使用上一节生成的 FRESH DAY 饮料营销图进行局部编辑。在保持饮料瓶主体、品牌 Logo、冰块、水花和整体构图基本不变的情况下，将画面右下角的黄色柠檬修改为绿色青柠。通过这一示例，可以进一步理解如何利用 Prompt 实现更加可控的图像编辑。</p>\n\n<pre data-language=\"Python\"><code>from diffsynth.pipelines.sensenova_u1_image import (\n    SenseNovaU1ImagePipeline,\n    ModelConfig\n)\nfrom PIL import Image\nimport torch\nimport os\n\nMODEL_ID = \"SenseNova/SenseNova-U1.5-8B-MoT\"\nINPUT_IMAGE = \"/mnt/workspace/drink_fresh_day_ad.png\"\nOUTPUT_IMAGE = \"/mnt/workspace/drink_fresh_day_lime.png\"\n\nvram_config = {\n    \"offload_dtype\": \"disk\",\n    \"offload_device\": \"disk\",\n    \"onload_dtype\": \"disk\",\n    \"onload_device\": \"disk\",\n\n    \"preparing_dtype\": torch.bfloat16,\n    \"preparing_device\": \"cuda\",\n\n    \"computation_dtype\": torch.bfloat16,\n    \"computation_device\": \"cuda\",\n}\n\nprint(\"正在加载模型...\")\n\npipe = SenseNovaU1ImagePipeline.from_pretrained(\n    torch_dtype=torch.bfloat16,\n    device=\"cuda\",\n\n    model_configs=[\n        ModelConfig(\n            model_id=MODEL_ID,\n            origin_file_pattern=\"model*.safetensors\",\n            **vram_config\n        ),\n    ],\n\n    tokenizer_config=ModelConfig(\n        model_id=MODEL_ID,\n        origin_file_pattern=\"./\"\n    ),\n\n    vram_limit=(\n        torch.cuda.mem_get_info(\"cuda\")[1] / (1024 ** 3)\n        - 0.5\n    ),\n)\n\nprint(\"模型加载完成。\")\n\nif not os.path.exists(INPUT_IMAGE):\n    raise FileNotFoundError(\n        f\"找不到输入图片：{INPUT_IMAGE}\"\n    )\n\nedit_image = Image.open(INPUT_IMAGE).convert(\"RGB\")\n\nprint(\n    f\"输入图片尺寸：\"\n    f\"{edit_image.width} × {edit_image.height}\"\n)\n\nprompt = \"\"\"\n这是一项局部图像编辑任务。\n\n请将图片右下角的黄色柠檬修改为新鲜的绿色青柠。\n\n保持原有水果的位置、大小、形状、切面结构和摆放角度基本不变，\n主要修改水果的颜色和外观，使黄色果皮和果肉自然变成绿色青柠。\n\n除右下角的柠檬外，不要修改图片中的其他内容。\n\n严格保持饮料瓶主体不变，包括瓶子的形状、瓶盖、瓶身颜色、\n包装结构和整体比例。\n\n保持瓶身上的“FRESH DAY”品牌 Logo、字体、位置和标签设计不变。\n\n保持原图中的冰块、水花、冷凝水珠、背景绿叶、\n光照效果、背景颜色以及整体构图不变。\n\n最终只改变右下角水果的视觉效果，\n使其从黄色柠檬自然变为绿色青柠，\n同时保持整张饮料营销图的原有风格和商业摄影质感。\n\"\"\"\n\nprint(\"开始执行局部图像编辑...\")\n\nimage = pipe(\n    prompt=prompt,\n    edit_image=edit_image,\n\n    seed=42,\n    height=1536,\n    width=1152,\n\n    num_inference_steps=50,\n    cfg_scale=4.0,\n    shift=3.0,\n)\n\nimage.save(OUTPUT_IMAGE)\n\nprint(\n    f\"局部编辑完成，图片已保存至：{OUTPUT_IMAGE}\"\n)</code></pre>\n\n<p>代码执行结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c22-d8b8569422064f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>生成的图片如下,可以看出黄色柠檬已经换成了绿色，但是其他部分也有变化，实际使用场景，我们可以通过修改提示词或者部署效果更好的模型来获得更好的图片生成效果。</p>\n\n<img src=\"assets/manuscript-20260914/c22-6d6c621af3511f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h2 id=\"c23-s4\">竖版还要变横版，试试扩图和尺寸适配</h2>\n\n<p>商品营销图片完成后，通常还需要发布到不同的平台。由于各平台的展示形式不同，对图片尺寸和画面比例的要求也有所差异。社交媒体常使用 1:1 或 4:5 的图片，短视频平台更适合 9:16 的竖版画面，而网页 Banner、横版广告等场景则通常采用 16:9 的比例。如果直接对原图进行缩放，可能会导致商品主体被拉伸或压缩，我们需要在保持商品主体不变的情况下，可以利用图像编辑模型对画面进行扩图，根据目标比例向图片四周补充背景和装饰内容。</p>\n\n<p>扩图与普通的图片缩放不同。它不是简单改变原图的长宽，而是根据原有画面的内容和风格生成新的区域。例如，将一张竖版饮料营销图转换为 16:9 横版时，可以保持饮料瓶的大小和比例基本不变，同时向左右扩展背景、水花、冰块和绿叶等内容，使新增区域与原图自然衔接。</p>\n\n<p>本部分仍然使用上一节编辑后的 FRESH DAY 饮料营销图，通过设置不同的输出尺寸，并在 Prompt 中要求模型保持饮料瓶、品牌 Logo 和整体视觉风格不变，分别生成 1:1、4:5、9:16 和 16:9 等不同比例的营销图片，使同一份商品素材能够适配不同的平台和展示场景，代码如下：</p>\n\n<pre data-language=\"Python\"><code>from diffsynth.pipelines.sensenova_u1_image import (\n    SenseNovaU1ImagePipeline,\n    ModelConfig\n)\n\nfrom PIL import Image\nimport torch\nimport os\n\nMODEL_ID = \"SenseNova/SenseNova-U1.5-8B-MoT\"\nINPUT_IMAGE = \"/mnt/workspace/drink_fresh_day_lime.png\"\n\nOUTPUT_DIR = \"/mnt/workspace/platform_images\"\n\nos.makedirs(OUTPUT_DIR, exist_ok=True)\n\nvram_config = {\n    \"offload_dtype\": \"disk\",\n    \"offload_device\": \"disk\",\n    \"onload_dtype\": \"disk\",\n    \"onload_device\": \"disk\",\n\n    \"preparing_dtype\": torch.bfloat16,\n    \"preparing_device\": \"cuda\",\n\n    \"computation_dtype\": torch.bfloat16,\n    \"computation_device\": \"cuda\",\n}\n\nprint(\"正在加载模型...\")\n\npipe = SenseNovaU1ImagePipeline.from_pretrained(\n    torch_dtype=torch.bfloat16,\n    device=\"cuda\",\n\n    model_configs=[\n        ModelConfig(\n            model_id=MODEL_ID,\n            origin_file_pattern=\"model*.safetensors\",\n            **vram_config\n        ),\n    ],\n\n    tokenizer_config=ModelConfig(\n        model_id=MODEL_ID,\n        origin_file_pattern=\"./\"\n    ),\n\n    vram_limit=(\n        torch.cuda.mem_get_info(\"cuda\")[1] / (1024 ** 3)\n        - 0.5\n    ),\n)\n\nprint(\"模型加载完成。\")\n\nedit_image = Image.open(INPUT_IMAGE).convert(\"RGB\")\n\nprint(\n    f\"原始图片尺寸：\"\n    f\"{edit_image.width} × {edit_image.height}\"\n)\n\nplatform_sizes = {\n    # 1:1 方图\n    \"square_1_1\": (1024, 1024),\n\n    # 4:5 竖图\n    \"portrait_4_5\": (1024, 1280),\n\n    # 9:16 短视频竖图\n    \"vertical_9_16\": (1152, 2048),\n\n    # 16:9 横版\n    \"banner_16_9\": (2048, 1152),\n}\n\nprompt = \"\"\"\n这是一项商品营销图片的扩图与尺寸适配任务。\n\n请严格保留原图中的饮料瓶主体，包括瓶身形状、瓶盖、\n瓶身颜色、包装标签、“FRESH DAY”品牌 Logo、\n冷凝水珠和整体产品外观。\n\n根据新的画布比例重新调整画面构图，\n让饮料瓶始终作为画面的核心视觉主体。\n\n如果画布变宽，请自然扩展左右两侧的背景，\n补充与原图一致的浅色背景、水花、冰块、绿叶或水果元素，\n保持商业产品摄影风格一致。\n\n如果画布变高，请自然扩展画面的上方和下方区域，\n保持瓶子的完整比例，不要拉伸或压缩商品主体。\n\n新增的背景和装饰元素应自然衔接原图，\n保持原有光照方向、色彩、景深和清爽夏日氛围一致。\n\n整体构图应简洁、平衡，\n适合作为饮料品牌营销广告使用。\n\n不要添加新的品牌、文字或无关元素。\n\"\"\"\n\nfor name, (width, height) in platform_sizes.items():\n\n    print(\n        f\"\\n正在生成：{name} \"\n        f\"{width} × {height}\"\n    )\n\n    image = pipe(\n        prompt=prompt,\n\n        edit_image=edit_image,\n\n        seed=42,\n\n        width=width,\n        height=height,\n\n        num_inference_steps=50,\n        cfg_scale=4.0,\n        shift=3.0,\n    )\n\n    output_path = os.path.join(\n        OUTPUT_DIR,\n        f\"{name}.png\"\n    )\n\n    image.save(output_path)\n\n    print(\n        f\"已保存：{output_path}\"\n    )\n\nprint(\"\\n所有平台尺寸生成完成。\")</code></pre>\n\n<p>代码执行结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c22-1b2439ea391bf2.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>最终生成的效果图如下，可以看出品牌 log 都是保持了，整体生成效果还不错。</p>\n\n<img src=\"assets/manuscript-20260914/c22-341b9f5b1edc71.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h2 id=\"c23-s5\">想让一组图片风格接近，再试试 LoRA</h2>\n\n<p>使用文生图和图生图模型，可以快速生成不同类型的营销图片，但由于每次生成都具有一定的随机性，即使使用相同或相似的 Prompt，不同批次生成的图片在色彩搭配、构图方式、光影效果和整体视觉风格上仍可能存在差异。</p>\n\n<p>对于企业营销而言，通常希望不同宣传素材保持相对统一的品牌视觉风格。本节引入 LoRA 微调，通过准备一组具有统一视觉风格的营销图片对模型进行训练，使模型进一步学习特定的色彩、构图和画面风格，从而在后续生成过程中更稳定地呈现所需的品牌视觉特征。</p>\n\n<p>本实验选择对 Z-Image-Turbo 进行 LoRA 微调。主要原因是该模型能够满足前面文生图实验的需求，同时相比本章图生图部分使用的 SenseNova-U1.5-8B-MoT，训练所需的显存资源更低，更适合在 24 GB 显存的单卡环境中完成 LoRA 微调实验。如果具备更充足的 GPU 显存和计算资源，也可以进一步尝试对 SenseNova-U1.5-8B-MoT 进行 LoRA 微调。</p>\n\n<pre data-language=\"SQL\"><code>!accelerate launch examples/z_image/model_training/train.py \\\n  --dataset_base_path /mnt/workspace/train_data \\\n  --dataset_metadata_path /mnt/workspace/train_data/metadata_training.csv \\\n  --max_pixels 589824 \\\n  --dataset_repeat 50 \\\n  --model_id_with_origin_paths \"Tongyi-MAI/Z-Image-Turbo:transformer/*.safetensors,Tongyi-MAI/Z-Image-Turbo:text_encoder/*.safetensors,Tongyi-MAI/Z-Image-Turbo:vae/diffusion_pytorch_model.safetensors\" \\\n  --learning_rate 1e-4 \\\n  --num_epochs 2 \\\n  --remove_prefix_in_ckpt \"pipe.dit.\" \\\n  --output_path \"/mnt/workspace/drink_lora\" \\\n  --lora_base_model \"dit\" \\\n  --lora_target_modules \"to_q,to_k,to_v,to_out.0,w1,w2,w3\" \\\n  --lora_rank 16 \\\n  --use_gradient_checkpointing \\\n  --dataset_num_workers 8</code></pre>\n\n<p>训练结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c22-356ea5fe353e11.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>接下来，使用训练好的模型进行推理，推理命令如下：</p>\n\n<pre data-language=\"Python\"><code>from diffsynth.pipelines.z_image import ZImagePipeline, ModelConfig\nimport torch\n\npipe = ZImagePipeline.from_pretrained(\n    torch_dtype=torch.bfloat16,\n    device=\"cuda\",\n    model_configs=[\n        ModelConfig(model_id=\"Tongyi-MAI/Z-Image-Turbo\", origin_file_pattern=\"transformer/*.safetensors\"),\n        ModelConfig(model_id=\"Tongyi-MAI/Z-Image-Turbo\", origin_file_pattern=\"text_encoder/*.safetensors\"),\n        ModelConfig(model_id=\"Tongyi-MAI/Z-Image-Turbo\", origin_file_pattern=\"vae/diffusion_pytorch_model.safetensors\"),\n    ],\n    tokenizer_config=ModelConfig(model_id=\"Tongyi-MAI/Z-Image-Turbo\", origin_file_pattern=\"tokenizer/\"),\n)\npipe.load_lora(pipe.dit, \"/mnt/workspace/drink_lora/epoch-1.safetensors\")\nprompt = \"\"\"\n青柠气泡水商品广告，竖版构图。一只修长的半透明绿色玻璃瓶居中直立，完整展示瓶身，配有绿色金属皇冠瓶盖。瓶身覆盖细密的凝结水珠，白色标签上印有绿色中文“青柠气泡水”，搭配青柠与绿叶图案。\n\n玻璃瓶放置在浅绿色半透明玻璃展台上，底部点缀少量透明冰块、半个青柠和新鲜绿叶。背景为浅薄荷绿渐变色\n\"\"\"\nimage = pipe(prompt=prompt, seed=42, rand_device=\"cuda\")\nimage.save(\"/mnt/workspace/drink_gen.png\")\nprint(\"图片保存成功\")</code></pre>\n\n<p>代码输出结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c22-1b27cb7e7f357c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>生成的图片如下，整体来说和 prompt 要求很接近，但是由于显存的原因，图片分辨率并不是特别高。</p>\n\n<img src=\"assets/manuscript-20260914/c22-85e859f818d78f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>具体实验数据以及代码，可参考：https://modelscope.cn/gallery/liucong/45785011-5e64-485c-883b-92a8b2d52ab7</p>\n","imageCount":14,"attachmentCount":0,"discussionId":"chapter-22"},{"id":"chapter-24","number":24,"title":"开源模型也能做出像样的 AI 视频吗？","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-24.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-24.html","minutes":0,"status":"pending","headings":[],"html":"<p>敬请期待，我们即将上部署 MiniMax H3 模型教程。</p>\n\n<p>同时呼唤这个领域做出过优秀作品的共建者！！</p>\n\n<p></p>\n","imageCount":0,"attachmentCount":0,"discussionId":"chapter-23"},{"id":"chapter-25","number":25,"title":"补充：AIGC 理论知识","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-25.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-25.html","minutes":40,"status":"ready","headings":[{"id":"c25-s1","text":"生成式模型的本质","level":"h2"},{"id":"c25-s2","text":"文本生成模型架构解析","level":"h2"},{"id":"c25-s3","text":"从文本到向量表示","level":"h3"},{"id":"c25-s4","text":"Transformer 与上下文建模","level":"h3"},{"id":"c25-s5","text":"自回归生成与解码策略","level":"h3"},{"id":"c25-s6","text":"生成对抗网络（GAN）原理与局限","level":"h2"},{"id":"c25-s7","text":"基本结构与训练机制","level":"h3"},{"id":"c25-s8","text":"GAN 的优势与训练难点","level":"h3"},{"id":"c25-s9","text":"GAN 的典型应用","level":"h3"},{"id":"c25-s10","text":" CycleGAN：无配对图像转换","level":"h4"},{"id":"c25-s11","text":"DragGAN：交互式图像编辑","level":"h4"},{"id":"c25-s12","text":"ESRGAN ：图像超分辨率","level":"h4"},{"id":"c25-s13","text":"变分自编码器（VAE）机制与角色演变","level":"h2"},{"id":"c25-s14","text":"结构与训练方式","level":"h3"},{"id":"c25-s15","text":"生成能力局限","level":"h3"},{"id":"c25-s16","text":"在现代模型中的作用","level":"h3"},{"id":"c25-s17","text":"扩散模型原理与技术突破","level":"h2"},{"id":"c25-s18","text":"扩散与去噪的基本原理","level":"h3"},{"id":"c25-s19","text":"DDPM 的训练与生成机制","level":"h3"},{"id":"c25-s20","text":"从 DDPM 到 Flow Matching","level":"h3"},{"id":"c25-s21","text":"文生图的演进","level":"h2"},{"id":"c25-s22","text":"早期探索到扩散模型","level":"h3"},{"id":"c25-s23","text":"潜在扩散与 Stable Diffusion","level":"h3"},{"id":"c25-s24","text":"从 U-Net 到 Diffusion Transformer","level":"h3"},{"id":"c25-s25","text":"Flow Matching 与新一代文生图模型","level":"h3"},{"id":"c25-s26","text":"高效生成与统一多模态的发展","level":"h3"}],"html":"<p>生成式人工智能（Artificial Intelligence Generated Content，AIGC）是近年来人工智能领域的重要发展方向。与传统的分类、预测和检索任务不同，生成式模型的目标是通过学习数据的概率分布，在文本、图像等条件的引导下生成新的内容。</p>\n\n<p>从早期的生成对抗网络（GAN）、变分自编码器（VAE），到近年来广泛应用的扩散模型（Diffusion Model）和 Diffusion Transformer（DiT），生成模型在生成质量、训练稳定性、语义理解能力和推理效率等方面不断提升。在此基础上，Stable Diffusion、FLUX、Qwen-Image、Z-Image 等文生图模型进一步推动了 AIGC 在内容创作、广告设计和营销素材生产等场景中的应用。</p>\n\n<h2 id=\"c25-s1\">生成式模型的本质</h2>\n\n<p>生成式模型（Generative Model）的核心目标，是学习训练数据中潜在的概率分布及其内在规律，并在给定条件下生成符合该分布的新样本。与分类、检索等任务不同，生成式模型并不是从预先给定的候选结果中选择一个答案，是利用已经学习到的数据规律，生成新的文本、图像、语音或视频等内容。</p>\n\n<p>从概率建模的角度来看，条件生成任务可以表示为：<span class=\"arithmatex\">\\(p(x \\mid c)\\)</span>表示：模型在给定条件 c 的情况下，生成内容 x 的条件概率分布。例如用户输入的 Prompt、上下文、参考图像或其他控制信息；x 表示需要生成的目标内容。模型训练的过程，本质上就是学习在不同条件下，各种可能结果出现的概率；模型推理的过程，则是在这一概率分布基础上产生具体结果。</p>\n\n<p>虽然大语言模型、图像生成模型和视频生成模型都属于生成式模型，但它们实现生成的具体方式并不完全相同。</p>\n\n<p>以大语言模型为例，当前主流模型通常采用自回归（Autoregressive）方式生成文本。给定已有上下文后，模型预测下一个 Token 的概率分布，模型根据该概率分布选择或采样下一个 Token，并将新生成的 Token 加入上下文，继续预测后续 Token。通过不断重复这一过程，最终形成完整的文本内容。</p>\n\n<p>图像生成模型则采用不同的生成机制，以扩散模型（Diffusion Model）为例，其生成过程通常从随机噪声开始，在文本提示词等条件的引导下逐步进行去噪，使随机噪声逐渐形成具有明确结构和语义的图像。无论采用自回归、扩散还是其他生成机制，其共同目标都是：学习真实数据的分布规律，并根据给定条件生成符合这些规律的新内容。</p>\n\n<p>生成式模型能够生成新的内容，一个重要原因在于模型学习的并不是某一条文本或某一张图片本身，是大量训练数据之间存在的统计关系和特征规律。</p>\n\n<p>例如，在学习大量饮料广告图片后，模型可能逐渐建立饮料瓶、冰块、水花、水果、背景颜色、光影以及商业摄影风格等视觉概念之间的关联。当输入“夏日青柠饮料商业广告”时，模型可以根据这些已经学习到的规律重新组织不同的视觉元素，形成新的图像结果。生成式模型的“生成”并不等同于从训练集中查找一条已有数据，是依据学习到的数据分布，对不同特征和概念进行建模与组合。</p>\n\n<p>生成式模型的另一个重要特点是生成结果具有多样性。例如，对于相同的提示词一只猫坐在窗边。模型可能生成不同品种、姿态和毛色的猫，也可能采用不同的光照、背景和画面构图。这些结果在语义上都符合输入条件，但具体内容并不完全相同。产生这种现象的原因在于，Prompt 通常只是对生成内容施加一定的条件约束，不是完整规定最终结果。在给定条件下，模型学习到的概率分布中通常存在多个合理结果。在实际生成过程中，采样策略、随机种子等因素会进一步影响最终输出。与具有确定答案的传统任务不同，生成任务通常不存在唯一的最优输出，同一个输入条件可以对应多个合理的生成结果。</p>\n\n<h2 id=\"c25-s2\">文本生成模型架构解析</h2>\n\n<p>文本生成技术在现代生成式人工智能的发展中具有重要的基础作用。一方面，大语言模型推动了 AIGC 技术的快速普及；另一方面，Tokenizer、Embedding、Transformer、概率分布和采样等概念，不仅适用于文本生成，也会在后续文生图、多模态生成等任务中反复出现。特别是在当前的 AIGC 系统中，文本 Prompt 已经成为用户控制生成内容的重要方式。无论是生成一段文本、一张图片，还是一段视频，模型通常都需要首先理解用户输入的自然语言。</p>\n\n<h3 id=\"c25-s3\">从文本到向量表示</h3>\n\n<p>自然语言本身不能直接参与神经网络计算。文本输入模型之前，首先需要经过 Tokenizer 进行切分，并转换为对应的 Token ID。Token 是语言模型处理文本的基本单位，可以是一个汉字、一个完整单词，也可以是单词的一部分，具体的切分方式由模型所采用的分词算法和词表决定。Token ID 本质上只是 Token 在词表中的编号，其数值大小并不表示 Token 之间的语义关系。例如，编号相近的两个 Token，在语义上并不一定相似。因此，在进入神经网络进行计算之前，还需要通过 Embedding 层将离散的 Token ID 映射为连续的高维向量，这种使用连续向量表示语言的思想，是现代自然语言处理的重要基础。2013 年，Mikolov 等人提出 Word2Vec，通过神经网络从大规模语料中学习词语的分布式向量表示。其基本思想来源于自然语言中的分布假设，即出现在相似上下文中的词语，通常具有相近的语义或语法特征。</p>\n\n<p>Word2Vec 主要包含两种训练结构：CBOW（Continuous Bag-of-Words）和 Skip-gram。二者均通过中心词与上下文词之间的关系学习词向量，但预测方向有所不同。</p>\n\n<img src=\"assets/manuscript-20260914/c24-84a037acca74b8.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>CBOW 根据上下文词预测中心词。 对于由词语组成的序列，首先选定一个中心词，并将其周围一定窗口范围内的词作为上下文。模型利用这些上下文词的向量表示预测中心词。比如，以“吃”为中心词，并选择其周围的“喜欢”和“新鲜”作为上下文。</p>\n\n<p>Skip-gram 则根据中心词预测其周围的上下文词。 与 CBOW 相比，其预测方向相反。对于同一个词语序列，当“吃”作为中心词时，Skip-gram 的训练目标是根据“吃”的向量表示预测其一定窗口范围内可能出现的其他词语，例如“喜欢”“新鲜”等。CBOW 与 Skip-gram 的主要区别在于训练目标不同，CBOW 利用多个上下文词预测一个中心词；Skip-gram：利用一个中心词预测多个上下文词。尽管二者的预测方向不同，其最终目的都是通过词语之间大量的共现关系学习词向量。训练完成后，每个词可以表示为一个固定维度的连续向量。</p>\n\n<p>Word2Vec 的提出推动了分布式词表示的发展，但其表示方式仍然具有一定局限性。Word2Vec 学习的是典型的静态词向量（Static Word Embedding），即一个词在训练完成后通常对应一个固定的向量，而不会随着具体上下文发生变化。现代语言模型进一步采用了上下文化表示（Contextual Representation）。Token 首先通过 Embedding 层获得初始向量，随后经过多层神经网络，根据序列中其他 Token 的信息不断更新自身表示。因此，同一个 Token 在不同上下文中可以形成不同的隐藏状态，从而表达与当前语境相关的语义。Word2Vec 的重要意义不仅在于提出了一种具体的词向量训练方法，更在于推动了分布式表示在自然语言处理中的广泛应用。语言不再仅通过离散的符号或编号表示，还被映射到连续向量空间中，使神经网络能够进一步学习词语之间的语义和语法关系。后续自然语言处理技术进一步关注如何利用上下文动态调整词语表示。</p>\n\n<h3 id=\"c25-s4\">Transformer 与上下文建模</h3>\n\n<p>2017 年，提出 的 Transformer 架构。Transformer 通过注意力机制建模序列中不同位置之间的关系，随后逐渐成为大语言模型的核心基础架构。Transformer 中最重要的机制之一是 Self-Attention，即自注意力机制,如下图：</p>\n\n<img src=\"assets/manuscript-20260914/c24-fb694fc44c10a2.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>自注意力机制的主要作用，是让模型在处理一个 Token 时，同时考虑序列中其他 Token 所包含的信息，并根据它们之间的关联程度动态分配不同的注意力权重。具体而言，每个 Token 的表示会分别映射为 Query（Q）、Key（K）和 Value（V）。模型通过 Query 与不同 Key 之间的匹配程度计算注意力权重，再利用这些权重对相应的 Value 进行加权组合，从而得到融合上下文信息后的新表示。</p>\n\n<p>Transformer 通常由多个网络层堆叠构成。在不同层中，模型会不断更新各个 Token 的表示，使其逐步融合更丰富的上下文信息。经过多层 Transformer 处理之后，同一个 Token 在不同语境下可以形成不同的上下文表示，从而支持模型对词义、句法结构以及较复杂语义关系的建模。Transformer 的重要意义不仅在于引入了一种新的网络结构，更在于提供了一套能够有效建模上下文关系的机制。现代大语言模型正是在 Transformer 的基础上，通过扩大模型规模、训练数据规模以及上下文长度等方式，不断提升语言理解与生成能力。</p>\n\n<h3 id=\"c25-s5\">自回归生成与解码策略</h3>\n\n<p>经过 Transformer 对输入序列进行上下文建模后，语言模型已经获得了包含上下文信息的 Token 表示。在此基础上，生成式语言模型需要进一步预测后续内容。GPT 等主流生成式大语言模型通常采用自回归生成（Autoregressive Generation）方式，即根据当前已经输入或生成的 Token，逐步预测并生成后续 Token。</p>\n\n<p>对于一个由多个 Token 构成的序列，其生成概率可以分解为一系列条件概率：</p>\n\n<p><span class=\"arithmatex display-math\">\\(p(x_1, x_2, \\ldots, x_T) = \\prod_{t=1}^{T} p(x_t \\mid x_1, x_2, \\ldots, x_{t-1}) \n\\)</span></p>\n\n<p>其中，<span class=\"arithmatex\">\\(x_t\\)</span> 表示当前需要生成的第 <span class=\"arithmatex\">\\(t\\)</span> 个 Token，<span class=\"arithmatex\">\\(x_1,x_2,\\ldots,x_{t-1}\\)</span> 表示此前已经输入或生成的 Token。该公式表明，一个完整文本序列的生成过程可以分解为多个连续的下一个 Token 预测过程。</p>\n\n<p>模型计算出下一个 Token 的概率分布之后，还需要通过解码策略（Decoding Strategy）确定实际输出的 Token。不同的解码策略会直接影响文本生成的稳定性、多样性和创造性。</p>\n\n<p>最直接的方式是贪婪解码（Greedy Decoding），即每一步都选择当前概率最高的 Token。这种方式具有较强的确定性，但连续选择局部最高概率的 Token，并不一定能够获得整体质量最优的文本，同时生成内容也容易趋于单一。为了提高生成结果的多样性，生成式语言模型通常还可以采用随机采样，即按照模型预测的概率分布选择下一个 Token。概率越高的 Token 被选中的可能性越大，但其他合理的候选 Token 同样具有被选中的机会。在实际应用中，通常还会结合 Temperature、Top-k 和 Top-p 等方法进一步调整采样过程。</p>\n\n<p>Temperature 用于调节概率分布的平滑程度，可以表示为：</p>\n\n<p><span class=\"arithmatex display-math\">\\(p_i =\n\\frac{\\exp(z_i / T)}\n{\\sum_j \\exp(z_j / T)}\n\\)</span></p>\n\n<p>其中，<span class=\"arithmatex\">\\(z_i\\)</span> 表示第<span class=\"arithmatex\">\\(i\n\\)</span>个 Token 对应的 Logit，即模型在 Softmax 之前，为每个候选 Token 计算出的原始分数。<span class=\"arithmatex\">\\(T\\)</span> 表示 Temperature。</p>\n\n<p>当 <span class=\"arithmatex\">\\(T\\)</span> 较小时，高概率 Token 的优势会进一步增强，模型更加倾向于选择概率较高的结果，生成内容通常更加稳定；当 <span class=\"arithmatex\">\\(T\\)</span> 较大时，概率分布相对更加平缓，低概率 Token 获得更多被选择的机会，生成结果通常具有更高的多样性。</p>\n\n<p>Top-k 则是在每一步生成时，仅保留概率最高的 <span class=\"arithmatex\">\\(k\\)</span> 个候选 Token，并在这些候选 Token 中重新归一化概率后进行采样。该方法可以过滤大量概率较低的 Token，减少明显不合理内容被选中的可能性。</p>\n\n<p>Top-p 不固定候选 Token 的数量，是按照概率从高到低排列，选择累计概率达到阈值 <span class=\"arithmatex\">\\(p\\)</span> 的最小候选集合，再从该集合中进行采样。因此，Top-p 可以根据当前概率分布动态调整候选 Token 的范围。</p>\n\n<p>例如，当模型对下一 Token 的预测较为确定时，少量 Token 的累计概率就可以达到设定阈值；而当模型存在多个合理候选结果时，则会保留更多 Token 参与采样。文本生成实际上包含两个紧密关联的过程：首先由语言模型根据上下文计算下一个 Token 的概率分布，再由解码与采样策略根据该概率分布确定实际生成的 Token。</p>\n\n<p>型负责学习在当前上下文中哪些 Token 更有可能出现，而解码策略负责决定如何从这些候选 Token 中选择具体结果”。二者共同决定了最终文本的准确性、稳定性与多样性。</p>\n\n<h2 id=\"c25-s6\">生成对抗网络（GAN）原理与局限</h2>\n\n<p>在深度学习生成模型的发展过程中，生成对抗网络（Generative Adversarial Network，GAN）是一类具有代表性的技术。2014 年，Goodfellow 等人提出 GAN，通过两个神经网络之间的对抗训练学习真实数据的分布规律。与传统的监督学习不同，GAN 不直接规定生成器应该输出怎样的图像，还引入一个判别器对生成结果进行评价，并利用判别结果不断改进生成器。GAN 提出后迅速应用于图像生成、人脸生成、图像风格迁移、超分辨率和图像编辑等任务，并形成了 CycleGAN、StyleGAN、ESRGAN 等一系列具有代表性的模型。</p>\n\n<h3 id=\"c25-s7\">基本结构与训练机制</h3>\n\n<p>GAN 通过生成器（Generator）<b>与</b>判别器（Discriminator）两个神经网络之间的对抗训练学习真实数据的分布，如图所示，在训练过程中，判别器同时接收来自训练集的真实样本和生成器产生的生成样本，并对两类样本进行区分；生成器则根据判别器提供的反馈不断调整自身参数，提高生成样本与真实样本之间的相似程度。</p>\n\n<img src=\"assets/manuscript-20260914/c24-da3ba9249a73ec.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>生成器记为 G。其输入通常是从某个简单概率分布中采样得到的随机变量 z，例如高斯分布或均匀分布。经过生成器的非线性映射后，随机变量被转换为生成样本：</p>\n\n<p><span class=\"arithmatex display-math\">\\(\\hat{x}=G(z), \\qquad z\\sim p_z(z)\n\\)</span></p>\n\n<p>在图像生成任务中，生成器的目标是将低维或高维的随机表示逐步映射为具有特定视觉结构的图像。</p>\n\n<p>判别器记为 D。其输入包括训练集中的真实样本 x 和生成器产生的样本 G(z)。判别器通过学习两类数据的特征差异，输出输入样本来自真实数据分布的概率：</p>\n\n<p><span class=\"arithmatex display-math\">\\(D(x)\\in[0,1]\n\\)</span></p>\n\n<p>对于真实样本，判别器希望输出结果接近 1；对于生成样本，判别器希望输出结果接近 0。生成器与判别器具有相反的优化目标。判别器需要不断提高对真实样本和生成样本的区分能力，而生成器则需要不断提高生成样本的真实性，使生成样本在数据特征上更加接近真实样本。两个网络在交替优化过程中形成对抗关系。在原始论文中将 GAN 的训练目标定义为一个极小极大博弈（Minimax Game）：</p>\n\n<p><span class=\"arithmatex display-math\">\\(\\min_G \\max_D V(D,G)\n=\n\\mathbb{E}_{x\\sim p_{\\mathrm{data}}(x)}\n[\\log D(x)]\n+\n\\mathbb{E}_{z\\sim p_z(z)}\n[\\log(1-D(G(z)))]\n\\)</span></p>\n\n<p>其中，<span class=\"arithmatex\">\\(p_{\\mathrm{data}}\\)</span> 表示真实数据分布，<span class=\"arithmatex\">\\(p_z\\)</span> 表示随机变量的先验分布。判别器 <span class=\"arithmatex\">\\(D\\)</span> 通过最大化目标函数，提高对真实样本和生成样本的区分能力；生成器 <span class=\"arithmatex\">\\(G\\)</span> 则通过优化自身参数，使生成数据的分布逐渐逼近真实数据分布。</p>\n\n<p>GAN 的训练通常采用交替优化方式进行。首先利用真实样本和生成样本更新判别器 <span class=\"arithmatex\">\\(D\\)</span>，使其学习两类数据之间的差异；随后利用判别器产生的梯度信息更新生成器 <span class=\"arithmatex\">\\(G\\)</span>，使生成器逐步改善生成结果。随着训练不断进行，生成器与判别器的能力同时发生变化，从而形成一个动态的对抗学习过程。</p>\n\n<p>生成器与判别器在 GAN 中承担不同的功能。判别器主要用于训练阶段，通过真假分类为生成器提供优化信号；当模型训练完成后，如果任务仅是生成新的图像，只需要使用生成器即可完成推理：</p>\n\n<img src=\"assets/manuscript-20260914/c24-7d3751920d02c8.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c25-s8\">GAN 的优势与训练难点</h3>\n\n<p>GAN 在图像生成领域受到广泛关注，一个重要原因在于其生成过程较为直接。模型完成训练后，只需要将随机变量输入生成器，通过一次前向计算即可得到生成结果。与需要多步迭代生成的模型相比，GAN 通常具有较高的生成效率，因此在图像生成、超分辨率、视频增强以及交互式图像处理等任务中得到了广泛应用。GAN 的优势主要体现在生成阶段，其训练过程则相对复杂，主要存在以下问题。</p>\n\n<p>1）训练过程容易出现不稳定</p>\n\n<p>GAN 需要同时训练生成器和判别器，两个网络具有相互对抗的优化目标。当生成器参数发生变化时，判别器面对的生成数据也随之发生变化；当判别器参数发生变化时，生成器接收到的优化信号同样会发生变化。因此，与在相对固定目标函数下进行优化的普通监督学习不同，GAN 的训练实际上是一个不断变化的动态博弈过程。如果判别器能力过强，生成样本可能很容易被识别，生成器难以获得有效的优化信号；如果判别器能力过弱，则无法准确反映真实样本与生成样本之间的差异，同样难以为生成器提供有效的训练反馈。因此，如何保持生成器与判别器之间相对稳定的训练状态，是 GAN 模型训练中的重要问题。</p>\n\n<p>2）容易出现模式崩溃（Mode Collapse）</p>\n\n<p>模式崩溃是 GAN 训练中的另一个典型问题。理想情况下，生成器不仅需要生成具有较高视觉质量的样本，还应当能够覆盖真实数据中的不同类型和特征。例如，假设训练数据中包含不同年龄、表情和外观特征的人脸，生成器应当能够产生具有相应多样性的人脸图像。但在实际训练过程中，生成器可能倾向于生成少数几类更容易获得判别器较高评价的样本，并不断产生相似结果。此时，单张生成图像可能具有较高的视觉质量，但大量生成结果之间的差异较小，无法充分覆盖真实数据的多样性。模式崩溃反映了生成模型中的一个重要问题：较高的单样本生成质量，并不意味着模型已经完整学习了真实数据的分布。</p>\n\n<p>针对训练不稳定、模式崩溃以及生成质量等问题，研究者从网络结构、损失函数和训练策略等多个方面对 GAN 进行了改进，并形成了大量衍生模型。这些研究不仅提高了 GAN 的生成能力，也使其逐渐从基本的图像生成扩展到图像转换、可控图像编辑和图像超分辨率等不同任务。下面通过几个具有代表性的模型介绍 GAN 在这些方向中的典型应用。</p>\n\n<h3 id=\"c25-s9\">GAN 的典型应用</h3>\n\n<p>随着 GAN 技术的发展，其应用范围逐渐从随机图像生成扩展到图像转换、图像编辑和图像增强等任务。不同模型通常针对具体任务重新设计生成器、判别器、损失函数或训练方式，从而形成具有不同功能的 GAN 衍生模型。其中 CycleGAN、DragGAN、ESRGAN 和 Real-ESRGAN 分别代表了 GAN 在无配对图像转换、交互式图像编辑和图像超分辨率等方向的典型应用。</p>\n\n<h4 id=\"c25-s10\"> CycleGAN：无配对图像转换</h4>\n\n<p>2017 年，Zhu 等人提出 CycleGAN，主要用于解决无配对图像到图像转换（Unpaired Image-to-Image Translation）问题。与需要成对训练样本的图像转换方法不同，CycleGAN 不要求源图像与目标图像之间具有一一对应关系，只需要分别提供两个图像域的数据，即可学习两个图像域之间的转换关系。例如，在马 → 斑马的转换任务中，传统方法通常需要准备相互对应的马和斑马图像，而 CycleGAN 只需要分别准备一组马的图像和一组斑马的图像，无须建立两组图像之间的对应关系。如下图所示，CycleGAN 由两个生成器和两个判别器组成，并同时学习两个方向的图像转换。 假设马的图像属于图像域 <span class=\"arithmatex\">\\(X\\)</span>，斑马的图像属于图像域 <span class=\"arithmatex\">\\(Y\\)</span>，生成器 <span class=\"arithmatex\">\\(G\\)</span> 负责完成从 <span class=\"arithmatex\">\\(X\\)</span> 到 <span class=\"arithmatex\">\\(Y\\)</span> 的转换，生成器 <span class=\"arithmatex\">\\(F\\)</span> 则负责完成从 <span class=\"arithmatex\">\\(Y\\)</span> 到 <span class=\"arithmatex\">\\(X\\)</span> 的转换：</p>\n\n<p><span class=\"arithmatex display-math\">\\(G:X\\rightarrow Y,\\qquad F:Y\\rightarrow X\n\\)</span></p>\n\n<p>其中，判别器 <span class=\"arithmatex\">\\(D_Y\\)</span> 用于判断生成的斑马图像是否符合真实斑马图像的分布，判别器 <span class=\"arithmatex\">\\(D_X\\)</span> 则用于判断生成的马图像是否符合真实马图像的分布。因此，CycleGAN 实际包含两个方向的对抗学习过程：</p>\n\n<p><span class=\"arithmatex display-math\">\\(\nG + D_Y:\\quad X \\rightarrow Y \\qquad \\text{（马} \\rightarrow \\text{斑马）}\n\\)</span></p>\n\n\n<p><span class=\"arithmatex display-math\">\\(\nF + D_X:\\quad Y \\rightarrow X \\qquad \\text{（斑马} \\rightarrow \\text{马）}\n\\)</span></p>\n\n<p>仅通过两个方向的对抗训练，还不能保证转换后的图像与原始输入保持对应关系。例如，将一幅马的图像转换为斑马时，生成结果虽然可能具有真实的斑马纹理，但马的姿态、位置或背景结构可能发生较大变化。也就是不能充分保证转换前后的内容一致性。</p>\n\n<img src=\"assets/manuscript-20260914/c24-f1d5c9fa7f99e1.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>为此，CycleGAN 进一步引入了循环一致性（Cycle Consistency）约束。如图中的下半部分所示，对于图像域 <span class=\"arithmatex\">\\(X\\)</span> 中的马图像 <span class=\"arithmatex\">\\(x\\)</span>，首先通过生成器 <span class=\"arithmatex\">\\(G\\)</span> 将其转换为斑马图像 <span class=\"arithmatex\">\\(G(x)\\)</span>，随后再通过生成器 <span class=\"arithmatex\">\\(F\\)</span> 转换回马图像：</p>\n\n<p><span class=\"arithmatex display-math\">\\(x\\rightarrow G(x)\\rightarrow F(G(x))\n\\)</span></p>\n\n<p>经过一次完整的正向和反向转换后，最终结果应尽可能接近原始图像：</p>\n\n<p><span class=\"arithmatex display-math\">\\(F(G(x))\\approx x\n\\)</span></p>\n\n<p>同样，对于图像域 <span class=\"arithmatex\">\\(Y\\)</span> 中的斑马图像 <span class=\"arithmatex\">\\(y\\)</span>，需要满足：</p>\n\n<p><span class=\"arithmatex display-math\">\\(G(F(y))\\approx y\n\\)</span></p>\n\n<p>因此，模型形成了两个完整的转换循环：马 → 斑马 → 马，斑马 → 马 → 斑马。CycleGAN 中为了约束两个循环，模型引入循环一致性损失：</p>\n\n<p><span class=\"arithmatex display-math\">\\(\\mathcal{L}_{\\mathrm{cyc}}(G,F)\n=\n\\mathbb{E}_{x\\sim p_{\\mathrm{data}}(x)}\n\\left[\\|F(G(x))-x\\|_1\\right]\n+\n\\mathbb{E}_{y\\sim p_{\\mathrm{data}}(y)}\n\\left[\\|G(F(y))-y\\|_1\\right]\n\\)</span></p>\n\n<p>其中，第一项约束马 → 斑马 → 马的重建结果，第二项约束斑马 → 马 → 斑马的重建结果。循环一致性损失越小，说明图像经过双向转换后，与原始图像的内容和结构越接近。CycleGAN 的训练主要包含对抗学习和循环一致性约束两个部分，二者共同作用，使 CycleGAN 能够在没有成对训练数据的情况下学习两个图像域之间的映射关系，从而实现马与斑马转换以及图像风格迁移等任务。</p>\n\n<h4 id=\"c25-s11\">DragGAN：交互式图像编辑</h4>\n\n<p>2023 年，Pan 等人提出 DragGAN，通过在图像中设置控制点（Handle Point）和目标点（Target Point），实现对生成图像中物体位置、姿态和形状的交互式调整。与传统像素级编辑不同，DragGAN 没有直接移动图像中的像素，是通过调整 GAN 的潜在表示，使生成图像按照用户指定的方向发生变化。</p>\n\n<img src=\"assets/manuscript-20260914/c24-664afe20135bb1.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>DragGAN 的核心过程主要包括运动监督（Motion Supervision）和点跟踪（Point Tracking）两个部分。 用户首先在图像中选择需要调整的控制点，并指定其希望移动到的目标位置。例如，图中通过设置狮子嘴部附近的控制点和目标点，使狮子的嘴部逐渐张开。</p>\n\n<p>在编辑过程中，DragGAN 保持预训练生成器的参数不变，不断优化输入生成器的潜在编码 <span class=\"arithmatex\">\\(w\\)</span>。初始图像可以表示为：<span class=\"arithmatex display-math\">\\(I = G(w)\n\\)</span>其中，<span class=\"arithmatex\">\\(G\\)</span> 表示预训练的 GAN 生成器，<span class=\"arithmatex\">\\(w\\)</span> 表示图像对应的潜在编码。通过调整 <span class=\"arithmatex\">\\(w\\)</span>，可以改变生成图像 <span class=\"arithmatex\">\\(I\\)</span> 的内容和结构。运动监督用于推动控制点附近的图像特征向目标位置移动。经过一次优化后，潜在编码由 <span class=\"arithmatex\">\\(w\\)</span> 更新为 <span class=\"arithmatex\">\\(w'\\)</span>，生成图像也随之发生变化。由于图像发生变化后，原来的控制点位置可能已经发生偏移，因此 DragGAN 还需要通过点跟踪重新确定控制点在新图像中的位置，再继续进行下一轮优化，也就是设置控制点与目标点→运动监督→优化潜在编码→生成新的图像→点跟踪并更新控制点→继续优化，直至接近目标位置，经过多次迭代后，控制点逐渐移动至用户指定的目标位置，最终得到编辑后的图像。由于这一过程是在 GAN 学习到的生成空间中进行优化，模型不仅会改变指定位置，还能够对周围相关结构进行相应调整。</p>\n\n<p>DragGAN 的关键并不是简单地拖动像素，是将用户的拖拽操作转换为对 GAN 潜在表示的迭代优化，并通过运动监督和点跟踪控制图像中的语义结构向指定位置移动。这一方法展示了 GAN 在可控图像编辑方面的应用能力，使用户能够通过较为直观的点位操作调整物体的姿态、表情和形状等视觉属性。</p>\n\n<h4 id=\"c25-s12\">ESRGAN ：图像超分辨率</h4>\n\n<p>图像超分辨率（Super-Resolution，SR）是 GAN 的典型应用方向之一，其目标是根据低分辨率图像生成具有更高分辨率和更丰富视觉细节的图像。与简单的插值放大不同，超分辨率模型需要根据已有图像内容，对缺失的高频纹理和局部结构进行重建。2018 年，Wang 等人提出 <a href=\"https://arxiv.org/pdf/1809.00219\" target=\"_blank\" rel=\"noopener noreferrer\">ESRGAN</a>，在已有研究基础上进一步改进网络结构、对抗损失和感知损失，以改善超分辨率图像的纹理表现和视觉质量。</p>\n\n<img src=\"assets/manuscript-20260914/c24-64eed81d0f76a8.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>ESRGAN 在网络结构上的一个重要改进是引入 Residual-in-Residual Dense Block（RRDB）结构。 SRGAN 的生成器主要采用普通残差块（Residual Block，RB），其中包含卷积层、批归一化（Batch Normalization，BN）和激活函数。ESRGAN 首先移除了残差块中的 BN 层，并进一步将多个密集连接模块组合为 RRDB。</p>\n\n<p>从图中可以看到，RRDB 内部由多个 Dense Block 组成，并结合了密集连接与残差连接。在 Dense Block 内部，不同卷积层之间采用密集连接，后续层可以接收并利用前面多个层提取的特征；在多个 Dense Block 之间以及 RRDB 外部，则进一步通过残差连接传递特征。这种结构能够加强不同层之间的信息传递与特征复用，提高网络对图像纹理和细节特征的建模能力。除了网络结构上的改进，ESRGAN 还对训练目标进行了优化。传统超分辨率方法如果主要采用像素级误差进行训练，模型通常倾向于生成较为平滑的结果，虽然像素误差可能较小，但部分边缘和高频纹理容易出现模糊。ESRGAN 进一步结合感知损失和对抗训练，使生成结果在保持整体内容的同时获得更加清晰、自然的纹理细节。</p>\n\n<p>生成式超分辨率并不等同于恢复已经丢失的原始信息。 当低分辨率图像中的部分高频信息已经丢失时，仅根据现有像素通常无法唯一确定原始细节。ESRGAN 实际上会结合输入图像以及训练过程中学习到的图像先验，生成视觉上合理的纹理。生成结果虽然可能更加清晰，但其中部分细节并不一定与原始高分辨率图像完全一致。这一特点使 ESRGAN 更适合强调视觉质量的图像增强任务。在医学影像、档案取证和科研图像等对信息真实性要求较高的场景中，则需要谨慎使用生成式超分辨率结果。</p>\n\n<h2 id=\"c25-s13\">变分自编码器（VAE）机制与角色演变</h2>\n\n<p>GAN 通过生成器与判别器之间的对抗训练学习真实数据分布，而变分自编码器（Variational Autoencoder，VAE）采用了另一种生成思路：首先将输入数据编码到一个连续的潜在空间，再从潜在空间中采样，并通过解码器生成或重建数据。2013 年，Kingma 和 Welling 提出了 Auto-Encoding Variational Bayes（AEVB）方法，并给出了后来广泛使用的 VAE 训练框架。VAE 将神经网络与变分推断结合，使模型能够通过端到端训练学习具有概率结构的潜在表示。</p>\n\n<h3 id=\"c25-s14\">结构与训练方式</h3>\n\n<p>VAE 的基本结构由编码器（Encoder）和解码器（Decoder）组成。编码器负责将输入数据压缩到维度较低的潜在空间，解码器则根据潜在表示恢复输入数据。普通自编码器通常将输入 <span class=\"arithmatex\">\\(x\\)</span> 直接编码为一个确定的潜在向量 <span class=\"arithmatex\">\\(z\\)</span>，再利用解码器进行重建。VAE 与其最大的区别在于，编码器并不直接输出一个确定的潜在向量，是输出潜在变量概率分布的参数。</p>\n\n<img src=\"assets/manuscript-20260914/c24-6fadd070a0fc63.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>上图中，VAE 的编码器并不会直接将输入数据 <span class=\"arithmatex\">\\(x\\)</span> 转换为一个固定的潜在向量，还输出两个参数：均值 <span class=\"arithmatex\">\\(\\mu\\)</span> 和标准差 <span class=\"arithmatex\">\\(\\sigma\\)</span>。这两个参数共同确定了潜在变量 <span class=\"arithmatex\">\\(z\\)</span> 的概率分布。<span class=\"arithmatex\">\\(\\mu\\)</span> 决定潜在变量大致位于什么位置，<span class=\"arithmatex\">\\(\\sigma\\)</span> 则表示它可以在这个位置附近有多大的变化范围。</p>\n\n<p>VAE 将潜在变量建模为高斯分布：</p>\n\n<p><span class=\"arithmatex display-math\">\\(q_{\\phi}(z\\mid x)\n=\n\\mathcal{N}\n\\left(\nz;\\mu_{\\phi}(x),\n\\operatorname{diag}\\left(\\sigma_{\\phi}^{2}(x)\\right)\n\\right)\n\\)</span>其中，<span class=\"arithmatex\">\\(q_{\\phi}(z\\mid x)\\)</span> 表示编码器根据输入 <span class=\"arithmatex\">\\(x\\)</span> 得到的潜在变量分布。与普通自编码器直接得到一个确定的 <span class=\"arithmatex\">\\(z\\)</span> 不同，VAE 得到的是一个可以进行采样的分布，因此同一个输入可以在一定范围内得到不同的潜在表示。接下来，模型需要从这个分布中得到一个具体的潜在变量 <span class=\"arithmatex\">\\(z\\)</span>，再将其送入解码器。为了在引入随机性的同时保证模型能够正常进行反向传播，VAE 使用了重参数化技巧（Reparameterization Trick）。模型首先从标准正态分布中采样随机变量 <span class=\"arithmatex\">\\(\\epsilon\\)</span>，然后结合编码器得到的 <span class=\"arithmatex\">\\(\\mu\\)</span> 和 <span class=\"arithmatex\">\\(\\sigma\\)</span> 计算潜在变量 <span class=\"arithmatex\">\\(z\\)</span>：</p>\n\n<p><span class=\"arithmatex display-math\">\\(z=\\mu+\\sigma\\odot\\epsilon\n\\)</span></p>\n\n<p>可以理解为，<span class=\"arithmatex\">\\(\\mu\\)</span> 决定位置，<span class=\"arithmatex\">\\(\\sigma\\)</span> 决定变化范围，<span class=\"arithmatex\">\\(\\epsilon\\)</span> 提供随机性，三者共同得到最终的潜在变量 <span class=\"arithmatex\">\\(z\\)</span>。这种处理方式将随机采样从网络参数的计算过程中分离出来，使梯度仍然能够通过 <span class=\"arithmatex\">\\(z\\)</span> 传递到编码器，从而实现整个 VAE 的端到端训练。得到潜在变量 <span class=\"arithmatex\">\\(z\\)</span> 后，解码器根据 <span class=\"arithmatex\">\\(z\\)</span> 生成重建结果 <span class=\"arithmatex\">\\(\\hat{x}\\)</span>。VAE 的整个过程可以概括为：输入数据 → 编码器 → 潜在分布 → 采样得到 <span class=\"arithmatex\">\\(z\\)</span> → 解码器 → 重建数据。</p>\n\n<p>VAE 在训练过程中还需要同时考虑两个目标：一方面，希望解码器生成的结果尽可能接近原始输入；另一方面，希望不同输入对应的潜在分布不要过于杂乱，是形成一个相对连续、规则的潜在空间。为此 VAE 的训练目标由重建项和 KL 散度项共同组成，通常通过证据下界（Evidence Lower Bound，ELBO）表示：</p>\n\n<p><span class=\"arithmatex display-math\">\\(\\mathcal{L}_{\\mathrm{ELBO}}\n=\n\\mathbb{E}_{q_{\\phi}(z\\mid x)}\n\\left[\n\\log p_{\\theta}(x\\mid z)\n\\right]\n-\nD_{\\mathrm{KL}}\n\\left(\nq_{\\phi}(z\\mid x)\n\\parallel\np(z)\n\\right)\n\\)</span></p>\n\n<p>其中，第一项可以理解为重建得好不好，用于衡量解码器能否根据 <span class=\"arithmatex\">\\(z\\)</span> 恢复输入数据；第二项可以理解为潜在空间是否足够规则，通过 KL 散度约束编码器得到的潜在分布，使其接近预先设定的先验分布。这两个目标需要共同发挥作用。如果只关注重建效果，模型虽然可以将输入较好地恢复出来，但潜在空间可能比较分散，不方便直接从中随机采样；加入 KL 散度约束后，不同样本的潜在表示会受到统一先验分布的约束，使潜在空间更加规则，也更适合进行采样和生成。</p>\n\n<h3 id=\"c25-s15\">生成能力局限</h3>\n\n<p>从生成过程来看，VAE 已经具备完整的生成能力。训练完成后，可以不再输入原始图像，能直接从标准正态分布中随机采样潜在变量 <span class=\"arithmatex\">\\(z\\)</span>再将 <span class=\"arithmatex\">\\(z\\)</span> 输入解码器，即可生成新的图像。理论上，这一过程能够成立，是因为训练阶段通,过 KL 散度不断约束编码器产生的潜在分布，使其接近预先设定的标准正态分布。但在实际训练中，编码器产生的潜在分布通常难以与标准正态分布完全一致。如果约束过强，虽然潜在空间更加规则，却可能损失部分图像信息；如果约束过弱，又可能导致潜在空间分布不够规则，使直接从标准正态分布中随机采样得到的 <span class=\"arithmatex\">\\(z\\)</span> 落在模型不熟悉的区域，进而影响解码结果。</p>\n\n<p>与此同时，VAE 需要在重建质量和潜在空间规则性之间进行平衡。经典 VAE 的训练目标更强调对整体数据分布和潜在空间的建模，在图像生成过程中容易损失部分高频纹理和局部细节，生成结果往往比较平滑。这也是 VAE 与 GAN 在图像生成效果上的一个明显区别。GAN 通过判别器直接约束生成图像的真实性，通常能够产生更加锐利的纹理；VAE 则更加关注潜在空间的连续性和概率结构，在直接生成高质量图像方面存在一定局限。</p>\n\n<h3 id=\"c25-s16\">在现代模型中的作用</h3>\n\n<p>随着生成模型的发展，VAE 的作用逐渐从直接生成最终图像转变为图像空间与潜在空间之间的编码和解码工具。其中，一个具有代表性的应用就是潜在扩散模型（Latent Diffusion Model，LDM）,早期扩散模型直接在像素空间中进行加噪和去噪。当图像分辨率较高时，模型需要反复处理大量像素，计算成本较高。Rombach 等人提出的潜在扩散模型将扩散过程转移到经过压缩的潜在空间中，从而降低扩散模型需要处理的数据维度。在这一结构中，首先利用编码器将原始图像压缩为潜在表示：原始图像 → Encoder → 潜在表示,这些潜在表示不再直接保存每一个像素，而是以更紧凑的形式保留图像中的主要结构和视觉信息。随后，扩散模型在这一潜在空间中进行加噪和去噪，而不再直接处理原始高分辨率图像。当扩散模型完成生成后，再利用解码器将生成的潜在表示恢复到像素空间，也就是生成的潜在表示 → Decoder → 最终图像。VAE 负责压缩和还原图像，扩散模型负责在潜在空间中生成图像内容。这种方式充分利用了 VAE 的潜在表示能力。扩散模型处理的不再是高维的原始像素，是经过压缩后的图像特征，从而显著降低后续扩散和去噪过程的计算量。</p>\n\n<p>VAE 本身在直接生成高质量图像方面存在一定局限，但其编码器、潜在空间和解码器结构却成为现代潜在扩散模型的重要组成部分。这也体现了 VAE 角色的变化：从独立的生成模型，逐渐演变为高质量图像生成系统中的潜在表示模块。</p>\n\n<h2 id=\"c25-s17\">扩散模型原理与技术突破</h2>\n\n<p>GAN 通常利用生成器通过一次前向计算完成图像生成，VAE 也可以从潜在变量出发，通过解码器直接得到生成结果。对于复杂的高维图像而言，让模型一次完成从随机变量到完整图像的映射并不是一件容易的事情。扩散模型（Diffusion Model）采用了不同的思路，将复杂的图像生成任务拆分为多个相对简单的步骤，每一步只对当前结果进行少量调整，经过多次迭代后逐渐得到完整图像。不要求模型一步生成最终结果，是通过多次去噪逐步完成生成。</p>\n\n<h3 id=\"c25-s18\">扩散与去噪的基本原理</h3>\n\n<p>扩散模型主要包含两个方向相反的过程：前向扩散和反向去噪。前向扩散从真实数据 <span class=\"arithmatex\">\\(x_0\\)</span> 开始，不断向其中加入少量随机噪声。随着时间步 <span class=\"arithmatex\">\\(t\\)</span> 增大，图像中的原始信息逐渐减少，噪声逐渐增加，最终接近随机高斯噪声。真实图像 → 少量噪声 → 更多噪声 → …… → 随机噪声，在经典 DDPM 中，可以直接根据原始数据 <span class=\"arithmatex\">\\(x_0\\)</span> 构造任意时间步的带噪数据：</p>\n\n<p><span class=\"arithmatex display-math\">\\(x_t\n=\n\\sqrt{\\bar{\\alpha}_t}x_0\n+\n\\sqrt{1-\\bar{\\alpha}_t}\\epsilon\n\\)</span></p>\n\n<p>其中，<span class=\"arithmatex\">\\(x_0\\)</span> 表示原始数据，<span class=\"arithmatex\">\\(\\epsilon\\)</span> 表示从标准高斯分布中采样的随机噪声，即 <span class=\"arithmatex\">\\(\\epsilon \\sim \\mathcal{N}(0,I)\\)</span>，<span class=\"arithmatex\">\\(\\bar{\\alpha}_t\\)</span> 用于控制当前时间步中原始数据与噪声所占的比例。时间步越靠后，原始信息越少，噪声越多。不同时间步对应不同的噪声水平（Noise Level），而这些噪声水平如何变化，则由噪声调度策略决定。</p>\n\n<p>前向扩散本身并不需要模型学习。训练时可以按照预先定义的规则直接构造不同噪声程度的数据。模型真正需要学习的是相反的过程，即如何从带噪数据中逐渐恢复出更加清晰的数据。生成时，模型从随机噪声出发，每一步只进行一定程度的去噪：随机噪声→初步结构→轮廓逐渐清晰→纹理逐渐形成→最终图像。这种机制将一次完成的复杂生成任务拆分成多个相对简单的局部修正任务。模型不需要一次预测出完整图像，而是不断根据当前状态进行调整，这也是扩散模型能够实现高质量生成的重要原因之一。</p>\n\n<h3 id=\"c25-s19\">DDPM 的训练与生成机制</h3>\n\n<p>经典 DDPM（Denoising Diffusion Probabilistic Model，去噪扩散概率模型）中建立了两个方向相反的过程：前向扩散过程不断向真实数据中加入噪声，而反向生成过程则学习如何逐步去除噪声，使随机噪声最终恢复为具有真实数据特征的图像。</p>\n\n<img src=\"assets/manuscript-20260914/c24-3cd45b5458e82c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p><span class=\"arithmatex\">\\(x_0\\)</span> 表示真实图像，随着前向扩散不断加入高斯噪声，数据依次经过 <span class=\"arithmatex\">\\(x_1,x_2,\\ldots,x_T\\)</span>，最终 <span class=\"arithmatex\">\\(x_T\\)</span> 接近随机高斯噪声。图中的虚线 <span class=\"arithmatex\">\\(q(x_t\\mid x_{t-1})\\)</span> 表示前向扩散过程，而 <span class=\"arithmatex\">\\(p_\\theta(x_{t-1}\\mid x_t)\\)</span> 表示模型需要学习的反向生成过程。前向过程可以理解为：<span class=\"arithmatex\">\\(x_0 \\rightarrow x_1 \\rightarrow x_2 \\rightarrow \\cdots \\rightarrow x_t \\rightarrow \\cdots \\rightarrow x_T\\)</span>，即从真实图像逐步转变为随机噪声；而真正生成图像时，则沿着相反方向进行：<span class=\"arithmatex\">\\(x_T \\rightarrow \\cdots \\rightarrow x_t \\rightarrow x_{t-1} \\rightarrow \\cdots \\rightarrow x_0\\)</span>，即从随机噪声逐步恢复为最终图像。DDPM 的关键问题是如何让模型学会从当前的 <span class=\"arithmatex\">\\(x_t\\)</span> 得到噪声更少的 <span class=\"arithmatex\">\\(x_{t-1}\\)</span>。</p>\n\n<p>在 DDPM 中，反向过程由神经网络进行参数化。一种重要且常用的实现方式，是让神经网络预测 <span class=\"arithmatex\">\\(x_t\\)</span> 中所加入的噪声。训练时，从真实图像 <span class=\"arithmatex\">\\(x_0\\)</span> 出发，随机选择一个时间步 <span class=\"arithmatex\">\\(t\\)</span>，并采样高斯噪声<span class=\"arithmatex display-math\">\\(\\epsilon\\sim\\mathcal{N}(0,I)\n\\)</span>，根据前向扩散过程，可以直接构造时间步 <span class=\"arithmatex\">\\(t\\)</span> 对应的带噪图像：</p>\n\n<p><span class=\"arithmatex display-math\">\\(x_t\n=\n\\sqrt{\\bar{\\alpha}_t}x_0\n+\n\\sqrt{1-\\bar{\\alpha}_t}\\epsilon\n\\)</span></p>\n\n<p>随后，将带噪图像 <span class=\"arithmatex\">\\(x_t\\)</span> 和时间步 <span class=\"arithmatex\">\\(t\\)</span> 输入神经网络，由模型预测其中的噪声<span class=\"arithmatex\">\\(\\epsilon_\\theta(x_t,t)\n\\)</span>，这里的 <span class=\"arithmatex\">\\(\\theta\\)</span> 表示神经网络的参数。由于训练过程中加入的真实噪声 <span class=\"arithmatex\">\\(\\epsilon\\)</span> 是已知的，可以直接比较模型预测的噪声与真实噪声之间的差异。DDPM 的训练过程可以理解为：先主动给真实图像加入不同程度的噪声，再训练模型识别其中加入了哪些噪声。模型经过大量训练后，就能够处理不同时间步、不同噪声程度的数据，并利用噪声预测结果构造图中所示的反向过程 <span class=\"arithmatex\">\\(p_\\theta(x_{t-1}\\mid x_t)\\)</span>。生成阶段则不再需要真实图像，是直接从高斯噪声开始，模型首先根据 <span class=\"arithmatex\">\\(x_T\\)</span> 得到噪声更少的 <span class=\"arithmatex\">\\(x_{T-1}\\)</span>，再根据 <span class=\"arithmatex\">\\(x_{T-1}\\)</span> 得到 <span class=\"arithmatex\">\\(x_{T-2}\\)</span>，如此反复。<span class=\"arithmatex display-math\">\\(x_T\n\\rightarrow\nx_{T-1}\n\\rightarrow\nx_{T-2}\n\\rightarrow\n\\cdots\n\\rightarrow\nx_1\n\\rightarrow\nx_0\n\\)</span></p>\n\n<p>随着反向过程不断进行，随机噪声中逐渐出现图像的整体结构、轮廓和纹理，最终得到完整的生成图像。DDPM 并不是让模型一次从噪声预测出完整图像，是学习多个噪声水平下的去噪能力，再通过多次反向迭代逐渐完成生成。 这种方式降低了单次生成任务的难度，也避免了 GAN 中生成器与判别器之间的对抗训练。不过，多步采样意味着生成一张图像需要多次执行神经网络推理，因此推理速度较慢也成为经典 DDPM 的主要局限之一。</p>\n\n<h3 id=\"c25-s20\">从 DDPM 到 Flow Matching</h3>\n\n<p>DDPM 将图像生成描述为一个逐步去噪过程，训练时向真实数据中加入噪声，并让模型学习预测噪声；生成时则从随机噪声出发，通过多次反向去噪逐渐得到图像。Flow Matching 采用了另一种描述方式。在随机噪声分布与真实数据分布之间构造一条连续的概率路径，并训练模型学习这条路径上的变化方向，使随机噪声能够沿着学习到的方向逐渐转换为真实数据。</p>\n\n<img src=\"assets/manuscript-20260914/c24-10acafbccb6673.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>从实现过程来看，Flow Matching 主要包括路径构造、速度场学习和生成采样三个阶段。</p>\n\n<p>1）构造连续的数据路径</p>\n\n<p>训练时，首先从真实数据分布中采样一个样本 <span class=\"arithmatex\">\\(x_0\\)</span>，同时从标准高斯分布中采样随机噪声 <span class=\"arithmatex\">\\(\\epsilon\\)</span>，为了建立真实数据与随机噪声之间的联系，可以在二者之间构造一系列连续的中间状态。一种较为直观的方式是采用线性插值：</p>\n\n<p><span class=\"arithmatex display-math\">\\(x_t=(1-\\sigma_t)x_0+\\sigma_t\\epsilon\n\\)</span>其中，<span class=\"arithmatex\">\\(\\sigma_t\\)</span> 用于控制真实数据与随机噪声在当前状态中的比例。为便于说明，可以令：</p>\n\n<p><span class=\"arithmatex display-math\">\\(\\sigma_0=0,\\qquad \\sigma_1=1\n\\)</span>当 <span class=\"arithmatex\">\\(\\sigma_t=0\\)</span> 时：<span class=\"arithmatex display-math\">\\(x_t=x_0\n\\)</span>此时对应真实数据；当 <span class=\"arithmatex\">\\(\\sigma_t=1\\)</span> 时：<span class=\"arithmatex display-math\">\\(x_t=\\epsilon\n\\)</span>此时对应随机噪声。</p>\n\n<p>随着 <span class=\"arithmatex\">\\(\\sigma_t\\)</span> 从 0 逐渐增加到 1，<span class=\"arithmatex\">\\(x_t\\)</span> 可以表示真实数据与随机噪声之间的一系列连续中间状态：真实数据 → 少量噪声 → 中等噪声 → 大量噪声 → 随机噪声，训练时通过随机选择不同的 <span class=\"arithmatex\">\\(t\\)</span>，可以得到不同位置的中间状态，从而为模型学习整条路径上的变化规律提供训练样本。与 DDPM 中预先定义的逐步加噪过程相比，Flow Matching 更关心如何定义连接两个分布的连续路径，以及数据沿这条路径应该如何变化。</p>\n\n<p>2）学习路径上的速度场</p>\n\n<p>构造出连续路径后，下一步需要确定数据在路径上的变化方向和变化速度。Flow Matching 使用神经网络学习一个速度场（Velocity Field）<span class=\"arithmatex display-math\">\\(v_\\theta(x_t,t)\n\\)</span>\n其中，<span class=\"arithmatex\">\\(x_t\\)</span> 表示当前中间状态，<span class=\"arithmatex\">\\(t\\)</span> 表示当前所处的时间位置，<span class=\"arithmatex\">\\(v_\\theta(x_t,t)\\)</span> 表示模型预测的当前位置对应的变化速度。对于前面的线性路径：</p>\n\n<p><span class=\"arithmatex display-math\">\\(x_t=(1-\\sigma_t)x_0+\\sigma_t\\epsilon\n\\)</span></p>\n\n<p>训练过程中，可以根据已知的真实数据 <span class=\"arithmatex\">\\(x_0\\)</span> 和随机噪声 <span class=\"arithmatex\">\\(\\epsilon\\)</span> 得到目标速度，并训练神经网络根据当前状态 <span class=\"arithmatex\">\\(x_t\\)</span> 和时间 <span class=\"arithmatex\">\\(t\\)</span> 预测相应的速度。Flow Matching 的训练目标也就是给定路径上的中间状态 <span class=\"arithmatex\">\\(x_t\\)</span> 和时间 <span class=\"arithmatex\">\\(t\\)</span>，训练模型预测该位置对应的数据变化速度。当模型在大量训练样本上学习之后，不同位置对应的速度共同构成一个速度场。这个速度场描述了数据空间中不同位置应该如何变化，从而为后续从随机噪声生成数据提供方向。</p>\n\n<p>3）沿速度场生成数据</p>\n\n<p>训练阶段建立了真实数据与随机噪声之间的路径，并学习了路径上的速度场。生成阶段则从随机噪声出发，利用学习到的速度场逐渐向真实数据分布移动。这一过程可以表示为：随机噪声 → 中间状态 → 数据结构逐渐形成 → 最终生成数据，<span class=\"arithmatex display-math\">\\(x_{t-1}\n=\nx_t+\n(\\sigma_{t-1}-\\sigma_t)\nv_\\theta(x_t,t)\n\\)</span>模型在每个时间步根据当前状态重新预测速度，并更新 <span class=\"arithmatex\">\\(x_t\\)</span>。经过多次迭代后，初始随机噪声逐渐转变为具有完整结构和语义信息的生成数据。</p>\n\n<p>从建模方式来看，DDPM 与 Flow Matching 都以简单的随机分布作为生成起点，但二者对生成过程的描述有所不同。DDPM 主要通过前向扩散和反向去噪描述数据变化，通常学习噪声预测；Flow Matching 则通过构造连接噪声与真实数据的连续路径，学习路径上的速度场，再从随机噪声出发沿速度场逐步生成数据。</p>\n\n<h2 id=\"c25-s21\">文生图的演进</h2>\n\n<p>文生图（Text-to-Image）是指根据自然语言描述自动生成对应图像。与普通图像生成相比，文生图不仅要求模型能够生成具有较高视觉质量的图像，还需要正确理解文本中的主体、属性、数量、位置、风格以及不同对象之间的关系。在文生图的发展过程中，技术关注点也逐渐能否根据文字生成图像”，转向能否准确理解复杂指令，并以更高质量、更高效率和更强可控性完成生成。</p>\n\n<h3 id=\"c25-s22\">早期探索到扩散模型</h3>\n\n<p>早期文生图研究主要基于 GAN 等生成模型。其基本方法是先将文本编码为向量，再将文本特征作为条件输入生成器，使生成结果与文本描述保持一致。这一阶段证明了文生图的可行性，但早期 GAN 在复杂场景生成、文本语义理解以及训练稳定性等方面仍存在明显限制。特别是当提示词中包含多个主体、复杂空间关系或较长文本描述时，模型较难准确还原其中的全部语义。2021 年，OpenAI 发布 DALL·E，使用基于 Transformer 的自回归方式处理文本和图像 Token，使文生图模型在复杂概念组合、属性控制等方面取得明显进展，也推动了大规模文生图模型的发展。与此同时，扩散模型开始进入文生图领域。GLIDE 将文本条件与扩散模型结合，并研究了 CLIP Guidance 和 Classifier-Free Guidance 等文本引导方式，表明扩散模型能够在文本控制下生成具有较高视觉质量的图像。2022 年，Google 提出的 Imagen 进一步将大型语言模型的文本理解能力与扩散模型的图像生成能力结合起来。Imagen 使用预训练的 T5 作为文本编码器，将 Prompt 转换为文本语义表示，再将其作为条件输入扩散模型。研究表明，增强文本编码器的语义理解能力，可以进一步提高生成图像与文本描述之间的一致性。随着 DALL·E、GLIDE 和 Imagen 等模型的发展，文生图逐渐形成了较为明确的技术流程：文生编码器→文本语义表示→扩散生成模型→逐步去噪→生成图像；扩散模型凭借较好的生成质量和训练稳定性，逐渐成为文生图领域的重要技术路线。</p>\n\n<h3 id=\"c25-s23\">潜在扩散与 Stable Diffusion</h3>\n\n<p>早期扩散模型通常直接在像素空间中进行多次加噪和去噪。当图像分辨率不断提高时，直接处理大量像素会带来较高的训练和推理成本。2022 年，Rombach 等人提出潜在扩散模型（Latent Diffusion Model，LDM），将扩散过程从像素空间转移到经过压缩的潜在空间中，可以参考 14.4.3 中 LDM 介绍。Stable Diffusion 的重要意义不仅在于提高了文生图质量，更在于推动了开放权重文生图生态的发展。围绕其潜在扩散架构，逐渐形成了 LoRA、ControlNet、图生图、局部重绘等大量扩展技术，使文生图从单纯的图片生成逐渐发展为完整的视觉内容生产体系。</p>\n\n<h3 id=\"c25-s24\">从 U-Net 到 Diffusion Transformer</h3>\n\n<p>早期扩散模型通常采用 U-Net 作为去噪网络。U-Net 通过多尺度特征提取和跳跃连接，能够较好地保留图像的空间结构，因此长期作为扩散模型的重要骨干网络。随着生成模型规模不断扩大，研究者开始探索使用 Transformer 替代 U-Net，以获得更好的模型扩展能力。2022 年，Peebles 和 Xie 提出了 Diffusion Transformer（DiT），使用 Transformer 替代扩散模型中常用的 U-Net 骨干网络。模型首先将潜在空间中的图像表示划分为多个 Patch，并将其转换为 Token 序列，再利用 Transformer 对这些 Token 进行建模，DIT 架构图如下：</p>\n\n<img src=\"assets/manuscript-20260914/c24-b4e79a92f7702a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>DiT 的研究进一步表明，可以通过增加 Transformer 的深度、宽度以及输入 Token 数量扩大模型的计算规模。在原论文实验中，随着模型计算量增加，生成质量总体呈现持续改善的趋势，说明 Transformer 架构在扩散模型中具有较好的可扩展性。DiT 的意义并不仅在于将 U-Net 替换为 Transformer，更重要的是为后续大规模图像生成模型提供了一种新的基础架构。此后，Transformer 逐渐成为文生图模型的重要技术路线，并进一步发展出同时处理文本 Token 与图像 Token 的多模态 Transformer 架构，为 Stable Diffusion 3、FLUX.1 等新一代文生图模型的发展奠定了基础。</p>\n\n<h3 id=\"c25-s25\">Flow Matching 与新一代文生图模型</h3>\n\n<p>随着 Transformer 逐渐成为文生图模型的重要骨干网络，图像生成过程本身也在不断演进。前面介绍的 DDPM 主要通过“前向加噪—学习反向过程—逐步去噪”描述数据生成，而 Flow Matching 则从连续概率路径和速度场的角度描述简单分布向真实数据分布的转换过程。2024 年，Stable Diffusion 3 相关研究将 Rectified Flow 与 Transformer 进一步结合，用于高分辨率文生图。该工作提出了新的 Multimodal Diffusion Transformer（MMDiT）架构。</p>\n\n<img src=\"assets/manuscript-20260914/c24-56b3814a5f109c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>与早期 DiT 主要处理图像潜在 Token 不同，MMDiT 分别对文本和图像表示使用独立的参数进行处理，同时通过联合注意力实现两种模态之间的信息交互，从而增强模型对文本内容、文字渲染以及复杂提示词的理解能力。同年发布的 FLUX.1 延续了 Transformer 与 Flow Matching 相结合的技术路线。FLUX.1 系列模型采用由多模态 Transformer Block 和并行 Diffusion Transformer Block 组成的混合架构，并基于 Flow Matching 进行训练。公开的 FLUX.1 模型规模达到 12B 参数，进一步体现了 Transformer 图像生成模型向大规模化发展的趋势。从这一阶段开始，文生图模型的技术重点已经不再局限于提高图像的视觉质量，而是逐渐向复杂 Prompt 理解、图中文字生成、高分辨率生成、少步采样和图像编辑等方向扩展。文本与图像也开始更多地以 Token 的形式进入 Transformer，通过注意力机制实现跨模态信息交互。这一变化也为后续更加高效和统一的多模态生成模型奠定了基础。</p>\n\n<h3 id=\"c25-s26\">高效生成与统一多模态的发展</h3>\n\n<p>随着文生图模型规模和生成质量不断提高，模型的实际应用开始面临新的问题。一方面，大规模生成模型通常需要较高的计算资源和较多的采样步骤；另一方面，实际 AIGC 应用已经不再局限于单一的文生图任务，还需要同时支持图像理解、图像生成、图像编辑以及参考图生成等能力。因此，降低生成成本和统一多模态能力逐渐成为文生图模型的重要发展方向。</p>\n\n<p>Z-Image 系列采用 Scalable Single-Stream Diffusion Transformer（S3-DiT） 架构，模型规模为 6B 参数。S3-DiT 的一个重要特点是采用单流（Single-Stream）架构,将文本 Token、视觉语义 Token 和图像 VAE Token 在序列维度进行拼接，然后输入统一的 Transformer 进行处理。与分别处理不同模态的双流结构相比，这种方式能够在统一的数据流中完成不同类型信息之间的交互。Z-Image-Turbo 体现了现代文生图模型的一个重要发展方向：在保持较强生成能力的同时，通过模型蒸馏和少步采样降低推理成本，使大规模 Transformer 图像生成模型更加适合实际部署。</p>\n\n<img src=\"assets/manuscript-20260914/c24-1e5b6feb2fabfb.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>SenseNova-U1.5-8B-MoT 从文生图走向统一多模态，除了提高生成效率之外，另一个重要方向是将过去相对独立的多模态理解与多模态生成能力进行统一。2026 年发布的 SenseNova-U1 提出了 NEO-unify 架构，尝试在统一模型中同时实现多模态理解和生成。在此基础上，SenseNova-U1.5-8B-MoT 进一步增强了图像生成和编辑能力。根据官方发布信息，正式版 SenseNova-U1.5-8B-MoT 于 2026 年 8 月发布，并进一步提升了指令遵循、文字与布局生成、原生 4K 图像生成、图像编辑和视觉控制等能力。</p>\n\n<img src=\"assets/manuscript-20260914/c24-72e3e81141b366.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>SenseNova-U1.5 建立在 NEO-unify 统一多模态架构之上，并采用新的图像 Patch 编码和解码机制。模型不仅可以根据文本描述生成图像，还可以结合输入图像与自然语言指令完成图生图、图像编辑以及视觉控制等任务。这意味着文生图模型正在逐渐从单一的内容生成工具，发展为能够同时处理理解与生成任务的统一多模态模型。</p>\n\n<p></p>\n","imageCount":14,"attachmentCount":0,"discussionId":"chapter-24"},{"id":"chapter-26","number":26,"title":"Agent 是什么，它能做什么事？","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-26.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-26.html","minutes":9,"status":"ready","headings":[{"id":"c26-s1","text":"Agent 到底是什么，和普通问答有什么不同？","level":"h2"},{"id":"c26-s2","text":"一个 Agent，需要哪些部分配合？","level":"h2"},{"id":"c26-s3","text":"LLM，理解任务并判断下一步","level":"h3"},{"id":"c26-s4","text":"Planning，把任务拆成可执行的步骤","level":"h3"},{"id":"c26-s5","text":"Memory，记住任务进展和需要的信息","level":"h3"},{"id":"c26-s6","text":"Tools，让模型能够实际执行操作","level":"h3"},{"id":"c26-s7","text":"接到任务以后，Agent 怎样一步步做下去？","level":"h2"},{"id":"c26-s8","text":"ReAct 模式","level":"h3"},{"id":"c26-s9","text":"Plan-and-Execute 模式","level":"h3"},{"id":"c26-s10","text":"Reflection 模式","level":"h3"},{"id":"c26-s11","text":"多 Agent 协作模式","level":"h3"}],"html":"<h2 id=\"c26-s1\">Agent 到底是什么，和普通问答有什么不同？</h2>\n\n<p>Agent（智能体）是一种以大模型为核心，能够围绕任务目标自主进行决策和执行的应用系统。大模型为 Agent 提供语言理解、推理和内容生成等基础能力，使其能够理解用户提出的任务并分析当前信息。在此基础上，Agent 进一步将模型的推理能力用于任务执行：面对一个目标，它不仅需要生成回答，还需要判断为了完成目标应该做什么，并通过工具获取外部信息或执行具体操作。当任务包含多个步骤时，Agent 还可以利用前一步获得的结果继续判断下一步操作，必要时调整原有计划，直到任务完成。因此，Agent 关注的重点从“根据输入生成什么内容”扩展到了“围绕目标如何完成任务”。这里所说的自主决策并不是完全不受限制地行动，而是在预先设定的任务范围、工具和权限内，根据当前状态选择合适的操作。所以说，Agent 不是一种独立于大模型的新模型，而是以大模型作为理解和决策核心，并将模型与外部能力结合起来形成的任务执行系统。</p>\n\n<p>Agent 适合处理包含多个步骤、需要使用外部信息或工具，并且执行过程可能根据中间结果发生变化的任务，常见的应用场景包括信息检索、数据分析、软件开发和业务自动化等。例如，在信息检索场景中，Agent 可以根据任务搜索并整理不同来源的资料；在数据分析场景中，可以读取数据、完成计算并生成分析结果；在软件开发场景中，可以编写代码、执行测试并根据运行结果继续修改；在业务自动化场景中，可以连接数据库和业务系统，完成数据查询、工单处理等操作。对于翻译、摘要、改写等可以直接通过模型生成完成的任务，通常不需要使用 Agent。</p>\n\n<h2 id=\"c26-s2\">一个 Agent，需要哪些部分配合？</h2>\n\n<img src=\"assets/manuscript-20260914/c33-30e5b32a2aac59.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c26-s3\">LLM，理解任务并判断下一步</h3>\n\n<p>Agent 的核心，负责处理用户输入、生成响应和进行决策等，相当于 Agent 的“大脑”。因为 Agent 不是按预设路径行动，而是根据实时情况动态调整其行为策略，所以 LLM 不仅负责理解和生成内容，还需要根据当前任务和执行结果判断下一步操作，决定如何与外部环境进行交互。Agent 通过 Prompt（提示词）规定 LLM 的角色、任务目标、行为规则和权限边界，让 LLM 在预先设定的范围内进行决策和执行。</p>\n\n<h3 id=\"c26-s4\">Planning，把任务拆成可执行的步骤</h3>\n\n<p>负责制定 Agent 的行动计划，包括将复杂任务分解为多个子任务，并确定后续的执行步骤。Agent 在处理多步骤任务时，不仅需要制定行动计划，还需要根据任务的执行情况进行调整。例如，当工具返回的结果与预期不同，或者获得的信息不足时，Agent 可以调整原有计划，重新确定下一步操作。Planning 模块使 Agent 能够根据任务状态动态规划和调整后续行动。</p>\n\n<h3 id=\"c26-s5\">Memory，记住任务进展和需要的信息</h3>\n\n<p>用于存储 Agent 的记忆，包括短期记忆和长期记忆。短期记忆主要保存当前会话或任务中的信息，长期记忆则用于保存需要长期使用的信息。记忆是一个动态、可访问且可更新的系统。动态是指记忆可以随着新的信息和任务状态发生变化；可访问是指 Agent 能够根据需要检索和使用存储在记忆中的信息；可更新则是指 Agent 可以根据新的信息和执行结果对已有记忆进行补充或修改。随着记忆不断更新，Agent 可以利用交互和任务经验改进后续的判断和决策，提升对不同任务和环境的适应能力。</p>\n\n<h3 id=\"c26-s6\">Tools，让模型能够实际执行操作</h3>\n\n<p>Agent 可以使用的各种工具，包括搜索引擎、数据库、计算器、代码执行环境以及通过 API 提供的各种外部服务。Tools 模块的核心作用是扩展 Agent 与外部环境交互的能力，使 Agent 不仅能够生成内容，还能够获取外部信息和执行具体操作。</p>\n\n<p>在使用 Tools 模块时，Agent 需要根据当前任务和上下文判断是否需要使用工具，以及选择哪个工具。有效地使用工具不仅指选择正确的工具，还包括怎么高效地利用工具，减少不必要的工具使用。当任务需要多个工具配合完成时，Agent 还需要根据工具返回的结果决定是否继续调用其他工具，并对不同工具的结果进行整合。因此，Tools 模块不仅为 Agent 提供外部能力，还要求 Agent 能够根据任务需要合理选择和使用工具。</p>\n\n<p>Agent 是一个集成了 LLM、Planning、Memory 和 Tools 的系统，如下图所示。这些模块共同协作，使 Agent 能够自主执行任务、学习并改进其行为。</p>\n\n<p></p>\n\n<h2 id=\"c26-s7\">接到任务以后，Agent 怎样一步步做下去？</h2>\n\n<p>Agent 接收到任务后，需要根据任务目标和当前信息决定如何推进任务。例如，有些任务可以一边调用工具一边根据返回结果决定下一步操作；有些复杂任务需要先拆分步骤，再逐步执行；对于生成的结果，还可以进一步检查和修改。当一个 Agent 难以完成全部任务时，也可以由多个 Agent 分工协作。</p>\n\n<p>根据任务特点和执行方式不同，Agent 可以采用不同的任务执行模式。下面介绍 ReAct、Plan-and-Execute、Reflection 和多 Agent 协作四种常见模式，它们分别从动态执行、任务规划、结果改进和任务分工等方面组织 Agent 的任务执行过程。</p>\n\n<h3 id=\"c26-s8\">ReAct 模式</h3>\n\n<p>ReAct（Reasoning and Acting）是一种将推理和行动结合起来的任务执行方式，由 Shunyu Yao 等人提出。大模型根据当前信息进行推理并采取行动，再根据行动获得的新信息继续推理和行动，直到任务完成。</p>\n\n<p>ReAct 的典型过程包括 Thought（推理）、Action（行动）和 Observation（观察）。其中，Thought 表示模型根据当前信息进行分析，判断下一步应该做什么；需要获取外部信息或执行某项操作时，模型生成相应的 Action，例如调用搜索工具或查询数据库；工具执行后返回的结果作为 Observation 提供给模型，模型再结合这些新信息继续进行判断。</p>\n\n<p>在任务执行过程中，Thought、Action 和 Observation 会根据任务需要交替出现，形成推理、行动和观察相互配合的执行过程。其典型过程可以表示为：</p>\n\n<p>Thought → Action → Observation → Thought → Action → Observation → …… → 最终结果</p>\n\n<p>例如，用户询问：“2024 年诺贝尔物理学奖获得者是谁？其中年龄最大的是哪一位？”</p>\n\n<p>Agent 首先判断需要获得 2024 年诺贝尔物理学奖的获奖名单，因此调用搜索工具进行查询。获得名单后，发现还需要比较获奖者的年龄，于是继续查询他们的出生日期。获得所需信息后，Agent 再进行比较并生成最终答案。</p>\n\n<p>在这个过程中，Agent 并没有在任务开始时确定所有执行步骤，而是根据每次工具返回的结果决定下一步操作。因此，ReAct 比较适合搜索、查询、工具调用等需要与外部环境不断交互的任务。对于步骤较多的复杂任务，如果完全采用这种边执行边决定下一步的方式，可能出现遗漏步骤或执行路径反复等情况。</p>\n\n<h3 id=\"c26-s9\">Plan-and-Execute 模式</h3>\n\n<p>Plan-and-Execute 将复杂任务的处理分为规划和执行两个阶段。Agent 接收到任务后，先分析任务目标并拆分出需要完成的步骤，形成整体计划，然后再按照计划逐步执行。</p>\n\n<p>例如，用户要求：“分析三家新能源汽车企业最近一年的经营情况，并形成一份比较报告。”这个任务涉及资料收集、数据整理、对比分析和报告生成等多个环节。对于这类步骤较多的任务，可以先形成如下计划：</p>\n\n<ol><li> 确定需要比较的三家企业； </li><li> 收集各企业最近一年的经营数据； </li><li> 整理销量、营收等关键指标； </li><li> 对各企业的经营情况进行比较； </li><li> 总结差异并生成报告。 </li></ol>\n\n<p>计划形成后，Agent 再依次完成其中的任务。在执行某个步骤时，仍然可以调用搜索、数据库或其他工具。如果执行过程中发现原来的计划无法继续，或者出现了新的任务需求，还可以对原有计划进行调整。</p>\n\n<p>Plan-and-Execute 的典型执行过程可以表示为：</p>\n\n<p>用户任务 → 制定计划 → 逐步执行 → 检查任务状态 → 最终结果</p>\n\n<p>其中，在检查任务状态时，如果发现原有计划需要调整，可以重新进行规划，再继续执行。</p>\n\n<p>与 ReAct 相比，Plan-and-Execute 更强调在执行前建立整体计划，因此适合步骤较多、任务目标明确并且能够提前进行拆分的任务。两种模式也可以结合使用，例如先使用 Plan-and-Execute 规划复杂任务，在执行其中某个步骤时，再使用 ReAct 根据工具返回结果动态决定下一步操作。</p>\n\n<h3 id=\"c26-s10\">Reflection 模式</h3>\n\n<p>Agent 第一次生成的结果并不一定能够满足任务要求。例如，生成的报告可能遗漏重要信息，回答可能存在事实错误，代码也可能无法正常运行。对于这类任务，可以在初步结果的基础上增加检查和改进过程，这就是 Reflection（反思）模式的基本思路。</p>\n\n<p>Reflection 的基本过程可以表示为：用户任务 → 生成初步结果 → 检查结果 → 发现问题 → 修改结果 → 再次检查→…… → 满足要求→ 最终结果。</p>\n\n<p>例如，让 Agent 根据多份资料生成一份产品比较报告。完成初稿后，可以进一步检查是否覆盖了所有需要比较的产品、产品参数是否与原始资料一致、是否遗漏重要差异等。如果发现某款产品的信息不完整，可以重新查找资料并补充；如果结论与资料不一致，则需要重新修改。</p>\n\n<p>在这个过程中，检查结果所依据的反馈可以来自不同来源。Agent 可以检查自己的执行结果，也可以使用其他 Agent 或模型进行评估，还可以结合规则、知识库或人工审核等方式提供反馈。</p>\n\n<p>这种模式比较适合报告生成、代码编写、复杂分析等对结果质量要求较高的任务。不过，多次检查和修改也会增加模型调用次数和执行时间，因此实际应用中通常需要设置结束条件，例如结果达到要求后结束，或者达到最大检查次数后停止。</p>\n\n<h3 id=\"c26-s11\">多 Agent 协作模式</h3>\n\n<p>对于一些复杂任务，一个 Agent 可能需要同时承担资料收集、数据分析、内容生成和结果检查等多项工作。此时，可以将任务分配给多个 Agent，由它们分别处理不同的部分，再通过协作完成最终目标。多 Agent 协作主要包括任务分工和信息交互两个方面。</p>\n\n<p>任务分工时，为不同 Agent 分配不同的角色、任务、工具和知识。例如，要完成一份行业研究报告，可以设置研究 Agent、数据分析 Agent、写作 Agent 和审核 Agent。研究 Agent 负责收集和整理资料，数据分析 Agent 负责处理相关数据，写作 Agent 负责生成报告，审核 Agent 负责检查报告。</p>\n\n<p>在信息交互方面，多 Agent 之间可以采用不同的协作方式。对于能够明确拆分的任务，可以让多个 Agent 分别处理不同的子任务，最后汇总结果；对于前后存在依赖关系的任务，可以将一个 Agent 的结果传递给下一个 Agent 继续处理；对于需要反复讨论或修改的任务，不同 Agent 还可以通过消息交互交换信息。此外，还可以设置一个负责协调的 Agent，由它根据当前任务决定接下来由哪个 Agent 处理。</p>\n\n<p>以生成行业研究报告为例，一种多 Agent 协作方式如下图所示：</p>\n\n<img src=\"assets/manuscript-20260914/c33-b0f78b4aaff740.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>多 Agent 能够通过分工降低单个 Agent 处理复杂任务的难度，但 Agent 数量并不是越多越好。随着 Agent 数量增加，任务分配、信息传递和结果协调也会变得更加复杂，同时会增加模型调用次数和执行成本。因此，对于单个 Agent 能够完成的任务，通常没有必要额外引入多个 Agent。</p>\n\n<p>这些任务执行模式并不是相互独立的，在实际应用中可以根据任务需要组合使用。例如，可以先使用 Plan-and-Execute 规划复杂任务，再使用 ReAct 完成其中需要多次工具调用的步骤，并通过 Reflection 检查和改进结果；对于规模更大的任务，还可以将不同子任务分配给多个 Agent 协作完成。</p>\n\n<p></p>\n","imageCount":2,"attachmentCount":0,"discussionId":"chapter-33"},{"id":"chapter-27","number":27,"title":"MCP：让模型连接外部工具","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-27.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-27.html","minutes":30,"status":"ready","headings":[{"id":"c27-s1","text":"MCP 是什么","level":"h2"},{"id":"c27-s2","text":"MCP 解决什么问题","level":"h2"},{"id":"c27-s3","text":"MCP 的基本架构","level":"h2"},{"id":"c27-s4","text":"Host：承载模型并控制整体过程","level":"h3"},{"id":"c27-s5","text":"Client：维护与单个 Server 的协议连接","level":"h3"},{"id":"c27-s6","text":"Server：把外部能力封装成标准接口","level":"h3"},{"id":"c27-s7","text":"两种常见连接方式","level":"h3"},{"id":"c27-s8","text":"MCP 与 API 的区别","level":"h2"},{"id":"c27-s9","text":"在魔搭中配置 MCP","level":"h2"},{"id":"c27-s10","text":"实验目标与运行流程","level":"h3"},{"id":"c27-s11","text":"准备魔搭 Notebook 实验环境","level":"h3"},{"id":"c27-s12","text":"启动 Qwen3-4B 推理服务","level":"h3"},{"id":"c27-s13","text":"编写天气查询 MCP 工具","level":"h3"},{"id":"c27-s14","text":"启动并注册天气 MCP Server","level":"h3"},{"id":"c27-s15","text":"使用 MCP 广场中的托管服务","level":"h3"},{"id":"c27-s16","text":"调用 MCP 工具","level":"h2"},{"id":"c27-s17","text":"发现并直接验证工具","level":"h3"},{"id":"c27-s18","text":"让 Qwen3-4B 调用天气 MCP","level":"h3"},{"id":"c27-s19","text":"处理工具返回与最终回答","level":"h3"},{"id":"c27-s20","text":"调用 MCP 广场中的 fetch 服务","level":"h3"},{"id":"c27-s21","text":"MCP 权限管理","level":"h2"},{"id":"c27-s22","text":"MCP 安全风险","level":"h2"},{"id":"c27-s23","text":"MCP 适合的场景","level":"h2"}],"html":"<p>大模型可以根据训练中学到的知识回答问题，也可以根据当前对话中的材料完成摘要、分类和内容生成。但是，模型通常不知道企业数据库里刚刚更新的订单状态，也不能只凭一段 Prompt 就真正读取本地文件、创建日程或者修改项目任务。要完成这些工作，模型需要通过应用连接外部数据和工具。</p>\n\n<p>过去，为每一种模型应用连接文件系统、数据库和办公软件，往往都要单独编写一套适配代码。换一个 Agent 工具，原来的接口还可能无法直接复用。MCP 提供了一种标准化的连接方式，使支持该协议的应用能够发现外部能力、了解参数要求并调用这些能力。</p>\n\n<p>重点介绍 MCP 的基本概念、架构、配置和调用过程，同时说明连接外部系统后必须建立的权限与安全边界。</p>\n\n<h2 id=\"c27-s1\"><b>MCP 是什么</b></h2>\n\n<p>MCP（Model Context Protocol），即模型上下文协议。它是一套面向大模型应用的开放协议，用来规范应用怎样连接外部数据源、工具和系统，以及双方怎样描述能力、传递参数和返回结果。这里所谓的“连接模型”，实际执行协议的通常不是模型参数本身，而是承载模型的应用。用户在聊天界面、编程工具或 Agent 平台中提出任务，应用根据需要连接一个或多个 MCP Server，再把可用工具和返回结果提供给模型。模型负责理解任务和决定下一步，Host 与 Client 负责协议通信、权限控制和结果转交，Server 负责执行具体能力。MCP 可以协助开发者能够<b>以一致的方式将各种数据源、工具和功能连接到 AI 模型</b>，就像 USB-C 让不同设备能够通过相同的接口连接一样，相关样例如下图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c25-8d70af57c23ef8.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>MCP Server 可以向应用提供三类常见能力，分别是 Resources、Prompts 和 Tools。Resources 主要用于向应用提供可读取并放入上下文的数据，例如文件内容、数据库记录和代码仓库信息；Prompts 主要由服务端提供可复用的提示模板或交互入口，例如代码审查模板、日报生成模板等；Tools 则用于提供可以被应用调用并实际执行操作的函数能力，例如搜索网页、读取文件、创建日程或更新任务。</p>\n\n<p>一个 MCP 工具通常会说明工具名称、功能描述、输入参数结构以及可能的返回内容。应用不需要预先把每个工具写死在代码中，而是可以先询问 Server 有哪些工具，再根据返回的结构调用其中一个工具。</p>\n\n<p></p>\n\n<h2 id=\"c27-s2\"><b>MCP 解决什么问题</b></h2>\n\n<p>以一个项目管理 MCP 服务为例，可能提供下面三个工具 list_projects、get_project_tasks 和 get_project_tasks。其中，list_projects 和 get_project_tasks 工具主要用于读取信息，update_task_status 工具会改变外部系统。它们都可以通过 MCP 暴露，在用户运行和相关授权的情况下，模型可以自动调用、组织和决策。其中 MCP 解决的是怎样连接和调用的问题，利用 MCP，大模型或 Agent 就不再需要关心后面连接的是文件、数据库、搜索引擎还是企业系统，只要通过 MCP 就能发现并调用这些能力。因此，可以把 MCP 理解成大模型连接外部世界的“通用插口”，集合 MCP 系统可以进一步降低了不同模型、工具和业务系统之间反复适配的开发成本。是否采用 MCP 结构间的差异如下图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c25-bc760b9d03689e.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>例如，我们构建的 3 种 Agent 应用，可能需要连接文件系统、知识库、数据库、项目管理和邮件系统等外部能力，采用 MCP 后，外部能力可以通过 MCP Server 按照统一方式暴露。支持 MCP 的 Host 只需要实现协议客户端，就可以连接不同 Server。这样做主要减少了下面几类重复工作：</p>\n\n<p>1. <b>能力发现，</b>Host 可以获取 Server 提供的工具列表，不必为每个工具单独编写固定菜单。</p>\n\n<p>2. <b>参数描述，</b>工具可以使用结构化 Schema 说明必填字段、字段类型和可选值，便于 Host 校验，也便于模型选择正确参数。</p>\n\n<p>3. <b>调用与返回，</b>不同工具使用统一的协议消息完成请求、响应、错误和通知，不需要每个应用重新定义一套交互格式。</p>\n\n<p>4. <b>复用与替换，</b>同一个 MCP Server 可以被多个兼容应用使用；更换底座模型时，只要 Host 仍然支持 MCP，外部工具层通常不需要全部重写。</p>\n\n<p>5. <b>边界集中管理，</b>Host 可以在统一位置管理 Server 连接、用户确认、日志、超时和可用范围，Server 也可以在执行侧再次检查身份和权限。</p>\n\n<p>当然，MCP 并没有消除外部系统原有的开发工作。结合实际业务场景，数据库仍然需要表结构和查询权限，业务系统仍然需要 API，邮件服务仍然需要身份认证。</p>\n\n<h2 id=\"c27-s3\"><b>MCP 的基本架构</b></h2>\n\n<p>MCP 采用 Host、Client 和 Server 组成的架构。一个 Host 可以连接多个 Server，通常会为每个 Server 建立相对独立的 Client 连接。不同 Server 不应该默认看到完整对话，也不应该直接读取其他 Server 的返回内容；需要共享哪些上下文，由 Host 决定，架构的相关样例如下图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c25-63e7cae9c84caa.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c27-s4\"><b>Host：承载模型并控制整体过程</b></h3>\n\n<p>Host 是用户实际使用的应用，例如魔搭 Notebook 中运行的 Agent 应用、魔搭创空间中的在线助手或支持 MCP 的企业智能助手。Host 主要负责承载大模型并接收用户任务，同时管理一个或多个 MCP Client，根据当前任务决定向模型提供哪些工具和资源。Host 还会负责用户授权、确认提示和安全策略，并把工具调用结果重新加入当前任务上下文，让模型可以继续完成后续推理和生成。</p>\n\n<p>因此，Host 并不只是一个简单的聊天窗口，而是整个 MCP 调用过程的协调者。它负责把模型、用户、MCP 连接和权限策略组织在一起，同时记录调用过程，并处理失败、取消和超时等情况。即使某个 MCP Server 声明自己具备删除文件等高风险能力，Host 仍然可以选择不向模型暴露该工具，或者要求用户在真正执行操作前再次确认，从而对整个调用过程进行统一控制。</p>\n\n<h3 id=\"c27-s5\"><b>Client：维护与单个 Server 的协议连接</b></h3>\n\n<p>Client 位于 Host 内部，负责与某个 MCP Server 通信。它会按照协议建立连接、交换支持的能力、发送工具发现与调用请求，并把 Server 返回的结果交给 Host。用户通常不会直接操作 Client，但连接失败、协议版本不兼容和响应超时等问题，往往发生在这一层。</p>\n\n<p>为了保持边界清楚，一个 Client 通常只对应一个 Server。Host 可以同时创建多个 Client，例如一个连接文件系统 Server，一个连接数据库 Server，另一个连接项目管理 Server。Host 负责决定哪些信息可以在这些连接之间传递，而不是让多个 Server 自行共享全部上下文。</p>\n\n<p></p>\n\n<h3 id=\"c27-s6\"><b>Server：把外部能力封装成标准接口</b></h3>\n\n<p>Server 负责向 Client 提供工具（Tools）、外部资源（Resources）或提示词（Prompts）。它既可以作为应用运行环境中的进程提供能力，也可以独立部署为通过网络访问的服务。再魔搭中搭建和托管 Server，可以利用魔搭 Notebook 中的 Client 发起请求。Server 内部还可以继续调用普通 API、数据库驱动或其他业务服务。例如，订单 MCP Server 收到<code>query_order</code>工具调用后，可以使用企业已有的订单 API 查询数据，再把必要字段整理成工具结果返回。MCP Server 不需要替代原有订单系统，它负责把原有能力转换成模型应用容易发现和调用的形式。</p>\n\n<p>MCP Server 能够访问的信息并不是由 MCP 协议自动决定的，而是由具体的调用方式和权限边界决定。通常情况下，Server 只会收到 Host 为完成当前任务而传递的参数和上下文，以及在相关权限下的其他资源。</p>\n\n<p>结合上述介绍的 MCP 相关结构，MCP 本身提供的是标准化连接和调用机制，并不会自动扩大 Server 的权限范围，一个 MCP Server 最终能做什么，主要取决于三方面：Host 实际传给它什么信息、Server 运行环境本身拥有什么系统权限，以及它连接外部系统时使用了什么账号或令牌<b>。</b></p>\n\n<p></p>\n\n<h3 id=\"c27-s7\"><b>两种常见连接方式</b></h3>\n\n<p>MCP 中，Client 和 Server 之间通常使用 JSON-RPC 交换消息，而这些消息需要通过具体的连接方式传输。最常见的两种方式是 stdio 和 Streamable HTTP。stdio 适合本地场景，Client 会直接启动 MCP Server，并通过标准输入输出进行通信，通常用于本地工具、命令行程序或桌面应用。它的特点是部署简单、通信直接，但需要注意本地运行环境、进程权限和文件访问范围。</p>\n\n<p>Streamable HTTP 更适合远程或共享服务。MCP Server 可以独立部署在服务器上，Client 通过 HTTP 与它通信，适合团队共用工具、云端服务和企业内部系统。由于需要经过网络访问，因此要重点考虑 HTTPS、身份认证和访问权限。部分旧版本 MCP 服务可能仍使用 SSE，但目前应优先根据实际客户端和服务端支持情况选择 stdio 或 Streamable HTTP。</p>\n\n<p></p>\n\n<h2 id=\"c27-s8\"><b>MCP 与 API 的区别</b></h2>\n\n<p>API 和 MCP 不是互相替代的两种技术，它们解决的问题不一样。API 主要是给程序调用的，通常定义某个具体系统可以提供什么接口，比如查询订单、提交审批、发送邮件；而 MCP 更关注怎么把这些能力以大模型容易理解和调用的方式提供出来。也就是说，API 更像是底层能力接口，MCP 更像是在这些能力外面加了一层统一的说明和调用规范，让 Host 或 Agent 能自动发现有哪些工具、每个工具需要什么参数，以及应该怎样调用。以订单查询为例，企业可能本来就有一个“根据订单号查询订单状态”的 API。传统程序需要开发人员先阅读接口文档，再按照接口要求写代码调用；如果接入 MCP，可以由 MCP Server 在内部调用这个 API，同时把它包装成一个模型可理解的工具，比如“查询订单状态”，并说明需要传入“订单号”这个参数。这样，大模型不需要直接理解各种复杂接口，只需要按照 MCP 提供的工具描述进行调用即可。以下是一个工具的相关样例：</p>\n\n<pre data-language=\"JSON\"><code>{\n  \"name\": \"query_order\",\n  \"description\": \"根据订单编号查询订单状态和预计发货时间\",\n  \"inputSchema\": {\n    \"type\": \"object\",\n    \"properties\": {\n      \"order_id\": {\n        \"type\": \"string\",\n        \"description\": \"完整订单编号\"\n      }\n    },\n    \"required\": [\"order_id\"]\n  }\n}</code></pre>\n\n<p></p>\n\n<p>参考上述样例，业务 API 负责真正查询订单，MCP Server 负责把该能力描述成标准工具，MCP Client 负责发现并调用工具，Host 负责把工具提供给模型并控制权限。原有 API 不需要因为接入 MCP 而取消，如果某个程序或服务只需要固定调用一个 API，并不需要模型发现工具或根据任务选择操作，直接使用 API 通常更简单。MCP 则更适合需要让多个模型应用动态连接多种能力的场景。</p>\n\n<p></p>\n\n<h2 id=\"c27-s9\"><b>在魔搭中配置 MCP</b></h2>\n\n<p>魔搭社区提供 MCP 广场、Notebook、MS-Agent 等与 MCP 相关的能力。MCP 广场用于查找和了解已经发布的 Server，Notebook 提供云端实验环境，MS-Agent 则负责把大模型与 MCP 工具连接起来。除了使用广场中的现成 Server，开发者也可以将自己的业务函数封装成 MCP 工具。本节采用后一种方式：在魔搭 GPU Notebook 中启动 Qwen3-4B，并自建一个天气查询 MCP Server，完整验证模型发现和调用外部工具的过程。</p>\n\n<p></p>\n\n<h3 id=\"c27-s10\"><b>实验目标与运行流程</b></h3>\n\n<p>本实验要求用户只用自然语言提出“查询杭州明天的天气”，由 Qwen3-4B 选择天气工具并生成参数。天气 MCP 根据地区和日期调用公开天气 API，再把结构化结果返回给模型。整个实验均在同一个魔搭 GPU Notebook 实例中完成。</p>\n\n<pre data-language=\"Plain Text\"><code>用户提出天气问题\n    ↓\nQwen3-4B判断是否需要调用工具\n    ↓\nMS-Agent向天气MCP发送工具名称和参数\n    ↓\n天气MCP调用Open-Meteo公开天气API\n    ↓\nMCP返回结构化数据，Qwen3-4B组织最终回答</code></pre>\n\n<p>在这条链路中，魔搭 Notebook 是运行环境，MS-Agent 承担 Host 和 Client 侧的工作，自建的天气服务是 MCP Server，Open-Meteo 则是 Server 在执行工具时访问的外部 API。模型不直接请求天气网站，也不凭训练数据猜测天气，而是根据 MCP 返回的本次查询结果作答。</p>\n\n<p></p>\n\n<h3 id=\"c27-s11\"><b>准备魔搭 Notebook 实验环境</b></h3>\n\n<p></p>\n\n<p>创建魔搭 Notebook 实例时，配置相关环境，本次实验验证时使用的主要版本如下：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col></colgroup><tbody><tr><td><p>组件</p></td><td><p>实验版本</p></td></tr><tr><td><p>Python</p></td><td><p>3.12.13</p></td></tr><tr><td><p>ms-agent</p></td><td><p>1.6.0</p></td></tr><tr><td><p>ms-swift</p></td><td><p>4.4.2</p></td></tr><tr><td><p>vLLM</p></td><td><p>0.19.1</p></td></tr><tr><td><p>OmegaConf</p></td><td><p>2.3.0</p></td></tr><tr><td><p>requests</p></td><td><p>2.34.2</p></td></tr></tbody></table>\n</div>\n<p>在 Notebook 中运行下面的代码，可以记录当前实验环境：</p>\n\n<pre data-language=\"Python\"><code>import sys\nimport importlib.metadata as metadata\n\ndef installed_version(package):\n    try:\n        return metadata.version(package)\n    except metadata.PackageNotFoundError:\n        return \"未安装\"\n\nprint(\"Python：\", sys.version.split()[0])\nfor package in [\"ms-agent\", \"ms-swift\", \"vllm\", \"omegaconf\", \"requests\"]:\n    print(f\"{package}：{installed_version(package)}\")</code></pre>\n\n<p></p>\n\n<p>本次实验中的相关环境如下：</p>\n\n<img src=\"assets/manuscript-20260914/c25-9092e50e383de4.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<img src=\"assets/manuscript-20260914/c25-e125e27203ef7a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h3 id=\"c27-s12\"><b>启动 Qwen3-4B 推理服务</b></h3>\n\n<p></p>\n\n<p>在魔搭 GPU Notebook 中打开一个终端，执行以下命令启动 Qwen3-4B。模型服务需要在后续实验期间持续运行，因此不要关闭这个终端。</p>\n\n<p></p>\n\n<pre data-language=\"Bash\"><code>CUDA_VISIBLE_DEVICES=0 swift deploy \\\n  --model Qwen/Qwen3-4B \\\n  --load_args false \\\n  --infer_backend vllm \\\n  --enable_thinking false \\\n  --host 0.0.0.0 \\\n  --port 18002 \\\n  --api_key 123 \\\n  --vllm_gpu_memory_utilization 0.8 \\\n  --vllm_max_model_len 8000 \\\n  --max_new_tokens 2000</code></pre>\n\n<p>其中，<code>--infer_backend vllm</code>指定使用 vLLM 推理，<code>--port 18002</code>指定服务端口，<code>--enable_thinking false</code>关闭本次实验不需要展示的思考输出。示例中的密钥只用于同一 Notebook 实例内的课堂实验；如果服务需要对外提供，应改用随机生成的高强度密钥，并结合网络访问控制。模型启动后，在 Notebook 代码单元中访问 OpenAI 兼容的模型列表接口：</p>\n\n<pre data-language=\"Python\"><code>import requests\n\nMODEL_API_BASE = \"http://127.0.0.1:18002/v1\"\nMODEL_API_KEY = \"123\"\nMODEL_ID = \"Qwen3-4B\"\n\nresponse = requests.get(\n    MODEL_API_BASE + \"/models\",\n    headers={\"Authorization\": \"Bearer \" + MODEL_API_KEY},\n    timeout=(10, 30),\n)\nresponse.raise_for_status()\navailable_models = [item[\"id\"] for item in response.json()[\"data\"]]\nprint(\"模型服务连接成功\")\nprint(\"模型：\", available_models[0])</code></pre>\n\n<p>启动服务及实验结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c25-8521b88bcf4abc.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<img src=\"assets/manuscript-20260914/c25-e6d2b273260778.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>实验中得到的结果为：</p>\n\n<img src=\"assets/manuscript-20260914/c25-c96ea9b457168c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h3 id=\"c27-s13\"><b>编写天气查询 MCP 工具</b></h3>\n\n<p></p>\n\n<p>天气 MCP 使用 Open-Meteo 提供的公开接口，不需要申请第三方 API Key。Server 先调用地理编码接口，把地区名称解析为经纬度和时区，再调用预报接口获取指定日期的天气。实验将工具查询范围限制为当地今天至未来十五天，<code>target_date</code>可以填写<code>today</code>、<code>tomorrow</code>或<code>YYYY-MM-DD</code>。下面是工具的核心代码。完整的天气代码、天气现象代码表和异常处理保存在本章配套 Notebook 中。</p>\n\n<pre data-language=\"Python\"><code>import socket\nimport requests\nfrom mcp.server.fastmcp import FastMCP\n\nwith socket.socket() as port_probe:\n    port_probe.bind((\"127.0.0.1\", 0))\n    WEATHER_MCP_PORT = port_probe.getsockname()[1]\n\nweather_mcp = FastMCP(\n    \"Public Weather MCP\",\n    host=\"127.0.0.1\",\n    port=WEATHER_MCP_PORT,\n    stateless_http=True,\n    json_response=True,\n)\n\n@weather_mcp.tool()\ndef get_weather(area: str, target_date: str) -&gt; dict:\n    \"\"\"查询某个地区在指定日期的天气预报。area应为城市或“城市,省份/国家”；target_date支持today、tomorrow或YYYY-MM-DD。\"\"\"\n    location = resolve_area(area)\n    day = parse_target_date(target_date, location[\"timezone\"])\n    response = requests.get(\n        \"https://api.open-meteo.com/v1/forecast\",\n        params={\n            \"latitude\": location[\"latitude\"],\n            \"longitude\": location[\"longitude\"],\n            \"start_date\": day.isoformat(),\n            \"end_date\": day.isoformat(),\n            \"daily\": (\"weather_code,temperature_2m_max,temperature_2m_min,\"\n                      \"precipitation_probability_max,wind_speed_10m_max\"),\n            \"timezone\": location[\"timezone\"],\n        },\n        timeout=20,\n    )\n    response.raise_for_status()\n    daily = response.json()[\"daily\"]\n    weather_code = daily[\"weather_code\"][0]\n    place = \"，\".join(filter(None, [\n        location.get(\"name\"), location.get(\"admin1\"), location.get(\"country\")\n    ]))\n    return {\n        \"requested_area\": area,\n        \"resolved_area\": place,\n        \"date\": daily[\"time\"][0],\n        \"timezone\": location[\"timezone\"],\n        \"weather_code\": weather_code,\n        \"weather\": WMO_WEATHER.get(weather_code, f\"未知代码 {weather_code}\"),\n        \"temperature_max_c\": daily[\"temperature_2m_max\"][0],\n        \"temperature_min_c\": daily[\"temperature_2m_min\"][0],\n        \"precipitation_probability_max_percent\": daily[\"precipitation_probability_max\"][0],\n        \"wind_speed_max_kmh\": daily[\"wind_speed_10m_max\"][0],\n        \"data_source\": \"Open-Meteo\",\n    }</code></pre>\n\n<p>其中，<code>@weather_mcp.tool()</code>把普通 Python 函数注册为 MCP 工具。FastMCP 会根据函数名、类型标注和说明文字生成工具 Schema，因此<code>area</code>和<code>target_date</code>的含义必须写清楚。<code>resolve_area</code>负责地区解析，<code>parse_target_date</code>负责日期格式和查询范围校验，函数最后把 API 结果整理为稳定字段。拆分这些职责，可以让错误发生时更容易判断是地区、日期还是天气接口出了问题。</p>\n\n<p>需要注意，不同版本 FastMCP 的启动参数可能不同。本实验使用的版本要求在创建<code>FastMCP</code>对象时填写<code>host</code>和<code>port</code>，而不是把这两个参数传给<code>run()</code>。否则会出现<code>FastMCP.run() got an unexpected keyword argument 'host'</code>。</p>\n\n<p></p>\n\n<h3 id=\"c27-s14\"><b>启动并注册天气 MCP Server</b></h3>\n\n<p>完成工具定义后，在 Notebook 后台线程中启动 Streamable HTTP 服务。后台线程使 MCP Server 能够持续响应请求，同时不阻塞后续代码单元。</p>\n\n<pre data-language=\"Python\"><code>import threading\n\ndef run_weather_server():\n    weather_mcp.run(transport=\"streamable-http\")\n\nweather_server_thread = threading.Thread(\n    target=run_weather_server,\n    daemon=True,\n    name=\"weather-mcp\",\n)\nweather_server_thread.start()\n\nWEATHER_MCP_URL = f\"http://127.0.0.1:{WEATHER_MCP_PORT}/mcp\"\nweather_mcp_config = {\n    \"mcpServers\": {\n        \"weather\": {\n            \"type\": \"streamable_http\",\n            \"url\": WEATHER_MCP_URL,\n        }\n    }\n}</code></pre>\n\n<p>这里的<code>weather</code>是 MS-Agent 内部使用的 Server 连接名称，<code>get_weather</code>才是模型能够调用的工具名称。配置中的<code>streamable_http</code>必须与 Server 启动时使用的<code>streamable-http</code>传输方式对应，地址末尾的<code>/mcp</code>也不能遗漏。实验中服务成功启动后输出如下：</p>\n\n<pre data-language=\"Plain Text\"><code>Application startup complete.\nUvicorn running on http://127.0.0.1:58545\n天气 MCP Server 已启动： http://127.0.0.1:58545/mcp</code></pre>\n\n<p>端口由 Notebook 在每次实验时动态选择，因此重新运行后出现不同端口属于正常现象，不应把示例中的<code>58545</code>固定写入配置。平台注册后的结果如下。</p>\n\n<img src=\"assets/manuscript-20260914/c25-9fff9add06d11f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c27-s15\"><b>使用 MCP 广场中的托管服务</b></h3>\n\n<p>自建天气 MCP 适合解释 Server 的实现过程，但并非每次使用 MCP 都要重新开发服务。魔搭 MCP 广场（https://modelscope.cn/mcp）集中展示了社区发布的 MCP Server，服务页面通常会提供工具说明、源码信息、运行参数和可用的连接配置。MS-Agent 的官方示例也使用广场中的<code>fetch</code>服务演示 Streamable HTTP 连接。本次择 MCP 广场中的<a href=\"https://modelscope.cn/mcp/servers/@modelcontextprotocol/fetch\" target=\"_blank\" rel=\"noopener noreferrer\"><code>@modelcontextprotocol/fetch</code></a>。它提供一个名为<code>fetch</code>的只读工具，可以根据 URL 获取公开网页内容。实际操作步骤如下：</p>\n\n<p></p>\n\n<ol><li>登录魔搭社区并进入 MCP 广场，在搜索框中输入<code>fetch</code>；</li><li>进入<code>@modelcontextprotocol/fetch</code>服务页面，检查发布者、源码、工具说明和所需参数；</li><li>在页面提供的托管入口启动服务，传输方式选择 Streamable HTTP；</li><li>等待服务状态变为可用，再复制页面生成的完整连接地址；</li><li>回到魔搭 Notebook，把地址填入<code>MCP_SERVER_URL</code>，不要自行拼接用户标识或端点路径。</li></ol>\n\n<p>魔搭 MCP 广场中的 fetch 页面如下：</p>\n\n<img src=\"assets/manuscript-20260914/c25-fca24d3413997d.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>随后按照 MS-Agent 要求建立<code>mcpServers</code>配置：</p>\n\n<pre data-language=\"Python\"><code>mcp_config = {\n    \"mcpServers\": {\n        \"fetch\": {\n            \"type\": \"streamable_http\",\n            \"url\": MCP_SERVER_URL,\n        }\n    }\n}</code></pre>\n\n<p>外层的<code>fetch</code>是当前 Agent 使用的 Server 连接名称，<code>type</code>必须与托管时选择的传输方式相符，<code>url</code>必须使用服务页面生成的完整地址。配置完成只说明客户端已经知道连接信息，服务是否真正可用还要通过工具发现和调用来验证。注册成功结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c25-6f1b45f76f4910.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h2 id=\"c27-s16\"><b>调用 MCP 工具</b></h2>\n\n<p></p>\n\n<p>MCP Server 启动后，不应立即把所有问题都交给大模型。更稳妥的验证顺序是先用 MCP Client 完成工具发现和直接调用，确认 Server 及外部 API 正常，再让 Qwen3-4B 自主选择工具。这样可以把协议连接故障、工具自身故障和模型调用问题分开排查，整体流程如下图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c25-d16b20c5ebe28a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h3 id=\"c27-s17\"><b>发现并直接验证工具</b></h3>\n\n<p></p>\n\n<p>下面的代码建立 MCP 会话，先调用<code>list_tools()</code>读取 Server 公开的工具列表，再以确定参数直接调用<code>get_weather</code>。这一步不经过 Qwen3-4B。</p>\n\n<pre data-language=\"Python\"><code>from mcp import ClientSession\nfrom mcp.client.streamable_http import streamable_http_client\n\nasync def check_weather_mcp():\n    async with streamable_http_client(WEATHER_MCP_URL) as streams:\n        read_stream, write_stream, _ = streams\n        async with ClientSession(read_stream, write_stream) as session:\n            await session.initialize()\n            tools = await session.list_tools()\n            print(\"发现的工具：\", [tool.name for tool in tools.tools])\n            result = await session.call_tool(\n                \"get_weather\",\n                arguments={\"area\": \"杭州,浙江\", \"target_date\": \"today\"},\n            )\n            if result.isError:\n                raise RuntimeError(str(result.content))\n            for block in result.content:\n                if getattr(block, \"type\", None) == \"text\":\n                    print(block.text)\n\nawait check_weather_mcp()</code></pre>\n\n<p>运行上述逻辑，执行结果如下：<br></p>\n\n<img src=\"assets/manuscript-20260914/c25-5e3021e752014b.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h3 id=\"c27-s18\"><b>让 Qwen3-4B 调用天气 MCP</b></h3>\n\n<p></p>\n\n<p>结合上述才注册的工具，可以继续把启动的 Qwen3-4B 配置给 MS-Agent。模型接口和天气 MCP 分别配置：前者负责推理，后者负责提供工具。</p>\n\n<pre data-language=\"Python\"><code>from omegaconf import OmegaConf\nfrom ms_agent import LLMAgent\n\nagent_config = OmegaConf.create({\n    \"llm\": {\n        \"service\": \"modelscope\",\n        \"model\": MODEL_ID,\n        \"modelscope_api_key\": MODEL_API_KEY,\n        \"modelscope_base_url\": MODEL_API_BASE,\n    },\n    \"generation_config\": {\n        \"temperature\": 0.0,\n        \"max_tokens\": 2000,\n        \"stream\": False,\n        \"extra_body\": {\"enable_thinking\": False},\n    },\n    \"max_chat_round\": 6,\n})\n\nweather_agent = LLMAgent(\n    config=agent_config,\n    tag=\"weather-demo\",\n    mcp_config=weather_mcp_config,\n)\n\nawait weather_agent.run(\n    \"请调用天气查询工具，查询杭州明天的天气。\"\n    \"请说明工具实际匹配的地区、日期、天气状况、最高和最低气温、\"\n    \"最高降水概率与最大风速。只使用本次工具返回的数据；调用失败时如实说明。\"\n)</code></pre>\n\n<p>运行时，MS-Agent 先连接名为<code>weather</code>的 Server 并发现<code>get_weather</code>。Qwen3-4B 根据用户问题和工具 Schema 生成了下面的调用：</p>\n\n<pre data-language=\"JSON\"><code>{\n  \"name\": \"weather---get_weather\",\n  \"arguments\": {\n    \"area\": \"杭州\",\n    \"target_date\": \"tomorrow\"\n  }\n}</code></pre>\n\n<p></p>\n\n<p>其中，<code>weather---get_weather</code>是 MS-Agent 组合 Server 连接名称和工具名称后显示的标识。<code>area</code>与<code>target_date</code>不是预先写死的调用参数，而是 Qwen3-4B 根据“杭州明天”生成的结构化参数。相关注册结果如下图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c25-8672cb597435bf.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h3 id=\"c27-s19\"><b>处理工具返回与最终回答</b></h3>\n\n<p>天气 MCP 收到参数后，将“杭州”解析为“杭州，浙江，中国”，根据当地时区把<code>tomorrow</code>转换为 2026 年 9 月 7 日，并返回：</p>\n\n<pre data-language=\"JSON\"><code>{\n  \"requested_area\": \"杭州\",\n  \"resolved_area\": \"杭州，浙江，中国\",\n  \"date\": \"2026-09-07\",\n  \"timezone\": \"Asia/Shanghai\",\n  \"weather_code\": 51,\n  \"weather\": \"小毛毛雨\",\n  \"temperature_max_c\": 29.0,\n  \"temperature_min_c\": 21.6,\n  \"precipitation_probability_max_percent\": 31,\n  \"wind_speed_max_kmh\": 13.0,\n  \"data_source\": \"Open-Meteo\"\n}</code></pre>\n\n<p>Qwen3-4B 随后依据这组结果生成自然语言回答，列出了实际匹配地区、日期、天气状况、最高和最低气温、降水概率及最大风速。至此，“理解问题—选择工具—生成参数—执行工具—处理结果”的调用链路完成。结合大模型执行结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c25-cdd5202f43a4b6.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c27-s20\"><b>调用 MCP 广场中的 fetch 服务</b></h3>\n\n<p>完成托管和配置后，先直接连接服务并检查工具列表。下面的测试样例网站信息<code>https://example.com</code>。</p>\n\n<pre data-language=\"Python\"><code>async def direct_fetch_check():\n    async with streamable_http_client(MCP_SERVER_URL) as streams:\n        read_stream, write_stream, _ = streams\n        async with ClientSession(read_stream, write_stream) as session:\n            await session.initialize()\n            tools = await session.list_tools()\n            print(\"Server返回的工具：\", [tool.name for tool in tools.tools])\n            result = await session.call_tool(\n                \"fetch\",\n                arguments={\n                    \"url\": \"https://example.com\",\n                    \"max_length\": 1000,\n                    \"start_index\": 0,\n                    \"raw\": False,\n                },\n            )\n            if result.isError:\n                raise RuntimeError(str(result.content))\n            for block in result.content:\n                if getattr(block, \"type\", None) == \"text\":\n                    print(block.text)\n\nawait direct_fetch_check()</code></pre>\n\n<p>实际运行时，Server 公开了<code>fetch</code>工具，并返回了目标网页的正文片段和链接。这说明魔搭托管服务、Streamable HTTP 连接以及网页抓取工具本身均可工作。直接连接魔搭托管 fetch 服务、发现工具并返回网页内容如下图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c25-327d9bc6ee4c29.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>直接调用成功后，再复用前面配置好的 Qwen3-4B，让模型自主选择工具：</p>\n\n<p></p>\n\n<pre data-language=\"Python\"><code>fetch_agent = LLMAgent(\n    config=agent_config,\n    tag=\"fetch-demo\",\n    mcp_config=mcp_config,\n)\n\nawait fetch_agent.run(\n    \"请使用网页抓取工具读取https://example.com。\"\n    \"调用时将max_length设为1000，并返回网页正文说明的主要用途和第一个链接。\"\n    \"只能使用本次工具返回的内容；调用失败时请明确说明，不要补写。\"\n)</code></pre>\n\n<p>实验日志显示，MS-Agent 成功连接名为<code>fetch</code>的 Server，Qwen3-4B 选择了<code>fetch---fetch</code>并生成以下参数：</p>\n\n<pre data-language=\"JSON\"><code>{\n  \"url\": \"https://example.com\",\n  \"max_length\": 1000\n}</code></pre>\n\n<p>工具返回网页内容后，模型据此说明该域名用于文档示例，并给出工具返回的第一个链接。检查这类结果时，应分别核对模型生成的参数、工具原始返回和最终回答。利用 Qwen3-4B 生成 fetch 工具调用参数、工具返回内容及最终回答，执行结果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c25-ffeaaa9aac2717.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>直接使用魔搭 MCP 广场中的应用服务与自建天气 MCP 的区别在于，Server 的实现和运行环境由魔搭托管能力负责，Notebook 只需要保存连接配置并发起调用。无论使用现成服务还是自建服务，验证顺序都相同：检查服务状态、发现工具、直接测试，再让模型自主调用。</p>\n\n<p></p>\n\n<h2 id=\"c27-s21\"><b>MCP 权限管理</b></h2>\n\n<p>MCP 的权限管理不能只停留在“这个 Server 能不能连接”这一层，更重要的是明确三个问题：当前用户能访问哪些资源，模型能够调用哪些工具，以及每次调用会对外部系统产生什么影响。因此，权限控制应该从 Host、MCP Server 到后端业务系统逐层落实，并始终遵循最小权限原则。即使是只读工具，也不能默认认为没有风险，因为它仍然可能读取客户信息、源代码、访问令牌等敏感内容，所以需要限制可访问的目录、库表、字段和返回范围，并结合 Server 的实际实现确认是否存在额外的数据记录、缓存或外传行为。</p>\n\n<p>对于会修改外部状态的操作，权限控制应随着风险逐步加强。创建文件、修改记录、发送邮件、添加日程等写入操作，最好在执行前向用户展示目标对象和关键参数，能够生成草稿或显示差异的，应优先让用户确认后再执行。同时还要避免网络超时、自动重试造成重复写入，可以通过幂等键、唯一约束或状态检查来降低风险。对于删除数据、转账、批量发送、公开发布、修改权限、执行命令和生产环境变更等高风险操作，则应默认限制自动执行，并保留明确的人工确认、二次审批和完整审计。</p>\n\n<p>除了工具本身的权限，凭据和账号也需要单独控制。API Key、访问令牌等敏感信息不应出现在 Prompt、聊天记录、代码仓库或普通日志中，MCP Server 访问后端系统时应使用范围受限的专用账号和凭据，而不是直接继承过大的权限。对于敏感系统，还可以进一步把查询和修改能力拆分到不同 Server、不同账号甚至不同网络区域中。总体来说，MCP 权限管理的核心原则是：工具可以被发现，不代表就可以直接执行；一次授权也不代表以后所有操作都自动获得权限，权限应根据访问范围和操作风险逐级控制。</p>\n\n<p></p>\n\n<h2 id=\"c27-s22\"><b>MCP 安全风险</b></h2>\n\n<p>MCP 让模型不仅能够生成内容，还可以读取文件、查询数据库、调用网络服务，甚至修改外部系统，因此它带来的安全风险也比普通聊天模型更复杂。风险来源不仅包括用户输入，还包括网页、文档、数据库记录、工具返回结果、Server 代码以及第三方依赖。其中最典型的是 Prompt 注入：外部内容中可能隐藏恶意指令，诱导模型继续调用文件、网络或消息工具，形成危险的跨系统操作链。对此，不能只依赖模型“识别恶意提示”，而应该把所有外部内容都视为不可信数据，严格区分用户目标、系统规则和工具返回结果，并限制外部内容可以继续触发的工具范围。</p>\n\n<p>另一类核心风险是越权访问和敏感信息泄漏。模型生成的参数不能被当作授权依据，Host 可以控制向模型暴露哪些工具，但 Server 仍然需要在每次调用时根据真实用户身份检查资源归属和访问权限，不能只在建立连接时验证一次。与此同时，还要控制数据在不同系统之间如何流动，只向 Server 传递完成当前任务所需的字段，对密钥、身份证号、手机号等敏感信息进行脱敏或拦截，远程连接使用 HTTPS，日志中避免保存完整凭据和敏感正文。尤其要注意，模型从一个 Server 读取到的数据，不应在没有明确授权的情况下自动发送到另一个外部 Server。</p>\n\n<p>MCP 还存在恶意工具和供应链风险，因此生产环境不能只关注单次调用，而应建立完整的多层安全机制。接入 Server 前要检查来源、代码、依赖和所需权限；连接时使用受限账号、受限凭据和受控网络；工具发现后按只读、写入和高风险分类，只向模型开放真正需要的能力；执行前校验参数，对删除、发送、上传、修改权限等敏感操作要求人工确认；执行过程中限制超时、并发、返回量和网络范围；执行结束后检查工具返回内容，并保留必要的审计日志。对于正式环境，最好维护经过审核的 Server 白名单和版本清单，工具或版本发生变化后重新评估权限，并确保出现异常时能够快速停用 Server、撤销令牌和追溯操作记录。</p>\n\n<p></p>\n\n<h2 id=\"c27-s23\"><b>MCP 适合的场景</b></h2>\n\n<p></p>\n\n<p>MCP 更适合这样的场景：外部工具和数据源比较多，希望这些能力能够被不同模型或 Agent 重复使用，同时又希望模型根据当前任务动态决定调用什么工具。 常见应用包括文件访问、数据库查询和搜索检索。例如，文件类 MCP Server 可以让模型浏览、读取和搜索项目文件，数据库类 Server 可以查询库存、订单和运营数据，搜索类 Server 则可以连接互联网、企业知识库或专业资料库。实际使用时，应尽量只开放完成任务所需的数据范围，文件访问优先采用只读方式，数据库优先使用只读账号或受控查询，并对查询范围、返回字段和结果数量进行限制；网页和搜索结果则应视为不可信输入，重要信息还需要保留来源并进行必要的交叉验证。</p>\n\n<p>MCP 也很适合连接邮件、日历、网盘、项目管理系统以及企业已有的业务 API，让模型从“查询信息”进一步扩展到“完成操作”。例如，模型可以查询会议时间、整理项目进度、生成邮件草稿，也可以调用客服、物流、审批、工单和设备管理等业务能力。设计这类工具时，最好把能力拆成边界清楚的业务动作，例如分别提供“查询工单”“添加工单备注”和“关闭工单”，而不是直接给模型一个可以调用任意接口的万能工具。查询和修改也应尽量分开设置权限，对于发送邮件、修改共享权限、关闭工单、批量更新等会改变外部状态的操作，应保留必要的人工确认和审计记录。</p>\n\n<p>但并不是所有接口都需要改造成 MCP。如果业务流程固定、调用关系明确、对延迟要求很高，而且根本不需要模型判断“下一步应该使用什么工具”，直接调用普通 API 往往更加简单可靠。对于删除数据、资金操作、生产环境变更等高风险任务，如果还没有完善的权限控制、人工审批、审计和回滚机制，也不应该仅仅因为 MCP 能够接入就立即实现自动化。简单来说，MCP 的价值主要体现在让模型能够灵活选择和组合多种外部能力，而固定、确定的系统调用仍然可以继续使用传统 API。</p>\n\n<p></p>\n\n<p>本章节所有实验数据和代码，可参考：</p>\n\n<p>https://modelscope.cn/gallery/liucong/fae5791c-a024-412f-97c3-5fb93fee708e</p>\n\n<p></p>\n","imageCount":17,"attachmentCount":0,"discussionId":"chapter-25"},{"id":"chapter-28","number":28,"title":"Skill：把任务方法封装成能力","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-28.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-28.html","minutes":39,"status":"ready","headings":[{"id":"c28-s1","text":"Skill 是什么","level":"h2"},{"id":"c28-s2","text":"Skill 与 Prompt 的区别","level":"h2"},{"id":"c28-s3","text":"Skill 与工作流的区别","level":"h2"},{"id":"c28-s4","text":"Skill 与 MCP 的区别","level":"h2"},{"id":"c28-s5","text":"Skill 的基本组成","level":"h2"},{"id":"c28-s6","text":"如何设计一个 Skill","level":"h2"},{"id":"c28-s7","text":"在魔搭中使用 Skill","level":"h2"},{"id":"c28-s8","text":"实验任务与环境准备","level":"h3"},{"id":"c28-s9","text":"创建与安装会议纪要 Skill","level":"h3"},{"id":"c28-s10","text":"加载 Skill 与读取参考文件","level":"h3"},{"id":"c28-s11","text":"部署模型并配置推理接口","level":"h3"},{"id":"c28-s12","text":"通过办公对话调用 Skill","level":"h3"},{"id":"c28-s13","text":"检查与调试调用过程","level":"h3"},{"id":"c28-s14","text":"Skill 的测试与版本","level":"h2"},{"id":"c28-s15","text":"准备测试案例","level":"h3"},{"id":"c28-s16","text":"定义检查条件","level":"h3"},{"id":"c28-s17","text":"运行测试并分析结果","level":"h3"},{"id":"c28-s18","text":"修改 Skill 并比较版本","level":"h3"},{"id":"c28-s19","text":"保存版本与回退","level":"h3"}],"html":"<p>第一次让 Agent 整理会议纪要时，可能要解释很多要求：哪些内容算结论，怎样提取待办，负责人缺失时怎么处理，最后用什么格式输出。第二次遇到类似任务，这些要求又要重新说一遍。换一个同事或换一个 Agent，还得再解释一次。问题不在于 Agent 不会整理会议纪要，而在于这套方法只存在于某次对话中。它没有被保存，也没有变成可以重复调用的能力。</p>\n\n<p>Skill 解决的就是这个问题，把一类任务的说明、步骤、工具、参考资料和验收标准放在一起。以后再遇到同类任务，Agent 可以先读取这套方法，再按照已经验证过的规则完成工作。</p>\n\n<h2 id=\"c28-s1\"><b>Skill 是什么</b></h2>\n\n<p>Skill 可以理解为提供给 Agent 的一份专业工作手册。不改变模型参数，是把完成某类任务时需要遵守的方法保存下来，让 Agent 知道什么时候使用、先做什么、需要哪些工具，以及怎样判断结果是否合格。以整理会议纪要为例。有经验的项目助理会先检查材料，再区分讨论意见、最终结论和未决问题，随后提取行动项、负责人和截止时间，最后按统一模板输出并复核。原文没有的信息不会凭常识补写。</p>\n\n<p>如果这些方法只保存在人的脑中，Agent 每次都要重新摸索。把它们写进 Skill 后，同一套方法就能在不同会议中重复使用，也可以由团队共同维护。换个方式说，Skill 是一种面向 Agent 的可复用能力包。它用任务说明、执行步骤、工具和资源、输出标准与异常处理，描述一类任务应该怎样完成。</p>\n\n<p>以会议纪要 Skill 为例。该 Skill 通过接收会议转写稿、聊天记录或散乱笔记，输出会议信息、关键结论、行动项和未决问题。先看最核心的<code>SKILL.md</code>。文件开头使用 YAML 元数据说明 Skill 的名称和使用场景，后面使用 Markdown 编写具体工作方法。</p>\n\n<p></p>\n\n<pre data-language=\"YAML\"><code>---\nname: meeting-notes\ndescription: 将会议转写稿、聊天记录或散乱笔记整理为结构化会议纪要。当用户要求提取会议结论、行动项或待确认问题时使用。\n---\n\n会议纪要整理\n\n执行步骤\n\n检查输入是否包含可识别的会议内容。\n区分讨论意见、最终结论和未决问题。\n提取行动项、负责人和截止时间。\n无法确认的信息标记为“待确认”。\n按规定模板输出，并与原始记录复核。</code></pre>\n\n<p>其中，<code>name</code>和<code>description</code>是规范中的必填字段。<code>name</code>是给当前 Skill 起个名字，<code>description</code>则给当前 Skill 进行任务描述，那 Skill 是怎样进入并完成一次任务的呢？</p>\n\n<p>安装 Skill 以后，系统不会把所有文件一股脑交给大模型。常见的运行过程分为三步：</p>\n\n<ol><li>系统先提供已安装 Skill 的名称和描述。大模型根据用户任务判断哪些能力可能相关。</li><li>某个 Skill 被选中后，系统再加载它的<code>SKILL.md</code>，大模型据此检查输入并执行核心步骤。</li><li>执行到特定环节时，才按说明读取参考资料、套用模板或运行脚本。</li></ol>\n\n<p>例如，用户提出把一份会议记录整理成纪要，大模型或 Agent 工具会从多个 Skill 中匹配会议纪要能力，再读取其完整规则。若任务还要求生成 Excel 行动项表，还可能同时选用表格处理 Skill。大模型负责结合当前任务作判断，Skill 负责提供稳定的方法，平台或 Agent 框架负责加载文件和调用工具。</p>\n\n<p>相较于维护的 Skill，与 Prompt、工作流、MCP 有什么区别呢？接下来做一些梳理。</p>\n\n<h2 id=\"c28-s2\"><b>Skill 与 Prompt 的区别</b></h2>\n\n<p>Prompt 是你在当前任务中交给大模型或 Agent 的输入，通常包括目标、材料、限制条件和输出要求。它回答的是这一次要做什么。例如：</p>\n\n<pre data-language=\"Plain Text\"><code>请把这段会议记录整理成会议纪要，列出结论和待办事项。</code></pre>\n\n<p>这条 Prompt 已经说明本次目标，但没有完整规定什么算最终结论、负责人缺失时怎么办、能否补全原文没有的信息，以及输出采用什么结构。Skill 把这些相对稳定的规则提前保存下来。下一次再处理会议记录时，你只需要提供本次材料和具体要求，不必重新复制整套方法。从作用、载体、加载方式以及版本管理等角度，对 Prompt 与 Skill 做个对比：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col><col></colgroup><tbody><tr><td><p>维度</p></td><td><p>Prompt</p></td><td><p>Skill</p></td></tr><tr><td><p>主要作用</p></td><td><p>描述当前任务</p></td><td><p>定义一类任务通常怎样完成</p></td></tr><tr><td><p>使用周期</p></td><td><p>多数只用于当前请求</p></td><td><p>可以被多次调用和持续维护</p></td></tr><tr><td><p>载体</p></td><td><p>对话中的文字、文件和上下文</p></td><td><p>包含<code>SKILL.md</code>及可选资源的目录</p></td></tr><tr><td><p>内容</p></td><td><p>目标、材料、约束和输出要求</p></td><td><p>触发条件、步骤、工具、模板、异常和验收</p></td></tr><tr><td><p>加载方式</p></td><td><p>通常直接进入当前上下文</p></td><td><p>可以按任务需要逐层加载</p></td></tr><tr><td><p>版本管理</p></td><td><p>容易散落在聊天记录中</p></td><td><p>可以纳入 Git 并保留变更记录</p></td></tr></tbody></table>\n</div>\n<p></p>\n\n<p>当然，Prompt 和 Skill 并不是二选一，Prompt 负责告诉大模型或 Agent 这一次要处理哪份材料、有什么临时要求，而 Skill 则负责提供这类任务已经沉淀好的做法。同一个 Skill 面对不同输入时，仍然需要 Prompt 说明本次目标。不是每条 Prompt 都值得做成 Skill。一个只做一次、目标还在变化的任务，先用 Prompt 跑通更合适。等同类任务反复出现、处理方法逐渐稳定，再把它沉淀成 Skill。</p>\n\n<p></p>\n\n<h2 id=\"c28-s3\"><b>Skill 与工作流的区别</b></h2>\n\n<p>工作流是一套用于组织多个任务节点的运行安排，通常会规定先后顺序、条件分支、数据传递和失败后的去向。它回答的是多个环节怎样衔接。</p>\n\n<p>例如，一条周报工作流可以是：</p>\n\n<pre data-language=\"Plain Text\"><code>读取原始数据 → 清洗字段 → 生成图表 → 撰写分析 → 导出周报</code></pre>\n\n<p>Skill 关注的是某个能力单元应该怎样完成。上面的每一步都可以由一个 Skill 提供方法，也可以由脚本、人工操作或其他工具完成。两者的区别不在于谁固定、谁灵活，而在于关注对象不同：</p>\n\n<ul><li>工作流主要组织任务节点、顺序、分支和数据传递；</li><li>Skill 主要封装完成某类任务所需的知识、规则、资源和工具使用方法。</li></ul>\n\n<p>一个 Skill 内部可以包含固定步骤，也可以包含条件判断。一个工作流也可以在某个节点调用 Agent，让 Agent 根据现场情况决定下一步。例如，会议纪要 Skill 可以根据输入作出不同处理：</p>\n\n<ul><li>输入没有会议正文时，停止生成并请求补充；</li><li>原文明确写出负责人时，直接写入行动项；</li><li>原文没有负责人时，标记为待确认；</li><li>多份材料互相矛盾时，列出冲突，不自行决定哪一份正确；</li><li>用户只要求行动项时，省略完整的会议摘要。</li></ul>\n\n<p>在真实系统中，Skill 和工作流经常组合使用：工作流负责把多个环节串起来，Skill 负责让其中某个环节按照稳定方法完成。</p>\n\n<p></p>\n\n<h2 id=\"c28-s4\"><b>Skill 与 MCP 的区别</b></h2>\n\n<p></p>\n\n<p>MCP 是模型上下文协议，用于以统一方式向 Agent 提供外部工具、资源和提示模板。它回答的是 Agent 怎样连接和调用外部能力。通过 MCP，Agent 可以连接数据库、知识库、项目管理系统、地图服务或其他业务系统。但连接成功只代表 Agent 可以使用某项能力，不代表它已经知道应该怎样完成业务任务。假设 Agent 已经能读取项目管理系统，它仍然需要知道：</p>\n\n<ul><li>应该查询哪个项目；</li><li>统计什么时间范围；</li><li>哪些状态算已完成；</li><li>哪些字段属于敏感信息；</li><li>最终用什么格式生成项目报告；</li><li>创建、修改或删除记录前是否需要确认。</li></ul>\n\n<p>这些任务规则更适合写进 Skill。可以把两者的关系概括为：MCP 提供连接外部工具和数据的标准方式，Skill 描述怎样使用这些能力完成一类任务。</p>\n\n<p>一个 Skill 可以指导 Agent 调用 MCP 工具，也可以完全不使用 MCP。一个 MCP 服务也可以被多个 Skill 采用。涉及发送消息、修改记录、删除数据、产生费用或公开发布时，Skill 应明确规定确认点，不能因为工具已经连接就默认允许执行。</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col><col></colgroup><tbody><tr><td><p>概念</p></td><td><p>主要回答的问题</p></td><td><p>典型内容</p></td></tr><tr><td><p>Prompt</p></td><td><p>这一次要做什么</p></td><td><p>当前目标、材料和临时要求</p></td></tr><tr><td><p>Skill</p></td><td><p>这类任务通常怎样做</p></td><td><p>方法、规则、资源、工具和验收</p></td></tr><tr><td><p>工作流</p></td><td><p>多个环节怎样衔接</p></td><td><p>节点、顺序、分支和结果传递</p></td></tr><tr><td><p>MCP</p></td><td><p>Agent 怎样连接外部能力</p></td><td><p>工具、资源、参数和调用接口</p></td></tr></tbody></table>\n</div>\n<p></p>\n\n<h2 id=\"c28-s5\"><b>Skill 的基本组成</b></h2>\n\n<p></p>\n\n<p>一个完整的 Skill，既要有说明任务方法的内容，也要有承载这些内容的文件。就任务本身来说，需要写清触发条件、输入、执行步骤、工具和资源、输出、异常处理；就文件组织来说，至少要有入口文件<code>SKILL.md</code>，复杂一些的 Skill 还会配上参考资料、模板、脚本和测试案例。以会议纪要 Skill 为例，完整目录可以这样组织：</p>\n\n<p></p>\n\n<pre data-language=\"Plain Text\"><code>meeting-notes/\n├── SKILL.md                         # 入口：名称、描述、输入、步骤和验收\n├── references/\n│   ├── terms.md                     # 术语表和内容分类规则\n│   └── privacy-rules.md             # 敏感信息与脱敏要求\n├── assets/\n│   └── meeting-template.md          # 纪要输出模板\n├── scripts/\n│   └── clean-transcript.py          # 清理时间戳和重复口头语\n└── tests/\n    ├── normal-case.md                # 信息完整的测试案例\n    ├── boundary-case.md              # 信息缺失或存在冲突的案例\n    └── failure-case.md               # 无法继续处理的案例</code></pre>\n\n<p></p>\n\n<p>其中，<code>SKILL.md</code>是整个目录的入口。Agent 先从这里了解 Skill 适合处理什么任务、需要什么输入、按什么步骤执行。进一步地，根据其他约束，可以补充其他工具或代码，如上述目录组织中，如果遇到专业术语或隐私要求时，可以读取<code>references/</code> 目录获取相关信息；再生成结果阶段，可以套用<code>assets/</code>中的模板；需要稳定完成文本清洗等机械操作时，再运行<code>scripts/</code>中的程序。<code>tests/</code>主要用于开发和改版后的验证，不是每次执行任务都要加载的内容。在构建阶段，目录不要求一次完全建设完毕。规则较少时，一个<code>SKILL.md</code>就能组成可运行的 Skill；当主文件越来越长，或者某项资料、模板和脚本需要反复使用时，可以进一步进行拆分。无论文件多少，Agent 实际执行的仍然是下面这条任务链：</p>\n\n<p></p>\n\n<pre data-language=\"Plain Text\"><code>用户提出任务\n    ↓\n大模型根据名称和description筛选相关Skill\n    ↓\n加载选中Skill的SKILL.md\n    ↓\n检查输入 → 执行步骤 → 使用工具和资源 → 生成并检查输出\n    ↓\n遇到异常时按规则停止、降级或请求补充</code></pre>\n\n<p></p>\n\n<p>当 l 同时存在多个 Skil 时，大模型通常先比较每个 Skill 的名称和描述，再选择与当前意图最相关的最小集合。例如，把会议记录整理成纪要并导出行动项表，可能同时需要会议纪要 Skill 和表格处理 Skill；如果只要求整理文字，则没有必要加载表格能力。具体筛选和调用方式由所用平台或 Agent 框架实现，但 Skill 作者必须把适用场景和边界写清楚，才能帮助大模型作出正确选择。</p>\n\n<p>下面从触发条件、输入检查、执行步骤、工具与资源以及输出控制等方面进行介绍</p>\n\n<p><b>1、触发条件</b></p>\n\n<p>所谓触发条件，是指 Skill 提供大模型在众多 Skill 中找到当前需要的能力，主要体现在<code>SKILL.md</code>开头的<code>name</code>和<code>description</code>中。以会议纪要 Skill 为例。<code>name</code>是 Skill 的标识名称，通常使用小写字母、数字和连字符，并与目录名称保持一致。它要简短、明确，让人和系统都能区分这项能力。例如：</p>\n\n<pre data-language=\"YAML\"><code>name: meeting-notes</code></pre>\n\n<p><code>description</code>用来说明这项 Skill 能做什么、遇到什么任务时应该使用。大模型读取用户任务后，会把任务意图与已安装 Skill 的名称和描述进行比较；匹配成功，才继续加载完整的<code>SKILL.md</code>。会议纪要 Skill 的 description 可以这样写：</p>\n\n<pre data-language=\"YAML\"><code>description: 将会议转写稿、聊天记录或散乱笔记整理为结构化会议纪要。当用户要求生成会议摘要、提取结论、行动项或待确认问题时使用。</code></pre>\n\n<p></p>\n\n<p>不要为了增加触发概率塞入大量无关关键词。描述太宽，Agent 会在不相关的任务中误用；描述太窄，又可能在真正需要时找不到它。因为，最终完成 Skill 的编写，还需要依赖测试，测试时需要同时验证应该触发和不应该触发的任务，例如有一个会议录音 ASR 识别的 Skill，就不能在会议纪要相关任务时触发。</p>\n\n<p></p>\n\n<p><b>2、输入检查</b></p>\n\n<p>所谓的输入检查，是指在开始执行某个 Skill 时，Agent 或大模型需要知道，在完成任务时，需要什么、哪些材料必需、哪些可以缺省，以及缺少关键信息时怎样处理。因此选中 Skill 后，下一步不是立刻生成结果，而是检查材料是否足够。以会议纪要 Skill 为例，对于输入部分，可以这样约定：</p>\n\n<ul><li>必需输入：会议转写稿、聊天记录或会议笔记中的至少一种；</li><li>可选输入：会议名称、时间、参与人名单和纪要模板；</li><li>输入边界：文件名和用户的概括不能替代会议原文；</li><li>缺失处理：没有会议正文时停止生成，并请用户补充材料。</li></ul>\n\n<p><b>3、执行步骤</b></p>\n\n<p>输入检查通过后，Agent 才开始正式处理任务。执行步骤要写清楚从读取材料到交付结果的完整顺序：每一步处理什么，得到什么中间结果，遇到不同情况时转到哪里。只写分析内容、提取重点，还不足以让 Agent 稳定执行。会议纪要 Skill 可以按照下面的顺序处理：</p>\n\n<pre data-language=\"Plain Text\"><code>1. 读取全部会议材料；文件为空或无法读取时，转入异常处理。\n2. 识别会议主题、时间和参与人，缺失字段暂时保留为空。\n3. 清理不影响原意的口头语和重复内容，同时保留原始材料不变。\n4. 按议题整理内容，把发言分别归入观点、决定和待确认问题。\n5. 从会议内容中提取已经明确分派的行动项，并记录任务、负责人和截止时间。\n6. 负责人或截止时间在原文中没有出现时，将对应字段标记为“待确认”。\n7. 把整理结果填入会议纪要模板。\n8. 逐项回查原文；无法找到依据的结论和行动项不得交付。</code></pre>\n\n<p>按照上述举例的这组步骤，有明确的入口、处理顺序和结束条件。第 1 步决定任务能否继续，第 2 至第 6 步形成结构化内容，第 7 步生成交付结果，第 8 步负责验收。执行步骤写到这个程度，后面的工具、模板和异常规则才知道应该接在哪个环节。</p>\n\n<p><b>4、工具和资源</b></p>\n\n<p>执行步骤说明要做什么，工具和资源说明具体依靠什么完成。工具负责执行动作，例如读取文件、运行脚本；资源负责提供规则和模板，例如术语表、隐私要求和纪要格式。两者都要写出具体名称和使用时机。会议纪要 Skill 可以使用下面这些内容：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col><col><col></colgroup><tbody><tr><td><p>类型</p></td><td><p>具体工具或资源</p></td><td><p>使用时机</p></td><td><p>作用</p></td></tr><tr><td><p>文件读取工具</p></td><td><p>Python 的<code>pathlib.Path.read_text()</code></p></td><td><p>执行第 1 步时</p></td><td><p>读取 UTF-8 编码的 TXT 或 Markdown 会议记录</p></td></tr><tr><td><p>Word 读取工具</p></td><td><p><code>python-docx</code></p></td><td><p>输入为 DOCX 时</p></td><td><p>读取 Word 文档中的段落和表格文字</p></td></tr><tr><td><p>清洗脚本</p></td><td><p><code>scripts/clean-transcript.py</code></p></td><td><p>划分议题前</p></td><td><p>清理时间戳、连续重复句和不影响原意的口头语</p></td></tr><tr><td><p>术语资源</p></td><td><p><code>references/terms.md</code></p></td><td><p>遇到简称或专业名词时</p></td><td><p>统一项目名、产品名和人员称谓</p></td></tr><tr><td><p>隐私资源</p></td><td><p><code>references/privacy-rules.md</code></p></td><td><p>输出前复核时</p></td><td><p>判断个人信息和业务敏感内容是否需要脱敏</p></td></tr><tr><td><p>输出模板</p></td><td><p><code>assets/meeting-template.md</code></p></td><td><p>生成纪要时</p></td><td><p>规定会议信息、结论、行动项和未决问题的排列方式</p></td></tr></tbody></table>\n</div>\n<p><code>SKILL.md</code>不能只列出这些名称，还要说明什么时候调用。例如，普通 TXT 直接使用<code>pathlib</code>读取；DOCX 改用<code>python-docx</code>；文本中出现项目简称时才读取术语表；需要对外发送时再检查隐私规则。脚本还应说明输入、输出和依赖，运行失败后进入哪条异常处理。访问令牌和密码不能写进 Skill 文件或脚本。</p>\n\n<p><b>5、输出控制</b></p>\n\n<p>完成处理后，Agent 需要知道怎样交付。输出控制阶段要求不能只写生成会议纪要，还要说明结构、格式和完成标准。根据要求，可以约定输出格式，例如：</p>\n\n<p></p>\n\n<pre data-language=\"Markdown\"><code>会议纪要\n\n一、会议信息\n\n会议主题：\n会议时间：\n参与人员：\n二、关键结论\n\n……\n三、行动项\n\n\n四、未决问题\n如果结果需要保存为文件，还应说明文件类型、命名规则和保存位置。</code></pre>\n\n<p><b>6、异常处理</b></p>\n\n<p>异常处理贯穿输入、执行、工具调用和输出检查，不是最后才补上的说明。它决定 Agent 遇到问题时是继续、降级、停止，还是请求用户确认。</p>\n\n<p>常见异常包括输入为空、文件损坏、依赖缺失、外部服务不可用、工具权限不足，以及不同材料中的信息互相矛盾。可以使用下面四条统一原则：</p>\n\n<p></p>\n\n<ol><li>不伪造缺失信息；</li><li>能安全降级时，说明限制后完成仍可完成的部分；</li><li>无法继续时，指出失败步骤、原因和需要补充的材料；</li><li>涉及覆盖、删除、发送、付费或敏感数据时，先请求确认。</li></ol>\n\n<h2 id=\"c28-s6\"><b>如何设计一个 Skill</b></h2>\n\n<p></p>\n\n<p>前面已经说明 Skill 怎样被发现、怎样执行，以及一项完整能力需要包含哪些部分。现在把这些内容真正落到会议纪要案例中。</p>\n\n<p>设计 Skill 的核心，不是先建目录或写一段很长的 Prompt，而是把人的工作经验整理成 Agent 能够执行和检查的方法。这个过程可以分成六步：选择适合封装的任务，确定目标和边界，复盘人工方法，把经验改写成规则，写出可以执行的第一版，最后跑通最短闭环。每一步都会产生下一步需要的内容：任务范围决定输入输出，人工方法形成执行步骤，初稿把这些内容组织起来，测试结果再推动下一版修改。</p>\n\n<p>下面继续以会议纪要 Skill 为例，从一项模糊需求逐步形成可以在魔搭中运行的能力。</p>\n\n<p><b>1、先选择适合封装的任务</b></p>\n\n<p>适合做成 Skill 的任务通常有几个共同点：</p>\n\n<ul><li>会重复出现；</li><li>每次输入不同，但处理方法相似；</li><li>规则相对稳定；</li><li>输出有明确结构；</li><li>结果可以抽查或测试；</li><li>失败后可以安全停止或重新运行。</li></ul>\n\n<p>整理会议纪要符合这些条件：每次会议内容不同，但检查材料、识别结论、提取行动项和组织输出的方法基本一致；结果还可以回到原文逐项复核。因此，本章选择它作为贯穿案例。</p>\n\n<p>如果任务目标本身还没有说清楚，或者每次都要完全依赖临场判断，先不要急着做 Skill。可以先用 Prompt 完成几次，等方法稳定后再沉淀。</p>\n\n<p><b>2、写清目标和边界</b></p>\n\n<p>选定任务后，要把宽泛愿望缩小成能够交付和验收的目标。做一个办公助手范围太大，无法成为一个稳定的 Skill。</p>\n\n<p>会议纪要 Skill 的第一版目标可以写成：</p>\n\n<blockquote><p>接收一份原始会议记录，输出包含会议信息、关键结论、行动项和未决问题的结构化纪要；不根据常识补写原文中没有的负责人、日期和结论。</p></blockquote>\n\n<p>这个目标说清了输入、输出和禁止事项。第一版的输入是已经形成文字的会议记录，不负责录音转写；结果先保存为 Markdown，不自动发邮件，也不写入项目管理系统。边界越清楚，后面的步骤、异常和测试越容易定义。</p>\n\n<p><b>3、复盘人工完成任务的方法</b></p>\n\n<p>目标明确后，可以先把人工完成任务时真正采用的方法找出来，再判断哪些动作可以固定，哪些地方仍然需要结合上下文判断。整理的逻辑可以按照法：先进行三层过滤，再完成四维提取。</p>\n\n<p>所谓三层过滤，可以按顺序处理会议转写稿：</p>\n\n<ol><li>过滤不影响原意的口头语和语气词；</li><li>排除与会议议题无关的闲聊和跑题内容；</li><li>合并重复表达，但保留不同人员提出的不同意见。</li></ol>\n\n<p>完成清理后，再从四个维度整理信息：</p>\n\n<p>1. <b>议题</b>：会议讨论了哪些问题；</p>\n\n<p>2. <b>观点</b>：围绕每个议题，各方提出了哪些关键意见；</p>\n\n<p>3. <b>决策</b>：哪些事项已经形成明确共识；</p>\n\n<p>4. <b>待办</b>：谁需要在什么时间之前完成什么任务。</p>\n\n<p></p>\n\n<p>这套方法非常适合用来复盘会议纪要任务，因为它还原了人工整理时的真实顺序：先降低原始材料中的噪声，再识别内容之间的关系，最后提取需要执行的任务。复盘时还要继续追问几个细节：删减内容是否会改变原意，什么表达才能算作正式决策，负责人或截止时间缺失时怎样处理，材料前后矛盾时是否需要请用户确认。经过这一步，得到的不是一段泛泛的经验总结，而是一条可以继续拆解的处理路线。下一小节再把其中的过滤、分类、提取和复核，改写成 Agent 可以执行的动作、条件和检查点。</p>\n\n<p><b>4、把经验改写成动作、条件和检查点</b></p>\n\n<p>接下来把上述记录中的经验，改写成 Agent 可以执行的三类语句：</p>\n\n<ul><li>动作：读取原始记录，提取明确出现的行动项。</li><li>条件：如果原文没有负责人，则标记为待确认。</li><li>检查点：输出前逐项核对关键结论能否在原文中找到依据。</li></ul>\n\n<p>三类语句要连起来使用。先执行动作，遇到不同情况时按条件分支，完成后用检查点验收。例如，先提取行动项；若负责人或截止时间缺失，则保留行动项并标记待确认；输出前再核对每项任务是否都能在原文中找到依据。如果一句要求无法判断是否完成，就继续把它拆小。</p>\n\n<p><b>5、把规则写进第一版 SKILL.md</b></p>\n\n<p>经过前面的梳理，第一版<code>SKILL.md</code>已经有了材料。写的时候可以按照 Agent 处理任务的顺序组织，比如在会议纪要任务中，需要先总结正常会议的结论，在进一步整理出下一步的任务以及待确认的问题等，以此顺序来输出 Skill 的描述信息。例如：</p>\n\n<pre data-language=\"Markdown\"><code>---\nname: meeting-notes\ndescription: 将会议记录整理为包含结论、行动项和待确认问题的结构化纪要。当用户要求整理会议内容或提取会议任务时使用。\n---\n\n# 会议纪要整理\n\n## 输入要求\n\n- 必须提供可读取的会议正文。\n- 会议名称、时间和参与人可以缺少，但不得自行补写。\n\n## 执行步骤\n\n1. 检查会议正文是否可读。\n2. 区分讨论意见、已经确认的结论和待确认问题。\n3. 提取行动项、负责人和截止时间。\n4. 对照原文复核结论和行动项。\n\n## 输出要求\n\n- 输出会议信息、关键结论、行动项和待确认问题。\n- 原文缺少的负责人或截止时间标记为待确认。\n\n## 异常处理\n\n- 没有会议正文时停止，并请用户补充材料。\n- 不同内容互相冲突时保留冲突，不自行选择答案。</code></pre>\n\n<p><b>6、先跑通最短闭环</b></p>\n\n<p>为了进一步考虑实际使用的底座模型、Agent 工具等，需要对当前整理的 Skill 进行验证。需要先验证整理时 Skill 能否从输入走到合格输出。第一版可以从以下几个方面进行观察：识别任务情况、核心步骤执行情况、输出结果是否满足条件等方面进行观察，进一步地，可以按下面的方法完成第一次验证：</p>\n\n<ol><li>只准备<code>SKILL.md</code>、一份信息完整的会议记录和一份期望的纪要结构。</li><li>用一条明确的 Prompt 提交任务，例如：读取<code>meeting.txt</code>，使用会议纪要 Skill 生成 Markdown 纪要，不要补写原文没有的信息。</li><li>检查 Skill 是否被正确选中，输入是否成功读取，结论和行动项能否回到原文找到依据。</li><li>检查缺失的负责人和日期是否保留为待确认，输出是否符合模板，原始文件是否保持不变。</li><li>记录第一次失败的位置。每次只调整一类规则，再用同一份材料重跑，确认修改确实解决了问题。</li><li>信息完整的样例通过后，再加入缺字段、内容冲突和空文件等测试，防止改好一种情况却破坏另一种情况。</li></ol>\n\n<p>当 Skill 能够被稳定选中、正确读取输入、按规定结构输出，并且面对缺失信息时不编造内容，整体执行过程、输出内容，基本符合 Skill 中的预定，才算跑通。此后再根据真实需求增加其他要求，例如数据清洗工具、脱敏规则、长文本分段或外部系统连接。下一节就在魔搭的环境中完成这条最短路线。</p>\n\n<p></p>\n\n<h2 id=\"c28-s7\">在魔搭中使用 Skill</h2>\n\n<p>前面已经完成了会议纪要 Skill 的设计。下面在魔搭 Notebook 中实现这项能力，以差旅报销和客户接待的办公例会为例，将会议记录整理为已确认事项、待办事项和待确认事项，并检查 Skill 的调用过程。下面按实验步骤介绍主要操作和关键代码，涉及的辅助函数和变量需提前定义。</p>\n\n<p></p>\n\n<h3 id=\"c28-s8\"><b>实验任务与环境准备</b></h3>\n\n<p>本实验使用 Qwen3-4B 完成推理。这里主要补充 Skill 所需的组件，ms-swift 和模型部署的基本用法可参考前面章节。</p>\n\n<p>首先，运行以下代码，查看当前 Python 和依赖包的版本：</p>\n\n<pre data-language=\"Python\"><code>import sys\nimport importlib.metadata as metadata\n\nprint(\"Python：\", sys.version)\nfor package in [\"ms-agent\", \"modelscope\", \"omegaconf\"]:\n    try:\n        print(package, metadata.version(package))\n    except metadata.PackageNotFoundError:\n        print(package, \"未安装\")</code></pre>\n\n<p>本次实验使用 Python 3.12.13、ms-agent 1.6.0、modelscope 1.39.0 和 omegaconf 2.3.0。其中，ms-agent 负责加载 Skill 和读取参考文件，modelscope 用于访问社区资源，ms-swift 和 vLLM 提供模型推理服务。如果依赖尚未准备好，先安装<code>ms-agent==1.6.0</code>等实验组件，保留已有训练包和基础数值计算包的版本。安装完成后重启内核，再继续后续操作。在<code>modelscope_skill_lab</code>下创建带有时间标识的目录，分别保存每次实验的 Skill、案例和报告。代码如下：</p>\n\n<pre data-language=\"Python\"><code>WORK_DIR = (Path.cwd() / \"modelscope_skill_lab\").resolve()\nRUN_DIR = WORK_DIR / (\n    datetime.now().strftime(\"%Y%m%d_%H%M%S\") + \"_\" + uuid.uuid4().hex[:6]\n)\nfor name in [\"releases\", \"tests\", \"reports\", \"downloads\", \"config\"]:\n    (RUN_DIR / name).mkdir(parents=True, exist_ok=True)</code></pre>\n\n<p>其中，<code>releases</code>保存 Skill 版本，<code>tests</code>保存测试输入，<code>reports</code>保存结果，<code>config</code>保存模型配置，便于后续核对和比较。</p>\n\n<p></p>\n\n<h3 id=\"c28-s9\"><b>创建与安装会议纪要 Skill</b></h3>\n\n<p>前面已经说明了 Skill 的组成，这里直接将会议纪要规则写入文件。本实验只需要一个入口文件和一份输出约定，目录形式如下：</p>\n\n<pre data-language=\"Plain Text\"><code>releases/\n└── 1.0.0/\n    └── meeting-notes/\n        ├── SKILL.md\n        └── references/\n            └── output-contract.md</code></pre>\n\n<p><code>SKILL.md</code>保存适用范围、处理步骤和异常规则，开头的名称、描述和版本用于标识这项能力。头部内容如下：</p>\n\n<pre data-language=\"YAML\"><code>---\nname: meeting-notes\ndescription: 将会议转写稿、群聊会议记录或会议笔记整理为中文会议纪要，提取已确认的结论、行动项和待确认问题。用户要求会议纪要或会议行动项时使用；一般知识问答、模型训练原理解释不使用。\nversion: \"1.0.0\"\n---</code></pre>\n\n<p>在执行步骤中，本实验特别保留了三类约束：没有会议正文时请求补充材料；负责人和截止时间没有明确出现时标为“待确认”；结论和行动项必须引用原文作为依据。这些规则会在 17.8 的测试中逐项检查。</p>\n\n<p>为了便于检查，模型先输出 JSON，再由程序整理成会议纪要。<code>output-contract.md</code>规定输出字段，并增加处理状态和原文依据，主要内容如下：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col></colgroup><tbody><tr><td><p>字段</p></td><td><p>内容</p></td></tr><tr><td><p><code>status</code></p></td><td><p><code>ok</code>、<code>needs_input</code>或<code>unsupported</code></p></td></tr><tr><td><p><code>title</code></p></td><td><p>会议主题</p></td></tr><tr><td><p><code>decisions</code></p></td><td><p>已确认的结论及原文依据</p></td></tr><tr><td><p><code>actions</code></p></td><td><p>任务、负责人、截止时间和原文依据</p></td></tr><tr><td><p><code>unresolved</code></p></td><td><p>待确认事项或存在的冲突</p></td></tr><tr><td><p><code>message</code></p></td><td><p>需要补充的材料或无法处理的原因</p></td></tr></tbody></table>\n</div>\n<p>其中，<code>decisions</code>和<code>actions</code>中的<code>evidence</code>字段要求引用原文的连续片段。模型可以概括任务内容，但用于核对的证据不能改写。当<code>status</code>不为<code>ok</code>时，结论和行动项应为空数组，同时在<code>message</code>中说明原因。</p>\n\n<p>将入口内容和输出约定分别保存在<code>SKILL_V1</code>、<code>OUTPUT_CONTRACT</code>两个变量中，再写入对应文件：</p>\n\n<pre data-language=\"Python\"><code>V1_DIR = RUN_DIR / \"releases\" / \"1.0.0\" / SKILL_NAME\n(V1_DIR / \"references\").mkdir(parents=True, exist_ok=True)\n(V1_DIR / \"SKILL.md\").write_text(SKILL_V1, encoding=\"utf-8\")\n(V1_DIR / \"references\" / \"output-contract.md\").write_text(\n    OUTPUT_CONTRACT, encoding=\"utf-8\"\n)</code></pre>\n\n<p>运行以后，可以在文件浏览器中找到上述目录，打开<code>SKILL.md</code>确认内容已经写入。相关内容如下图所示。</p>\n\n<p>当然，魔搭 Skills 中心（<u>https://modelscope.cn/skills</u>）也提供了相关资源，读者可以直接从魔搭 Skills 中心选择已有资源，将页面中的安装标识填入<code>COMMUNITY_SKILL_ID</code>，通过下面的接口下载：</p>\n\n<pre data-language=\"Python\"><code>COMMUNITY_SKILL_ID = \"\"  # 填写从社区页面复制的安装标识。\nif COMMUNITY_SKILL_ID:\n    COMMUNITY_DIR = Path(HubApi().download_skill(\n        skill_id=COMMUNITY_SKILL_ID,\n        local_dir=str(RUN_DIR / \"downloads\"),\n    )).resolve()\n    print((COMMUNITY_DIR / \"SKILL.md\").read_text(encoding=\"utf-8\"))</code></pre>\n\n<p></p>\n\n<h3 id=\"c28-s10\"><b>加载 Skill 与读取参考文件</b></h3>\n\n<p>文件准备好后，使用 ms-agent 的<code>SkillLoader</code>加载本地目录，读取名称、描述、版本和资源路径。代码如下：</p>\n\n<pre data-language=\"Python\"><code>from ms_agent.skill.loader import SkillLoader\nfrom ms_agent.skill.schema import SkillContext\n\nloader = SkillLoader()\nregistered = loader.load_skills(str(V1_DIR.resolve()))\nprint(loader.list_skills())</code></pre>\n\n<p>本次加载记录为<code>['meeting-notes@1.0.0']</code>。其中，<code>meeting-notes</code>是 Skill 的 ID，<code>1.0.0</code>是规则版本，与 ms-agent 软件包的版本不同。加载目录以后，可以通过<code>SkillContext</code>读取参考文件。下面的代码只加载名为<code>output-contract.md</code>的参考资料：</p>\n\n<pre data-language=\"Python\"><code>skill = next(iter(registered.values()))\ncontext = SkillContext(skill=skill, root_path=V1_DIR)\nreferences = context.load_references(names=[\"output-contract.md\"])\nprint(references[0][\"content\"])</code></pre>\n\n<p>为了让模型请求读取文件，本实验通过自定义的<code>NotebookSkillTools</code>封装了两个工具函数：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col><col></colgroup><tbody><tr><td><p>工具函数</p></td><td><p>作用</p></td><td><p>主要参数</p></td></tr><tr><td><p><code>skills_list</code></p></td><td><p>列出已加载 Skill 的 ID、版本和用途</p></td><td><p>无</p></td></tr><tr><td><p><code>skill_view</code></p></td><td><p>读取 Skill 入口或指定参考文件</p></td><td><p><code>skill_id</code>、可选的<code>file_path</code></p></td></tr></tbody></table>\n</div>\n<p>读取入口时，调用<code>skill_view</code>并传入<code>{\"skill_id\": \"meeting-notes\"}</code>；读取参考文件时，再指定<code>file_path</code>。其中，<code>meeting-notes</code>是读取对象，<code>skill_view</code>是执行读取的工具。</p>\n\n<p>加载与读取检查如下：</p>\n\n<pre data-language=\"Python\"><code>ACTIVE_DIR = V1_DIR\nruntime = make_runtime(ACTIVE_DIR)\n\nviewed = runtime.tools.call_tool(\n    tool_name=\"skill_view\",\n    tool_args={\"skill_id\": SKILL_NAME},\n)\nreference = runtime.tools.call_tool(\n    tool_name=\"skill_view\",\n    tool_args={\n        \"skill_id\": SKILL_NAME,\n        \"file_path\": \"references/output-contract.md\",\n    },\n)\nprint(\"入口读取成功：\", \"content\" in json.loads(viewed))\nprint(\"参考资料读取成功：\", \"content\" in json.loads(reference))</code></pre>\n\n<p><code>make_runtime()</code>负责校验目录、加载版本和准备工具。本次入口和参考资料的读取结果均为<code>True</code>，说明文件能够正常访问，接下来就可以让模型发起读取请求。Skill 加载与资源读取结果如下。</p>\n\n<p></p>\n\n<h3 id=\"c28-s11\"><b>部署模型并配置推理接口</b></h3>\n\n<p>本实验使用魔搭社区的<code>Qwen/Qwen3-4B</code>，通过 ms-swift 部署为 OpenAI 兼容接口。在已安装 ms-swift 和 vLLM 的 GPU 环境终端中运行：</p>\n\n<pre data-language=\"Bash\"><code>CUDA_VISIBLE_DEVICES=0 swift deploy \\\n  --model Qwen/Qwen3-4B \\\n  --load_args false \\\n  --infer_backend vllm \\\n  --enable_thinking false \\\n  --host 0.0.0.0 \\\n  --port 18002 \\\n  --api_key 123 \\\n  --vllm_gpu_memory_utilization 0.8 \\\n  --vllm_max_model_len 8000 \\\n  --max_new_tokens 2000</code></pre>\n\n<p></p>\n\n<p>在魔搭中，模型启动阶段的结果如下：</p>\n\n<p>服务日志，当输出如下内容表示服务已启动完毕：</p>\n\n<pre data-language=\"Markdown\"><code>INFO:Application startup complete.\nINFO:Uvicorn running on http://0.0.0.0:18002 (Press CTRL+C to quit)</code></pre>\n\n<p>魔搭端启动成功的日志如下：</p>\n\n<p></p>\n\n<h3 id=\"c28-s12\"><b>通过办公对话调用 Skill</b></h3>\n\n<p>下面使用一份行政例会记录进行调用。材料中包含两项已经确认的安排、两名负责人的具体任务，以及一项仍需确认的信息：</p>\n\n<pre data-language=\"Plain Text\"><code>会议主题：本周行政工作安排。会议时间：2026-09-04。\n主管：今天确认两件事。第一，本月差旅报销材料统一在下周二前收齐；\n第二，客户接待会定在下周三下午三点，地点是三楼会议室。\n李明：报销材料我来收集，截止时间为2026-09-08。\n王敏：我负责预订三楼会议室并发送会议邀请，截止时间为2026-09-07。\n小周：客户可能有两个人线上参加，不过还没确认。\n主管：线上参会名单先记为待确认，其他事项按刚才的安排推进。</code></pre>\n\n<p>模型需要整理会议中已经确认的安排，列出任务、负责人和截止时间，并保留尚未确定的信息。这里生成的是纪要，不实际办理报销或发送邀请。先使用明确指定 Skill 的请求，再使用日常办公中常见的自然语言请求，对比两种表达能否调用同一项能力。核心调用代码如下：</p>\n\n<pre data-language=\"Python\"><code>explicit_result = await run_skill_task(\n    \"用 meeting-notes 帮我整理一下刚才的办公例会，\"\n    \"把报销和客户接待的安排、负责人、截止时间列清楚。\"\n    + \"\\n\\n会议记录：\\n\" + DEMO_TEXT,\n    ACTIVE_DIR,\n)\n\nnatural_result = await run_skill_task(\n    \"这是刚才例会的记录，帮我整理成一份简短纪要，\"\n    \"方便发到工作群。还没确定的事情单独列出来。\"\n    + \"\\n\\n会议记录：\\n\" + DEMO_TEXT,\n    ACTIVE_DIR,\n)</code></pre>\n\n<p>其中，<code>run_skill_task()</code>为每次任务新建对话，并将 Skill 索引和工具定义发送给模型。模型提出读取请求后，由程序执行文件读取，再把内容返回给模型。为了避免模型反复读取已经获得的资料，程序会检查入口和输出约定是否都已读取成功。两者都具备后，进入最终答案生成阶段。关键判断如下：</p>\n\n<pre data-language=\"Python\"><code>ready_to_answer = (\n    read_skill_successfully(result)\n    and read_skill_successfully(result, \"references/output-contract.md\")\n)\n\nif ready_to_answer:\n    messages[0][\"content\"] = (\n        system + \"\\n技能入口和输出规则已读取完成。\"\n        \"现在根据原始会议材料生成最终结果，\"\n        \"只输出符合规则的 JSON，不再调用工具。\"\n    )\n\nmessage, finish_reason, usage = await asyncio.to_thread(\n    model_call, messages, [] if ready_to_answer else api_tools\n)</code></pre>\n\n<p>资料读取完成后，请求使用<code>tool_choice=\"none\"</code>，让模型生成最终答案。提示更新在已有的<code>system</code>消息中，工具结果后仍由模型回复，保持角色顺序。本次两种请求都读取了 Skill 和输出约定，生成了办公纪要。结果保留了报销和客户接待安排，列出李明、王敏的任务，并将线上参会人数记为待确认。不同调用的措辞略有差异，可对照原文复核。</p>\n\n<p>页面展示的是整理后的纪要，保存的 JSON 仍包含<code>evidence</code>等字段。原文引用和异常输入的处理，将在后面的测试中继续检查。</p>\n\n<p></p>\n\n<h3 id=\"c28-s13\"><b>检查与调试调用过程</b></h3>\n\n<p>调用完成后，检查入口、输出约定是否读取，以及读取失败时能否报告原因。<code>trace</code>保存了每轮模型回复、工具参数和返回内容，可用于定位问题。例如，检查前面两次调用的读取情况，可以使用下面的代码：</p>\n\n<pre data-language=\"Python\"><code>rows = []\nfor label, result in [(\"显式调用\", explicit_result), (\"自然语言调用\", natural_result)]:\n    rows.append({\n        \"调用方式\": label,\n        \"请求状态\": result[\"run_status\"],\n        \"入口已读取\": read_skill_successfully(result),\n        \"规则已读取\": read_skill_successfully(\n            result, \"references/output-contract.md\"\n        ),\n    })\ndisplay(pd.DataFrame(rows))</code></pre>\n\n<p>本次两种调用均为<code>completed</code>，入口和规则读取均为<code>True</code>，说明请求结束且资料已读取。纪要内容是否正确，还需要进一步检查。为了检查工具的异常处理，还可以故意传入不存在的 Skill ID 和不存在的参考文件。例如：</p>\n\n<pre data-language=\"Python\"><code>missing_resource = runtime.tools.call_tool(\n    tool_name=\"skill_view\",\n    tool_args={\n        \"skill_id\": SKILL_NAME,\n        \"file_path\": \"references/does-not-exist.md\",\n    },\n)\nprint(\"缺失文件处理是否符合预期：\", \"error\" in json.loads(missing_resource))</code></pre>\n\n<p>这里故意使用不存在的文件，返回<code>error</code>表示正确识别了问题。表示本次所传入的内容将其标为“处理是否符合预期”，可以避免与实际运行故障混淆。</p>\n\n<p>常规展示保留检查结果即可。需要排查时，再从<code>reports</code>中查看对应轮次的完整回复和工具记录。</p>\n\n<p></p>\n\n<h2 id=\"c28-s14\">Skill 的测试与版本</h2>\n\n<p>一次调用完成后，还需要用不同材料检查 Skill 是否可靠。下面准备固定案例，验证信息提取和异常处理，并将结果与 Skill 版本一起保存。</p>\n\n<p>本节继续使用同一份会议纪要 Skill。先建立测试矩阵和检查条件，再运行第一版测试，最后修改规则、比较两个版本并演示回退。</p>\n\n<p></p>\n\n<h3 id=\"c28-s15\"><b>准备测试案例</b></h3>\n\n<p>案例需要同时记录输入和预期行为。正常案例检查信息提取，边界案例检查不确定信息的处理，失败案例检查能否停止并说明原因，另设不相关任务检查误触发。</p>\n\n<p>测试矩阵如下：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col><col><col></colgroup><tbody><tr><td><p>编号</p></td><td><p>类别</p></td><td><p>输入特征</p></td><td><p>预期行为</p></td></tr><tr><td><p>N01</p></td><td><p>正常</p></td><td><p>包含报销和客户接待安排的完整办公例会</p></td><td><p>保留结论、任务、负责人和截止时间</p></td></tr><tr><td><p>N02</p></td><td><p>正常</p></td><td><p>已经写明行动项的简短会议笔记</p></td><td><p>提取明确的任务、负责人和日期</p></td></tr><tr><td><p>B01</p></td><td><p>边界</p></td><td><p>已经安排任务，但未确定截止时间</p></td><td><p>日期填写“待确认”</p></td></tr><tr><td><p>B02</p></td><td><p>边界</p></td><td><p>有人提出建议，会议尚未批准</p></td><td><p>不生成已确认决定，保留待确认事项</p></td></tr><tr><td><p>B03</p></td><td><p>边界</p></td><td><p>两份记录对同一事项的时间表述冲突</p></td><td><p>保留冲突，不自行选择结论</p></td></tr><tr><td><p>B04</p></td><td><p>边界</p></td><td><p>有人提出需要完成一项工作，但未指定负责人</p></td><td><p>不把发言人自动作为负责人</p></td></tr><tr><td><p>F01</p></td><td><p>失败</p></td><td><p>没有会议正文</p></td><td><p>返回<code>needs_input</code>并说明需要补充材料</p></td></tr><tr><td><p>F02</p></td><td><p>失败</p></td><td><p>提供的是产品说明书</p></td><td><p>返回<code>unsupported</code>，不生成会议结论和行动项</p></td></tr><tr><td><p>T01</p></td><td><p>不应触发</p></td><td><p>一般知识问答，与会议纪要无关</p></td><td><p>直接回答，不读取会议纪要 Skill</p></td></tr></tbody></table>\n</div>\n<p>每个案例用一个字典保存输入和检查条件。下面分别选取一个正常案例、边界案例和失败案例，展示其写法：</p>\n\n<pre data-language=\"Python\"><code># 办公例会中的两项截止日期应与原文保持一致。\nnormal_case = {\n    \"id\": \"N01\", \"kind\": \"正常\", \"text\": DEMO_TEXT, \"status\": \"ok\",\n    \"owners\": [\"李明\", \"王敏\"],\n    \"due_values\": [\"2026-09-08\", \"2026-09-07\"],\n    \"min_actions\": 2, \"min_decisions\": 1,\n}\n\nboundary_case = {\n    \"id\": \"B01\", \"kind\": \"边界\",\n    \"text\": \"测试会议记录：已安排陈晨整理测试用例，会上没有确定截止时间。\",\n    \"status\": \"ok\", \"owners\": [\"陈晨\"],\n    \"due_values\": [\"待确认\"], \"min_actions\": 1,\n}\n\nfailure_case = {\n    \"id\": \"F01\", \"kind\": \"失败\", \"text\": \"\", \"status\": \"needs_input\",\n}</code></pre>\n\n<p><code>status</code>规定预期状态，<code>owners</code>和<code>due_values</code>检查负责人和日期，<code>min_actions</code>规定最少行动项数量。这些条件只用于验收，模型接收的仍是用户请求和案例正文。</p>\n\n<p>更换材料后，预期日期和负责人也要同步更新。上面的 N01 已按办公例会填写日期；已保存的测试记录仍有旧日期预期，后面会说明其影响。</p>\n\n<p></p>\n\n<h3 id=\"c28-s16\"><b>定义检查条件</b></h3>\n\n<p>案例准备好以后，需要把“正确完成”的要求写成可以执行的检查。使用<code>check_output()</code>对模型返回结果进行验收，主要包括下面几个方面：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col></colgroup><tbody><tr><td><p>检查内容</p></td><td><p>判断方法</p></td></tr><tr><td><p>请求是否结束</p></td><td><p>检查<code>run_status</code>，区分正常返回、服务错误和轮次耗尽</p></td></tr><tr><td><p>资料是否读取</p></td><td><p>检查工具记录中入口和输出约定的实际返回</p></td></tr><tr><td><p>输出格式是否正确</p></td><td><p>检查 JSON 能否解析，以及字段名称和类型是否符合约定</p></td></tr><tr><td><p>关键信息是否符合案例</p></td><td><p>检查状态、负责人、截止时间和条目数量</p></td></tr><tr><td><p>原文依据是否存在</p></td><td><p>检查<code>evidence</code>是否为会议正文中的连续片段</p></td></tr><tr><td><p>失败输入是否停止生成</p></td><td><p>检查异常状态下结论和行动项为空，并说明原因</p></td></tr><tr><td><p>是否误用 Skill</p></td><td><p>对不相关任务检查是否发生了<code>skill_view</code>调用</p></td></tr></tbody></table>\n</div>\n<p>以原文依据为例，模型生成的结论可以是概括性的文字，但<code>evidence</code>需要保留原始表述。下面是验收函数中的关键判断：</p>\n\n<pre data-language=\"Python\"><code>if item[\"evidence\"] not in case[\"text\"]:\n    problems.append(field + \" 的证据不在原文中\")</code></pre>\n\n<p><code>item</code>表示当前条目，<code>field</code>表示正在检查的字段。该判断可发现引用被改写或引用来源错误，但证据能否支持结论，仍需人工复核。对于没有正文或材料不适用的案例，还需要检查模型有没有停止生成纪要内容。关键代码如下：</p>\n\n<pre data-language=\"Python\"><code>if data[\"status\"] != case[\"status\"]:\n    problems.append(\"status 不符合预期\")\n\nif case[\"status\"] != \"ok\":\n    if data[\"decisions\"] or data[\"actions\"] or not data[\"message\"].strip():\n        problems.append(\"失败情形应停止生成纪要内容并说明原因\")</code></pre>\n\n<p>例如，F01 应返回<code>needs_input</code>。如果仍返回<code>ok</code>，或者说明材料不足的同时生成了会议决定，都不符合要求。还可以人为加入错误日期、删除读取记录，检查验收函数能否识别问题。这是检查代码的自测，不能代替真实模型测试。文件缺失、模拟服务失败和输入过长用于检查程序能否报告异常；F01、F02 则检查模型面对异常材料时的回答。两类结果分别记录。</p>\n\n<p></p>\n\n<h3 id=\"c28-s17\"><b>运行测试并分析结果</b></h3>\n\n<p><code>run_suite()</code>依次运行案例，每次新建对话，保存结果和工具记录，再调用<code>check_output()</code>生成问题清单。主要代码如下：</p>\n\n<pre data-language=\"Python\"><code>CASE_IDS = None   # None表示运行全部案例。\nREPEATS = 1       # 本次每个案例运行一次。\n\nv1_results, v1_report_dir = await run_suite(\n    V1_DIR, case_ids=CASE_IDS, repeats=REPEATS\n)\ndisplay(v1_results)\nprint(\"原始输出和轨迹：\", v1_report_dir)</code></pre>\n\n<p><code>PASS_AUTO</code>表示自动检查通过，<code>FAIL</code>表示结果不符合要求，<code>ERROR</code>表示运行未正常完成。<code>human_review</code>记录人工复核状态，本次仍为<code>pending</code>。本次 1.0.0 版本的实际记录中，9 个案例有 6 个自动通过、3 个失败，未出现标记为<code>ERROR</code>的案例。结果如下：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col><col></colgroup><tbody><tr><td><p>案例</p></td><td><p>自动检查结果</p></td><td><p>记录中的主要问题</p></td></tr><tr><td><p>N01</p></td><td><p>FAIL</p></td><td><p>结论的证据未匹配原文；旧的截止日期预期未满足</p></td></tr><tr><td><p>N02</p></td><td><p>PASS_AUTO</p></td><td><p>未触发当前检查条件中的问题</p></td></tr><tr><td><p>B01</p></td><td><p>PASS_AUTO</p></td><td><p>未触发当前检查条件中的问题</p></td></tr><tr><td><p>B02</p></td><td><p>PASS_AUTO</p></td><td><p>未触发当前检查条件中的问题</p></td></tr><tr><td><p>B03</p></td><td><p>PASS_AUTO</p></td><td><p>未触发当前检查条件中的问题</p></td></tr><tr><td><p>B04</p></td><td><p>PASS_AUTO</p></td><td><p>未触发当前检查条件中的问题</p></td></tr><tr><td><p>F01</p></td><td><p>FAIL</p></td><td><p>处理状态不符合预期，未满足异常输入的停止生成要求</p></td></tr><tr><td><p>F02</p></td><td><p>FAIL</p></td><td><p>处理状态不符合预期，未满足异常输入的停止生成要求</p></td></tr><tr><td><p>T01</p></td><td><p>PASS_AUTO</p></td><td><p>未发生会议纪要 Skill 的读取请求</p></td></tr></tbody></table>\n</div>\n<p>N01 首先存在测试预期不一致的问题：正文中王敏的截止日期为<code>2026-09-07</code>，测试代码却仍要求<code>2026-09-09</code>。因此，这项日期报错不能直接归为模型错误，应先修正<code>due_values</code>再重跑。N01 还存在证据未匹配原文的问题。同一办公示例的调用轨迹中，模型将原文“第二，客户接待会定在下周三下午三点，地点是三楼会议室。”改成了“主管：客户接待会定在下周三下午三点，地点是三楼会议室。”。含义虽然接近，但增加了该片段中没有的称谓，不符合<code>evidence</code>逐字摘录的要求。</p>\n\n<p>F01 没有正文，应请求补充材料；F02 提供产品说明书，应说明材料不适用。本次两项均未满足状态和停止生成要求，需要结合案例报告中的最终文本和工具轨迹进一步分析。后续应先校准测试预期，再完善证据引用和异常处理规则。自动通过的案例也要对照原文复核。</p>\n\n<p></p>\n\n<h3 id=\"c28-s18\"><b>修改 Skill 并比较版本</b></h3>\n\n<p></p>\n\n<p>保留 1.0.0，在新目录中创建候选版本 1.0.1，并补充负责人判定规则。两个版本分别加载和测试，便于比较。下面是版本创建代码的主要部分：</p>\n\n<pre data-language=\"Python\"><code>V2_DIR = RUN_DIR / \"releases\" / \"1.0.1\" / SKILL_NAME\nif not V2_DIR.exists():\n    shutil.copytree(V1_DIR, V2_DIR)\n    updated = (V2_DIR / \"SKILL.md\").read_text(encoding=\"utf-8\")\n    updated = updated.replace('version: \"1.0.0\"', 'version: \"1.0.1\"', 1)\n    updated += (\n        \"\\n## 负责人判定补充\\n\"\n        \"发言人不自动等于负责人；提出安排、建议或疑问的人，\"\n        \"没有明确接受任务时，负责人仍为“待确认”。\\n\"\n    )\n    (V2_DIR / \"SKILL.md\").write_text(updated, encoding=\"utf-8\")</code></pre>\n\n<p>这项规则针对 B04：提出工作的人不一定是负责人。由于输入输出结构未变，本实验将其记为修订版本，并在<code>CHANGELOG.md</code>中记录规则、关联案例和候选状态。</p>\n\n<p>接下来使用相同的模型、案例和重复次数测试候选版本，再按案例编号进行合并比较。核心代码如下：</p>\n\n<pre data-language=\"Python\"><code>v2_results, v2_report_dir = await run_suite(\n    V2_DIR, case_ids=CASE_IDS, repeats=REPEATS\n)\ncomparison = v1_results.merge(\n    v2_results,\n    on=[\"case_id\", \"repeat\", \"kind\"],\n    suffixes=(\"_v1\", \"_v2\"),\n)\ndisplay(comparison[[\n    \"case_id\", \"automatic_result_v1\", \"automatic_result_v2\",\n    \"problems_v1\", \"problems_v2\",\n]])</code></pre>\n\n<p>相关过程结果如下：</p>\n\n<p>在本次保存的记录中，1.0.0 有 6 项自动通过，1.0.1 有 7 项自动通过。逐项对比以后，可以看到变化集中在 F01：</p>\n\n<p>本次只观察到 F01 由失败变为自动通过，尚不能证明新增规则带来了稳定改善。修改针对负责人判定，变化却发生在空输入案例；每例也只运行一次，N01 预期仍需校准。采用候选版本前，应修正测试条件，让两版重跑相同案例，并提高<code>REPEATS</code>检查稳定性。人工复核后，再决定是否采用；F02 等未通过的情况继续完善规则。</p>\n\n<p></p>\n\n<h3 id=\"c28-s19\"><b>保存版本与回退</b></h3>\n\n<p>每个版本都需要保存完整文件和测试记录。本实验在<code>release-manifest.json</code>中记录文件清单和哈希，同时保存变更说明、配置和报告。主要产物如下：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col></colgroup><tbody><tr><td><p>文件或目录</p></td><td><p>保存内容</p></td></tr><tr><td><p><code>releases/1.0.0/</code>、<code>releases/1.0.1/</code></p></td><td><p>两个版本的 Skill 目录和版本清单</p></td></tr><tr><td><p><code>CHANGELOG.md</code></p></td><td><p>规则变化及关联案例</p></td></tr><tr><td><p><code>tests/</code></p></td><td><p>固定的测试输入与预期条件</p></td></tr><tr><td><p><code>reports/</code></p></td><td><p>模型输出、工具轨迹、自动检查结果和版本比较表</p></td></tr><tr><td><p><code>config/active-skill.json</code></p></td><td><p>当前使用的版本、路径和文件哈希</p></td></tr><tr><td><p><code>environment.json</code>、<code>pip-freeze.txt</code></p></td><td><p>本次运行环境记录</p></td></tr></tbody></table>\n</div>\n<p><code>activate_version()</code>检查目录和文件哈希，再创建新的运行器，保证版本清单与实际规则一致。</p>\n\n<pre data-language=\"Python\"><code>ACTIVE_DIR, runtime = activate_version(\"1.0.1\")\nprint(\"切到候选版：\", ACTIVE_DIR)\n\nACTIVE_DIR, runtime = activate_version(\"1.0.0\")\nprint(\"回退到旧版：\", ACTIVE_DIR)</code></pre>\n\n<p>本次已完成从 1.0.1 切回 1.0.0 的文件校验和加载，但回退后没有再次调用模型，尚未验证回退后的测试结果。</p>\n\n<p>如果需要确认回退后的行为，应继续对当前目录执行测试：</p>\n\n<pre data-language=\"Python\"><code>rollback_results, rollback_report_dir = await run_suite(\n    ACTIVE_DIR, case_ids=CASE_IDS, repeats=REPEATS\n)\ndisplay(rollback_results)</code></pre>\n\n<p>保存规则、案例、结果和环境信息后，就能追溯每个版本的表现。后续修改应对应具体案例，经过测试和复核，再决定采用新版本还是恢复原版。</p>\n\n<p></p>\n\n<p>本章节所有实验数据和代码，可参考：</p>\n\n<p>https://modelscope.cn/gallery/liucong/0e182f07-3330-40cc-ba59-173b7cc609b7</p>\n","imageCount":0,"attachmentCount":0,"discussionId":"chapter-26"},{"id":"chapter-29","number":29,"title":"快速使用 Claude Code","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-29.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-29.html","minutes":9,"status":"ready","headings":[{"id":"c29-s1","text":"Claude Code 是什么？","level":"h2"},{"id":"c29-s2","text":"安装前，准备好这几样","level":"h2"},{"id":"c29-s3","text":"安装 Claude Code","level":"h2"},{"id":"c29-s4","text":"进入项目，开始第一次对话","level":"h2"},{"id":"c29-s5","text":"把 Kimi 接入 Claude Code","level":"h2"},{"id":"c29-s6","text":"让 Claude Code 真正开始干活","level":"h2"}],"html":"<p>这篇我们直接上手 Claude Code，从安装、登录、接入模型，一直用到自己的项目里。</p>\n\n<p>如果你想让 AI 帮你改代码、整理文章，或者开发一个每天都能用上的小工具，可以跟着往下做。</p>\n\n<h2 id=\"c29-s1\">Claude Code 是什么？</h2>\n\n<p>Claude Code 是 Anthropic 推出的 AI 编程工具。你给它一个任务，它可以读取项目文件、分析实现方式、修改代码，再运行命令检查结果。</p>\n\n<img src=\"assets/manuscript-20260914/c27-0c498f3aa4fdd2.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>它的工作过程，可以理解为：<b>理解需求 → 查看文件 → 制定方案 → 执行修改 → 检查结果。</b></p>\n\n<p>比如，你可以让它解释一个看不懂的项目，排查一个报错，补一个功能，也可以让它读取本地的 Markdown 文章，生成摘要、整理目录、改写成不同平台的文案。</p>\n\n<p>这篇主要使用终端版。安装后，在项目文件夹里输入 <code>claude</code>，就能开始对话；习惯图形界面的朋友，也可以选择桌面应用或编辑器集成。<a href=\"https://code.claude.com/docs/en/overview\" target=\"_blank\" rel=\"noopener noreferrer\">Claude Code 产品介绍</a></p>\n\n<p><b>先跑通一个小任务，后面再逐步加上项目规则、Skills 和外部工具。</b></p>\n\n<blockquote><p>截图用于说明操作位置与界面效果，具体版本、模型名称和套餐信息以实际页面为准。命令请使用正文中的代码块。</p></blockquote>\n\n<h2 id=\"c29-s2\">安装前，准备好这几样</h2>\n\n<p>先检查电脑环境：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col></colgroup><thead><tr><th><p>项目</p></th><th><p>要求</p></th></tr></thead><tbody><tr><td><p>macOS</p></td><td><p>macOS 13 或更高版本</p></td></tr><tr><td><p>Windows</p></td><td><p>Windows 10 1809 或更高版本，可以原生使用，也可以使用 WSL</p></td></tr><tr><td><p>Linux</p></td><td><p>Ubuntu 20.04+、Debian 10+ 等受支持的发行版</p></td></tr><tr><td><p>内存</p></td><td><p>至少 4GB</p></td></tr><tr><td><p>网络</p></td><td><p>能正常连接所选模型服务，完成认证和请求</p></td></tr><tr><td><p>Node.js</p></td><td><p>原生安装不需要；选择 npm 安装时，需要 Node.js 22 或更高版本</p></td></tr></tbody></table>\n</div>\n<p>Windows 推荐安装 Git for Windows，以便使用 Bash 工具；没有安装时，当前版本也支持使用 PowerShell 工具。完整要求见<a href=\"https://code.claude.com/docs/en/setup\" target=\"_blank\" rel=\"noopener noreferrer\">安装文档</a>。</p>\n\n<p>接着确定模型从哪里来：</p>\n\n<ul><li>有 Claude Pro、Max，或者已开通相应权限的 Team、Enterprise 账号，可以用 Claude 账号登录。</li><li>使用 Claude Console，则按 API 用量计费。</li><li>想接入 Kimi，可以按第 5 节配置对应的 API 服务。</li></ul>\n\n<p>Claude 免费账号不包含 Claude Code 使用权限。Claude 订阅和 Console API 余额是两套计费方式，购买订阅不会自动给 API 账户充值。<a href=\"https://code.claude.com/docs/en/authentication\" target=\"_blank\" rel=\"noopener noreferrer\">账号与认证说明</a></p>\n\n<h2 id=\"c29-s3\">安装 Claude Code</h2>\n\n<p>下面两种安装方式，<b>选一种即可</b>。</p>\n\n<p><b>方式一：原生安装</b></p>\n\n<p>macOS 打开“终端”；Linux 或 WSL 打开自己的终端，输入：</p>\n\n<pre data-language=\"bash\"><code>curl -fsSL https://claude.ai/install.sh | bash</code></pre>\n\n<p>Windows 在开始菜单中搜索并打开 PowerShell，输入：</p>\n\n<pre data-language=\"powershell\"><code>irm https://claude.ai/install.ps1 | iex</code></pre>\n\n<p>注意，Windows 这条命令要在 <b>PowerShell</b> 中执行。原生安装默认自动更新，安装命令来自<a href=\"https://code.claude.com/docs/en/quickstart\" target=\"_blank\" rel=\"noopener noreferrer\">官方快速开始</a>。</p>\n\n<p><b>方式二：通过 npm 安装</b></p>\n\n<p>如果电脑已经装好 Node.js，也可以走 npm。</p>\n\n<p>先在终端检查版本：</p>\n\n<pre data-language=\"bash\"><code>node -v\nnpm -v</code></pre>\n\n<p>Node.js 需要 22 或更高版本。还没安装的朋友，可以从 <a href=\"https://nodejs.org/en/download\" target=\"_blank\" rel=\"noopener noreferrer\">Node.js 官网</a>获取符合要求的版本。</p>\n\n<p>Windows 使用这条路线时，可以按 <code>Windows + R</code>，输入 <code>cmd</code>，点击“确定”，打开命令提示符：</p>\n\n<img src=\"assets/manuscript-20260914/c27-54353c06d54dd2.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>然后执行安装命令，macOS、Linux 也可以在自己的终端里执行同一条：</p>\n\n<pre data-language=\"bash\"><code>npm install -g @anthropic-ai/claude-code</code></pre>\n\n<p>看到安装完成的提示后，就可以继续了：</p>\n\n<img src=\"assets/manuscript-20260914/c27-55d0b16cc95231.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>如果 npm 报权限错误，先检查 Node.js 的安装方式，或改用前面的原生安装方式。不要直接给安装命令加 <code>sudo</code>。<a href=\"https://code.claude.com/docs/en/setup\" target=\"_blank\" rel=\"noopener noreferrer\">npm 安装说明</a></p>\n\n<p><b>最后，检查是否装成功。</b></p>\n\n<p>无论选择哪种方式，都在终端执行：</p>\n\n<pre data-language=\"bash\"><code>claude --version\nclaude doctor</code></pre>\n\n<p>第一条显示版本号，第二条检查安装环境。能显示版本，说明程序已经装好，下一步完成账号登录。</p>\n\n<h2 id=\"c29-s4\">进入项目，开始第一次对话</h2>\n\n<p><b>第一步：打开自己的项目目录。</b></p>\n\n<p>先准备一个测试文件夹，可以放一个小项目，也可以放几篇文章的副本。</p>\n\n<p>在终端输入：</p>\n\n<pre data-language=\"bash\"><code>cd \"你的项目文件夹路径\"\nclaude</code></pre>\n\n<p>把引号里的内容换成自己的实际路径，路径里有空格时保留引号。</p>\n\n<p>下面这张图，就是用 <code>cd</code> 进入文件夹的过程。前面的路径变了，说明终端已经切换到对应目录：</p>\n\n<img src=\"assets/manuscript-20260914/c27-c8afa6f6553dbe.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>记住，<b>你在哪个文件夹里启动 Claude Code，它就从哪个工作目录开始处理任务。</b></p>\n\n<p><b>第二步：选择界面主题。</b></p>\n\n<p>首次启动时，按提示完成界面设置。如果出现主题选择，使用方向键选择，再按回车确认。</p>\n\n<p>我选的是 Dark，黑底界面看着挺舒服。</p>\n\n<img src=\"assets/manuscript-20260914/c27-422e7a719360c9.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p><b>第三步：选择登录方式。</b></p>\n\n<p>有 Claude 订阅的朋友，选择 Claude 账号；使用官方 API 的朋友，选择 Console 对应的入口。</p>\n\n<img src=\"assets/manuscript-20260914/c27-17cbb2cca1037f.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>按照提示在浏览器完成授权，完成后回到终端。如果浏览器没有自动打开，就复制界面提供的登录链接。</p>\n\n<p>需要重新登录时，在 Claude Code 对话里输入：</p>\n\n<pre data-language=\"text\"><code>/login</code></pre>\n\n<p>使用 Kimi 的朋友，可以先跳到第 5 节完成配置，再回来测试。</p>\n\n<p><b>第四步：确认状态，发一句话。</b></p>\n\n<p>在 Claude Code 中输入：</p>\n\n<pre data-language=\"text\"><code>/status</code></pre>\n\n<p>检查当前目录、账号或凭据来源、模型配置，再发一句“你好”。</p>\n\n<p>终端里的对话效果如下：</p>\n\n<img src=\"assets/manuscript-20260914/c27-d81112228d0ee0.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>能收到回复以后，再让它做一点真正和文件有关的事：</p>\n\n<pre data-language=\"text\"><code>请用中文回复。\n列出当前目录第一层的文件和文件夹，说明这个目录主要用来做什么。\n先只阅读，不修改文件。信息不足的地方直接告诉我。</code></pre>\n\n<p>当它能够找到你准备的文件，就完成了从“装上工具”到“开始使用”的第一步。</p>\n\n<p>这里分清两种命令：<code>claude</code>、<code>claude doctor</code> 在电脑终端里输入；<code>/status</code>、<code>/login</code> 在进入 Claude Code 后输入。</p>\n\n<h2 id=\"c29-s5\">把 Kimi 接入 Claude Code</h2>\n\n<p>Claude Code 负责操作文件和工具，模型负责理解与生成。接入 Kimi 后，你依然在 Claude Code 里工作，但模型请求会发给 Kimi。</p>\n\n<p>先分清两个服务：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col><col></colgroup><thead><tr><th><p>使用方式</p></th><th><p>Key 从哪里获取</p></th><th><p>服务地址</p></th></tr></thead><tbody><tr><td><p>Kimi 开放平台，按 API 用量计费</p></td><td><p>Kimi 开放平台控制台</p></td><td><p><code>https://api.moonshot.cn/anthropic</code></p></td></tr><tr><td><p>Kimi Code，使用对应套餐权益</p></td><td><p>Kimi Code 控制台</p></td><td><p><code>https://api.kimi.com/coding/</code></p></td></tr></tbody></table>\n</div>\n<p>两种服务的 Key、模型名称和额度需要分别配置。下面演示<b>开放平台路线</b>；Kimi Code 用户使用<a href=\"https://www.kimi.com/code/docs/third-party-tools/claude-code.html\" target=\"_blank\" rel=\"noopener noreferrer\">对应的官方接入配置</a>。</p>\n\n<p><b>第一步：创建 API Key。</b></p>\n\n<p>打开 <a href=\"https://platform.kimi.com/\" target=\"_blank\" rel=\"noopener noreferrer\">Kimi 开放平台</a>，注册或登录后，进入 API Key 管理页面。</p>\n\n<img src=\"assets/manuscript-20260914/c27-66da1746ca21c9.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>点击“创建 API Key”，填写一个方便自己识别的名称，比如 <code>claude-code</code>，再确认创建。</p>\n\n<img src=\"assets/manuscript-20260914/c27-cd16ec4618a436.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>把生成的 Key 保存在自己的密码管理器中，配置时需要使用。它相当于调用接口的凭证，不要放进文章、截图或公开代码仓库。</p>\n\n<p><b>第二步：写入模型配置。</b></p>\n\n<p>退出正在运行的 Claude Code。找到用户目录中的 <code>.claude/settings.json</code>：</p>\n\n<ul><li>macOS、Linux：<code>~/.claude/settings.json</code></li><li>Windows：<code>C:\\Users\\你的用户名\\.claude\\settings.json</code></li></ul>\n\n<p>没有这个文件，就创建 <code>.claude</code> 文件夹和 <code>settings.json</code>。文件已存在时，先留一份副本，再合并下面的 <code>env</code> 字段，保留其他设置。</p>\n\n<p>下面将各任务使用的模型统一配置为 <code>kimi-k3[1m]</code>，先跑通一套模型：</p>\n\n<pre data-language=\"json\"><code>{\n  \"env\": {\n    \"ANTHROPIC_BASE_URL\": \"https://api.moonshot.cn/anthropic\",\n    \"ANTHROPIC_AUTH_TOKEN\": \"YOUR_MOONSHOT_API_KEY\",\n    \"ANTHROPIC_MODEL\": \"kimi-k3[1m]\",\n    \"ANTHROPIC_DEFAULT_OPUS_MODEL\": \"kimi-k3[1m]\",\n    \"ANTHROPIC_DEFAULT_SONNET_MODEL\": \"kimi-k3[1m]\",\n    \"ANTHROPIC_DEFAULT_HAIKU_MODEL\": \"kimi-k3[1m]\",\n    \"ANTHROPIC_DEFAULT_FABLE_MODEL\": \"kimi-k3[1m]\",\n    \"CLAUDE_CODE_SUBAGENT_MODEL\": \"kimi-k3[1m]\",\n    \"CLAUDE_CODE_AUTO_COMPACT_WINDOW\": \"1000000\",\n    \"CLAUDE_CODE_EFFORT_LEVEL\": \"max\"\n  }\n}</code></pre>\n\n<p>把 <code>YOUR_MOONSHOT_API_KEY</code> 换成自己的 Key。这里的模型映射和参数依据 <a href=\"https://platform.kimi.com/docs/guide/claude-code-kimi\" target=\"_blank\" rel=\"noopener noreferrer\">Kimi 开放平台接入文档</a>整理；使用前确认账号有对应模型的访问权限。</p>\n\n<p>如果首次使用第三方服务，仍停在账号引导页，按同一份官方文档完成一次初始化：在用户目录的 <code>.claude.json</code> 顶层加入 <code>\"hasCompletedOnboarding\": true</code>，保留已有字段。这个文件和 <code>.claude/settings.json</code> 是两个不同的文件。</p>\n\n<p>检查配置时，留意有没有同时设置 <code>ANTHROPIC_API_KEY</code>。这条开放平台路线使用 <code>ANTHROPIC_AUTH_TOKEN</code>，应移除冲突的认证字段。<code>settings.json</code> 中的 <code>env</code> 会覆盖终端同名变量，所以不要在几处填互相矛盾的地址和模型。</p>\n\n<p><b>第三步：重新启动并验证。</b></p>\n\n<p>在项目目录中运行：</p>\n\n<pre data-language=\"bash\"><code>claude</code></pre>\n\n<p>再输入 <code>/status</code>，确认服务地址为 <code>https://api.moonshot.cn/anthropic</code>，模型为配置的 Kimi 模型。</p>\n\n<p>先发送“你好”，再试一次文件读取。两个操作都成功，才说明对话和项目操作都跑通了。</p>\n\n<p>如果使用终端里的 <code>export</code> 临时设置变量，需要在<b>同一个终端窗口</b>里启动 <code>claude</code>；关掉窗口后，临时变量不会自动保留。</p>\n\n<h2 id=\"c29-s6\">让 Claude Code 真正开始干活</h2>\n\n<p>我更建议从一个明确的小需求开始。</p>\n\n<p>比如读懂项目：</p>\n\n<pre data-language=\"text\"><code>阅读 README 和主要入口文件，告诉我：\n1. 这个项目解决什么问题。\n2. 主要目录分别负责什么。\n3. 应该用什么命令启动、构建和检查。\n先给我说明，不修改代码。</code></pre>\n\n<p>比如修复问题：</p>\n\n<pre data-language=\"text\"><code>平台选择框里的文字显示不全。\n复现方式：打开内容分发助手，展开平台列表，选择“小红书”。\n预期效果：完整显示平台名称，窗口变窄后仍能正常操作。\n请定位相关样式，只修改这个问题，并告诉我怎么验证。</code></pre>\n\n<p>任务稍复杂时，先让它出方案。在终端启动计划模式：</p>\n\n<pre data-language=\"bash\"><code>claude --permission-mode plan</code></pre>\n\n<p>也可以在现有会话中按 <code>Shift+Tab</code>，切换到 Plan 模式。看完方案，确认方向，再选择实施。想查看各项操作的权限提示，可以使用 <code>claude --permission-mode default</code> 启动，并留意界面的权限状态。<a href=\"https://code.claude.com/docs/en/permission-modes\" target=\"_blank\" rel=\"noopener noreferrer\">权限模式说明</a></p>\n\n<p><b>把长期规则写进 CLAUDE.md。</b></p>\n\n<p>有些话，不需要每次都重复。比如使用什么语言、项目用什么技术、结果放在哪里，都可以写进项目根目录的 <code>CLAUDE.md</code>。</p>\n\n<p>在 Claude Code 中输入 <code>/init</code>，生成初稿后再检查、修改。以内容分发插件为例，可以写成：</p>\n\n<pre data-language=\"markdown\"><code># 项目说明\n\n这是一个 Obsidian 内容分发助手插件，使用 TypeScript。\n目标是把 Markdown 笔记转换成不同平台的文案。\n\n# 协作要求\n\n- 使用中文回复，修改前先阅读相关代码。\n- 使用 Obsidian 的 API 和主题变量，保持与应用界面一致。\n- 原始笔记只用于读取，生成结果放在独立的预览区域。\n- API Key 通过设置页配置，不写死在源码里。\n- 每次完成后说明改动、检查结果和仍需手动验证的部分。</code></pre>\n\n<p>项目规则要具体，能指导实际操作。<code>CLAUDE.md</code> 的文件名按这个大小写写；需要查看记忆设置时，可以使用 <code>/memory</code>。<a href=\"https://code.claude.com/docs/en/memory\" target=\"_blank\" rel=\"noopener noreferrer\">项目记忆说明</a></p>\n\n<p></p>\n","imageCount":9,"attachmentCount":0,"discussionId":"chapter-27"},{"id":"chapter-30","number":30,"title":"快速使用 PI","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-30.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-30.html","minutes":12,"status":"ready","headings":[{"id":"c30-s1","text":"这次测试的本地环境","level":"h2"},{"id":"c30-s2","text":"Pi 到底是什么？","level":"h2"},{"id":"c30-s3","text":"安装","level":"h2"},{"id":"c30-s4","text":"方法一：直接丢给 Agent 帮忙安装","level":"h3"},{"id":"c30-s5","text":"方法二：自己动手，一步步装","level":"h3"},{"id":"c30-s6","text":"三个官方案例，更了解 Pi","level":"h2"},{"id":"c30-s7","text":"案例一：一份树形会话","level":"h3"},{"id":"c30-s8","text":"案例二：把别人的扩展、Skills 和主题直接装进来","level":"h3"},{"id":"c30-s9","text":"案例三：把 Doom 塞进终端","level":"h3"},{"id":"c30-s10","text":"把你现成的 Codex、Claude Skills 接进来","level":"h2"},{"id":"c30-s11","text":"让 Pi 扩展更多能力","level":"h2"},{"id":"c30-s12","text":"Pi 适合谁？","level":"h2"}],"html":"<p>Pi 的核心是 Agent Loop、模型调用、工具和终端界面。你需要 Skills、Prompt Templates、Extensions 和 Packages ，配上就行。</p>\n\n<img src=\"assets/manuscript-20260914/c28-f8d0dc4f94a6ac.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>另外，如果你平时已经在折腾 Codex、Claude Code、Agent Skills，Pi 还是很值得学习一下的。</p>\n\n<p></p>\n\n<h2 id=\"c30-s1\"><b>这次测试的本地环境</b></h2>\n\n<ul><li>系统：macOS 26.0.1，Apple Silicon；</li><li>Node.js：v26.7.0；</li><li>npm：11.19.0；</li><li>Pi：v0.85.1；</li><li>安装方式：临时目录隔离安装，165 个包，17 秒完成；</li></ul>\n\n<p></p>\n\n<h2 id=\"c30-s2\"><b>Pi 到底是什么？</b></h2>\n\n<p>官方给它的定位是“minimal terminal coding harness”，翻成大白话，就是一套极简的终端 Agent 底座。</p>\n\n<p>它的仓库里有几块核心组件：</p>\n\n<ul><li><code>pi-ai</code>：统一连接 OpenAI、Anthropic、Google、DeepSeek、OpenRouter、Ollama 等模型；</li><li><code>pi-agent-core</code>：负责工具调用、状态管理和 Agent 循环；</li><li><code>pi-tui</code>：终端交互界面；</li><li><code>pi-coding-agent</code>：我们真正安装和使用的 CLI。</li></ul>\n\n<p>Pi 默认交给模型的核心工具只有四个：<code>read</code>、<code>write</code>、<code>edit</code> 和 <code>bash</code>。</p>\n\n<p>新版 CLI 里还能按需开放 <code>grep</code>、<code>find</code>、<code>ls</code>，Windows 也有可选的 PowerShell 工具。</p>\n\n<p>看起来东西不多，但 Agent 底层无非就是读、写、改、执行。很多更复杂的能力，其实都可以从这几个原语长出来。</p>\n\n<img src=\"assets/manuscript-20260914/c28-f3d9d1eae668f1.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>很多 Agent 产品会把计划、子 Agent、MCP、权限审批和任务列表全部做好，用户只需要设置一下就行。</p>\n\n<p>Pi 是给你一个能跑的 Agent 骨架，可以自定义 Agent。想要计划模式，就用文件写或者装扩展；想要子 Agent，就用 tmux 拉起多个 Pi，或者自己写 Extension；想接 MCP，也可以装对应扩展。</p>\n\n<p>它更像一套已经通水通电的毛坯房。住得舒不舒服，取决于你后面怎么装修。</p>\n\n<h2 id=\"c30-s3\">安装</h2>\n\n<h3 id=\"c30-s4\"><b>方法一：直接丢给 Agent 帮忙安装</b></h3>\n\n<p>如果你已经在用 Codex、Claude Code 或其他能操作终端的 Agent，可以把下面这段完整发给它：</p>\n\n<pre data-language=\"text\"><code>请根据我当前电脑环境安装并验证 Pi Coding Agent：\n\n1. 先检查操作系统、CPU 架构、Node.js、npm，以及当前 PATH 中真正生效的 node 和 npm；\n2. Pi 当前要求 Node.js &gt;= 22.19.0，不满足时先说明升级方案，不要直接改动我的环境；\n3. 使用官方 npm 包 @earendil-works/pi-coding-agent 安装，保留官方建议的 --ignore-scripts；\n4. 安装后运行 pi --version 和 pi --help 验证；\n5. 不读取、不打印、不修改任何 API Key 或登录凭据；\n6. 告诉我如何通过 /login 登录已有订阅，以及如何在空白测试目录中做一次只读验证；\n7. 遇到问题先核对项目官方仓库和官网的当前文档，不要猜参数。</code></pre>\n\n<p></p>\n\n<h3 id=\"c30-s5\"><b>方法二：自己动手，一步步装</b></h3>\n\n<p><b>第 1 步：检查 Node.js 和 npm</b></p>\n\n<p>macOS、Linux 打开终端，Windows 打开 PowerShell，先执行：</p>\n\n<pre data-language=\"bash\"><code>node -v\nnpm -v</code></pre>\n\n<p>Pi 当前 npm 包的 Node.js 要求是：</p>\n\n<pre data-language=\"text\"><code>&gt;= 22.19.0</code></pre>\n\n<p>如果 <code>node -v</code> 低于这个版本，先升级 Node.js。电脑里同时装过 nvm、Homebrew、系统 Node 的朋友，再多看两眼：</p>\n\n<pre data-language=\"bash\"><code>which node\nwhich npm</code></pre>\n\n<p>有时候你以为自己升级过了，但被终端调用的还是旧版本。这个坑在 CLI 工具里非常常见。</p>\n\n<p>Windows 还有一个额外要求。Pi 默认通过 Git Bash 跑 Bash 命令，装好 Git for Windows 就行。</p>\n\n<p><b>第 2 步：安装 Pi</b></p>\n\n<p>官方 npm 安装命令：</p>\n\n<pre data-language=\"bash\"><code>npm install -g --ignore-scripts @earendil-works/pi-coding-agent</code></pre>\n\n<p>提醒一下，这里的 <code>--ignore-scripts</code> 需要保留。它会禁止依赖在安装阶段执行生命周期脚本，而 Pi 的正常 npm 安装并不需要这些脚本。</p>\n\n<p>官网还提供一键安装脚本：</p>\n\n<pre data-language=\"bash\"><code>curl -fsSL https://pi.dev/install.sh | sh</code></pre>\n\n<p>这几个方案都可以。但<code>curl | sh</code> 确实是最省事的。</p>\n\n<p>安装完验证：</p>\n\n<pre data-language=\"bash\"><code>pi --version\npi --help</code></pre>\n\n<p>截至 2026 年 9 月 12 日，官方仓库当前版本是 <code>0.85.1</code>。项目迭代很快，版本号对不上不一定是安装失败，可以先看 GitHub Releases 和本地 <code>pi --version</code>。</p>\n\n<img src=\"assets/manuscript-20260914/c28-1f91a7b2f904a0.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>官网文档的 Quick start 也是这一条 npm 命令。</p>\n\n<img src=\"assets/manuscript-20260914/c28-3a8ce1166a3644.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p><b>装好了，为什么还是启动不了？</b></p>\n\n<p>最常见的一种情况，是终端提示：</p>\n\n<pre data-language=\"text\"><code>pi: command not found</code></pre>\n\n<p>可以先关掉终端重新打开，再看 npm 的全局安装位置：</p>\n\n<pre data-language=\"bash\"><code>npm config get prefix\nnpm list -g --depth=0</code></pre>\n\n<p>如果列表里已经有 <code>@earendil-works/pi-coding-agent</code>，说明包大概率装上了，只是 npm 的全局可执行目录没进 PATH。macOS 和 Linux 通常要检查全局前缀下的 <code>bin</code> 目录，Windows 则检查 npm 的全局目录。</p>\n\n<p>另一类问题是 Node.js 版本看着没问题，执行 Pi 的时候却还在用旧环境。把下面几项一起看看：</p>\n\n<pre data-language=\"bash\"><code>which node\nwhich npm\nwhich pi\nnode -v\npi --version</code></pre>\n\n<p>如果 <code>pi</code> 能打开，但模型列表是空的，先执行 <code>/login</code>，再用 <code>/model</code> 选择模型。也可以退出后在命令行检查：</p>\n\n<pre data-language=\"bash\"><code>pi --list-models</code></pre>\n\n<p></p>\n\n<p><b>第 3 步：登录模型</b></p>\n\n<p>直接启动：</p>\n\n<pre data-language=\"bash\"><code>pi</code></pre>\n\n<p>打开以后，可以看到界面比较简洁：中间就是输入框，底部显示当前目录、上下文和模型。</p>\n\n<img src=\"assets/manuscript-20260914/c28-85acc4da5f90b2.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>进入终端界面后输入：</p>\n\n<pre data-language=\"text\"><code>/login</code></pre>\n\n<p>Pi 会让你选择模型提供商。官方目前支持 15 家以上，既能走 API Key，也能登录已有的订阅。</p>\n\n<p>如果你有 ChatGPT Plus 或 Pro，可以选择 OpenAI Codex；也支持 GitHub Copilot。Claude Pro/Max 虽然能登录，但官方特别提醒，第三方 Harness 的调用会走 Anthropic 的 extra usage，按 Token 额外计费，不会直接消耗套餐内额度。这一点要特别注意一下。</p>\n\n<p>用 DeepSeek API 的朋友，也可以在环境变量里提供：</p>\n\n<pre data-language=\"bash\"><code>export DEEPSEEK_API_KEY=你的Key\npi</code></pre>\n\n<p>不过对小白来说，我还是更建议在 Pi 里用 <code>/login</code>。凭据会保存在 <code>~/.pi/agent/auth.json</code>。</p>\n\n<p>登录成功后，用下面两个命令，可以切模型和思考等级：</p>\n\n<pre data-language=\"text\"><code>/model\n/thinking</code></pre>\n\n<p><code>Ctrl+L</code> 也能快速打开模型选择器，<code>Shift+Tab</code> 可以切换思考等级。</p>\n\n<img src=\"assets/manuscript-20260914/c28-299dc6897340df.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p><b>第 4 步：跑一次只读检查</b></p>\n\n<p>进入一个你准备测试的项目目录，再执行下面的指令：</p>\n\n<pre data-language=\"bash\"><code>pi --tools read,grep,find,ls -p \"阅读当前目录，告诉我项目是做什么的、入口文件在哪里、应该运行什么检查。不要修改任何文件。\"</code></pre>\n\n<p>这里用了 Pi 的 Print 模式，任务完成后会直接退出；<code>--tools</code> 把可用工具锁定在读取范围里面。然后检查三件事：模型能不能连上、Pi 能不能读到工作区、工具限制有没有生效。</p>\n\n<p>如果你只是想看某几个文件，也可以把文件直接塞给它：</p>\n\n<pre data-language=\"bash\"><code>pi -p @README.md \"用大白话解释这个项目的安装和启动方式\"</code></pre>\n\n<p>Pi 也支持查看图片：</p>\n\n<pre data-language=\"bash\"><code>pi -p @screenshot.png \"看看这个报错页面，给我排查顺序\"</code></pre>\n\n<p><b>第 5 步：验证写入能力</b></p>\n\n<p>建一个专门的测试目录：</p>\n\n<pre data-language=\"bash\"><code>mkdir pi-playground\ncd pi-playground\npi</code></pre>\n\n<p>把下面这个最小任务发给它：</p>\n\n<pre data-language=\"text\"><code>请先确认当前工作目录，并列出已有文件。\n然后创建一个 hello-pi.md，内容包括：当前时间、工作目录、你能使用的工具名称。\n除 hello-pi.md 外，不要创建、修改或删除任何文件。\n完成后重新检查目录，并告诉我实际发生了哪些变化。</code></pre>\n\n<p>这个任务没什么技术含量，但很适合做验证。它会碰到目录读取、文件写入、约束遵守和结果复查。</p>\n\n<p>确认这些都正常后，我们才能更放心的把 Pi 放进真实项目。</p>\n\n<p>如果项目里带有 <code>.pi/settings.json</code>、<code>.pi</code> 资源或项目级 Skills，Pi 可能会询问是否信任当前目录。</p>\n\n<p>信任项目意味着它可以加载项目配置、安装缺失的项目包，并执行项目 Extension。</p>\n\n<p></p>\n\n<p><b>先记住这些 Pi 的常用命令</b></p>\n\n<ul><li><code>/login</code>：登录或切换模型提供商；</li><li><code>/model</code>：换模型；</li><li><code>/thinking</code>：调整思考等级；</li><li><code>/new</code>：新建会话；</li><li><code>/resume</code>：继续历史会话；</li><li><code>/tree</code>：打开会话树，跳回任意节点；</li><li><code>/compact</code>：手动压缩上下文；</li><li><code>/reload</code>：重新加载配置、Skills、扩展和主题；</li><li><code>/hotkeys</code>：查看全部快捷键；</li><li><code>/session</code>：看当前会话文件、ID、Token 和费用。</li></ul>\n\n<p>我觉得这个指令很重要 <code>/tree</code>。</p>\n\n<p>普通聊天如果前面如果方向错了，后面可能只有重新开一个会话。Pi 的 Session 用树结构保存，你可以跳回某条旧消息，从那里长出一条新分支，原来的分支还在同一个 JSONL 文件里。</p>\n\n<p>比如让 Agent 重构代码，跑到一半如果发现路线不对，直接回到“开始重构”之前，再换个方案，这样就不用把前面的上下文全喂一遍。特别是长任务里，这个功能非常实用。</p>\n\n<p>pi 的会话默认按工作目录保存在：</p>\n\n<pre data-language=\"text\"><code>~/.pi/agent/sessions/</code></pre>\n\n<p>命令行里还可以这样继续对话：</p>\n\n<pre data-language=\"bash\"><code>pi -c\npi -r</code></pre>\n\n<p>前者继续最近一次会话，后者让你挑历史会话。</p>\n\n<p><b>用到自己的产品里</b></p>\n\n<p>Pi 不止能在终端里聊天。</p>\n\n<p>官方把它的使用方式归成四组：Interactive、Print/JSON、RPC 和 SDK。其中 Print 与 JSON 都适合程序化的一次性或流式任务，所以被放在同一组。</p>\n\n<p>默认直接执行 <code>pi</code>，进入 Interactive 模式，适合日常写代码和长任务。</p>\n\n<p>Print 模式适合一次性命令：</p>\n\n<pre data-language=\"bash\"><code>pi -p \"检查这个项目有哪些明显问题\"</code></pre>\n\n<p>JSON 模式会持续输出事件流，方便接 Shell 脚本、日志系统或自己的自动化程序：</p>\n\n<pre data-language=\"bash\"><code>pi --mode json \"分析当前项目\"</code></pre>\n\n<p>RPC 模式通过标准输入输出接收 JSONL，适合把 Pi 嵌进非 Node.js 应用：</p>\n\n<pre data-language=\"bash\"><code>pi --mode rpc</code></pre>\n\n<p>Pi 还有 SDK。开发者可以直接在 TypeScript 项目里创建 Agent Session，把 Pi 当成自己的 Agent Runtime。</p>\n\n<p>所以你看到的是同一套内核，但在终端、脚本、服务和自己的产品里都能用。</p>\n\n<h2 id=\"c30-s6\"><b>三个官方案例，更了解 Pi</b></h2>\n\n<p>下面三个案例来自 Pi 官方公开页面。主要是想让大家更清楚这套 Harness 到底怎么用。</p>\n\n<h3 id=\"c30-s7\"><b>案例一：一份树形会话</b></h3>\n\n<p>刚刚说了，Pi 的会话不是只有一条聊天流水线。官方共享会话页左边直接展示了 241 个节点和不同分支；右边则能展开 System Prompt、工具、消息和调用记录。</p>\n\n<p>这比“支持历史记录”更具体，更实用。你可以回到旧节点另起一条路，也可以把完整过程导出。对</p>\n\n<img src=\"assets/manuscript-20260914/c28-c72aaef02c6baa.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c30-s8\"><b>案例二：把别人的扩展、Skills 和主题直接装进来</b></h3>\n\n<p>Pi 官网有一个 Package Catalog。有 5,435 个条目，里面既有 MCP Adapter、网页访问，也有工作流、数据库和待办组件。</p>\n\n<p>这些是能用 <code>pi install npm:&lt;package&gt;</code> 直接装进本机的能力包。换句话说，Pi 的扩展生态是非常丰富的。</p>\n\n<img src=\"assets/manuscript-20260914/c28-825b36602d2e3b.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c30-s9\"><b>案例三：把 Doom 塞进终端</b></h3>\n\n<p>这个 Case 很离谱，但也最能说明 Extension 能扩展到什么程度。官方演示里，Agent 在后台执行任务，Doom 直接嵌在 Pi 的 TUI 里！这个交付做的真心不错，而且底部也仍然保留了模型、Token、费用和任务状态。</p>\n\n<p>Extension 能接事件、改界面、注册工具，甚至把整个交互层换掉。</p>\n\n<img src=\"assets/manuscript-20260914/c28-c3b89b5e1fba71.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h2 id=\"c30-s10\"><b>把你现成的 Codex、Claude Skills 接进来</b></h2>\n\n<p>Pi 会自动扫描这些位置：</p>\n\n<pre data-language=\"text\"><code>~/.pi/agent/skills/\n~/.agents/skills/\n.pi/skills/\n.agents/skills/</code></pre>\n\n<p>其中项目级 Skills 只有在项目被信任后才会加载。</p>\n\n<p>如果你已经积累了一堆 Claude Skills 或 Codex Skills，可以编辑全局设置：</p>\n\n<pre data-language=\"text\"><code>~/.pi/agent/settings.json</code></pre>\n\n<p>加入：</p>\n\n<pre data-language=\"json\"><code>{\n  \"skills\": [\n    \"~/.claude/skills\",\n    \"~/.codex/skills\"\n  ]\n}</code></pre>\n\n<p>回到 Pi 里执行：</p>\n\n<pre data-language=\"text\"><code>/reload</code></pre>\n\n<p>Skills 会以渐进式方式加载。启动时只把名称和描述放进上下文，任务匹配后再读取完整 <code>SKILL.md</code>，不会一上来把所有说明都塞进上下文。</p>\n\n<p>想强制调用某个 Skill，可以输入：</p>\n\n<pre data-language=\"text\"><code>/skill:skill-name</code></pre>\n\n<p>这个设计和现在的 Agent Skills 标准是对齐的。你过去给 Codex、Claude Code 做的很多 Skill 能力包，都可以复用。</p>\n\n<h2 id=\"c30-s11\"><b>让 Pi 扩展更多能力</b></h2>\n\n<p>Skills 主要解决“怎么做”，Extensions 能直接改 Pi 本身，功能更加强大。</p>\n\n<p>一个 Extension 可以注册新工具、新命令、新快捷键和事件；能改状态栏、编辑器、弹窗、主题；也能实现子 Agent、计划模式、权限门、Git 自动提交、SSH 执行、沙箱，甚至补上 MCP。</p>\n\n<p>官方首页展示的 Doom，就是一个很直观的 Extension 例子。</p>\n\n<p>官方建议：你缺什么能力，就让 Pi 给自己写一个 Extension。改完执行 <code>/reload</code>，还可以继续当前的工作。</p>\n\n<p>如果这套能力以后还要复用，可以把 Extension、Skill、Prompt 和主题都装进一个 Pi Package：</p>\n\n<pre data-language=\"bash\"><code>pi install npm:@foo/pi-tools\npi install git:github.com/user/repo\npi list\npi config</code></pre>\n\n<p>说明一下，Pi Package 跟浏览器里的隔离插件完全两回事。</p>\n\n<p></p>\n\n<p><b>它最容易被忽略的坑：默认没有权限保护</b></p>\n\n<p>Pi 默认不会像一些桌面 Agent 那样，删文件、跑命令之前弹一个确认框。它直接继承了启动进程的文件、网络、凭据和系统权限。</p>\n\n<p>但是这不算隐藏 Bug，官方首页就把“No permission popups”写得很明白。</p>\n\n<p>对熟悉终端的人，这代表 YOLO 模式，也就是任务不会频繁被打断。</p>\n\n<p>但对小白来说，可能一句写得不够严谨的 Prompt，真有可能让 Agent 干坏事，比如清空你的 C 盘啥的。。。</p>\n\n<p>小白第一次使用，一定要注意以下几点：</p>\n\n<ul><li>先用空白目录，不要上来就打开桌面、文档或整个 Home 目录；</li><li>读代码时，用 <code>--tools read,grep,find,ls</code> 锁成只读；</li><li>不要让模型读取 <code>~/.pi/agent/auth.json</code>、<code>.env</code> 和其他凭据文件；</li><li>第三方 Package 和 Extension 先审源码，再安装；</li><li>真要让它长时间自动跑，放进 Docker、OpenShell 或其他沙箱。</li></ul>\n\n<p>官方给了多种隔离方案。最容好用的是把整个 Pi 放进 Docker。</p>\n\n<p>需要注意，挂载进去的工作目录依然能写回宿主机；把本机的 <code>~/.pi/agent</code> 也挂进去，则会把认证和会话文件一起暴露给容器。</p>\n\n<p>另外，Pi 启动时会检查新版本；首次安装或检测到升级后，还会发送匿名版本信息。介意这块，可以在设置里关闭安装遥测，或者使用：</p>\n\n<pre data-language=\"bash\"><code>PI_TELEMETRY=0 pi</code></pre>\n\n<p>需要完全关闭启动时的联网检查，可以用：</p>\n\n<pre data-language=\"bash\"><code>PI_OFFLINE=1 pi</code></pre>\n\n<h2 id=\"c30-s12\"><b>Pi 适合谁？</b></h2>\n\n<p>如果你只想下载一个产品，登录，然后在安全护栏里稳定使用，Pi 不一定是最省心的选择。它有意省掉了一些“装好就该有”的功能，很多东西需要自己配。</p>\n\n<p>已经在用多个模型、手里有不少 Skills、想把 Agent 接进脚本或产品、对终端和权限有基本判断的人，会更容易感受到它的价值。尤其是做一人公司和独立开发的朋友。</p>\n\n<p>当然，自由度越高，安全这块更需要自己把好关。</p>\n\n<p></p>\n\n<p></p>\n","imageCount":9,"attachmentCount":0,"discussionId":"chapter-28"},{"id":"chapter-31","number":31,"title":"快速使用 DeepSeek Harness","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-31.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-31.html","minutes":13,"status":"ready","headings":[{"id":"c31-s1","text":"我这台机器的实测环境","level":"h2"},{"id":"c31-s2","text":"方法一：直接把活儿甩给 Agent","level":"h2"},{"id":"c31-s3","text":"方法二：自己动手，一步步装","level":"h2"},{"id":"c31-s4","text":"第 1 步：检查环境","level":"h3"},{"id":"c31-s5","text":"第 2 步：尝个鲜/长期用","level":"h3"},{"id":"c31-s6","text":"第 3 步：启动网页端","level":"h3"},{"id":"c31-s7","text":"装好了，为什么还是启动失败？","level":"h2"},{"id":"c31-s8","text":"第一次打开网页","level":"h2"},{"id":"c31-s9","text":"选工作区，新建会话","level":"h2"},{"id":"c31-s10","text":"用一个最小任务，验证它真的能干活","level":"h2"},{"id":"c31-s11","text":"四种模式怎么选","level":"h2"},{"id":"c31-s12","text":"标准模式","level":"h3"},{"id":"c31-s13","text":"PTC 模式","level":"h3"},{"id":"c31-s14","text":"极简模式","level":"h3"},{"id":"c31-s15","text":"创造模式","level":"h3"},{"id":"c31-s16","text":"它和 Codex、Claude Code 最不一样的地方","level":"h2"},{"id":"c31-s17","text":"全网目前最火的 5 类玩法","level":"h2"},{"id":"c31-s18","text":"接进完整 AI 工作台","level":"h3"},{"id":"c31-s19","text":"做跨平台内容发现 Agent","level":"h3"},{"id":"c31-s20","text":"魔改 Web UI","level":"h3"},{"id":"c31-s21","text":"给纯文本 Agent 补视觉","level":"h3"},{"id":"c31-s22","text":"替换交互层","level":"h3"},{"id":"c31-s23","text":"生成复杂 3D 交互应用，并用 Trajectory 追踪全过程","level":"h4"},{"id":"c31-s24","text":"生成高精度工程类 3D 仿真","level":"h4"},{"id":"c31-s25","text":"用插件魔改 UI，把预览面板直接嵌进工作台","level":"h4"},{"id":"c31-s26","text":"把 Harness 做成可学习、可运行的教程和知识产品","level":"h4"},{"id":"c31-s27","text":"实测 Case 1：把 Harness 变成\"对话 + 文件 + 预览\"工作台","level":"h2"},{"id":"c31-s28","text":"实测 Case 2：OpenPencil 可编辑设计组合，为什么只能判\"部分成功\"","level":"h2"},{"id":"c31-s29","text":"实测 Case 3：让 Agent 在运行中给自己造一个新工具","level":"h2"},{"id":"c31-s30","text":"最后","level":"h2"}],"html":"<p>DeepSeek 在 2026 年 8 月 13 号开源了他们的第一个 Agent 产品：DeepSeek Harness。</p>\n\n<p>这 Star 涨得快的离谱。我记得仅仅一天就涨到了 <b>64.2k Star</b>，写这篇的时候已经 221K Star 了。一个月的时间，涨了 22 万星，恐怖如斯。</p>\n\n<img src=\"assets/manuscript-20260914/c29-742525f0e682d2.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>安装其实非常简单：只要你电脑环境没问题，核心就一条命令：</p>\n\n<pre data-language=\"bash\"><code>npx @deepseek-ai/dsh web</code></pre>\n\n<p>简单，但不代表每台电脑都能一次安装成功，还是有一些小坑在这里面。</p>\n\n<p>我在自己的 Mac 上完整跑了一遍安装流程，安装明明成功了，启动的时候还是踩了一个 Node.js 版本的坑（后面细说）。</p>\n\n<img src=\"assets/manuscript-20260914/c29-f846a2a5a71b4c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>这篇将分享两种安装方案，都是实际跑通过的：</p>\n\n<ol><li>懒人路线：把提示词直接甩给 Codex、Claude Code 或者其他 Agent，让它替你检查环境、安装和验证。</li><li>动手路线：自己一步一步装，遇到报错丢给 AI 看。</li></ol>\n\n<h2 id=\"c31-s1\">我这台机器的实测环境</h2>\n\n<ul><li>macOS 26.0.1，Apple Silicon（arm64）</li><li>zsh 5.9</li><li>Node.js v22.19.0</li><li>npm / npx 10.9.3</li><li><code>@deepseek-ai/dsh 0.1.0-rc.6</code></li><li>Web UI：<code>http://127.0.0.1:3080</code></li></ul>\n\n<h2 id=\"c31-s2\">方法一：直接把活儿甩给 Agent</h2>\n\n<p>把下面这段完整发给 Codex、Claude Code 或者其他能跑终端命令的 Agent：</p>\n\n<pre data-language=\"text\"><code>请根据我当前电脑环境安装并启动 DeepSeek Harness：\n1. 先检查 Node.js、npm、系统架构和 3080 端口；缺少依赖时先说明再安装。\n2. 使用 npm 全局安装 @deepseek-ai/dsh。\n3. 根据当前版本的 --help 和官方 GitHub README 确认正确的 Web UI 启动命令，不要猜参数。\n4. 启动后验证本机端口和网页是否可访问，并告诉我打开地址。\n5. 遇到问题先查 https://github.com/deepseek-ai/deepseek-harness ，不要输出或修改任何 API Key。</code></pre>\n\n<p></p>\n\n<h2 id=\"c31-s3\">方法二：自己动手，一步步装</h2>\n\n<h3 id=\"c31-s4\">第 1 步：检查环境</h3>\n\n<pre data-language=\"bash\"><code>node -v\nnpm -v\nuname -m</code></pre>\n\n<p>我这边分别得到 Node.js v22.19.0、npm 10.9.3 和 arm64。</p>\n\n<p>DeepSeek Harness 当前仓库声明的 Node.js 范围是：</p>\n\n<pre data-language=\"text\"><code>^22.19.0 || &gt;=24.0.0</code></pre>\n\n<p>低于这个范围，先把 Node.js 升上去再说。</p>\n\n<h3 id=\"c31-s5\">第 2 步：尝个鲜/长期用</h3>\n\n<p>如果只想尝鲜体验：</p>\n\n<pre data-language=\"bash\"><code>npx @deepseek-ai/dsh web</code></pre>\n\n<p>准备长期使用：</p>\n\n<pre data-language=\"bash\"><code>npm install -g @deepseek-ai/dsh</code></pre>\n\n<p>我实测的时候，官方 npm registry 在这台电脑上 DNS 超时了，最后是走镜像装好的：</p>\n\n<pre data-language=\"bash\"><code>npm install -g @deepseek-ai/dsh --registry=https://registry.npmmirror.com</code></pre>\n\n<p>验证安装：</p>\n\n<pre data-language=\"bash\"><code>dsh --version\ndsh --help</code></pre>\n\n<p>实测版本 <code>0.1.0-rc.6</code>。当前版本里，<code>dsh web</code> 是 <code>dsh --profile web</code> 的短写。</p>\n\n<h3 id=\"c31-s6\">第 3 步：启动网页端</h3>\n\n<pre data-language=\"bash\"><code>dsh web</code></pre>\n\n<p>等价写法：</p>\n\n<pre data-language=\"bash\"><code>dsh --profile web</code></pre>\n\n<p>启动成功会显示：</p>\n\n<pre data-language=\"text\"><code>dsh web: http://127.0.0.1:3080</code></pre>\n\n<p>浏览器打开 <code>http://127.0.0.1:3080</code> 就能看到界面了。</p>\n\n<h2 id=\"c31-s7\">装好了，为什么还是启动失败？</h2>\n\n<p>我第一次启动，<code>dsh</code> 实际调用的是 <code>/usr/local/bin/node</code>，对应 Node.js v22.14.0，但是直接报错了：</p>\n\n<pre data-language=\"text\"><code>The requested module 'node:zlib' does not provide an export named 'createZstdDecompress'</code></pre>\n\n<p>排查下来，是执行 <code>dsh</code> 的 Node 太旧，缺少它需要的 Zstd API。切到 Node.js v22.19.0，Harness 就能正常启动🤦‍♂️</p>\n\n<img src=\"assets/manuscript-20260914/c29-7021cec3f8f494.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>所以不能只看某个终端里的 <code>node -v</code>，还要看：</p>\n\n<pre data-language=\"bash\"><code>which node\nwhich dsh</code></pre>\n\n<p>如果你电脑上同时装了 nvm、Homebrew 和系统 Node，特别容易出现\"你看到的是一个 Node，真正干活的是另一个 Node\"这种灵异现象。</p>\n\n<h2 id=\"c31-s8\">第一次打开网页</h2>\n\n<p>首次访问会先看到\"内测声明\"。</p>\n\n<img src=\"assets/manuscript-20260914/c29-d4c55308985133.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>点继续，进入 API Key 引导。</p>\n\n<img src=\"assets/manuscript-20260914/c29-0dfb6d0697d126.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>你可以立刻填 DeepSeek 的 API Key，也可以先点\"稍后配置\"。如果环境或 Harness 设置里已经有可识别的 Key，可能不用重复填，以当前版本的实际页面为准。</p>\n\n<p>我后面是通过 Web 引导页保存的 Key。当前版本会把凭据写进 <code>~/.dsh/.credentials.yaml</code>，实测文件权限是 <code>600</code>，只有当前用户可读写。</p>\n\n<h2 id=\"c31-s9\">选工作区，新建会话</h2>\n\n<p>点左侧\"添加工作区\"，选一个你准备交给 Harness 接管的文件夹。我用的是一个空白测试目录 <code>DeepSeek-harness-Work</code>。</p>\n\n<p>新建会话后，输入框解锁，主界面有四个关键状态：</p>\n\n<img src=\"assets/manuscript-20260914/c29-f8ad18cd3d42d9.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<ol><li>工作区：Agent 能动的项目范围；</li><li>模式：标准、PTC、极简或创造；</li><li>权限：比如 Workspace Write；</li><li>模型与推理等级：我这次是 DeepSeek-V4-Flash High。</li></ol>\n\n<p>第一次玩，千万不要直接选重要项目。先拿空目录确认权限和行为，再上真实的代码仓库。</p>\n\n<h2 id=\"c31-s10\">用一个最小任务，验证它真的能干活</h2>\n\n<p>我在测试目录放了两个文件：</p>\n\n<ul><li><code>project-notes.md</code>：项目目标、文件说明和约束；</li><li><code>tasks.csv</code>：3 条待办。</li></ul>\n\n<p>发给 Harness 的提示词：</p>\n\n<pre data-language=\"text\"><code>请先阅读当前目录里的 project-notes.md 和 tasks.csv，整理这个小项目的目标、文件说明和待办事项，并生成一份简洁的 README.md。开始前先给出计划；除 README.md 外不要修改其他文件。</code></pre>\n\n<p>别小看这个任务，它一次能验证五件事：工作区、文件读取、计划、文件写入和约束遵守。</p>\n\n<p>我先把权限切到 Read Only，跑了一次纯连通性测试。DeepSeek-V4-Flash 按要求返回\"DeepSeek Harness API 实测成功\"，没有调用任何工具。界面显示用时约 2 秒、首 token 约 2.5 秒、约 241 tok/s，这个速度是真挺快的。</p>\n\n<img src=\"assets/manuscript-20260914/c29-096c573d38eae8.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>然后在切换回 Workspace Write，跑完整的任务。Agent 的实际流程是：</p>\n\n<ol><li>先列计划；</li><li>读取 <code>project-notes.md</code> 和 <code>tasks.csv</code>；</li><li>归纳项目目标、文件说明和待办事项；</li><li>生成 <code>README.md</code>；</li><li>再检查一遍目录，确认其他文件没被动过。</li></ol>\n\n<p>整个任务用时约 12 秒，唯一新增的文件就是 <code>README.md</code>。我还是不太放心，又在 Agent 外部对两个原始文件做了 SHA-256 对比，跟测试前的副本完全一致。\"除 README 外不要修改其他文件\"这条约束，它是真的遵守。</p>\n\n<img src=\"assets/manuscript-20260914/c29-21feb289c94428.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h2 id=\"c31-s11\">四种模式怎么选</h2>\n\n<img src=\"assets/manuscript-20260914/c29-4681294155d0a3.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>本机\"设置 → Agent 预设\"页能直接看到四张预设卡片，每个预设都能查看或复制，页面还有\"用创造模式创作自定义预设\"的入口。</p>\n\n<img src=\"assets/manuscript-20260914/c29-7c7927e707f1d6.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c31-s12\">标准模式</h3>\n\n<p>完整编码 Agent，支持文件编辑、Shell、文件与网页检索、Skills、计划、目标、子代理和工作流。第一次用，直接选它。</p>\n\n<h3 id=\"c31-s13\">PTC 模式</h3>\n\n<p>标准模式的能力都有，另外通过 Code Mode SDK 暴露工具，让模型用 TypeScript 程序组合多步操作。适合批量处理和自动化流程。</p>\n\n<h3 id=\"c31-s14\">极简模式</h3>\n\n<p>只保留持久 Bash 和 <code>str_replace_editor</code>，适合最小环境和模型基准测试。</p>\n\n<h3 id=\"c31-s15\">创造模式</h3>\n\n<p>在标准模式上加了运行时检查、插件实验和自定义 preset 创作，适合魔改 Harness、开发新的 Agent 模板。</p>\n\n<h2 id=\"c31-s16\">它和 Codex、Claude Code 最不一样的地方</h2>\n\n<p>一句话：DeepSeek Harness 不只是一个固定的编程 Agent，更像一套\"给 Agent 装能力的装扮系统\"。</p>\n\n<p>官方的说法是：<b>一切皆插件</b>。</p>\n\n<p>模型、工具、技能、会话、沙箱、存储、循环、调度和 UI，全都是插件。你可以换模型、装外部工具、接 Skills、调权限、开搜索、改界面、调用子 Agent，甚至重新组合运行模式。</p>\n\n<p>这话听起来像宣传语，但我在本机\"设置 → 插件 → 插件列表\"里有一大堆插件：模型、会话、凭据、沙箱、权限、Skills、目标、子 Agent、工作流、Web 搜索、工作区、Trajectory，还有一大堆 UI 组件。</p>\n\n<img src=\"assets/manuscript-20260914/c29-504091d01836ce.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>它还提供仅追加的会话日志，把系统提示词、工具调用、子 Agent 调度和上下文注入放进同一条事件流，方便恢复、分叉、检索和回放。</p>\n\n<p>所以它更像一个能被开发者反复魔改的 Agent 底座，而不是单纯的\"DeepSeek 版 Codex\"。</p>\n\n<h2 id=\"c31-s17\">全网目前最火的 5 类玩法</h2>\n\n<p>GitHub 的 <a href=\"https://github.com/topics/dsh-plugin\" target=\"_blank\" rel=\"noopener noreferrer\">dsh-plugin Topic</a> 有非常多的公开仓库。</p>\n\n<img src=\"assets/manuscript-20260914/c29-673698eb90f6a2.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c31-s18\">接进完整 AI 工作台</h3>\n\n<p><a href=\"https://github.com/Devin-AXIS/iPolloWork\" target=\"_blank\" rel=\"noopener noreferrer\">iPolloWork</a>：覆盖代码、设计、PPT、网站和视频工作流，把 DSH 用作专门子 Agent 的委派层。</p>\n\n<h3 id=\"c31-s19\">做跨平台内容发现 Agent</h3>\n\n<p><a href=\"https://github.com/whiteguo233/OpenBiliClaw\" target=\"_blank\" rel=\"noopener noreferrer\">OpenBiliClaw</a>：覆盖 B 站、小红书、抖音、YouTube、X、知乎、Reddit、微博和开放 Web，并声明支持 DeepSeek Harness 插件。</p>\n\n<h3 id=\"c31-s20\">魔改 Web UI</h3>\n\n<p><a href=\"https://github.com/zhu1090093659/dsh-web-ui\" target=\"_blank\" rel=\"noopener noreferrer\">dsh-web-ui</a>：提供任务看板、Git 图、右侧面板、远程手机 UI、Token 统计，甚至还有宠物和皮肤中心，整挺花🤣</p>\n\n<img src=\"assets/manuscript-20260914/c29-2eccfd67e8e506.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c31-s21\">给纯文本 Agent 补视觉</h3>\n\n<p><a href=\"https://github.com/liustack/modlens\" target=\"_blank\" rel=\"noopener noreferrer\">modlens</a>：可以把截图转成 OCR、布局和语义等结构化证据，可以拿来做图片问答、前端 UI 还原和 GUI 自动化。</p>\n\n<h3 id=\"c31-s22\">替换交互层</h3>\n\n<p><a href=\"https://github.com/ccch1mneyyy/dsh-TUI\" target=\"_blank\" rel=\"noopener noreferrer\">dsh-TUI</a>：目标是做 Claude Code 风格的全屏终端。</p>\n\n<p><a href=\"https://github.com/omdsh-dev/DSH-better-sidebar\" target=\"_blank\" rel=\"noopener noreferrer\">DSH-better-sidebar</a>：把文件、终端、Git 和子代理集中到侧边栏工作台。</p>\n\n<h4 id=\"c31-s23\">生成复杂 3D 交互应用，并用 Trajectory 追踪全过程</h4>\n\n<p><a href=\"https://x.com/stevibe/status/2087903879845515676\" target=\"_blank\" rel=\"noopener noreferrer\">@stevibe</a> 用 Harness 生成了一个 3D 魔方：27 分钟、52 个步骤。还特别强调，可以在 Trajectory 页面逐条检查 assistant 和工具调用。</p>\n\n<img src=\"assets/manuscript-20260914/c29-b94a339a40636c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>这个案例说明，Harness 不只是改几行代码的工具。需要较长执行链、又想保留全程证据的复杂任务，才是它的主场。</p>\n\n<h4 id=\"c31-s24\">生成高精度工程类 3D 仿真</h4>\n\n<p><a href=\"https://x.com/NFT_Chen/status/2087958659838149033\" target=\"_blank\" rel=\"noopener noreferrer\">@NFT_Chen</a> 展示了一个四冲程柴油机 3D 交互仿真：气缸、活塞、曲轴、配气、燃油、润滑、冷却系统全都有，还能拖拽视角、看相位和实时运行参数。</p>\n\n<img src=\"assets/manuscript-20260914/c29-1fddb246534d0e.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>比起一句\"帮我做个网页\"，这种带明确结构、物理逻辑和交互要求的任务，更能看出模型、工具调用和长任务调度到底行不行。</p>\n\n<h4 id=\"c31-s25\">用插件魔改 UI，把预览面板直接嵌进工作台</h4>\n\n<p><a href=\"https://x.com/dotey/status/2088059102131794012\" target=\"_blank\" rel=\"noopener noreferrer\">@dotey</a> 展示了插件化 UI：左边是 Agent 对话和执行过程，右边直接打开 Explorer、Tasks、网页或 PPT 预览。</p>\n\n<img src=\"assets/manuscript-20260914/c29-0da519f1eefe32.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>这可能是目前最能体现\"一切皆插件\"的大众化玩法：不动 Agent 的核心任务，只换交互层，就能把 Harness 改造成 PPT 工作台、网页工作台或者代码预览器。</p>\n\n<h4 id=\"c31-s26\">把 Harness 做成可学习、可运行的教程和知识产品</h4>\n\n<p><a href=\"https://x.com/yanhua1010/status/2087918781326848460\" target=\"_blank\" rel=\"noopener noreferrer\">@yanhua1010</a> 做了一套\"从零实现一个 AI Agent\"的 Harness 教程，包含原理、源码拆解、渐进式 Demo 和可运行项目。</p>\n\n<img src=\"assets/manuscript-20260914/c29-32aa21aba79b5e.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>很多人已经不满足于\"把它装起来\"，更想搞懂插件、Agent Loop、上下文和运行时到底是怎么组合的。</p>\n\n<p></p>\n\n<h2 id=\"c31-s27\">实测 Case 1：把 Harness 变成\"对话 + 文件 + 预览\"工作台</h2>\n\n<p>先复现 @dotey 那个插件化工作台，就是上面表格里收藏反超点赞的那条，对应插件 DSH-better-sidebar。</p>\n\n<p>安装没有跑任何远程脚本，直接走 DSH 官方插件命令：</p>\n\n<pre data-language=\"bash\"><code>dsh plugin --profile web add dsh-better-sidebar@0.10.3</code></pre>\n\n<p>安装器最终退出码 0，<code>dsh plugin --profile web list --depth 0</code> 也明确列出了 <code>dsh-better-sidebar 0.10.3</code>。重启的时候又踩到那个 Node 版本坑：沙箱外登录 Shell 默认调用 Node.js 22.14.0，而当前 DSH 的持久化模块需要更新的 Zstd API；显式切回本机的 Node.js 22.19.0 之后，DSH 正常监听 <code>127.0.0.1:39201</code>。</p>\n\n<img src=\"assets/manuscript-20260914/c29-782c093a2dcd7a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>重新打开原来的测试会话，右侧已经出现 Explorer，能直接看到工作区里的 <code>project-notes.md</code>、<code>tasks.csv</code> 和 Harness 生成的 <code>README.md</code>。这一步验证的重点是插件 UI 真的加载了，不是\"依赖写进了 package.json\"就完事。</p>\n\n<img src=\"assets/manuscript-20260914/c29-f89d6137c60e47.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>接着在 Explorer 里点 <code>README.md</code>，右侧不用离开会话就出现 Markdown 预览，还能切换编辑和保存。这就是 Harness 的实用点：左边继续让 Agent 干活，右边同时检查文件和成品。插件不是单独的玩具，它在同一个运行时里直接拼成了工作台。</p>\n\n<p><b>实测结论：完整跑通。</b>安装、配置登记、Node 兼容性处理、插件加载、工作区文件浏览和 Markdown 实时预览，六个环节全部验证。</p>\n\n<h2 id=\"c31-s28\">实测 Case 2：OpenPencil 可编辑设计组合，为什么只能判\"部分成功\"</h2>\n\n<p>先安装项目包：</p>\n\n<pre data-language=\"bash\"><code>dsh plugin --profile web add @zseven-w/dsh-openpencil@0.1.0-rc.1</code></pre>\n\n<p>安装器退出码 0，重启后的创造模式也确实拿到了 <code>openpencil_new</code> 和 <code>openpencil_render</code> 两个工具。我让它创建一个 1200×675 的可编辑 <code>harness-case-board.op</code>，并且明确禁止用普通文本或 SVG 冒充。</p>\n\n<p>结果工具真正执行到事务层，返回 <code>OpenPencil editor host binary is unavailable</code>🤔</p>\n\n<p>翻了一圈：本机 <code>/Applications</code> 里没有 OpenPencil.app，PATH 里也没有对应可执行文件，插件缓存只有 <code>render-access.key</code>，没有 editor host binary。创建失败后没有残留 <code>.op</code> 文件，后续渲染自然又报 <code>.op file not found</code>。</p>\n\n<img src=\"assets/manuscript-20260914/c29-39e7a7f59fa15a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p><b>实测结论：部分成功。</b>插件安装和工具注册没问题，但可编辑文件的创建与渲染没跑通。缺的不是 API Key，而是 OpenPencil 的桌面/编辑器 Host。只装 DSH 插件包，不等于拥有外部桌面运行时。遇到同样的报错，也不要拿 SVG 改后缀伪造成功。</p>\n\n<h2 id=\"c31-s29\">实测 Case 3：让 Agent 在运行中给自己造一个新工具</h2>\n\n<p>我设计了一个 Case：</p>\n\n<p>创造模式必须在当前会话里临时注册一个 <code>task_csv_summary</code> 工具，立刻调用它分析 <code>tasks.csv</code>，再生成 <code>tasks-dashboard.md</code>。</p>\n\n<p>Agent 先用 <code>cordis_inspect_*</code> 查询 <code>harness.defineTool/registerTool</code>、Host <code>fs</code> 服务和工具列表，然后通过 <code>cordis_define</code> 创建 <code>csvsum-1/pkg-1</code>，再用 <code>cordis_run</code> 启动为 <code>run-1</code>。<code>Tool.listTools</code> 确认 <code>task_csv_summary</code> 已经进了当前 Agent 的工具集。</p>\n\n<img src=\"assets/manuscript-20260914/c29-f56acd3cdd3c2d.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>关键一步不是\"代码写出来了\"，而是下一轮模型步骤真的调用了 <code>task_csv_summary(\"tasks.csv\")</code>。工具经 Host <code>fs</code> 服务读取 CSV，返回：总任务数 3；P0=2、P1=1；待处理=2、进行中=1；P0 未完成 2 项。整个统计没有用 Bash、Python、Node 脚本或者模型心算代替，就是它自己刚造出来的工具在干活。</p>\n\n<p>然后 Agent 只根据工具返回的 JSON 写出 <code>tasks-dashboard.md</code>，侧边栏立刻打开 Markdown 预览。原来的 <code>tasks.csv</code>、<code>README.md</code>、<code>project-notes.md</code> 我逐字节复查过，一个字都没变。</p>\n\n<img src=\"assets/manuscript-20260914/c29-873ba97a9f9341.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p><b>实测结论：完整跑通，而且这个 Case 最能体现 Harness 的差异。</b></p>\n\n<p>同一个会话里跑完\"检查运行时 → 创建插件 → 加载插件 → 动态工具进入模型工具集 → 实际调用 → 生成可预览产物\"，全程不用重启。看到它把自己刚造的工具用起来的那一刻，我承认有被小小震撼到。这个临时插件重启后会失效，想长期用的话，再打包成正式 DSH 插件就行。</p>\n\n<h2 id=\"c31-s30\">最后</h2>\n\n<p>只想最快打开：</p>\n\n<pre data-language=\"bash\"><code>npx @deepseek-ai/dsh web</code></pre>\n\n<p>准备长期用：</p>\n\n<pre data-language=\"bash\"><code>npm install -g @deepseek-ai/dsh\ndsh web</code></pre>\n\n<p>打不开的时候，先确认真正执行 <code>dsh</code> 的 Node.js 满不满足 <code>^22.19.0 || &gt;=24.0.0</code>，我这次前后两个坑全都是它。</p>\n\n<p>打开之后：添加 API Key、选一个测试工作区、用标准模式跑一个能读文件并生成 README 的最小任务。五分钟，就能验证它在你机器上到底行不行。</p>\n\n<p>从一条命令装好，到插件工作台，再到 Agent 在运行中给自己造工具，DeepSeek Harness 并不是“DeepSeek 版 Codex”：它把模型、工具、UI 全部拆成了可替换的插件。<b>它不是一个定死的产品，你可以按自己的工作流，把它重新拼装成一套属于你的 Agent 底座。</b></p>\n\n<p></p>\n\n<p></p>\n","imageCount":22,"attachmentCount":0,"discussionId":"chapter-29"},{"id":"chapter-32","number":32,"title":"补充：Agent 框架知识","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-32.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-32.html","minutes":25,"status":"ready","headings":[{"id":"c32-s1","text":"主流 Agent 开发框架","level":"h2"},{"id":"c32-s2","text":"LangChain 框架","level":"h3"},{"id":"c32-s3","text":"LlamaIndex 框架","level":"h3"},{"id":"c32-s4","text":"AutoGen 框架","level":"h3"},{"id":"c32-s5","text":"CrewAI 框架","level":"h3"},{"id":"c32-s6","text":"LangGraph 框架","level":"h3"},{"id":"c32-s7","text":"MS-Agent 框架","level":"h3"},{"id":"c32-s8","text":"Agent SDK","level":"h2"},{"id":"c32-s9","text":"OpenAI Agents SDK","level":"h3"},{"id":"c32-s10","text":"Claude Agent SDK","level":"h3"},{"id":"c32-s11","text":"Harness","level":"h2"},{"id":"c32-s12","text":"Harness 是什么","level":"h3"},{"id":"c32-s13","text":"Harness 的主要组成","level":"h3"},{"id":"c32-s14","text":"Harness 与 Agent 的关系","level":"h3"},{"id":"c32-s15","text":"DeepSeek Harness","level":"h3"}],"html":"<h2 id=\"c32-s1\">主流 Agent 开发框架</h2>\n\n<p>随着 Agent 应用逐渐复杂，开发者需要处理工具调用、知识接入、状态管理和多 Agent 协作等问题。如果这些功能都从头开发，不仅工作量较大，各个模块之间的连接和运行控制也比较复杂。</p>\n\n<p>Agent 框架将这些常用能力封装起来，为 Agent 开发提供统一的实现方式。下面介绍几种具有代表性的 Agent 开发框架。</p>\n\n<h3 id=\"c32-s2\">LangChain 框架</h3>\n\n<p>LangChain 于 2022 年推出，是较早用于构建大模型应用的开源框架之一。LangChain 早期主要用于组织大模型、提示词和外部数据等组件，随后逐渐扩展到工具调用和 Agent。目前，LangChain 已经将 Agent 作为框架的重要组成部分，并提供模型、工具、中间件等组件，可以用于快速构建具有工具调用能力的 Agent。</p>\n\n<p>LangChain 的核心运行方式是让大模型和工具形成一个循环。大模型接收用户任务以及可以使用的工具，根据当前信息判断是直接生成结果，还是调用某个工具。如果选择调用工具，框架执行相应工具，再将工具返回的结果提供给大模型。大模型根据新的信息继续判断，直到不再需要调用工具并生成最终结果。官方将这一过程称为 Agent Loop，如下图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c30-b7280769e7f811.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>例如，可以为一个旅行助手配置天气查询、地图查询和网页搜索等工具。当用户提出“帮我规划明天去杭州的一日游”时，大模型可以根据任务判断需要查询天气和景点信息，调用相应工具获得结果，再结合这些结果完成行程规划。</p>\n\n<p>Tool 是实现外部能力的重要方式，本质是具有明确输入和输出的可调用函数，可以用于获取实时数据、执行代码、查询数据库或操作外部系统。开发者将需要的工具提供给 Agent 后，大模型可以根据当前任务决定何时调用哪个工具，以及调用时需要提供哪些参数。</p>\n\n<p>这种组件化方式是 LangChain 的重要特点。LangChain 为不同模型和工具提供了较为统一的接口，开发者可以在同一个框架中组合这些组件，不需要分别处理不同模型和外部能力之间的调用逻辑。因此，比较适合快速搭建工具调用型 Agent，适合初学者理解 Agent 基本运行过程。</p>\n\n<h3 id=\"c32-s3\">LlamaIndex 框架</h3>\n\n<p>LlamaIndex 于 2022 年推出，最初主要用于解决大模型与外部数据之间的连接问题。它提供数据加载、索引、检索和查询等能力，可以将企业文档、数据库等外部数据接入大模型应用。在此基础上，LlamaIndex 又逐渐加入 Agent 和 Workflow 等功能，使 Agent 能够利用这些数据完成更加复杂的任务。</p>\n\n<p>大模型本身并不了解企业内部的产品文档、业务资料和数据库等外部数据。当 Agent 需要利用这些数据完成任务时，需要先找到与当前任务相关的信息。LlamaIndex 可以对外部数据进行加载、组织和索引，再通过 Retriever、Query Engine 等组件进行检索和查询。其中，Query Engine 可以接收自然语言问题，从索引中查找相关数据，也可以封装成 Agent 能够调用的工具。如下图所示，Agent 接收用户任务后，可以根据需要选择相应的查询工具获取信息，再结合大模型进行分析并生成最终结果。通过这种方式，不需要将所有外部数据直接放入上下文，而是可以根据任务按需查询和使用相关数据。</p>\n\n<img src=\"assets/manuscript-20260914/c30-e549ba05352041.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>例如，为一个企业经营分析 Agent 配置产品文档查询和经营数据查询等工具。用户要求比较两款产品的功能差异时，Agent 调用产品资料对应的查询工具；如果用户继续询问两款产品的销售情况，则调用经营数据查询工具。对于更加复杂的问题，Agent 还可以连续调用多个数据查询工具，再综合不同数据源的结果完成任务。</p>\n\n<p>LlamaIndex 最初并不是专门为 Agent 开发设计的，其特点主要体现在数据和知识处理方面。它不仅可以让大模型检索文档，还可以将不同的数据和查询能力组织成 Agent 能够选择和使用的工具。比较适合知识库问答、文档分析、多数据源查询，以及需要访问大量企业文档、知识库或结构化数据的 Agent 应用。</p>\n\n<h3 id=\"c32-s4\">AutoGen 框架</h3>\n\n<p>AutoGen 由微软研究团队于 2023 年推出，是多 Agent 开发领域具有代表性的开源框架。以 Multi-Agent Conversation（多 Agent 对话）作为核心思路，让多个 Agent 通过相互对话共同完成任务。</p>\n\n<p>AutoGen 将 Agent 设计为可对话、可定制的实体，一个 Agent 可以由大模型、工具、人工输入或者这些能力的组合构成。开发者还可以定义 Agent 之间的交互方式，使不同 Agent 形成不同的对话模式。整体设计如下图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c30-48c93d8062c1aa.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>这张图反映了 AutoGen 的两个重要设计思路。一是，不同 Agent 可以具有不同能力。二是，多个 Agent 可以通过不同的 Conversation Pattern 组织起来，以不同方式进行通信和协作。</p>\n\n<p>例如，在软件开发任务中，可以设置一个 Agent 分析需求，一个 Agent 编写代码，另一个 Agent 检查结果。编程 Agent 生成代码后，将结果发送给检查 Agent；如果发现问题，检查 Agent 可以反馈给编程 Agent 继续修改，直到满足任务要求。</p>\n\n<p>当前 AutoGen 主要提供 AgentChat 和 Core 两个层次的能力。AgentChat 是用于构建单 Agent 和多 Agent 应用的高层接口，提供 Agent、Team 等组件。开发者可以将多个 Agent 组成 Team，并采用轮流发言、动态选择下一 Agent 等方式组织协作。Core 则采用事件驱动方式，为更加灵活和可扩展的多 Agent 系统提供底层能力。</p>\n\n<p>AutoGen 的特点是将 Agent 之间的通信和协作作为框架设计的重点，适合需要多个 Agent 分工完成的复杂任务。多 Agent 系统需要额外设计 Agent 的职责和协作方式，对于简单任务，通常没有必要使用多个 Agent。</p>\n\n<h3 id=\"c32-s5\">CrewAI 框架</h3>\n\n<p>CrewAI 同样面向多 Agent 协作，但它采用了更加接近现实团队的组织方式。开发者可以为不同 Agent 定义角色、目标和工具，再通过任务和流程组织这些 Agent 共同完成工作。CrewAI 主要提供 Crews 和 Flows 两种组织方式：Crews 强调多个 Agent 之间的自主协作，Flows 则强调结构化的流程控制。</p>\n\n<p>CrewAI 官方给出的框架结构如下图所示。</p>\n\n<div class=\"source-grid\"><div class=\"source-column\"><img src=\"assets/manuscript-20260914/c30-0d8988dde85e63.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></div><div class=\"source-column\"><img src=\"assets/manuscript-20260914/c30-06c91e889ddd72.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></div></div>\n\n<p>在 Crew 中，Agent 可以理解为具有特定职责的团队成员，Task 表示需要完成的具体任务，Process 规定 Agent 和任务的执行方式，Crew 则将这些 Agent 和任务组织起来，共同完成最终目标。</p>\n\n<p>例如，要完成一份行业研究报告，可以创建研究 Agent、写作 Agent 和审核 Agent。研究 Agent 负责查找和整理资料，写作 Agent 根据研究结果形成报告，审核 Agent 负责检查报告中的问题。不同 Agent 承担不同职责，并通过任务之间的衔接共同完成整个工作。</p>\n\n<p>Flow 可以按照预定的流程组织任务执行，并支持条件判断、循环和状态管理等能力。Crew 和 Flow 也可以组合使用，例如使用 Flow 控制整体业务流程，在其中某个复杂步骤中调用 Crew，由多个 Agent 协作完成任务。</p>\n\n<p>CrewAI 的特点是通过角色、任务和团队来组织多 Agent 协作，整体设计比较接近现实中的团队分工。比较适合角色和任务边界清晰的多 Agent 应用，例如研究、内容生成、数据分析和审核等场景。</p>\n\n<h3 id=\"c32-s6\">LangGraph 框架</h3>\n\n<p>LangGraph 由 LangChain 团队在 2024 年推出，是一个用于构建和管理长时间运行、有状态 Agent 的底层编排框架。与直接使用预构建 Agent 不同，LangGraph 允许开发者显式定义 Agent 的执行结构，特别适合包含状态、分支、循环和人工介入等复杂流程的 Agent。</p>\n\n<p>LangGraph 既可以构建执行路径相对明确的 Workflow，也可以构建由大模型动态决定下一步操作的 Agent。Workflow 可以预先定义任务的执行路径，并通过顺序、并行、路由、循环等方式组织不同步骤；Agent 可以根据当前状态和工具返回结果动态决定下一步操作。实际应用中，Workflow 和 Agent 也可以结合使用，由 Workflow 确定整体流程，Agent 负责其中需要动态决策的环节。</p>\n\n<p>LangGraph 支持的 Workflow 和 Agent 模式如下图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c30-0c25b57c88438b.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>为了实现这些不同的执行方式，LangGraph 使用图来描述任务的执行过程，其中三个基本概念是 State、Node 和 Edge。State 用于保存任务执行过程中需要共享的信息；Node 表示一个具体的处理步骤，例如调用大模型、检索知识或者执行工具；Edge 连接不同节点，并决定任务下一步进入哪个节点。</p>\n\n<p>例如，一个知识问答 Agent 可以先分析用户问题，再检索知识、生成答案并检查结果。如果答案检查不通过，可以通过条件分支重新进入检索节点；如果检查通过，则进入最终输出节点。在整个过程中，用户问题、检索结果、中间答案等信息都可以保存在 State 中，并在不同节点之间共享。</p>\n\n<p>这种设计与完全依靠大模型决定下一步操作不同。LangGraph 允许预先规定任务的大体执行结构，在需要判断的节点中使用大模型进行决策。这样既保留了 Agent 根据实际情况动态判断的能力，也能够对关键执行路径进行控制。</p>\n\n<p>除了图结构和状态管理，LangGraph 还提供 Durable Execution、Human-in-the-loop 等能力。长时间运行的任务可以保存执行状态，并在中断后继续；在重要操作前，也可以暂停 Agent，等待人工检查或修改状态后再继续运行。因此，官方将 LangGraph 定位为面向长时间运行、有状态 Agent 的低层编排框架。</p>\n\n<p>LangGraph 能够对状态和执行路径进行较细粒度的控制，但也需要开发者进行更多流程设计。对于简单的工具调用 Agent，没有必要一开始就构建复杂的图。LangGraph 官方也建议，如果刚开始学习 Agent 或者需要更高层的 Agent 抽象，可以先从 LangChain 的 Agent 开始。</p>\n\n<h3 id=\"c32-s7\"><b>MS-Agent 框架</b></h3>\n\n<p>MS-Agent 是魔搭社区开源的轻量级 Agent 开发框架，主要面向需要自主探索和多步骤执行的任务，提供模型调用、工具接入和多 Agent 协作等能力，可以用于构建深度研究、文档分析和代码生成等应用。</p>\n\n<p>MS-Agent 的基础组件是 LLMAgent，负责组织大模型对话和工具调用。开发者可以通过配置文件指定模型、提示词和工具。接收用户任务后，LLMAgent 将任务与可用工具提供给大模型，由模型判断下一步操作。如果需要调用工具，框架执行相应操作，再将结果交给模型继续处理，直到模型生成不再包含工具调用的回复，或者达到设定的最大运行轮数。</p>\n\n<p>LLMAgent 的基本运行流程如下图所示。Agent 完成配置初始化和消息准备后，在模型调用与工具执行之间循环，并在需要时压缩上下文。图中的 cb 表示回调，开发者可以在相应环节加入日志记录或其他自定义处理。</p>\n\n<img src=\"assets/manuscript-20260914/c30-ca0aea3c815efa.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>工具接入是 MS-Agent 的重要能力。框架提供文件读写、代码执行和任务拆分等内置工具，也支持通过 MCP（Model Context Protocol，模型上下文协议）接入外部工具。开发者可以复用已有的 MCP 服务，或者编写自定义工具，使 Agent 能够访问任务所需的数据和外部系统。</p>\n\n<p>对于需要多个步骤配合的任务，MS-Agent 支持通过工作流组合不同 Agent。其中，LLMAgent 负责需要大模型判断和生成的环节，CodeAgent 负责按照代码执行的确定性操作。开发者可以将资料分析、数据处理和结果生成组织成一个工作流，并通过配置文件规定各个步骤的执行关系。</p>\n\n<p>例如，MS-Agent 项目中的 Code Genesis 提供了多 Agent 协作完成代码生成的示例，将过程分为设计与编码、检查与优化两部分。架构 Agent 负责设计，任务拆分工具将工作分配给多个编程 Agent；进入优化阶段后，再根据构建或人工检查的反馈拆分修改任务，由编程 Agent 继续处理。</p>\n\n<img src=\"assets/manuscript-20260914/c30-1c7cf90db3602c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p></p>\n\n<h2 id=\"c32-s8\">Agent SDK</h2>\n\n<p>除了使用 Agent 开发框架，一些模型厂商也以 SDK（Software Development Kit，软件开发工具包）的形式提供 Agent 开发能力，将模型调用、工具调用和任务执行等能力封装成接口、类和组件，帮助开发者在自己的应用中创建和运行 Agent。Agent 框架和 Agent SDK 都可以用于构建 Agent，两者提供的能力存在一定交叉，只是在组织方式和关注重点上有所不同。下面介绍两个具有代表性的 Agent SDK。</p>\n\n<h3 id=\"c32-s9\">OpenAI Agents SDK</h3>\n\n<p>OpenAI 于 2025 年推出了 OpenAI Agents SDK，它强调使用较少的核心抽象构建 Agent 应用。以 Agent 作为基本执行单元，围绕 Agent 提供工具调用、任务转交、运行约束和执行追踪等能力。Tools 用于查询数据、执行代码、调用 API 或操作其他外部系统；Handoffs 允许当前 Agent 将任务转交给另一个更加适合处理该任务的 Agent；Guardrails 负责检查 Agent 的输入、输出以及部分工具调用；Tracing 则记录 Agent 执行过程中发生的模型生成、工具调用、任务转交和 Guardrail 等事件。</p>\n\n<p>OpenAI Agents SDK 还提供 Agent Visualization，可以将 Agent 以及它连接的其他 Agent、Tools 和 MCP Server 生成图结构。其中 Agent 之间的有向连接可以表示 Handoff，工具和 Agent 之间的连接则表示工具调用。</p>\n\n<img src=\"assets/manuscript-20260914/c30-d8506a997ef88a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>例如，在客服系统中，可以设置一个入口 Agent 负责识别用户问题，再设置订单 Agent、退款 Agent 和 FAQ Agent 处理不同类型的业务。当用户咨询退款问题时，入口 Agent 可以通过 Handoff 将任务转交给退款 Agent；退款 Agent 再根据需要调用订单查询、退款申请等工具完成任务。Handoff 尤其适合这种由不同专业 Agent 分别处理不同任务的场景。</p>\n\n<p>Guardrails 和 Tracing 考虑了 Agent 实际运行中的约束和观察问题。Guardrails 可以在 Agent 输入、最终输出以及自定义函数工具执行前后进行检查；Tracing 则记录一次 Agent 运行中的模型生成、工具调用、Handoff 和 Guardrail 等事件，帮助开发者了解 Agent 经过了哪些步骤以及问题出现在哪里。</p>\n\n<p>OpenAI Agents SDK 的特点是核心概念相对集中。开发者可以先从单个 Agent 和 Tools 开始，再逐渐加入多 Agent 协作、Guardrails 和 Tracing 等能力。与 LangGraph 相比，两者的设计重点有所不同：LangGraph 更加突出状态和工作流的细粒度控制；OpenAI Agents SDK 则围绕 Agent 提供工具调用、任务转交、运行约束和执行追踪等常用能力。</p>\n\n<h3 id=\"c32-s10\">Claude Agent SDK</h3>\n\n<p>Claude Agent SDK 是 Anthropic 推出的 Agent 开发 SDK，原名为 Claude Code SDK，后来更名为 Claude Agent SDK。它将 Claude Code 中的 Agent 能力开放给开发者，使开发者可以在自己的应用中构建能够使用工具、访问运行环境并持续执行任务的 Agent。</p>\n\n<p>与前面介绍的 Agent 开发工具相比，Claude Agent SDK 的一个突出特点是更加关注 Agent 对实际运行环境的访问和操作。除了调用外部工具，它还提供文件读取、文件修改和命令执行等内置能力，使 Agent 能够直接处理文件、运行程序和执行命令，并可以通过 MCP 连接更多外部工具和数据。此外，它提供权限控制、Hooks 和 Subagents 等机制，通过权限控制限制 Agent 能够执行的操作；Hooks 可以在工具调用等关键阶段加入自定义处理逻辑；Subagents 可以将部分任务交给独立的子 Agent 处理。</p>\n\n<p>在运行过程中，Agent 会根据当前任务和上下文判断下一步操作，例如读取文件、修改代码或执行命令。工具执行后的结果会重新返回给 Agent，Agent 再根据新的信息继续判断，直到任务完成。Claude Agent SDK 负责组织这一执行过程，并在其中处理工具调用、权限检查和上下文传递等工作。</p>\n\n<p>例如，在软件开发任务中，可以让 Agent 读取项目代码，根据用户需求修改文件，然后执行测试。如果测试失败，Agent 可以读取错误信息并继续修改，直到完成任务。在这一过程中，大模型负责理解任务和决定下一步操作，Claude Agent SDK 则负责提供文件系统、终端和工具等运行能力，并对权限和任务执行过程进行管理。</p>\n\n<p>Claude Agent SDK 比较适合需要访问文件和运行环境、连续调用工具并执行多步骤任务的 Agent 应用，尤其适合软件开发和自动化任务等场景。</p>\n\n<p>在实际的 Agent 开发中，运行环境、上下文管理、权限控制、任务状态和执行反馈等能力越来越受到重视。如何围绕大模型组织这些能力，并对任务执行过程进行管理和控制，已经成为 Agent 系统设计中的重要问题，Harness 关注的正是这些问题。</p>\n\n<h2 id=\"c32-s11\">Harness</h2>\n\n<h3 id=\"c32-s12\">Harness 是什么</h3>\n\n<p>大模型能力不断提升后，Agent 面临的问题逐渐从“模型能不能完成某项任务”转向“模型能不能持续、可靠地完成实际任务”。在一次简单的问答中，大模型只需要根据输入生成结果；但在软件开发、数据分析、业务自动化等复杂任务中，Agent 可能需要连续工作较长时间，在多个步骤之间保存进度，调用不同工具，并根据实际执行结果不断调整后续操作。此时，仅依靠模型自身的推理能力并不能保证任务最终正确完成。</p>\n\n<p>例如，一个代码 Agent 可能能够正确理解“修改某个功能”的需求，也能够生成质量较高的代码，但在实际执行过程中仍可能出现各种问题：没有阅读项目规范就开始修改代码、同时修改过多文件、遗漏必要步骤、执行过程中丢失前面的任务进度，或者代码尚未通过测试就认为任务已经完成。这些问题并不完全来自模型本身的能力，而与模型所处的工作环境以及任务执行方式有关。</p>\n\n<p>Harness 就是围绕大模型建立的一套工作环境和运行机制，用于规定模型能够获得哪些信息、可以执行哪些操作、如何保存任务状态、如何判断任务是否完成，以及出现问题后如何继续处理。通过这些机制，可以将模型一次次独立的推理和操作组织成一个受到约束、能够持续运行并可以验证结果的任务执行过程。</p>\n\n<p>Harness 的重点并不是进一步增强大模型本身的知识或推理能力，而是让模型已有的能力能够更加可靠地作用于实际任务。模型仍然负责理解任务、分析问题和决定下一步操作；Harness 则负责为模型建立完成任务所需要的工作条件，并对整个执行过程施加约束和反馈。</p>\n\n<p>从运行过程来看，Harness 使 Agent 形成一个持续的闭环：Agent 根据当前任务和状态进行判断，执行相应操作，再从运行环境中获得新的结果；这些结果经过记录、检查和反馈后重新成为下一步决策的依据。如果结果不满足要求，Agent 继续修改和执行；只有达到预先规定的完成条件后，任务才真正结束。</p>\n\n<p>Harness 与简单的 Prompt 或工具调用存在明显区别。Prompt 主要通过指令告诉模型任务目标和行为要求，工具使模型能够执行具体操作，而 Harness 进一步关注这些指令和工具如何在整个任务执行过程中被组织和管理。它需要让 Agent 知道从哪里开始、当前进行到哪里、哪些操作可以执行、结果如何验证、什么时候可以结束，以及任务中断后如何继续。对于需要长时间运行和多步骤执行的 Agent，这些机制直接影响任务能否稳定完成。</p>\n\n<h3 id=\"c32-s13\">Harness 的主要组成</h3>\n\n<p>Harness 并没有统一的实现方式，不同 Agent 系统会根据任务类型提供不同的运行机制。从 Agent 实际运行需要解决的问题来看，可以将 Harness 归纳为五个相互配合的部分：指令与上下文、工具、运行环境、状态与任务连续性、验证与反馈。</p>\n\n<p>（1）指令与上下文</p>\n\n<p>指令与上下文决定 Agent 在当前任务中“知道什么”以及“应该遵循什么”。</p>\n\n<p>指令不仅包括用户当前输入的任务，还包括系统提示词、项目规则、代码规范、业务约束、任务边界以及完成条件等内容。例如，编程 Agent 可以通过<code>AGENTS.md</code>、<code>CLAUDE.md</code>或项目文档了解目录结构、开发规范、测试方法以及禁止修改的区域。</p>\n\n<p>上下文指在执行过程中提供给模型的有效信息，包括用户需求、历史操作、文件内容、工具执行结果以及当前任务状态等。由于模型上下文窗口有限，Harness 通常还需要对上下文进行选择、压缩和重新组织，使模型能够获得当前任务真正需要的信息，而不是简单地不断追加全部历史内容。</p>\n\n<p>对于复杂任务，可以采用分层指令和渐进式上下文加载。例如，先让 Agent 读取项目整体说明，在进入某个模块后再加载该模块的局部规则；当任务执行时间较长、上下文不断增加时，则可以压缩早期过程，只保留关键结论、任务状态和后续仍然需要使用的信息。通过这些机制，可以在有限的上下文窗口中持续为 Agent 提供有效信息，并通过指令约束 Agent 的任务范围和行为边界。</p>\n\n<p>（2）工具</p>\n\n<p>大模型本身主要负责理解、推理和生成决策，要真正读取外部信息或执行具体操作，还需要通过工具完成。因此，工具决定了 Agent 能够实际执行哪些动作。</p>\n\n<p>对于编程 Agent，常见工具包括文件读取、文件修改、代码搜索、Shell 命令执行、Git 操作和测试工具；对于企业 Agent，则可能包括数据库查询、知识库检索、浏览器、业务 API 以及通过 MCP 接入的外部系统。</p>\n\n<p>Harness 在这一层不只是维护一个工具列表，还需要处理工具描述、参数组织、调用路由、执行结果返回以及异常处理。例如，大模型决定“运行项目测试”之后，Harness 需要调用相应工具，在指定环境中执行测试命令，再将标准输出、错误信息和退出状态整理后返回给模型，使 Agent 能够根据执行结果决定下一步操作。</p>\n\n<p>工具调用需要配合相应的安全与权限控制。某些工具只能读取文件，某些工具允许修改内容；涉及删除文件、访问网络、执行系统命令或操作生产系统时，还可以根据风险等级进行限制，必要时要求人工确认。Hooks 等机制也可以插入工具调用前后，用于检查参数、记录执行过程或阻止不符合规则的操作。</p>\n\n<p>在支持 Subagent 的系统中，主 Agent 还可以将部分任务交给专门的 Subagent 处理。例如，将代码搜索、测试分析或资料整理等相对独立的工作分配给不同 Subagent，再汇总它们返回的结果。这样可以扩展单个 Agent 的任务处理能力，并减少复杂任务集中在一个执行上下文中的压力。</p>\n\n<p>（3）运行环境</p>\n\n<p>工具回答的是“Agent 能够做什么”，运行环境则决定这些操作“在哪里发生”。</p>\n\n<p>对于编程 Agent，运行环境可能是本地项目目录、容器、虚拟机、云端 Sandbox 或者独立的 Git Worktree。Agent 读取和修改文件、执行 Shell 命令、安装依赖以及运行测试，都需要依托具体的运行环境。</p>\n\n<p>Harness 需要对运行环境进行准备和管理，如初始化代码仓库、安装依赖、设置环境变量、启动必要服务，以及检查当前环境是否满足任务执行条件。对于长时间运行的任务，还需要避免环境在执行过程中出现依赖缺失、服务异常或资源状态不一致等问题。</p>\n\n<p>环境隔离也是运行环境管理的重要内容。如果多个 Agent 或多个任务同时处理同一个项目，直接操作同一个工作目录可能产生文件覆盖和状态冲突。可以为不同任务建立独立的 Sandbox、容器或 Worktree，使不同任务在相对隔离的空间中运行。即使某个任务执行失败，也可以降低对其他任务和宿主环境的影响。</p>\n\n<p>运行环境还承担资源访问边界的控制。例如，限制 Agent 只能访问指定目录、只能连接特定网络、不能读取系统敏感文件，或者为不同任务配置不同的访问凭证。工具定义了 Agent 可以使用哪些能力，运行环境则限定这些能力实际能够作用于哪些文件、进程、网络和系统资源。</p>\n\n<p><b>（4）状态与任务连续性</b></p>\n\n<p>大模型的一次调用本身没有天然的长期任务状态，而复杂 Agent 任务可能持续几十分钟、数小时甚至跨越多个会话。如果任务信息只存在于当前上下文中，一旦上下文被压缩、进程中断或者会话重新启动，Agent 就可能无法准确判断之前已经完成了哪些工作。</p>\n\n<p>Harness 需要维护任务状态，例如当前目标、任务拆分结果、已经完成的步骤、正在处理的步骤、修改过的文件、重要工具执行结果以及接下来需要继续处理的内容。</p>\n\n<p>这些状态可以保存在内存中，也可以写入任务文件、数据库、Git 历史或其他外部存储。对于长时间运行的 Agent，持久化状态尤其重要。新的 Agent 会话可以读取此前保存的任务进度和关键结果，从上一次中断的位置继续执行，而不必重新分析整个任务。</p>\n\n<p>状态管理贯穿任务的整个生命周期。任务开始时建立初始状态，执行过程中持续记录进度和关键结果，任务完成后保存最终状态；如果任务因为异常、超时或系统重启而中断，可以通过检查点等机制恢复到之前的执行位置。对于包含 Subagent 的复杂任务，可以记录各个子任务的完成情况、执行结果以及相互之间的依赖关系。</p>\n\n<p>状态与任务连续性的重点是维护一个能够持续更新、持久化和恢复的任务执行过程，使 Agent 在长时间运行或跨会话执行时仍然知道任务进行到了哪里。</p>\n\n<p>（5）验证与反馈</p>\n\n<p>Agent 执行了某个动作，并不意味着任务已经正确完成。例如，Agent 成功修改了代码文件，但代码可能无法编译；生成了一份分析报告，但其中可能缺少关键数据；调用业务接口成功，也可能产生不符合业务规则的结果。因此，Harness 还需要建立验证与反馈机制，对执行结果进行检查，并将检查结果重新提供给 Agent。</p>\n\n<p>验证方式取决于具体任务。对于软件开发任务，可以使用单元测试、Lint、类型检查、编译和端到端测试；对于业务任务，可以使用业务规则检查、结果比对或独立评估器。验证的核心是为任务完成提供可以检查的依据，而不是仅依赖模型自己判断“任务已经完成”。</p>\n\n<p>反馈则将验证结果重新提供给 Agent。例如，测试失败后，Harness 可以将错误信息和相关日志返回给模型，Agent 根据这些信息继续分析原因、修改代码并重新测试，从而形成：“执行 → 验证 → 获取反馈 → 修正 → 再验证”。这样的闭环能够让 Agent 根据真实执行结果不断修正前面的决策，而不是在一次操作之后直接结束任务。</p>\n\n<p>Harness 还可以在这一过程中加入执行控制。例如，只有测试全部通过后才允许提交代码；连续多次执行失败后停止任务或转交人工处理；涉及生产环境修改、数据删除等高风险操作时，则在真正执行之前暂停任务并要求人工审批。获得确认后继续执行，未获得确认则终止或调整操作。</p>\n\n<p>这五个部分共同支撑 Agent 的持续运行：指令与上下文提供任务目标、规则和必要信息；工具提供实际行动能力；运行环境承载这些操作并限定资源边界；状态与任务连续性记录任务进展并支持恢复；验证与反馈检查执行结果，并推动 Agent 继续修正或结束任务。通过这些机制，Harness 将 Agent 一次次独立的模型调用组织成一个能够持续执行、受到约束并可以验证结果的完整任务过程。</p>\n\n<img src=\"assets/manuscript-20260914/c30-a8356c8cede482.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<h3 id=\"c32-s14\">Harness 与 Agent 的关系</h3>\n\n<p>Agent 与 Harness 并不是两个相互替代的概念，而是处在不同层次。在 Agent 运行过程中，大模型主要负责理解任务、分析当前信息并决定下一步操作；Harness 则围绕 Agent 提供指令与上下文、工具、运行环境、状态管理以及验证反馈等支撑，使 Agent 的决策能够真正执行，并在执行过程中受到约束和检查。</p>\n\n<p>二者之间是一种“决策与执行支撑”的关系。Agent 根据当前任务和已有信息判断下一步应该做什么，Harness 负责提供完成这一操作所需的工具和环境，并记录执行过程中产生的状态和结果。执行完成后，Harness 还可以通过测试、规则检查等方式验证结果，再将反馈提供给 Agent。Agent 根据新的状态和反馈继续判断下一步操作，如此循环，直到满足任务完成条件。</p>\n\n<p>例如，一个编程 Agent 判断需要修改 login.py 并运行测试。其中，分析代码并决定如何修改主要依赖大模型的理解和推理能力；而 login.py 是否允许修改、从哪里读取文件、通过什么工具修改、测试命令在哪个 Sandbox 中执行、修改进度如何保存、测试结果如何检查，以及失败后如何继续执行，则需要 Harness 提供相应的运行机制。测试失败后，错误信息会重新提供给 Agent，Agent 据此分析原因并决定下一步修改方案。</p>\n\n<p>Harness 的完善程度会影响 Agent 能够处理的任务类型。简单的 Harness 可能只提供少量工具和基本的调用机制，适合完成步骤较少的任务；更加完善的 Harness 则可以进一步提供上下文管理、环境隔离、任务状态持久化、权限控制、自动验证、失败恢复和人工审批等机制，使 Agent 能够持续执行更长、更复杂的任务。即使使用相同的大模型，不同 Harness 提供的运行条件不同，Agent 在复杂任务中的执行能力和稳定性也可能存在明显差异。</p>\n\n<p>Harness 与 Agent 框架之间存在一定的交集，都会涉及工具调用、上下文管理、状态管理和任务执行控制等能力，但二者的关注重点不同。Agent 框架更偏向于提供 Agent 构建和任务组织所需的开发能力，如 Agent 定义、工具调用、状态管理、工作流编排和多 Agent 协作等；Harness 更关注围绕 Agent 运行建立的支撑和控制机制，例如如何组织上下文、如何提供工具和运行环境、如何保持任务连续，以及如何验证执行结果。随着 Agent 框架和 Agent SDK 不断发展，一些 Harness 能力也逐渐被集成到框架或 SDK 内部，因此二者在具体实现上并不存在绝对的功能边界。</p>\n\n<p>从整体上看，可以将三者的关系概括为：大模型提供智能，Agent 组织决策，Harness 保障执行<b>。</b></p>\n\n<p>随着 Agent 执行任务复杂度的提升，Harness 的作用会更加明显。</p>\n\n<h3 id=\"c32-s15\">DeepSeek Harness</h3>\n\n<p>下面以 DeepSeek Harness 为例，进一步了解 Harness 在实际 Agent 系统中的组织方式。</p>\n\n<p>DeepSeek Harness 是 DeepSeek 在 2026 年推出的开源 Agent Harness，其特点是通过插件化架构组织模型运行所需要的工具、Skills、会话、沙箱和任务执行等能力，为 Agent 提供可扩展的运行支撑。</p>\n\n<p>DeepSeek 将一个可运行的 Agent 理解为：</p>\n\n<p>Agent = Model + Harness</p>\n\n<p>其中，Model 负责理解任务和进行决策，Harness 负责组织模型完成任务所需要的工具、上下文和执行环境。</p>\n\n<p>DeepSeek Harness 的核心设计是“Everything is a Plugin（一切皆插件）”。模型、工具、Skills、会话、沙箱、存储、执行循环、任务调度和子 Agent 等能力都可以通过插件提供，并由底层的 Cordis 插件系统进行组织。Cordis 负责插件的加载、卸载、依赖管理和生命周期管理，具体的 Agent 能力则由不同插件提供。开发者可以根据实际需要组合、替换或扩展不同插件。</p>\n\n<p>整体插件化架构如下图所示<b>。</b></p>\n\n<img src=\"assets/manuscript-20260914/c30-c887b0e57c1da9.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>例如，使用 DeepSeek Harness 构建一个数据分析 Agent。Agent 读取用户提供的数据文件，根据任务调用相应工具完成数据处理和分析，也可以使用 Skills 完成特定的数据分析任务。如果任务比较复杂，还可以将部分工作交给 Subagent 处理。模型负责判断下一步需要完成什么操作，Harness 则负责提供和组织完成这些操作所需要的能力。</p>\n\n<p>除了插件化设计，DeepSeek Harness 还提供会话和运行过程记录等机制。模型看到的系统提示词、工具调用及其结果、Subagent 调度和上下文注入等信息可以记录在会话日志中，并通过 Trajectory 查看。基于这些记录，还可以对任务进行恢复、分叉、检索和回放，方便开发者观察和调试 Agent 的执行过程。</p>\n\n<p>目前 DeepSeek Harness 仍处于 Developer Preview 阶段，其核心插件和 API 仍在持续迭代。因此，更适合将其作为理解 Harness 插件化设计和工程实现的一种代表性案例，而不是将当前接口视为已经固定的开发标准。</p>\n","imageCount":11,"attachmentCount":0,"discussionId":"chapter-30"},{"id":"chapter-33","number":33,"title":"产线巡检 Agent：用 Penguin Harness 快速开发与优化","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-33.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-33.html","minutes":15,"status":"ready","headings":[{"id":"c33-s1","text":"1. 背景","level":"h2"},{"id":"c33-s2","text":"2. Penguin Harness 的基础使用","level":"h2"},{"id":"c33-s3","text":"2.1 下载并打开客户端","level":"h3"},{"id":"c33-s4","text":"2.2 接入模型 API","level":"h3"},{"id":"c33-s5","text":"2.3 选择 DeepSeek 模型","level":"h3"},{"id":"c33-s6","text":"2.4 开始一次任务","level":"h3"},{"id":"c33-s7","text":"3. 从零开发产线巡检 Agent 应用","level":"h2"},{"id":"c33-s8","text":"3.1 准备业务规范文件与输入示例","level":"h3"},{"id":"c33-s9","text":"3.2 了解两个 MCP 服务","level":"h3"},{"id":"c33-s10","text":"3.3 开发并启动 Agent 应用","level":"h3"},{"id":"c33-s11","text":"3.4 演示 Penguin Harness 开发的 Agent 应用","level":"h3"},{"id":"c33-s12","text":"4. 用 Penguin Harness 优化产线巡检 Agent","level":"h2"},{"id":"c33-s13","text":"4.1 准备并发起评测与优化任务","level":"h3"},{"id":"c33-s14","text":"4.2 查看优化结果","level":"h3"},{"id":"c33-s15","text":"5. 投入与运行成本","level":"h2"},{"id":"c33-s16","text":"5.1 Agent 开发与优化的成本","level":"h3"},{"id":"c33-s17","text":"5.2 单次巡检任务的运行成本","level":"h3"},{"id":"c33-s18","text":"6. 总结","level":"h2"}],"html":"<h2 id=\"c33-s1\">1. 背景</h2>\n\n<p>当前，制造业正持续推进自动化与智能化转型，生产现场对设备运行管理和异常响应效率也提出了更高要求。让 Agent 技术更深入地参与日常巡检与异常处理，成为提升生产运维效率的一个重要方向。</p>\n\n<p>在生产运维中，产线的稳定运行离不开日常巡检和异常处理。现场人员需要结合设备状态与生产信息判断问题，并协调后续处置。例如，部分产线在供电恢复后仍需人工重启，响应不及时就会延长停机时间，影响生产。如何减轻重复工作、提高异常响应效率，是产线运维中的一个实际需求。</p>\n\n<p>目前，Agent 为这类工作提供了一种新的实现方式：将业务知识、信息分析和工具调用结合起来，协助完成从发现问题到执行处理的工作流程。在这类场景中，Agent 可以结合运行资料和设备当前状态分析停机原因（例如停电），并在确认具备运行条件后调用工具恢复产线。下面以供电恢复后输送带仍未运行的情况为例，对比人工与 Agent 的处理流程：</p>\n\n<p><img src=\"assets/penguin-harness/figure-01.png\" alt=\"图 1：来电后输送带未恢复，人工与 Agent 的处理流程示意。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p>不过，开发这样的 Agent 仍需要完成应用开发和效果验证，并在运行后持续优化。Penguin Harness 是一个用于自动化开发和优化 Agent 的框架，可以大大减少这些工作所需的人工投入。使用者提供业务需求、示例、规范和相关接口，Penguin Harness 据此开发 Agent 及配套网页应用，并结合评测反馈持续改进 Agent 的任务表现。具体流程如下图所示：</p>\n\n<p><img src=\"assets/penguin-harness/figure-02.png\" alt=\"图 2：Penguin Harness 开发与优化产线巡检 Agent。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p>本文以产线巡检为例，展示如何使用 Penguin Harness 开发和优化 Agent 应用。在本例中，Agent 负责分析设备异常和调用设备工具，配套网页供用户上传资料、发起任务和查看结果。</p>\n\n<h2 id=\"c33-s2\">2. Penguin Harness 的基础使用</h2>\n\n<p>在开始开发之前，我们需要先安装 Penguin Harness 客户端、接入模型，并完成一次简单交互。下面以桌面客户端和 DeepSeek Flash 为例，介绍后文会用到的基础操作。</p>\n\n<h3 id=\"c33-s3\">2.1 下载并打开客户端</h3>\n\n<p><strong>第 1 步：下载并安装客户端</strong></p>\n\n<p>打开 <a href=\"https://penguin.ooo/download\">Penguin Harness 客户端下载页面</a>，选择与自己电脑系统对应的安装包，下载后按提示安装。</p>\n\n<p><img src=\"assets/penguin-harness/figure-03.png\" alt=\"图 3：客户端下载页：按操作系统选择安装包。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p><strong>第 2 步：打开客户端</strong></p>\n\n<p>安装完成后，打开客户端。在主界面左侧点击“模型库”，即可配置接下来要使用的模型。</p>\n\n<p><img src=\"assets/penguin-harness/figure-04.png\" alt=\"图 4：Penguin Harness 主界面，左侧为“模型库”入口。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<h3 id=\"c33-s4\">2.2 接入模型 API</h3>\n\n<p><strong>第 1 步：获取 API Key</strong></p>\n\n<p>Penguin Harness 通过模型 API 完成分析和任务执行。使用 DeepSeek 时，先在 <a href=\"https://platform.deepseek.com/\">DeepSeek 开放平台</a>创建 API Key，再将它填入客户端。API Key 用于验证模型调用身份。</p>\n\n<p><strong>第 2 步：填写并保存模型配置</strong></p>\n\n<p>在“模型库”中展开 DeepSeek 分组，打开 DeepSeek Flash 的模型配置，填入自己的 API Key，点击“确认”保存。通过官方服务接入时，API 地址为 <a href=\"https://api.deepseek.com\"><a href=\"https://api.deepseek.com\">https://api.deepseek.com</a></a>，模型 ID 为 deepseek-flash。下图标出了对应位置。</p>\n\n<p><img src=\"assets/penguin-harness/figure-05.png\" alt=\"图 5：DeepSeek Flash 模型配置：填写 API Key、API 地址和模型 ID。密钥已遮挡。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<h3 id=\"c33-s5\">2.3 选择 DeepSeek 模型</h3>\n\n<p><strong>第 1 步：确认模型已保存</strong></p>\n\n<p>保存后，DeepSeek Flash 会显示在模型库中。下图中的“默认”标签表示它已设为默认模型；也可以在每次任务开始前单独选择模型。</p>\n\n<p><img src=\"assets/penguin-harness/figure-06.png\" alt=\"图 6：模型库中的 DeepSeek Flash；“默认”标签表示当前默认模型。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p><strong>第 2 步：选择本次任务使用的模型</strong></p>\n\n<p>点击左侧“新建对话”，在输入框右下方打开模型列表，选择 DeepSeek Flash。列表中的勾选标记表示本次任务将使用该模型。</p>\n\n<p><img src=\"assets/penguin-harness/figure-07.png\" alt=\"图 7：在输入框右下方选择本次任务使用的模型。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<h3 id=\"c33-s6\">2.4 开始一次任务</h3>\n\n<p><strong>第 1 步：克隆示例仓库</strong></p>\n\n<p>为了跟着本文从零开发，可以先将 <a href=\"https://github.com/rank-Yu/line-recovery-starter\">line-recovery-starter 仓库</a>克隆到本地。仓库只提供业务需求、数据契约、示例数据包和模拟设备服务。在已安装 Git 的电脑上，打开终端，运行：</p>\n\n<pre class=\"bash\" data-language=\"text\"><code class=\"language-text\">git clone https://github.com/rank-Yu/line-recovery-starter.git</code></pre>\n\n<p><strong>第 2 步：选择工作区</strong></p>\n\n<p>命令完成后，当前目录下会生成 line-recovery-starter 文件夹。回到 Penguin Harness，点击输入框下方的工作区入口，选择这个文件夹，再点击“使用此目录”。后续任务就可以读取其中的项目材料。</p>\n\n<p><img src=\"assets/penguin-harness/figure-08.png\" alt=\"图 8：选择项目文件夹后，点击“使用此目录”。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p><strong>第 3 步：了解技能入口</strong></p>\n\n<p>输入框下方的“技能”菜单列出了当前可用的技能，它们为特定任务提供操作说明。可以先熟悉这个入口，开发或评测时再选择相应技能；本次读取项目的简单任务无需额外选择。</p>\n\n<p><img src=\"assets/penguin-harness/figure-09.png\" alt=\"图 9：输入框下方的“技能”菜单，列出当前可用技能。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p><strong>第 4 步：发送首次任务</strong></p>\n\n<p>准备好后，在会话输入框中发送一条简单指令，让 Penguin Harness 先了解工作区：</p>\n\n<blockquote>\n<p>请查看当前工作区的 README.md，简要介绍项目用途，以及 contracts、examples、interfaces 三个目录的作用，控制在 150 字以内。只读取文件，不修改或运行项目。</p>\n</blockquote>\n\n<p><img src=\"assets/penguin-harness/figure-10.png\" alt=\"图 10：选定工作区并输入指令后，点击右侧箭头发送。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p><strong>第 5 步：查看任务回复</strong></p>\n\n<p>发送后，可以在对话中查看回复，并展开工具调用记录，了解它读取了哪些文件。得到与工作区内容相符的介绍后，就完成了首次交互。</p>\n\n<p><img src=\"assets/penguin-harness/figure-11.png\" alt=\"图 11：任务完成后的项目介绍与文件读取记录。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p>接下来，我们以产线巡检为例，准备业务材料，并让 Penguin Harness 开发 Agent 及配套网页应用。</p>\n\n<h2 id=\"c33-s7\">3. 从零开发产线巡检 Agent 应用</h2>\n\n<h3 id=\"c33-s8\">3.1 准备业务规范文件与输入示例</h3>\n\n<p>第二章已经将 <a href=\"https://github.com/rank-Yu/line-recovery-starter\">line-recovery-starter 仓库</a>克隆到本地。这个仓库保留了原项目的业务规范文件、输入示例、数据契约和两个模拟设备 MCP 服务，尚未包含生成后的 Agent 应用。本章从这份材料出发，介绍开发、启动和结果核查步骤。</p>\n\n<p><strong>第 1 步：阅读业务需求</strong></p>\n\n<p>先看仓库根目录的 <a href=\"https://github.com/rank-Yu/line-recovery-starter/blob/main/README.md\">README.md</a>。它是交给 Penguin Harness 的需求文档，下面概括业务场景和交付要求，完整内容以仓库原文为准：</p>\n\n<pre class=\"text\" data-language=\"text\"><code class=\"language-text\">目标：制作中文产线恢复助手，支持上传 ZIP 或加载示例。\n\n业务场景：\n1. 来电后输送带仍未运行：核对供电、驱动与运行许可，\n   满足条件后请求恢复，再检查带速和新的出口计数。\n2. 温度升高导致输送暂停：请求开启散热并观察温度，\n   温度达标且重新获得运行许可后，再请求恢复输送。\n\n处理要求：结合运行记录、日志、状态和图片判断；\n先查询工具当前状态，再决定操作，最后核查新反馈。\n条件不足或反馈未确认时，如实说明并转人工。\n\n交付内容：在 app/ 生成 Agent、后端、中文前端和启动说明，\n接入已有的两个 MCP 服务，展示证据、动作记录和结果。\n工具未连接时明确提示，不伪造动作成功。</code></pre>\n\n<p><strong>第 2 步：了解工作区材料</strong></p>\n\n<p>业务说明之外，Penguin Harness 还需要知道应用将收到哪些资料，以及应该输出什么格式。仓库中的材料分别承担以下作用：</p>\n\n<div class=\"table-scroll\"><table>\n<thead>\n<tr class=\"header\">\n<th>工作区材料</th>\n<th>开发时的用途</th>\n</tr>\n</thead>\n<tbody>\n<tr class=\"odd\">\n<td>README.md</td>\n<td>业务需求、材料阅读顺序和应用交付要求</td>\n</tr>\n<tr class=\"even\">\n<td>examples/input/</td>\n<td>示例数据包，包括运行记录、事件日志、状态快照、工位图片与操作说明</td>\n</tr>\n<tr class=\"odd\">\n<td>contracts/</td>\n<td>输入、设备状态、操作和报告的格式规范</td>\n</tr>\n<tr class=\"even\">\n<td>interfaces/</td>\n<td>输送恢复与散热控制 MCP 服务，以及初始化和接入说明</td>\n</tr>\n</tbody>\n</table>\n</div>\n<p><strong>第 3 步：查看输入示例</strong></p>\n\n<p>打开 <a href=\"https://github.com/rank-Yu/line-recovery-starter/tree/main/examples/input\">examples/input/</a>，可以看到这份示例数据包，主要文件如下：</p>\n\n<pre class=\"plaintext\" data-language=\"text\"><code class=\"language-text\">examples/input/\n├── request.json          # 本次要解决的问题\n├── telemetry.csv         # 电压、带速、温度和产出记录\n├── events.jsonl          # 设备事件日志\n├── device_state.json     # 采集时的设备状态\n├── images/frame_001.png  # 工位图片\n├── operating_guide.md    # 设备操作说明\n└── ...</code></pre>\n\n<p>例如，events.jsonl 记录了设备状态变化。下面摘取两条记录，只展示时间和消息内容，其余字段与记录省略：</p>\n\n<pre class=\"json\" data-language=\"text\"><code class=\"language-text\">{\n  \"timestamp\": \"2026-09-15T09:00:40+08:00\",\n  \"message\": \"上游24 V电源有效反馈由1变0，驱动支路电压降至0 V；独立供电的控制器保持在线。\"\n}\n...\n{\n  \"timestamp\": \"2026-09-15T09:01:12+08:00\",\n  \"message\": \"驱动重新就绪；配置禁止来电自动启动，等待新的获准运行请求。\"\n}</code></pre>\n\n<p>这些日志会和运行数据、工位图片一起交给 Agent，供它分析停机原因和恢复条件。</p>\n\n<p>接下来，先看看仓库中的两个 MCP 服务分别提供哪些设备工具。</p>\n\n<h3 id=\"c33-s9\">3.2 了解两个 MCP 服务</h3>\n\n<p>Agent 分析出停机原因后，还需要查询设备当前的状态，并执行相应操作。仓库的 interfaces/ 目录已经提供了两个 MCP 服务，Penguin Harness 开发应用时会接入它们，无需从头编写设备接口。</p>\n\n<div class=\"table-scroll\"><table>\n<thead>\n<tr class=\"header\">\n<th>MCP 服务</th>\n<th>用途</th>\n</tr>\n</thead>\n<tbody>\n<tr class=\"odd\">\n<td>power-control</td>\n<td>查询供电和输送带状态，在条件满足时启动输送带。</td>\n</tr>\n<tr class=\"even\">\n<td>cooling-control</td>\n<td>查询温度和风机状态，开启风机散热。</td>\n</tr>\n</tbody>\n</table>\n</div>\n<p>例如，供电恢复后输送带仍未运行，Agent 会先通过 power-control 检查设备状态，满足条件后请求启动，再查看带速和产出是否恢复。如果是温度过高导致停机，则先通过 cooling-control 开启风机，等温度降下来，再检查能否启动输送带。</p>\n\n<p>两个服务都在本地模拟环境中运行，不连接真实产线设备。后文演示中的启动、降温和产出变化，都来自这个模拟环境。</p>\n\n<p>这一节先了解两个服务的分工和使用顺序。具体的启动方式与接入配置，可以查看 <a href=\"https://github.com/rank-Yu/line-recovery-starter/blob/main/interfaces/README.md\">interfaces/README.md</a>。</p>\n\n<h3 id=\"c33-s10\">3.3 开发并启动 Agent 应用</h3>\n\n<p><strong>第 1 步：选择工作区</strong></p>\n\n<p>回到 Penguin Harness，新建对话，确认工作区选中 line-recovery-starter。</p>\n\n<p><img src=\"assets/penguin-harness/figure-12.png\" alt=\"图 12：选择 line-recovery-starter 目录作为开发工作区。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p><strong>第 2 步：选择开发技能</strong></p>\n\n<p>打开输入框下方的“技能”菜单，选择 agent-initialization 技能。</p>\n\n<p><img src=\"assets/penguin-harness/figure-13.png\" alt=\"图 13：在技能菜单中选择 agent-initialization。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p><strong>第 3 步：发送开发指令</strong></p>\n\n<p>接着，将 README 中的开发指令发送给 Penguin Harness，让它根据仓库中的已有材料开发产线巡检 Agent 与配套网页。</p>\n\n<blockquote>\n<p>请使用 agent-initialization，先读 README.md，再看 contracts/、interfaces/ 和 examples/input/，在 app/ 制作产线恢复助手，包括 Agent、后端、中文前端和启动说明。按现有契约实现两个 MCP 的客户端；服务未交付时明确显示未连接，不伪造动作成功。用 example 联调输入读取和页面，保留初始版本，本轮不扩充数据、不做优化或正式评分。</p>\n</blockquote>\n\n<p><img src=\"assets/penguin-harness/figure-14.png\" alt=\"图 14：确认工作区、技能和模型后，发送完整开发指令。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p>收到指令后，Penguin Harness 开始读取材料，开发产线巡检 Agent 和配套网页。展开运行记录，可以查看具体的运行记录。</p>\n\n<p><img src=\"assets/penguin-harness/figure-15.png\" alt=\"图 15：Penguin Harness 开发过程中读取项目资料。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p><strong>第 4 步：查看开发结果</strong></p>\n\n<p>开发用时约 20 分钟，完成后 Penguin Harness 给出了以下交付说明：</p>\n\n<p><img src=\"assets/penguin-harness/figure-16.png\" alt=\"图 16：原项目的初始交付说明：应用框架先交付，设备接口随后继续接通。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p>本例已完成的应用保存在 <a href=\"https://github.com/lzh368/line-recovery/tree/1e1f1e33d2ea55feafa89ae22895623179f68cfd/app\">line-recovery 仓库的 app/ 目录</a>中，主要文件结构如下：</p>\n\n<pre class=\"plaintext\" data-language=\"text\"><code class=\"language-text\">app/\n├── agent/                 # Agent 的角色说明与技能\n│   ├── persona.md\n│   └── skills/\n├── src/                   # 后端、案例分析与工具调用\n├── public/                # 配套网页\n├── config/\n│   └── interfaces.json    # 两个 MCP 服务的连接配置\n├── package.json           # 项目依赖与启动命令\n└── README.md              # 安装、配置与启动说明</code></pre>\n\n<h3 id=\"c33-s11\">3.4 演示 Penguin Harness 开发的 Agent 应用</h3>\n\n<p>Penguin Harness 每次开发出的应用，页面和文件结构可能有所不同。下面使用我们已经开发好的产线恢复助手进行演示。</p>\n\n<p><strong>第 1 步：安装并初始化应用</strong></p>\n\n<p>准备好 Node.js 和 Python ，将 <a href=\"https://github.com/lzh368/line-recovery\">line-recovery 仓库</a>克隆到本地，切换到本文演示的版本，再安装并初始化应用：</p>\n\n<pre class=\"bash\" data-language=\"text\"><code class=\"language-text\">git clone https://github.com/lzh368/line-recovery.git\ncd line-recovery\ngit checkout 1e1f1e33d2ea55feafa89ae22895623179f68cfd\npython3 interfaces/manage.py init --profile power-return\ncd app\nnpm install\nnpm run setup\ncp -n .env.example .env</code></pre>\n\n<p><strong>第 2 步：配置模型并启动应用</strong></p>\n\n<p>在 app/.env 中填入 DEEPSEEK_API_KEY，随后在当前 app/ 目录启动应用：</p>\n\n<pre class=\"bash\" data-language=\"text\"><code class=\"language-text\">npm start</code></pre>\n\n<p>在浏览器打开终端显示的本地地址，即可进入产线恢复助手。详细配置见 <a href=\"https://github.com/lzh368/line-recovery/tree/1e1f1e33d2ea55feafa89ae22895623179f68cfd/app/README.md\">app/README.md</a>。下图为创空间演示版尚未载入资料时的初始界面。</p>\n\n<p><img src=\"assets/penguin-harness/figure-17.png\" alt=\"图 17：产线恢复助手初始界面。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p><strong>第 3 步：加载案例资料</strong></p>\n\n<p>下面我们以 lr_101 产线资料包为例，演示供电恢复后输送带仍未运行时的分析和恢复过程。所有设备操作都在模拟环境中进行。</p>\n\n<p>打开应用后，左侧是案例列表和“上传资料包”入口，右侧展示当前案例。下图是已载入示例数据包的首页，红框标出了资料上传入口。</p>\n\n<p><img src=\"assets/penguin-harness/figure-18.png\" alt=\"图 18：产线恢复助手首页，红框为资料上传入口。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p>本次使用<a href=\"https://github.com/lzh368/line-recovery/tree/1e1f1e33d2ea55feafa89ae22895623179f68cfd/data/dataset/optimization/lr_101\">产线巡检示例数据包（lr_101）</a>，包含模拟工位的图片、运行记录和日志。下面的截图与结果来自已开发完成应用对该案例的一次运行；另外，line-recovery-starter 仓库自带的示例数据包对应的也是 lr_001 数据包。</p>\n\n<p>点击“载入 example”，即可加载 lr_101 示例资料。也可以下载 <a href=\"https://github.com/lzh368/line-recovery/raw/refs/heads/main/docs/penguin-harness/attachments/lr_101.zip\">lr_101.zip</a>，在左侧选择文件，点击“上传并解析”，再从案例列表中选中它。</p>\n\n<p><strong>第 4 步：查看工位图片</strong></p>\n\n<p>加载后，点击右侧“工位图片”页签，查看工位和纸箱的分布。</p>\n\n<p><img src=\"assets/penguin-harness/figure-19.png\" alt=\"图 19：查看案例中的工位图片。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p><strong>第 5 步：查看时序数据</strong></p>\n\n<p>接着切换到“时序数据”页签，查看供电和带速的变化。如下图所示，供电已经恢复，但带速仍为零，产出计数也没有增加。</p>\n\n<p><img src=\"assets/penguin-harness/figure-20.png\" alt=\"图 20：供电已经恢复，但带速仍为零。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p><strong>第 6 步：发起诊断并查看证据</strong></p>\n\n<p>切换到“诊断与证据”页签，点击“开始诊断”；已有报告时，按钮显示为“重新诊断”。完成后，页面会展示 Agent 的分析结论和对应证据。</p>\n\n<p><img src=\"assets/penguin-harness/figure-21.png\" alt=\"图 21：Agent 的停机分析与对应证据。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p>从上图中可以看到，Agent 判断输送带因供电中断而停机。虽然供电已经恢复、驱动已经就绪，但设备没有收到新的运行请求，因此仍未启动。本次停机与温度无关，无需开启风机。</p>\n\n<p><strong>第 7 步：核查动作与反馈</strong></p>\n\n<p>诊断结束后，切换到“动作与反馈”页签，可以查看 Agent 的操作记录：通过 MCP 查询当前设备状态，确认可以启动后恢复输送，再读取带速和产出反馈。</p>\n\n<p><img src=\"assets/penguin-harness/figure-22.png\" alt=\"图 22：恢复输送后，带速和产出计数发生变化。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p>上图中的执行反馈显示，带速恢复到 0.397 m/s，出口累计计数从 1813 增至 1816，说明模拟输送带已经重新运行，并有新的纸箱通过。应用将这些结果和分析结论一起记录在报告中。</p>\n\n<h2 id=\"c33-s12\">4. 用 Penguin Harness 优化产线巡检 Agent</h2>\n\n<p>上一章演示了一个案例的处理过程。接下来，我们让 Penguin Harness 用更多案例评测原版应用，根据发现的问题进行优化。下图展示了从原版应用到优化后应用的过程：</p>\n\n<p><img src=\"assets/penguin-harness/figure-23.png\" alt=\"图 23：Penguin Harness 评测与优化 Agent 应用。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<h3 id=\"c33-s13\">4.1 准备并发起评测与优化任务</h3>\n\n<p><strong>第 1 步：选择评测工作区</strong></p>\n\n<p>在 Penguin Harness 中新建对话，选择上一节已克隆到本地的 <a href=\"https://github.com/lzh368/line-recovery/tree/1e1f1e33d2ea55feafa89ae22895623179f68cfd\">line-recovery 仓库</a>作为工作区，再点击“使用此目录”，如下图所示。</p>\n\n<p><img src=\"assets/penguin-harness/figure-24.png\" alt=\"图 24：选择 line-recovery 根目录，点击“使用此目录”。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p><strong>第 2 步：确认数据集与评分方式</strong></p>\n\n<p>本次任务使用 <a href=\"https://github.com/lzh368/line-recovery/tree/1e1f1e33d2ea55feafa89ae22895623179f68cfd/data/dataset/optimization\">10 份案例</a>作为训练集，用于指导优化，另外用 <a href=\"https://github.com/lzh368/line-recovery/tree/1e1f1e33d2ea55feafa89ae22895623179f68cfd/data/gate-set-7\">7 份案例</a>作为测试集，检查优化后的表现。</p>\n\n<p>每份案例都准备了参考答案。评测时，对照答案检查 Agent 是否判断正确、操作得当，并取得预期结果，逐项评分后计算平均分。</p>\n\n<p><strong>第 3 步：选择评测与优化技能</strong></p>\n\n<p>我们选中 agent-evaluation 和 agent-optimization 技能，如下图所示。前者负责组织评测，后者根据评测结果优化 Agent。</p>\n\n<p><img src=\"assets/penguin-harness/figure-25.png\" alt=\"图 25：选中 agent-evaluation 和 agent-optimization，菜单中两项均显示勾选。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p><strong>第 4 步：填写并发送任务指令</strong></p>\n\n<p>接下来，将下面的指令填入输入框，让 Penguin Harness 评测初版、根据训练集中的问题修改 Agent，再用测试集检查效果。</p>\n\n<pre class=\"text\" data-language=\"text\"><code class=\"language-text\">使用 agent-evaluation 和 agent-optimization，先读 data/dataset/evaluator/README.md 和 data/gate-set-7/README.md。用 example 联调工具与反馈，保存代码和完整 Agent State 为 v1。评测 10 份 optimization 案例和 gate-set-7 七例，封存测试结果。只依据 optimization 结果和 trace 优化一轮；冻结 v2 后复测相同测试，测试材料不用于优化。每例 runs=1，独立设备库和 State，模型、预算及评分规则一致。将版本、报告、工具记录和成绩存入 reports/experiments/ 新目录，不覆盖已有记录，给出前后对比与路径。</code></pre>\n\n<p><img src=\"assets/penguin-harness/figure-26.png\" alt=\"图 26：填好优化指令后的待发送状态；红框为发送按钮，本次截图未启动任务。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p>点击图中红框标出的发送按钮。展开运行记录，可以看到 Penguin Harness 读取评测说明和相关技能，准备评测与优化任务，如下图所示。</p>\n\n<p><img src=\"assets/penguin-harness/figure-27.png\" alt=\"图 27：评测与优化任务启动后的工具调用记录。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p><strong>第 5 步：查看报告位置</strong></p>\n\n<p>任务完成后，Penguin Harness 会给出报告的保存位置。下面结合仓库中已保存的完整实验报告，查看优化结果。</p>\n\n<h3 id=\"c33-s14\">4.2 查看优化结果</h3>\n\n<p>我们在 <a href=\"https://github.com/lzh368/line-recovery/tree/1e1f1e33d2ea55feafa89ae22895623179f68cfd/reports/experiments\">line-recovery 仓库</a>中保存了已完成的评测与优化报告，下面看看优化后的表现。</p>\n\n<p>如下图所示，训练集平均分从 69.23 提高到 100.00，七例测试的历史汇总均分从 86.65 提高到 98.57。</p>\n\n<p><img src=\"assets/penguin-harness/figure-28.png\" alt=\"图 28：优化前后成绩。训练集来自 round1；七例测试来自 gate-set-7，先取每例历史运行均分，再对七例求平均，各案例和版本的运行次数不完全相同。\" class=\"article-image\" loading=\"lazy\" decoding=\"async\"></p>\n\n<p>从 <a href=\"https://github.com/lzh368/line-recovery/tree/1e1f1e33d2ea55feafa89ae22895623179f68cfd/reports/experiments\">line-recovery 仓库中已保存的报告</a>可以看到，在设备状态不明确的案例中，优化后的 Agent 不再贸然请求操作；在过热停机的案例中，它能在降温后继续恢复输送，并确认产出是否恢复。因此，评测分数提升和具体案例分析两方面都说明，Penguin Harness 优化后的产线巡检 Agent，在异常判断和恢复操作上都有了更好的表现。</p>\n\n<h2 id=\"c33-s15\">5. 投入与运行成本</h2>\n\n<p>除了任务表现，开发和使用 Agent 的成本也是落地时需要考虑的问题。下面分别介绍开发与优化阶段、日常运行阶段的成本。</p>\n\n<h3 id=\"c33-s16\">5.1 Agent 开发与优化的成本</h3>\n\n<p>在开发阶段，Penguin Harness 可以承担应用生成、工具联调和批量评测等工作，使用者主要负责提供需求与示例，并核查交付结果。以这类应用的开发和一轮优化为例，使用 DeepSeek Flash 的模型调用费用约为 5～20 元。</p>\n\n<div class=\"table-scroll\"><table>\n<thead>\n<tr class=\"header\">\n<th>投入项目</th>\n<th>人工开发与调试</th>\n<th>使用 Penguin Harness</th>\n</tr>\n</thead>\n<tbody>\n<tr class=\"odd\">\n<td>开发与一轮优化</td>\n<td>开发、联调和评测的人工费用</td>\n<td>使用 DeepSeek Flash，模型调用费用约 <strong>5～20 元</strong></td>\n</tr>\n<tr class=\"even\">\n<td>配套投入</td>\n<td>需求梳理、环境配置、测试验收</td>\n<td>提供需求与示例、核查结果</td>\n</tr>\n</tbody>\n</table>\n</div>\n<h3 id=\"c33-s17\">5.2 单次巡检任务的运行成本</h3>\n\n<p>Agent 投入使用后，每处理一份案例都会产生相应的模型调用费用。按单次任务累计输入 10 万 Token、输出 1 万 Token 估算，使用 DeepSeek Flash 的模型调用费用约为 0.15～0.30 元。</p>\n\n<div class=\"table-scroll\"><table>\n<thead>\n<tr class=\"header\">\n<th>指标</th>\n<th>人工处理</th>\n<th>Agent 处理</th>\n</tr>\n</thead>\n<tbody>\n<tr class=\"odd\">\n<td>单次直接费用</td>\n<td>处理工时 × 人力单价</td>\n<td>模型调用费约 <strong>0.15～0.30 元/次（估算）</strong></td>\n</tr>\n<tr class=\"even\">\n<td>其他成本</td>\n<td>管理、工具等费用</td>\n<td>服务器、维护及人工复核费用</td>\n</tr>\n</tbody>\n</table>\n</div>\n<h2 id=\"c33-s18\">6. 总结</h2>\n\n<p>本文以产线恢复助手为例，演示了如何用 Penguin Harness 开发 Agent 和配套网页，并通过评测和优化改善 Agent 的实际业务表现。对于其他业务场景，也可以先准备明确的需求、示例数据和工具接口，让 Penguin Harness 完成应用开发，再用实际案例检查效果、逐步改进。</p>\n\n<hr>\n\n<p>可以在魔搭创空间体验本文的两个演示：</p>\n\n<ul>\n<li><a href=\"https://modelscope.cn/studios/rankyu/line-recovery\">Penguin Harness 演示</a>：体验用 Penguin Harness 开发 Agent 应用。</li>\n<li><a href=\"https://modelscope.cn/studios/rankyu/line-recovery-app\">产线恢复助手演示</a>：体验已开发完成的 Agent 应用。</li>\n</ul>\n\n<p>请勿在公用创空间中输入或保存私人 API Key。如需使用自己的 API Key，请先将空间复制到自己的账号下，设置为非公开后再使用。</p>\n\n","imageCount":28,"attachmentCount":0,"discussionId":"penguin-harness-line-inspection"},{"id":"chapter-34","number":34,"title":"补充：大模型基础知识","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-34.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-34.html","minutes":31,"status":"ready","headings":[{"id":"c34-s1","text":"18.1　Transformer 基本结构","level":"h2"},{"id":"c34-s2","text":"18.2　Token：模型处理文本的基本单位","level":"h2"},{"id":"c34-s3","text":"18.3　Embedding：把内容转换为向量","level":"h2"},{"id":"c34-s4","text":"18.4　Attention：计算输入之间的关联","level":"h2"},{"id":"c34-s5","text":"18.4.1　从 Q、K、V 理解注意力","level":"h3"},{"id":"c34-s6","text":"18.4.2　多头注意力（MHA）","level":"h3"},{"id":"c34-s7","text":"18.4.3　多查询注意力（MQA）","level":"h3"},{"id":"c34-s8","text":"18.4.4　分组查询注意力（GQA）","level":"h3"},{"id":"c34-s9","text":"18.4.5　滑动窗口注意力（SWA）","level":"h3"},{"id":"c34-s10","text":"18.4.6　多头潜在注意力（MLA）","level":"h3"},{"id":"c34-s11","text":"18.4.7　DeepSeek 稀疏注意力（DSA）","level":"h3"},{"id":"c34-s12","text":"18.4.8　MiniMax 稀疏注意力（MSA）","level":"h3"},{"id":"c34-s13","text":"18.4.9　Qwen 稀疏注意力（QSA）","level":"h3"},{"id":"c34-s14","text":"18.4.10　Kimi Delta Attention（KDA）","level":"h3"},{"id":"c34-s15","text":"18.4.11　压缩稀疏注意力（CSA）","level":"h3"},{"id":"c34-s16","text":"18.4.12　高度压缩注意力（HCA）","level":"h3"},{"id":"c34-s17","text":"18.5　预训练：从大规模数据中学习基础规律","level":"h2"},{"id":"c34-s18","text":"18.6　指令微调：让模型学会按照要求完成任务","level":"h2"},{"id":"c34-s19","text":"18.7　后训练：继续调整模型行为","level":"h2"},{"id":"c34-s20","text":" 18.8　上下文长度：模型一次能够处理多少内容","level":"h2"},{"id":"c34-s21","text":"18.9　KV Cache：长文本生成中的显存占用","level":"h2"},{"id":"c34-s22","text":"18.10   参数规模、量化与硬件","level":"h2"},{"id":"c34-s23","text":"18.11　领域模型：让通用模型适应专业场景","level":"h2"},{"id":"c34-s24","text":"18.13　模型幻觉：看似合理但缺少依据","level":"h2"},{"id":"c34-s25","text":"18.13.1　常见幻觉类型","level":"h3"},{"id":"c34-s26","text":"18.13.2　怎样降低幻觉风险","level":"h3"}],"html":"<p>第一次准备下载大模型时，模型页面上的 7B、32B、128K 和 INT4 很容易让人困惑：7B 表示模型有多大，128K 是不是可以直接放入一本书，INT4 为什么能让模型占用更少显存？</p>\n\n<p>真正开始使用以后，还会遇到会议记录放不下、长对话忘记前文、模型回答没有依据、同一个模型换种量化方式效果发生变化等问题。</p>\n\n<p>要判断一个模型适不适合当前任务，需要先了解文字怎样进入模型、模型的能力怎样训练出来，以及模型运行时为什么会占用内存和显存。</p>\n\n<p>这些知识可以分成三条主线，第一条从 Transformer 总体结构出发，再看 Token、Embedding 和 Attention，说明一段文字怎样进入模型并被处理；第二条是预训练、指令微调和后训练，说明模型的能力怎样形成；第三条是上下文长度、KV Cache、量化和硬件，决定模型能不能在目标设备上稳定运行。</p>\n\n<p>本章内容是对前面相关章节内容的基础知识提炼，例如本次所提及的模型推理相关例如可以参考第六章和第七章的内容；针对模型 SFT 可以参考第九章所介绍的魔搭自带的 ms-swift 微调框架；针对后训练相关内容，也可以参考第十章中介绍的关于偏好对齐与强化学习的内容。针对领域模型的应用和幻觉问题等，也可以参考第十三章介绍的 RAG 方法，利用外部知识即提升大模型在领域类的问答下效果也能进一步降低模型的幻觉。</p>\n\n<h2 id=\"c34-s1\"><b>18.1　Transformer 基本结构</b></h2>\n\n<p>Transformer 是目前大多数大型语言模型采用的基础架构。与早期循环神经网络按顺序处理文本不同，Transformer 在训练和输入处理阶段可以并行计算多个位置，并通过 Attention 建立不同位置之间的联系。经典 Transformer 由编码器和解码器组成。每个 Transformer 层通常包含 Attention 和前馈网络。前馈网络也叫 FFN 或 MLP，它会对每个位置的向量进行非线性变换。层中还会使用残差连接和层归一化，帮助信息稳定地通过深层网络。大量相似层堆叠起来后，模型才能逐步形成复杂的文本表示和生成能力。Transformer 的整体架构如下图所示，图中左侧是编码器（Encoder），右侧是解码器（Decoder）。图中的<code>N×</code>表示同类模块会重复堆叠多层，实际层数会根据模型架构和参数规模变化。</p>\n\n<img src=\"assets/manuscript-20260914/c31-5a97057c9d1670.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>在模型进行推理时，当用户输入一段文字后，模型内部会经过下面的处理过程：大模型在处理文本时，首先会通过 Tokenizer 将原始文字切分成一个个 Token，并转换成对应的 Token ID；随后，Embedding 层会把这些离散的 Token ID 映射成连续的向量表示，同时加入位置信息，让模型知道各个 Token 在句子中的先后顺序。接下来，这些向量会经过多层 Transformer 反复处理，其中 Attention 机制负责让不同 Token 之间相互关注、交换上下文信息，前馈网络则进一步加工每个位置上的特征。经过多层计算后，模型会得到对当前上下文的综合表示，并据此计算下一个 Token 的概率分布，选择或采样出下一个 Token，再将其加入上下文继续重复这一过程，直到完成整段内容的生成。</p>\n\n<p>原始 Transformer 中的三个部分各有不同作用：</p>\n\n<p>- <b>编码器（Encoder-only）结构：</b>主要负责理解输入，可以同时参考输入序列中的不同位置，适合分类、抽取和语义表示等任务；</p>\n\n<p>- <b>解码器（Decoder-only）结构：</b>主要负责生成输出，生成当前位置时只能查看已经出现的内容；</p>\n\n<p>- <b>编码器—解码器（Encoder-Decoder）结构：</b>先理解输入，再逐步生成输出，常见于翻译和摘要等序列转换任务。</p>\n\n<p>现代大模型不一定同时使用完整的编码器和解码器。在此基础之上，衍生了基于 Transformer 多种架构，比如，GPT 类生成模型通常采用 Decoder-only 结构，BERT 类模型主要使用 Encoder-only，T5 类模型则保留 Encoder-Decoder 结构。</p>\n\n<h2 id=\"c34-s2\"><b>18.2　Token：模型处理文本的基本单位</b></h2>\n\n<p>词元（Token）是模型读取和生成文本时使用的基本单位。它可能是一个汉字、一个词、词的一部分、标点符号，也可能是空格或特殊控制标记。例如，同一句话在不同 Tokenizer 中可能得到不同结果：</p>\n\n<pre data-language=\"Plain Text\"><code>原文：大型语言模型正在改变办公方式。\n\n分词器A：大型 / 语言 / 模型 / 正在 / 改变 / 办公 / 方式 / 。\n分词器B：大 / 型 / 语言 / 模型 / 正 / 在 / 改变 / 办公 / 方式 / 。</code></pre>\n\n<p>两个结果表达的是同一句话，但 Token 数量并不相同。模型实际接收的是每个 Token 对应的数字编号，也就是 Token ID。以整理会议纪要为例，模型看到的不是一篇完整的会议记录，而是一串 Token ID。会议名称、发言内容、时间、标点和输出要求都会占用 Token；模型生成的摘要、结论和行动项同样会继续占用 Token。因此，同一份材料要求输出得越详细，留给原始记录的空间就可能越少。</p>\n\n<p>如果完全按字符切分，词表可以比较小，但序列会变长；如果完全按完整单词切分，词表会非常庞大，还会不断遇到新词、缩写和拼写变化。大模型通常采用子词分词，在字符和完整单词之间寻找平衡。BPE、WordPiece、Unigram 和 SentencePiece 都是常见方法。</p>\n\n<p></p>\n\n<h2 id=\"c34-s3\"><b>18.3　Embedding：把内容转换为向量</b></h2>\n\n<p>通常，模型不能直接对文字或图片进行矩阵运算，需要先把内容转换为数字向量。这个过程称为叫嵌入或向量表示（Embedding）。对于文本，分词器（Tokenizer）先把文字转换为 Token ID，Embedding 层再根据 ID 找到对应向量。向量中的单个数通常没有直观含义，但整个向量可以表示该 Token 在模型内部的特征。以 Token 为“会议”为例，对应的 Token ID 可以是某个整数编号，例如“15872”，而该 Token 对应的表征即 Embedding，可以是：[0.12, -0.37, 0.08, ……]，这样一组向量。Embedding 会在训练过程中不断调整，出现在相似上下文中的 Token，其向量往往会形成某些相近特征。由于模型结构和训练的过程不同，同一个词经过多层 Transformer 处理后，还会根据上下文形成不同表示。</p>\n\n<p>在模型获取输入文本的语义信息时，不仅要知道 Token 是什么，还要知道它位于什么位置。仅有 Token Embedding 时，例如“会议延期”和“延期会议”包含相似 Token，但由于语序差异，却表达不同结含义。因此，模型还需要加入位置编码或位置表示。下图以 BERT 的输入层为例。</p>\n\n<img src=\"assets/manuscript-20260914/c31-b953381f9bd0aa.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>如上图 BERT 中所采用的表征方式所示，展示了 Token Embedding、句段 Embedding 和位置 Embedding 的组合方式。当然，不同模型结构和设计方式，采用的具体方案并不完全相同。以多模态大模型为例，图片、音频等内容也要先转换为向量。图片通常先被切成图像块，由视觉编码器提取特征；音频会先转换为声学特征，再由音频编码器处理。不同模态的特征经过投影或连接模块（Projection）后，才能进入大模型能够处理的向量空间。如下图所示，通过连接模块讲图像信息与语义信息进行融合。</p>\n\n<img src=\"assets/manuscript-20260914/c31-7405b55ce19469.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<h2 id=\"c34-s4\"><b>18.4　Attention：计算输入之间的关联</b></h2>\n\n<p>Attention 负责判断当前位置应该重点参考输入中的哪些内容。它会在模型已经形成的向量表示上计算关联程度，而不只是查找相同词语。例如，在“项目延期了，报告解释了它为什么没有按期完成”这句话中，模型处理代词“它”时，需要结合上下文判断它更可能指向“项目”，还是“报告”。在会议记录中提取负责人时，模型也需要把任务描述、人员姓名和表示分工的句子联系起来。</p>\n\n<p></p>\n\n<h3 id=\"c34-s5\"><b>18.4.1　从 Q、K、V 理解注意力</b></h3>\n\n<p></p>\n\n<p>每个输入向量会经过不同变换，形成 Query、Key 和 Value，通常简写为 Q、K 和 V。Attention 的计算公式可以简化写成：</p>\n\n<p></p>\n\n<p><span class=\"arithmatex\">\\(\\mathrm{Attention}(Q, K, V)=\\mathrm{softmax}\\left(\\frac{QK^{\\mathrm{T}}}{\\sqrt{d_k}}\\right)V\\)</span></p>\n\n<p>其中，Q 表示当前位置正在寻找什么信息，K 表示每个位置可以用什么特征被匹配，V 表示匹配后需要取回的内容。模型先比较 Q 和 K，得到当前位置与其他位置之间的相关分数；再通过 softmax 把分数转换为权重，按照权重对 V 进行加权汇总。公式中的<span class=\"arithmatex\">\\(d_k\\)</span>是 Key 向量的维度，除以<span class=\"arithmatex\">\\(\\sqrt{d_k}\\)</span>用于调整分数尺度，使计算更加稳定。</p>\n\n<p>如果 Q、K、V 来自同一段序列，就称为自注意力（Self-Attention），用于建立序列内部的联系；如果 Q 来自一段序列，而 K、V 来自另一段序列，就称为交叉注意力（Cross-Attention）。例如，编码器—解码器翻译模型可以让解码器通过交叉注意力读取编码器提供的原文信息。</p>\n\n<p>对于生成式大模型，当前位置只能参考已经出现的内容，因此 Attention 中还需要加入因果掩码，屏蔽后续位置。即使屏蔽了未来 Token，完整因果注意力处理的关联数量仍会随序列长度近似按平方增长。同时，生成阶段还要保存历史 Token 的 K 和 V，形成后文介绍的 KV Cache。随着上下文变长，Attention 的计算和缓存开销会越来越突出，下面介绍的不同结构正是围绕这些问题逐步发展起来的。</p>\n\n<p></p>\n\n<h3 id=\"c34-s6\"><b>18.4.2　多头注意力（MHA）</b></h3>\n\n<p></p>\n\n<p>多头注意力（Multi-Head Attention，MHA）由 2017 年的 Transformer 论文《Attention Is All You Need》提出，后来应用于 BERT 以及 Llama 2 7B、13B 等模型。它在单组注意力计算的基础上增加多个并行的注意力头，使模型能够从不同的表示空间提取上下文关系。</p>\n\n<p>在 MHA 中，每个头都有各自生成 Q、K、V 的投影参数，分别计算注意力，再把各头的结果连接起来，通过线性层形成输出。如下图所示，左侧展示一组缩放点积注意力的计算过程，右侧把这一过程并行执行多次，再进行结果融合。不同头的关注模式由训练形成，可以共同支持指代判断、语义关联和远距离依赖等任务。</p>\n\n<img src=\"assets/manuscript-20260914/c31-dd04e51bfbe004.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>MHA 提升了模型聚合不同类型信息的能力，也便于在训练中并行计算。不过，在逐 Token 生成时，各个头都要读取自己的历史 K、V。模型层数、头数和上下文长度增加后，这部分缓存和数据读取会占用大量资源。因此，后续改进首先考虑能否保留多个 Query 头，同时减少需要保存的 Key 头和 Value 头。</p>\n\n<p></p>\n\n<h3 id=\"c34-s7\"><b>18.4.3　多查询注意力（MQA）</b></h3>\n\n<p>多查询注意力（Multi-Query Attention，MQA）由 Noam Shazeer 在 2019 年的论文《Fast Transformer Decoding: One Write-Head is All You Need》中提出，后来应用于 Falcon-7B 等大模型。它主要针对解码阶段的内存带宽问题：模型生成每一个新 Token 时，都需要读取已有的 KV Cache，读取量过大时，即使计算单元仍有余量，生成速度也会受到限制。</p>\n\n<p>MQA 保留多个 Query 头，但让它们共享一组 Key 和 Value。这样，不同头仍然可以生成不同的查询，历史位置却只需要保存一份供各头共用的 K、V。下图展示了将已有 MHA 模型转换为 MQA 时的一种初始化方法：把多个 Key 投影矩阵做平均，得到一个共享投影，Value 投影也可以采用同样处理；转换后还要继续训练，使模型适应新的共享结构。</p>\n\n<img src=\"assets/manuscript-20260914/c31-b12533e5299d38.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>采用这一结构后，MQA 可以明显减少 KV Cache 以及每步解码的数据读取量，为较长输入或较多并发请求腾出空间。例如，Falcon-7B 配置了 71 个 Query 头，但只有 1 个 KV 头。相对于相同头维度、相同精度的 71 组 KV 结构，其理论 KV 缓存只需约七十一分之一。共享也会约束 K、V 的表示容量，因此需要通过训练和评测确认这种效率收益对任务质量的影响。</p>\n\n<p></p>\n\n<h3 id=\"c34-s8\"><b>18.4.4　分组查询注意力（GQA）</b></h3>\n\n<p></p>\n\n<p>分组查询注意力（Grouped-Query Attention，GQA）由 Google Research 团队在 2023 年的 GQA 论文中系统提出，并应用于 Llama 2 70B、Mistral 7B 等大模型。它希望在 MHA 的多组表示能力和 MQA 的低缓存开销之间取得平衡，避免所有 Query 头都只能使用同一组 K、V。</p>\n\n<p>GQA 把 Query 头划分为若干组，每组共享一组 Key 和 Value，不同组保留不同的 K、V 表示。如下图所示，MHA 为每个 Query 头配置对应的 K、V，MQA 让所有 Query 头共享一组 K、V，GQA 则在组内共享。当组数等于 Query 头数时，GQA 等价于 MHA；当只有一组时，就等价于 MQA。</p>\n\n<img src=\"assets/manuscript-20260914/c31-a33c19025f4f45.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>以 Mistral 7B v0.1 为例，它有 32 个 Query 头和 8 个 KV 头，每 4 个 Query 头共享一组 K、V。在头维度和精度相同的情况下，这部分缓存约为 MHA 的四分之一。对实际部署而言，减少缓存和读取量通常有利于提高并发能力与解码效率；由于不同组仍保留各自的 K、V，GQA 也比完全共享的 MQA 保留了更多表示空间。这里减少的是 KV 头数，模型仍然可以对允许访问的全部历史位置计算注意力。</p>\n\n<p></p>\n\n<h3 id=\"c34-s9\"><b>18.4.5　滑动窗口注意力（SWA）</b></h3>\n\n<p></p>\n\n<p>滑动窗口注意力（Sliding Window Attention，SWA）来自长序列建模中的局部注意力思路，Mistral 7B v0.1 将它与 GQA 结合使用。GQA 减少了每个位置需要保存的 KV 头数，SWA 则进一步限制每个位置直接关注的历史范围，以降低长文本的计算和缓存压力。</p>\n\n<p>SWA 为每个 Token 设置固定大小的窗口，只对窗口内的近期位置计算注意力。下图左侧是完整因果注意力，中间是滑动窗口注意力，右侧说明信息如何通过多层网络向后传递。虽然单层只读取局部范围，但上一层的 Token 表示已经包含更早的上下文信息，因此模型经过多层处理后，可以间接利用窗口以外的内容。</p>\n\n<img src=\"assets/manuscript-20260914/c31-5586222f5a5ecb.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>Mistral 7B v0.1 采用 4096 个 Token 的窗口，并使用滚动缓存覆盖已经移出窗口的旧 K、V，使各层缓存可以维持在固定范围。论文在 16K 序列、4096 窗口的测试中报告，经过相应内核优化后，速度约为完整注意力基线的 2 倍。这种结构能够控制长文本的资源增长，但远处的原始细节需要经过中间表示传递，因此局部窗口大小并不能直接等同于模型能够准确检索的上下文长度。</p>\n\n<p></p>\n\n<h3 id=\"c34-s10\"><b>18.4.6　多头潜在注意力（MLA）</b></h3>\n\n<p></p>\n\n<p>多头潜在注意力（Multi-head Latent Attention，MLA）由 DeepSeek 团队在 DeepSeek-V2 中提出，随后继续用于 DeepSeek-V3。它同样针对 KV Cache 过大的问题，但进一步改变了缓存的保存形式：通过学习一个紧凑的联合表示，减少每个 Token 需要保存的数据，同时支持多个注意力头使用这些信息。</p>\n\n<p>具体来说，MLA 先用低秩投影把 K 和 V 联合表示为较低维度的潜在向量，推理时主要缓存这一向量，以及单独承载旋转位置信息的 Key 分量。多个 Query 头可以利用这个共享表示完成匹配和信息聚合，部分投影矩阵还可以在推理时合并，减少显式展开完整 K、V 的操作。下图中的斜线区域表示需要缓存的内容，可以看到 MLA 将多头 K、V 的缓存转移到了右侧较小的潜在表示中。</p>\n\n<img src=\"assets/manuscript-20260914/c31-ca9b169844f044.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>这种设计让 DeepSeek-V2、V3 能够在保留多头表达能力的同时，降低长上下文生成中的缓存压力。DeepSeek-V2 报告中，MLA 每 Token 的 KV 缓存规模相当于只有 2.25 组 KV 头的 GQA。它压缩的是每个位置的表示维度，历史位置本身仍然保留，因此在完整注意力模式下，模型依然需要读取并匹配全部历史条目。进一步减少每次实际参与计算的条目，就需要引入稀疏注意力。</p>\n\n<p></p>\n\n<h3 id=\"c34-s11\"><b>18.4.7　DeepSeek 稀疏注意力（DSA）</b></h3>\n\n<p></p>\n\n<p>DeepSeek 稀疏注意力（DeepSeek Sparse Attention，DSA）由 DeepSeek 团队提出，应用于 DeepSeek-V3.2-Exp 和 DeepSeek-V3.2。它在 MLA 基础上增加动态选择机制，解决长上下文中即使单个 KV 条目已经较小，读取和计算全部历史条目仍然昂贵的问题。</p>\n\n<p>DSA 先通过轻量索引器 Lightning Indexer 计算当前 Query 与历史 Token 的相关分数，再取分数最高的 Top-k 个位置，交给主 Attention 进行计算。下图中的绿色部分是新增的索引过程，Top-k Selector 负责选择位置，上方的核心 Attention 则读取所选位置对应的 MLA 缓存。索引器使用较小的表示和较少的头完成筛选，其计算成本低于直接执行完整的主 Attention。</p>\n\n<img src=\"assets/manuscript-20260914/c31-d111bded53c358.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>DeepSeek-V3.2 报告中的配置是每个 Query 最多选择 2048 个 KV 条目。随着上下文增长，核心 Attention 仍可以集中处理这一有限范围，因而明显降低长输入处理和后续生成的成本。未被本次选中的历史条目仍可供后续 Query 检索，所以稀疏选择主要减少的是本次读取和计算量，而不是按相同比例删除全部缓存。索引器本身仍需处理历史候选，实际开销也会受到上下文长度影响。</p>\n\n<p></p>\n\n<h3 id=\"c34-s12\"><b>18.4.8　MiniMax 稀疏注意力（MSA）</b></h3>\n\n<p>MiniMax 稀疏注意力（MiniMax Sparse Attention，MSA）由 MiniMax 团队提出，应用于 MiniMax-M3。它面向百万 Token 上下文中的效率问题，特别考虑了如何让稀疏注意力适合 GPU 批量计算，而不仅是在理论上减少参与计算的 Token。</p>\n\n<p>MSA 建立在 GQA 基础上，包含索引分支和主分支。索引分支先计算 Token 级相关分数，再取每个块内的最高分数作为块分数，为不同 GQA 组分别选择 Top-k 个历史块。主分支随后读取选中块中满足因果条件的 Token 级 K、V，同时保留当前所在的局部块。如下图所示，右侧两个 Query 组可以形成不同的选择范围，同组内的 Query 头则共享选择结果。</p>\n\n<img src=\"assets/manuscript-20260914/c31-e890dd80a296d1.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>按块组织访问可以提高 GPU 处理数据的规则性，而分组选择使不同组能够保留不同的检索模式。在 109B 参数实验模型的 1M 上下文测试中，论文报告 MSA 的每 Token 注意力计算量约为 GQA 的 1/28.4；配合专用内核，在 H800 上处理输入的 Prefill 阶段和逐 Token 生成的 Decode 阶段分别获得约 14.2 倍和 7.6 倍的速度提升。这说明稀疏算法与计算内核需要共同设计，减少计算量才能更充分地转化为实际速度收益。</p>\n\n<p></p>\n\n<h3 id=\"c34-s13\"><b>18.4.9　Qwen 稀疏注意力（QSA）</b></h3>\n\n<p>Qwen 稀疏注意力（Qwen Sparse Attention，QSA）由 Qwen 团队提出，并应用于 Qwen3.8-Flash-Next。该模型采用三层 Gated DeltaNet 搭配一层 QSA 的混合结构：前者通过状态更新高效处理序列，后者保留对历史 Token 的直接检索。QSA 重点处理的问题是，长上下文中的索引器自身也会产生较大开销。</p>\n\n<p>为此，QSA 先将连续多个 Token 的索引 Key 通过平均池化压缩成微块表示，在较短的微块序列上计算相关分数并选择 Top-k 块。选中后，再把块展开为原始 Token 位置，让核心 Attention 读取对应的 K、V；尚未组成完整块的尾部 Token 也会保留。下图左侧是压缩索引过程，右侧是根据选择结果进行的微块稀疏 Attention，两部分之间传递的是位置索引。</p>\n\n<img src=\"assets/manuscript-20260914/c31-346289a9285559.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>这样既减少了主 Attention 访问的位置，也缩短了索引器需要扫描的序列。Qwen3.8-Flash-Next 报告在 1M 上下文的内核测试中，QSA 相对稠密注意力的 Prefill 和 Decode 速度分别提升约 7.6 倍和 4.9 倍。理解这一结构时，需要区分索引表示与主 Attention 缓存：QSA 压缩前者来降低筛选成本，选中后仍读取 Token 级 KV，因此可以保留所选区域的细粒度信息。</p>\n\n<p></p>\n\n<h3 id=\"c34-s14\"><b>18.4.10　Kimi Delta Attention（KDA）</b></h3>\n\n<p></p>\n\n<p>Kimi Delta Attention（KDA）由 Moonshot AI 团队在 Kimi Linear 中提出，实际应用于 Kimi Linear 的 48B 总参数、3B 激活参数模型。它采用线性注意力思路，把历史信息持续写入固定大小的状态，减少每一步生成时反复读取长历史序列的开销。</p>\n\n<p></p>\n\n<p>KDA 在 Gated DeltaNet 的基础上引入更细粒度的门控。新 Token 到来时，模型先控制旧状态中不同通道的信息保留程度，再通过 Delta 规则修正已有的键值关联。这种更新可以理解为利用新输入与当前状态所预测内容之间的差异，调整状态中保存的信息。由于衰减门按通道变化，不同部分可以采用不同的保留速率。计算时还可以按块并行处理一段 Token，以提高 GPU 效率。</p>\n\n<img src=\"assets/manuscript-20260914/c31-bc3814a6007c94.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>上图展示了 Kimi Linear 将三层 KDA 与一层 MLA 交错使用的方式。KDA 层用固定大小的状态降低长序列成本，MLA 层则补充对具体历史位置的直接检索，缓解固定状态容量有限的问题。在论文采用相同训练方案的比较中，这一混合模型相对完整 MLA 基线最多减少 75%的 KV Cache，并在 1M 上下文下获得最高约 6 倍的解码吞吐量。这些效果对应 Kimi Linear 的混合架构，而不是所有 Kimi 型号的统一配置。</p>\n\n<p></p>\n\n<h3 id=\"c34-s15\"><b>18.4.11　压缩稀疏注意力（CSA）</b></h3>\n\n<p></p>\n\n<p>压缩稀疏注意力（Compressed Sparse Attention，CSA）由 DeepSeek 团队在 DeepSeek-V4 中提出，应用于 DeepSeek-V4-Pro 和 DeepSeek-V4-Flash。前面的 MLA 主要压缩每个 Token 的 KV 表示，CSA 进一步沿序列方向减少条目数量，将多个 Token 的信息汇聚到较少的 KV 条目中，再进行稀疏选择。</p>\n\n<p>CSA 先用可学习的 Token 级压缩器处理连续 Token，并结合相邻块的重叠信息，形成压缩 KV。接着，Lightning Indexer 为这些压缩条目打分，选择 Top-k 条目参与主 Attention。下图中，压缩器位于索引和选择之前，主 Attention 直接读取压缩后的 KV；左侧还有一条滑动窗口分支，把近期未压缩的 Token 一起送入计算，保留局部细节。</p>\n\n<img src=\"assets/manuscript-20260914/c31-0aac58ba333b88.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>DeepSeek-V4 报告中的 CSA 压缩率为 4，即历史 KV 条目数量约降至四分之一，再从中选择部分条目进行核心 Attention 计算。因此，CSA 既降低长期缓存量，也减少每次读取和计算的历史内容。QSA 压缩索引后会回到原始 Token，而 CSA 直接在压缩条目上完成信息聚合，因而对历史 KV 本身也进行了压缩。这种设计需要压缩器保留有用特征，并由局部窗口补充近期细节。</p>\n\n<p></p>\n\n<h3 id=\"c34-s16\"><b>18.4.12　高度压缩注意力（HCA）</b></h3>\n\n<p></p>\n\n<p>高度压缩注意力（Heavily Compressed Attention，HCA）同样由 DeepSeek 团队在 DeepSeek-V4 中提出，并与 CSA 在 DeepSeek-V4-Pro 和 Flash 的多层结构中交错使用。它采用更强的序列压缩，让模型以较少的缓存保留覆盖大范围历史的信息，与 CSA 的选择性读取形成补充。</p>\n\n<p>HCA 的压缩率在报告中设为 128，远高于 CSA 的 4。多个 Token 通过可学习的加权汇聚形成一个压缩条目后，当前 Query 会对全部因果可见的压缩历史执行 Attention，不再进行 Top-k 筛选。如下图所示，HCA 保留压缩分支和局部滑动窗口分支，但省去了 CSA 中的索引器和选择器。窗口提供近期 Token 的细节，压缩历史提供更大范围的上下文信息。</p>\n\n<img src=\"assets/manuscript-20260914/c31-db5fcb025aa2e7.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p></p>\n\n<p>CSA 与 HCA 交错配置，使 DeepSeek-V4 能够同时利用不同粒度的历史表示。报告中，在 1M 上下文下，DeepSeek-V4-Pro 的单 Token 推理 FLOPs 和 KV Cache 分别约为 DeepSeek-V3.2 的 27%和 10%。这是混合 Attention 及低精度计算、存储共同作用的整体效果。对使用者而言，这类结构的意义在于使超长上下文更容易在有限硬件上运行；具体模型能否准确提取长材料中的细节，仍取决于压缩方式、训练过程和实际任务。</p>\n\n<p>从这些模型的设计可以看出，Attention 的改进并不只有一种方向。MHA、MQA 和 GQA 主要改变头之间的共享关系，MLA 压缩每个位置的 KV 表示，DSA、MSA 和 QSA 减少核心 Attention 访问的位置，KDA 通过状态更新处理历史，而 CSA、HCA 进一步压缩历史条目。模型可以将其中多种方法组合使用，因此分析资源需求时，需要结合每种结构的层数和缓存形式。无论采用哪种结构，Attention 都只负责聚合上下文信息，模型的整体能力仍由它与 Embedding、前馈网络及训练过程共同形成。</p>\n\n<p></p>\n\n<h2 id=\"c34-s17\"><b>18.5　预训练：从大规模数据中学习基础规律</b></h2>\n\n<p>模型结构决定信息怎样计算，训练过程则决定模型最终学到什么。大模型的训练通常可以分为几个阶段。首先，预训练阶段，模型会在海量文本、代码等数据上学习语言规律、知识结构和基本的推理模式，获得通用能力；随后，进入指令微调（Supervised Fine-Tuning，SFT）阶段，通过大量“指令—回答”样本，让模型学会理解用户要求，并按照指定任务形式进行回答；在此基础上，还会进一步进行偏好优化或强化学习，根据人工反馈或规则对模型行为进行调整，使回答在有用性、准确性、表达风格、安全性以及行为边界等方面更加符合预期。</p>\n\n<p>预训练是模型形成基础能力的阶段。模型会在大规模文本、代码或多模态数据上反复训练。对于 Decoder-only 语言模型（比如 GPT 系列），常见训练任务是根据前面的内容预测下一个 Token，预测错误时再调整模型参数。海量样本不断重复后，模型逐渐学习语言结构、表达方式和不同概念之间的统计关系，并形成完成问答、摘要、翻译和代码生成等任务所需的基础表示与生成能力。</p>\n\n<p>模型在预训练中学到的知识分布在大量参数中，并不是一套可以逐条查询的数据库。它可以组合已经学到的规律，生成训练数据中没有原样出现过的内容，也可能把相关但不完全匹配的信息组合在一起，形成看似合理的错误答案。当然，训练数据也不是越多越好。重复内容、低质量网页、错误知识、隐私数据和有害内容都会影响模型。预训练前通常需要进行格式解析、去重、质量筛选、安全过滤和数据配比。数据规模决定模型能够接触多少内容，数据质量则直接影响模型能够从中学到什么。</p>\n\n<p></p>\n\n<h2 id=\"c34-s18\"><b>18.6　指令微调：让模型学会按照要求完成任务</b></h2>\n\n<p>完成预训练后，模型已经能够续写文本，但不一定会稳定地按照用户要求完成任务。例如，用户要求列出三个行动项，基础模型可能继续扩写问题，也可能忽略指定格式。指令微调会使用指令和期望答案组成的示例继续训练模型，以下是一个指令微调的例子：</p>\n\n<pre data-language=\"Plain Text\"><code>指令：把下面的会议记录整理成行动项。\n输入：会议记录正文。\n输出：包含任务、负责人和截止时间的表格。</code></pre>\n\n<p>经过问答、摘要、信息抽取、代码、工具调用和安全拒答等不同类型的示例训练，模型会逐渐学会识别用户意图并遵守输出要求。指令微调主要教模型怎样使用已有能力。它可以补充部分知识，但不适合解决所有知识更新问题。需要频繁更新或者必须提供来源的内容，更适合通过知识库、检索或外部工具在运行时提供。</p>\n\n<p></p>\n\n<h2 id=\"c34-s19\"><b>18.7　后训练：继续调整模型行为</b></h2>\n\n<p>后训练是模型完成预训练后进行的一系列训练和对齐工作的总称。指令微调通常属于后训练的一部分，进一步地指令微调（SFT）、偏好优化和强化学习也可能出现在这个阶段。因此，指令微调和后训练不是两个互相独立的步骤，前者是一类具体方法，后者是包含多种方法的阶段。几种训练方式可以先按下面的关系理解如下：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col><col></colgroup><tbody><tr><td><p>阶段或方法</p></td><td><p>主要使用的数据</p></td><td><p>主要解决的问题</p></td></tr><tr><td><p>预训练</p></td><td><p>大规模文本、代码和多模态数据</p></td><td><p>让模型学习语言和知识等基础规律</p></td></tr><tr><td><p>指令微调／SFT</p></td><td><p>指令与期望回答</p></td><td><p>让模型学会按照任务要求输出</p></td></tr><tr><td><p>偏好优化</p></td><td><p>同一问题下的较好回答和较差回答</p></td><td><p>让模型更倾向符合偏好的回答</p></td></tr><tr><td><p>强化学习</p></td><td><p>提示、模型回答和奖励信号</p></td><td><p>根据奖励继续调整生成策略</p></td></tr></tbody></table>\n</div>\n<p>参考 InstructGPT 中采用的基于人类反馈的强化学习（Reinforcement Learning from Human Feedback，RLHF）框架，目前已经行程了一条经典后训练路线。RLHF 通常第一步使用人工示范数据完成 SFT，第二步使用回答排序数据训练奖励模型，第三步根据奖励模型给出的分数，使用 PPO 优化语言模型的生成策略。具体过程如下图所示。</p>\n\n<img src=\"assets/manuscript-20260914/c31-0040f1515f396a.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>其中，奖励模型给出的分数代表一种偏好，不等于客观真理。如果偏好数据覆盖不足，或者奖励规则设计不合理，模型可能只是学会迎合评分方式。</p>\n\n<p>实际模型不一定采用完全相同的路线。有的模型使用 SFT 加 DPO，有的使用 SFT、奖励模型和 PPO，还有的会加入 AI 反馈、安全对齐、工具使用训练和多轮对话数据。前面的预训练、指令微调和后训练解释了模型能力怎样形成。模型训练完成以后，使用者更常遇到的问题是一次能输入多少内容、运行时需要多少显存，以及有限硬件应该选择什么精度。</p>\n\n<h2 id=\"c34-s20\"><b> 18.8　上下文长度：模型一次能够处理多少内容</b></h2>\n\n<p>模型完成训练后，就进入实际推理和部署阶段。上下文长度决定一次请求能够放入多少 Token；上下文越长，模型能够读取的材料越多，但 Attention 计算和 KV Cache 占用也会增加，进而影响推理速度和显存需求。这里所谓的上下文，即一次请求中的系统提示、对话历史、当前输入、文件内容、工具说明、工具返回结果和模型已经生成的内容，可以使用如下公式来表达：上下文占用= 系统指令 + 对话历史 + 当前输入+ 附件内容 + 工具信息 + 模型输出。</p>\n\n<p>正如上文所提及的，大模型的上下文占用并不只是用户当前输入的内容，而是由多部分共同构成，包括系统指令、此前的对话历史、用户当前输入、附件中的相关内容、工具调用所需的信息以及模型已经生成的输出等。这些内容都会占用模型的上下文窗口，因此随着对话变长、附件增多或工具信息变复杂，可用于后续输入和生成的上下文空间也会逐渐减少。比如，模型标注支持上下文长度为：128K，通常表示一次请求能够处理的 Token 总量级。具体是否包含输出、单次最大输出是多少，还要看模型和服务接口的限制。通常，超出上下文后，平台可能拒绝请求、截断部分内容，或者自动压缩历史记录。</p>\n\n<p>为了提升模型在长文档场景下的推理效率和回答质量，在输入材料前应尽量对内容进行整理，删除与任务无关、重复或价值较低的信息，并明确告诉模型需要重点关注的章节、字段或问题范围，避免模型在大量无关上下文中分散注意力。对于篇幅特别长、一次难以完整处理的材料，可以先按照章节、主题或固定长度进行分段，让模型分别完成信息提取、摘要或分析，再对各部分结果进行统一汇总和综合判断。对于关键结论、重要数据或需要进一步核验的内容，还应要求模型同时标明对应的原文位置、章节或页码，便于后续人工检查和结果追溯，从而提高整体处理过程的准确性、可解释性和可靠性。</p>\n\n<p></p>\n\n<h2 id=\"c34-s21\"><b>18.9　KV Cache：长文本生成中的显存占用</b></h2>\n\n<p>基于 Transformer 架构的生成式模型，通常会逐个生成 Token，在生成的构成中，如果每生成一个 Token 都重新计算此前全部 Token 的注意力信息，会产生大量重复工作，因此不妨引入一套缓存机制，即 KV Cache。KV Cache 会保存各层已经计算过的 Key 和 Value，生成下一个 Token 时只需要计算新增部分，从而提高生成速度。每增加一个 Token，各个 Transformer 层都要保存对应的 K 和 V。因此，KV Cache 会随着上下文长度、输出长度、并发数量和模型层数增加。</p>\n\n<p>对于常见 Decoder-only 模型，可以用下面的简化公式理解单条序列的缓存占用，可以用以下公式表示：</p>\n\n<p><span class=\"arithmatex display-math\">\\(\\text{KV Cache 占用}\n\\approx\n2 \\times \\text{层数}\n\\times \\text{Token 数}\n\\times \\text{KV 头数}\n\\times \\text{每个头的维度}\n\\times \\text{每个数的字节数}\n\\)</span></p>\n\n<p>模型权重加载完成后占用相对固定，KV Cache 却会随请求变化，因此，同一个量化模型在短问答中可以顺利运行，换成长文档或者多人并发后仍可能显存不足。例如，同一台服务器只处理一份短会议记录时可能运行正常，如果同时让十名用户上传长记录后，模型需要为多条序列分别保存 KV Cache，显存压力会明显增加。这也解释了，即使模型文件能够放进显存，随着输入的文本长度和并发规模的增大，算力资源的消耗也在发生变化。因此，在实际部署时，可以通过缩短无关上下文、限制最大生成长度、降低并发量、使用分页缓存，或者选择采用 GQA、MQA 结构的模型降低 KV Cache 压力，提升算力服务的效率。</p>\n\n<p></p>\n\n<h2 id=\"c34-s22\"><b>18.10   参数规模、量化与硬件</b></h2>\n\n<p>模型文件实际是保存当前模型结构即即相关参数，这些参数本质上是一组数值。结合计算机组成原理中的相关只是，这些参数通常在计算机中是一堆 0 和 1 的表达，而采用不同位数规模来表达同一数值，即规定的数值范围也规约了数值的精度，在模型训练时，通常用采用 FP32、FP16 或 BF16 等浮点格式进行运算，实际表示采用 32 位或 16 位 0 和 1 来表示一个数，位数越多，虽然表示的数值范围和数字精度都有所增加，但实际的资源消耗也非常大，因而，为进一步减少资源消耗，提出了量化方法，所谓量化，就是把这部分原本多位的浮点数值转换为 8 位的 INT8、4 位大的呃 INT4 等更低精度表示，从而减少模型文件和运行时的资源占用。下表展示了浮点数精度对应的相关特点：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col><col></colgroup><tbody><tr><td><p>精度</p></td><td><p>每个参数的理论字节数</p></td><td><p>典型特点</p></td></tr><tr><td><p>FP32</p></td><td><p>4</p></td><td><p>精度较高，资源占用较大</p></td></tr><tr><td><p>FP16／BF16</p></td><td><p>2</p></td><td><p>常见训练和高精度推理格式</p></td></tr><tr><td><p>INT8</p></td><td><p>1</p></td><td><p>权重理论占用约为 16 位的一半</p></td></tr><tr><td><p>INT4</p></td><td><p>0.5</p></td><td><p>权重理论占用约为 16 位的四分之一</p></td></tr></tbody></table>\n</div>\n<p>实际量化不是把所有小数简单取整，而是把一组浮点数映射到有限范围，并保存缩放系数、零点或分组信息。常见做法包括只压缩模型权重、同时量化权重和激活，以及在模型训练完成后进行训练后量化，我们过于不用担心因为量化导致的数值变化，因为最终模型在生成时，是预测每个候选 Token 作为下一个词的概率，在选择时，更多地时关系排序以及经过归一化后的得分，因此量化的精度虽有损失，但是在严格的控制下，可以有效地兼顾模型本身的生成效果。</p>\n\n<p>在进行模型选型时，经常需要判断显存是否够用。模型名称中的 7B、14B 和 70B 通常表示大约 70 亿、140 亿和 700 亿个参数。参数是模型训练中学到的数值，分布在 Attention、前馈网络和 Embedding 等模块中。</p>\n\n<p>参数量不能直接等同于模型文件大小，还要结合每个参数采用的精度。只计算模型权重时，可以使用下面的公式：</p>\n\n<p><span class=\"arithmatex display-math\">\\(权重占用 ≈ 参数量 × 每个参数的字节数\n\\)</span></p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col><col><col></colgroup><tbody><tr><td><p>参数规模</p></td><td><p>FP16／BF16</p></td><td><p>INT8</p></td><td><p>INT4</p></td></tr><tr><td><p>7B</p></td><td><p>约 14GB</p></td><td><p>约 7GB</p></td><td><p>约 3.5GB</p></td></tr><tr><td><p>14B</p></td><td><p>约 28GB</p></td><td><p>约 14GB</p></td><td><p>约 7GB</p></td></tr><tr><td><p>32B</p></td><td><p>约 64GB</p></td><td><p>约 32GB</p></td><td><p>约 16GB</p></td></tr><tr><td><p>70B</p></td><td><p>约 140GB</p></td><td><p>约 70GB</p></td><td><p>约 35GB</p></td></tr></tbody></table>\n</div>\n<p>表中的数字只表示模型权重的理论下限。量化模型还要保存缩放系数等信息，实际推理还需要 KV Cache、中间计算结果、推理框架工作区和并发请求缓存。</p>\n\n<p>例如，14B 模型的 INT4 权重理论上约 7GB，但在 8GB 显卡上几乎没有空间留给 KV Cache 和运行缓冲。短上下文、单请求时可能运行，处理长文档时则容易显存不足。使用 12GB 或 16GB 显存更加宽松。70B INT4 权重约 35GB，一般需要 48GB 以上的显存，在部署时，可以选择更大现存的算力卡、多卡或者使用 CPU 内存进行混合推理。</p>\n\n<p></p>\n\n<h2 id=\"c34-s23\"><b>18.11　领域模型：让通用模型适应专业场景</b></h2>\n\n<p>通用大模型需要覆盖大量知识和任务，在医疗、金融、法律和代码等专业场景中，不一定熟悉行业术语、业务流程和风险边界。领域模型通常是在通用底座模型上，使用专业数据和任务进行继续训练或适配，使模型更加适合某个领域。它不一定从零训练，也不只是通过 Prompt 把模型设定成专业人士。常见训练和适配方式包括：</p>\n\n<p></p>\n\n<ol><li>使用领域文献、法规、代码库等未标注数据继续预训练；</li><li>使用专业问答、信息抽取、报告生成等数据进行领域指令微调；</li><li>使用专家偏好、拒答规则和风险案例进行安全对齐；</li><li>连接专业知识库、数据库和外部工具，提供可以更新和追溯的信息。</li></ol>\n\n<p>这里需要区分领域模型和领域应用。领域模型经过专业数据训练，专业能力已经写入模型参数；领域应用则可以直接使用通用模型，再连接专业知识库、规则和工具。实际系统也经常把两种方式组合起来。</p>\n\n<p>下图中的法律问答系统先提取关键词，再从法规向量库中检索参考条文，最后由法律大模型结合依据生成回答。这说明专业能力不一定只依靠模型参数，外部资料也是领域应用的重要组成部分。</p>\n\n<img src=\"assets/manuscript-20260914/c31-ffa20d77d9ad6c.webp\" alt=\"正文配图\" class=\"article-image\" loading=\"lazy\" decoding=\"async\">\n\n<p>专业数据并不等于专业可靠。医疗模型仍要检查医学证据和安全边界，法律模型要检查法条和引用，金融模型要检查数据时效和计算结果，代码模型则要通过运行和测试验证。医疗、法律和金融等高风险任务还应保留专业人员审批。</p>\n\n<p></p>\n\n<h2 id=\"c34-s24\"><b>18.13　模型幻觉：看似合理但缺少依据</b></h2>\n\n<p>使用模型整理会议记录时，如果原文没有指定负责人，模型却自行补出一个姓名；使用模型抽取合同时，如果原文没有金额，模型却生成了一个具体数字，这些都属于模型幻觉。模型幻觉是指大模型生成了语言流畅、结构完整，却与事实、输入材料或可验证来源不一致的内容。它还可能表现为编造不存在的政策、论文或网址，写错人物和时间，或者把没有形成的讨论写成已经确定的结论。</p>\n\n<p>大模型的核心任务是根据上下文预测下一个 Token，在生成的过程中，模型无法验证每句话是否真实。导致模型幻觉的原因有很多，比如，预训练数据可能存在错误或过时信息；用户问题可能缺少关键条件；长文本中的证据也可能被忽略或截断。后训练如果设计的奖励函数，更加偏向于鼓励完整、流畅的回答，也可能使模型在缺少依据时仍然继续生成。</p>\n\n<h3 id=\"c34-s25\"><b>18.13.1　常见幻觉类型</b></h3>\n\n<p>模型产生的幻觉也多种多样，通常可以分为事实幻觉、应用幻觉、推理幻觉等，详细分类和典型表现如下表所示：</p>\n\n<div class=\"table-scroll\"><table><colgroup><col><col></colgroup><tbody><tr><td><p>类型</p></td><td><p>典型表现</p></td></tr><tr><td><p>事实幻觉</p></td><td><p>人名、日期、数字和事件与真实情况不符</p></td></tr><tr><td><p>引用幻觉</p></td><td><p>编造论文、法条、链接或出处</p></td></tr><tr><td><p>输入幻觉</p></td><td><p>生成原始材料中没有出现的结论和字段</p></td></tr><tr><td><p>推理幻觉</p></td><td><p>中间推导存在错误，但回答表达得很确定</p></td></tr><tr><td><p>工具幻觉</p></td><td><p>声称已经读取文件或调用接口，实际上没有成功</p></td></tr></tbody></table>\n</div>\n<p>除了通用模型外，训练获取的领域模型同样也会产生幻觉。通常，专业数据能够改善领域表现，但不能保证每个答案都准确。知识库检索也不能完全解决所有幻觉问题，如果检索到的材料不相关、已经过期或者本身存在错误，模型仍可能得到错误结论，进一步导致幻觉。</p>\n\n<h3 id=\"c34-s26\"><b>18.13.2　怎样降低幻觉风险</b></h3>\n\n<p>大模型产生幻觉是当前应用过程中比较常见的问题，仅仅在提示词中添加一句“不要编造”或者“请保证回答准确”，通常无法从根本上消除这类风险。模型本质上仍然是在根据上下文预测最可能出现的内容，当输入信息不足、材料存在冲突，或者问题本身超出模型可靠知识范围时，仍然可能生成看起来合理但实际上并不准确的答案。因此，降低幻觉风险更依赖一整套输入约束、外部验证和人工复核机制。</p>\n\n<p>首先，应尽量为模型提供清楚、相关且相互一致的材料，减少无关信息和冲突信息对判断过程的干扰。对于事实、数字、政策条款、实验结果等关键内容，可以要求模型同时标明对应的来源、章节、页码或原文位置，使回答具备可追溯性，方便后续核验。对于新闻、政策、价格、法规、产品参数等更新频繁的知识，则不应只依赖模型内部记忆，而应通过搜索引擎、知识库、数据库、API 或其他外部工具获取最新信息，再让模型基于这些可靠材料进行分析。</p>\n\n<p>其次，当原始材料中确实缺少某项信息时，应明确要求模型使用“待确认”、“原文未提供”、“无法根据现有材料判断”等方式进行标记，而不是根据经验自行补全。对于日期、金额、身份证号、统计数据、公式、代码执行结果等适合结构化检查的内容，还可以引入程序进行自动校验，例如检查数值范围、字段格式、计算结果以及代码是否能够实际运行，从而避免模型仅凭语言生成结果。</p>\n\n<p>此外，对于重要结论，特别是会影响业务决策、项目验收或用户权益的内容，还应保留人工复核环节。语言表达流畅、逻辑看起来完整，并不代表事实一定正确，因此不能把“说得像真的”直接等同于“确实是真的”。在医疗、法律、金融、安全生产等高风险场景中，更应明确模型只是辅助工具，最终结论需要由具备相应资质和经验的专业人员审核和确认。</p>\n\n<p>因此，降低幻觉的核心思路并不是要求模型“永远回答”，而是让模型学会在缺少可靠依据时合理停止。当现有信息不足以支持结论时，最合适的行为可能是明确说明“当前缺少哪些关键材料”“哪些结论暂时无法确认”，并进一步告诉用户需要补充哪些数据、文档或证据。相比继续生成一个貌似完整但缺乏依据的答案，这种做法通常更加可靠，也更适合实际业务应用。</p>\n\n<p></p>\n","imageCount":16,"attachmentCount":0,"discussionId":"chapter-31"},{"id":"chapter-35","number":35,"title":"主流 LLM 评测","sourceUrl":"https://github.com/modelscope/ms-cookbook/blob/main/content/chapters/chapter-35.md","editUrl":"https://github.com/modelscope/ms-cookbook/edit/main/content/source-html/chapter-35.html","minutes":1,"status":"ready","headings":[],"html":"<p>欢迎大家来共建，一起测试现在主流的模型，看看熟强孰弱。</p>\n","imageCount":0,"attachmentCount":0,"discussionId":"chapter-34"}]};
